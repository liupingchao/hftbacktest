# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0828T005

状态：
- 已通过

更新时间：
- 2026-08-28 11:03 CST

验收线程：
- QA验收线程

验收对象：
- SKHYNIX OBI Reversal Track A1/A2 业务线程
- implementation commit：`f0361388`
- business report commit：`468b4585`

验收范围：
- 验收 A0 frozen contracts 和 feature-cache closure。
- 验收 A1 decision-time state ledger、common risk set 和无 future leakage。
- 验收 A2 one-tick midpoint first-passage target、censoring 和允许保存的
  outcome surface。
- 验收 target variation gates、temporal-resolution warning 和
  confirmation-delay warning。
- 验收 tests、determinism、artifact closure、plan 回写和 exact-path git
  scope。

验收步骤：
1. 核对 implementation 和 business-report commit paths。
2. 执行 A0/A1/A2 focused pytest、Python compile、`git diff --check` 和
   Markdown fence parity。
3. 核对 15 个正式 artifacts 的 size/SHA closure。
4. 核对 A1/A2 classification 和全部 gates。
5. 核对 directional entry ledger、common support 和 target counts。
6. 核对 target ledger schema 仅保留 first type/time/censoring。
7. 核对 outcome-access ledger 未拟合 H0/H1、未生成 markout 或 economics。
8. 核对两个 diagnostic warnings 被写入 classification 和 canonical
   plan。

实际结果：
- `f0361388` 仅包含 0828T005 task、canonical plan、A1/A2 runner、
  focused tests 和 15 个 compact artifacts；`468b4585` 仅包含业务回报。
- A0/A1/A2 focused pytest 为 `10 passed`；compile、diff check 和
  Markdown fence parity 通过。
- 两个独立临时构建的 15 个 artifact manifests 完全一致；正式
  15/15 artifact size/SHA closure 通过。
- A1 精确复现 4,021 reversals、3,162 controls；primary common risk set
  为 4,005 reversals、3,159 controls，共 7,164 entries。
- A1 state features 均为 decision-time 信息；post-decision midpoint
  mutation 不改变 state features。
- A2 follow 为 4,247、fail 为 2,917；两类 cause 均覆盖 9 日期且最大
  单日占比低于 0.35。
- Reversal entries 同时包含 follow/fail 2,345/1,660；controls 为
  1,902/1,257。
- Target ledger 字段严格为 identity、entry type、side、decision time、
  event type、cause code、event time、censoring 和 ambiguity；不存在
  future displacement、markout、return 或 PnL。
- First passage 在命中后立即停止；post-first-hit path mutation 测试不
  改变 target。
- 24.46% entries 在 100ms 第一格命中，66.02% 在 500ms 内命中，
  30s 内全部命中，warning 为
  `one_tick_barrier_near_100ms_grid_resolution`。
- 4,005 primary reversals 中 3,702 在 cross-to-detection 期间已经命中
  follow 或 fail，比例 0.9243，warning 为
  `high_pre_detection_transition_fraction`。
- Grid-close ambiguity 为 0；方案明确这不证明 100ms 内不存在双
  barrier 路径。
- Classification 为 `A1_A2_state_and_targets_materialized`，允许进入
  A3；H0/H1 尚未拟合，predictive-value claim 仍被禁止。
- 初次 direct CLI 的 import failure 发生在 target 读取前；最终 artifact
  曾保留 exit displacement 的问题在提交前被删除并由测试封闭。

验收结论：
- 已通过
- 结论说明：
  - A1 state ledger 和 A2 target materialization 已闭合，target variation
    足够进入 A3。该通过不证明 OBI reversal 有预测增量，也不消除极短
    barrier 与高 confirmation-delay 的 actionability 风险。

通过项：
1. Frozen A0 contracts 未被 outcome 反向修改。
2. A1 common risk set 和 decision-time features 可审计。
3. A2 first-passage target 与 outcome-retention boundary 正确。
4. Both-cause/date-support/concentration gates 通过。
5. Pre-detection transition 与 post-decision target 分离。
6. 两个关键 warning 已进入正式 classification。
7. Tests、determinism、artifact closure 和 git scope 通过。

不通过项：
1. 无。

缺陷清单：
1. 无。

阻塞项：
- 无 A1/A2 验收阻塞。
- A3 正结论仍可能被 current state、date、elapsed-time bin 或 generic OBI
  slope 解释。
- Maker actionability 面临高 confirmation-delay 风险。
- 最终确认仍依赖 protocol-frozen prospective sessions。

建议总控下一步：
1. 新建正式任务执行 A3 H0 baseline 和 H1 reversal-indicator incremental
   competing-risk test。
2. A3 使用 frozen role split、date-blocked proper scores 和
   session/date-block uncertainty。
3. 强制报告 first 100ms/500ms bin influence、unadjusted reversal/control
   marginals 和 pre-detection warning。
4. 禁止因 A3 结果修改 barrier、grid、threshold、dwell、alignment 或
   `tau_max`。

提交信息：
- commit：待 QA 事实源提交
