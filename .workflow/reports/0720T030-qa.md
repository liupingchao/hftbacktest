# QA 验收结果

执行线程：
- 独立 QA 验收线程

任务ID：
- 0720T030

状态：
- 已通过

更新时间：
- 2026-07-20 02:03 CST

验收线程：
- 独立 QA 验收线程

验收对象：
- 业务执行线程 / `0720T030`
- implementation commit `ae8468c324750f502c7b137124db032c5efc81d4`
- business/report commit `d645fc393b58dbb52d0392c9a78151db85e2b4df`

验收范围：
- Manager public-event pump 的 per-event demand/ack、stop token、bounded join 和 source close ownership。
- Built-in disconnect、idle L2、blocking read、blocking close、source exhaustion 和 exception 生命周期。
- Public-state mutation 线程所有权和 manager cancel fail-closed 行为。
- T029 estimator replay 四类 hostile contract/quarantine 语义不回归。
- T026、T016、T022 exact offline acceptance/replay 历史边界。
- Implementation/report commit scope、编译、focused/full Hyperliquid regression。

验收步骤：
1. 读取 workflow manual、QA template、T030 task/business 和 T029 QA，核对 action、verify、stop conditions 和 offline boundary。
2. 审查 implementation/report commit diff，沿 request queue、result queue、stop event、pump `finally` close 和 manager cancel 调用链检查竞态与资源所有权。
3. 独立构造首次 fake connect failure，运行完整 manager cycle，返回后额外等待 `0.35s` 检查 connect count 和 pump thread。
4. 独立构造 built-in 单一 L2 recv 后 idle stop，检查 generator/source close、websocket close 和无第二次 recv。
5. 独立运行 3 秒 hold 加 3.6 秒 blocking `next()`，量测首 cancel deadline、两侧 cancel 和 final owned orders。
6. 独立构造阻塞 arbitrary iterator `close()`，检查 close 线程身份、交易线程 cancel timing、单次 read 和释放后的 pump 退出。
7. 记录 `EventDrivenPublicState.observe()` 线程 identity，并验证 exhaustion、disconnect、source exception 均 fail closed 且 manager cancel 继续。
8. 运行 T029 四类 replay hostile tests、`py_compile`、focused tests 和完整 Hyperliquid regression。
9. 精确离线 replay T026、T016、T022，并分别运行 estimator CLI 核对 contract/exposure/quarantine。

实际结果：
- Code review 未发现 P0、P1 或 P2。
- Per-event demand/ack 成立：
  - Pump 每次 `next(source_iterator)` 前必须消费一个交易线程 request token。
  - 发布一个 result 后 pump 阻塞等待下一 token，不会自行 prefetch。
  - Disconnect、exhaustion、exception 和 deadline 后交易线程不再发送 read demand。
- Built-in connect-failure hostile probe：
  - 首次 fake connect failure 后完整 manager cycle fail closed。
  - Cycle 返回并额外等待 `0.35s` 后 connect count 仍为 `1`。
  - `pump_stop_acknowledged=true`，无 `manager-hold-public-event-pump` thread。
  - `cancel_count=2`，final owned orders 为空。
- Built-in normal L2 idle-stop probe：
  - `recv_calls=1`，没有第二次 recv。
  - `pump_source_closed=true`，generator close 完成，fake websocket `closed=true`。
  - `pump_stop_acknowledged=true`，无残留 pump thread。
- Exact blocking-read deadline probe：
  - `quote_hold_seconds=3`，单次 `next()` 阻塞 `3.6s`。
  - 首 cancel 比 hold deadline 晚 `0.050212s`，小于 `0.25s` 上限。
  - `cancel_count=2`，final owned orders 为空；blocking read 返回后 pump 退出。
- Arbitrary blocking-close probe：
  - 完整 cycle 在约 `0.189329s` 返回，首 cancel 比 `0.1s` hold deadline 晚 `0.050208s`。
  - `close()` 的 thread name 为 `manager-hold-public-event-pump`，thread id 与交易线程不同。
  - Close 阻塞期间交易线程已完成两次 cancel 和空终态；source `next_calls=1`。
  - 释放 close blocker 后 pump 退出，没有第二次 read。
- Public-state ownership 和 fail-closed matrix：
  - 所有 `state.observe()` 调用 thread id 均等于交易/调用线程 id。
  - Source exhaustion 返回 `manager_hold_public_source_exhausted`，完整 manager cycle 仍完成两次 cancel 和空终态。
  - Disconnect 返回 `manager_hold_public_source_disconnect`。
  - Source exception 被 manager 记录为 `manager_hold_observer_failed:...`，仍完成两次 cancel 和空终态。
- Estimator replay implementation/test blob 在 implementation commit 前后完全相同：
  - implementation blob `cda2e357bba8116982c8271fb5fb2b7523e7b2f0`
  - test blob `26bb7799ad30fa94fc527dc470c1559daf47ab45`
- T029 四类 replay hostile tests：`4 passed`。
  - Invalid interval quarantine 继续使 snapshot/CLI fail closed。
  - Present-empty contract 继续排除 forged confirmed exposure。
  - Invalid schema 继续 fail closed。
  - Contract absent v1 replay 继续 exact pass。
- `py_compile`：通过。
- Focused watcher/estimator modules：`120 passed in 18.66s`。
- 独立 `/tmp` hostile probes：`5 passed in 4.60s`。
- Full Hyperliquid regression：`984 passed in 47.32s`。
- Exact T026 acceptance exit `0`：
  - provenance `112/112`
  - config `72/72`
  - decision `43/43`
  - lifecycle `71/71`
  - economics `6/6`
  - estimator contract absent，snapshot exact，confirmed interval/persisted/rebuilt/quarantine 为 `0/0/0/0`
- Exact T016 acceptance exit `2`：
  - provenance `112/112`
  - config `72/72`
  - decision `43/43`
  - lifecycle `59 pass / 12 fail`
  - economics `6/6`
  - estimator contract absent，snapshot exact，confirmed interval/persisted/rebuilt/quarantine 为 `0/0/0/0`
- Exact T022 acceptance exit `2`，计数和 estimator 结果与 T016 相同；历史 terminal blocker 未升级。
- 三份 historical acceptance boundary 均为 `offline_only=true`；network/private/order/cancel/remote/new-live 全部 false。
- Implementation commit 只修改 watcher implementation 和对应 test；report commit 只修改 task/business/tracker 文件。
- 本次 QA 未触发 live、private、account、order、cancel、network、remote 或 service 操作。

验收结论：
- 已通过
- 结论说明：
  - T029 的 post-disconnect prefetch/reconnect P2 已关闭；pump 现在严格按交易线程逐事件 demand 读取，idle stop 可确认关闭 built-in generator/websocket。
  - Blocking read 或 arbitrary blocking close 均不能拖延交易线程进入 cancel，且返回后不会 publish 或发起第二次 read。
  - Estimator replay、历史 acceptance 和 activation-off 边界保持不变，T030 可解锁后续独立派发的 Task 8 observe-only tiny-live。

通过项：
1. Per-event demand/ack 和 explicit stop token 通过。
2. Built-in disconnect 后无 reconnect、无残留 pump 通过。
3. Built-in 单次 recv、generator/websocket close 和 bounded idle acknowledgement 通过。
4. 3 秒 hold 加 3.6 秒 blocking read cancel deadline 通过。
5. Arbitrary blocking close 的 pump ownership 和 cancel isolation 通过。
6. Public-state mutation 交易线程所有权通过。
7. Exhaustion、disconnect、exception fail-closed 与 cancel continuation 通过。
8. Estimator replay 文件不变和四类 hostile replay 回归通过。
9. T026 零 exposure 与 T016/T022 historical blocker 保持。
10. 编译、focused/full regression、commit scope 和 offline boundary 通过。

不通过项：
1. 无

缺陷清单：
1. 无

阻塞项：
- 无

建议总控下一步：
1. 将 `0720T030` 更新为 `已通过`。
2. 可按计划独立派发 Task 8 observe-only tiny-live；dynamic spread、fill feedback、inventory skew、multi-level 和 actual quote behavior activation 继续保持关闭。
3. 保留当前 explicit boundary：任意第三方阻塞 read/close 可暂时保留 daemon pump，但不得控制 cancel timing、publish stop 后结果或发起后续 read；built-in source 必须继续保持 `0.25s` read bound。

提交信息：
- commit：无
- QA 未提交代码或报告 commit。
