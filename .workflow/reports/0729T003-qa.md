# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0729T003

状态：
- 未通过

更新时间：
- 2026-07-29 12:47 CST

验收线程：
- QA验收线程

验收对象：
- 业务线程-remote-public-data `0729T003`
- c6in winner `i-0a962e47210528526`
- local package
  `local_live_analysis/cross_exchange_public_sample_skhynix_0729T003_2h_20260729T022724Z/`

验收范围：
- 独立复核 2 小时 Binance `SKHYNIXUSDT` / Hyperliquid `xyz:SKHX`
  public collection 的运行稳定性。
- 复核 Hyperliquid `l2Book fast=true` 的 ACK、密度和 L2 结构。
- 复核远端归档、本地回拉、SHA256、gzip、JSON 和 manifest counts。
- 复核 Binance REST snapshot 与 diff-depth stream 是否满足订单簿启动
  bridge，判断是否可以声称 exact dual-book replay。
- QA 只读，不执行 AWS mutation、不访问私有/account/order/cancel
  endpoint，也不修改策略或采集器代码。

验收步骤：
1. 阅读 task、business report、controller docs 和本地 check summary。
2. 检查双方 collection/run/sample manifests、raw hashes 和远端逐文件
   SHA manifest。
3. 复核 Hyperliquid subscription ACK、120 个完整分钟的 L2 密度和每条
   L2 双边结构。
4. 复核 Binance snapshot `lastUpdateId`、首条 `U/u/pu` 和后续
   `pu == previous u` 连续性。
5. 按任务声明的完整订单簿复现目标给出 fail-closed 裁决。

实际结果：
- 2 小时运行层通过：
  - Binance duration `7200.081912776s`
  - Hyperliquid duration `7200.042246051s`
  - effective overlap `7200.037246452s`
  - 双方 connection attempts `1`
  - 双方 reconnect `0`
  - 双方 disconnect events `0`
- Binance raw:
  - total lines `3,873,345`
  - bookTicker `2,003,786`
  - depthUpdate `255,775`
  - trade `1,613,783`
  - JSON errors `0`
  - non-monotonic local timestamps `0`
  - invalid depth shapes `0`
  - symbol mismatches `0`
  - raw SHA and manifest counts match
- Hyperliquid raw:
  - total lines `55,151`
  - fast L2 `13,265`
  - trades `41,645`
  - pongs `239`
  - subscription responses `2`
  - JSON errors `0`
  - non-monotonic local timestamps `0`
  - invalid L2 shapes `0`
  - wrong coin rows `0`
  - raw SHA and manifest counts match
- Hyperliquid fast L2 通过：
  - ACK contains
    `{"coin":"xyz:SKHX","fast":true,"type":"l2Book"}`
  - 120 full-minute min/p50/p99/max: `108 / 111 / 112 / 112`
  - overall rate `110.541/min`
  - L2 gap p50/p99/max:
    `540.059 / 695.511 / 1945.517 ms`
  - all L2 rows contain two non-empty sides
- Transfer/integrity 通过：
  - remote/local tar SHA:
    `5f116f9466815322257c6244074de98d7151d12330a49e00146d48e74a0a0cc2`
  - remote per-file SHA verification `14/14`
  - both gzip files pass integrity checks
- P1 Binance startup replay defect:
  - snapshot `lastUpdateId=11158119028898`
  - first depth event:
    `U=11158119040568 / u=11158119042645 / pu=11158119040354`
  - `U <= lastUpdateId <= u` is false
  - startup gap is `11,670` update IDs / about `142.727ms`
  - all `255,775` captured depth events are continuous from the first event,
    but the missing initial bridge prevents reconstruction of the book state
    at capture start
- Post-run operational state:
  - collection unit `inactive/dead`, `Result=success`, exit `0`
  - temporary SSH key removed
  - c6in winner remains running
  - `DisableApiTermination=true`
  - remote archive remains available as backup

验收结论：
- 未通过
- 结论说明：
  - The two-hour public collection, Hyperliquid fast L2 and local transfer
    integrity are valid, but Binance cannot reconstruct its initial order book
    because the REST snapshot does not bridge to the captured diff stream.
  - This sample cannot be accepted or described as exact Binance replay or
    exact Binance+Hyperliquid dual-book replay.

通过项：
1. Two-hour dual-venue collection duration and overlap.
2. Zero reconnect/disconnect runtime stability.
3. Hyperliquid `fast=true` ACK, density and L2 structure.
4. Local transfer, archive/file/raw hashes, gzip and JSON parsing.
5. Manifest counts and local timestamps.
6. Binance stream continuity after the first captured depth event.
7. Instance retention, termination protection and temporary-key cleanup.

不通过项：
1. Binance startup snapshot does not bridge into the captured diff-depth
   sequence.
2. Exact Binance and exact dual-book replay claims are unsupported.

缺陷清单：
1. **P0：无。**
2. **P1：Binance snapshot/bootstrap ordering loses the bridge before the
   collector begins draining buffered WebSocket depth events.**
3. **P2：无。**
4. **P3：无。**

阻塞项：
- Exact Binance L2 reconstruction from the beginning of this sample is
  blocked.

建议总控下一步：
1. Create a narrow offline collector repair task.
2. Buffer Binance depth events concurrently while fetching the REST snapshot.
3. Accept only when `U <= lastUpdateId <= u`, then require every subsequent
   `pu == previous u`.
4. Fail closed and refetch/reconnect when no bridge exists.
5. Add focused bootstrap-gap and replay reconstruction tests before another
   paid 2H/8H collection.
6. Retain this package for raw-feed stability, Hyperliquid replay and defect
   regression evidence; do not delete or promote it as dual-book replay data.

提交信息：
- commit：无
