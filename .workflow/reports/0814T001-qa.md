# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0814T001

状态：
- 未通过

更新时间：
- 2026-08-15 10:55 CST（星期六）

验收线程：
- QA验收线程

验收对象：
- 业务线程-python/research + 0814T001

验收范围：
- 四个 session 的 input identity、manifest/hash、采集拓扑和研究执行
  provenance。
- Binance `@trade q`、no-`nq`、RPI unavailable、`depth@0ms`，KRX
  unknown-state fail-closed，以及 Hyperliquid cadence 缺失值纪律。
- Family A/B、Candidate/Confirmed landmarks、逐 feature `observed_at`、
  interval censoring、outcome、CRPS/Brier/interval-log-loss 和 A0/B0
  freeze。
- 两次完整构建 determinism、源 inventory 不变、Aug07 first-read
  边界和 atomic/fail-closed hostile behavior。

验收步骤：
1. 阅读 workflow、任务、业务报告、研究方案、实现、测试和正式产物。
2. 独立复算 artifact closure、manifest/hash binding、topology
   fingerprint、source inventory 和 Aug07 access ledger。
3. 运行聚焦测试、compileall、Ruff、CLI verify-only 和两次独立完整构建。
4. 注入 cadence 补造、Aug07 compact event-row、topology fingerprint
   伪造和合同语义漂移 hostile cases。

实际结果：
- 正式包 `13` 个 manifest-tracked artifacts 全部闭合；核心 SHA256 为
  `4267c9506499082bef157991757899601d66741a665345fe75522adf27122752`。
- QA 两次独立完整构建与正式包的全部 `14` 个文件路径、bytes 和 SHA256
  完全一致。
- 三次构建均记录源 inventory 为 `961` files、`2,835,271,526` bytes，
  SHA256 为
  `fc94f6d3dd8b0d1527c03dae839201ca494807264ecfdaa1218ecdb24aaff3a0`，
  before/after 相同。
- Jul30/Aug03/Aug04 的允许 raw rows 通过结构化扫描；历史 Binance
  stream 为 `@trade + @depth@0ms + @bookTicker`，trade `q` 完整，
  `nq` 为零，RPI 标记不可用。
- 四个 session 的正式 KRX state 均在缺少权威 calendar 时保持
  `unknown_calendar_state`；未来价格推断标记为 false。
- 当前正式包和两次 QA 构建的 Aug07 ledger 均为
  `event_rows_opened=false`，每次只 content-read `4` 个允许 manifest，
  stat `39` 个 raw paths，content-read 路径中没有 `raw.gz` 或
  `*.csv.gz`。
- 聚焦测试通过：`22 passed`；compileall、Ruff、CLI verify-only 和
  `git diff --check` 通过。
- 但是四个独立 hostile cases 均输出 `ACCEPTED_FAIL_OPEN`：
  fabricated cadence missing values、Aug07 compact full-event path、
  forged collection topology fingerprint、Family/landmark/interval
  contract drift。

验收结论：
- 未通过
- 结论说明：
  - 当前正式产物本身可复现且未越过 Aug07 边界，但关键 validator
    不能对任务明确要求的伪造/补造/语义漂移 fail closed，不能作为后续
    Episode v3 和 Aug07 first-read 的冻结门禁。

通过项：
1. 四 session input inventory、manifest SHA 和当前产物 closure。
2. 当前 topology payload 的 fingerprint 可独立复算，采集 topology 与
   research execution host 分离记录。
3. 历史 Binance `q`/no-`nq`/RPI unavailable/`depth@0ms` 测量边界。
4. 当前 KRX unknown-state、Family A/B、landmark、observed-at、
   interval-censoring、scoring 和 A0/B0 内容。
5. 两次独立完整构建的 `14/14` 文件 determinism。
6. 源 inventory before/after 不变。
7. 当前 Aug07 完整事件 rows 未被打开。
8. atomic publication 的现有 failure tests。

不通过项：
1. Aug07 first-read policy 对 compact 根目录无条件放行；若 compact
   中出现 `decision_labels.csv.gz`、`basis_features.csv.gz` 或其他完整
   event artifact，policy 会允许读取，ledger 仍可自报
   `event_rows_opened=false`。
2. cadence validator 不核对 `missing_fields` 所列列值必须为空；
   partial/unavailable row 可填入伪造 quantile 后通过。
3. topology validator 只检查 fingerprint 是 64 位 SHA 格式，不从
   canonical acquisition facts 重算；任意合法格式的伪造 fingerprint
   可通过。
4. frozen-contract validator 未 exact-freeze Family B decision landmark、
   rejected landmark nullability、interval point-coercion 等关键语义；
   同 schema 漂移可通过。

缺陷清单：
1. P0：无。
2. P1：Aug07 compact event-row guard fail-open，违反 first-read hard
   boundary。
3. P1：Hyperliquid cadence unavailable/partial 值可被静默补造。
4. P1：collection topology fingerprint 未按绑定事实重算验证。
5. P1：Family/landmark/interval 等冻结合同只做部分语义校验。
6. P2：无。
7. P3：无。

阻塞项：
- 无外部阻塞；需要业务线程修复上述 P1 后重新验收。

建议总控下一步：
1. 保持 `0814T001` 为 `未通过`，不得解锁 trigger density、detector
   parity、Episode v3、outcome/model/actionability 或 Aug07 first-read。
2. Aug07 policy 应不分 raw/compact 根目录拒绝所有 full-event
   文件名/后缀，并将 compact inventory 改为 metadata allowlist。
3. cadence validator 应解析 `missing_fields` 并断言对应值为空；
   unavailable row 的全部 metric 值必须为空。
4. topology validator 应保存或重建完整 canonical payload，并 exact
   recompute fingerprint；同时要求精确 session cardinality。
5. frozen-contract validator 应 exact-freeze Family A/B、landmarks、
   observation contract、outcomes、censoring、hypotheses、完整 scoring
   公式和 A0/B0，并补齐上述四个 hostile tests。
6. 修复后重新生成正式包、重跑双构建 determinism，再派发同任务 re-QA。

提交信息：
- commit：无
