# QA 验收结果

## Findings

1. **P1 - Buy submitted reference lacks authoritative target-bound terminal proof.**
   - Attempt 1 buy `0.005 BTC @ 65729.0` received an exact reference-bound `resting` response.
   - Its cancel response was only the generic error `Order was never placed, already canceled, or filled` and was correctly recorded as `cancel_unknown`.
   - The bounded fallback completed `5` oid/cloid direct rounds (`10` direct queries) plus `1` historical-orders query in `1.188220s`; every result remained `unknown`.
   - Final account open orders `0`, BTC position `0.0`, and observed fills `0` prove current account safety, but do not distinguish canceled, filled, rejected-after-resting, or another terminal path for this exact reference.
   - Account-wide absence therefore cannot replace per-reference terminal proof. The no-fill lifecycle remains `no_fill_unproven / fail_closed`, which violates the task stop condition requiring every submitted attempt to have authoritative terminal evidence.

2. **P2 - Producer/replay and Task 12 independent rebuild disagree on the leading partial bucket censor contract.**
   - The buy confirmed interval starts at local receive time `1784571457887`.
   - The first in-interval local receive batch at `1784571458374` contains a book with exchange time `1784571457626` and trades through `1784571458029`.
   - The effective event-time coverage starts at `1784571457888`; bucket `[1784571457888, 1784571458000)` has no eligible reference book. The first eligible post-start book is at exchange time `1784571458168`.
   - Producer and estimator replay persist `bucket_reference_book_missing` for bucket `1784571457000`; Task 12 independent rebuild silently skips the same prefix. Both produce the same `6` exposure rows, but quarantine reasons differ (`1` versus `0`).
   - This leading prefix is left-censored before the first usable reference book, not evidence that should be reconstructed from the delayed pre-interval book. It should be explicitly censored/skipped by both implementations, while malformed, out-of-order, future, or non-leading missing-reference cases remain quarantined.

3. **P0 findings: none.**

执行线程：
- 独立 QA 验收线程

任务ID：
- 0720T031

状态：
- 未通过

更新时间：
- 2026-07-20 18:35 UTC

验收线程：
- 独立 QA 验收线程

验收对象：
- 总控 auto-loop / 业务执行线程 / live-awsserver1
- exact implementation/live source commit `83238c104163813ed5c89a8f71ed8be561d17b1e`
- business/report commit `62e9e60876a5f394300c8665eb566d11c4b63dcd`

验收范围：
- Exact source、唯一 window、task envelope、runtime provenance、terminal checksum 和 remote/local evidence equality。
- Child/reap/termination、pre/post account/service proof、kill-switch 和 activation-off 边界。
- 两个 submitted attempts 的 raw response、manager action、cancel、direct/history query、fill 和账户终态重建。
- Confirmed manager intervals、raw public event rows、event-time exposure、A/k fit、dynamic candidate 和 producer/replay/Task 12 rebuild 一致性。
- Exact estimator replay、same-window acceptance、focused regression 和完全 offline 边界。
- 不支持 economics、promotion、multi-level 或任何 adaptive activation claim。

验收步骤：
1. 读取 workflow manual、QA template、T031 task/business 和 T030 QA，核对 verify、stop conditions、single-window 和 offline QA 边界。
2. 独立检查 task/source/window/envelope、runner command、runtime source provenance、source start/postrun verification、run lifecycle 和 terminal checksum。
3. 从 order intent、private raw response、manager actions、cancel proof、terminal query audit、fills 和 account proofs 重建两个 attempt 的生命周期。
4. 从 raw response 和 cancel-request timestamps 独立重建 confirmed resting interval contract。
5. 从 `551` 条 raw event rows 分别调用 producer 和独立 Task 12 rebuild，比较 exposure rows、quarantine 和首个 partial bucket。
6. 独立重建 exposure/A-k/fallback 状态，核对 activation-off 和 fixed authoritative half-spread。
7. 在 `/tmp` 重跑 exact estimator CLI replay 和 exact same-window acceptance，不修改 immutable evidence root。
8. 运行 estimator/acceptance focused tests、`py_compile`、Git source hash、archive hash、manifest checksum、diff/status 和 offline boundary 检查。

实际结果：
- Exact source/provenance：
  - Local `git archive` SHA-256 为 `cae96401ea5a6705845b28e57c62daa07e890c9957f381dfc7ab9f3764229e4b`，与业务报告记录一致。
  - Runtime provenance 的 `62/62` non-test Hyperliquid Python files 与 exact source commit Git blobs 独立逐文件匹配，mismatch `0`。
  - Pre-watcher start 和 postrun source verification 均为 `pass`；watcher、estimator 和 acceptance 当前文件 hashes 与 exact source commit 一致。
  - Immutable evidence 未单列 remote transfer archive digest 字段，但 remote runtime exact-byte proof 已独立成立。
- 唯一 window/envelope：
  - `run_status.completed_windows=["01"]`，仅存在一个 `window_status.json`，没有 `window_02/window_03`。
  - Exact profile `two-sided-manager`；BTC、Binance public lead、Hyperliquid `Alo`、`0.005 BTC/order`、`0.01 BTC` position、`1 USDC` loss、`2` submissions、`2` requotes、`3s` hold、`10s` wait、external duration `1800s` 全部匹配。
  - Actual watcher elapsed `86.060554s`，submissions `2`，未启动第二个 window。
- Process/account/evidence seal：
  - Child return code `0`、reaped `true`、termination requested `false`、SIGKILL `false`，open-orders proof 在 child exit 后执行。
  - Remote terminal SHA-256 manifest 本地重验 `106/106`，missing/mismatch `0/0`。
  - Pre/post account-service proof 内容完全一致：open orders `0`、BTC position `0.0`、kill-switch clear、conflicting service inactive、conflicting process count `0`。
  - Independent after-child open orders `0`；estimated loss `0.0`。
- Attempt lifecycle independent rebuild：
  - Attempt 1 buy 和 attempt 2 sell 均有 distinct intent/attempt/reference，均调用真实 order endpoint 并收到 `resting`。
  - Sell cancel 是 exact authoritative `success`，reference status `pass`。
  - Buy cancel 是 generic ambiguous error；`10` direct queries 和 `1` history fallback 均 unknown，reference status `fail_closed`。
  - Fill ledger、fill attribution 和 user pullback均为 `0` fills；final open orders `0`，post BTC position `0.0`。
  - Generic cancel error加 account-wide empty orders只能证明当前账户无遗留挂单，不能替代 buy target-bound terminal proof。
- Confirmed intervals/exposure：
  - Raw submit/cancel reconstruction reasons `[]`，与 persisted interval contract exact-match。
  - Two intervals均为 pass：buy `3.526s`，sell `3.749s`，reconnect/disconnect counters均保持 `0`。
  - Producer和独立 rebuild exposure rows均为 `6` 且 projection exact-match；总 positive duration `5.028s`。
  - Buy `3` rows / `2.774s` / `6` arrivals / `0.06709 BTC`；sell `3` rows / `2.254s` / `0` arrivals。
  - Buy intensity fit `pass`：`A=9936.40915821`、`k=2.50532025`、`3` observations。
  - Sell fit `unavailable`：`insufficient_distance_variation`。
  - Dynamic candidate `fallback_fixed`；fixed/authoritative half-spread均为 `0.5` ticks。
- Partial-bucket contract：
  - Producer/replay quarantine：`1 x bucket_reference_book_missing`。
  - Task 12 independent rebuild quarantine：`0`。
  - Exposure rows仍 exact `6/6`，说明差异只在 leading left-censor/quarantine contract，不在已生成 exposure 数值。
  - Producer/replay source snapshot SHA 和 replay snapshot SHA 都是 `ff80b097a50ac4e69276836c311792745a1bb60e130f68e455febe4f0cdbf5c6`，但 fail-closed manifest 因非空 quarantine 返回 `snapshot_match=false`。
- Activation/economics boundary：
  - Preflight明确 dynamic spread、fill feedback、inventory skew、multi-level 和 actual quote behavior change 全部为 false。
  - Live watcher/status/replay/acceptance继续证明 dynamic spread、fill feedback、multi-level 和 quote behavior activation关闭；runner command无 adaptive activation flag。
  - 无第二个 window、无 flatten、无 taker/crossing、无 adaptive 或 multi-level activation。
  - Supported claims为空；stable PnL、fill-rate、fee/rebate calibration、queue priority、maker viability、multi-level、promotion 和 final MVP pass均不支持。
- Exact offline rerun：
  - Estimator CLI exit `1`：interval `2`，persisted/rebuilt exposure `6/6`，exposure match `true`，quarantine `1`，snapshot match `false`。
  - Same-window acceptance exit `2`：
    - provenance `112/112 pass`
    - config `72/72 pass`
    - decision `43/43 pass`
    - lifecycle/evidence `57 pass / 14 fail`
    - economics `6/6 pass`
    - optimism `6/6 pass`
    - final recommendation `principal_task12_same_window_acceptance_blocked`
  - `14` failures归并为两个根问题：buy per-reference terminal proof缺失，以及 persisted quarantine与independent rebuild不一致。
- Regression：
  - Estimator + same-window acceptance focused tests：`220 passed in 8.16s`。
  - `py_compile`：通过。
  - 本次 QA 全程未执行 network、private/account、order、cancel、remote、service 或 new-live 操作。

验收结论：
- 未通过
- 结论说明：
  - T031 的 exact source、single-window、envelope、process cleanup、terminal account safety、confirmed intervals、6-row exposure、activation-off 和 evidence seal均成立。
  - 但 buy submitted reference仍为 bounded unknown，违反逐引用 authoritative terminal proof要求；同时 estimator leading partial bucket 的 producer/replay 与独立 acceptance 语义不一致。
  - 因存在 P1 和 P2，当前 immutable window不能完成 Task 8 evidence gate，也不能解锁 dynamic spread 或 Principal Task 9-12。

通过项：
1. Exact source commit、62-file runtime provenance和local archive hash通过。
2. 唯一 window、exact envelope、two-sided real resting endpoint coverage通过。
3. Child exit/reap、无 termination/SIGKILL和after-child proof顺序通过。
4. Remote/local terminal checksum `106/106`通过。
5. Pre/post account/service、kill-switch、open orders、position和loss安全通过。
6. Two confirmed resting intervals及其raw重建通过。
7. Producer/replay的6-row exposure、A/k和fallback-fixed数值复现通过。
8. Dynamic/fill-feedback/inventory-skew/multi-level activation-off和quote behavior unchanged通过。
9. No-second-window、offline QA和unsupported economics/promotion boundary通过。
10. Focused tests和编译通过。

不通过项：
1. P1：buy attempt缺少authoritative target-bound terminal proof。
2. P2：leading partial bucket的producer/replay quarantine与Task 12 independent skip语义不一致。

缺陷清单：
1. `cancel_unknown`不能由账户级最终空单替代为逐引用terminal success。
2. `bucket_reference_book_missing`对leading left-censored prefix没有统一、显式且可独立重建的合同。

阻塞项：
- T031 Task 8 evidence gate未完成。
- Dynamic spread activation、fill feedback activation、inventory skew activation、multi-level activation、economics calibration和promotion继续锁定。

建议总控下一步：
1. 将 `0720T031` 更新为 `未通过`，保留 immutable evidence，不在本任务启动第二个 window。
2. 单独派发 offline repair：统一 producer/replay/acceptance 的 leading-prefix left-censor合同；显式记录 censor reason/count，禁止静默 skip；interior missing reference、invalid/out-of-order/future evidence继续 quarantine。
3. 为 generic cancel error增加reference-bound terminal获取/持久化的离线协议与 hostile tests；不得把 account-wide empty orders升级为该reference的terminal proof。
4. 上述offline repair经独立QA后，另行派发一个新的bounded single-window任务；继续保持fixed quote和所有adaptive/multi-level activation关闭。

提交信息：
- commit：无
- QA 未提交代码或报告 commit。
