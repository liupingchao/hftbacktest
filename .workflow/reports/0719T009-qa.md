# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0719T009

状态：
- 未通过

更新时间：
- 2026-07-19 23:59 CST

验收线程：
- QA验收线程

验收对象：
- implementation commit `6fd4089310a5dbf9694c977808081494ec63e3ac`
- business report commit `2ea756407579246e28ddca8b55c902feacb8c57c`

验收范围：
- fill 的 symbol、side、side-aware limit 语义。
- terminal SHA-256 manifest 独立验证。
- fresh manager-watcher zero-fill fixture。
- T005-T008 相关离线回归及策略、风险、activation 边界。

验收步骤：
1. 阅读工作流、任务、业务报告、T008 QA 和两个提交的完整差异。
2. 审阅 producer direct-reference attribution、raw pullback 重建、full-fill terminal proof 和独立 manifest verifier。
3. 运行 focused/full regression，并在 `/tmp` 构造同步、重新 seal 的独立对抗 fixture。

## Findings

1. P1：`Close Long`/`Close Short` 的真实成交方向可被反向解释，并成为 full-fill terminal proof。
   - Producer 和 acceptance 使用包含 `long` 即 buy、包含 `short` 即 sell 的推断。
   - 按 Hyperliquid `dir` 语义，`Close Long` 是卖出，`Close Short` 是买入。
   - 独立反例删除 raw fill 的 `side`，将 buy fill 写为 `dir="Close Long"`，保持其余派生证据同步并重新 seal。
   - Producer 仍生成 1 条 buy attribution；完整 acceptance 返回 `principal_task12_mechanism_and_evidence_integrity_passed`。
   - 违反任务要求“fill side 必须等于 canonical attempt side”。
2. P2：manifest 空白行未按 malformed line fail closed。
   - Independent verifier 对空白行直接跳过。
   - 在已 seal manifest 末尾追加空白行后，verifier 返回 `status=pass, reasons=[]`，完整 acceptance 仍 pass。
   - 未满足任务明确要求的“reject malformed lines”。

实际结果：
- Exact/better buy 与 sell 限价正常通过：
  - buy `63999 <= 64000`：pass。
  - sell `66001 >= 66000`：pass。
- Buy-above-limit、sell-below-limit 的现有 producer/acceptance 回归通过。
- Fresh manager-watcher 生成两次 mock submit、两次 mock cancel、零 fill，完整 acceptance：`1 passed`。
- Acceptance 未导入 producer 或 orchestrator verification helper。
- 普通 post-seal artifact 字节变异、digest mismatch、duplicate、traversal、missing 和 unexpected file 均能被阻断。
- 未发现策略公式、风险上限或 activation 边界变更。
- 未执行 live、网络、远端、账户、private、order、cancel 或 service 操作。
- 仓库未修改。

命令与结果：
- Fill-attribution + acceptance focused：`195 passed in 2.67s`。
- Fresh zero-fill fixture：`1 passed in 0.18s`。
- Orchestrator/event-watcher focused：`27 passed in 1.56s`。
- Full `examples/hyperliquid`：`666 passed in 34.72s`。
- Modified Python `py_compile`、两个 CLI `--help`、implementation range `git diff --check`：pass。
- `/tmp` 独立 `Close Long` 同步重放：
  - producer attributed count：`1`
  - acceptance：pass，复现 P1。
- `/tmp` manifest 空白行重放：
  - independent verifier：pass
  - acceptance：pass，复现 P2。

验收结论：
- 未通过
- 结论说明：
  - 限价与普通 terminal artifact 字节校验主体修复成立，但 side 语义仍可伪造 terminal proof，且 malformed manifest 行未完全 fail closed。

通过项：
1. Buy/sell side-aware limit 比较及正负价格回归。
2. Raw pullback 和 full-fill terminal 的价格复核。
3. 当前 run-root 普通文件的独立 SHA-256 重算。
4. Fresh manager-watcher zero-fill fixture。
5. Full Hyperliquid `666 passed`。
6. 无策略、风险、activation 或 live 边界扩展。

不通过项：
1. `Close Long`/`Close Short` fallback side 解释错误。
2. 空白 manifest line 未被判定 malformed。

缺陷清单：
1. P1：错误 `dir` 方向可成为 full-fill terminal proof。
2. P2：空白 manifest line fail-open。

阻塞项：
- 0719T009 不能关闭。
- 新 tiny-live、Principal Task 12 closure 和 Task 10 unlock 继续锁定。

建议总控下一步：
1. 明确映射 `Open Long`/`Close Short` 为 buy，`Open Short`/`Close Long` 为 sell；未知或冲突值 fail closed。
2. Producer 与 acceptance 独立实现该映射，并增加同步、重新 seal 的四种 `dir` 回归。
3. Manifest verifier 对任何非标准非空或空白记录 fail closed，并增加 blank-line regression。

提交信息：
- QA commit：无
- 文件修改：无
