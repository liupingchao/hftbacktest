# QA 验收结果

## Findings

1. **P1：部分 malformed position rows 仍被语义解析为有效快照。**
   - [hyperliquid_tiny_live_real_order_executor.py](/home/molly/project/hftbacktest/examples/hyperliquid/hyperliquid_tiny_live_real_order_executor.py:892) 将缺少 `coin` 的 `{}` 或 `{"position": {}}` 当作 foreign row 跳过，最终返回 verified `0.0`。
   - 同一解析器通过 `float(True)` 将 BTC `szi=true` 接受为 `1.0`。
   - [hyperliquid_maker_order_manager.py](/home/molly/project/hftbacktest/examples/hyperliquid/hyperliquid_maker_order_manager.py:573) 因而返回 `position_snapshot_status=pass`。
   - Finalizer 随后输出：
     - `position_snapshot_status=pass`
     - 缧少 `final_position_snapshot_unavailable`
     - `{}`/empty position row 显示 verified `0.0`
     - boolean BTC position 显示 verified `1.0`
     - root、exposure、risk 均同步显示该数值
   - 已知 final open orders 和 `0.01 BTC` working exposure仍正确，但 position evidence 被错误标记为可信，违反 T020 对 malformed list rows 和 invalid BTC position 的 fail-closed 要求。
   - 无 P0、无其他 P1、无 P2。

执行线程：
- 独立只读 QA 验收线程

任务ID：
- `0720T020`

状态：
- **未通过**

更新时间：
- `2026-07-20 12:27 CST`

验收对象：
- implementation commit `9a332c13e3a00d496e50c9ff73bc77b1962cec96`
- business report commit `1f05af958a76a55b7d6205c33dbccf4d160c570e`

验收范围：
- Final position semantic validity、operator status、working-order independence、T017-T019 回归、T016 monotonic replay及策略漂移。
- 未执行 live、private/account endpoint、真实 order/cancel、network、remote 或 service 操作。

关键命令与结果：
- Focused manager/watcher/fill/acceptance：`377 passed in 16.49s`。
- Full Hyperliquid：`771 passed in 36.83s`。
- Malformed/classifier/query-filled/cancel/foreign/cardinality targeted：`57 passed, 211 deselected`。
- `py_compile` 4 个修改文件：通过。
- Acceptance CLI `--help`、implementation `git show --check`、implementation diff check：通过。
- 对抗矩阵发现失败输入：`assetPositions=[{}]`、`assetPositions=[{"position": {}}]`、BTC `szi=true`。
- Dirty worktree内容与验收前一致；未编辑或清理仓库文件。

通过项：
1. Finalizer 已正确使用 `reconcile_supplied_snapshot()` 返回的 semantic status，而非外层 `dict(...)` 成功与否。
2. `{}`、root `[]`、`assetPositions=None/object`、非对象行、duplicate BTC、missing/None/NaN/Inf BTC `szi` 均 fail-closed。
3. 对已识别的 invalid snapshot，root/exposure/risk position 为 unknown，伪造的 risk `pass/0` 会被覆盖。
4. Invalid position 不会丢失已知 open orders、working exposure或 cancel audit。
5. Valid `assetPositions=[]`、foreign-only、单个 nonzero BTC row语义正确。
6. 普通 reconcile、cancel-requested reappearance、malformed order status、query-filled、v2/v3/foreign/cardinality未回归。
7. 无 pricing、signal、risk cap、activation、multi-level、spread或skew漂移。

T016 replay：
- 预期 exit `2`。
- Decision：`43 pass`。
- Lifecycle：`49 pass / 12 fail`。
- Provenance：`112 pass`；config：`72 pass`。
- Attempt 2 仍为 `cancel_or_full_fill_terminal_proof_missing`，历史 blocker未升级。
- 输入共 `118` 个文件；回放前后 path+bytes hash均为 `a5443f8f9cb777509ffde21f9caed3837a9ef58f31b25cd82acef2d6a3991398`。
- Replay boundary确认 offline-only，未调用 network/private/order/cancel/remote或新 live window。

验收结论：
- **未通过**
- Semantic status传播修复成立，但 position parser仍将部分 malformed rows和boolean BTC数量错误认证为有效仓位，evidence-completeness gate尚未关闭。

建议总控下一步：
1. 拒绝缺少有效非空 `coin` identity 的 position row，而不是作为 foreign row跳过。
2. 在数值转换前显式拒绝 boolean `szi`。
3. 增加 empty row、empty nested position、boolean `coin/szi`、conflicting nested/direct coin identity回归。
4. 修复并通过独立 QA 前，继续锁定 bounded live、Task 8和adaptive/multi-level activation。

提交信息：
- commit：无；本次为只读验收。
