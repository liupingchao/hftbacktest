# QA 验收结果

## Findings（按严重性）

- P0：无。
- P1：无。
- P2：无。
- P3：派发文本和 business/workflow 文档记录的 implementation 全长 hash
  `44da8a50f1953f116ea6c89a5c6204a5bf71e405` 不存在；当前分支短 hash
  `44da8a50` 实际解析为
  `44da8a50749559ff8e930d3993dd98708b63ef50`。该记录错误不影响实现内容
  与证据复核，QA 状态文档已使用真实对象 hash。

执行线程：
- QA验收线程

任务ID：
- 0722T065

状态：
- 已通过

更新时间：
- 2026-07-22 17:28 Asia/Shanghai

验收线程：
- QA验收线程

验收对象：
- 业务执行线程 `0722T065`
- implementation commit
  `44da8a50749559ff8e930d3993dd98708b63ef50`
- business/workflow commit
  `fe6c55578f1551ad69068b563795ec70d50eb780`

验收范围：
- 两份 checkout 内 SSM invocation receipt 的 command、instance、status、
  root 和 task identity。
- Receipt exact base64 到 10 个 materialized source files 的 SHA-256。
- Exposure/intensity/lifecycle/feedback 的本地派生与交叉校验。
- Current negative case、eligible positive case、hostile drift 和
  deterministic rebuild。
- Repo-relative、offline/no-live/private/order/cancel/service 边界。

验收步骤：
1. 阅读 T065 task、business report、两个指定提交、runner、tests 和 official
   artifacts。
2. 不调用实现 helper，独立解析两个 invocation receipts，严格 base64 解码，
   重算 10 个 SHA-256 并逐字节比较 materialized files。
3. 从 exposure/intensity/lifecycle/feedback CSV/JSON 独立重算 observation、
   distance variation、eligible lifecycle 和 exposure seconds。
4. 审查并运行 current negative、eligible positive、filename/hash/command/
   count drift tests；额外注入 instance/status/root/task drift。
5. 从 `fe6c5557` 创建临时 clean archive，两次运行 official builder，比较
   stdout、19 个文件 hash 和 committed artifact tree。
6. 运行 focused、`py_compile`、完整 Hyperliquid regression 和
   `git diff --check`。

实际结果：
- 两份 receipt 均为 `Success / ResponseCode 0 / empty stderr`，且
  command ID、instance
  `i-02c64c088f311cbc1`、task ID 和 remote root 与 receipt index 精确一致。
- 10/10 receipt payload bytes 通过严格 base64 和 SHA-256 校验，并与 checkout
  materialized files 逐字节一致。
- 独立派生：
  - T047：buy `4 observations / 1 distance`，sell
    `4 observations / 1 distance`，feedback `0 / 0s`。
  - T052：buy `0 observations / 0 distances`，sell
    `4 observations / 3 distances`，feedback `0 / 0s`。
  - Combined：buy `4 / 1`，sell `8 / 4`，feedback `0 / 0s`。
- Current case 正确 fail closed：
  dynamic 与 fill-feedback seed 均不 eligible，recommendation 为
  `route_to_public_multi_distance_dynamic_seed_then_three_window_dynamic_live`。
- Eligible positive case 正确得到两个 eligibility 为 true，并移除两个
  threshold blockers；recommendation 切换到 source-pinned dynamic seed route。
- Filename、non-hex/hash mismatch、command、count、instance、status、root 和
  task drift 均 fail closed。
- Clean archive 两次重建 stdout 和全部 19 个文件 byte-identical，且与
  committed artifact tree 完全一致。
- Runner 仅有本地文件解析/写入；无 AWS SDK、network、subprocess、private、
  order、cancel 或 service 调用。
- Focused：
  `8 passed in 0.04s`。
- Full Hyperliquid：
  `1291 passed, 2 skipped in 57.96s`。
- `py_compile`、`git diff --check`：通过。
- QA 未访问 AWS，未触发 live/private/order/cancel/service。

验收结论：
- 已通过。
- 结论说明：
  - T064 的 source provenance、content hash、local derivation 和 computed
    recommendation 缺陷已关闭；T065 的 offline source-materialization
    contract 可由 clean checkout 独立复现。

通过项：
1. Receipt command/instance/status/root/task identity。
2. Exact base64 materialization 和 10/10 SHA-256。
3. CSV/JSON-derived observation、distance、lifecycle 和 exposure counts。
4. Negative/positive eligibility、blockers 和 recommendation。
5. Hostile provenance/hash/count fail-closed。
6. Deterministic clean rebuild、repo-relative 和 no-live boundary。
7. Focused 与完整 Hyperliquid regression。

不通过项：
1. 无。

缺陷清单：
1. 无阻断缺陷。
2. P3 full-hash 文档记录错误已在 QA 状态文档中纠正；原 business report
   保留为历史执行记录。

阻塞项：
- T065：无。
- 后续真实订单任务仍需新的 formal task、public-only seed 先行验收和 fresh
  exact live authorization；本验收不授权 live。

建议总控下一步：
1. 按 accepted recommendation 派发 public-only multi-distance dynamic
   calibration/seed contract。
2. 该 seed QA 通过前不要开始 three-window dynamic-spread live evidence。

提交信息：
- QA commit：由本报告提交后的线程回报提供。
