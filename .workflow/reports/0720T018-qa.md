# QA 验收结果

## Findings

1. 高：partial final snapshot 会再次造成 shutdown proof 与最终 operator facts 矛盾。
   - `hyperliquid_tiny_live_m2_public_watcher.py:7995-8017` 只有 final open-orders 和 user-state 同时成功时才刷新 manager；任一失败便完全跳过 reconciliation。
   - `/tmp/0720T018-qa-partial-snapshot-xuxiv_dg` 中，final open-orders 成功返回 2 个 tracked orders，但 final user-state 抛错。
   - `cancel_shutdown_proof.json` 正确记录 2 个 open orders 和 `proof_status=fail_closed`，但 `live_status.json` 仍显示：
     - `owned_open_order_count=0`
     - working exposure `0.0 BTC`
     - 两单均为 `cancel_confirmed`
   - blocker 虽包含 `final_manager_snapshot_account_evidence_missing` 和 `tracked_order_still_open`，但 operator 订单与暴露事实仍与已取得的最终 open-orders 证据冲突。
   - 新增测试 `test_hyperliquid_tiny_live_m2_event_driven_watcher.py:1256-1282` 只覆盖两个 snapshot 都成功的情况，未覆盖 partial-snapshot。
   - 触发任务 stop condition：“Final live_status can disagree with final tracked open orders or working exposure”。

2. 中：最终 exchange open row 不能覆盖本地 `cancel_requested` 状态。
   - `hyperliquid_maker_order_manager.py:375-383` 在订单状态为 `cancel_requested` 时更新 size/oid，但故意不恢复为 `resting/partial`。
   - 完整 finalizer 探针 `/tmp/0720T018-qa-cancel-pending-yt5h_01m` 中，cancel 返回 authoritative success，但最终仍有 2 个 open orders；owned count、working `0.01 BTC` 和 blocker 正确，订单状态却仍是两个 `cancel_requested`。
   - 这不符合任务要求“最终 exchange row 将重现订单恢复为 resting/partial”。
   - 现有 manager 测试 `test_hyperliquid_maker_order_manager.py:442-476` 只从 `cancel_confirmed` 状态开始重现，未覆盖 `cancel_requested`。

执行线程：
- 独立只读 QA 验收线程

任务ID：
- 0720T018

状态：
- 未通过

更新时间：
- 2026-07-20 11:43 CST

验收对象：
- implementation commit `d475fbb261fba912e61a3c39fa2bf95df998ea84`
- business report commit `fcfb57dafc7f27ff156bf38230656fe1f98ee838`

验收范围：
- Malformed status、classifier exception、final snapshot、query-filled、v2/v3、foreign-order、submitted cardinality、T016 replay及策略漂移。
- 未执行 live、private/account endpoint、order/cancel、network、remote 或 service 操作。

验收步骤：
1. 检查 workflow、任务合同、提交祖先关系和实现差异。
2. 运行 focused/full regression、py_compile、CLI 和 Git 完整性检查。
3. 使用 `/tmp` 构造 malformed、classifier exception、订单重现、partial-snapshot 和 query-filled 反例。
4. 重放 T016 并验证输入字节不变。
5. 检查 strategy、risk caps 和 activation 是否变化。

命令与结果：
- Focused suite：`362 passed in 15.89s`。
- Full `python -m pytest -q -p no:cacheprovider examples/hyperliquid`：`756 passed in 36.03s`。
- v3/foreign/cardinality targeted suite：`22 passed`。
- `python -m py_compile ...`、acceptance `--help`、`git show --check`、`git diff --check`：通过。
- 六类 list/dict/bool/integer/float/None status：manager、producer、acceptance 均返回 `unknown`。
- Classifier exception：订单保持 active/unknown；两条 query evidence 均保留 redacted result/error。
- Query-filled/no-raw-fill：unknown states、`last_query_status=filled`、zero fill、working `0.01 BTC`、机制 blocker 保持。
- v2 producer/acceptance：schema v2、status pass、结果完全相等。
- T016 replay：预期 exit `2`；decision `43/43`，lifecycle `49 pass / 12 fail`，attempt 2 blocker保持。
- T016 path+bytes aggregate 重放前后均为 `62da666bbb575b01b61aecdde594d994973ddaf50df7342321e24c4f76ca88b8`。
- Implementation 未修改 pricing/signal/executor risk constants、activation 或 quote behavior。

验收结论：
- 未通过
- 结论说明：
  - Malformed evidence 和 query-filled 修复成立，但 partial final snapshot 仍可使最终 operator status 与已取得的 open-order 证据矛盾，任务尚未关闭原始缺陷。

通过项：
1. Non-string status 和 classifier exception 均 fail-closed。
2. Query-filled 无 raw fill proof 时保持 unknown、零成交和机制 blocker。
3. Valid canceled-query v3、foreign-order、v2 reconstruction、submitted cardinality 和 T016 monotonic replay未回归。
4. Strategy、risk caps 和 activation 无变化。

不通过项：
1. Final user-state 失败时，成功取得的 final open-orders 没有刷新最终 manager/status。
2. Final open row 无法覆盖 `cancel_requested` 为 exchange-authoritative resting/partial。

建议总控下一步：
1. 派发 offline-only 修复：将 open-order reconciliation 与 position reconciliation 解耦；只要 final open-orders 有效，就必须刷新订单和 working exposure。
2. 让 supplied final snapshot 的 open row 覆盖 `cancel_requested` 为 resting/partial，同时保留 cancel 请求审计字段。
3. 增加 partial-snapshot 和 cancel-ack-but-still-open 回归测试，独立 QA 通过前继续锁定新 bounded live 和 Task 8。

提交信息：
- commit：无；本次只读验收未编辑或提交仓库文件。
