# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0801T008

状态：
- 已通过

更新时间：
- 2026-08-01 21:46 UTC

验收线程：
- QA验收线程

验收对象：
- 业务线程-python/cross-exchange-research / 0801T008 第二轮

验收范围：
- 首轮两项 P1 修复：T007 R2 source/input exact identity closure，以及
  output entry exact schema/JSON row-count closure。
- 首轮四个 coordinated hostile cases 重放。
- 68-field feature allowlist、discovery-only transform/PCA/mutual
  kNN/Louvain、community filters、真实 medoid 和 p95 assignment。
- 199 次 full-pipeline response-block surrogate、BH gate、
  post-selection-only 标签和零正式 supported claim。
- 正式 manifests/input/output SHA、旧 `motif/` immutability 和两次真实
  deterministic rebuild。

验收步骤：
1. 阅读任务、更新后的业务报告、首轮 QA、计划、实现、测试和正式
   `motif_v2/` artifacts。
2. 在隔离 hierarchy 中用 canonical baseline/episode symlink 和正式
   `motif_v2/` 副本验证未篡改 control package。
3. 协调重放 duplicate provenance role、三项 source SHA、unknown
   nested output key 和 JSON row-count `999` 四个 hostile cases。
4. 独立复算十项 input identity、八项 output metadata/content closure、
   68 维 transform、PCA、mutual graph、communities、pairwise medoids、
   p95 和 post-selection assignments。
5. 不调用业务 surrogate helper，独立重跑 199 次完整 response-block
   permutation pipeline，并复算 empirical p 和 BH q。
6. 在真实目录执行两次完整构建，比较 `motif_v2/` 与旧 `motif/` 全目录
   SHA。
7. 运行 focused pytest、联合回归、ruff、py_compile、CLI help、
   internal validation 和 `git diff --check`。

实际结果：
- Findings（按严重度）：
  - P0：无。
  - P1：无。
  - P2：无。
  - P3：无。
- 首轮 P1-1 input/source identity closure 已闭合：
  - `examples/hyperliquid/cross_exchange_liquidity_response_motif_v2.py:
    924-929` 重新调用 `_validate_source()`，并将 contract 中十项
    provenance 与 fresh expected `role/path/row_count/SHA` list 做 exact
    comparison。
  - `examples/hyperliquid/cross_exchange_liquidity_response_motif_v2.py:
    930-946` 将 baseline manifest、post-selection manifest 和 baseline
    contract 三项 source SHA 绑定到 canonical 文件，并验证 source
    schema。
  - 未篡改隔离 control package 通过 `_validate_existing()`。
  - 将十项 provenance 全部替换为同一份有效 `baseline_manifest` 项，
    保持 cardinality=10，并协调重算 contract SHA、manifest
    frozen-contract SHA 和 contract output SHA后，以
    `motif_v2 input identity drift` 拒绝。
  - 将三项 `source_*_sha256` 全改为 64 个 `0`，同步重算上述 SHA
    binding 后，以 `motif_v2 source binding drift` 拒绝。
- 首轮 P1-2 output metadata closure 已闭合：
  - `examples/hyperliquid/cross_exchange_liquidity_response_motif_v2.py:
    947-966` 要求每个 output entry exact key set 为
    `path/row_count/sha256`，并验证 row-count 类型、非负值、path 和
    SHA。
  - `examples/hyperliquid/cross_exchange_liquidity_response_motif_v2.py:
    967-990` 分别验证 CSV schema/row-count、NPZ schema/row-count 和
    JSON `row_count=1`。
  - 向 `motif_prototypes` output entry 加入未知 nested key 后，以
    `motif_v2 output schema drift: motif_prototypes` 拒绝。
  - 将 `frozen_motif_contract` output `row_count` 改为 `999` 后，以
    `motif_v2 JSON row count drift: frozen_motif_contract` 拒绝。
- 正式 T007 R2 source/input closure 通过：
  - 十项 provenance exact role set 和唯一性正确。
  - 每项 key set 均为 `role/path/row_count/sha256`；实际 path、
    row-count 和 SHA 全部匹配。
  - 三项 source SHA 与对应 provenance SHA 一致，source schema 为
    `hyperliquid_liquidity_response_baseline_v2_r2`。
- 正式 output closure 通过：
  - 八项 output entry 当前均为 exact `path/row_count/sha256`。
  - 八项实际 path、schema、row-count 和 SHA 全部匹配 manifest。
  - motif manifest SHA 为
    `0d11d98a3b8fa624e625a6a5ada75b83281f9d5c300298d687a8278729dba221`。
  - frozen contract SHA 为
    `b61a24eab6a047d0ea467b1a92157500812392d3bf822c59194e25750f19dc82`。
- Feature/discovery-only 回归通过：
  - motif feature 数恰为 `68`。
  - 对
    `adverse/pnl/profit/fee/fill/markout/segment/timestamp/ts_ns`
    做宽匹配，命中数为 `0`。
  - discovery eligible `3,350`，只含 `segment_0001-0003`；
    post-selection eligible `9,071`，只含 `segment_0004-0008`。
  - 独立重算的 median/IQR scale、8 维 PCA mean/components 与 frozen
    contract 一致。
- Graph/community/medoid 回归通过：
  - self removal 后前十近邻保持原距离顺序。
  - mutual graph 为 `3,350` nodes、`9,960` edges、max degree `10`。
  - 独立 Louvain 得到同一组 8 个 communities，size 为
    `130/111/109/109/106/105/103/101`。
  - 每个 community 均覆盖三个 discovery segments，且单段占比不超过
    `0.50`。
  - 八个 prototype 均由完整 community pairwise Euclidean distance
    总和最小值独立确定，medoid episode 和 p95 threshold 与 artifacts
    逐个一致。
- Post-selection 回归通过：
  - 仅使用 frozen discovery transform/PCA/prototype vectors 独立重算。
  - `6,838` 条 assignment 的 episode、segment、motif、distance 和
    p95 threshold 全部匹配。
  - `8 motifs x 5 segments = 40` 条 stability count、median distance
    和 ratio 全部匹配。
  - 正式九个 `motif_v2/` 文件中 exact `held_out` token 数为 `0`，
    formal supported motif 数为 `0`。
- 199 次 surrogate 回归通过：
  - 独立执行 `199` 次完整 transform/PCA/mutual kNN/Louvain/community
    filter pipeline，并整体置换 25 residual + 25 mask response block。
  - surrogate max-size 范围 `118-187`，p95 为 `169.1`。
  - empirical p 为 `0.73` 或 `1.0`，BH q 全为 `1.0`，与 artifacts
    完全一致。
  - 八个 motif 全部正确分类为 `not_supported`。
- 两次真实 deterministic rebuild 通过：
  - 第一次约 `73.48s`，第二次约 `73.84s`。
  - 九个 `motif_v2/` 文件两次构建前后全部 byte-identical。
  - 旧 `motif/` 七个文件全部不变；旧 manifest SHA 保持
    `007bb7f1c00a9f15a81a86aea336f20d5f2cff963a9e4e36868fe01fed86e783`。
- 验证命令：
  - motif v2 focused tests：`9 passed in 42.76s`。
  - case hierarchy + baseline v2 + motif v2 combined：
    `41 passed in 156.42s`。
  - ruff、py_compile、CLI help、formal validator、四个 hostile cases、
    独立 graph/medoid/post-selection、199-surrogate、双真实构建和
    `git diff --check`：通过。

验收结论：
- 已通过
- 结论说明：
  - 首轮两项 P1 均已通过 coordinated hostile 重放闭合，所有正式数值、
    标签、source/output closure 和 deterministic regressions 通过，
    未发现新的 P0-P3。

通过项：
1. 十项 T007 R2 provenance exact identity 与三项 source SHA binding。
2. Output entry exact key/path/row-count/SHA 和 JSON `row_count=1`。
3. 首轮四个 coordinated hostile cases 全部 fail closed。
4. 68-field allowlist 零禁用字段和 discovery-only transform/PCA。
5. Mutual graph、community filters、真实 medoid 和 p95 assignment。
6. 199 full-pipeline surrogate、BH q 和 `not_supported` gate。
7. `0004-0008` post-selection-only、zero exact `held_out`、zero formal
   supported。
8. 正式 artifact closure、双真实 byte determinism 和旧 `motif/`
   immutability。

不通过项：
1. 无。

缺陷清单：
1. P0：无。
2. P1：无。
3. P2：无。
4. P3：无。

阻塞项：
- 无。

建议总控下一步：
1. 将 `0801T008` 更新为 `已通过`。
2. 可按既定顺序解锁后续 T009；QA 线程本轮未创建、解锁或修改 T009。
3. 继续保持无 fresh segment/date 时不得声明正式 held-out motif support
   的边界。

提交信息：
- commit：无
