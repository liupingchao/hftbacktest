# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0729T009

状态：
- 已通过

更新时间：
- 2026-07-29 CST

验收线程：
- QA验收线程

验收对象：
- 业务线程-python/public-data-infra + 0729T009

验收范围：
- 多标的双交易所 collection supervisor、分段采集时序、进程组生命周期、
  严格 freshness/replay/SHA 门禁、common L2 timeline、runtime provenance、
  两段四 profile c6in canary 和本地 pullback。

验收步骤：
1. 独立审查 supervisor、timeline、symbol registry 和 focused tests。
2. 构造 child nonzero/orphan、timeout、spawn failure、stale channel、
   wrong bridge、source-age、NaN/inf 和 CLI fail-open 反例。
3. 复核所有 segment 先采集、后统一 postprocess 的执行顺序。
4. 独立解析 v5 `timeline_index.csv`、8 个 gzip timeline 和 strict quality。
5. 核对本地源码、远端 runtime source、tar 成员和 SHA。

实际结果：
- P0：`0`
- P1：`0`
- P2：`0`
- `timeline_index.csv` 有标准表头，`csv.DictReader` 精确解析 `8` 行。
- V5 runtime SHA 与当前本地源码完全一致：
  - collector:
    `70faaff444b582d5068050f2c7dcdcd72de2935db50603753367a39c0029fbc4`
  - supervisor:
    `06f9d7806869dc575b16483481f91c6dbcc5ffab5f802156feb3cd1ab9c2a149`
  - registry:
    `b8237f8439c54dd8d6f44ac13a879bcec2735721e0c65c94f67c656f59311f69`
  - timeline:
    `c1651853ac9d38f022e87edf2f999971998a6941fe76855801ea15591434d41f`
- `8/8` strict quality gates 和 `8/8` timeline gates 通过。
- 8 个 gzip timeline 共 `8714` 行，manifest、实际 CSV 和 SHA 一致。
- 独立逐行验证：
  - future join `0`
  - timestamp regression `0`
  - source local timestamp 不晚于 common timestamp
  - source age 非负且符合计算
- 8 个 collector 均 return code `0`。
- 8 个 post-exit process group 均
  `group_alive_after_cleanup=false`。
- Segment common timeline gap：
  - BTC `1236.574798ms`
  - ETH `1223.738625ms`
  - SKHYNIX `1222.624943ms`
  - MU `1323.322063ms`
- tar 成员 `254` 个，与本地 artifact 逐文件一致。
- tar SHA：
  `8f512ab7c0353c5bf9e82aaf70eca9148a7beefcc1975ebecd3e9838aab0ea9b`
- Focused tests：
  - `63 passed in 3.66s`
- Conda `hftbacktest` suite：
  - `1357 passed, 2 skipped, 1 deselected`
  - 被 deselect 的既有 deadline timing test 隔离运行 `1 passed`
- `py_compile`、两个 CLI `--help` 和 `git diff --check` 通过。

验收结论：
- 已通过
- 结论说明：
  - 多标的分段 public collection、严格失败关闭、可观测进程生命周期、
    freshness/replay 门禁和同机 common L2 timeline 满足 0729T009
    验收标准。

通过项：
1. BTC、ETH、SKHYNIX、MU 双交易所 profile 可统一调度。
2. 所有 segment 先背靠背采集，再统一 postprocess。
3. child failure、timeout、orphan 和 spawn failure 均失败关闭并留证。
4. Binance bridge/continuity 和 Hyperliquid dual-L2 信息质量严格验证。
5. Head/coverage/tail/arrival-gap/source-age 均有门禁。
6. Timeline 无 future join，source age 和 segment gap 显式可见。
7. 最终 live artifact 与当前源码字节绑定。

不通过项：
1. 无

缺陷清单：
1. 无 P0-P2。

阻塞项：
- 无

残余边界：
- 当前 live evidence 为 `2 x 30s` canary，不能替代正式 8H soak。
- 跨 segment 有约 `1.2-1.3s` 显式空窗，不保证 exact continuity。
- 当前能力是 L2 重建，不代表 L3/L4 queue 或 exact fill simulation。

建议总控下一步：
1. 使用已验收 supervisor 运行正式 8H soak，并按 segment index 审核 gap、
   freshness、reconnect、disk 和 timeline source-age。
2. 在正式 8H 数据通过后，另立 L2 execution model / fill-assumption
   校准任务，不把 public L2 误称为 exact fill。

提交信息：
- commit：无
