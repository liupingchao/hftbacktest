# QA 验收结果

## Findings

1. **P0：无。**

2. **P1 - 缺失或错误的 delayed-history protocol marker 仍可通过 producer 和独立 audit helper。**
   - `examples/hyperliquid/hyperliquid_tiny_live_m2_fill_window.py:2998` 仅在 marker 等于 `delayed_one_call_history_v1` 时进入 exact timing 校验。
   - `examples/hyperliquid/cross_exchange_t024_same_window_acceptance.py:3533` 的独立 audit helper 使用相同条件。
   - Hostile probe 删除 marker，或将其改成 `legacy` 后，两者均返回 `status=pass / reasons=[]`。
   - Task 12 外层 rollout 会最终阻断，因此不能直接绕过最终 acceptance；但 producer 和独立 audit helper 的自报结果 fail-open，不满足多方一致验证合同。

3. **P2：无。**

执行线程：
- 独立 QA 验收线程

任务ID：
- 0721T033

状态：
- 未通过

更新时间：
- 2026-07-21 11:24:44 CST

验收线程：
- 独立 QA 验收线程

验收对象：
- implementation `7369e16a9b2a7c47a99e7ac6c3e157cc85d192de`
- business report commit `1adc50f005ddd0ea5af82ace0e5609c70d35a3bd`

验收范围：
- Exact delayed-history production contract。
- Manager-resting contract required rollout。
- Quarantine artifact presence、schema、row shape 和 canonical equality。
- T031/T026/T016/T022 历史边界。
- Live executor、quote、risk、submission 和 activation 不变性。

验收步骤：
1. 审查任务、业务报告、上一轮 QA 和 committed diff。
2. 运行 delayed timing、quarantine 和 derived-evidence deletion hostile tests。
3. 独立测试 zero/short/long/missing/string/bool timing 配置及 marker 删除/改写。
4. 运行 focused/full regression 和四组历史 acceptance/replay。

实际结果：
- Focused：`514 passed in 27.38s`。
- Full Hyperliquid：`1017 passed in 47.57s`。
- Delay/reserve/rounds/budget/call-cap 的 zero、short、long、missing、string、bool 均使 producer 和 independent audit `fail_closed`。
- Missing/wrong protocol marker 使 producer 和 independent audit helper 错误返回 `pass`；完整 Task 12 外层仍 `raw_cancel_proof_inputs_valid=fail` 并最终 blocked。
- Exact header-only quarantine artifact 通过；missing、bad header、extra cell/None key、missing key、invalid integer、duplicate、forged row 和 reason-equal field forgery 均被拒绝或识别。
- 删除全部派生 manager evidence 不能降级为 contract absent。
- T031 estimator replay exit `1`：exposure `6/6`，persisted/rebuilt censor `0/2`，persisted/rebuilt quarantine `1/0`，`snapshot_match=false`。
- T031 acceptance exit `2`：provenance `112/112`、config `72/72`、decision `43/43`、lifecycle `55 pass / 23 fail`、economics `6/6`。
- T026 acceptance exit `0`：lifecycle `78/78 pass`。
- T016/T022 acceptance 均 exit `2`：lifecycle 均为 `66 pass / 12 fail`。
- 历史 evidence 未被修改。
- QA 未执行 live、private/account、order、cancel、network、remote 或 service 操作。

验收结论：
- 未通过
- 结论说明：
  - 最终 Task 12 仍能阻断错误 marker，但 producer 和独立 audit helper 存在 protocol marker fail-open。

通过项：
1. Exact `4.0/0.5/5/5.0/1` 数值和类型校验。
2. Manager contract 删除旁路及 quarantine 完整性修复。
3. T031 blocked、T026 pass、T016/T022 blocked 边界。
4. Focused/full regression和策略不变性。

不通过项：
1. Missing/wrong delayed protocol marker 可通过 producer 和独立 audit helper。

缺陷清单：
1. P1：是否执行 delayed validation 不能由被验证的 exact marker 自身决定。

阻塞项：
- 不得启动新的 private read/live window。
- Dynamic spread、fill feedback、inventory skew 和 multi-level activation 继续锁定。

建议总控下一步：
1. 新建 offline repair，在存在 historical call 或新任务要求时强制验证 exact protocol marker。
2. 增加 missing/wrong marker 的 producer、independent helper 和 end-to-end tests。
3. 重跑 focused/full 及四组历史边界后重新独立 QA。

残余风险：
- 尚未通过真实 live reference 验证完整 `4s` delayed-history 路径；当前证据仍以离线和 virtual-clock 验证为主。

提交信息：
- commit：无
- QA 未修改仓库文件或任何 local_live_analysis evidence。
