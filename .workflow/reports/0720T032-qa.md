# QA 验收结果

## Findings

1. **P1 - delayed-history 合同允许零 propagation delay，能够把立即 history 查询标记为新协议通过。**
   - `examples/hyperliquid/hyperliquid_tiny_live_m2_fill_window.py:3054` 和 `examples/hyperliquid/cross_exchange_t024_same_window_acceptance.py:3452` 只拒绝 `< 0`，因此 `0.0` 被接受。
   - Task 12 在 `examples/hyperliquid/cross_exchange_t024_same_window_acceptance.py:6277` 只要求协议 marker 为 `delayed_one_call_history_v1`，没有绑定生产配置的 `4.0s` delay。
   - Hostile probe 将 propagation delay、planned/actual wait 都设为 `0.0`，producer audit 和 Task 12 independent audit 均返回 `pass / reasons=[]`。
   - 这会允许没有 deliberate propagation wait 的旧行为被重新封装为新协议通过。

2. **P1 - Task 12 未要求空 quarantine artifact 存在和 schema 完整，删除证据文件后仍可最终通过。**
   - `examples/hyperliquid/cross_exchange_t024_same_window_acceptance.py:5258` 只读取 quarantine rows，没有读取文件存在性或 header。
   - `examples/hyperliquid/cross_exchange_t024_same_window_acceptance.py:7170` 只比较 reason 列表，`:7300` 只检查两边列表为空。
   - Hostile end-to-end probe 删除原本通过 fixture 的空 `confirmed_resting_exposure_quarantine.csv`，重建 terminal seal 后，Task 12 仍返回 `mechanism_and_evidence_integrity_acceptance=pass`。
   - Estimator replay 已要求 artifact 存在且 schema 精确，但 Task 12 没有实现同一合同。

3. **P0 findings：无。P2 findings：无。**

执行线程：
- 独立 QA 验收线程

任务ID：
- 0720T032

状态：
- 未通过

更新时间：
- 2026-07-20 CST

验收线程：
- 独立 QA 验收线程

验收对象：
- implementation `a01593b545fa5cb50c69460e1e96805bf46d6a78`
- business report commit `9e0896f346f6ea24e11f3828dadd400a95ac13d5`

验收范围：
- Leading censor producer、replay、Task 12 重建和 hostile artifact 合同。
- Delayed one-call history 的 rounds、deadline、not-before、call、snapshot 顺序。
- T031/T026/T016/T022 历史证据边界。
- Quote、risk、submission 和 activation 不变性。
- 全程 offline，不修改 T031 evidence。

验收步骤：
1. 审查 T032 task、business report、implementation diff 和相关测试。
2. 逐路径核对 censor/quarantine persistence、replay 和 Task 12 independent rebuild。
3. 审查 terminal recovery 的 SDK deadline、五轮 direct、单次 history 和 final snapshot reserve。
4. 运行 focused/full regression、四组历史 acceptance/replay 和两个额外 hostile probes。

实际结果：
- Focused 四文件回归：`489 passed in 27.08s`。
- Full Hyperliquid 回归：`992 passed in 47.19s`。
- T031 estimator replay exit `1`：exposure `6/6` match；旧证据缺少 `2` 条 censor，persisted quarantine `1`，保持 blocked。
- T031 acceptance exit `2`：provenance `112/112`、config `72/72`、decision `43/43`，lifecycle `52 pass / 23 fail`。
- T026 acceptance exit `0`，lifecycle `75/75 pass`。
- T016/T022 acceptance 均 exit `2`，lifecycle 均为 `63 pass / 12 fail`。
- 生产配置仍为 `4.0s delay / 0.5s snapshot reserve / 5 rounds / 5s budget / 1 history call per reference`。
- Implementation diff 未改变 desired quote、size、position/loss caps、submission count 或 adaptive activation。
- `git show --check`、working/cached diff check 通过；T031 evidence 未修改。
- QA 未执行 live、private/account、order、cancel、network、remote 或 service 操作。

验收结论：
- 未通过
- 结论说明：
  - 正常生产路径、回归测试和历史边界基本成立，但两个证据验证 fail-open 允许零延迟 history 和缺失 quarantine artifact 被接受，尚不能授权下一次 live。

通过项：
1. Leading censor 正常 producer/replay/Task 12 数值重建及 missing/forged censor 拒绝通过。
2. History call、wait end、call end、snapshot start 的正常顺序实现通过。
3. 五轮/五秒/单调用/final snapshot 的生产路径通过。
4. T031 immutable blocked、T026 pass、T016/T022 blocked 边界保持。
5. Quote、risk、submission 和 activation 行为未改变。

不通过项：
1. 零 propagation delay 可通过 delayed-history audit。
2. Task 12 可在 quarantine artifact 缺失时通过。

缺陷清单：
1. P1：delayed protocol 未要求正数或 exact `4.0s` propagation delay。
2. P1：Task 12 quarantine artifact 缺少 presence、schema 和完整 canonical-row 校验。

阻塞项：
- 不得启动新的 private read/live window。
- Dynamic spread、fill feedback、inventory skew 和 multi-level activation 继续锁定。

建议总控下一步：
1. 对下一协议版本强制绑定 exact delay/reserve 配置，至少拒绝 `delay <= 0`。
2. Task 12 要求 quarantine artifact 即使零行也必须存在、header 精确，并 exact-compare 完整 canonical rows。
3. 增加上述两个 end-to-end hostile tests，重跑 focused/full 和 T031/T026/T016/T022 后重新独立 QA。

残余风险：
- 当前测试证明正常代码路径正确，但尚未通过一个真实 `4s` delayed-history live reference；在本次 P1 修复和 QA 通过前不应以新 live 验证替代离线合同修复。

提交信息：
- commit：无
- QA 未修改任何仓库文件或 immutable evidence。
