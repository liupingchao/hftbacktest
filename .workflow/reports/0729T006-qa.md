# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0729T006

状态：
- 已通过

更新时间：
- 2026-07-29 16:28 CST

验收线程：
- QA验收线程

验收对象：
- 业务线程-python/public-collector + 0729T006

验收范围：
- Binance 同连接 snapshot refresh、refresh 上限、manifest 证据、
  CLI 传递及相关回归。

验收步骤：
1. 审查 T005 live 反例与修复后的 bootstrap 控制流。
2. 运行 stale snapshot 同连接 bridge 和 refresh exhaustion 回归。
3. 运行 focused、双 public collector 和环境兼容目录回归。

实际结果：
- snapshot 过旧不会关闭当前 WebSocket 或丢弃 retained buffer。
- 第二次 snapshot 可以从同一 buffer 建立 bridge。
- refresh 次数有界，耗尽后 fail closed。
- manifest 对 snapshot 请求、刷新和每次候选关系提供明确证据。
- focused `19 passed`。
- 双 public collector `22 passed`。
- 目录回归 `1310 passed, 2 skipped`。
- py_compile、CLI help 和 diff check 通过。

验收结论：
- 已通过
- 结论说明：
  - T005 暴露的同连接 snapshot refresh 缺陷已关闭，离线 contract 和
    回归满足下一次 live canary 前置条件。

通过项：
1. 同连接 snapshot refresh 语义正确。
2. bridge 重新扫描完整 retained buffer。
3. refresh exhaustion 和 buffer 边界 fail closed。
4. 既有 reconnect、continuity 和 shutdown 行为未回归。

不通过项：
1. 无

缺陷清单：
1. 无

阻塞项：
- 无

建议总控下一步：
1. 新建 live validation 任务，部署新 SHA。
2. 重新执行 60 秒 gate，通过后再执行 30 分钟窗口。

提交信息：
- commit：无
