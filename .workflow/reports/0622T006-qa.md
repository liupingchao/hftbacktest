# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0622T006

状态：
- 已通过

更新时间：
- 2026-06-24 15:32 CST

验收线程：
- QA验收线程

验收对象：
- 业务线程-live 0622T006

验收范围：
- 验收 `0622T006` 是否按 M2 anti-drift maker-only submit gate / live calibration 范围完成实现、受控 live 校准、产物、边界和 fail-closed 结论；不把无 fill / 无 PnL 解释为 M2 完成。

验收步骤：
1. 复核 `.workflow/tasks/0622T006.md` 与 `.workflow/reports/0622T006-business.md`。
2. 复核 `local_live_analysis/hyperliquid_tiny_live_m2_anti_drift_gate_0622T006*/` 下的 anti-drift、inline reprice、final gate、open-orders proof 和 T008 ledger 产物。
3. 运行 focused regression、`py_compile`、CLI help、JSON/CSV/empty-file 检查和 `git diff --check`。

实际结果：
- 已实现 `--event-driven-anti-drift-live`，anti-drift 参数为 `bbo_lookback_ms=750`、`min_stable_ms=250`、`flow_lookback_ms=1000`、`pressure_ratio_threshold=2.0`、`min_pressure_qty_btc=0.01`。
- 主 formal run 评估 `914` 个 current candidates，anti-drift gate `99` 次评估中 `6` pass / `93` block。
- 真实提交保持 `Alo` post-only、`<=0.005 BTC`、无 taker/crossing/Ioc/one-tick-back/cap relaxation；实际只有 `2` 次 order endpoint call，低于 `30` 上限。
- 两次买单分别为 `0.00422 BTC @ 64956.0` 和 `0.00179 BTC @ 65032.0`，均因 exchange-side BBO drift 被 post-only validation 拒绝；`fill_count=0`、`maker_fill_count=0`。
- T008 ledger 结果为 `live_realized_pnl_proof=false` / `fail_closed_no_realized_live_pnl`，因此 M2 仍 blocked。
- formal rerun 2 为 `19` 次 anti-drift 评估，`2` pass / `17` block，`0` order calls；rerun 3 的 SSH/control-plane 中断已清理，后续 open-orders proof 为空。
- 产物健康复核：三组 T006 artifact 共 `266` 个文件，空文件检查无输出。
- 验证通过：event watcher `37 passed`、public watcher `4 passed`、fill-loop/ledger `27 passed`、`py_compile`、public watcher/fill_window/fill_loop CLI help、关键 JSON manifest 校验、`git diff --check`。

验收结论：
- 已通过
- 结论说明：
  - `0622T006` 满足任务级验收：anti-drift gate 与受控 live calibration 已完成，风险边界和 fail-closed ledger 保持成立；但没有 live maker fill / fee / inventory / realized PnL 证明，M2 不完成。

通过项：
1. anti-drift / touch-stability gate 已接入 watcher-local inline reprice path，并产生可复核产物。
2. 受控 live 执行保持 `Alo`、`<=0.005 BTC`、`<=30` order calls、tracked cancel、final open-orders proof 和 T008 fail-closed。
3. 测试、CLI、JSON/CSV/empty-file 和 diff 检查通过。

不通过项：
1. 无

缺陷清单：
1. 无

阻塞项：
- M2 里程碑仍阻塞：无 live maker fill / fee / inventory / realized PnL 证明。

建议总控下一步：
1. 不要把 `0622T006` 解释为 M2 完成或 M3 ready。
2. 后续 live 方向仍需先处理 post-`open_orders` public-state freshness、candidate staleness 和 edge/alpha 质量，不能通过 taker/crossing/one-tick-back/cap relaxation 强行成交。

提交信息：
- commit：无
