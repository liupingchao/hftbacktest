# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0728T074

状态：
- 未通过

更新时间：
- 2026-07-28 18:48 CST

验收线程：
- QA验收线程

验收对象：
- 业务执行线程 / 0728T074

验收范围：
- T073 P1/P2 repair、fault regressions、final T074 Canary、evidence hashes
  和 AWS 零遗留。

验收步骤：
1. 重跑 T073 的 S3 403、tag AccessDenied、setup receipt、PUT failure、
   signal/finally 和 clock journal 反例。
2. 独立复算 T074 state、archives、raw summaries、source archive 和
   binary identity。
3. 执行 focused Python/Rust/fmt/clippy/diff check。
4. 只读查询 T074 AWS 资源和 final bucket。

实际结果：
- T073 P1 全部关闭：S3 403/tag 查询失败均 fail closed。
- setup receipt 缺失/截断/gate fail 会在 probe 前失败。
- PUT failure、SIGTERM cleanup 和 failed clock journal 通过。
- final Canary 和 AWS 零遗留全部通过。
- 直接抛出的 KeyboardInterrupt cleanup 会执行，但 journal 写
  `interrupted=false`。
- CLI `--task` 默认仍为 T073；README 一处仍写 final T073。

验收结论：
- 未通过
- 结论说明：
  - 实际 AWS/data/control path 已通过，但任务明确要求的直接
    KeyboardInterrupt 审计语义和 task identity 仍有离线缺陷。

通过项：
1. HeadBucket 403、tag AccessDenied、setup gate、PUT failure 全部
   fail closed。
2. Python `16/16`、Rust `15/15`、fmt/clippy/diff check 通过。
3. Final T074 setup/run/archive/rebuild 全部 `2/2` 或 `4/4`。
4. 当前 AWS 零活动资源，final bucket 明确 404。

不通过项：
1. P2：直接 KeyboardInterrupt journal 的 interrupted 标志错误。
2. P2：CLI 默认 task identity 过期。
3. P3：README final Canary task ID 过期。

缺陷清单：
1. 捕获 KeyboardInterrupt 时强制设置 interrupted=true，并新增直接
   KeyboardInterrupt regression。
2. `--task` 改为必填，避免默认身份漂移。
3. README 改为 final T074。

阻塞项：
- 无外部阻塞。

建议总控下一步：
1. 新建纯离线 T075 修复，不重复 EC2 Canary。
2. 重跑 focused tests 后独立 QA。

提交信息：
- commit：无
