# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0618T010

状态：
- 阻塞

更新时间：
- 2026-06-18 14:51 CST

验收线程：
- QA验收线程

验收对象：
- 业务线程-research 0618T010

验收范围：
- `M2B fill-acquisition repair with adaptive maker requote`
- 验收 adaptive maker-only requote 修复、final gate、真实 post-only `Alo` attempts、tracked cancel、final open-orders proof、T008 ledger reconciliation、artifact pullback、redaction 和 M2 是否可完成。

验收步骤：
1. 阅读 `.workflow/tasks/0618T010.md` 和 `.workflow/reports/0618T010-business.md`。
2. 复核 `local_live_analysis/hyperliquid_tiny_live_m2_fill_loop_0618T010/m2_fill_loop_manifest.json`、`window_result_matrix.csv`、`quote_attempt_matrix.csv`、`aggregate_live_fill_ledger.csv`。
3. 复核 window 的 `m2_fill_window_manifest.json`、`executor_manifest.json`、`cancel_shutdown_proof.json`。
4. 复核 `ledger_reconciliation/m2_pnl_ledger_manifest.json`。
5. 运行 focused tests、`py_compile`、JSON 校验、artifact 非空检查、redaction scan、独立远端 open-orders 复核和 `git diff --check`。

实际结果：
- Remote git-safe refresh 完成，远端最终为 `cross-exchange:5391b79439a4f0cb24fd40e4e6aa4b8de53f73e3:0`。
- Final gate 输出 `final_recommendation=tiny_live_ready_for_controller_go`、`allow_create_0617T008=true`、`blocking_reasons=[]`。
- Aggregate manifest 输出 `final_recommendation=hyperliquid_tiny_live_m2_fill_loop_blocked`、`blocking_reasons=[ledger_no_live_realized_pnl_proof]`。
- Adaptive window 完成 `6/6` attempts，`side_policy=alternate`，attempts 为 buy/sell 交替，全部 `post_only_tif=Alo`，全部 `crossing_guard_status=pass`。
- 6 次 attempt 均达到 `resting`，但 `fill_count=0`、`maker_fill_count=0`、`ledger_fill_rows=0`。
- Window shutdown 通过：`real_cancel_endpoint_called=true`、`shutdown_proof_status=pass`、`final_open_orders_count=0`。
- T008 ledger rerun in `live_pulled_back` mode 输出 `live_realized_pnl_proof=false`、`realized_pnl_proof_status=fail_closed_no_realized_live_pnl`。
- 独立远端 open-orders 复核返回 `final_open_orders_count=0`、`final_open_orders_empty=true`。
- Focused tests 通过：`27 passed`。
- JSON validation 通过。
- Artifact 非空检查通过：`41` files，`0` empty。
- Redaction scan 未发现未脱敏 `0x40` 地址或 `0x64` secret/signature-shaped values。
- `git diff --check` 通过。

验收结论：
- 阻塞
- 结论说明：
  - Adaptive maker-only requote 修复已执行且安全边界通过，但同 caps/post-only 条件下仍无被动成交，T008 ledger 不能形成真实 realized PnL proof。

通过项：
1. Adaptive cancel/requote 机制已实现并执行。
2. 所有 attempts 均保持 maker-only/post-only `Alo`，未发现 taker/crossing、cap relaxation、scheduled-cancel 依赖、continuous live bot 或 Binance maker route 修改。
3. 最终 open orders 为空，redaction / artifact / tests / JSON / diff check 均通过。

不通过项：
1. 无 passive maker fill，`aggregate_live_fill_ledger.csv` 为空。
2. T008 ledger 输出 `fail_closed_no_realized_live_pnl`，因此 M2 仍未完成。

缺陷清单：
1. 无代码缺陷；阻塞原因仍是 approved caps/post-only envelope 下无成交。

阻塞项：
- `no_fill_observed`
- `ledger_no_live_realized_pnl_proof`

建议总控下一步：
1. 保持 M2 blocked，不进入 M3。
2. 继续 retry 的边际价值较低；下一步应先做 no-fill 诊断/设计决策，比较同 caps 下更长时间段、队列位置、盘口深度、最小 size、side 选择和时段，而不是继续盲跑。
3. 不要为了成交切 taker/crossing 或放宽风险 caps，除非新建并批准新的风险 envelope。

提交信息：
- commit：`5391b79`
