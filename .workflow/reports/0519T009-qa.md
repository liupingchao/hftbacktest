# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0519T009

状态：
- 已通过

更新时间：
- 2026-05-19 22:47 CST

验收线程：
- QA验收线程

验收对象：
- 测试线程 + 0519T009

验收范围：
- 验收 `0519T009` 是否按边界完成 `5-19-day-control-30min` current-format 30min no-rule / default-off control sample collection、T006 quote-update audit 字段检查、audit replay / maker acceptance、sidecar/join、Stage 5 / Step 5C diagnostics、T008 Step 9B offline runner rerun 和 archive 证据整理。
- 本 QA 不验收 live promotion、default-on、production behavior change、Step 5C promotion、inventory-control implementation、queue/touch repair 或 generalized PnL proof。

验收步骤：
1. 读取 `.workflow/tasks/0519T009.md` 和 `.workflow/reports/0519T009-business.md`。
2. 抽查本地 artifact：`maker_acceptance.json`、`alignment_report_audit_replay.json`、`stage9b_quote_adjustment_replay_0519T009/candidate_summary.json`、`audit_field_coverage.csv` 和 archive sha256。
3. 核对 preflight、live collection marker、early T006 header coverage、audit replay / maker acceptance、sidecar/join、T008 rerun classification 是否满足任务验收标准。
4. 核对结果是否保持 no-rule / default-off / single-sample diagnostic 边界。

实际结果：
- Dataset: `5-19-day-control-30min`。
- Deployed commit: `2d0cae2`。
- Remote preflight: `decision=preflight_passed`，`dirty=false`，`compatibility.passed=true`，`audit_field_count=159`。
- Collection markers:
  - start marker UTC `2026-05-19T09:13:58Z`
  - stop marker UTC `2026-05-19T09:46:46Z`
  - stop marker exit code `0`
- Live audit rows `122124`，live audit fields `159`。
- 15 个 T006 quote-update audit 字段全部存在：
  - `quote_update_intent`
  - `quote_update_action`
  - `quote_update_reason`
  - `min_move_passed`
  - `quote_age_ms`
  - `join_age_ms`
  - `anchor_age_ms`
  - `latency_bucket`
  - `throttle_state`
  - `token_bucket_state`
  - `cancel_readd_bucket`
  - `reject_throttle_drop_cause`
  - `post_only_pre_check`
  - `post_only_post_check`
  - `inventory_request_id`
- `maker_acceptance.py` passed: `true`，hard failures `[]`，common rows `96340`，checks passed `21/21`。
- action / planned / reject / throttle match: `1.0 / 1.0 / 1.0 / 1.0`。
- strict replay lag gate passed，breach/drop/fail `0/0/0`。
- Sidecar / join:
  - first valid update aligned `true`
  - depth `pu` mismatch count `0`
  - final data row mapping coverage `1.0`
  - decision join coverage `1.0`
  - future join count `0`
  - gap-crossed join count `0`
  - stale join count `587`
  - top5 join age p50/p90/p99 ms `14.477155 / 25.97522 / 66.10700560000005`
- Stage 5 / Step 5C:
  - submit orders `4098`
  - filled orders `110`
  - fill-after-cancel orders `53`
  - fast-cancel churn rate about `0.86164`
  - Step 5C decision rows `96341`
  - bookTicker anchor rows `79449`
  - guarded depth fallback rows `16892`
  - post-only risk after re-check rows `0`
- T008 rerun output exists at `local_live_analysis/5-19-day-control-30min/stage9b_quote_adjustment_replay_0519T009/`。
- T008 rerun summary:
  - classification `promising_but_single_sample`
  - missing T006 field count `0`
  - decision rows `96341`
  - submit orders `4098`
  - candidate count `8`
- Archive exists:
  - `local_live_analysis/archive/5-19-day-control-30min.tar.gz`
  - sha256 `3150b89ece1ca9509095630500b0d3d39eac63b25f075fb8f1e74114b7290492`
- Raw gzip note is documented: collector gzip initially lacked footer after tmux shutdown; original remote `.corrupt` was preserved; complete raw lines were recovered and recompressed; local `gzip -t` passed. This is collection hygiene to improve later, not a blocker for this task because replay, sidecar and acceptance artifacts succeeded.
- Scope boundary respected: no live promotion, no default-on, no production behavior change, no Step 5C promotion, no inventory-control implementation, no queue/touch repair, no single-sample PnL proof.

验收结论：
- 已通过
- 结论说明：
  - `0519T009` 完成 current-format no-rule / default-off control sample collection，并消除了新样本上的 T006 字段缺失 blocker；audit replay、maker acceptance、sidecar/join 和 T008 rerun 证据满足本任务验收标准。结果只能作为 single-sample offline diagnostic evidence，不授权 promotion、default-on 或 live readiness。

通过项：
1. Current-format sample 采集完成，preflight 和 start/stop marker 证据完整。
2. 15 个 T006 quote-update audit 字段全部覆盖，`missing_t006_field_count=0`。
3. Audit replay / maker acceptance hard gates 通过，action/planned/reject/throttle match 均为 `1.0`。
4. Sidecar/join 关键质量指标通过：join coverage `1.0`，future join `0`，gap-crossed join `0`，depth `pu` mismatch `0`。
5. T008 rerun 完成并产出 `promising_but_single_sample`，但未越界解释为 promotion。
6. Archive sha256 已记录。

不通过项：
1. 无

缺陷清单：
1. 无

阻塞项：
- 无

建议总控下一步：
1. 将 `0519T009` 视为已通过。
2. 保持结论边界：该样本只支持后续 multi-sample validation planning / Step 9 diagnostic interpretation，不支持 live、default-on、promotion 或 production behavior change。
3. 后续采集可单独改善 collector graceful shutdown，避免 raw gzip footer recovery，但不阻塞当前 roadmap。

提交信息：
- commit：635926d / 2628124
