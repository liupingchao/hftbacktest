# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0902T001

状态：
- 已通过

更新时间：
- 2026-09-02 CST

验收线程：
- 独立只读 QA agent `McClintock`

验收对象：
- implementation：
  `5eed10e59dcab91657bd332ca2b94ba3d2b7476b`
- business report：
  `00ae475d3899011259f337411c9986abb0a9f3f6`
- handoff：
  `14e8f5ee995589a5a16e669745eddd58413bb61b`

验收范围：
- target-local Python argv contract repair。
- outer、producer、recovery、verifier 和 armed-claim argv ownership。
- runtime、script、argv、cwd 与 `shell = false` evidence。
- hostile mutations、predecessor regression 和 effect-free boundary。
- 不验收、不注册也不执行 successor Q0。

验收限制：
- reviewer 未修改、暂存或提交任何候选文件。
- reviewer 未创建 refs、tags、claims、receipts、baseline 或 output root。
- business/scientific/formal Q0 execution：`false`。

## Review Identity

- authority base：
  `1051f2b29059e6b7465fe8051de01072f9ff7e19`
- task registration：
  `cc8c21f987497bd0f96a64183574e98b6db7d3e9`
- implementation：
  `5eed10e59dcab91657bd332ca2b94ba3d2b7476b`
- business report：
  `00ae475d3899011259f337411c9986abb0a9f3f6`
- handoff：
  `14e8f5ee995589a5a16e669745eddd58413bb61b`

提交链线性，三个被审提交均通过 `git show --check`。

## Severity

- **P0/P1/P2/P3 = 0/0/0/0**

## Findings

- 无 P0-P3 finding。
- 无阻塞项或非阻塞缺陷。

## Contract Reconstruction

四个 Python 进程边界均保留完整：

```text
exec_argv = [python_executable, script_path, ...args]
program_argv = exec_argv[1:]
observed Python sys.argv == program_argv
observed Python sys.argv != exec_argv
```

- outer driver：通过。
- formal producer：通过。
- recovery driver：通过。
- terminal verifier：通过。
- armed claim：使用 `[sys.executable, *sys.argv]` 重建完整命令。

所有边界同时验证 runtime executable。Producer/verifier 仍由
`Popen(list(argv), ...)` 启动，没有 shell command string 或
`shell = true` 路径。

## Independent Verification

- argv regression：
  `18 passed, 130 deselected`。
- hostile argv mutations：
  `10 passed, 138 deselected`。
- applicable current suite：
  `147 passed, 1 deselected`。
- accepted predecessor regressions：
  `410 passed, 1 skipped`。
- exact old implementation
  `879a763944e6b8052333b6102a2f940e18a0f664`：
  `130 passed`。
- ruff check / format check：
  passed。
- Python compile：
  passed。
- diff and commit checks：
  passed。

Business report 的 `16 passed` 使用了更窄的 selector；QA 使用更宽的
`-k argv` 独立执行得到 `18 passed`，不构成验收缺口。

## Historical Deselection

Authority base 上单独运行
`test_development_formal_pipeline_replays_all_registered_negatives`
稳定失败于：

```text
AUTHORITY_BINDING:.workflow/tasks/0831T001.md
```

原因仅为任务状态从 `执行中` 改为 `未通过`：

- frozen SHA256：
  `19585d2501994535eca3d462b62860be76c54cc1196609ce9fd9a788255b138b`
- closed SHA256：
  `79142da5690d3611f2157accb03bf6f2b9dd84256b7ead55b4e0d1e00d0914f0`

从 `879a763...` 到 authority base，runner、verifier 和原测试没有变化。
旧实现的精确 130 项 suite 全部通过。因此该 deselection 是关闭后的
authority-byte 不兼容，不是 argv 修复回归。

## Effect-Free Qualification

实际 qualification 返回：

- `business_execution = false`
- `effectful_outputs = false`
- `successor_q0 = ABSENT`
- `observed_sys_argv == program_argv`
- `shell = false`

Runtime、resolved runtime、runtime SHA256、script path、script SHA256、
完整 argv 和 cwd 均与实际执行一致。运行前后 Git status、refs 和旧
claim SHA256 完全相同。

## Boundary Audit

- 0902 armed/claimed claim：absent。
- 0902 consumption/terminal receipt：absent。
- 0902 baseline/output root：absent。
- 0902 controller/Q0 ref：absent。
- old formal attempt root：absent。
- successor Q0：absent。
- old 0831 armed claim unchanged：
  `8a6b7f1078f2b6540771a5ea5144745de5893f2cdddc2132206892a1c5d501fc`。
- final worktree：clean。

## 验收结论

- **已通过**
- `P0/P1/P2/P3 = 0/0/0/0`
- `business_execution = false`
- `successor_Q0 = absent`

`0902T001` 满足独立 QA 验收条件。该结论只接受 argv repair，不授权
注册或执行 successor Q0。

通过项：
1. 完整命令与 Python program argv 语义正确分离。
2. 四个进程边界和 armed claim 使用同一修复语义。
3. runtime/script/argv/cwd/shell hostile cases fail closed。
4. applicable current tests、predecessor tests 和原 implementation
   baseline 均通过。
5. effect-free boundary 和旧失败证据保持不变。

不通过项：
1. 无。

缺陷清单：
1. 无。

阻塞项：
- 无。

建议总控下一步：
1. 接受 `0902T001`，关闭为 `已通过`。
2. 保持 successor Q0 锁定，等待单独的新 task、candidate、attempt、
   claim 和 output-root 授权。

提交信息：
- 由后续 QA evidence commit 承载，不在报告内自引用。
