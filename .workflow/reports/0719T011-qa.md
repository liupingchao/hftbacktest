# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0719T011

状态：
- 未通过

更新时间：
- 2026-07-19 23:59 CST

验收线程：
- QA验收线程

验收对象：
- source commit `d8e22c2d9288fef86707d9b26f7791d7d8711c09`
- business report commit `d9db42d1e256feeaf29764abda648181cace41d4`
- artifact `local_live_analysis/principal_alignment_single_level_two_sided_0719T011/`

验收范围：
- 唯一 bounded live window、source/terminal provenance、运行 envelope 和 activation。
- 前后账户、open orders、position、进程和 kill-switch 安全。
- public trigger、anti-drift、immediate guard、edge gate 与最终 blocker。
- two-sided submit/resting/cancel/fill lifecycle。
- same-window acceptance 结果及 remote/local path-binding portability。

验收步骤：
1. 审查 workflow、T011 task/business report、T010 QA 和提交关系。
2. 独立核验 exact Git archive、62 个 runtime source 文件和 106 项 terminal manifest。
3. 逐行重建 trigger、immediate guard、edge gate、attempt 和 endpoint 事实。
4. 离线重跑 same-window acceptance、focused regression 和完整 Hyperliquid 回归。
5. 在 `/tmp` 重写并重新 seal 物理路径，验证 path-binding failures 的性质。

## Findings

1. P1：stop-condition 汇总与逐行证据不一致。
   - manifest/business report 声称 `trigger_count=1`、找到一个 public trigger。
   - `event_driven_trigger_decision_matrix.csv` 实际有 `26` 行 `trigger_found=true`：
     - 5 次 anti-drift block
     - 10 次 immediate guard 通过后 edge block
     - 11 次 immediate guard fail-closed
   - 10 次 edge block 中，7 次为 `fair_mid_source_stale`，3 次为 `edge_below_required_buffer`。
   - `edge_gate_no_fresh_sufficient_signal` 是真实贡献 blocker，但不是完整、唯一的 no-submit 因果摘要。
   - “first exact stop condition 已保存”的结论因此不能按当前证据直接接受。

2. P2：private endpoint 汇总字段与原始证据冲突。
   - 顶层 watcher manifest 记录 `public_waiting_phase_private_or_order_endpoint_called=false`。
   - 逐行证据有 `2237` 行记录该字段为 true，nested producer artifacts 也记录 `private_endpoint_called=true`。
   - 实际调用是允许的只读 `open_orders`，没有 order/cancel 调用，因此不构成账户安全事故，但属于 evidence-integrity 缺陷。
   - Acceptance 当前没有独立重建并检查该冲突。

3. Path-binding failures 是 acceptance portability gap，不是 live source 伪造。
   - 原始 acceptance 有两个 provenance path failure 和一个 output-dir failure。
   - `/tmp` 中仅规范化 remote/local 物理路径并重新 seal 后，provenance 从 `105 pass / 2 fail` 变为 `107/107 pass`，config 只剩 edge-gate failure。
   - exact Git archive SHA-256 为 `7619e61c96c62d41f5b62a5ff7c8def80134ba119fbf4f3a2d50a5b37d0f8fc5`。
   - 62 个 runtime source 文件集合和 SHA-256 均与 exact source commit 一致。
   - 因此这些路径失败不得解释为 source mutation、remote source forgery 或 pullback byte corruption。

实际结果：
- 恰好一个 orchestrator window：通过。
- Window duration：`900.045779s`，未启动第二窗口。
- Runtime source：`62/62` exact Git blob match。
- Terminal manifest：`106/106`，missing/mismatch/unexpected 均为 0。
- Envelope：`0.005 BTC/order`、`0.01 BTC position`、`1 USDC loss`、`2 submissions`、post-only `Alo`。
- Multi-level、dynamic spread、fill feedback、inventory skew 和 actual quote behavior activation 均关闭。
- Pre/post open orders：`0/0`；independent final open orders：`0`。
- Pre/post BTC position：`0.0/0.0`；conflicting process：`0/0`；kill-switch clear。
- Child `rc=0`、已 reap、无 SIGKILL；writer healthy、failure count 0。
- 实际 submissions/order calls/cancel calls/fills：`0/0/0/0`。
- 不存在真实 two-sided submit/resting/cancel/full-fill lifecycle。
- Same-window acceptance 返回 blocked 是正确结果；不得关闭 Principal Task 7/12 或解锁 multi-level。

命令与结果：
- Focused acceptance/orchestrator/watcher/manager regression：`164 passed in 4.55s`。
- Full `examples/hyperliquid`：`689 passed in 35.40s`。
- 独立 terminal `sha256sum -c`：`106/106 OK`。
- 原始 same-window acceptance：return code `2`，mechanism/evidence `fail`。
- Path-normalized、重新 seal 的 `/tmp` 对照：provenance `107/107 pass`，仍因零提交和 edge/guard/lifecycle 证据失败而 blocked。
- 未执行任何 live、private、account、order、cancel、network、remote 或 service 操作。
- QA 未修改仓库，原有工作区状态保持不变。

验收结论：
- 未通过
- 结论说明：
  - T011 的唯一 live window 安全结束，source、checksum、账户终态和 activation 边界可信；但 blocker/trigger/private-read 汇总未与原始证据严格一致，且没有形成目标 two-sided lifecycle，不能接受为 Principal Task 7/12 baseline。

通过项：
1. Exactly one live window。
2. Exact source、62/62 runtime provenance 和 106/106 terminal checksum。
3. Envelope、activation、账户、position、open-orders、process 和 kill-switch 安全。
4. Same-window acceptance 正确 fail-closed。
5. Path-binding failure 被确认是 portability gap，而非 source 伪造。

不通过项：
1. Trigger 和 stop-condition 汇总不准确。
2. Public-waiting private-endpoint 汇总与逐行证据冲突。
3. 无真实 two-sided submit/resting/cancel/fill lifecycle。

缺陷清单：
1. Producer 应分别记录 eligible trigger count、anti-drift block、immediate-guard block 和 edge-gate block，不能用布尔化 `trigger_count` 代替计数。
2. Acceptance 应从逐行矩阵独立重建 blocker 和 endpoint-call 汇总。
3. Remote canonical path 与 local physical pullback path 应分字段验证，避免 byte-exact pullback 必然失败。

阻塞项：
- Principal Task 7/12 lifecycle 仍未完成。
- Multi-level 必须继续锁定。

建议总控下一步：
1. 先执行离线 evidence-summary/path-portability repair 并独立 QA。
2. 修复通过后派发新的隔离 single-level two-sided bounded live task；不得复用 T011 作为已接受 lifecycle。
3. 保持现有风险上限及所有 adaptive/multi-level activation 关闭。

提交信息：
- source commit：`d8e22c2d9288fef86707d9b26f7791d7d8711c09`
- business report commit：`d9db42d1e256feeaf29764abda648181cace41d4`
- QA commit：无
- 文件修改：无
