# QA 验收结果

执行线程：
- QA验收线程（全新独立验收）

任务ID：
- 0815T002

状态：
- 未通过

更新时间：
- 2026-08-15 19:57 CST（星期六）

验收线程：
- QA验收线程

验收对象：
- 业务线程-python/research + `0815T002`
- 当前 source：
  - `examples/hyperliquid/cross_exchange_liquidity_response_trigger.py`
  - `examples/hyperliquid/cross_exchange_liquidity_response_episodes.py`
  - `examples/hyperliquid/cross_exchange_trigger_parity_admission.py`
- 正式 Stage 3 package：
  `local_live_analysis/skhynix_trigger_aligned_episode_research_v1_stage03_detector_parity/`
- Build A：
  `/tmp/0815T002-parity-a.YcHSFQ/package`
- Build B：
  `/tmp/0815T002-parity-b.020IeP/package`
- Pre-extraction baseline：
  `/tmp/0815T002-pre-extraction.4r9BcF`
- QA 独立重放：
  `/tmp/0815T002-qa-replay.0WLUIO/package`

验收范围：
- 审查 shared trigger 是否纯、版本化且由历史 builder 真正复用。
- 独立重放 Jul30/Aug03/Aug04 全部 candidates，逐 36-field text、
  row order、candidate sequence、session/segment count、attribution、
  rejection 和 primary 做 parity。
- 检查 rejected candidates 是否完整保留，防止 confirmed-only parity。
- 检查 fixture 四文件 pre/post byte identity、Formal/A/B determinism、
  Stage 1/2、历史 package 和 detector input immutability。
- 检查 package admission、manifest schema、artifact/binding closure、
  source/archive verify-only、zero-write、atomic publication和 hard
  boundary。
- 特别攻击 `/tmp` baseline 依赖、runtime coherent rehash、未知 artifact、
  CSV row width、path canonicality、unknown scope、report 和 boundary。
- 本轮不修改业务代码、正式 package、accepted dependencies、历史数据或
  controller tracking，不开始 Stage 4。

普通代码审查：
- P0：`0`
- P1：`3`
- P2：`2`
- P3：`1`

P1 findings：
1. 正式 package 的 verify-only 依赖易失的外部 `/tmp` baseline，package
   在正常临时目录清理后不能自包含验证。
   - `input_bindings.csv` 的 before/after baseline row 固定指向
     `/private/tmp/0815T002-pre-extraction.4r9BcF`。
   - `cross_exchange_trigger_parity_admission.py:1575-1583` 在已经验证
     package 内 `pre_extraction_source/` 和 `fixture/pre|post/` 后，仍从
     binding path 调用 `_validate_pre_extraction_baseline()`。
   - QA 将 baseline 原子 rename 到隔离路径并在 `finally` 中恢复；
     source `python -B ... --verify-only` 和 archived `python -B ...
     --verify-only` 均返回 `rc=2`：
     `pre-extraction source drift:
     cross_exchange_liquidity_response_episodes.py`。
   - 这不是当前路径尚存在时的理论风险。`/tmp` 被清理是正常生命周期，
     清理后 accepted package 即失去验证能力；也不满足任务要求的
     “归档到 parity package”和后续阶段可消费的 auditable package
     contract。
2. 包内 archived admission verifier 没有绑定到当前 source identity，
   可被 coherent rehash 后继续获得 source verifier 的 `verified=true`。
   - `cross_exchange_trigger_parity_admission.py:1682-1698` 只把 archived
     runtime hashes 与 package manifest 的自报值比较，并只对 shared
     trigger 和 historical builder 使用外部 SHA anchor；遗漏 archived
     `cross_exchange_trigger_parity_admission.py` 与当前 source 的等值
     断言。
   - QA 在生产大小 package copy
     `/tmp/0815T002-qa-runtime-rehash-v17szxrt/package` 中修改 archived
     admission source，再同步更新 artifact records、core SHA 和
     `runtime_source_sha256_by_path`。
   - 当前 source verifier 与被修改的 archived verifier 均返回 `rc=0`、
     `verified=true`；新 core SHA 为
     `572bf573599cc62a368c81447fcc802d95d25edf68c7096c840812aa3562eb09`。
   - 后续阶段无法相信 package 自带 verifier 与 QA 验收的 source
     verifier 是同一逻辑，runtime closure 未闭合。
3. Artifact universe 没有 exact allowlist；可加入 outcome-like 文件并
   在全部 boundary flags 仍为 false 时通过 verification。
   - `_artifact_records()` 在
     `cross_exchange_trigger_parity_admission.py:1167-1179` 接受任意新增
     文件；`_artifact_closure()` 在 `:1354-1365` 只检查实际目录与
     manifest 自报 artifacts 一致，没有冻结 Stage 3 的精确 artifact
     path set，也没有对 package artifact path 执行 Aug07/outcome
     boundary scan。
   - QA 在生产大小 copy
     `/tmp/0815T002-qa-extra-artifact-7363givs/package` 中加入
     `outcomes/future_outcome.csv`，同步更新 artifacts/core。
   - Source verifier 返回 `rc=0`、`verified=true`、`artifact_count=21`，
     同时七个 manifest boundary flags 仍全部为 `false`。
   - 这使 package 的 no-outcome/no-response 边界可被 coherent rehash
     绕过，直接影响 Stage 4 对 Stage 3 admission 的信任。

P2 findings：
1. Archived verifier 的默认直接调用不是 zero-write，并会因自己生成的
   `.pyc` 使 verification 失败。
   - 在干净的 `21`-file 正式 package 上直接运行：
     `python <formal>/runtime_source/cross_exchange_trigger_parity_admission.py
     --output-dir <formal> --verify-only`。
   - Python import 在 package 内新增两个文件：
     `runtime_source/__pycache__/cross_exchange_liquidity_response_trigger.cpython-313.pyc`
     和
     `runtime_source/__pycache__/cross_exchange_liquidity_response_episodes.cpython-313.pyc`。
   - 文件数从 `21` 变为 `23`，命令返回 `rc=2`、
     `artifact closure drift`。QA 随后仅删除本次验证生成的 pycache，
     将正式 package 恢复为原始 `21` files。
   - 使用 `python -B` 时 source/archive 均 `rc=0` 且 package inventory
     与 mtime zero-write，但任务要求 verifier 本身满足 verify-only
     zero-write，不能依赖未冻结的调用方环境开关。
2. Stage 3 新增测试源码没有归档或绑定，不满足
   `.workflow/tasks/0815T002.md:175` 的
   “bind source/test/runtime hashes”。
   - `input_bindings.csv` 的 `current_runtime_source` 只有三个生产 source
     role，before/after 共 `6` rows。
   - 当前 Stage 3 的
     `test_cross_exchange_liquidity_response_trigger.py`、
     `test_cross_exchange_liquidity_response_episodes.py` 和
     `test_cross_exchange_trigger_parity_admission.py` 均不在 binding
     universe，也不在 package artifacts。
   - Package 内唯一 `test_*.py` 是 pre-extraction historical test
     snapshot，不能证明当前 `37 passed` 所对应的测试 bytes。

P3 findings：
1. `parity_manifest.json` 的 exact key/value schema 已验证，但 canonical
   raw JSON bytes 未冻结。
   - QA 将 manifest 改为单行 minified JSON，未改变语义值或 artifacts；
     source verifier 仍返回 `rc=0`。
   - Formal/A/B 当前 bytes 仍完全确定，因此本项不单独影响当前数值或
     parity；建议 bounded repair 时一并采用 canonical manifest byte
     comparison。

独立正向证据：
1. Shared detector 是纯计算模块。
   - 仅导入 Python stdlib；无文件、网络、subprocess、outcome、
     markout、PnL、Aug07 或 private/order 访问。
   - `CONTRACT_VERSION` 精确为
     `cross_exchange_queue_shock_trigger_v1`，`AUDIT_FIELDS` 精确 `36`。
2. Historical builder 真正调用 shared module。
   - constants、`AUDIT_FIELDS`、`TimelineState`、`BboState`、
     `_side_values` 和 `_candidate_from_burst` 均为 shared aliases。
   - `_process_segment()` 调用 `shared_trigger.detect_candidates()`；
     builder 中不存在第二份 candidate/primary state-machine copy。
3. 旧/新 detector 的 synthetic branch parity 精确一致。
   - accepted、no confirmation、mixed attribution、missing BBO、
     missing fast-L2、insufficient room 六类 audit dict 均完全相等。
   - 单元测试覆盖 fixed-first-trade burst、strict pre-state、
     threshold `>=0.30`、inclusive `100ms`、decision-time attribution、
     rejection precedence、reuse-before-dedup 和 all-rejected preservation。
4. QA 独立逐行读取全部 `463612` 条 historical audit 与正式 projection。
   - 每条比较全部 `36` 个 field texts，未只检查 confirmed/primary。
   - 总计 `267554` primary、`196058` rejected；field-text mismatch `0`，
     candidate-sequence mismatch `0`，projection 末尾无 extra rows。
   - Jul30：`268522 / 141768 / 126754`，row-stream SHA
     `e2b4e998b87c81c1f8350a92bd1253db35fcb396cabe557773364535190ceed4`。
   - Aug03：`127622 / 82533 / 45089`，row-stream SHA
     `73b8e5ae2ad6652c811e26c7df60d5cacba1a4febe812c0960450a7ea88fc309`。
   - Aug04：`67468 / 43253 / 24215`，row-stream SHA
     `fa537846e50760a50ee38882c8d39884d6f240f3f844c3ae8b1b66fe10367dcf`。
   - 19 个 segment 的 candidate/primary count 和 row-stream SHA 全部与
     frozen anchors 一致；attribution/rejection counts 逐 session 一致。
5. QA 从原始 detector/pre-state inputs 完成一套全新 production replay。
   - 输出：
     `/tmp/0815T002-qa-replay.0WLUIO/package`。
   - 构建返回 `463612` candidates、`267554` primaries、core SHA
     `837f633ee0de74b3c223eb3491b11399d1445dce9dd9b6f563778d2847569fb7`。
   - QA replay 与 Formal 的全部 `21` paths/bytes/SHA 完全一致，full
     inventory SHA 为
     `959eccb87a834ab32c5e3d6ae1413b60616f8cc083a66738e8173e9e594a7518`。
6. Formal、Build A、Build B 完全一致。
   - 均为 `21` files、`40,426,884` bytes；
   - full inventory SHA 均为
     `959eccb87a834ab32c5e3d6ae1413b60616f8cc083a66738e8173e9e594a7518`。
7. Fixture 的完整四文件 pre/post byte identity 通过。
   - episode gzip：
     `072fb1806fd2eec41fbd716364e8dbd68f44cfbdb9b29addae9ad3b3fd95a86a`
   - manifest：
     `683807fcd04cfe6340eb5f23cf984ebd2733f915b143eca56bbbcffa2acf0b2a`
   - summary：
     `c286db6860796320e6353356f2fc5170f065c5f74b20a6bca9c167166e3d4a1b`
   - trigger audit：
     `7a9bc1f8ab814a1371e73f8ff9323edfdb2bd7f7d78a6646b1b524d24ea81709`
8. Independent binding/on-disk audit 通过。
   - `188` rows，before/after 各 `94`，精确镜像。
   - Before scope：Stage dependencies `2`、baseline `1`、historical
     opaque inventories `3`、historical trigger truth `6`、detector inputs
     `79`、current runtime source `3`。
   - `88` file rows 与 `6` directory/aggregate rows全部重新检查；
     canonical path mismatch `0`，size/SHA/inventory mismatch `0`。
9. Accepted dependencies 和历史 package 当前保持不变。
   - Stage 1：`14` files / `1,401,387` bytes / full SHA
     `c540cc056313716b3bdd2b9c0fe076cda15a7f152b399ae6a1283b3aa8aa6590`。
   - Stage 2：`16` files / `19,834,728` bytes / full SHA
     `bdade16c53aed7bba54fdb9408ba3a8a03036d183720f589bda2a762448a3833`。
   - Jul30 historical：`11` files / `79,979,087` bytes / SHA
     `603565e840ff99fdf050f9c862c382524934ef8e2034cf5bc668bfdf8d7a6387`。
   - Aug03 historical：`13` files / `46,747,139` bytes / SHA
     `50a95175db0e0abc36604e1f6d3481d094270ee9ceebf24259356dfe8479e648`。
   - Aug04 historical：`4` files / `24,531,687` bytes / SHA
     `1a182c9ae346e6a3c2989734d18316ffc78e26bc3f40aeadc335f1a625c856c3`。
10. Negative controls 正常 fail closed。
    - coherent unknown binding scope：`rc=2`；
    - raw `/./` noncanonical detector path：`rc=2`；
    - CSV extra cell：`rc=2`；
    - canonical report drift：`rc=2`；
    - boundary flag changed to true：`rc=2`；
    - dropped rejected row、row reorder、field-text drift、historical
      projection replacement和Stage dependency drift由 focused tests 通过。

运行命令与结果：
1. Required Stage 3 suite：
   `python -B -m pytest -q
   examples/hyperliquid/test_cross_exchange_liquidity_response_trigger.py
   examples/hyperliquid/test_cross_exchange_liquidity_response_episodes.py
   examples/hyperliquid/test_cross_exchange_trigger_parity_admission.py`
   - `37 passed in 83.96s`
2. Alignment/density regressions：
   `python -B -m pytest -q
   examples/hyperliquid/test_cross_exchange_trigger_aligned_episode_contract.py
   examples/hyperliquid/test_cross_exchange_trigger_density_core.py
   examples/hyperliquid/test_cross_exchange_candidate_episode_merging.py
   examples/hyperliquid/test_cross_exchange_trigger_density_inputs.py
   examples/hyperliquid/test_cross_exchange_trigger_density_admission.py`
   - `183 passed in 8.99s`
3. Merging/recovery regressions：
   `python -B -m pytest -q
   examples/hyperliquid/test_cross_exchange_candidate_episode_merging.py
   examples/hyperliquid/test_cross_exchange_liquidity_response_case_hierarchy.py`
   - `33 passed in 0.18s`
4. Ruff：
   - `All checks passed!`
5. 外置 `PYTHONPYCACHEPREFIX` compileall：
   - `rc=0`
6. Formal package 全部 gzip：
   - `gzip -t`，`rc=0`
7. Scoped `git diff --check`：
   - `rc=0`
8. Source/archived `python -B ... --help`：
   - 均 `18` lines、`rc=0`、bytes 相同。
9. Source/archived `python -B ... --verify-only`：
   - 均 `rc=0`、相同 core/count；
   - verify 前后 `21` files 的 paths/bytes/SHA/mtime 相同。
10. Archived 默认 `python ... --verify-only`：
    - `rc=2`，新增两个 package-local pyc，违反 zero-write。

边界结论：
- 本轮没有读取任何历史 `episodes/*.csv.gz` 的 response/outcome rows。
- 对历史 package 只做 opaque compressed-byte inventory/hash；fixture
  episode rows仅用于允许的小型 pre/post byte comparison。
- 本轮没有读取 Aug07 raw/R0/R1/basis event rows。
- 本轮没有构建 Episode v3、feature、outcome、case distribution、model、
  score、calibration、incremental test 或 actionability。
- 本轮没有访问网络、AWS、SSH、private/account/order/cancel endpoint，
  没有启动采集、下单或修改 live config。
- 当前正式 package 的真实数据内容没有发现 Aug07/outcome/response
  artifact；P1 finding 3 是 verifier 对生产大小攻击副本的 fail-open。

验收结论：
- 未通过
- 结论说明：
  - Shared detector 提取、历史 builder integration、fixture byte parity
    和三 session 全量 `463612`-candidate detector parity 本身均成立。
  - 但是正式 package 仍依赖易失 `/tmp` baseline，archived verifier
    identity 未闭合，且未知 outcome-like artifact 可在 false boundary 下
    被 coherent rehash 接受。这三项均直接影响 Stage 4 依赖可信度和
    package 自包含可验证性，按裁决规则不能放行。

通过项：
1. Shared detector 纯度、版本、核心语义和历史 builder 复用通过。
2. 全候选、全 36-field、全 session/segment 历史 parity 通过。
3. Fixture、Formal/A/B 和 QA 独立重放 determinism 通过。
4. 当前 Stage 1/2、历史 package、detector inputs 的实际身份通过。
5. Focused tests、回归、Ruff、compileall、gzip 和 diff check 通过。

不通过项：
1. 正式 package 不具备脱离易失 `/tmp` baseline 的自包含 verification。
2. Archived admission runtime 可 coherent-rehash 后被 source verifier
   接受。
3. Artifact path universe 和 no-outcome hard boundary 可 coherent-rehash
   绕过。
4. Archived default verify-only 不是 zero-write。
5. 当前 Stage 3 test source/hash 未归档或绑定。

缺陷清单：
1. P0：无。
2. P1：外部 baseline 生命周期依赖。
3. P1：archived admission verifier identity closure 缺失。
4. P1：exact artifact allowlist/hard-boundary artifact scan 缺失。
5. P2：archived verifier 默认调用自写 pycache 并失败。
6. P2：Stage 3 tests 未进入 source/test/runtime binding。
7. P3：manifest canonical raw bytes 未冻结。

阻塞项：
- 无外部阻塞；均可在同一任务内 bounded repair。

建议总控下一步：
1. 不关闭 Stage 3，不开始 Stage 4；将 `0815T002` 退回同任务 bounded
   repair。
2. Verify-only 应直接验证 package 内
   `pre_extraction_source/`、`fixture/baseline_inventory.json` 和
   `fixture/pre|post/`，移除对外部 `/tmp` baseline root 的运行时依赖。
3. 冻结精确 artifact path allowlist，并对完整 artifact universe 执行
   Aug07/outcome/response/private/order path policy；未知文件必须 fail
   closed。
4. 将 archived admission bytes 与当前 source binding 精确关联，并把
   三个 Stage 3 test files 归档、绑定到 manifest/input bindings。
5. 在 sibling imports 前禁止 bytecode 写入，新增默认
   `python archived_cli ... --verify-only` 的 permanent zero-write test。
6. 重新构建 Formal/A/B，发布新 core/full SHA，再派发全新独立 re-QA。

报告文件一致性：
- 本报告与 `docs/qa-acceptance-report.md` 使用同一正文机械复制。
- 写入后执行 `cmp -s` 和 `shasum -a 256` 验证 byte-identical。
- 为避免文件自嵌自身哈希造成自引用变化，最终 SHA256 记录在 QA 完成
  回复中。

提交信息：
- commit：无
