# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0730T016

状态：
- 已通过

更新时间：
- 2026-07-30 23:59 CST

验收线程：
- QA验收线程

验收对象：
- 业务线程-python/cross-exchange-research + 0730T016 第二轮修复

验收范围：
- T016 首轮 QA 的 P1 x3/P2 x2 修复。
- R1 builder、failure-injection tests、真实八段 alignment 产物。
- Label 双重 publication 扫描、80 个 R0 输入稳定性、exact masks、
  warmup/intrahorizon labels、coverage/reconciliation/atomic gates。
- 本轮仅做本地只读验收，不修改业务代码或真实产物。

## Findings

- P0: none
- P1: none
- P2: none
- P3: none

验收步骤：
1. 审查 T016 task、第二轮 business report、builder、tests 和首轮 QA 缺陷。
2. 独立重放 label 截断、第二次扫描前同 schema/row 重压缩、normalized
   auxiliary sidecar 篡改、degraded reason 伪造后缀和 final rename failure。
3. 运行 focused、组合 regression、py_compile、CLI help 和 diff check。
4. 独立重算 56 source + 24 normalized R0 inputs 的 SHA/row count，并与
   80 行 provenance、segment manifest 和 source manifest 闭合。
5. 对八个 label gzip 逐行重放 Binance decision dedupe、same-segment
   timeline/BBO as-of、八组 primary/wall labels、warmup、boundary 和
   intrahorizon price-change 语义。
6. 独立重算 64 行 horizon coverage、24 行 reconciliation 和 10 条 exact
   masks，并核对 manifest 的所有 output SHA/row count。
7. 静态确认 builder/test 不含网络、AWS、SSH 或 collector 执行路径。

实际结果：
- 修复实现：
  - label scanner 每次实际重开 gzip，严格检查完整 schema、实际 row count
    和 SHA；在 segment 完成后和 publication 前各执行一次。
  - build 前后稳定性集合恰好包含 `56` source 和 `24` normalized R0
    outputs。
  - degraded reason 使用完整 canonical string equality。
  - warmup decision 继续生成 horizon target、primary、wall 和
    intrahorizon 字段，但只对 complete-feature eligible decision 计入
    coverage denominator。
- 独立 failure injection：
  - segment 返回后把 label 截断为 `0` 行，第一遍实扫报
    `frozen label row mismatch`；`clean-output` 旧产物保留，tmp 无残留。
  - 第一遍实扫后将 label 以相同 schema/row 重新压缩，仅改变文件 SHA，
    第二遍实扫报 `frozen label changed before publication`；旧产物保留。
  - processing 后篡改未被 label processor 读取的
    `hyperliquid_auxiliary_events.csv.gz`，报
    `R0 input changed during R1 build`。
  - forged degraded reason suffix 报 `missing exact degraded interval`。
  - final publish rename failure 后旧产物恢复，tmp/backup 均无残留。
- Tests：
  - T016 focused：`8 passed`
  - alignment/R0/timeline/supervisor 组合：`67 passed`
  - py_compile、CLI `--help`、`git diff --check`：pass
- Provenance：
  - `80/80` provenance key 与 manifest 精确闭合，无 missing/unexpected。
  - `56/56` source 和 `24/24` normalized R0 input 的实际 SHA/row count
    重算通过。
  - 当前 `source_hashes_unchanged=true`、
    `r0_output_hashes_unchanged=true`、`input_hashes_unchanged=true`。
- Label 全量独立扫描：
  - rows `2,366,631`
  - eligible `2,366,574`
  - unique warmup `57`
  - warmup reason：missing BBO `57`、missing timeline `2`、overlap `2`
  - decision identity/dedupe/order、as-of、future join、primary、wall、
    effective horizon、boundary、schema/SHA/row count problems：`0`
  - 57 个 warmup 共 `456` 个 horizon slots；target 和 intrahorizon
    audit fields `456/456`，primary `456/456` 可用。
  - warmup wall label `303` 个有 source；另外 `153` 个 target 前没有任何
    BBO，字段按 unavailable 留空，独立 source replay 一致。
  - warmup 不进入 denominator；quality eligible 总数与逐段 horizon
    eligible 口径一致。
  - intrahorizon true flags `12,149,677`
  - change-then-revert `114,616`
  - intrahorizon semantic mismatches `0`
- Exact masks：
  - `8` segment epochs + `2` degraded intervals 全字段 exact。
  - label 内 `1,330` 个 degraded decisions 与区间闭合，其中 `146` 个同时
    落入两个 auxiliary degraded intervals；flag/ID problems `0`。
- Coverage：
  - future join `0`，cross-segment label `0`。
  - 仅 `1000ms/2000ms` 达到每段 `95%`，最低分别为
    `95.66738691015544%`、`95.42929284122495%`。
  - `10-500ms` 最低逐段 coverage 为 `40.00%-68.26%`，保持 diagnostic。
- Reconciliation：
  - `24/24` 行逐指标独立重算一致，全部 gate pass。
  - Binance depth/bookTicker exact match 仅 `1.085%-3.531%`，但最低
    near-time coverage `99.9969%`，最差 near-time p99/max distance
    `7.190/40.580bps`，低于冻结 `10/60bps` 门限。
  - Hyperliquid fast/BBO exact match `5.643%-13.912%`，最低 near-time
    coverage `99.9451%`，最差 p99/max distance `13.277/49.335bps`，
    低于 `15/60bps` 门限。
  - 因此低 exact equality 被明确限定为异步 public-feed
    price-distance diagnostic；source age、near/stale、mismatch duration
    和 frozen distance gates 已量化，满足 R1 的 quantified and
    explainable 要求。
- Output closure：
  - 5 个 aggregate CSV 和 8 个 label gzip 的实际 SHA/row count 与
    manifest 一致。
  - 当前 alignment manifest SHA256：
    `6123b408cbdfdc95758c8d27e6e0664959caaebeb0de843a5978cfee6c6c8ffd`
  - alignment 大小约 `557M`。
- Boundary：
  - 未访问 network、AWS、SSH 或交易端点。
  - 未执行新增采集或信号拟合。
  - R6 新增采集仍需独立任务、用户明确授权和用户确认的活跃交易时段。

验收结论：
- 已通过
- 结论说明：
  - T016 首轮 P1 x3/P2 x2 已全部关闭，未发现新的 P0-P3。真实 R1
    alignment 的 publication、provenance、mask、label、coverage、
    reconciliation 和 atomic gates 均可独立复算并 fail closed；R1 完成。

通过项：
1. Label publication 两次实际 schema/row/SHA 重扫和 clean preservation。
2. 56 source + 24 normalized R0 input 的 build 前后稳定性。
3. Exact degraded reason、mask application 和 segment boundary。
4. 57 warmup 的完整 horizon 审计字段及 denominator 排除。
5. Intrahorizon price-update 与 114,616 个 change-then-revert 表达。
6. 2,366,631 labels、80 provenance、24 reconciliation 全量闭合。
7. Per-segment 95% coverage、future/cross-segment 和 atomic publication。
8. 本地-only 与新增采集授权边界。

不通过项：
1. 无。

缺陷清单：
1. P0：无。
2. P1：无。
3. P2：无。
4. P3：无。

阻塞项：
- 无。

残余风险：
- `10-500ms` 仍是 diagnostic horizon，不能作为 R2/R3 primary label。
- Exact top equality 不是同步真值；R1 只接受已冻结的 same-host
  local-time、source-age 和 price-distance 口径。
- 当前四小时单 session 只能支持后续信号研究，不能证明 maker PnL、精确成交
  或生产可用性。

建议总控下一步：
1. 将 `0730T016` 标记为`已通过`，将 R1 标记为完成。
2. 按研究计划解锁下一项本地-only R2/R3 工作；继续保持 R6 采集授权边界。

提交信息：
- commit：无
