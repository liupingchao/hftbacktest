# QA 验收结果

执行线程：
- 独立 QA 验收线程

任务ID：
- 0720T027

状态：
- 已通过

更新时间：
- 2026-07-20 20:30 CST

验收线程：
- 独立 QA 验收线程

验收对象：
- 业务执行线程 / `0720T027`
- implementation commit `b8e008afc90a25ffb155f09bb5d77aa90cdbb4c4`
- report commit `326b30fd1610f24b921ae5e02678853104220226`
- immutable T026 evidence source `40dc56a3225df4afb0f2185873c91f17b578f550`

验收范围：
- Exact submit-response rejected terminal 合同及 fail-closed 边界。
- Rejected、cancel/query、fill 四类 per-attempt terminal path 的互斥性。
- v2/v3/v4 历史 schema 兼容与 v5 启用条件。
- Fill-feedback canonical submitted row、rejected denominator 排除和冲突 quarantine。
- Acceptance 独立重建、T026 exact bridge 范围及伪造反例。
- 六个 comparison CSV 的 `PYTHONHASHSEED` byte determinism。
- T026、T016、T022 exact offline replay、输入 hash、terminal checksum 和 offline boundary。
- Implementation/report commit scope、编译、focused/full Hyperliquid regression。

验收步骤：
1. 读取 workflow manual、QA template、任务文件、业务报告和 T026 前序 QA；核对任务边界与 stop conditions。
2. 审查 implementation/report commit 的实际 diff，沿 producer、acceptance、fill-feedback 三条数据流检查实现。
3. 独立运行 `py_compile`、focused 四文件测试和完整 `examples/hyperliquid` 测试。
4. 构造 non-string error、缺失/伪造 manager 字段、result side/type/key mismatch、reject 加 cancel/query/fill 等 hostile cases。
5. 对 T026 用两个 hash seed 精确离线 replay，并逐字节比较六个 comparison CSV。
6. 精确离线 replay T016/T022，确认历史 lifecycle blocker 和 schema 未被升级。
7. 在 `/tmp` 副本中攻击 T026 bridge 的 malformed response、伪造旧 v4 shape、错误 task 和错误 source。
8. 独立重建 T026 fill-feedback，并复核三个 evidence root 的 checksum、aggregate hash 和工作树边界。

实际结果：
- 未发现 P0、P1、P2 或其他验收阻断缺陷。
- `py_compile`：通过。
- Focused regression：`451 passed in 19.13s`。
- Full Hyperliquid regression：`955 passed in 39.46s`。
- Exact T026 replay 两次均 exit `0`：
  - provenance `112/112`
  - config `72/72`
  - decision `43/43`
  - lifecycle `64/64`
  - economics `6/6`
  - `legacy_submit_rejected_bridge_authorized=true`
  - `legacy_submit_rejected_bridge_applied=true`
- T026 六个 comparison CSV 在 `PYTHONHASHSEED=1` 和 `7` 下逐字节一致。
- Exact T016 replay exit `2`：decision `43/43`，lifecycle `52 pass / 12 fail`，schema 保持 v2，bridge 未授权/未应用。
- Exact T022 replay exit `2`：decision `43/43`，lifecycle `52 pass / 12 fail`，schema 保持 v3，bridge 未授权/未应用。
- Existing v4 regression 保持通过；v5 仅在 raw exact reject 或 producer v5 contract 存在时启用。
- 额外 hostile 单元反例全部 fail closed：
  - non-string/empty error
  - outer failure
  - multiple/conflicting status
  - attempt/attempt_id/side/key/cloid mismatch
  - 缺失或伪造 manager action/state/query/endpoint/side
  - result side/type mismatch
- Exact reject 加 cancel 或 terminal query 时 reconciliation 为 `fail_closed`，但 independently validated raw reject count 保持 `1`。
- Exact reject 加 reference-bound fill 时 acceptance 输出 `fill_conflicts_with_submit_rejection:1` 并阻断。
- Malformed authoritative pullback response：bridge authorized/applied 均为 `false`，exit `2`。
- 伪造旧 v4 producer shape：bridge scope authorized 为 `true`，但 applied 为 `false`，exit `2`。
- 错误 task 或 source：bridge authorized/applied 均为 `false`，exit `2`。
- T026 fill-feedback 独立重建：2 lifecycle rows、quarantine `0`；buy 为 `submitted=true`、`rejected=true`、`excluded_rejected`、`included_in_feedback=false`；sell 保留 resting/canceled censoring 事实。
- Conflicting submitted rows 仍产生 `conflicting_duplicate_submitted_attempt_lifecycle` quarantine，lifecycle integrity 为 `fail_closed`。
- Acceptance 仅使用标准库和本地 Git/文件输入，未导入 producer reconciliation helper。
- T016/T022/T026 terminal SHA-256 manifest 均为 `104/104 OK`。
- 输入 aggregate hash 复核后未变化：
  - T016 `a5443f8f9cb777509ffde21f9caed3837a9ef58f31b25cd82acef2d6a3991398`
  - T022 `71c9ba35f62e6657d0d48f63abb451ad538f14d85d7113a567b16dfe2b92833c`
  - T026 `1f5e394cc14ab2bd7cff4e14fc05df945d866276c55330ea6ccd28773599083a`
- Implementation commit 只修改任务列明的 4 个实现文件和 4 个测试文件；report commit 只修改 task/business/tracker 文件。
- 本次 QA 未触发 live、private/account、order、cancel、network、remote 或 service 操作；所有生成物仅写入 `/tmp` 和本 QA 报告。

验收结论：
- 已通过
- 结论说明：
  - T026 rejected-terminal、fill-feedback canonical-row 和 deterministic-output 三项前序 findings 已关闭。
  - Exact submit rejection 只有在完整 request/manager identity 合同时才成为 authoritative terminal；所有歧义和冲突均保持 fail closed。
  - T026 可在不修改 immutable evidence 的前提下被 exact bridge 接受，T016/T022 历史 blocker 未被升级。

通过项：
1. Exact synchronous rejected terminal 合同和 raw reject count 通过。
2. Rejected 与 cancel/query/fill terminal path 互斥通过。
3. v2/v3/v4 兼容和 v5 条件启用通过。
4. Fill-feedback submitted row 选择、rejected 排除和冲突 quarantine 通过。
5. Acceptance 独立重建和 exact T026 bridge 范围通过。
6. 六个 CSV hash-seed determinism 通过。
7. T026/T016/T022 exact replay、checksum、input immutability 和 offline boundary 通过。
8. Focused/full regression 和 commit scope 通过。

不通过项：
1. 无

缺陷清单：
1. 无

阻塞项：
- 无

建议总控下一步：
1. 将 `0720T027` 更新为 `已通过`。
2. 可按 Principal 对齐串行计划解锁 Task 8；dynamic spread、fill-feedback、inventory skew 或 multi-level 的实际激活仍必须遵循各自后续 formal task envelope。

提交信息：
- commit：无
- QA 未提交代码或报告 commit。
