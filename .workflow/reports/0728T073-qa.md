# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0728T073

状态：
- 未通过

更新时间：
- 2026-07-28 18:24 CST

验收线程：
- QA验收线程

验收对象：
- 业务执行线程 / 0728T073

验收范围：
- EC2 hunt archive upload、setup clock receipt、S3 object verification、
  cleanup failure semantics、deterministic build provenance、final Canary 和
  AWS 零遗留。

验收步骤：
1. 独立检查 staged code、任务/业务报告、tracked evidence 和本地 raw
   Canary/full-run 证据。
2. 独立重建 source archive hashes、raw summaries、archive hashes 和
   state cleanup post-condition。
3. 运行 focused Python/Rust tests，并对 S3 403、receipt 缺失等失败路径
   做额外故障注入。
4. 使用只读 AWS describe/head/list 查询复核当前资源状态。

实际结果：
- deterministic source archive 和逐文件 Cargo/source hashes 一致；
  rebuilt binary SHA 与 T072 accepted binary 完全相同。
- final Canary setup/run 均 `2/2 Success`，archive size/SHA `2/2`，
  raw-summary rebuild `4/4`，当前 AWS 资源为零。
- `HeadBucket=403 AccessDenied` 被 cleanup 当作 bucket 不存在，故障注入
  实际得到 `verified=true`。
- setup clock JSON 未被 orchestrator 解析为成功门禁。
- upload/signal/setup failure 测试没有执行到业务报告所声称的完整故障
  语义。

验收结论：
- 未通过
- 结论说明：
  - 实际 Canary 已清理干净，但可复用 cleanup 仍可能在无法确认 S3
    状态时写出成功，不满足核心 fail-closed 标准。

通过项：
1. T072 accepted 10-host data/build provenance 已闭环。
2. Final Canary 的实际 upload/object/download/rebuild/cleanup 结果通过。
3. 当前只读 AWS 查询为零遗留。
4. Python `11 passed`、Rust `15 passed`、fmt/clippy/diff check 通过。

不通过项：
1. P1：S3 查询 403/权限/网络错误可能被误判为 bucket 不存在。
2. P2：setup clock receipt 缺失/截断不阻止 execution。
3. P2：故障测试覆盖声明过强。

缺陷清单：
1. 明确区分 S3 404/NoSuchBucket 与其他错误；只有前者代表不存在。
2. 对 setup stdout 解析完整 clock JSON，验证 gate/bound/identity，并写
   verified receipt event。
3. 增加可执行 PUT failure、KeyboardInterrupt、clock failure journal
   和 HeadBucket 403 regressions。

阻塞项：
- 无外部阻塞。

建议总控下一步：
1. 新建 T074 做上述 fail-closed 修复。
2. 用最终代码再跑 2 台短 Canary 后重新进入独立 QA。

提交信息：
- commit：无
