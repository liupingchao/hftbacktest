# QA 验收结果

## Findings（按严重性）

- P0：无。
- P1：无。
- P2：无。
- P3：业务报告提交后仍写 `commit：待提交`，本 QA 同步校正为
  `ace486f6a54fbcd2cf25a5aec03617d3cb106e42`。

执行线程：
- QA验收线程

任务ID：
- 0722T063

状态：
- 已通过

更新时间：
- 2026-07-22 16:48 Asia/Shanghai

验收线程：
- QA验收线程

验收对象：
- 业务执行线程 `0722T063`
- implementation commit
  `ace486f6a54fbcd2cf25a5aec03617d3cb106e42`
- prior failed QA
  `96176ded`

验收范围：
- T062 三项 P2 repair：
  exact frozen-contract validation/hash、legacy side-mapping parity、
  inherited/source boundary truthfulness。
- Official numerical artifact invariance 和 no-submit safety boundary。
- 不验收 live、private/account、order/cancel、economics 或 promotion。

验收步骤：
1. 阅读 T062 QA、T063 task/business 和 implementation diff。
2. 独立审查 exact top/nested field sets、identity/training metadata、
   expected canonical hash gate 和 regression coverage。
3. 独立审查 legacy invalid/missing/valid side-mapping behavior。
4. 独立审查 current-task boundary 与 T061 source snapshot 的区分。
5. 运行 mandatory focused kernel/shadow tests。
6. 核对 official artifact diff 和 endpoint boundary。

实际结果：
- Exact contract field set、normalization/coefficient nested field set、
  `task_id=0722T061`、`source_task_id=0627T001`、normalization、
  effective horizon、basis definition、formula、caveat、
  `training_row_count=10704`、sample IDs 和 per-feature source row count
  全部 fail closed。
- Basis-enabled kernel 缺 expected hash 时 block，错误 hash 时 block，
  official canonical hash
  `675c86d43625713bec580e777d2c1e83471b26d958660af26e9689b709baacef`
  通过。
- Legacy invalid mapping 恢复
  `ValueError: unsupported_side_mapping`；missing mapping 恢复
  `KeyError("side_mapping")`；valid path行为不变。
- Boundary 顶层明确
  `shared_kernel_changed_in_task=true`；T061 内容仅位于
  `source_t061_boundary_snapshot`，并有
  `source_boundary_snapshot_task_id=0722T061`。
- 相对 T062 implementation `da0a6198`，official decision rows、
  per-window summary 和 warning propagation 无 diff；仅 boundary manifest
  发生预期变化。
- Recommendation、`10704` would-submit、edge summary、max contribution 和
  六项 warning 不变。
- No-submit、no private/order/cancel/credentials 和 repo-relative path
  boundary 保持。
- Independent focused：
  `28 passed in 0.10s`，exit `0`。
- Business full suite：
  `1283 passed, 2 skipped in 59.02s`。
- QA 未执行 network、remote、private/account、order/cancel、live 或
  service 操作。

验收结论：
- 已通过。
- 结论说明：
  - T063 完整关闭 T062 三项 P2，basis regression shared-kernel public
    shadow 现可在 default-off/no-submit 边界内验收；这仍不是 live
    economics 或 promotion proof。

通过项：
1. Exact frozen-contract schema/identity/training/hash gate。
2. Legacy side-mapping valid/invalid/missing parity。
3. Current-task 与 source-task boundary truthfulness。
4. Official numerical artifact、warning 和 recommendation invariance。
5. Focused/full regression 与 no-submit safety boundary。

不通过项：
1. 无。

缺陷清单：
1. 无未修复缺陷。

阻塞项：
- 无。

建议总控下一步：
1. 第二阶段 basis regression alpha + production-equivalent public shadow
   可关闭。
2. 下一正式任务先建立 dynamic-spread/fill-feedback/economics live evidence
   preflight；真实订单必须取得新的 exact live envelope 授权。

提交信息：
- QA commit：由本报告提交后的线程回报提供。
- Implementation commit：
  `ace486f6a54fbcd2cf25a5aec03617d3cb106e42`

