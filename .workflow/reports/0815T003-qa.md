# QA 验收结果

执行线程：
- QA验收线程（全新独立 Stage 4 验收）

任务ID：
- `0815T003`

状态：
- 未通过

更新时间：
- 2026-08-15 23:59 CST（星期六）

验收线程：
- QA验收线程

验收对象：
- 业务线程-python/research + `0815T003`
- 正式 package：
  `local_live_analysis/skhynix_trigger_aligned_episode_research_v1_stage04_jul30_episode_v3/`
- 实现：
  - `examples/hyperliquid/cross_exchange_trigger_aligned_episodes.py`
  - `examples/hyperliquid/cross_exchange_jul30_episode_v3_admission.py`
  - 两个对应 focused test files
- QA fresh rebuild：
  `/tmp/0815T003-qa.EandeD/rebuild/package`
- QA hostile evidence：
  `/tmp/0815T003-qa-attacks.vRdvR5/`

验收范围：
- 仅 Jul30 immutable public-data inputs。
- 不解析 Jul30 legacy `episodes/*.csv.gz`。
- 不读取 Aug03/Aug04 future event rows 或任何 Aug07 event rows。
- 不运行 case/model/score/actionability/orders/network/live config。
- 历史采集和 accepted Stage 1/2/3 package 只读。
- 除本报告及其 mirror 外，不修改业务代码、测试、研究包、任务或总控
  tracking 文件。

## Findings First

普通代码审查：
- P0：`0`
- P1：`4`
- P2：`1`
- P3：`0`

### P0 Findings

- 无。

### P1 Findings

#### P1-1 Family B confirmation features 的 provenance 不是 detector confirmation state

判定：
- 未通过。`141768/141768` 个 confirmed candidate 的一组 Confirmed-time
  `T` features 使用了早于真正 confirmation 的 Binance `bookTicker`
  事件作为统一 `observed_at/source_event_id`。
- 数值虽然来自 candidate/detector audit，但 provenance 声称这些值由
  一个无关的 BBO-asof 事件观测到，违反逐 feature
  `value/observed_at/source_event_id/source_book_version` contract。

代码证据：
- Detector confirmation 是 common L2 timeline 中首次满足 depletion/drop
  条件的 `TimelineState`：
  `examples/hyperliquid/cross_exchange_liquidity_response_trigger.py:258-280`。
- Detector 把该 state 的 `decision.ts_ns` 和确认字段写入 audit：
  `examples/hyperliquid/cross_exchange_liquidity_response_trigger.py:338-385`。
- Stage 4 却执行
  `binance_bbo.asof_index(confirm_ts)`，然后把该 bookTicker 的时间和
  source ID 赋给全部 confirmation-derived features：
  `examples/hyperliquid/cross_exchange_trigger_aligned_episodes.py:2868-2883`。
- Verifier 仅检查 `observed_at <= landmark`：
  `examples/hyperliquid/cross_exchange_trigger_aligned_episodes.py:4886-4914`；
  它不检查 source event 是否真正生成该 detector value。
- 方案要求完整 provenance，而非仅时间不晚于 landmark：
  `docs/skhynix_trigger_aligned_episode_research_implementation_plan.md:617-643`。

QA 命令与证据：
- 命令：`python -B - <<'PY'`，独立流式连接正式 Family B feature ledger、
  anchors、Stage 3 candidate projection 和 Jul30 common timeline。
- 全量结果：
  - confirmed rows：`141768`
  - confirmation-attribution feature rows：`141768`
  - published `observed_at_ns < t_confirm_ns`：`141768`
  - published `observed_at_ns == t_confirm_ns`：`0`
- 最小复现：
  - candidate：`jul30:segment_0001:1`
  - published observed_at：
    `1785369289572296568`
  - true detector `t_confirm_ns`：
    `1785369289588021657`
  - 提前：`15725089 ns`
  - published source：
    `jul30:segment_0001:binance_book_ticker:78:81:0`
  - raw common-timeline confirmation row 位于真正 `t_confirm_ns`，其
    `trigger_kind=depth_update`，不是上述 bookTicker。

影响：
- Family B 不能声称这些 confirmation fields 的 source provenance
  可审计。
- 后续 case distance、retrieval、model 或因果检查会把错误 source event
  当作 detector decision 的生成事件。

#### P1-2 `confirmed_burst_*_through_decision` 扫入 detector burst 终止后的成交

判定：
- 未通过。Stage 4 没有复用 frozen fixed-origin detector burst，而是从
  burst start 扫到 confirm，重新收集期间所有同向 trade。
- 当原 burst 已因 fixed-origin gap 或 opposite trade 终止时，后续同向
  trade 会被错误并入 Confirmed-time trigger severity。

代码证据：
- Frozen detector burst partition 以 burst 首笔为固定原点，并在 opposite
  side 或超过 burst window 时结束：
  `examples/hyperliquid/cross_exchange_liquidity_response_trigger.py:159-204`。
- Detector 的 through-decision quantity 只遍历该 frozen burst：
  `examples/hyperliquid/cross_exchange_liquidity_response_trigger.py:351-363`。
- Stage 4 重新扫描
  `[candidate.burst_start_ts_ns, confirm_ts]` 的全部 Binance trades，并只按
  direction sign 过滤：
  `examples/hyperliquid/cross_exchange_trigger_aligned_episodes.py:2793-2816`。

QA 命令与证据：
- 命令：`python -B - <<'PY'`，独立按 shared detector
  `iter_trade_bursts()` 重建 Jul30 burst membership，再与全部 Family B
  ledger 比较。
- 全量结果：
  - Family B：`141768`
  - `confirmed_burst_trade_count_through_decision` 与 detector burst prefix
    不一致：`32711`
- 最小复现：
  - candidate：`jul30:segment_0001:14`
  - detector burst trade count：`57`
  - detector burst end：`1785369291137129501`
  - published confirmed burst count：`73`
  - 被错误加入的 burst-end 后同向 trades：`16`
  - first extra trade：`1785369291142384566`
  - confirm：`1785369291155663463`

影响：
- Confirmed-time `T` severity 和 continuation features 不再与 detector
  truth/contract 同义。
- 后续 dose-response 会把独立后续 flow 错当成原 trigger burst 的剂量。

#### P1-3 target-trade first-event interval 的 lower bound 可早于 Candidate

判定：
- 未通过。用于“Candidate 后首次 target trade at/through vulnerable quote”
  的 interval lower bound 在大量行中取了 Candidate 前最后一笔 trade。
- 对 post-trigger first-event outcome，Candidate 前 trade 不是
  post-trigger non-event observation；风险区间必须至少从 Candidate
  左截断。

代码证据：
- `_first_event_interval()` 使用 `store.asof_index(start_ns)` 初始化
  `last_non_event`：
  `examples/hyperliquid/cross_exchange_trigger_aligned_episodes.py:2983-3018`。
- target-trade outcome 直接将 `hl_trades` 传给该通用函数：
  `examples/hyperliquid/cross_exchange_trigger_aligned_episodes.py:3114-3128`。
- Frozen contract 声称区间为
  `(last_non_event_observed_at,first_event_observed_at]`：
  `examples/hyperliquid/cross_exchange_trigger_aligned_episodes.py:4017-4023`。
- 方案的 first-event contract 位于
  `docs/skhynix_trigger_aligned_episode_research_implementation_plan.md:773-803`。

QA 命令与证据：
- 命令：`python -B - <<'PY'`，流式扫描全部 outcome rows，并按
  `t_candidate_ns` 检查 target-trade interval support。
- 全量结果：
  - interval-censored target-trade outcomes：`206784`
  - lower `< t_candidate_ns`：`115637`
  - lower `== t_candidate_ns`：`0`
  - lower `> t_candidate_ns`：`91147`
- 最小复现：
  - candidate：`jul30:segment_0001:1`
  - `t_candidate_ns=1785369289560809650`
  - lower：`1785369289446294264`
  - upper：`1785369289787703544`
  - lower 比 Candidate 早 `114515386 ns`

影响：
- 发布的 post-trigger time-to-first trade 分布包含负时间支持。
- 后续 interval likelihood、survival 和 latency/actionability 研究会消费
  错误区间。

#### P1-4 Admission 只验证自洽形状/哈希，不能 fail closed 到 source semantics

判定：
- 未通过。QA 对高基数 anchor/path/outcome 字段做 coherent mutation，
  同步刷新 artifact records、core hash 和 manifest 后，source 与 archived
  verifier 均返回 `verified=true`。
- 这违反任务要求的 source-reconstructable admission。哈希只能证明包
  自洽，不能证明内容仍等于 accepted Jul30 source truth。

代码证据：
- Manifest/artifact/core 自洽检查：
  `examples/hyperliquid/cross_exchange_trigger_aligned_episodes.py:4535-4555`。
- Anchor verifier 检查 identity、order、Family linkage 和 shape，但没有
  从 Stage 3/source 重建 `classification` 或
  `quality_flags_json`：
  `examples/hyperliquid/cross_exchange_trigger_aligned_episodes.py:4558-4715`。
- Sparse range verifier 不重建 first/last/count：
  `examples/hyperliquid/cross_exchange_trigger_aligned_episodes.py:4719-4745`。
- Event-count verifier 检查非空、distinct 和时间排序，但不重建 exact
  source event identity：
  `examples/hyperliquid/cross_exchange_trigger_aligned_episodes.py:4824-4864`。
- Outcome verifier 检查 canonical type/availability，但不从 source
  重算 markout：
  `examples/hyperliquid/cross_exchange_trigger_aligned_episodes.py:4646-4715`。

QA hostile 命令：
- `python -B - <<'PY'`：为每种 mutation 建立 production-size APFS clone，
  deterministic gzip 重写目标 artifact，刷新 manifest artifact
  bytes/SHA 与 `core_package_sha256`，再分别运行：
  - `python -B examples/hyperliquid/cross_exchange_jul30_episode_v3_admission.py --package-dir <attack> --verify-only`
  - `python -B <attack>/runtime_source/cross_exchange_jul30_episode_v3_admission.py --package-dir <attack> --verify-only`

被错误接受的攻击：
1. `classification` coherent relabel：
   - 将 confirmed anchor 改为 `arbitrary_relabel`
   - source/archive：`rc=0`, `verified=true`
   - core：
     `94cf76328195befed7b2807934632848d9f1bdee7661ad815161325195b8eaec`
2. corrected degraded-evidence deletion：
   - target：`jul30:segment_0002:13781`
   - 原 `auxiliary_degraded_at_candidate=true`
   - 原 interval：
     `segment_0002:main_all_mids:4`
   - 将 anchor 中 degraded bool/interval IDs 删除并 coherent rehash
   - source/archive：`rc=0`, `verified=true`
   - core：
     `8ac884e692ae68ffb5491c9e0c7a9e6f083210cd02eefa5e5237c85194560403`
3. joint semantic path/outcome mutation：
   - sparse range `binance_trade_count += 1`
   - event-count `event_1_source_event_id` 改为不存在的 source ID
   - `target_midpoint_markout_250ms_bps += 1`
   - attack root：
     `/tmp/0815T003-qa-attacks.vRdvR5/semantic_paths`
   - source/archive：`rc=0`, `verified=true`
   - core：
     `66cc5d5b445fc9cc557a9012facb3d7a87d3cca361253d727b02a669e73c95c2`

`classification` 专项判断：
- 当前正式包 `classification` 在全部 `268522` anchors 上与 Stage 3
  detector `attribution` 文本一致，mismatch=`0`。
- Builder 明确执行 `classification = candidate.attribution`：
  `examples/hyperliquid/cross_exchange_trigger_aligned_episodes.py:3400-3402`。
- Frozen contract 只把 `classification` 列入 anchor schema，没有冻结
  “classification := detector attribution”的值域、source 和 semantic
  mapping：
  `examples/hyperliquid/cross_exchange_trigger_aligned_episodes.py:3984`，
  contract artifact `frozen_episode_v3_contract.json:1-28`。
- 因而当前实现是未定义语义的字段别名；任意 coherent relabel 仍通过。
  按用户要求，这不能视为符合冻结 classification contract。

正确 fail-closed 的攻击：
- source-age 删除：source/archive `rc=2`，invalid integer。
- `quality_intervals.csv` 删除 accepted interval：source/archive `rc=2`，
  `quality interval publication drift`。
- manifest exact count 篡改：source/archive `rc=2`。
- archived verifier source coherent mutation：source/archive `rc=2`，
  fixed worktree archive identity drift。
- forbidden input binding：source/archive `rc=2`。
- partial artifact deletion：source/archive `rc=2`。
- forbidden Aug07 artifact：source/archive `rc=2`。

影响：
- 当前正式包多数抽样值可从 source 重建，但 verifier 无法保证它们在
  publication 后仍保持该语义。
- 任何影响 contract、因果或可重建性的 coherent data mutation 都可能被
  错误宣告为 accepted package。

### P2 Findings

#### P2-1 同 receipt timestamp 时，outcome baseline 可借用 trigger-time event

判定：
- 当前 Jul30 数据未触发，但实现存在 latent causal boundary defect。
- QA 检查 8 个 segment，Candidate shock 与 Hyperliquid BBO/trade/common
  timeline 的 exact same-nanosecond collision 均为 `0`，因此正式包当前
  行没有由此造成的实测 corruption。
- 仍需在 contract/builder/admission 中 fail closed，否则同 timestamp
  输入会把 trigger 后同批事件作为 vulnerable baseline。

代码证据：
- `NumericEventStore.asof_index()` 使用 `bisect_right(...)-1`，包含
  `ts == shock_ts`：
  `examples/hyperliquid/cross_exchange_trigger_aligned_episodes.py:873-877`。
- Outcome baseline 使用该 inclusive as-of：
  `examples/hyperliquid/cross_exchange_trigger_aligned_episodes.py:3035-3044`。
- Shared detector 的 pre-trigger BBO 使用严格
  `bisect_left(...)-1`：
  `examples/hyperliquid/cross_exchange_liquidity_response_trigger.py:282-283`。

最小复现命令：
- `python -B -c "import bisect; t=[99,100]; print(bisect.bisect_right(t,100)-1, bisect.bisect_left(t,100)-1)"`
- 输出：`1 0`。当前 Stage 4 baseline 选 timestamp `100`，严格
  pre-trigger baseline 应选 timestamp `99`。

### P3 Findings

- 无。

## 独立通过证据

### 1. Fresh Rebuild、Inventory 和正式 Hash

命令：
- `python -B examples/hyperliquid/cross_exchange_trigger_aligned_episodes.py --output-dir /tmp/0815T003-qa.EandeD/rebuild/package`
- `python -B - <<'PY'`：独立枚举 formal/fresh 全部 relative paths，
  逐文件比较 type、size、raw SHA，并重算 manifest/core/full inventory。

结果：
- fresh rebuild 完成全部 8 segments。
- Formal/fresh 均为：
  - artifacts：`106`
  - files：`107`
  - bytes：`1562104138`
  - Family A：`268522`
  - Family B：`141768`
  - rejected：`126754`
- Relative path、raw bytes、size、逐文件 SHA 全量一致。
- Formal/fresh manifest SHA256：
  `c94150a71520db7db1f1bba36f9de5716514d1e3cff4cf12299bebe271d08266`
- Formal/fresh core SHA256：
  `14d945253ecae82cdb73bdc365ae01ae6f614b8ed7fc9ea56e08d65c4ba77398`
- Formal/fresh full inventory SHA256：
  `25c02766e4eb263d4ba4bf20cfe48b37a9812f20d7eb402ddbcffb7ffe450785`
- Contract SHA256：
  `2239a170e670b8af7bca901524101b0beb3abc834deadfff714137f1b83d7347`
- Fresh `input_bindings.csv`：`788` rows，before/after 各 `394`，逐行一致。
- 没有 forbidden path；scope 仅含 accepted Stage 1/2/3、Jul30
  raw/R0/R1 和冻结 Stage 4 source/tests。
- Fresh build 后无残留 staging dir，package 内无 `.pyc/__pycache__`。

Atomic publication injection：
- QA 在 `/tmp/0815T003-qa-partial.hapvwo` 注入 `_process_segment`
  中断并先写 partial marker。
- Builder 抛错后 output 不存在，staging 被清理；未暴露 partial package。

### 2. Focused、Inherited、Static 和 Integrity Tests

命令与结果：
1. Stage 4 focused：
   - `python -B -m pytest -q -p no:cacheprovider examples/hyperliquid/test_cross_exchange_trigger_aligned_episodes.py examples/hyperliquid/test_cross_exchange_jul30_episode_v3_admission.py`
   - `12 passed in 0.06s`
2. Stage 1/2/3 relevant inherited：
   - `python -B -m pytest -q -p no:cacheprovider`，覆盖 trigger contract、
     density core/inputs/admission、candidate merging、liquidity trigger、
     parity admission。
   - `216 passed in 131.75s`
3. R0/alignment/recovery relevant inherited：
   - 覆盖 l2 timeline、alignment acceptance、research dataset、
     liquidity response episodes、case hierarchy。
   - `83 passed in 0.47s`
4. Ruff：
   - `ruff check` 四个 Stage 4 source/test files。
   - `All checks passed!`
5. Compile：
   - 外置 `PYTHONPYCACHEPREFIX=/tmp/0815T003-qa-pycache.HBiJDQ`
     执行 compileall。
   - `48` 个 bytecode files，`rc=0`。
6. Scoped diff：
   - `git diff --check` Stage 4 source/tests，`rc=0`。
7. Gzip：
   - `find <formal> -type f -name '*.gz' -print0 | xargs -0 -n 1 gzip -t`
   - gzip files：`96`
   - `gzip_test_rc=0`
8. CLI：
   - Builder `--help` 和 admission `--help` 均 `rc=0`。

### 3. Default Python Zero-Write 和只读边界

命令：
- 默认 `python`、无 `-B` 分别运行 source 和 archived admission：
  - `python examples/hyperliquid/cross_exchange_jul30_episode_v3_admission.py --package-dir <formal> --verify-only`
  - `python <formal>/runtime_source/cross_exchange_jul30_episode_v3_admission.py --package-dir <formal> --verify-only`
- 前后对 formal root 全量 snapshot：
  path/type/bytes/SHA/mtime_ns/ctime_ns。

结果：
- Source admission：`rc=0`, `verified=true`。
- Archived admission：`rc=0`, `verified=true`。
- 两者均返回正式 core/full 和 A/B/rejected counts。
- Snapshot entries before/after：`129/129`。
- `snapshot_identical=true`。
- package pycache paths：`[]`。
- Fresh input inventory before/after 完全一致，未修改 accepted
  dependencies、Jul30 raw/R0/R1 或正式 package。
- Builder/import AST 未发现 network client；未访问网络、AWS、SSH、
  private/account/order/cancel endpoint。

### 4. Family A/B 和 Selection Bias

命令：
- `python -B - <<'PY'`：流式连接 Stage 3 Jul30 projection、anchors、
  Family A、Family B 和 outcomes，检查顺序、ID、确认状态和 rejection。

结果：
- Stage 3 truth candidates：`268522`
- Stage 3 primary/confirmed：`141768`
- Stage 3 rejected：`126754`
- Anchors / Family A / outcomes：
  `268522 / 268522 / 268522`
- Family B：`141768`
- Exact candidate order/linkage：true
- duplicate/missing/extra ID：`0`
- rejected non-null `t_confirm_ns`：`0`
- rejection reason mismatch：`0`
- 当前 classification 与 Stage 3 attribution mismatch：`0`
- clusters / flows / 2000ms overlap blocks：
  `39928 / 10536 / 9`

判定：
- 当前正式包的 Family A outcome 不是 confirmation-selected；全部 rejected
  candidates 均有 outcome/censor row。
- Family A/B population 和 selection-bias 要求本身通过。

### 5. Sparse Range、Grid、Event Count 和 Outcome 抽样重建

命令：
- `python -B - <<'PY'`：从 accepted structured Jul30 source 独立重建
  选定 segment/candidate 的 exact sparse first/last/count、fixed-grid
  strict-as-of、event-count source identity 和 outcome predicates。

结果：
- Sparse range：
  - 抽取 `12` rows，覆盖 segment `0001/0002/0008`、Family A/B、
    rejected/confirmed 和 segment 边界。
  - 六个 channel 的 first/last/count mismatch：`0`。
  - 未跨 segment/epoch/quality barrier。
- Fixed grid：
  - 独立重建 `68` rows，覆盖 buy/sell confirmed 样本。
  - value/source identity/observed_at/no-new-info/availability mismatch：`0`。
- Event count：
  - 独立重建 `12` rows，覆盖 BBO/trade/fast-L2。
  - unique source identity、顺序和 first/last event mismatch：`0`。
- Outcome：
  - buy direction sign=`+1`，adverse upward，vulnerable quote=`ask`。
  - sell direction sign=`-1`，adverse downward，vulnerable quote=`bid`。
  - buy/sell markout、BBO adverse/retreat 和 target-trade predicate 抽样值
    与 source 一致。
- 以上正向抽样不抵消 P1-3 的 trade interval lower-bound 全量缺陷，也
  不抵消 P1-4 的 admission semantic fail-open。

### 6. Contract、Context 和 Measurement Limitations

Formal contract/artifact 已落实：
- Family A/B linked views 和 population contract：
  `frozen_episode_v3_contract.json:1-28`。
- 每 feature `observed_at_ns/source_event_id/source_book_version` schema：
  `frozen_episode_v3_contract.json:406-421`。
- `underlying_market_state=unknown_calendar_state`：
  `frozen_episode_v3_contract.json:424`。
- Nominal clock 仅为 diagnostic，formal state 没有由未来价格推断。
- `trade_quantity_source=binance_trade_q`：
  `frozen_episode_v3_contract.json:421`。
- `rpi_adjustment_status=unavailable_historical_feed`：
  `frozen_episode_v3_contract.json:407`。
- `depth_stream=depth@0ms`：
  `frozen_episode_v3_contract.json:295`。
- `collection_topology_fingerprint` 和
  `hyperliquid_feed_cadence_regime=jul30_observed_public_feed` 已进入
  feature contract。
- Plan 中 RPI/nq、`@depth@0ms`、public-feed interval censoring 和
  topology caveat 位于
  `docs/skhynix_trigger_aligned_episode_research_implementation_plan.md:1541-1588`。
- Artifact 未出现 own-order/fill/PnL/actionability/model/score 字段。

判定：
- 上述 context/limitation publication 要求通过。
- Classification 语义冻结不足单列于 P1-4。

### 7. Aggregate Counts 和 Zero-Mismatch Counters

正式 manifest/segment summaries：
- anchors / Family A / outcomes：`268522 / 268522 / 268522`
- Family B：`141768`
- rejected：`126754`
- sparse range A/B：`268522 / 141768`
- fixed grid A/B：`4564874 / 2410056`
- event-count A/B：`805566 / 425304`
- feature-ledger A/B：`23092892 / 15310944`
- unavailable feature rows：`2863855`
- interval-censored：`627406`
- right-censored：`177826`
- segment-censored：`316`
- quality-censored：`18`
- epoch-censored：`0`
- point-coerced：`0`
- auxiliary-degraded grid rows：`4316`
- core-degraded grid rows：`0`

Published mismatch counters：
- future-feature observation：`0`
- anchor ordering：`0`
- cross-segment path：`0`
- cross-epoch path：`0`
- synthetic rejected confirm：`0`

解释：
- 这些 published counters 按当前 verifier 定义均为零。
- P1-1 说明 `observed_at <= landmark` 不足以证明 source provenance
  正确；P1-4 说明 coherent semantic mutation 可在 counters 仍自洽时通过。

## Hard Boundary 结论

- Jul30 legacy `episodes/*.csv.gz` parsed：false
- Aug03/Aug04 future event rows read：false
- Aug07 event rows read：false
- case/model/score/actionability run：false
- own-order/fill/PnL data used：false
- network/private/order endpoint accessed：false
- collection/order placement：false
- accepted Stage 1/2/3、Jul30 raw/R0/R1 modified：false
- business code/tests/package/task/tracking modified by QA：false

## 验收结论

- 未通过
- 结论说明：
  - 正式 package 可以从允许输入确定性重建，107 个文件及
    manifest/core/full hashes 全量一致，Family A/B population、selection
    bias、边界和大量 source 抽样重建均通过。
  - 但 Confirmed-time feature provenance、burst membership、target-trade
    interval support 和 admission source-semantic fail-closed 存在实际
    contract/因果/可重建性缺陷；按任务硬门槛不能验收。
  - Stage 5 保持锁定。

通过项：
1. Jul30-only fresh rebuild 与正式 package byte-identical。
2. Formal manifest/core/full identity 和全量 inventory 正确。
3. Family A 覆盖全部 candidates/rejected，Family B exact linked。
4. Focused/inherited tests、Ruff、compile、gzip、diff-check 通过。
5. Default Python verify-only 对正式 package zero-write。
6. Partial publication、forbidden input/artifact、manifest count、
   quality interval/source-age 删除等攻击 fail closed。
7. 抽样 sparse/grid/event-count/outcome source reconstruction 通过。
8. Context、RPI/nq、`depth@0ms`、topology/feed cadence 和 public-only
   limitations 已发布。

不通过项：
1. Family B confirmation feature provenance 错绑 BBO-asof。
2. Confirmed burst features 并入 frozen detector burst 外成交。
3. Target-trade first-event interval lower bound 大量早于 Candidate。
4. Admission 对 classification/degraded anchor/sparse/event/markout coherent
   semantic mutation fail open。
5. Same-timestamp baseline strict-pre boundary 尚未 fail closed。

缺陷清单：
1. P1-1：修正 Confirmed-time feature 的真实 detector timeline
   `observed_at/source_event_id/source_book_version`。
2. P1-2：直接复用 detector frozen burst membership 或发布 immutable
   burst event IDs，不得重新按时间窗搜同向成交。
3. P1-3：对 post-trigger trade first-event interval 做 Candidate
   left truncation；在首次 post-Candidate non-event 前 lower 应为
   `t_candidate_ns`。
4. P1-4：Admission 必须从 accepted Stage 3/source exact 重建 anchor
   classification/quality evidence、sparse counts、event identities 和
   outcome values；冻结 classification semantic mapping。
5. P2-1：Outcome vulnerable baseline 使用 strict-pre source ordering，
   并增加 same receipt timestamp hostile test。

阻塞项：
- 无外部环境阻塞；当前阻塞 Stage 5 的原因是业务缺陷，非 QA 环境问题。

建议总控下一步：
1. 保持 `0815T003` 未通过和 Stage 5 锁定。
2. 派发 bounded repair，只修复上述 P1/P2，不扩大到 Stage 5
   case/model/actionability。
3. 修复后重新完整构建正式 package，并由全新 QA 线程复跑全量
   determinism、semantic reconstruction 和 coherent-rehash attacks。

提交信息：
- commit：无
