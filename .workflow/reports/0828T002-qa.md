# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0828T002

状态：
- 已通过

更新时间：
- 2026-08-28 10:00 CST

验收线程：
- QA验收线程

验收对象：
- SKHYNIX Interpretable Conditional Transition Track A 方案业务线程
- plan commit：`75daafa8`
- business report commit：`1267e087`

验收范围：
- 验收方案是否正确 supersede motif-first primary route。
- 验收 continuous background、M-state、N-state、risk set、
  competing-risk hazard 和 H0/H1 comparison 是否形成一个可执行、
  可拒绝的 interpretable conditional transition 合同。
- 验收 support-before-target、session blocking、censoring、null、
  prospective 和 claim boundary。
- 验收 git exact-path scope，确认未混入工作树已有无关文件。

验收步骤：
1. 核对 plan 和 business report commit 的 changed paths。
2. 执行 `git diff --check` 和 Markdown fence parity 检查。
3. 核对 `m1/m2/m3/q_M` 的因果、方向和联合状态定义。
4. 核对 `n_recovery/n_propagation/n_reversal` 的互斥、first-event、
   ambiguity 和 right-censoring。
5. 核对 H0 已控制 component main effects，H1 只增加 joint `q_M`。
6. 核对 elapsed-time、proper score、blocked uncertainty、null 和
   prospective gates。
7. 核对 DTW/clustering 仅为 primary pass 后的 secondary diagnostics。

实际结果：
- `75daafa8` 仅包含 0828T002 task、新正式方案和旧方案 superseded
  标记；`1267e087` 仅包含业务回报。
- `git diff --check` 通过；正式方案为 1263 行，96 个 fenced-code
  markers，数量为偶数。
- M-state 被定义为 pressure-side depth withdrawal、aggressive-flow
  persistence 和 replenishment deficit；`q_M=min(m1,m2,m3)` 表达
  可解释 conjunction。
- Broad landmark 提供共同 pressure-activation risk set，M-positive 和
  weak-M landmarks 使用同一 target/censoring contract。
- N targets 为 recovery、propagation、reversal 三个互斥 first events；
  无事件、capture end、gap、reconnect、reset 和 quality failure 均按
  right censoring 处理。
- H0 包含 context 与 `m1/m2/m3` 加性主效应，H1 仅新增 joint `q_M`。
  因此 primary increment 不会只依赖单组件普通自相关。
- Primary estimator 为低参数 discrete-time multinomial competing-risk
  hazard；`tau_max` 根据 zero-target support geometry 机械选择。
- Primary evidence 使用完整 follow-up 上的 held-out log loss、integrated
  Brier、calibration、blocked confidence 和 null separation，不允许
  favorable-horizon rescue。
- Existing historical dates 最高只能达到
  `historical_support_only_pending_prospective`；最终确认需要新的
  protocol-frozen prospective sessions。
- soft-DTW、k-medoids 和 shapelets 被正确降级，不能修改 M/N 或救回失败
  的 H0/H1 gate。

验收结论：
- 已通过
- 结论说明：
  - 新方案已形成 hypothesis-driven、低自由度、可解释并能 fail closed
    的 conditional transition Track A 合同。`已通过`只表示研究设计和
    流程闭合，不表示 M-to-N transition 已在数据中成立。

通过项：
1. Motif-first primary route 已显式 superseded。
2. M-state、N-state、risk set、hazard 和 censoring 语义完整。
3. H0 足够强，H1 joint increment 对应清晰的 pattern claim。
4. Support-before-target 和 data-determined time-scale 防止 outcome/horizon
   反向调优。
5. Proper scoring、blocked uncertainty、null、transport 和 prospective
   gate 可执行。
6. Git scope 干净，未混入已有无关 staged/untracked files。

不通过项：
1. 无。

缺陷清单：
1. 无。

阻塞项：
- 无文档验收阻塞。
- 最终正结构结论仍依赖未来另行授权的 prospective 数据。

建议总控下一步：
1. 派发
   `SKHYNIX-BINANCE-INTERPRETABLE-TRANSITION-TRACK-A0-SUPPORT-AND-TUPLE-FREEZE`。
2. A0 保持 zero N-target access，只冻结 source、grid、M/N formulas、
   risk-set、censoring、H0/H1、time basis 和 numeric gates。

提交信息：
- commit：待 QA 事实源提交
