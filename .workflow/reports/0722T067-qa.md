# QA 验收结果

## Findings（按严重性）

- P0/P1/P2：无。
- P3：旧的 `hyperliquid_tiny_live_final_go_no_go_gate` 两个正向测试依赖
  `git branch --show-current == cross-exchange`。在 detached QA worktree
  中它们因 `local_branch_not_cross_exchange` 失败；在同一精确 commit 的
  clean `cross-exchange` branch clone 中两项 `4 passed`，完整 Hyperliquid
  suite 全绿。该环境敏感性早于 T067 且不影响本任务行为，但后续可将 fixture
  改为显式注入 branch。

执行线程：
- QA验收线程

任务ID：
- 0722T067

状态：
- 已通过

更新时间：
- 2026-07-26 15:35 Asia/Shanghai

验收线程：
- QA验收线程

验收对象：
- implementation
  `4ca476496b7033100cda0b9ff2678a223d5c31ff`
- evidence/workflow
  `1969ed4c92e51342a7c909251b8ea26f715c297c`

验收范围：
- T066 exact seed contract/hash、exposure source 和 current-market isolation。
- Committed T067 884-row shadow matrix 的独立逐行重算。
- Strict gate 在 manager submit/reconcile 前的调用顺序和 hostile fail-closed。
- 新 seeded orchestrator exact profile、legacy profile compatibility 和 T024
  strict acceptance。
- Clean source rebuild、artifact hashes、focused/full regression。
- No credential/private/account/order/cancel/service/live 和 no-overclaim
  boundary。

验收步骤：
1. 从 clean detached checkout 读取 T066/T067 committed contract、source
   rows、matrix 和源码，不调用 production summary helper。
2. 独立重算 contract canonical self-hash、输入文件 SHA-256、row counts 和
   counterfactual/non-resting boundary。
3. 独立解析全部 884 个 shadow rows，比较 candidate、strict gate、最终
   tick-rounded bid/ask 和 endpoint fields。
4. 检查 manager cycle 源码顺序，以及 loader 是否存在 book/trade current
   market ingest call。
5. 从 evidence commit 的 Git archive 创建第二份 clean snapshot，运行正式
   shadow CLI，并逐字节比较四个 artifacts。
6. 运行 focused、hostile、`py_compile`、`git diff --check` 和完整
   Hyperliquid regression。
7. 对 detached full-suite 的两个失败独立复现 blocking reason，并在同一
   commit 的 clean branch clone 重跑确认。

实际结果：
- Seed contract exact `17` fields；canonical self-hash 与 externally pinned
  hash 均为
  `e35c7fd8f3ec8268e5d50c7963889b73409470c9f01f92ca3a0d598a96562be9`。
- Exposure CSV hash 与 contract 一致，共 `280` rows；全部
  `resting_confirmed=false` 且 source 为
  `public_multi_distance_counterfactual_non_resting_seed`。
- Current event source hash 与 contract 一致，共 `884` rows。
- Loader 源码不调用 `ingest_book`/`ingest_trade`；正式 load result 记录
  current event/bucket before/after 均为 `0`，无 contamination。
- 独立 shadow matrix 统计：
  - candidate：`540 pass / 344 fallback_fixed`。
  - strict gate：`540 pass / 344 fail_closed`。
  - final quote changed：`540`。
  - strict-allowed event set、candidate-pass event set、final-changed event set
    三者完全相同。
  - `344` fallback rows 中 strict allowed 为 `0`；每行都有明确 fail-closed
    reason。
  - changed rows 的最终 bid 或 ask 必然不同；unchanged rows 的最终双边价格
    均与 fixed quote 相同。
  - `884/884` rows 的 order endpoint field 为 false。
- Manager cycle 顺序为：
  `startup_reconcile -> build_task7_desired_quotes -> strict gate ->
  reconcile_desired`；hostile mock test 验证 fallback 时 order/cancel calls
  均为零。
- New `two-sided-seeded-dynamic-manager` 强制 exact seed inputs/hash/strict
  flag；legacy `two-sided-dynamic-manager` 行为未收紧。
- T024 新 profile 只接受 candidate `pass`、no fallback、final quote changed、
  exact seed load 和 strict gate pass。
- Git archive clean rebuild 的四个 artifact 与 committed bytes 完全一致：
  - boundary:
    `c70c908ba379011dc1a8fdd1e2bc57e245e9a36948e07cd2ce7b3afd696f512d`
  - recommendation:
    `a55ab25a595ba318b4e6d6b8b82a70e0926de13a60a9dd7e0767ab90da94e284`
  - quote matrix:
    `2a4b3a0c68c46ab38c715b3792f53c1603ece53e8fd33fe84b5d04ddad167e2d`
  - summary:
    `1bdec801aceb7584b4570067e6c4f031626565d74d2a6026f72fa72a2779ac72`
- Focused changed-scope：
  `304 passed in 22.90s`。
- Detached full run：
  `1304 passed, 3 skipped, 2 failed`；两项失败仅为 detached branch name。
- Exact evidence commit clean branch clone：
  old branch-sensitive file `4 passed`；full Hyperliquid
  `1306 passed, 3 skipped in 66.72s`。
- `py_compile`、`git diff --check` 和 clean checkout status：通过。
- QA 未访问 AWS、网络、credentials、private/account/order/cancel/service/live。

验收结论：
- 已通过。
- 结论说明：
  - Exact public seed 已安全接入 production quote builder；新 profile 在
    exact hash、current candidate 或最终报价变化不成立时 fail closed。
    Committed evidence 可独立重建，且没有把 same-sample no-submit shadow
    误称为 fill、OOS 或 economics evidence。

通过项：
1. Exact seed/hash/source boundary 和 current-market isolation。
2. 884-row independent candidate/gate/final-price equivalence。
3. Strict pre-submit order boundary 和 hostile fail-closed。
4. Seeded orchestrator profile、legacy compatibility 和 strict T024 acceptance。
5. Deterministic clean rebuild、focused/full regression。
6. No-live/no-overclaim boundary。

不通过项：
1. 无。

缺陷清单：
1. 无阻断缺陷。
2. P3 detached branch-sensitive legacy fixture 见 Findings。

阻塞项：
- T067：无。
- 后续真实订单仍须新的 formal task 和 fresh exact live authorization。
- T067 不提供 fill-rate、OOS 或 economics 结论。

建议总控下一步：
1. 派发 fresh-authorized bounded live evidence task，必须使用
   `two-sided-seeded-dynamic-manager`、exact T066 seed hash 和 strict gate。
2. Live acceptance 必须同时取得 role-known maker fill、dynamic quote
   behavior、fill-feedback observation 和 fee/PnL source evidence；若无 fill，
   不得将 T067 shadow 升格为 economics 通过。

提交信息：
- QA commit：由本报告提交后的线程回报提供。
