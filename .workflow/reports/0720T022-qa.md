# QA 验收结果

执行线程：
- 独立只读 QA 验收线程

任务ID：
- 0720T022

状态：
- 未通过

更新时间：
- 2026-07-20 13:54 CST

验收线程：
- QA验收线程

验收对象：
- 业务执行线程 / 0720T022
- Source commit：`4134ada2503912fe844bb76df68cd539bb4f3732`
- Business report commit：`6652a14beba4d76f2e3284b58ea1349e3eeb8d3b`

验收范围：
- Exact source/task/root/window/duration/envelope/activation
- 两侧 submission/resting 与逐 reference terminal proof
- Fills、position、loss、child、writer、checksum
- Post-live account safety
- Same-window acceptance 和业务报告准确性
- 全程只读；未访问 network、remote 或 private/account endpoint

验收步骤：
1. 解析完整业务报告 commit，确认任务和报告文件与该 commit 中的 blob 一致。
2. 核对 preflight、runtime provenance、runner command、run status 和 activation evidence。
3. 独立重建两侧 intent、response、cancel、terminal query、fill 和 position 证据。
4. 静态确认 acceptance 仅使用标准库和本地 Git 操作。
5. 将 acceptance 离线复跑至 `/tmp/0720T022-qa-acceptance-rerun`。
6. 比较复跑矩阵、manifest、输入文件 SHA-256 和工作树状态。

实际结果：
- Source commit 是 business report commit 的祖先；runtime source start/postrun 均为 `62/62` exact。
- Exact task、remote source、run root、window `01` 和 command identity 通过。
- Canonical watcher duration 为 `1800s`，实际 elapsed `706.021258s`，仅完成一个 window。
- Envelope 为 BTC、post-only `Alo`、`0.005 BTC/order`、`0.01 BTC position`、`1 USDC loss`、`2 submissions`。
- Dynamic spread、fill feedback、inventory skew、multi-level 和 quote behavior change 均未激活。
- Buy `0.005 @ 64440`、sell `0.005 @ 64455` 均实际提交并收到 `resting`。
- Sell reference 获得 authoritative cancel success。
- Buy cancel 返回通用 already-absent error；exact cloid query 返回 `unknownOid`。
- Buy reference reconciliation 为 `fail_closed`，总计仅 `1/2` references proven。
- Fill ledger、attribution 和 liquidity-role rows 均为 `0`；BTC position `0.0`，estimated loss `0.0 USDC`。
- Child `rc=0` 且已 reap；无 termination/SIGKILL；writer `635` success、`0` failure。
- Terminal checksum `104/104`；post-child 和 post-live proof 均显示 open orders `0`、BTC position `0.0`。
- 离线 acceptance 复跑 exit `2`：provenance `112/112`、config `72/72`、decision `43/43`、lifecycle `49 pass / 12 fail`。
- 复跑的六个矩阵及 validation report 与原结果字节一致；manifest 去除生成时间、Git 短 hash 和绝对输入路径后完全一致。
- 输入根 118 个文件复跑前后 SHA manifest digest 均为 `71c9ba35f62e6657d0d48f63abb451ad538f14d85d7113a567b16dfe2b92833c`；工作树状态未变化。
- 业务报告准确地区分了账户当前安全与逐 reference 历史终态，没有做乐观转换。

验收结论：
- 未通过
- 结论说明：
  - T022 的执行安全、证据保存和报告准确性通过，但核心 single-level two-sided lifecycle 未获得完整逐 reference terminal proof，不能作为后续策略阶段的已接受基线。
  - Generic already-absent 加 `unknownOid` 不构成 authoritative terminal。即使该订单此前确实 resting，这组结果仍不能权威区分 canceled 与 filled 等历史终态。
  - Account-wide open-orders `0` 只能证明当前账户没有活动订单，不能替代特定 oid/cloid 的历史终态证据。

通过项：
1. Exact source/task/root/window/envelope/activation 和单窗约束通过。
2. Buy/sell 两侧真实 submission/resting 通过。
3. Submission、position、loss 和 duration cap 未超限。
4. Child、writer、runtime source 和 terminal checksum 通过。
5. Post-live account safety 通过。
6. Same-window acceptance offline-only boundary 和可复现性通过。
7. 业务报告对 blocker、限制条件和 unsupported economics claims 的描述准确。

不通过项：
1. Buy attempt 1 缺少 authoritative reference-bound cancel 或 complete-fill terminal proof。
2. Same-window mechanism/evidence integrity 为 `fail`，multi-level activation 仍为 locked。

缺陷清单：
1. [P1] Buy reference terminal history 未闭环。证据：
   - `run/window_01/cancel_shutdown_proof.json:95-119`：cancel 为非权威通用错误。
   - `run/window_01/cancel_shutdown_proof.json:257-271`：exact cloid query 为 `unknownOid`。
   - `run/window_01/cancel_shutdown_proof.json:37-73`：attempt 1 为 `fail_closed`，仅 `1/2` proven。
   - `acceptance/lifecycle_evidence_comparison.csv:26-27,41-58`：terminal contract 和 no-fill reconciliation 失败。
2. [P2] 人类可读 acceptance 标题仍写成旧任务 `0718T024`：
   - `acceptance/validation_report.md:1`
   - JSON manifest 中 `task_id=0720T022` 正确，因此不影响 P1 裁决，但应修正证据可读性。

阻塞项：
- Single-level lifecycle acceptance 尚未完成；Task 8、adaptive 和 multi-level live 必须继续锁定。

建议总控下一步：
1. 派发 offline-only 修复任务，补充可权威查询 oid/cloid 历史终态的 bounded fallback。
2. 不得把 `unknownOid`、generic already-absent 或 account-wide absence 定义为 cancel success。
3. 修复后先用 T022 evidence 做离线回归和独立 QA；通过前不启动新 live window。
4. 修正 acceptance validation report 的硬编码旧任务标题。

提交信息：
- commit：无
