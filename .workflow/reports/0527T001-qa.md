# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0527T001

状态：
- 已通过

更新时间：
- 2026-05-28 14:49 CST

验收线程：
- QA验收线程

验收对象：
- 测试线程 0527T001

验收范围：
- 验收 `0527T001` 是否按 planning/diagnosis-only 边界分析 `0526T008 / 5-26-active-minmove-control-30min-b` replay audit `cancel_ack` bloat。
- 检查是否回答根因来源、duplicate key、Stage 6 是否语义冗余、后续 repair 边界和后续验收标准。
- 不验收任何 repair、live、策略行为、Stage 6 实现改动、参数搜索或 promotion。

验收步骤：
1. 阅读 `.workflow/tasks/0527T001.md`。
2. 阅读 `.workflow/reports/0527T001-business.md`。
3. 核对诊断是否只使用 `0526T008` 输入样本。
4. 核对是否通过轻量/流式方式检查 21GB full replay audit，而不是全量载入表。
5. 核对任务状态、workflow 文档和提交记录。

实际结果：
- Business report 已产出，任务状态由 `待验收` 切为 `已通过`。
- 诊断输入限定为 `local_live_analysis/5-26-active-minmove-control-30min-b`。
- 诊断未修改策略、live、Stage 6 实现、replay lifecycle repair、参数或 promotion 状态。
- 关键事实明确：
  - full replay audit 约 `21GB`。
  - full replay audit rows: `33,293,775`。
  - full replay audit `cancel_ack` rows: `32,893,719`。
  - unique `order_id` with `cancel_ack`: `10,380`。
  - lifecycle-min canonical Stage 6 input rows: `401,642`。
  - lifecycle-min `cancel_ack` rows: `10,380`。
- 根因判断明确：主要是 audit replay lifecycle export / order-state tracking repetition，不是 Stage 6 label-generation 自己制造重复。
- Duplicate key 判断明确：Stage 6 语义上可用 `event_type=cancel_ack + order_id`，保留每个订单首个 terminal cancel fact；更严格 forensic key 可加入 `cancel_request_ts`、稳定 terminal timestamp、`order_status` 和 `lifecycle_detail` class。
- Stage 6 语义判断明确：额外 `32.88M` repeated rows 对 Stage 6 labels 冗余，保留首个 terminal fact 即可。
- 业务提交存在：
  - commit `cafd164`
  - message `diagnose 0527T001 replay audit bloat`

验收结论：
- 已通过
- 结论说明：
  - 关键发现：`0526T008` 的 21GB replay audit bloat 主要来自 audit replay lifecycle export / order-state tracking 重复输出，同一批约 `10,380` 个 cancel-ack 订单被反复写成 `32,893,719` 条 `cancel_ack` 行；这些重复行对 Stage 6 标签语义冗余。
  - 下一步建议 1：新建一个窄 implementation task，做 compact replay lifecycle audit export 或 writer-side terminal-order de-dup，并让 Stage 6 通过合同消费 compact artifact。
  - 下一步建议 2：full forensic audit 只保留为显式可选追溯产物，不进入默认 Stage 6 路径；后续任务仍不得改策略、live/default-on、参数搜索或 promotion。

通过项：
1. 任务按 planning/diagnosis-only 边界完成，没有做 repair。
2. 根因、duplicate key、Stage 6 冗余性和后续 repair 边界均已回答。
3. 诊断证据包含数据计数、样例模式和代码路径解释。
4. 明确下一步应修 compact lifecycle audit / writer-side de-dup，而不是策略或 live。

不通过项：
1. 无

缺陷清单：
1. 无

阻塞项：
- 无

建议总控下一步：
1. 按 QA 结论新建窄 implementation task：compact replay lifecycle audit export for Stage 6 input。
2. 继续保持 full forensic audit optional，禁止把本诊断扩展成策略/live/promotion 任务。

提交信息：
- commit：`cafd164`
