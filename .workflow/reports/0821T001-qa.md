# QA 验收结果

执行线程：
- 独立 QA 验收线程

任务ID：
- `0821T001`

状态：
- 已通过

更新时间：
- 2026-08-21 16:40 UTC

验收线程：
- 独立 QA 验收线程

验收对象：
- `SKHYNIX-STAGE-H0A-SUPPORT-ONLY`
- candidate commit：
  `a63ee9b71c9ba002a875b1259cb58de290ac3a46`
- Mac candidate worktree：
  `/Users/liu/Documents/hftbacktest-0814t001-skhynix-episode-research`

验收范围：
- 独立复核 H0-A support-only package 的 Gate 0-7、Trust Kernel pin、
  hostile topology、source boundary、fresh Mac source-semantic replay、
  formal package admission、amdserver kernel-only admission、archive exact
  tree 和 H0-B lock。
- 不打开 H0-B outcomes，不修改 accepted registry、accepted dependencies、
  research source 或 authoritative business formal package。

P0/P1/P2/P3：
- `0 / 0 / 0 / 0`

验收步骤：
1. 核对 candidate checkout、任务边界、registry revision 1、accepted
   Stage 1-4 pins、package tree 和 authoritative business identity。
2. 在 Linux candidate 上独立复跑 Gate 0、focused tests、compileall、
   shell syntax、diff check 和 package admission。
3. 在 Mac 新建原先不存在的 disposable detached worktree，复制 accepted
   Stage 1-4 dependency package，使用独立 Build A/Build B/formal 路径执行
   fresh source-semantic replay。
4. 从 Mac 对 amdserver 执行 authoritative package 的
   `--verify-kernel-only` admission。
5. 比较 fresh replay 与 authoritative formal 的研究输出、输入内容哈希、
   identity、远端 exact tree 和 H0-B 状态。

实际结果：
- Candidate HEAD 为
  `a63ee9b71c9ba002a875b1259cb58de290ac3a46`，工作树干净并与
  `origin/episode-research` 对齐。
- Gate 0 通过：`24 surfaces / 24 artifacts / 24 negative mutations /
  7 exit criteria`；Surface Matrix SHA256：
  `97b14d90d2c9499ad7a7daf606c7a11e0acb64b77b0c2b8425562cafc53afdcc`。
- Linux focused suite：`17 passed`；`compileall`、`bash -n` 和
  `git diff --check` 通过。Mac fresh Ruff：`All checks passed!`。
- Mac fresh hostile preflight 通过：current/frozen `48` executions，
  fail-open `0`，receipt SHA256：
  `8b83950f5dedf4c2aa2e13c263fbbf487593a9be2f4a62d9a346db54d8bdf141`。
- Mac fresh source-semantic replay 通过。Build A、Build B、formal 均为
  `23 files / 4 dirs / 591108 bytes`，fresh Build A/B/formal identities
  一致，research outputs byte-identical；build receipt SHA256：
  `570dd36bfca2e167159fa0f898df1090f76c1d22445aea3a3c4ff1ac269aae6c`。
- Fresh replay 选中 `50ms`，selection status 为 `selected`。Fresh
  package admission 为 `verified=true`、`zero_write=true`；package
  admission receipt SHA256：
  `ea872f5712d70b386eaef0063f7100605c84ecfd8e1a06a29c094c7c795ed73f`。
- Fresh replay 的 254 条 input bindings 与 authoritative business
  inventory 在 scope/role/session/segment/relative_path/bytes/content
  SHA/authoritative manifest SHA 上完全一致；fresh 绝对 root/path 仅因
  detached worktree 位置不同。
- Fresh 与 authoritative formal 的 10 项语义 research outputs 全部一致：
  7 个核心 CSV 字节一致；`landmark_crosscheck.csv`、`primary_tuple_freeze.json`
  和 report 在剥离环境绝对路径及 self-binding identity 后一致。
- Fresh package 的独立 identity 为：
  - R `bbb4e67d326e6b0e8825406dedb8e0e051a6ce6b37a74f2466b9bb6ac3eab9a9`
  - C `b7184cd755cba58f5415e804dff8760b08c5f87a3d08fe55f214108b0037a51c`
  - E `d2e82a0271a5fe13927ad15907dcbf80f09110deb3a7a2d848499a2750b8d09d`
  - composite `40f29a7cc794d497e4078362a595331a0d4bad95baaf6add3b94885886d10bec`
- Fresh 与 authoritative identity 不相等，原因已定位为实现将 detached
  dependency 的绝对 provenance path 纳入 input inventory 和 landmark
  evidence。该差异不改变输入内容或研究数值；authoritative package 的
  固定身份仍以业务 formal package 为准：
  - R `7176c78c2b6bfadf11432e9f5a1c1eaf9627c8028a4a22257d67d5a5404dc8fd`
  - C `4e8ccc7466f84d9eb2f71f681557424b59ca58249d67426325ecdbd661189636`
  - E `8745458fcf4e0ab31f8ad3b2bc2d1d93704f18b13a776c515f14195b51213969`
  - composite `2682c32eefac427eed1899a3d492b3fc7545520f72021723e8fef7a0d4d8d9d0`
- Mac -> amdserver kernel-only admission 通过：
  `verified=true`、`zero_write=true`、`23 files / 4 dirs / 596028 bytes`；
  kernel source SHA：
  `cee2395afad9420c38235ba195bf030e92330015e1a15937ebc22fa707c80203`；
  remote tree SHA：
  `843fd6fa16c416349b990eec185e5425d185c49058a93ce1997c6f8900d46503`；
  report SHA：
  `9d1e4f486f8f60a221eb482f1fc1d9ce287303922b893a5f5f6dfb0483bbd6f5`。
- amdserver admission 明确为 kernel/package-only：
  `source_semantic_replay_executed=false`，符合
  `full_source_semantic_replay_portable=false`。
- Authoritative archive R/C/E/composite 与本地 formal package 完全绑定；
  本轮未修改 registry。H0-B 仍锁定，100ms challenge=false 仍需 controller
  在 H0-B 前明确保留或修订 primary tuple。

验收结论：
- 已通过
- 结论说明：
  - H0-A support eligibility、primary tuple freeze、fresh source-semantic
    replay、Trust Kernel package admission、amdserver kernel-only admission
    和 authoritative archive evidence 均满足验收标准。
  - Fresh identity 的绝对路径差异已被定位并在本报告中单独记录；未把
    fresh identity 冒充 authoritative identity。

通过项：
1. Gate 0、hostile current/frozen、17 个 focused tests、Ruff、compileall、
   shell syntax 和 package zero-write admission 通过。
2. Fresh Mac source-semantic replay 使用独立 detached worktree 和独立
   Build A/Build B/formal 路径完成，未使用 business Build A/B 作为 source
   oracle。
3. Fresh 与 authoritative research outputs 的语义比较通过，输入内容
   inventory 完全一致。
4. amdserver kernel-only admission、remote exact tree、Trust Kernel source
   pin、R/C/E/composite binding 和 zero-write 通过。
5. 未发现 H0-A outcome、Aug07 event、Stage 4 outcome/features/views、
   private/order/network 或 live action 越界。

不通过项：
1. 无

缺陷清单：
1. 无阻断性缺陷。环境绝对路径被纳入 fresh package identity 的行为已作为
   provenance observation 记录；本轮不改变 authoritative package，也不影响
   本任务的语义 replay 和 admission 结论。

阻塞项：
- 无

建议总控下一步：
1. 保持本 QA 报告为 `0821T001` 最新验收事实源。
2. H0-B 继续锁定，等待 controller 明确 100ms scenario 的保留或 tuple
   revision，并完成 H0-B 前置合同。
3. 由总控决定是否将 absolute provenance path identity 作为后续 Trust
   Kernel / H0-A identity normalization 改进项；本轮不修改已验收 package。

提交信息：
- commit：待提交
