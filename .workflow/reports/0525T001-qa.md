```md
# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0525T001

状态：
- 已通过

更新时间：
- 2026-05-26 00:19 CST

验收线程：
- QA验收线程

验收对象：
- 业务线程-python 0525T001

验收范围：
- 验收 Step 9D keep-for-research candidate fine-bucket refinement 是否按任务要求完成 runner、五样本细 bucket 输出、三个 candidate 判断、补样本方向和 no-promotion 边界。

验收步骤：
1. 阅读 `.workflow/tasks/0525T001.md` 与 `.workflow/reports/0525T001-business.md`。
2. 检查 `local_live_analysis/stage9d_candidate_bucket_refinement_0525T001/` 下的 required artifacts。
3. 读取 `candidate_bucket_recommendations.md` 与 `sample_gap_recommendations.csv`，核对三个 candidate 的 seed / reject / needs-more-fills 结论。
4. 运行验收命令：
   - `python examples/binance_tick_mm/candidate_bucket_refinement.py --help`
   - `python examples/binance_tick_mm/quote_adjustment_replay.py --help`
   - `python -m pytest examples/binance_tick_mm/test_quote_adjustment_replay.py`
   - `python examples/binance_tick_mm/candidate_bucket_refinement.py --run-dir local_live_analysis/5-19-day-control-30min --run-dir local_live_analysis/5-19-night-active-30min-a --run-dir local_live_analysis/5-19-night-active-30min-b --run-dir local_live_analysis/5-19-night-active-30min-c --run-dir local_live_analysis/5-21-day-control-60min --output-dir /tmp/0525T001-qa-stage9d --caveated-sample-id 5-19-night-active-30min-a`

实际结果：
- Required artifacts 存在：
  - `fine_bucket_metrics.csv`
  - `fine_bucket_stability_summary.csv`
  - `fine_bucket_stability_summary.json`
  - `candidate_bucket_recommendations.md`
  - `sample_gap_recommendations.csv`
  - `run_manifest.json`
- `candidate_bucket_refinement.py --help` 通过。
- `quote_adjustment_replay.py --help` 通过。
- `python -m pytest examples/binance_tick_mm/test_quote_adjustment_replay.py` 通过：`5 passed`。
- QA 重跑 `candidate_bucket_refinement.py` 成功，产出 `/tmp/0525T001-qa-stage9d/` 下同名 required artifacts。
- `min_move_quote_age_churn_guard` 是最强近线 seed：
  - stable promising buckets `20`
  - parameter-sweep seed buckets `8`
  - reject buckets `0`
  - seed 覆盖 API/churn normal、young quote churn、large inventory skew / low score、stale latency high/medium、post-only clean、one-tick tight spread、medium volatility。
- `inventory_reservation_shift_band` 不适合作为近期参数搜索主线：
  - parameter-sweep seed buckets `0`
  - reject buckets `17`
  - aggregate 正向不能被验收为稳定 inventory/reservation rule。
- `size_reduction_or_add_side_suppression_pressure` 只适合作为窄 seed：
  - parameter-sweep seed buckets `2`
  - seed 为 `spread_quote_distance=one_tick_tight` 与 `volatility_markout_dispersion=volatility_high`
  - reject buckets `12`，不能全局启用。
- 任务保持 read-only：未执行 live、default-on、guard relaxation、策略行为修改、参数搜索或 promotion。
- 仍不满足 `ready_for_tiny_live_design`，accepted fills 为 `340 / 500`，缺口 `160` fills。

验收结论：
- 已通过
- 结论说明：
  - `0525T001` 完成了 keep-for-research candidate 的细 bucket 判定，并给出了可执行的下一步：围绕 `min_move_quote_age_churn_guard` seed regimes 补 active current-format no-rule/default-off 样本，再进入后续参数搜索设计。

通过项：
1. Required artifacts 齐全，并可通过 QA 重跑再生成。
2. 三个 candidate 的 stable / seed / reject / needs-more-fills 结论明确。
3. `min_move_quote_age_churn_guard` 的后续优先级和目标 bucket 明确。
4. `inventory_reservation_shift_band` 与 `size_reduction...` 的边界明确，避免盲扫。
5. no-live / no-default-on / no-guard-relaxation / no-promotion 边界保持成立。

不通过项：
1. 无

缺陷清单：
1. 无

阻塞项：
- 无

建议总控下一步：
1. 可派发并执行 `0526T001`，采集 active current-format no-rule/default-off control samples，优先补 `min_move_quote_age_churn_guard` seed regimes 和自然 fills 缺口。
2. 不要直接进入 tiny live；也不要围绕 `inventory_reservation_shift_band` 做近期参数搜索。

提交信息：
- commit：待总控收尾提交
```
