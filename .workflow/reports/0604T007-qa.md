# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0604T007

状态：
- 已通过

更新时间：
- 2026-06-04 10:05 CST

验收线程：
- QA验收线程

验收对象：
- 业务线程-research + 0604T007

验收范围：
- `0604T007` 规定的 read-only canonical horizon / regime diagnostics runner、focused tests、task-scoped artifacts，以及对 `0604T003` canonical event-mode source 和 synthetic diagnostic comparison 的输入拒绝复验。

验收步骤：
1. 阅读 `.workflow/tasks/0604T007.md` 与 `.workflow/reports/0604T007-business.md`，核对任务边界、验收标准、业务结论与提交信息。
2. 核对 commit `066a6a8` 的改动范围，确认包含 task、runner、focused tests 和 task-scoped artifacts；核对业务报告提交 `6606088`。
3. 阅读 `examples/hyperliquid/canonical_horizon_regime_diagnostics.py`、focused tests、manifest/report/CSV 产物，确认 canonical-only 输入约束、watch-only 输出和 boundary flags。
4. 复跑最小验证命令：`--help`、`py_compile`、focused pytest。
5. 对 `local_live_analysis/event_mode_canonical_pricing_signal_0604T003/` 运行 diagnostics runner 到 `/tmp/qa_0604T007_horizon_regime/`，解析复现 manifest。
6. 对 `local_live_analysis/event_mode_canonical_pricing_signal_0604T003/synthetic_diagnostic_comparison/` 运行 diagnostics runner，确认 diagnostic-only synthetic input 被拒绝。
7. 运行 `git diff --check`，确认无格式问题。

实际结果：
- `git show --stat --oneline 066a6a8` 确认提交为 `0604T007 canonical horizon regime diagnostics`，包含 `.workflow/tasks/0604T007.md`、runner、focused tests 和 `local_live_analysis/canonical_horizon_regime_diagnostics_0604T007/**` artifacts。
- `git show --stat --oneline 6606088` 确认业务报告提交为 `0604T007 business report`。
- 正式 artifact manifest 记录 `canonical_sample_count=3`、`diagnostic_rejection_count=0`、`horizon_support_counts={"diagnostic_supported": 4, "watch_needs_more_samples": 2}`、`regime_support_counts={"diagnostic_supported": 10, "reject_aliased_or_concentrated": 2, "watch_needs_more_samples": 6}`。
- `python examples/hyperliquid/canonical_horizon_regime_diagnostics.py --help` 通过。
- `python -m py_compile examples/hyperliquid/canonical_horizon_regime_diagnostics.py examples/hyperliquid/test_canonical_horizon_regime_diagnostics.py` 通过。
- `python -m pytest examples/hyperliquid/test_canonical_horizon_regime_diagnostics.py` 通过，结果为 `5 passed`。
- QA 复跑 canonical 输入输出 `/tmp/qa_0604T007_horizon_regime/`，结果 `canonical_sample_count=3`、`horizon_rows=6`、`regime_rows=18`。
- `python -m json.tool /tmp/qa_0604T007_horizon_regime/horizon_regime_diagnostics_manifest.json` 通过；复现 manifest 记录 `horizon_support_counts={"diagnostic_supported": 4, "watch_needs_more_samples": 2}`、`regime_support_counts={"diagnostic_supported": 10, "reject_aliased_or_concentrated": 2, "watch_needs_more_samples": 6}`。
- QA 对 synthetic diagnostic comparison 输入运行 runner，命令按预期失败并返回 `DiagnosticsInputError: input has no canonical event-mode samples`，证明 diagnostic-only synthetic artifacts 不能作为 formal horizon/regime diagnostics 输入。
- `git diff --check` 通过。
- Caveat：正式 artifact manifest 的 `git_commit` 为 `494eb58`，这是 T007 artifacts 在提交前生成时的 HEAD；QA 在当前 HEAD `ae72fdb` 复现得到相同核心计数和边界 flags，未发现可复现性问题。

验收结论：
- 已通过
- 结论说明：
  - `0604T007` 满足 read-only canonical horizon / regime diagnostics 验收标准：runner 只接受 canonical event-mode evidence，拒绝 diagnostic-only synthetic input，产物明确把 `100/250ms` 标记为 watch-only，把 `500/1000/5000/10000ms` 保留为 diagnostic-supported，并且所有 regime bucket 仍为 watch-only diagnostics，不构成最终 regime selection 或 maker action。

通过项：
1. Runner 使用 `0604T004` canonical loader path，并通过本任务 guard 拒绝无 canonical event-mode sample 的 synthetic diagnostic input。
2. Horizon diagnostics 输出 nominal horizon coverage、effective future age、future-row-delta 支持和短 horizon weak-independence caveat。
3. Regime diagnostics 输出 sample count、row count、direction consistency、effect concentration、row concentration proxy、horizon-level feature/correlation context 和 support status。
4. 产物完整存在：`horizon_independence_diagnostics.csv`、`regime_conditioning_diagnostics.csv`、`regime_watch_list.csv`、`horizon_regime_diagnostics_manifest.json`、`horizon_regime_diagnostics_report.md`。
5. Focused tests 覆盖 horizon alias classification、minimum-support classification、concentration penalty、non-canonical input refusal 和 report artifact generation。
6. 边界保持正确：未引入 new collection、final regime selection、case-library、shadow decisions、strategy/private/order/live/default-on/tiny-live/parameter search/promotion。

不通过项：
1. 无

缺陷清单：
1. 无

阻塞项：
- 无

建议总控下一步：
1. 将 `0604T007` 记为 `已通过`。
2. 后续如进入 high-confidence regime definition，应新建独立任务，并继续使用 `0604T003` canonical event-mode artifacts 与 `0604T004/0604T005` loader/guard source-lock；本任务不授权 final regime selection 或 maker strategy。

提交信息：
- commit：066a6a8（业务代码/产物）；6606088（业务报告）
