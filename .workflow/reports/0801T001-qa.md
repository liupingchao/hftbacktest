# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0801T001

状态：
- 已通过

更新时间：
- 2026-08-01 15:14 CST

验收线程：
- QA验收线程

验收对象：
- 业务线程-python/cross-exchange-research + 0801T001

验收范围：
- T017 首轮 QA 的 P1 x3 / P2 x1 修复。
- segment/timeline/venue row identity、34-file provenance stability、R1
  contract drift 和 existing-output atomic publication。
- 真实八段 v2 trigger audit、episodes、segment summary 和 manifest。
- 本轮未修改实现代码或真实产物，未访问网络、AWS、SSH，也未新增采集。

## Findings

- P0：无。
- P1：无。
- P2：无。
- P3：无。

验收步骤：
1. 阅读 workflow-kit、0801T001 task/business report、T017 首轮 QA 和
   motif contract。
2. 审查 exact identity、segment-manifest provenance/rehash、R1 gates 和
   macOS atomic directory exchange 实现。
3. 对 identity、R1 contract、构建中 mutation 和 publication 执行独立
   failure injection。
4. 运行 focused、alignment + motif、py_compile 和 CLI 验证。
5. 从原始八段 Binance trade、timeline 和 Hyperliquid BBO 独立重建
   threshold candidate、confirmation、attribution 和 primary episode。
6. 全量复算 input/output SHA、row、identity、horizon direction、
   coverage、isolation、rejection 和 aggregate reconciliation。

实际运行命令：
1. `python -m pytest examples/hyperliquid/test_cross_exchange_liquidity_response_episodes.py -q`
2. `python -m pytest examples/hyperliquid/test_cross_exchange_alignment_acceptance.py examples/hyperliquid/test_cross_exchange_liquidity_response_episodes.py -q`
3. `python -m py_compile examples/hyperliquid/cross_exchange_liquidity_response_episodes.py examples/hyperliquid/test_cross_exchange_liquidity_response_episodes.py`
4. `python examples/hyperliquid/cross_exchange_liquidity_response_episodes.py --help`
5. `PYTHONPATH=examples/hyperliquid python - <<'PY' ...`：27 项 identity、
   R1 drift、mutation 和 atomic-publication failure injection。
6. `python - <<'PY' ...`：真实八段完整 provenance/output/episode
   independent rescan。

实际结果：
- Tests：
  - motif focused：`14 passed`
  - alignment + motif：`22 passed`
  - py_compile：通过
  - CLI `--help`：通过
- 首轮 identity 缺陷已闭合：
  - segment manifest 的 campaign/profile/Binance symbol/Hyperliquid coin
    漂移均失败。
  - timeline 行 campaign/segment/profile 漂移均失败。
  - Binance trade symbol 和 Hyperliquid BBO coin 漂移均失败。
  - 真实数据扫描 `10,744,733` 条 Binance hot、`541,122` 条
    Hyperliquid hot 和 `556,861` 条 timeline，identity 错误为 `0`。
- provenance/stability 已闭合：
  - 最终 v2 manifest 含 `34` 个输入：
    R0 manifest `1`、R1 manifest `1`、segment manifest `8`、Binance hot
    `8`、Hyperliquid hot `8`、timeline `8`。
  - 34 个输入 SHA 和适用的 row count 全部一致。
  - 构建中修改 segment manifest 会以
    `input changed during build` 失败。
- R1 contract 已闭合：
  - primary horizons 必须精确为 `1000/2000ms`。
  - 两档 tolerance 必须精确为 `250ms`。
  - `100/250/500ms` 必须属于 diagnostic subset。
  - 七个 pass gate 任一回退均失败。
  - timestamp regression、future decision join、cross-segment label 三个
    zero counter 任一非零均失败。
- atomic publication 已闭合：
  - macOS 使用单次 `renamex_np(RENAME_SWAP)` 交换目录。
  - 已有 output 成功替换后只保留完整新产物。
  - 注入 exchange failure 时旧 output 和 marker 保持存在，temporary
    output 被清理。
  - `2,000` 次原生目录交换期间完成 `51,065` 次并发可见性观察，
    output path 缺失次数为 `0`。
- 真实 v2 产物：
  - task ID `0801T001`，schema
    `hyperliquid_liquidity_response_motif_v2`，`passes=true`。
  - `10/10` 个输出文件 SHA/row 全部一致。
  - trigger candidates：`268,522`。
  - primary episodes：`141,768`。
  - buy/sell：`71,507 / 70,261`。
  - economic/zero-economic trades：`5,117,387 / 377`。
  - attribution：
    `234,526 trade_driven / 19,032 mixed / 0 cancel_driven /
    14,964 uncertain`。
  - 从原始 trade 流重建 burst、`>=30%` threshold、strict pre-state、
    首个 `0-100ms` confirmation、decision-time attribution 和 unique
    confirmation，差异为 `0`。
  - diagnostic `100/250/500ms` source 均 `<= target`；primary
    `1000/2000ms` source 均 `>= target`，future/cross-segment/fast-source
    错误为 `0`。
  - 八段 primary coverage 均 `>=95%`，最低为
    `95.51959489211801%`。
  - isolation `100/250/500/1000/2000ms` 为
    `25,561 / 4,949 / 540 / 19 / 1`。
  - rejection、isolation、contamination 和 aggregate reconciliation
    差异均为 `0`。
  - output 同级目录无 `.tmp` 或 `.backup-*` 残留。

验收结论：
- 已通过
- 结论说明：
  - T017 首轮 P1 x3 / P2 x1 已由硬门禁、反例和真实八段复算闭合。
    `Hyperliquid liquidity-response motif family` 的 M1 episode-builder
    已完成。

通过项：
1. profile/timeline/venue identity fail closed。
2. 34-file provenance 和构建后 input stability。
3. R1 horizon、tolerance、七个 pass gate 和三个 zero counter。
4. macOS existing-output 原子目录交换和失败保留语义。
5. 真实八段 threshold、confirmation、attribution、horizon 和
   reconciliation 全量复算。
6. 本地-only、无网络/AWS/SSH/新增采集边界。

不通过项：
1. 无

缺陷清单：
1. 无

阻塞项：
- 无

建议总控下一步：
1. 将 `0801T001` 标记为 `已通过`。
2. 将 M1 episode-builder 标记为完成。
3. motif clustering、walk-forward stability、经济过滤或 shadow signal
   evaluation 必须另建正式任务，不应视为本任务已执行内容。

提交信息：
- commit：无
