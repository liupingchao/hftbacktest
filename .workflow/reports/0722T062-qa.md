# QA 验收结果

## Findings（按严重性）

- P0：无。
- P1：无。
- P2：`boundary_manifest.json` 将 T061 的
  `no_shared_kernel_change=true` 原样嵌入
  `inherited_basis_boundary`，但顶层没有明确说明 T062 已修改 shared
  kernel。嵌套 task ID 能解释来源，但对人工审计和扁平化消费者仍有误导
  风险。
- P2：shared-kernel validator 未完整冻结 accepted contract。它仍接受额外
  字段、错误 task/source identity、错误 normalization 标签和变更后的
  training row count；canonical hash 只被计算，没有在 kernel 入口核对预期
  frozen hash。
- P2：legacy path 的 `side_mapping` 被统一硬编码。对 accepted contract
  行为等价，但缺失或非法 side mapping 的旧调用不再保持提交前 fail-closed
  语义，因此“legacy path 完全不变”尚未成立。

执行线程：
- QA验收线程

任务ID：
- 0722T062

状态：
- 未通过

更新时间：
- 2026-07-22 16:33 Asia/Shanghai

验收线程：
- QA验收线程

验收对象：
- 业务执行线程 `0722T062`
- implementation commit
  `da0a6198f8186df410f22b2eea641ce5abafe3bb`
- workflow/business commit
  `32eb26ec2f59d55ba6b047189672b7e5a55d358d`

验收范围：
- Basis contract validator/hash、legacy/default-off parity、forecast unit、
  shared-kernel entry point、warning propagation、artifact boundary 和
  deterministic rebuild。
- 不验收 live、private/account、order/cancel、economics、promotion 或
  default-on。

验收步骤：
1. 阅读 T062 task/business、implementation diff、shared kernel、shadow
   runner、tests 和 official artifacts。
2. 运行 kernel + production-shadow focused tests。
3. 静态核对 basis forecast ticks、shared-kernel call path、endpoint boundary、
   warning set、same-package scope 和 artifact portability。
4. 对 official artifact 原位重建，比较重建前后全部 SHA-256。
5. 独立审查 inherited boundary、strict frozen-contract validation 和 legacy
   side-mapping parity。

实际结果：
- Focused：
  `20 passed in 0.09s`；独立 QA 复跑为 `20 passed in 0.10s`。
- Official runner exit `0`，重建前后全部 artifact hash 一致，
  `git status` 未出现 artifact diff。
- Basis branch 默认 `None`；official runner 调用
  `evaluate_shared_kernel()`。
- Basis output 单位直接为 forecast move ticks，未再次乘
  `expected_move_ticks_per_signal_z`。
- `10,704` decision rows 的 contract canonical hash 一致；private/order/
  credential flags 均为 false。
- 六项 T061 warning 全部传播；scope 明确为 same accepted training package
  mechanism/counterfactual shadow，不是新 OOS 或 live economics。
- Official paths 均为 repo-relative，未发现本机绝对路径。
- 核心计算、no-submit boundary 和 deterministic artifact 没有 P0/P1
  finding。
- 三项 P2 contract-truthfulness/parity finding 可稳定复现，现有测试没有将
  它们封闭。

验收结论：
- 未通过。
- 结论说明：
  - T062 核心数值和 no-submit shadow 可用，但 strict frozen-contract、
    legacy parity 和 artifact truthfulness 尚未达到任务声明，必须先完成
    小范围 repair。

通过项：
1. Basis forecast 单位正确，无二次乘系数。
2. Production shadow 经过 shared kernel，同一 package 重建确定。
3. No-submit/private/order/cancel/credential boundary 通过。
4. 六项 warning、same-package-not-new-OOS 语义和 repo-relative paths 通过。

不通过项：
1. T062 顶层 boundary 未明确区分“继承的 T061 未改 kernel”与“T062 已改
   kernel”。
2. Shared-kernel validator 未完整冻结 accepted contract identity/schema/hash。
3. Legacy invalid-side-mapping fail-closed parity 未保持。

缺陷清单：
1. 增加明确的 source-boundary snapshot 命名及 T062 kernel-change truth
   字段，避免继承声明被当作当前任务声明。
2. 冻结 exact accepted contract field set、identity、normalization/training
   metadata 和 expected canonical hash。
3. Legacy path 恢复读取/校验 legacy contract 的 `side_mapping`，并增加父
   提交行为 parity regression。

阻塞项：
- T062 在 repair QA 通过前不得进入 live 阶段。

建议总控下一步：
1. 创建唯一 repair task，严格只修复以上三个 P2。
2. 修复后重跑 focused、official deterministic rebuild 和 independent QA；
   不扩大到 watcher/live/default-on。

提交信息：
- QA commit：由本报告提交后的线程回报提供。

