# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0805T001

状态：
- 已通过

更新时间：
- 2026-08-05 17:36 CST（星期三）

验收线程：
- QA验收线程

验收对象：
- 业务线程-python/public-data-infra + cross-exchange-replay + 0805T001

验收范围：
- core-L2 recovered reconnect 严格证明与默认拒绝边界。
- epoch-aware L2 replay、R0 exact reconnect masks、R1 exact horizon masks。
- 2026-08-04 `0804T009` 4H raw 在 amdserver 的只读重建、provenance、
  gzip 完整性和重复构建确定性。

验收步骤：
1. 检查任务、实现、focused tests 和 hard boundaries。
2. 对两个隔离副本执行完整 postprocess/R0/R1。
3. 核对 reconnect transport marker、re-ACK、channel recovery、snapshot、
   epoch、freshness 和旧状态清理。
4. 核对 R0 mask identity、R1 horizon intersection、coverage 和
   cross-epoch/future-join 计数。
5. 对源 campaign 全树 SHA、全部 gzip 和两次构建核心文件 SHA 做对账。

实际结果：
- 默认 core reconnect hard failure 保留；opt-in 只接受全部恢复证据闭合
  的 fast/standard L2 reconnect。
- 非空 malformed JSON、缺少 recovery snapshot 等 hostile cases 均被拒绝。
- 两个 core track 各发布 `2` 个 epochs；reconnect 时旧 L2 state 被删除，
  timeline 在新 snapshot 前不输出旧状态。
- strict quality 通过且明确记录
  `continuous_exact_replay=false`、`segmented_replay_eligible=true`。
- timeline `501,495` 行，fast/standard/Binance stale rows 均为 `0`。
- R0 发布 `2` 条 core-L2 reconnect masks；Hyperliquid hot events 中
  epoch 0/1 分别为 `168,279 / 38,710` 行。
- R1 对 reconnect 内 `130` 个 decisions 和与 reconnect 相交的 `1,717`
  个 horizons 做精确排除。
- 八个 primary horizons coverage 均为 `100%`；cross-epoch label、
  future decision join、timestamp regression 均为 `0`。
- QA 首轮发现 gzip mtime 使压缩 SHA 不稳定；修复后两个完整副本的
  timeline、R0、R1 核心文件全部字节级一致。
- deterministic timeline SHA：
  `14161e17f331785a03cb86a5239146caf5f753f1d16357d4d2ffe3a16557ff86`。
- deterministic R1 label SHA：
  `a4a1076cb7c9bfb5223149679f04d3791441dcd3da58820767bb38630b0e2cd7`。
- 源 campaign `33` 个文件 SHA 在全部操作前后完全不变；全部生成 gzip
  通过完整性检查。
- 本机 `90` 项 focused tests、`py_compile`、`git diff --check` 全部通过。

验收结论：
- 已通过
- 结论说明：
  - recovered reconnect、epoch isolation、exact horizon masks、源数据
    只读性和整批 4H 重复构建确定性均满足任务验收要求。

通过项：
1. legacy empty-frame transport marker 与 malformed JSON 严格区分。
2. fast/standard recovery snapshot、re-ACK 和 channel recovery closure。
3. reconnect epoch 隔离与禁止旧簿跨 epoch forward-fill。
4. R0 core reconnect exact masks。
5. R1 decision/horizon exact intersection exclusion。
6. amdserver source immutability、runtime provenance、gzip 和 byte-level
   determinism。

不通过项：
1. 无。

缺陷清单：
1. P0：无。
2. P1：无。
3. P2：无。
4. P3：无。

阻塞项：
- 无。

建议总控下一步：
1. 关闭 0805T001。
2. 后续正式 signal/arbitrage 研究使用 R0/R1 segmented replay masks，
   不跨 reconnect epoch 形成 feature window、label 或 PnL。
3. 继续保留 exact fill、L3/L4 queue、可执行套利和 live promotion 的
   未授权边界。

提交信息：
- commit：无
