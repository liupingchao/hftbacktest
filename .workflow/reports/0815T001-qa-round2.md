# QA 验收结果

执行线程：
- QA验收线程（第二轮独立 re-QA）

任务ID：
- 0815T001

状态：
- 未通过

更新时间：
- 2026-08-15 14:38 CST（星期六）

验收线程：
- QA验收线程

验收对象：
- 业务线程-python/research + 0815T001 首轮 QA bounded repair
- 修复版正式包
  `local_live_analysis/skhynix_trigger_aligned_episode_research_v1_stage02_density/`
- 业务声称的修复版 core SHA256：
  `532448d498c8206cd5669d26f1c459b46c3c0505690b15957925182b3f98ee03`
- 修复版隔离构建：
  `/tmp/0815T001-r1-density-a.jFmowo/package`
  和 `/tmp/0815T001-r1-density-b.ogtFnV/package`

验收范围：
- 独立复核首轮 P1 x3 / P2 x1，不沿用业务结论或自动翻转首轮状态。
- 审查 exact session/count/segment、source/Stage1 inventory、per-session
  merging result/category external anchors 是否由 production verify-only 消费。
- 在 `/tmp` 正式包副本执行 production-sized coherent-rehash hostile
  attacks，并区分目标 invariant 拒绝与无关 hash/runtime-source 拒绝。
- 运行 focused tests、recovery helper regression、Ruff、compileall、
  source/archived CLI、零写入检查和 `git diff --check`。
- 比较 formal、R1 Build A、R1 Build B 的全部 paths/bytes/SHA，并实际
  重哈希 source 与 accepted Stage 1。
- spot-check 首轮已确认数值、taxonomy、范围边界，并继续寻找新 P0-P3。

验收步骤：
1. 阅读 workflow、任务、首轮 QA、R1 业务报告、主实施方案和 controller
   tracking files，按首轮缺陷逐项建立独立验收矩阵。
2. 审查 production `verify_package()`、canonical contract、fixture
   monkeypatch 和新增回归，确认 external constants 的实际调用路径。
3. 复制修复版正式包到 `/tmp`，同步修改 payload、manifest artifact
   records 和 core hash，分别由 source CLI 与 archived CLI 验证。
4. 运行完整 focused regression、原始 recovery helper regression、Ruff、
   compileall、CLI help、正式包 verify-only 和全仓 `git diff --check`。
5. 对正式包 verify-only 前后执行完整文件 inventory，确认 archived CLI
   不产生缓存、新路径或字节变化。
6. 独立计算 formal/Build A/Build B 全目录 inventory，并按 published
   binding rows 实际重哈希全部 source/Stage1 文件。
7. 与首轮 Build A 逐文件比较，并从当前 CSV/gzip spot-check counts、
   rates、quantiles、coverage、merging、ESS、sensitivity 和禁止字段。
8. 在旧缺陷定向复现之外继续做 coherent-rehash code review attacks。

实际结果：
- 首轮 P1 完整 frozen session 删除的原攻击形态已 fail closed：
  `/tmp/0815T001-qa2-hostile-0ofa2hy5/drop-complete-aug04/package`
  删除两个 membership gzip 和全部 small CSV 的完整 `aug04`，同步更新
  manifest session/exact/artifact/core hashes。source CLI 与 archived CLI
  均返回 `rc=2`、`ERROR: frozen session set drift`。
- 首轮 P1 provenance 行删除的原攻击形态已 fail closed：
  `/tmp/0815T001-qa2-hostile-0ofa2hy5/omit-provenance-rows/package`
  删除 Jul30 source manifest 与 accepted Stage 1
  `data_admission/data_admission.md` 的 before/after 行，并同步更新
  inventory/artifact/core hashes。两个 CLI 均返回 `rc=2`、
  `ERROR: frozen provenance inventory contract drift`。
- 首轮 P1 recovery/decision 分类替换的原攻击形态已 fail closed：
  `/tmp/0815T001-qa2-hostile-0ofa2hy5/forge-recovery-categories/package`
  对三 session 写入总数守恒的 forged category maps 并同步重哈希。
  两个 CLI 均返回 `rc=2`、
  `ERROR: frozen episode-merging result drift`。
- 首轮 P2 taxonomy 的替换攻击已 fail closed：
  `/tmp/0815T001-qa2-hostile-0ofa2hy5/forge-session-evidence-labels/package`
  恢复旧 Jul30/Aug03 非法 aggregate labels 后，两个 CLI 均返回 `rc=2`
  并命中 `trigger_density.evidence_label drift`。
- Jul30 segment role 精确冻结已 fail closed：
  `/tmp/0815T001-qa2-hostile-0ofa2hy5/forge-jul30-segment-role/package`
  仅把 `segment_0005` 从 `historical_internal_validation` 改为允许枚举内的
  `historical_discovery` 并同步重哈希，两个 CLI 均返回 `rc=2`，精确命中
  `frozen_segment_evidence_labels.jul30.segment_0005` canonical value drift。
- 上述五项均使用当前修复版 runtime source，source/archived 的拒绝原因
  完全一致；没有被旧 runtime source、runtime copy drift 或普通 artifact
  hash drift 抢先拒绝。
- Production verify-only 确实消费新增 anchors：
  `cross_exchange_trigger_density_admission.py:2748-2760` 精确比较 frozen
  session set/count/segment set；
  `:2828-2841` 比较 source/Stage1 file_count、total_bytes 和包含
  path/size/SHA/role/session/segment 的 inventory SHA；
  `:2270-2325` 比较每 session cluster/flow/overlap、boundary、
  merge/non-merge、recovery 和 decision exact mapping。
- Fixture monkeypatch 只在 pytest fixture 生命周期内替换小型测试
  constants；正式包 source/archived hostile attacks 未使用 monkeypatch，
  且均命中 production constants。
- 继续代码审查发现新 P1：small CSV lookup 未验证 exact primary-key set
  和 cardinality。`density/inter/episode/sensitivity` 通过 dict
  last-write-wins 折叠，只有 ESS 检查 duplicate，且 ESS 也不检查额外
  unique key。
- 在
  `/tmp/0815T001-qa2-duplicate-h5ajngyz/duplicate-forged-episode/package`
  前置一条 Jul30 forged recovery/decision row、保留后面的合法同 key 行，
  同步重哈希后 source/archived CLI 均返回 `rc=0`、`verified=true`，
  core SHA256 为
  `2b379d66abdd9dc9a0f20080163bf596298bb4e7fe86a7c5ae2a07896e6b13be`。
- 在
  `/tmp/0815T001-qa2-duplicate-h5ajngyz/duplicate-invalid-evidence/package`
  前置一条非法 Jul30 evidence label、保留后面的合法同 key 行，同步
  重哈希后两个 CLI 均返回 `rc=0`、`verified=true`，core SHA256 为
  `0a36527ac7d18e3a51d0b47e30923a5fb691f47c9285295be5284f67ead1ee07`。
- 在 `/tmp/0815T001-qa2-extra-row-z6skcqjx/package` 追加一个
  `forged_session` episode row 和一个 `forged_population` density row，
  两行包含任意 forged category/evidence values；同步重哈希后两个 CLI
  仍返回 `rc=0`、`verified=true`，core SHA256 为
  `9908ebfb02f9b19b48cb6865c539f38cf39904c0b17ccc6678b6970584e12711`。
- 完整 focused tests：
  `python -m pytest -q` 四个 Stage 2 focused test files 得到
  `80 passed in 6.77s`。
- Reused hierarchy helper 的三项原始 regression 得到
  `3 passed in 0.04s`，覆盖 dense no-recovery merge、accepted recovery
  checkpoint 和 missing recovery fail-closed。
- Ruff 返回 `All checks passed!`；compileall 使用外置
  `/tmp/0815T001-qa2-pycache.2j6LyI` 通过；全仓
  `git diff --check` 返回 `0`；source/archived CLI help 均通过。
- 当前正式包 source CLI 和 archived CLI `--verify-only` 均返回
  `artifact_count=15`、`candidate_membership_rows=463612`、
  `confirmed_rows=267554`、`session_count=3`、`verified=true` 和业务声称的
  core SHA256
  `532448d498c8206cd5669d26f1c459b46c3c0505690b15957925182b3f98ee03`。
- Archived CLI 运行前后正式包完整 `(path, bytes, SHA256)` inventory
  完全相同，包内没有 `__pycache__`、`.pyc` 或新增路径。
- Formal、R1 Build A、R1 Build B 均为 `16` files、
  `19,814,426` bytes；全部 `(relative_path, bytes, SHA256)` mismatch 为
  `0`，独立全目录 inventory SHA256 均为
  `1cd78478575cff2eb4fe8bf167f570a1c8412126ab4219cfd05f76b152d3f977`。
- 三包 manifest core SHA256 均为
  `532448d498c8206cd5669d26f1c459b46c3c0505690b15957925182b3f98ee03`。
- Source binding 实际重哈希为 `123` files、`169,677,903` bytes、
  inventory SHA256
  `94bad85bfd4981b351f84c53628099468ec27f13d308402ac7125a9d582a6644`；
  accepted Stage 1 为 `14` files、`1,401,387` bytes、inventory SHA256
  `c540cc056313716b3bdd2b9c0fe076cda15a7f152b399ae6a1283b3aa8aa6590`。
  Published before/after rows相同，实际 bytes/SHA mismatch 为 `0`，验收
  前后重哈希结果不变；Stage 1 core SHA256 保持
  `9c2756f966a1a05816f9e6a91d7361d76cfea10fc3c587944ca2f0c1d2eb7c96`。
- 修复版与首轮 Build A 比较，只有 `density_manifest.json`、
  `frozen_density_contract.json`、
  `runtime_source/cross_exchange_trigger_density_admission.py` 和
  `trigger_density_by_session.csv` 变化；其余 `12` 个文件逐字节不变。
- `trigger_density_by_session.csv` 的旧新差异仅为 Jul30/Aug03
  evidence label/caveat；counts、rates、coverage 和 blocks 均未变化。
- 首轮数值 spot-check 保持：
  counts 为 `268522/141768`、`127622/82533`、`67468/43253`；
  cluster/flow/all-candidate-overlap 为 `39928/10536/9`、
  `47193/23397/232`、`23113/9451/6`；
  Bartlett A/B ESS 为
  `1096.308299192916/837.102916741897`、
  `2195.991765333518/2085.852133598072`、
  `275.582545311320/263.249320034331`。
- Inter-trigger p01/p50/p99、六类 sensitivity selected counts 和三
  session recovery/decision exact maps 与首轮 QA 报告一致。
- 两个 membership gzip 各为 `463612` rows；candidate session counts 和
  segment sets 精确为 Jul30 8、Aug03 10、Aug04 1，schema 中没有
  response/outcome/markout/PnL/model/score/action/order/cancel/private 字段。
- Source binding role 只包含 trigger audit/manifest、timeline、public
  collector manifests 和 common L2 timeline；按 production date/basename
  policy检查，Aug07 full rows 和 outcome-like source path 均为 `0`。
- 本轮未读取 Aug07 full event rows，未运行 response/outcome/model/PnL/
  actionability，未访问 private/account/order/cancel endpoint。

验收结论：
- 未通过
- 结论说明：
  - 首轮四项缺陷的原始“删除或替换”复现已按目标 invariant fail closed，
    当前正式包数值、双构建、只读验证和范围边界也均有充分正向证据。
  - 但 production verifier 没有冻结 small CSV 的 exact key set 和
    cardinality，导致 forged recovery categories 与非法 evidence labels
    可作为 duplicate shadow row 或额外 unique row 进入同步重哈希包并
    `verified=true`。这直接绕过本轮要关闭的分类/taxonomy invariant，
    因此不能裁决首轮 P1/P2 已真正关闭。

通过项：
1. 完整 frozen session 删除、provenance omission、分类替换、非法 label
   替换和 Jul30 segment role drift 的定向修复均命中目标 invariant。
2. External session/count/segment、inventory 和 merging-result anchors
   已进入 production verify-only；fixture monkeypatch 未削弱正式常量。
3. Focused tests、recovery helper regression、Ruff、compileall、CLI help、
   source/archived verify-only 和全仓 diff check 均通过。
4. Formal 与两个 R1 隔离构建全部 paths/bytes/SHA 相同；archived CLI
   零写入；source 与 accepted Stage 1 实际文件保持 immutable。
5. 首轮数值 artifacts 未变化；taxonomy 当前值正确；本轮未越过 Stage 2
   的 Aug07/outcome/model/PnL/actionability/private/order/cancel 边界。

不通过项：
1. P1：small CSV exact key/cardinality verification fail-open，可用 duplicate
   shadow row 或额外 unique row 绕过 recovery/category 与 evidence
   taxonomy anchors。

缺陷清单：
1. P0：无。
2. P1：small-artifact primary-key closure 缺失，首轮分类/taxonomy invariant
   仍可被 coherent-rehash shadow rows 绕过。
   - 文件/行号：
     `examples/hyperliquid/cross_exchange_trigger_density_admission.py:2649-2671`
     对 density/inter/episode/sensitivity 使用 dict comprehension；
     duplicate key 被 last-write-wins 覆盖。代码只检查 ESS duplicate，
     且未对任一 small artifact 比较完整 expected key set/cardinality。
     `:2596-2603` 和 `:2270-2325` 只验证 lookup 选中的单个 episode row。
   - 复现 A：
     `/tmp/0815T001-qa2-duplicate-h5ajngyz/duplicate-forged-episode/package`
     在合法 Jul30 row 前插入总数任意的 forged recovery/decision row，
     保留合法同 key row 并同步重哈希；source/archived CLI 均 `rc=0`、
     `verified=true`，core
     `2b379d66abdd9dc9a0f20080163bf596298bb4e7fe86a7c5ae2a07896e6b13be`。
   - 复现 B：
     `/tmp/0815T001-qa2-duplicate-h5ajngyz/duplicate-invalid-evidence/package`
     前置非法 evidence label、保留合法同 key row并同步重哈希；两个 CLI
     均 `rc=0`、`verified=true`，core
     `0a36527ac7d18e3a51d0b47e30923a5fb691f47c9285295be5284f67ead1ee07`。
   - 复现 C：
     `/tmp/0815T001-qa2-extra-row-z6skcqjx/package` 追加未冻结 session /
     population 的任意 category/evidence rows 并同步重哈希；两个 CLI 均
     `rc=0`、`verified=true`，core
     `9908ebfb02f9b19b48cb6865c539f38cf39904c0b17ccc6678b6970584e12711`。
   - 影响：
     CSV 下游可能遍历全部行或采用 first occurrence，而 verifier 采用
     last occurrence 或完全忽略额外 key。正式包因而可同时包含合法锚定行
     和伪造分类/非法 taxonomy 行，破坏 exact-one-row 语义与可审计消费。
3. P2：无。
4. P3：无。

阻塞项：
- 无。新 P1 可在 0815T001 范围内 bounded repair。

建议总控下一步：
1. 保持 `0815T001` 为 `未通过`；不得解锁 detector parity、Episode v3、
   response/outcome/model/actionability 或 Aug07 event-row first-read。
2. 对 density、inter-trigger、episode summary、sensitivity summary 和
   ESS 分别构造完整 expected primary-key set，要求
   `len(rows) == len(lookup) == len(expected_keys)` 且 key set 精确相等。
3. 永久回归应覆盖 duplicate forged row 位于合法行前后、额外 unique
   session/population/side/sensitivity/ESS key，以及同步 artifact/core
   rehash；每一行都必须经过 taxonomy/category 语义校验。

提交信息：
- commit：无
