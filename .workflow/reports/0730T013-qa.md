# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0730T013

状态：
- 未通过

更新时间：
- 2026-07-30 23:50 CST

验收线程：
- QA验收线程

验收对象：
- 业务线程-python/cross-exchange-research + 0730T013

验收范围：
- R0 builder fail-closed/atomic contract、timeline boundary、raw
  identity/count/provenance、degraded masks、真实八段产物和新增采集边界。

实际结果：
- P0: none
- P1: none
- P2: `3`
- P3: none
- P2-1:
  `--clean-output` 在新构建成功前删除旧输出，后续失败不能保留旧产物。
- P2-2:
  timeline index 的 campaign id、continuity、首尾时间没有与 timeline
  manifest 和真实 CSV 闭环。
- P2-3:
  Binance depth symbol、standard-L2 coin、raw row/extra channel 和 timeline
  source-raw SHA 门禁不完整。
- 真实产物复算本身通过：
  - `24/24` output gzip SHA/row/time/event-seq/symbol/coin
  - `56/56` source SHA
  - `8/8` timeline gzip
  - `48/48` raw gzip
  - Binance hot `10,744,733`
  - Hyperliquid hot `541,122`
  - auxiliary `49,169`
  - exploded Hyperliquid trades `398,224`
  - degraded asset_context `5` rows
  - degraded main_all_mids `2` rows
- `46 passed`; compile/help/diff checks pass。
- 新增采集授权边界保持。

验收结论：
- 未通过
- 结论说明：
  - 当前真实产物没有发现损坏，但 builder 未达到声明的 fail-closed
    contract，不能解锁 R1。

通过项：
1. 真实产物内容、计数、SHA、时间序和 degraded masks。
2. 本地-only 与新增采集授权边界。

不通过项：
1. Clean replacement failure preservation。
2. Timeline index/manifest/CSV boundary closure。
3. Raw identity/count/provenance closure。

缺陷清单：
1. P2 atomic replacement。
2. P2 timeline boundary validation。
3. P2 raw validation。

阻塞项：
- R1 alignment acceptance 被阻塞，直到修复任务独立 QA 通过。

建议总控下一步：
1. 派发 offline T014 repair。
2. 增加 failure injection tests。
3. 重建真实产物并重复完整复算。

提交信息：
- commit：无
