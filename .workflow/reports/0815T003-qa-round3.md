# QA 验收结果

执行线程：
- QA验收线程（`0815T003` Stage 4 第三轮全新独立 QA）

任务ID：
- `0815T003`

状态：
- 未通过

更新时间：
- 2026-08-16 20:01 CST（星期日）

验收线程：
- QA验收线程

验收对象：
- 业务线程-python/research + `0815T003`
- 工作目录：
  `/Users/liu/Documents/hftbacktest-0814t001-skhynix-episode-research`
- 分支：
  `codex/0814t001-skhynix-episode-research`
- 正式 package：
  `local_live_analysis/skhynix_trigger_aligned_episode_research_v1_stage04_jul30_episode_v3/`
- 全新随机 QA 根：
  `/tmp/0815T003-qa-round3.eMzRXA`

验收范围：
- 仅 Jul30 immutable public-data inputs。
- 未复用任何 `/tmp/0815T003-qa*`、`/tmp/0815T003-r1*`、
  `/tmp/0815T003-r2*` 或业务 evidence。
- 未解析禁止的 Jul30 legacy `episodes/*.csv.gz`。
- 未读取 Aug03/Aug04 future event rows 或任何 Aug07 event rows。
- 未运行 case/model/score/actionability/orders/network/live config。
- accepted Stage 1/2/3、Jul30 raw/R0/R1、正式 package 和输入只读。
- 仓库写入仅限本报告及 byte-identical
  `docs/qa-acceptance-report.md`。

## Findings First

严重度汇总：
- P0：`0`
- P1：`1`
- P2：`0`
- P3：`0`

### P0 Findings

- 无。

### P1 Findings

#### P1-1 Standalone admission 接受不完整且自相矛盾的 aggregate evidence

判定：
- 未通过。
- 第二轮返修已经让实际 `verify_package()` 重建完整 Family A/B
  feature/view projection；当前正式 package 数据和 production package
  mutation 均未发现新的 source-semantic 漏口。
- 但 standalone admission 自己只验证
  `_source_semantic_verification["aggregate"]` 是一个 `dict`，不验证：
  - 必须存在全部 `12` 个 exact projections；
  - projection key universe 不可缩小或增加；
  - `expected_rows` / `observed_rows` 等于 frozen contract count；
  - `expected_sha256 == observed_sha256`；
  - digest 是合法 64-hex；
  - `mismatch_rows == 0`；
  - `fields` 精确等于对应 frozen schema。
- 因此，source 与 archived standalone admission 都会把缺失 `11` 个
  projections、row counts/digests 明确冲突、`mismatch_rows=999` 的
  aggregate evidence 宣称为 `source_semantic_verified=true`。
- 这直接违反本轮预注册验收条件：
  standalone admission 不能在 aggregate 缺失、缩小、row count/digest
  不符时宣称 source-semantic verified。

代码证据：
- `examples/hyperliquid/cross_exchange_jul30_episode_v3_admission.py:50-58`
  只检查 `aggregate` 的 Python 类型。
- `examples/hyperliquid/cross_exchange_jul30_episode_v3_admission.py:63-75`
  随后无条件输出 `verified=true`、`source_semantic_verified=true` 并原样
  回传 aggregate。
- `examples/hyperliquid/test_cross_exchange_jul30_episode_v3_admission.py:20-55`
  的正向 fixture 自身只提供一个 `features_family_a` projection，也没有
  证明完整 aggregate universe。
- 现有负向测试
  `examples/hyperliquid/test_cross_exchange_jul30_episode_v3_admission.py:59-82`
  只覆盖整个 semantic evidence 缺失，不覆盖 aggregate 缩小、count、
  digest、mismatch 或 fields drift。

最小复现：
- QA 脚本：
  `/tmp/0815T003-qa-round3.eMzRXA/qa_aggregate_evidence_attack.py`
- 该脚本分别加载 current source 与正式 package 内 archived admission，
  保持 exact scope，但让 `verify_package()` 返回：

```json
{
  "aggregate": {
    "features_family_a": {
      "expected_rows": 1,
      "observed_rows": 0,
      "expected_sha256": "bbbb...bbbb",
      "observed_sha256": "cccc...cccc",
      "mismatch_rows": 999,
      "fields": []
    }
  }
}
```

- source 实际结果：
  `rc=0`, `verified=true`, `source_semantic_verified=true`。
- archived 实际结果：
  `rc=0`, `verified=true`, `source_semantic_verified=true`。
- 两侧都原样回传上述伪造 aggregate。

影响：
- 当前正式 package 的实际 rows 仍可正确；本 finding 不表示现有
  Episode v3 data rows 已损坏。
- 但 standalone admission 的 attestation contract 仍是 fail-open：
  它宣称的 aggregate 完整性大于实际检查范围。
- Stage 5 会把 standalone admission 作为 Stage 4 输入信任边界；在该
  断言闭合前，不能把 `source_semantic_verified=true` 当作 durable
  publication fact。

必须返修：
1. 新增唯一的 expected aggregate contract，精确冻结全部 `12`
   projections、每个 projection 的 row count 和 fields。
2. standalone admission 在成功前逐 projection 验证 exact key set、
   exact fields、expected/observed counts、digest equality、64-hex 和
   `mismatch_rows=0`。
3. aggregate 缺失、缩小、增加、row count drift、digest mismatch、
   malformed digest、fields drift 和 nonzero mismatch 必须全部 `rc=2`。
4. 正向 test fixture 必须包含完整 aggregate，不得再用单 projection
   fixture 代表 complete evidence。
5. 修复后重建正式 package/runtime archive，并进入第四轮全新独立 QA。

### P2 Findings

- 无。

### P3 Findings

- 无。

## Fresh Full Rebuild And Identity

命令：
- `/usr/bin/time -l python -B
  examples/hyperliquid/cross_exchange_trigger_aligned_episodes.py
  --output-dir
  /tmp/0815T003-qa-round3.eMzRXA/rebuild/package`

结果：
- 8 个 segment 全部完成。
- fresh rebuild：
  - files：`107`
  - symlinks：`0`
  - bytes：`1561252064`
  - Family A：`268522`
  - Family B：`141768`
  - rejected：`126754`
- QA 独立枚举 formal/fresh 全部 relative path、type、size 和 raw SHA：
  exact identical。
- 独立重算：
  - core：
    `4fc6ce074200a3c08f7a504f0ce2412f5dad8d7ad51aa44e1799f00f6dd886c9`
  - full：
    `1f3ddd61fa0bbbf3268ae6f09b303d4876e83f456d9e6ce63956b1306707dc72`
  - contract：
    `7e07af13742edadddcda32784b64e398586b7cff94c7fa9fdeb2d880b41bfb5c`
  - manifest：
    `898d5143a8a47a36b8ec0f8572621cd7a8bc542b692425feba9fcfe4efd08165`
- fresh rebuild wall time：`1305.37s`。
- fresh rebuild max RSS：`989986816 bytes`。
- 该 RSS 高于前轮业务报告的观察区间，记录为性能 caveat，不影响本轮
  correctness 判定。

## Complete Feature And View Projection

QA 临时独立扫描器：
- `/tmp/0815T003-qa-round3.eMzRXA/qa_inventory_projection.py`

扫描方式：
- 不读取 admission 成功结论。
- 对 formal/fresh 分别解压并流式遍历全部 rows。
- exact header/cell width。
- 8 segments 固定顺序。
- numeric candidate sequence 单调、无 duplicate/reorder。
- 每 candidate feature-name universe 和字段顺序固定。
- end-of-stream、missing/extra candidate 和 row cardinality 闭合。
- 全部 `VIEW_FIELDS` 由 anchor identity、landmark、segment boundary 和
  artifact path 逐字段独立重建。
- 全部 available feature 完整携带 value/provenance/calculation。
- 全部 unavailable feature 不携带 value/provenance。
- 每条 available feature 全量断言
  `observed_at_ns <= decision landmark`。

结果：
- Family A features：
  - rows：`23092892`
  - candidates：`268522`
  - feature names：`86`
  - unavailable：`1611806`
  - future observation：`0`
- Family B features：
  - rows：`15310944`
  - candidates：`141768`
  - feature names：`108`
  - unavailable：`1252049`
  - future observation：`0`
- total unavailable：`2863855`
- Family A views：`268522`
- Family B views：`141768`
- formal/fresh 全部 projection 扫描结果一致。

Source-sample replay：
- QA 脚本：
  `/tmp/0815T003-qa-round3.eMzRXA/qa_feature_source_samples.py`
- 对 8 个 segment 各自从 accepted source 独立重建并比较完整
  `FEATURE_FIELDS`：
  - Candidate pre-state `pre_binance_bid_px`；
  - Candidate trigger prefix count；
  - unavailable `trailing_basis_residual`；
  - Confirmed frozen burst count；
  - early-R Hyperliquid BBO count。
- 共 `40` 个跨 segment/category whole-row samples，mismatch=`0`。

Contract coverage：
- `exact_feature_projection` fields 精确等于全部 `FEATURE_FIELDS`，
  families/counts 为 `23092892 / 15310944`，feature universe
  `86 / 108`。
- `exact_view_projection` fields 精确等于全部 `VIEW_FIELDS`，
  populations `268522 / 141768`。
- production contract scope shrink 被 source/archive 双侧 `rc=2`。
- production manifest exact count 和 aggregate output count falsification
  也被双侧 `rc=2`。
- 本轮 P1 仅位于 standalone CLI 对返回 aggregate evidence 的二次断言。

## Confirmation And Outcome Replay

QA 独立全量脚本：
- `/tmp/0815T003-qa-round3.eMzRXA/qa_confirmation_outcome_replay.py`

Confirmation：
- confirmed candidates：`141768`
- confirmation feature rows：`1984752`
- 对每个 candidate：
  - 从 Candidate 起扫描 common timeline，确认第一条 depletion/drop
    state 精确等于 `t_confirm_ns`；
  - 按 fixed-origin 10ms、same-side、zero-economic reset 规则重建唯一
    frozen burst；
  - 重建 through-confirm prefix、touch qty、continuation 和 local Binance
    source-event count；
  - 比较全部 `14` confirmation feature rows 的完整
    `FEATURE_FIELDS`。
- mismatch：`0`。

First-event outcomes：
- interval-censored：`627406`
- right-censored：`177826`
- segment-censored：`316`
- quality-censored：`18`
- epoch-censored：`0`
- point-coerced：`0`
- lower `< Candidate`：`0`
- source event missing：`0`
- predicate/sign mismatch：`0`
- 非同 timestamp 的 earlier qualifying event：`0`
- same receipt timestamp 多 item 被正确视为同一 observation interval。

其他不变量：
- auxiliary-degraded grid rows：`4316`
- core-degraded grid rows：`0`
- anchor ordering mismatch：`0`
- cross-segment / cross-epoch path mismatch：`0 / 0`
- synthetic rejected confirm：`0`
- Family A 覆盖全部 rejected candidates，outcome/censor 不存在
  confirmation selection。

## Production-Size Coherent-Rehash Matrix

方法：
- 每个 attack 从正式 `1.56 GB` package 建立全量 APFS clone。
- 修改 load-bearing artifact 后使用 deterministic gzip 重写。
- 同步刷新 artifact bytes/SHA、core 和 canonical manifest。
- source 与 clone 内 archived verifier 分别运行。
- 不依赖陈旧 artifact/core/manifest hash。

结果：
- production package attacks：`32` 类。
- source/archive verifier calls：`64/64` 次均 `rc=2`。
- 通过的攻击类别：
  - 第二轮原例 Family A `pre_binance_bid_px` value；
  - feature identity、landmark、name、value、observed/source/book/
    calculation、availability；
  - feature missing、extra、duplicate、reorder、name substitution；
  - unavailable -> zero/available；
  - Family B non-confirmation 与 confirmation features；
  - view combined identity/landmark/artifact/population/quality mutation；
  - view missing、extra、duplicate、reorder；
  - anchor classification；
  - corrected degraded evidence deletion；
  - sparse count；
  - fixed-grid source age；
  - fixed-grid no-new-information + source identity；
  - event-count source identity；
  - outcome markout；
  - first-event interval lower；
  - dependency claim；
  - real input clone/source binding；
  - archived source + test coherent rehash；
  - contract scope shrink；
  - manifest exact count；
  - manifest aggregate output count。

决定性失败：
- standalone aggregate-evidence hostile：
  source/archive `0/0` 错误接受。
- 该 hostile 不修改 accepted package/input；它直接测试 standalone
  admission 对 `verify_package()` 返回的 attestation evidence 是否按其
 公开 contract fail closed。

## Tests And Static Verification

Pytest：
- Stage 4 focused：`46 passed in 0.09s`
- Stage 1/2/3 relevant inherited：
  `216 passed in 127.57s`
- R0/alignment/recovery：
  `83 passed in 0.45s`
- total：`345 passed`

其他：
- Ruff：`All checks passed!`
- external compileall：`4` 个目标 bytecode files，全部写入 QA `/tmp`
  pycache。
- scoped `git diff --check`：通过。
- deterministic gzip integrity：`96/96`。
- package 内 `.pyc/__pycache__`：`0`。

Atomic publication：
- 注入 `_process_segment` failure。
- injected failure observed：`true`
- output visible：`false`
- residual staging directories：`0`
- partial markers：`0`

## Positive Admission, Zero-Write And Performance

默认 Python、无 `-B`：
- source admission：
  - `rc=0`
  - `verified=true`
  - `source_semantic_verified=true`
  - wall：`911.80s`
  - max RSS：`818118656 bytes`
- archived admission：
  - `rc=0`
  - `verified=true`
  - `source_semantic_verified=true`
  - wall：`911.29s`
  - max RSS：`808075264 bytes`

两侧共同返回：
- files：`107`
- bytes：`1561252064`
- core：
  `4fc6ce074200a3c08f7a504f0ce2412f5dad8d7ad51aa44e1799f00f6dd886c9`
- full：
  `1f3ddd61fa0bbbf3268ae6f09b303d4876e83f456d9e6ce63956b1306707dc72`
- A/B/rejected：`268522 / 141768 / 126754`

Formal/input before-after snapshot：
- formal files：`107`
- unique input files：`394`
- records：`501`
- total bytes：`2973872801`
- path/bytes/raw SHA/mtime_ns/ctime_ns snapshot SHA：
  `8cc6cc715580262710e6d8c6977422a521b13511932496fbcca5eec07cd1ca9e`
- before/after `cmp`：identical。
- package/input 未被修改。

性能 caveat：
- complete source-semantic admission 是重型离线校验。
- fresh build 单进程约 `21m45s`，正向 source/archive 并行时每侧约
  `15m12s`。
- 当前实现可以终止且无 swap/kill，但不适合低延迟或频繁在线调用。
- 这是运维/研究成本 caveat，不改变本轮 P1 correctness 判定。

## Hard Boundary

- manifest 十项 boundary 均为 `false`。
- input binding rows：`788`。
- forbidden binding rows：`0`。
- runtime source network imports：`0`。
- 未读取 Jul30 legacy episode rows。
- 未读取 Aug03/Aug04 future event rows。
- 未读取任何 Aug07 event rows。
- 未运行 case/model/score/actionability/orders/network/live。
- 未修改 `.workflow/tasks/0815T003.md`、`task_plan.md`、`progress.md`、
  `findings.md`、实现、测试、正式 package 或 accepted inputs。

## 验收结论

- `未通过`
- 当前正式 Episode v3 rows、complete feature/view projection、
  confirmation/outcome semantics、deterministic rebuild 和 production
  package mutation boundary 均通过本轮独立检查。
- 但 standalone admission 会错误接受不完整且矛盾的 aggregate
  attestation evidence，并仍宣称
  `source_semantic_verified=true`。这是唯一但决定性的 P1。
- `0815T003` 不能置为 `已通过`。
- Stage 5 必须继续锁定。

通过项：
1. Fresh rebuild 与正式 package 107 文件逐字节一致。
2. 完整 Family A/B feature/view projection、row counts、field order、
   EOS 和 observed-at invariants 通过。
3. `141768` confirmation 与全部 first-event outcome 语义独立重放通过。
4. `32` 类 production package attacks 的 source/archive `64/64`
   fail closed。
5. `345` tests、Ruff、compileall、96 gzip、atomic、zero-write、
   input/formal immutability 和 hard boundary 通过。

不通过项：
1. Standalone admission aggregate evidence validation fail-open。

缺陷清单：
1. P1-1：aggregate 只做 `dict` 类型检查；shrunk/mismatched aggregate
   被 source/archive 错误接受并宣称 source-semantic verified。

阻塞项：
- 无外部 blocker；需要同一任务的第三轮有界返修和第四轮独立 QA。

建议总控下一步：
1. 将 `0815T003` 保持/置为 `未通过`，Stage 5 继续锁定。
2. 仅返修 standalone aggregate evidence exact validation 和对应 tests。
3. 不改 Episode v3 research rows、detector、feature/view values 或
   first-event semantics。
4. 返修后完整重建 package/runtime archive，并派发第四轮全新独立 QA。

提交信息：
- commit：无
