# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0511T004

状态：
- 已通过

更新时间：
- 2026-05-12 07:12 Asia/Shanghai

验收线程：
- QA验收线程

验收对象：
- 测试线程 + 0511T004

验收方式：
- 正常验收

验收范围：
- 基于 `0511T002` 的 Stage 6J replay 输出和新增 audit 字段，诊断 pure adverse timing candidates 为什么与 baseline 完全一致。核心问题是区分：`target_deterioration` 条件本身没有触发、触发了但没有落到 add-side action path、还是当前 replay / source-path 指标对 adverse-selection timing 风险不敏感。本任务只做诊断和报告，不改策略代码，不启动 live。

验收步骤：
1. 读取 `.workflow/tasks/0511T004.md`。
2. 读取 `.workflow/reports/0511T004-business.md`。
3. 检查任务状态、业务回报、verify 证据、done 结论和阻塞项。
4. 写入 `.workflow/reports/0511T004-qa.md` 并刷新看板。

实际结果：
- 任务文件存在：.workflow/tasks/0511T004.md
- 业务回报状态：待验收
- 业务回报存在：.workflow/reports/0511T004-business.md
- 结论 1：`target_deterioration` 不是没有触发。它在 replay audit 里大量触发，pure candidates 每个候选跨 4 样本为 `buy_active=118429`、`sell_active=160062`，reason 主要是 `target_deterioration`。
- 结论 2：当前 trigger 没有命中有效 add-side action path。所有 pure adverse timing candidates 的 active rows 与 `submit_buy` / `submit_sell` overlap 为 `0`，因此没有阻断实际 add-side submit。
- 结论 3：baseline 与 pure adverse timing candidates 在 row-level action path 上完全一致。4 个 run x 3 个 pure candidates 的 `action`、`planned_action`、position、target/working ticks 差异数均为 `0`。
- 结论 4：第一版 `target_deterioration` 语义与目标风险错位。helper 只在 `working.buy is not None` / `working.sell is not None` 时比较 working price 与 target price；但很多需要阻断的 add-side submit / re-add 场景发生在该 side 没有 working order 时，audit 中表现为 `working_*_tick=-1` 且 `adverse_timing_guard_*_active=0`。
- 结论 5：当 trigger active 时，它多数落在已有 stale working quote 的 cancel/keep 路径上；baseline 本来也会 cancel stale quote，所以没有增量 action delta。
- 结论 6：当前 Stage 6J replay 对 live adverse-selection source-path 不足够敏感。live/current-format 风险诊断显示 adverse-selection candidate 存在，例如 `5-10-day-control-1h-06=14`、`5-9-small=11`、`5-9-noon=13`、`5-8-stage3-15m-livetest-v4=25`；但 Stage 6J replay summary 中 baseline 和 adverse timing candidates 的 `guard_candidate_adverse_selection_count` 均为 `0`。

验收结论：
- 已通过
- 结论说明：
  - QA 通过。T004 已用 0511T002 replay audit 证实：target_deterioration 不是没触发，而是大量触发但没有覆盖 submit_buy / submit_sell action path；pure adverse timing candidates 与 baseline row-level action path 零差异。T004 也确认 Stage 6J replay 当前对 live adverse-selection source-path 不足够敏感：live/current-format 风险诊断有 positive adverse-selection counts，但 replay summary 中 baseline/adverse timing candidates 的 guard_candidate_adverse_selection_count 为 0。结论支持后续先执行 0512T001 对齐 replay/source-path 观测，再执行 0512T002 设计 add-side submit/re-add toxic timing 合同；不允许 live micro test。

通过项：
1. 业务回报已进入待验收状态
2. 任务声明需要 QA 验收
3. 业务回报包含 verify 证据
4. QA 通过。T004 已用 0511T002 replay audit 证实：target_deterioration 不是没触发，而是大量触发但没有覆盖 submit_buy / submit_sell action path；pure adverse timing candidates 与 baseline row-level action path 零差异。T004 也确认 Stage 6J replay 当前对 live adverse-selection source-path 不足够敏感：live/current-format 风险诊断有 positive adverse-selection counts，但 replay summary 中 baseline/adverse timing candidates 的 guard_candidate_adverse_selection_count 为 0。结论支持后续先执行 0512T001 对齐 replay/source-path 观测，再执行 0512T002 设计 add-side submit/re-add toxic timing 合同；不允许 live micro test。

不通过项：
1. 无

缺陷清单：
1. 无

阻塞项：
- 无

业务回报阻塞项：
- 无执行阻塞。本任务诊断已完成。
- 下一轮策略实现存在前置阻塞：需要补充可观测合同，至少能证明新 trigger 覆盖 add-side submit/re-add path，并且 replay 能观察 live adverse-selection source-path 或明确替代验收指标。

建议总控下一步：
1. 总控可以将该任务视为验收通过。
2. 如存在后续任务，可按前置条件派发下一任务。

提交信息：
- commit：无
