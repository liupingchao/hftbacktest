# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0604T016

状态：
- 未通过

更新时间：
- 2026-06-05 11:16 CST

验收线程：
- QA验收线程

验收对象：
- 业务线程-python / 0604T016

验收范围：
- 验收 live shutdown cancel acknowledgement proof semantics 修复是否满足 T016 的独立 `order_response_received` / `terminal_confirmed` 语义、固定枚举、summary 口径、测试覆盖和禁止越界要求。

验收步骤：
1. 读取 `.workflow/tasks/0604T016.md`，确认本任务是正常 QA，范围限于 `live_tick_mm.py` shutdown helper 结果语义、summary logging、focused tests 和 workflow report。
2. 读取 `.workflow/reports/0604T016-business.md`，核对业务报告、验证命令、commit id 和未做 REST reconciliation 的边界。
3. 检查提交 `b080751` 的文件范围，确认是否触碰 forbidden 文件。
4. grep 检查 `wait_result_raw`、`order_response_received`、`wait_outcome`、`terminal_confirmed`、`terminal_confirmation_source`、`final_order_status` 和 `ack_waits` 使用情况。
5. 复跑 focused pytest：`/home/molly/anaconda3/envs/hftbacktest/bin/python -m pytest examples/binance_tick_mm/test_backtest_tick_mm.py -k "live_shutdown or shutdown_cancel or wait_order_response or terminal_confirmed or order_response_received"`。
6. 复跑 package-level test 文件集合：`/home/molly/anaconda3/envs/hftbacktest/bin/python -m pytest examples/binance_tick_mm/test_*.py`。
7. 运行 `git diff --check`。
8. 复查 terminal proof 语义是否覆盖 active local order 状态，不只覆盖现有测试里的 `NEW` / absent 场景。

实际结果：
- 提交 `b080751` 文件范围为 `.workflow/reports/0604T016-business.md`、`.workflow/tasks/0604T016.md`、`examples/binance_tick_mm/live_tick_mm.py`、`examples/binance_tick_mm/test_backtest_tick_mm.py`，未触碰 py binding、Rust live bot/backtest、connector、production config 或 audit schema。
- 实现新增了 `wait_result_raw`、`order_response_received`、`wait_outcome`、`terminal_confirmed`、`terminal_confirmation_source`、`final_order_status`，summary log 不再输出 `ack_waits`。
- Focused pytest 通过：`13 passed, 170 deselected`。
- Package-level `test_*.py` 通过：`298 passed`。
- `git diff --check` 通过，无输出。
- 但 terminal proof 使用 `collect_working_orders(hbt.orders(asset_no))` 后再检查 order id 是否存在于 active working set。
- `collect_working_orders(...)` 当前只保留 `order.status == NEW` 的订单；`py-hftbacktest/hftbacktest/order.py` 明确 `cancellable` active order 可以是 `NEW` 或 `PARTIALLY_FILLED`。
- 因此如果 wait 后本地订单快照仍存在同一 `order_id` 且状态为 `PARTIALLY_FILLED`，T016 实现会把它过滤掉并返回 `terminal_confirmed=True` / `final_order_status=local_absent_from_working_orders`，误判一个仍 active/cancellable 的订单为 terminal/absent。

验收结论：
- 未通过
- 结论说明：
  - T016 已修复 raw wait-result 分类和 summary 命名，但 independent final terminal-state proof 不完整：active `PARTIALLY_FILLED` 本地订单会被误判为 terminal confirmed，违反 “terminal confirmation must require independent final order-state proof” 的验收线。

通过项：
1. `order_response_received` 与 `terminal_confirmed` 已拆成独立字段，`wait_result == 3` 不直接设置 terminal proof。
2. `wait_result == 0` 已分类为 `ok_unknown_or_timeout`，不再作为 confirmed ack。
3. `wait_outcome` 和 `terminal_confirmation_source` 使用固定枚举；本任务未使用 `rest_open_orders` source。
4. shutdown summary 已移除误导性的 `ack_waits`，分开统计 wait request、order response、terminal confirmation 和 unknown/timeout。
5. 指定 focused 和 package-level pytest 均通过，且未越界修改 forbidden 文件。

不通过项：
1. terminal proof 复用了只收集 `NEW` 订单的 `collect_working_orders(...)`，没有把 `PARTIALLY_FILLED` 这类仍 active/cancellable 的本地订单视为 active proof。

缺陷清单：
1. `examples/binance_tick_mm/live_tick_mm.py`：`_confirm_shutdown_terminal_state_from_local_orders(...)` 通过 `collect_working_orders(hbt.orders(asset_no))` 判断 active set，漏掉 `PARTIALLY_FILLED` active order，可能错误设置 `terminal_confirmed=True`。
2. `examples/binance_tick_mm/test_backtest_tick_mm.py`：focused tests 覆盖了 `NEW` active 和 absent/no-proof 场景，但缺少 `PARTIALLY_FILLED` local order still active 的 regression case。

阻塞项：
- 无；这是可修复代码缺陷，不是环境阻塞。

建议总控下一步：
1. 派发窄修复任务：terminal proof 应直接遍历 local `hbt.orders(asset_no)` snapshot，按订单状态区分 active (`NEW` / `PARTIALLY_FILLED`) 与 terminal (`EXPIRED` / `FILLED` / `CANCELED` / `REJECTED`)，不要复用只收集 `NEW` 的 `collect_working_orders(...)` 作为 final proof。
2. 补 focused regression：`wait_result=3 + local PARTIALLY_FILLED same order_id` 必须得到 `order_response_received=True`、`terminal_confirmed=False`。
3. 保持本任务其它已通过语义不扩大：不接入 REST、不改 binding/Rust/connector、不改 normal loop cancel 或 audit schema。

提交信息：
- commit：`b080751` / `dbdcbaf`
