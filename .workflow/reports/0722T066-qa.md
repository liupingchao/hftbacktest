# QA 验收结果

## Findings（按严重性）

- P0：无。
- P1：无。
- P2：无。
- P3：`source_transfer_manifest.json` 记录了 incremental bundle SHA-256，
  但 checkout 未保存 bundle bytes 或 bundle base-range receipt，因此 QA
  不能从 committed package 单独重算该 bundle hash。该缺口不影响本轮 source
  identity 结论：setup receipt 成功 checkout 精确 source commit，source commit
  blob 与 runner SHA-256 一致，collection/post-state receipts 继续绑定同一
  instance、task 和 source commit。

执行线程：
- QA验收线程

任务ID：
- 0722T066

状态：
- 已通过

更新时间：
- 2026-07-22 18:10 Asia/Shanghai

验收线程：
- QA验收线程

验收对象：
- 业务执行线程 `0722T066`
- source implementation commit
  `903a3e68284942852cf30997c6fd19c960995afc`
- evidence/workflow commit
  `0705aac9991e568ce9d4c718c25db1fc8d51f63d`
- readiness commit
  `50ec3fa3e6f73428fecd0856fb46c31827594d08`

验收范围：
- Source commit blob、runner SHA-256 和 transfer/setup/collection/post-state
  identity chain。
- Remote SHA-256 manifest 与 pulled root files。
- 884 committed public event rows 到 280 counterfactual/non-resting exposure
  rows 的独立重建。
- Directional at-or-through 语义、固定距离网格、双侧 observation/distance
  counts。
- 不调用 production fit helper 的 intensity OLS、RMSE、confidence 和置信区间
  独立复算。
- Seed exact fields、自哈希、expected hash、strict loader 和 hostile tamper。
- Committed offline rebuild、clean checkout deterministic rebuild 和 existing
  estimator replay。
- No-credentials/private/trading-live endpoint、no real behavior change 和
  no-overclaim 边界。

验收步骤：
1. 阅读 T066 task、business report、三个指定提交、runner、tests、receipts
   和 committed evidence。
2. 从 Git object 读取 source runner blob，重算 SHA-256，并核对 transfer
   manifest 与 setup/collection/post-state command、instance、status 和
   source identity。
3. 解析 remote SHA-256 manifest，逐个重算 12 个 pulled root files。
4. 不调用 production exposure/fit helper，直接从 884 event rows 构造 35 个
   完整 5 秒区间、固定四距离双边 exposure，并逐字段比较 280 行结果。
5. 独立实现 log-linear OLS，复算 A、k、RMSE、confidence 和 95% confidence
   bounds。
6. 独立重算 seed canonical self-hash，运行 official loader，并注入 extra
   field、contract hash、expected hash、exposure hash 和 repinned boundary
   tamper。
7. 创建两份 source-commit clean checkout，离线运行正式 CLI，比较 9 个输出
   文件彼此及 committed offline rebuild；另运行 existing estimator replay。
8. 审查 terminal candidate/boundary，运行 focused、full Hyperliquid、
   `py_compile` 和 `git diff --check`。

实际结果：
- Git blob
  `903a3e68284942852cf30997c6fd19c960995afc:examples/hyperliquid/cross_exchange_public_multi_distance_dynamic_seed.py`
  与 working runner 均为
  `da8c30aae4e72001f182ceda3699c4480f8ffd5e2e564431b81ac50e25c72d4f`。
- Transfer manifest 绑定 source commit、instance
  `i-02c64c088f311cbc1` 和三个 accepted command ID。
- Setup、collection、最终 post-state receipts 均为
  `Success / ResponseCode 0`，command/instance identity 精确匹配。
- 第一次 post-state command
  `6a24519c-8ba5-42c6-bb21-f0e1a0645288` 为
  `Failed / ResponseCode 2`，未被 transfer manifest 引用；后续独立 command
  `a6865318-0670-419d-80e6-5f89caabc0c4` 成功并构成最终 post-state proof。
- Remote manifest 的 12/12 SHA-256 与 pulled root files 一致。
- 884 event rows 包含 `334` book、`550` trade；独立 ingest accounting 为
  `881 accepted / 3 dedup / 0 quarantine`。
- 独立 exposure 重建得到 35 个完整区间、固定距离
  `[0.5, 2.5, 5.5, 10.5]`，buy/sell 各 `140` rows、各 `4` distinct
  distances，280/280 行字段匹配。
- Directional arrival 语义精确为：
  buy quote 统计 interval 内 sell-aggressor 且 `trade_px <= quote`；
  sell quote 统计 buy-aggressor 且 `trade_px >= quote`。
- 独立回归精确复现：
  - buy：`A=0.3606537`、`k=0.14777385`、
    `RMSE=0.87664656`、`confidence=0.4161762`。
  - sell：`A=0.58759448`、`k=0.20059328`、
    `RMSE=1.04945934`、`confidence=0.350127`。
  - 两侧 95% A/k confidence bounds 也与 committed fit 完全一致。
- Seed contract exact `17` fields；canonical self-hash 与 expected hash 均为
  `e35c7fd8f3ec8268e5d50c7963889b73409470c9f01f92ca3a0d598a96562be9`。
- Official strict loader 加载 `280` rows，双侧 fit `pass`；extra field、
  contract tamper、expected-hash tamper、exposure tamper 和 repinned
  resting-boundary tamper 均 fail closed。
- Root 与 committed offline rebuild 的 8/8 core artifacts byte-identical。
- 两份 source-commit clean checkout 的 9 个正式 rebuild files 彼此及
  committed offline rebuild byte-identical。
- Existing estimator replay：
  `snapshot_match=true`，source/replay snapshot SHA-256 均为
  `99458fec0b50fccd2a741ce6385852b15cb7969a1150691d8c703a5577e0551d`。
- Source commits只新增 task-scoped runner/tests；watcher、executor 和 online
  estimator implementation 未改变。Runner 中无 credential、private/account、
  order/cancel call site。
- Terminal dynamic candidate 仍为
  `fallback_fixed / missing_latest_market_estimator`，latest toxicity 为空，
  `actual_quote_behavior_changed=false`，activation disabled。
- T066 只证明 intensity seed eligible；没有 live candidate pass、fill、
  economics、promotion、default-on 或 live authorization 结论。
- Focused：
  `30 passed in 0.23s`。
- Full Hyperliquid：
  `1297 passed, 2 skipped in 56.17s`。
- `py_compile`、`git diff --check`：通过。
- QA 未访问 AWS、网络、credentials、private/account/order/cancel/service/live。

验收结论：
- 已通过。
- 结论说明：
  - T066 的 source-pinned public multi-distance intensity seed、严格 loader、
    deterministic rebuild 和 no-live boundary 可由 committed evidence 独立
    复现；terminal dynamic candidate 未通过的事实被准确保留，没有 overclaim。

通过项：
1. Source blob/runner hash 与 receipt identity chain。
2. Remote manifest 12/12 pulled file hashes。
3. 884 event rows 到 280 exposures 的独立 directional rebuild。
4. 双侧独立 intensity regression 与 confidence 复算。
5. Exact seed contract、自哈希、strict loader 和 hostile fail-closed。
6. 8/8 core、9-file clean rebuild 和 estimator snapshot replay。
7. No-credentials/private/trading-live endpoint、no behavior change 和
   no-overclaim boundary。
8. Focused 与完整 Hyperliquid regression。

不通过项：
1. 无。

缺陷清单：
1. 无阻断缺陷。
2. P3 bundle transport hash 独立重算缺口见 Findings；source identity 已由更强
   的 commit/blob/setup/runner chain 覆盖。

阻塞项：
- T066：无。
- 后续真实订单仍必须是新的 formal task，并取得 fresh exact live
  authorization。
- 后续 submit 前必须加载 exact accepted seed，并同时证明当前 dynamic
  candidate 为 `pass`；本任务不能被当作 live candidate 或 economics 验收。

建议总控下一步：
1. 固化 exact seed hash 到新的 explicit wiring/pre-submit gate task。
2. 先用 public/offline 或 no-submit 方式证明 current volatility、liquidity、
   toxicity 完整且 terminal candidate `pass`。
3. 只有上述任务独立 QA 通过后，才考虑 fresh-authorized bounded live evidence。

提交信息：
- QA commit：由本报告提交后的线程回报提供。
