# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0717T004

状态：
- 已通过

更新时间：
- 2026-07-17 14:35 CST

验收线程：
- QA验收线程

验收对象：
- 业务线程-live-safety-audit 0717T004

验收范围：
- 验收本轮是否只读定位 `WTIOIL Open Short 1.14 @ 78.51` 的来源。
- 本 QA 不验收修复、service stop/restart、live retry、quote-policy、T004 public shadow、fee/PnL calibration、maker viability 或 promotion。

验收步骤：
1. 检查 0717 trade history 的 WTIOIL row pattern。
2. 检查 awsserver1 running process/systemd/service evidence。
3. 检查 XEMM config 是否指向 `CLUSDT -> xyz:CL`。
4. 检查 XEMM journal 是否有与目标 WTIOIL row 精确匹配的 hedge execution。
5. 检查是否有 live submit/cancel/service mutation。

实际结果：
- WTIOIL trade history 有 `251` 行，从 April 起已有长期历史，不是 0717T002 单点异常。
- awsserver1 上 `xemm.service` 正在运行，进程为 `/home/admin/XEMM_rust/target/release/xemm_rust`。
- `xemm.service` 配置为 `maker_symbol=CLUSDT`、`hedge_symbol=xyz:CL`、`order_notional_usd=90.0`。
- XEMM journal 在 `2026-07-17T05:41:39Z` 到 `05:41:40Z` 显示：
  - Binance `CLUSDT` maker fill `BUY 1.14 @ 78.47`
  - Hyperliquid hedge `SELL 1.14 xyz:CL`
  - hedge filled `1.14 @ 78.51`
- 该 hedge 与 trade-history `WTIOIL (xyz) Open Short 1.14 @ 78.51` 精确匹配。
- 未执行 live run、submit、cancel、service stop/restart，未输出 credential values。

验收结论：
- 已通过
- 结论说明：
  - WTIOIL 订单来源已闭环：它来自 awsserver1 上并行运行的 `xemm.service` CL/WTI hedge，不是 0717T002 Python runner 的 symbol-mismatch。

通过项：
1. WTIOIL root cause has exact log evidence.
2. XEMM config and journal match the trade-history row.
3. Cross-exchange Python runner symbol mismatch is ruled out for this WTIOIL row.
4. No live mutation was performed.

不通过项：
1. 无。

缺陷清单：
1. Live evidence runs were not isolated from another live trading service on the same host/account.
2. Account-level trade history can contain unrelated service fills, so artifact conclusions need service/account provenance.
3. XEMM logs also show Hyperliquid REST fill lookup did not find the fill and used event data, reinforcing that REST fill source coverage is incomplete for this path.

阻塞项：
- Future live tests remain blocked until either `xemm.service` is explicitly stopped/isolated under authorization or live evidence uses a clean account/subaccount with provenance guard.

建议总控下一步：
1. Do not repair Python symbol routing for WTIOIL; current evidence does not support that bug.
2. Add an isolation/preflight task before future live: detect active non-task trading services and fail-closed.
3. Keep the account provenance guard task, but include concurrent-service isolation as a separate required gate.

提交信息：
- commit：无
