# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0815T001

状态：
- 未通过

更新时间：
- 2026-08-15 14:03 CST（星期六）

验收线程：
- QA验收线程

验收对象：
- 业务线程-python/research + 0815T001
- Stage 2 trigger density / `episode_merging_v1` / named sensitivity /
  effective support 正式包
  `local_live_analysis/skhynix_trigger_aligned_episode_research_v1_stage02_density/`

验收范围：
- 四个 `cross_exchange_trigger_density*` 模块、
  `cross_exchange_candidate_episode_merging.py` 及 focused tests。
- Family A 全 candidate、Family B decision landmark、segment/connection
  epoch 边界、refractory last-retained、60s zero/partial block、recovery
  lookahead、transitive runtime-source closure。
- 正式包 verify-only、archived CLI、双构建一致性、source/accepted Stage 1
  immutability、独立数值复算和临时副本 hostile checks。
- Stage 2 禁止边界：不读取 Aug07 full event rows，不运行
  response/outcome/model/PnL/actionability，不访问 private/order/cancel。

验收步骤：
1. 阅读 workflow 手册、QA 模板、任务文件、业务报告、前序 QA、主实施方案
   和 controller tracking files；不采用业务 summary 作为验收事实源。
2. 审查实现和测试，重点检查 selection、timing、boundary、provenance、
   verification fail-open 与 frozen contract 差距。
3. 运行 focused pytest、Ruff、compileall、`git diff --check`、CLI help、
   正式包 `--verify-only`；再从包内 archived CLI 验证并检查路径不变。
4. 以独立 stdlib 脚本逐行比对原始 frozen trigger audit 与 membership，
   并从 membership/small CSV 复算 counts、rates、quantiles、2000ms union、
   episode conservation、60s catalog、Bartlett ESS 和六类 sensitivity。
5. 独立比较正式包、Build A、Build B 的全部相对路径、bytes、SHA256，并在
   验收前后重哈希 source 与 accepted Stage 1 inventories。
6. 仅在 `/tmp` 临时副本执行 runtime-source drift、landmark 伪造、
   candidate drop/duplicate、cross-segment/epoch、outcome-like path、
   partial publication、extra file 及 coherent rehash hostile checks。

实际结果：
- Focused tests：
  `python -m pytest -q examples/hyperliquid/test_cross_exchange_trigger_density_core.py examples/hyperliquid/test_cross_exchange_candidate_episode_merging.py examples/hyperliquid/test_cross_exchange_trigger_density_inputs.py examples/hyperliquid/test_cross_exchange_trigger_density_admission.py`
  得到 `76 passed in 7.03s`；额外 recovery lookahead、partial publication、
  drop/duplicate targeted selection 得到 `4 passed in 0.09s`。
- Ruff 对四个实现和四个测试返回 `All checks passed!`；compileall 在外置
  `PYTHONPYCACHEPREFIX` 下通过；scope `git diff --check` 通过。
- 正式包 source CLI 和包内 archived CLI 的 `--verify-only` 均返回
  `artifact_count=15`、`candidate_membership_rows=463612`、
  `confirmed_rows=267554`、`session_count=3`、`verified=true`，
  core package SHA256 为
  `ed4cbaf7f6474739f6008d7717a3c7fe228c564d6974006e6478f75828e17598`。
  archived CLI 运行前后文件清单和 SHA 无差异，未生成 `__pycache__`、
  `.pyc` 或新增路径。
- 正式包、`/tmp/0815T001-density-a.PlkRiF/package`、
  `/tmp/0815T001-density-b.Q6QxdJ/package` 各有 `16` 个文件、
  `19,801,138` bytes；全部 `(relative_path, bytes, SHA256)` mismatch 为
  `0`。QA 对完整 tuple list 的独立 canonical SHA256 均为
  `59a6980ea2a3d33da5be353f207ec1c068882cb11cbd242dbd35129ec1e92c67`。
- Source inventory 验收前后均为 `123` files、`169,677,903` bytes，
  独立 tuple-list SHA256
  `94bad85bfd4981b351f84c53628099468ec27f13d308402ac7125a9d582a6644`；
  accepted Stage 1 均为 `14` files、`1,401,387` bytes，独立 SHA256
  `c540cc056313716b3bdd2b9c0fe076cda15a7f152b399ae6a1283b3aa8aa6590`。
  两者 before/after 缺失、bytes、SHA 错误均为 `0`；Stage 1 core SHA256
  为 `9c2756f966a1a05816f9e6a91d7361d76cfea10fc3c587944ca2f0c1d2eb7c96`。
- 独立逐行复算确认三 session 原始 audit 与 membership 的 candidate /
  confirmed counts 分别为：
  `jul30=268522/141768`、`aug03=127622/82533`、
  `aug04=67468/43253`；session、candidate id、shock/decision timestamp、
  primary/rejection/impact 字段 mismatch 均为 `0`。
- 独立 rates（Family A/B，每秒）：
  `jul30=18.646808695928/9.844708348680`、
  `aug03=7.089796343633/4.584963106902`、
  `aug04=9.370385808591/6.007252288181`。
- 独立 inter-trigger `p01/p50/p99` spot checks（ms）：
  `jul30 A=0.600193890/29.139860000/344.679074520`，
  `B=0/79.394451000/450.212933290`；
  `aug03 A=1.014078310/59.912762500/1137.006387680`，
  `B=24.545819480/117.593711000/1411.189326020`；
  `aug04 A=1.218875240/58.078078000/667.059626960`，
  `B=25.075885850/108.719261000/824.999149020`。
- 2000ms union coverage fraction（A/B）：
  `jul30=0.999823231481/0.999704108077`，
  `aug03=0.992360372650/0.989371035050`，
  `aug04=0.999855679985/0.999555619709`；对应 block counts
  `9/9`、`232/279`、`6/12`。
- Episode conservation 的 `cluster/continuous-flow/all-candidate-overlap`
  counts 为：`jul30=39928/10536/9`、
  `aug03=47193/23397/232`、`aug04=23113/9451/6`；未发现
  cross-group/window/pair conservation 错误。
- 60s catalog `all/complete/partial/occupied-A/occupied-B`：
  `jul30=248/240/8/241/240`、
  `aug03=310/300/10/300/300`、
  `aug04=121/120/1/120/120`。
- Segment-summed Bartlett ESS（A/B）：
  `jul30=1096.308299192916/837.102916741897`，
  `aug03=2195.991765333518/2085.852133598072`，
  `aug04=275.582545311320/263.249320034331`。
- 六类 sensitivity selected counts
  （impact50/impact70/refr100/refr250/refr500/first-flow）：
  `jul30 A=254572/243124/119415/71481/43970/10536`，
  `B=139864/138316/94677/57720/35613/9579`；
  `aug03 A=119765/113472/75993/53737/38032/23397`，
  `B=80687/79162/65285/46505/32977/21456`；
  `aug04 A=63274/60198/39657/27041/18311/9451`，
  `B=42347/41720/33706/23135/15639/8621`。
- Runtime-source drift、单行 Family B landmark 伪造、单行 candidate
  drop/duplicate、cross-segment/epoch、outcome-like Aug07 path、额外文件
  和 partial publication hostile checks 均按预期 fail closed。
- 静态调用闭包确认 archived local runtime-source 恰为 `5` 个模块；本轮
  未读取 Aug07 full event rows，未执行 response/outcome/model/PnL/
  actionability，也未访问 private/order/cancel。
- 尽管数值与普通 hostile checks 通过，独立 coherent-rehash hostile
  checks 确认三个 P1 verifier fail-open；另有 evidence label P2 合同偏差。

验收结论：
- 未通过
- 结论说明：
  - 正式产物的当前数值、确定性、普通完整性检查和 Stage 2 禁止边界均有
    充分正向证据，但 verifier 未把完整 frozen population、完整 provenance
    inventory 和 recovery 决策分类锚定到独立可信事实。三个 P1 均可在
    临时副本同步篡改并重哈希后通过 `--verify-only`，因此不能接受为正式
    Stage 2 门禁。

通过项：
1. 当前 formal package 的三 session counts、rates、timing quantiles、
   2000ms coverage、episode conservation、60s catalog、ESS 与 sensitivity
   数值经独立复算一致。
2. Family A 使用全部 candidates，Family B 当前使用 decision landmark；
   refractory last-retained、segment/epoch isolation、zero/partial block 和
   recovery lookahead 的现有 targeted tests 通过。
3. 正式包与双构建副本全部路径、bytes、SHA 一致；archived CLI 可只读验证，
   不产生缓存或额外文件。
4. 当前 source 与 accepted Stage 1 inventories 在验收前后保持不变。
5. Stage 2 未越界读取 Aug07 full rows，未运行后续 outcome/model/PnL/
   actionability，也未访问 private/order/cancel。

不通过项：
1. P1：verify-only 未锚定精确三 session frozen population，可整体删除
   `aug04` 后同步改写产物与 manifest 并通过。
2. P1：input binding verifier 接受自声明的 inventory 子集，可删除 source
   与 accepted Stage 1 provenance 行后同步重哈希并通过。
3. P1：recovery status / decision reason 只校验总数守恒，可伪造任意分类名
   后同步重哈希并通过。
4. P2：Aug03 evidence label 为未冻结的
   `historical_transfer_diagnostic`，不是主方案要求的
   `historical_transfer`；Jul30 同样使用了未列入允许枚举的组合 label。

缺陷清单：
1. P0：无。
2. P1：完整 frozen population/session contract 验证 fail-open。
   - 文件/行号：
     `examples/hyperliquid/cross_exchange_trigger_density_admission.py:44-48`
     定义了 canonical counts，但 verify 路径
     `:2430-2560` 仅把 observed membership 与同包 small CSV/manifest/
     exact_counts 互相比对，未与 `FROZEN_SESSION_COUNTS` 比对。
   - 复现：在 `/tmp/0815T001-hostile-drop-session-mfjl6x27/package`
     同时删除两个 membership gzip 和全部 small CSV 中的 `aug04`，从
     manifest 删除该 session 并把 exact counts 改为
     `396144 candidates / 224301 confirmed / 2 sessions`，随后重算 artifact
     与 core hashes。source CLI `--verify-only` 返回 `rc=0`、
     `verified=true`，core SHA256
     `57fdf7f1b08fe9fb9debed6c7096e518aa2cd3c3e168041439353a421ea54a4d`。
   - 影响：一个完整 frozen session 及其全部 candidates 可被静默移除，
     破坏 Family A complete population 和精确三 session 合同。
3. P1：source / accepted Stage 1 inventory closure 验证 fail-open。
   - 文件/行号：
     `examples/hyperliquid/cross_exchange_trigger_density_admission.py:2564-2627`
     仅从 published CSV 行重建 before/after inventory 并验证自哈希，没有
     锚定预期 role/path/cardinality，也没有按可信 inventory 重建完整集合。
   - 复现：在
     `/tmp/0815T001-hostile-binding-omission-so0fptzy/package` 删除 Jul30
     `motif_episode_manifest.json` 和 Stage 1
     `data_admission/data_admission.md` 的 before/after binding 行，重算
     inventory/artifact/core hashes。source CLI 返回 `rc=0`、
     `verified=true`，core SHA256
     `10d7b500ed8ce428095812196cff2478ea72baaeb9a0284fc7d26909166f068c`。
   - 影响：transitive provenance closure 与 accepted Stage 1 full
     inventory 可被缩减，验证仍错误放行。
4. P1：recovery lookahead / merge-decision audit 分类验证 fail-open。
   - 文件/行号：
     `examples/hyperliquid/cross_exchange_trigger_density_admission.py:2111-2146`
     从 published row 读取两个 JSON，仅校验 value sum 等于 boundary
     count，随后把同一 published JSON 当成 expected value。
   - 复现：在
     `/tmp/0815T001-hostile-forged-recovery-y_n_6bso/package` 将每个 session
     的 recovery JSON 改为
     `{"forged_recovery_status": boundary_count}`，decision JSON 同理，
     再重算 hashes。source CLI 返回 `rc=0`、`verified=true`，core SHA256
     `3d258286165377a5823021d88f89ea51fa02bed996f066ee7c854c43c70c7a24`。
   - 影响：recovery lookahead、missing-evidence 和 flow-merging 的审计语义
     未被独立重算，任意伪造分类可进入正式包。
5. P2：evidence label 与 frozen taxonomy 不一致。
   - 文件/行号：
     `examples/hyperliquid/cross_exchange_trigger_density_admission.py:49-63`；
     主实施方案
     `docs/skhynix_trigger_aligned_episode_research_implementation_plan.md:281-300`
     限定允许枚举并明确 Aug03 为 `historical_transfer`。
   - 复现：检查正式
     `trigger_density_by_session.csv:2-5`，Aug03 为
     `historical_transfer_diagnostic`；Jul30 为
     `historical_discovery_and_internal_validation`。两者均不在冻结枚举中。
   - 影响：下游无法把 `evidence_label` 当作 frozen enum 稳定消费；诊断/
     eligibility 差异应由 caveat 或独立布尔字段表达。
6. P3：无。

阻塞项：
- 无。缺陷均可在 0815T001 范围内 bounded repair。

建议总控下一步：
1. 保持 `0815T001` 为 `未通过`，不得把 Stage 2 正式包作为后续
   response/outcome/model 阶段的可信门禁。
2. verify-only 必须把 observed session set/counts 与 canonical frozen
   contract 精确比较，并为 source 与 Stage 1 inventories 验证完整预期
   path/role/cardinality 和实际文件重哈希，而非只接受 published 自描述。
3. 从 membership/boundary facts 独立重算 recovery status 与 decision
   reason，或发布可重放的逐 boundary audit；严格校验允许分类集合。
4. 将 Aug03 改为 frozen `historical_transfer`，Jul30 用冻结枚举按
   segment 表达 discovery/internal-validation，诊断与 formal eligibility
   保留在 caveat/boolean；补充上述三种 coherent-rehash regression 后再
   进入独立 QA。

提交信息：
- commit：无
