# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0801T007

状态：
- 已通过

更新时间：
- 2026-08-01 20:51 UTC

验收线程：
- QA验收线程

验收对象：
- 业务线程-python/cross-exchange-research / 0801T007 R2

验收范围：
- 第三轮独立复核第二轮唯一剩余 P1：consumption actual-row-count
  closure。
- R2 consumption intent 的 exact schema、1+5+5 cardinality、
  row-count/SHA、首次 outcome 读取顺序与永久消费纪律。
- 协调 M1 row-count metadata drift 和 existing-consumption
  unknown/missing/cardinality/row-count hostile cases。
- R2 schema、正式 artifacts closure、双 discovery/双 post-selection
  确定性。
- 回归首轮已闭合的完整 interval、neighbor summary、标签、
  adverse/PnL 隔离和无正式 held-out 支持边界。

验收步骤：
1. 阅读任务、R2 业务报告、第二轮 QA、计划、实现、测试和正式 R2
   artifacts，并确认 R1 失败包独立归档。
2. 独立扫描正式 consumption manifest 的 11 项输入，复算十份 CSV
   row-count 和十一份输入 SHA。
3. 在隔离目录协调修改 `segment_0004` M1 row-count、M1 manifest SHA、
   contract SHA 和 baseline manifest binding，执行完整 post-selection
   入口并插桩首次 CSV/outcome 打开。
4. 对 existing consumption 执行 row-count drift、unknown key、missing
   row-count 和 cardinality hostile cases。
5. 独立重建 discovery 全部 82,410 条 matched-neighbor 预测与完整
   evidence/label interval，并重算 neighbor summary。
6. 以真实数据执行两次完整 discovery build 和两次 post-selection
   build，比较全目录 SHA、run ID 和 discovery immutability。
7. 运行 focused pytest、联合回归、ruff、py_compile、CLI help、
   internal validators 和 `git diff --check`。

实际结果：
- Findings（按严重度）：
  - P0：无。
  - P1：无。
  - P2：无。
  - P3：无。
- 第二轮唯一剩余 P1 已闭合：
  - 正式 `heldout_consumption_manifest.json` 包含恰好 `11` 个
    `input_sha256` 条目。
  - 11/11 条目 exact key set 均为
    `segment_id/role/path/row_count/sha256`。
  - role/cardinality 恰为一个 M1 manifest、五份 M1 episode、五份
    timeline，segment-role set 与 `segment_0004-0008` 完全一致。
  - M1 manifest 的声明/实际 row-count 均为 `1`。
  - 十份 CSV 声明总行数与独立实际扫描总行数均为 `426,625`。
  - 11 项 row-count 全部匹配，11 项 SHA 全部匹配。
  - consumption run ID
    `1b0e19dd-7526-4aaa-9127-3d2a69d77ffa` 与 post manifest binding
    一致。
- 协调 M1 row-count hostile 通过：
  - 将 `segment_0004` M1 声明行数从实际 `20,243` 改为 `20,244`，
    并协调重算 M1 manifest SHA、contract SHA 和 baseline manifest
    binding。
  - 完整 `build_post_selection_evaluation` 以
    `consumption input row count drift` 拒绝。
  - consumption intent 已持久化，且其 `segment_0004` row-count 冻结为
    篡改后的 `20,244`，保留了实际消费尝试事实。
  - 首次 post-selection CSV 扫描发生在 intent 已存在之后；
    `SegmentFileGuard` outcome 打开次数为 `0`。
  - `post_selection_manifest.json` 未发布。
  - 失败后 discovery freeze 以
    `post-selection data was consumed; discovery freeze cannot overwrite it`
    永久拒绝；intent SHA 前后不变。
- Existing-consumption hostile 通过：
  - row-count `+1` -> `consumption input row count drift`。
  - unknown top-level key -> `consumption manifest schema drift`。
  - missing item `row_count` -> `consumption input schema drift`。
  - 11 项缩减为 10 项 -> `consumption input cardinality drift`。
  - 以上均在协调更新 post manifest 中 consumption output SHA 后由完整
    入口 fail closed。
- R2 schema 和正式 output closure 通过：
  - baseline manifest、frozen contract、model bundle 和 post manifest
    均为 `hyperliquid_liquidity_response_baseline_v2_r2`。
  - Discovery/post-selection 十个正式 outputs 的实际 row-count 和 SHA
    全部与 manifest 声明一致。
  - 业务报告列出的 baseline/contract/model/consumption/post manifest
    SHA 全部独立匹配。
  - R1 失败包仍位于
    `/tmp/0801T007-baseline-v2-r1-failed-qa`，关键 SHA 与第二轮失败包
    完全一致，schema 仍为 R1。
- 真实数据重复构建确定性通过：
  - 第一次 discovery build 耗时约 `396.7s`，产出 8 个 R2 文件。
  - 第二次 discovery build 后 8 个文件 byte-identical；六个内容
    artifacts 与正式 R2 SHA 全部相同。
  - 第一次 post-selection build 复用真实数据并发布 13 个文件。
  - 第二次 post-selection build 复用同一 run ID，13 个文件
    byte-identical，八个 discovery artifacts 全部不变。
- 首轮 interval 缺陷回归通过：
  - 独立重建 25 targets、三个 discovery folds 的全部 `82,410` 条
    matched-neighbor query。
  - query/candidate 双侧均使用
    `[episode_start, max(episode_end, outcome_known_from)]`，再施加
    `60s` embargo。
  - false-eligible 数 `0`、缺失 prediction 数 `0`、neighbor count 及
    q25/q50/q75 artifact mismatch 数 `0`。
  - 实际 neighbor count 为 `min=186/max=200`。
- 首轮 neighbor summary 缺陷回归通过：
  - 独立摘要与 `baseline_manifest.json` 逐字段完全相等。
  - query `82,410`、available `82,410`、unavailable `0`。
  - distribution 为
    `min=186/p05=200/p25=200/p50=200/p75=200/p95=200/max=200`。
  - 25 个 target 的分组摘要均无差异。
- Discovery identity 和模型回归通过：
  - discovery 只包含 `segment_0001-0003` 的 3+3 输入，
    post-selection 文件打开数为 `0`。
  - 独立重建 atom count `60,820`，canonical SHA
    `bce395180f2773011d323a7647efa7513709cdf4190aa89c53572c2f4d220b2d`，
    与 T006 calibration contract 一致。
  - Official model selection 只使用三个 discovery folds；HGB 在三个
    folds 均胜出，contract selection 逐值一致。
  - 三类真实 targets、九个 quantile models 的 portable/sklearn
    prediction `array_equal`，最大误差 `0.0`，export 与模型包一致。
- 标签和研究边界回归通过：
  - 25 targets 的 feature allowlist 共 `772` 项，
    `adverse/pnl/profit/fee/fill/markout` 命中数为 `0`。
  - Discovery feature 标签为 `discovery`，CV prediction 标签为
    `discovery_cv`。
  - `0004-0008` feature/evaluation/prediction 标签全部为
    `post_selection`。
  - 正式 held-out 授权为 `false`；13 个正式 artifacts 中 exact
    `held_out` token 数为 `0`。
  - 未发现 fresh segment/date，也未发现正式 held-out motif support、
    tradable signal、maker identity、fill 或 PnL 声明。
- 验证命令：
  - baseline v2 focused tests：`10 passed in 150.94s`。
  - case hierarchy + baseline 联合回归：`32 passed in 150.68s`。
  - ruff、py_compile、CLI help、internal discovery/post validators 和
    `git diff --check`：通过。

验收结论：
- 已通过
- 结论说明：
  - R2 已关闭第二轮唯一剩余的 consumption actual-row-count P1；首轮
    三项缺陷及所有标签、数据隔离和研究边界回归均通过，未发现新的
    P0-P3。

通过项：
1. Consumption 1+5+5 exact schema、actual row-count/SHA closure。
2. 协调 M1 row-count hostile 在发布 post manifest 前 fail closed。
3. Consumption intent 首读顺序和永久消费纪律。
4. Existing-consumption row-count/unknown/missing/cardinality fail closed。
5. R2 正式 output closure、R1 失败包隔离和双 discovery/双 post
   byte determinism。
6. 完整 interval false-eligible=0 和 neighbor summary 精确复算。
7. Discovery/T006 identity、discovery-only model selection 和 portable
   HGB parity。
8. Post-selection-only 标签、零 adverse/PnL/markout features、零 exact
   `held_out` token和无正式 held-out motif 支持。

不通过项：
1. 无。

缺陷清单：
1. P0：无。
2. P1：无。
3. P2：无。
4. P3：无。

阻塞项：
- 无。

建议总控下一步：
1. 将 `0801T007` 更新为 `已通过`。
2. 可按既定顺序解锁后续 `0801T008`，同时继续保持无 fresh segment/date
   时不得声明正式 held-out motif 支持的边界。

提交信息：
- commit：无
