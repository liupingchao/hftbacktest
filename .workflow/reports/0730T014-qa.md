# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0730T014

状态：
- 已通过

更新时间：
- 2026-07-30 23:59 CST

验收线程：
- QA验收线程

验收对象：
- 业务线程-python/cross-exchange-research + 0730T014

验收范围：
- T013 三个 P2 和 T014 首轮两个 P2 counterexample、真实八段 R0
  产物、source provenance 及新增采集授权边界。

验收步骤：
1. 审查 timeline CSV row identity 和 main-allMids dex 修复。
2. 独立重放五组 P2 counterexample。
3. 运行 focused 和组合 regression。
4. 独立复算 24 个 output gzip、56 个 source、8 个 timeline 和 48 个 raw。
5. 核对本地-only 和 R6 新增采集授权边界。

实际结果：
- P0: none
- P1: none
- P2: none
- P3: none
- T014 首轮两个 counterexample 已关闭：
  - timeline CSV 的 campaign/segment/profile 三种行内身份篡改均 fail closed
  - main_all_mids named-dex payload fail closed
- T013 原三个 P2 未回归：
  - failed `--clean-output` rebuild 被拒绝，全部旧文件逐 SHA 保留，tmp 清理
  - bad index campaign/continuity/first/last 均 fail closed
  - bad Binance depth symbol、standard-L2 coin、extra channel 和 timeline
    source SHA 均 fail closed
- Tests:
  - focused `18 passed`
  - dataset/timeline/supervisor `59 passed`
  - py_compile、CLI help、git diff check pass
- 真实产物复算：
  - `24/24` output gzip SHA/row/time/event-seq/identity
  - `56/56` source SHA
  - `8/8` timeline gzip row/identity/time/boundary
  - `48/48` raw gzip channel/control/raw-row/time/coin/dex
  - Binance hot `10,744,733`
  - Hyperliquid hot `541,122`
  - Hyperliquid auxiliary `49,169`
  - raw/output exploded trades `398,224 / 398,224`
  - common timeline `556,861`
  - segment gaps `7/7` exact
- Manifest 为 `task_id=0730T014`、`source_hash_count=56`、`passes=true`。
- 无 temporary/backup publication residue。
- Builder 没有 network/AWS/SSH 或新增采集路径；R6 继续要求独立任务、
  用户明确授权和用户确认的活跃交易时段。

验收结论：
- 已通过
- 结论说明：
  - 五组 P2 counterexample 均 fail closed，真实重建产物和 provenance
    完整通过，可由总控解锁 R1 alignment acceptance。

通过项：
1. Atomic clean replacement 和 rollback contract。
2. Timeline index/manifest/CSV identity、boundary 和 gap closure。
3. Binance/Hyperliquid raw identity、count、control 和 source-SHA closure。
4. 真实八段 R0 package 完整性。
5. 本地-only 和新增采集授权边界。

不通过项：
1. 无

缺陷清单：
1. 无

阻塞项：
- 无

建议总控下一步：
1. 将 T014 标记为`已通过`。
2. 按 T012 顺序创建并执行 R1 alignment acceptance 正式任务。

提交信息：
- commit：无
