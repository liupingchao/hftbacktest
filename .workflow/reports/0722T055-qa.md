# QA 验收结果

## Findings（按严重性）

- P0：无。
- P1：无。
- P2：无。
- 残余边界：本轮只证明 offline code/action-path readiness；不提供
  multi-level live、durable restart provenance、经济性或 promotion 证据。

执行线程：
- QA验收线程

任务ID：
- 0722T055

状态：
- 已通过

更新时间：
- 2026-07-22 11:34 Asia/Shanghai

验收线程：
- QA验收线程

验收对象：
- 业务执行线程 `0722T055`
- implementation commit
  `bf1ca7a5776ef9c0213277006ee024bb9880ef4e`
- workflow/business commit
  `83caf87ccaa1f75480bd5b073668258193187d40`

验收范围：
- 严格 offline-only 独立 code review、静态调用点审查和 fake/local
  counterexample。
- 覆盖 `_submit()` callback transaction boundary、generation consistency、
  watcher startup/disconnect/candidate/cycle/terminal status identity、
  default-off 和 actual quote-result 语义。
- 核对 business report exact implementation commit、offline-only 边界和
  unsupported claims。
- 不修改实现代码、task 文件、task 状态或 controller tracking 文件。

验收步骤：
1. 阅读 workflow-kit、task、business report、T054 failed QA、
   `task_plan.md`、`progress.md` 和 `findings.md`，核对固定提交完整 SHA。
2. 审查 implementation diff、`MakerOrderManager._submit()` mutation 顺序，
   并以 AST 检查全部 ladder-aware `task7_status_payload()` 调用点。
3. 运行独立 hostile counterexamples：
   - 第二次 halt callback 抛错后的 proposed addition 零 manager mutation。
   - callback 返回前 generation 被外部改写时在 endpoint 前 fail-closed。
   - active 两层 early/no-cycle 四类 status 的 exact config identity。
   - 显式单层 actual manager cycle 两类 status 的 quote-result identity。
   - default-off `None` 和 disabled two-level config 语义。
4. 运行正式 hostile、focused、related、full Hyperliquid、py_compile、
   implementation diff 和 workflow diff 检查。

实际结果：
- `_submit()` 在 callback 前只读取 generation candidate 并完成纯验证；
  callback 抛错时，被阻止 logical key 不进入 `generation_by_key` 或
  `orders_by_key`，`submissions_used` 和 fake endpoint call count 不增加。
- callback 内模拟 generation 改写后，返回路径触发
  `generation_changed_during_before_submit`；order endpoint 为 `0`，
  `orders_by_key` 为空且 `submissions_used=0`。
- AST 独立检查找到 `6` 个携带 `quote_ladder_config` 的
  `task7_config_hash` status call site；`6/6` 均显式提供 matching
  `multi_level_snapshot`。两条 cycle status 使用实际 quote-result，
  startup、disconnect、candidate waiting 和无 cycle terminal 使用同一
  config-derived helper。
- 独立 active `levels=2` watcher 场景通过 `4` 类 lifecycle status；
  每条均携带同一 expected config hash、`requested_levels=2`、activation、
  prerequisite 和 exact config，且 order endpoint 为 `0`。
- 独立显式单层 manager cycle 产生 `2` 条 cycle status、`2` 次 fake
  submit 和 `2` 次 fake cancel；两条 status 均精确使用同一 actual
  quote-result multi-level snapshot。
- 独立 default-off 检查通过：无 config 保持 authoritative single-level；
  disabled two-level config 保持 `requested_levels=2`、实际 `levels=1`、
  activation false、无 executable ladder intents。
- 正式 hostile：
  `2 passed in 0.12s`（wall `0.29s`）。
- Focused manager/quote-ladder/event watcher：
  `259 passed in 20.89s`（wall `21.02s`）。
- Related public watcher/shared kernel/executor：
  `93 passed in 0.27s`（wall `0.53s`）。
- Full `python -m pytest examples/hyperliquid -q`：
  `1250 passed in 51.92s`（wall `52.23s`）。
- py_compile：manager、watcher 和两个 focused test 文件通过
  （wall `0.09s`）。
- implementation `git diff --check` 通过（wall `0.01s`）；workflow commit
  未修改四个实现/测试文件。
- 全部执行使用 synthetic public events、临时目录和 fake/local client；
  未执行 live、private/account、真实 order/cancel、network、remote、
  service 或 cap expansion。

验收结论：
- 已通过。
- 结论说明：
  - T054 的两个 P1 均已在精确 transaction/evidence identity 边界修复，
    独立 hostile、focused 和 full regression 未发现 P0/P1/P2。

通过项：
1. Final `before_submit` callback failure 对 proposed addition 保持
   generation/order/submission/endpoint 零 mutation。
2. Callback 返回后的 generation consistency 在 endpoint 前 fail-closed。
3. 全部 ladder-aware watcher lifecycle status 的 config hash 和
   multi-level snapshot 由同一 exact ladder config 或其 actual
   quote-result 派生。
4. Default-off、actual cycle snapshot、standing submission cap、
   restart hold/cancel-only 和 adaptive isolation 相关回归保持通过。
5. Business report 的 exact implementation SHA、offline-only 说明和
   unsupported claims 准确。

不通过项：
1. 无。

缺陷清单：
1. 无。

阻塞项：
- 无。

Unsupported claims：
- 本轮不支持 multi-level live、private/account、真实 order/cancel、
  network/remote/service、stable PnL、fill rate、fee/rebate、queue
  priority、maker viability、promotion 或 final MVP 结论。
- Restart 仍只支持 recovered owned-order 的 hold/cancel-only
  fail-closed 路径；不支持 durable historical generation/submission
  provenance 或完整 restart readiness。

建议总控下一步：
1. 可将 Principal Task 10 关闭为 code/action-path readiness。
2. 继续锁定 multi-level live；任何 live activation 或 standing envelope
   变更必须进入新的正式任务和独立证据 gate。

提交信息：
- QA commit：无。
- Implementation commit：
  `bf1ca7a5776ef9c0213277006ee024bb9880ef4e`
- Workflow/business commit：
  `83caf87ccaa1f75480bd5b073668258193187d40`
