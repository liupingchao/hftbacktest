# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0519T001

状态：
- 已通过

更新时间：
- 2026-05-19 09:28 CST

验收线程：
- QA验收线程

验收对象：
- 测试线程 + 0519T001

验收范围：
- 验收 Step 6 final lifecycle calibration rerun 是否基于 `5-13-day-control-30min` 完成修复后的 replay/live lifecycle 对齐复核，并确认本任务未扩大为 replay repair、queue repair、策略修改、live 采集或 default-on 行为。

验收步骤：
1. 读取 `.workflow/tasks/0519T001.md`、`.workflow/reports/0519T001-business.md` 和 Stage 6 final calibration 产物。
2. 运行 `python -m pytest examples/binance_tick_mm/test_execution_outcome_calibration.py examples/binance_tick_mm/test_replay_lifecycle_mismatch_diagnosis.py`。
3. 运行 `python examples/binance_tick_mm/execution_outcome_calibration.py --help` 和 `python examples/binance_tick_mm/replay_lifecycle_mismatch_diagnosis.py --help`。
4. 复跑 `execution_outcome_calibration.py --run-dir local_live_analysis/5-13-day-control-30min --output-dir local_live_analysis/5-13-day-control-30min/stage6_final_calibration_0519T001`。
5. 复跑 `replay_lifecycle_mismatch_diagnosis.py --run-dir local_live_analysis/5-13-day-control-30min --output-dir local_live_analysis/5-13-day-control-30min/stage6_final_calibration_0519T001/residual_diagnosis --residual-only`。

实际结果：
- focused tests 通过，`10 passed`。
- 两个 runner 的 `--help` 命令输出正常。
- final calibration 复跑返回 `decision_state: requires_more_current_format_samples`。
- submit 对齐完整：live submit `2516`、replay submit `2516`、matched submit `2516`，matched price tick equality `2516/2516`，matched qty equality `2516/2516`。
- aggregate lifecycle 已明显收敛：live/replay filled `53/54`，live/replay fill-after-cancel `16/15`。
- 对齐项：
  - fill horizon gap：`100ms 0.001192`、`500ms 0.000397`、`1000ms 0.000397`、`5000ms 0.000397`
  - final state gap：canceled `0.000397`、filled `0.000397`、expired `0.0`、open_or_missing `0.0`
  - cancel race gap：fill-after-cancel-request rate `0.000397`、fast-cancel-churn rate `0.0`
  - markout coverage gap 接近：`500ms 0.00159`、`5000ms 0.00119`
- 未完全对齐项：
  - time-to-fill magnitude 仍有差异：matched-any-filled mean gap about `400.07ms`，matched-both-filled mean gap about `364.54ms`
  - cancel-to-fill delay magnitude 仍有差异：both-observed mean gap about `96.88ms`
  - sparse strata 仍有 timing magnitude gap，例如 latency `q2`、same-side top1 qty `q4`、placement `touch`
  - residual diagnosis 仍有 1 条：`28940|sell` / order `4948`，`live_canceled_replay_filled`，class `residual_replay_fill_trigger_uncertain`
- queue/priority、opportunity cost、realized PnL decomposition 仍是 observed-only proxy，不是 exact queue proof。
- 未发现本任务实施 repair、策略行为修改、live/AWS 操作、schema 修改或 default-on 行为。

验收结论：
- 已通过
- 结论说明：
  - `0519T001` 满足 Step 6 final lifecycle calibration rerun 的验收要求：原 Stage 6B 的 `diagnostic_only_gap_too_large` 已改善为 `requires_more_current_format_samples`，可进入 `0519T002` 做 Step 6 closure decision，但不能据此直接授权 Step 9 promotion 或 live readiness。

通过项：
1. 复跑产物完整，关键 live/replay lifecycle 指标已报告。
2. matched submit coverage、price tick、qty 对齐完整。
3. fill/final-state/cancel-race 事件分类层面已收敛到小 gap。
4. 剩余 timing magnitude gap、`4948` residual 和 current-format sample 不足均已明确记录。
5. 任务边界保持只读验证，没有 repair、策略变化、live 或 default-on。

不通过项：
1. 无

缺陷清单：
1. 无

阻塞项：
- 无

建议总控下一步：
1. 启动 `0519T002`，以 planning-only 方式正式判断 Step 6 是否关闭，以及 Step 7 / Step 8 / Step 9 的进入边界。
2. 不要把 `0519T001` 单样本结果解释为 quote-adjustment promotion 或 live readiness。
3. 将 `4948` / queue-ahead proxy residual 继续作为 design-only residual，等待后续更多 current-format samples 或明确新任务再处理。

提交信息：
- commit：16599f4
