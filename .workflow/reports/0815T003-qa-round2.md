# QA 验收结果

执行线程：
- QA验收线程（`0815T003` Stage 4 第二轮全新独立 QA）

任务ID：
- `0815T003`

状态：
- 未通过

更新时间：
- 2026-08-15 23:59 CST（星期六）

验收线程：
- QA验收线程

验收对象：
- 业务线程-python/research + `0815T003`
- 工作目录：
  `/Users/liu/Documents/hftbacktest-0814t001-skhynix-episode-research`
- 分支：
  `codex/0814t001-skhynix-episode-research`
- 正式 package：
  `local_live_analysis/skhynix_trigger_aligned_episode_research_v1_stage04_jul30_episode_v3/`
- 全新随机 QA 根：
  `/tmp/0815T003-qa-round2.na5Cuh`

验收范围：
- 仅 Jul30 immutable public-data inputs。
- 未解析禁止的 Jul30 legacy
  `/Users/liu/Documents/hftbacktest/local_live_analysis/skhynix_liquidity_response_0730T017/episodes/*.csv.gz`。
- 未读取 Aug03/Aug04 future event rows 或任何 Aug07 event rows。
- 未运行 case/model/score/actionability/orders/network/live config。
- accepted Stage 1/2/3、Jul30 raw/R0/R1、正式 package 和输入只读。
- 未复用 `/tmp/0815T003-r1-*`、第一轮 QA `/tmp/0815T003-qa*`
  或业务 attack/rebuild 结果。
- 本轮仓库写入仅限本报告及
  `docs/qa-acceptance-report.md` 的 byte-identical mirror。

## Findings First

严重度汇总：
- P0：`0`
- P1：`1`
- P2：`0`
- P3：`0`

### P1-1 Source-semantic admission 未覆盖完整 decision feature ledger

判定：
- 未通过。
- 当前正式 package 的已发布数据在本轮独立重放范围内没有发现值错误，
  但 admission 的 `source_semantic_verified=true` 不是完整 package
  source semantics 的保证。
- 一个 load-bearing Family A Candidate-time decision feature 可被修改，
  在同步重算 artifact records、core SHA、canonical manifest 后，
  source verifier 和 clone 内 archived verifier 均返回 `rc=0`、
  `verified=true`、`source_semantic_verified=true`。
- 这直接触发本轮硬规则：任何 load-bearing coherent mutation 被 source
  或 archive 接受，最终状态必须为 `未通过`。

代码与 contract 证据：
- `_verify_source_semantics()` 为 anchors、sparse、fixed-grid、
  event-count、outcomes 和仅限 14 个 confirmation features 创建 exact
  stream：
  `examples/hyperliquid/cross_exchange_trigger_aligned_episodes.py:5276-5306`。
- observed feature projection 只筛选
  `CONFIRMATION_FEATURE_NAMES` 的 Family B rows：
  `examples/hyperliquid/cross_exchange_trigger_aligned_episodes.py:5431-5438`。
- 其余 Family A/B feature rows 仅接受 schema、feature universe、
  coverage、availability 和 `observed_at <= landmark` 等结构检查，
  没有从 accepted source 重建 exact
  `value/observed_at/source_event_id/source_book_version`：
  `examples/hyperliquid/cross_exchange_trigger_aligned_episodes.py:5172-5245`。
- Family A/B view payload 也不在 exact source-semantic stream 清单内；
  普通 verifier 只检查 linkage/landmark 等少数字段：
  `examples/hyperliquid/cross_exchange_trigger_aligned_episodes.py:4864-4938`。
- Frozen contract 自己公开的 `exact_feature_projection` 也只有 14 个
  confirmation feature names：
  `local_live_analysis/skhynix_trigger_aligned_episode_research_v1_stage04_jul30_episode_v3/frozen_episode_v3_contract.json:694-724`。
- 目标方案要求每个 decision feature 都有完整 observation ledger，
  且这是逐行逐 feature invariant：
  `docs/skhynix_trigger_aligned_episode_research_implementation_plan.md:617-643`。
- Standalone admission 只要 `verify_package()` 返回，就无条件宣告
  `source_semantic_verified=true`：
  `examples/hyperliquid/cross_exchange_jul30_episode_v3_admission.py:50-63`。

最小 production-size coherent-rehash 复现：
1. 从正式 package 创建全量 APFS clone：
   `/tmp/0815T003-qa-round2.na5Cuh/attacks/nonconfirmation_feature`。
2. 修改
   `features/family_a/segment_0001.csv.gz`：
   - candidate：`jul30:segment_0001:1`
   - feature：`pre_binance_bid_px`
   - value：`948.13 -> 948.14`
   - `observed_at_ns` 保持
     `1785369289548608530`
   - `source_event_id` 保持
     `derived:96475b2035862c0aea23979a1a81eded31476a14f4af566df2b8e45d2eaa463a`
   - `source_book_version` 保持
     `3|1785369289446094938|32`
   - `calculation_version` 和 `availability_reason=available` 保持不变。
3. 同步重算目标 gzip、全部 artifact records、core 和 canonical
   manifest，不依赖陈旧 hash：
   - files：`107`
   - bytes：`1536559413`
   - core：
     `bec05ca6b292a0b2a56bdf4485f114b91fdadc4c1548391bb3cab462dce79574`
   - full：
     `80bcb1169e10ca9e7e4a620dcc7ff3dc65db33b21b92da781de54165609d900c`
   - manifest：
     `73ecbae9e9e4e9c5f58620d2bd075aee674c53ce457979ac80c2d87685a5e57a`
4. 分别运行：
   - `python -B examples/hyperliquid/cross_exchange_jul30_episode_v3_admission.py --package-dir <attack> --verify-only`
   - `python -B <attack>/runtime_source/cross_exchange_jul30_episode_v3_admission.py --package-dir <attack> --verify-only`
5. 实际结果：
   - source：`rc=0`, `verified=true`,
     `source_semantic_verified=true`
   - archive：`rc=0`, `verified=true`,
     `source_semantic_verified=true`

影响：
- Stage 5 的 case retrieval、distance、model 和 policy research 会直接消费
  Family A/B decision features；当前 admission 无法证明这些值和 provenance
  仍等于 accepted Jul30 source truth。
- 当前 formal data 可在独立重放中正确，并不等于 publication trust
  boundary 已闭合。被接受的 coherent mutation 证明 accepted package
  可以在不触发 admission 的情况下发生研究语义漂移。
- `source_semantic_verified=true` 的声明范围大于实际 exact projection。

必须返修：
- 从 accepted Stage 2/3 candidate projection、Jul30 R0/common timeline/hot
  events 和固定 quality/topology inputs，为完整 Family A/B feature ledger
  重建 exact whole-row stream；必须覆盖正式 schema 的全部 load-bearing
  `value/observed_at/source_event_id/source_book_version/calculation_version/
  availability_reason`。
- 将 Family A/B view 的 load-bearing population/quality eligibility 和
  artifact linkage rows 纳入 immutable source-derived exact projection，
  或证明并冻结它们完全由已验证 rows 唯一派生。
- 更新 frozen contract，明确完整 projection universe，不得只列 14 个
  confirmation features。
- 新增 production-size regression：至少包含本轮
  `pre_binance_bid_px` coherent-rehash 攻击，并要求 source/archive
  均 `rc!=0`。
- 修复并完整重建 package 后，需要第三轮全新独立 QA；Stage 5 继续锁定。

## 第一轮 Findings 复核

### 1. Confirmation state/provenance：已独立关闭

独立 QA 脚本：
- `/tmp/0815T003-qa-round2.na5Cuh/qa_replay.py`
- 脚本直接读取 accepted Stage 3 Jul30 candidate projection、Jul30 R0 hot
  events/common timeline 和正式 artifacts，不调用 package admission
  作为 oracle。

全量结果：
- confirmed candidates：`141768`
- confirmation features：每 candidate `14` 个，共 `1984752` rows
- detector first qualifying TimelineState mismatch：`0`
- feature value mismatch：`0`
- `observed_at_ns != t_confirm_ns`：`0`
- timeline `source_event_id` mismatch：`0`
- `source_book_version` mismatch：`0`

Hostile：
- confirmation value-only coherent mutation：
  source/archive 均 `rc=2`，
  `confirmation_features_family_b source-semantic drift`。
- value + `observed_at` + timeline source ID + book version coherent
  mutation：
  source/archive 均 `rc=2`，
  `confirmation feature provenance drift`。

### 2. Frozen fixed-origin burst/prefix：已独立关闭

全量 `141768` confirmed candidates：
- 按 shared detector fixed-origin 10ms burst 重建 membership/prefix。
- burst reconstruction mismatch：`0`
- confirmed count/qty/duration mismatch：`0`
- touch-through-decision mismatch：`0`
- continuation mismatch：`0`
- gap/opposite/zero-economic reset 后 trade 被错误纳入：`0`

第一轮最小示例：
- candidate：`jul30:segment_0001:14`
- detector/publication count：`57 / 57`
- 第一轮错误值 `73` 已不再出现。

### 3. First-event interval/predicate/sign：已独立关闭

对全部 `268522` outcomes 的三个 first-event 字段独立重建：
- interval-censored：`627406`
- right-censored：`177826`
- segment-censored：`316`
- quality-censored：`18`
- epoch-censored：`0`
- 全部 status/interval/censor/source whole-row mismatch：`0`
- lower `< t_candidate_ns`：`0`
- point/empty interval：`0`
- first observed source predicate/sign mismatch：`0`
- 未发现点化或 zero-fill。

First-event lower/source coherent mutation：
- source/archive 均 `rc=2`，
  `outcomes source-semantic drift`。

### 4. Strict-pre baseline：已独立关闭

正式 Jul30 same-receipt timestamp collision census：
- Hyperliquid BBO：`0`
- Hyperliquid trade：`0`
- common timeline：`0`

因此正式数据不能单独激活 equal-timestamp 边界；QA 另行运行独立最小
synthetic：
- `test_outcome_baseline_is_strict_pre_at_equal_receipt_timestamp`
- `test_same_receipt_trade_items_share_one_observation_interval`
- 结果：`2 passed in 0.08s`
- baseline 和 first-event 都排除了 `event_ts == t_candidate_ns`。

### 5. Classification/source-semantic trust boundary：未完全关闭

当前正式数据：
- classification 与 accepted Stage 3 attribution mismatch：`0 / 268522`
- allowed values：
  `cancel_driven / mixed / trade_driven / uncertain`
- observed distribution：
  - `trade_driven=234526`
  - `mixed=19032`
  - `uncertain=14964`
- contract mapping/source 已冻结。

第一轮明确指出的 anchors/sparse/event/outcome 攻击现均 fail closed；
但是本轮 P1-1 证明完整 source-semantic trust boundary 仍有
non-confirmation decision feature 漏口。因此第一轮 P1-4 的具体旧攻击已
关闭，其总体 trust-boundary finding 不能判定为完全关闭。

## Fresh Full Rebuild

命令：
- `python -B examples/hyperliquid/cross_exchange_trigger_aligned_episodes.py --output-dir /tmp/0815T003-qa-round2.na5Cuh/rebuild/package`

QA 独立按 relative path/type/size/raw SHA 比较 formal 与 fresh：
- inventory exact：`true`
- entries：`128 / 128`
- files：`107 / 107`
- bytes：`1561201692 / 1561201692`
- 全部 per-file raw SHA exact match。

独立重算并验证：
- core：
  `5134f1fd31333f0c4c6a972e524b03e2a34bb4435abe6beddda4c62e34e63586`
- full：
  `c581b65420a08ee2ec658369ecbfde240228127d1a70a243829fa1279feb77e6`
- manifest：
  `d00a68bc038ff21a9ec706d3403c983809f92ad9da914cf810fe377b72138764`
- contract：
  `167acb400d6b288a36aa35b4b10b813d4527cbf0eabd8f5f366a3a46e3a767e5`

这些待验证值全部被 fresh rebuild 和独立 inventory/hash 计算确认，不把
业务报告或 manifest 自报当作 oracle。

## Hostile Trust-Boundary Matrix

所有 load-bearing data attack 都在 production-size APFS clone 上执行；
修改 artifact 后同步重算 artifact records、core 和 canonical manifest。
下表 `2/2` 表示 source/archive 都 `rc=2`；`0/0` 表示两者都错误接受。

| Attack | source/archive | 实际 fail boundary |
| --- | --- | --- |
| allowed-value classification relabel | `2/2` | anchors source-semantic drift |
| corrected auxiliary degraded bool + interval IDs deletion | `2/2` | anchors source-semantic drift |
| sparse count mutation | `2/2` | sparse-range source-semantic drift |
| sparse first-boundary/source identity mutation | `2/2` | sparse-range source-semantic drift |
| event-count nonexistent source ID | `2/2` | event-count source-semantic drift |
| event-count existing-but-wrong source ID | `2/2` | event-count source-semantic drift |
| fixed-grid value/observed/source-age/source identity | `2/2` | fixed-grid source-semantic drift |
| fixed-grid no-new-information + source identity | `2/2` | fixed-grid source-semantic drift |
| fixed-grid degraded mask，保持 degraded row count 不变 | `2/2` | fixed-grid source-semantic drift |
| outcome midpoint markout | `2/2` | outcomes source-semantic drift |
| outcome first-event interval/source predicate identity | `2/2` | outcomes source-semantic drift |
| confirmation value-only | `2/2` | confirmation source-semantic drift |
| confirmation observed/source/book provenance | `2/2` | confirmation provenance drift |
| before/after 同步指向真实 APFS attacker input clone | `2/2` | actual Stage 4 input inventory drift |
| dependency claim drift | `2/2` | manifest/actual dependency identity drift |
| archived admission verifier + test coherent code rehash | `2/2` | fixed worktree source/test archive identity drift |
| unknown artifact | `2/2` | package file universe drift |
| forbidden Aug07 artifact | `2/2` | package file universe drift |
| partial/missing required artifact | `2/2` | package file universe drift |
| **Family A non-confirmation decision feature value** | **`0/0`** | **错误接受；`source_semantic_verified=true`** |

补充：
- 原 degraded-mask deletion 会先触发 `segment summary canonical value drift`；
  QA 另做“mask 改成另一非空 identity、保持 summary count 不变”的攻击，
  才确认拒绝来自 full fixed-grid source-semantic digest。
- 输入 binding 攻击同时修改 matching before/after rows，并让路径指向真实
  attacker clone；拒绝来自 fixed current input inventory，而不是单边
  before/after mismatch。
- archived attack 同时修改 admission verifier 与其 test，并 coherent
  rehash；source/archive 都绑定当前 worktree bytes。

## Verifier Coverage Audit

通过项：
- `_verify_source_semantics()` 对 8 个 segment 顺序迭代，已纳入的
  anchors/sparse/grid/event/outcome/confirmation streams 是全 rows digest，
  不是抽样。
- 这些 expected rows 来自 accepted Stage 2/3 candidate truth、Jul30 R0
  hot events/common timeline/quality sources，不来自可与 artifact 同时修改的
  package truth。
- 正式全量 row counts：
  - sparse A/B：`268522 / 141768`
  - fixed-grid A/B：`4564874 / 2410056`
  - event-count A/B：`805566 / 425304`
  - outcomes：`268522`
  - confirmation exact projection：`1984752`
- Runtime source/test 与当前固定 worktree identity：
  `examples/hyperliquid/cross_exchange_trigger_aligned_episodes.py:4036-4054,4708-4722`。
- Input bindings 会与固定 current source inventory 逐行比较，并做 canonical
  path/allowed-root guard：
  `examples/hyperliquid/cross_exchange_trigger_aligned_episodes.py:4725-4759`。
- Dependency、quality interval、source catalog 和 source semantics 的调用
  顺序：
  `examples/hyperliquid/cross_exchange_trigger_aligned_episodes.py:5459-5492`。

不通过项：
- exact expected/observed feature projection 不覆盖完整正式 feature schema。
- Family A/B view payload 不在 exact source-derived stream 中。
- 因此 verifier 虽然对已列 stream 全量运行，仍不能代表“正式 schema 所有
  load-bearing 字段”已被 immutable source semantics 覆盖。

## Stage 4 原始 Acceptance 复核

Population/linkage：
- anchors：`268522`
- Family A：`268522`
- outcomes：`268522`
- Family B/confirmed：`141768`
- rejected：`126754`
- rejected `t_confirm_ns` non-null：`0`
- Family A 包含全部 rejected，outcome 全覆盖；未发现 confirmation
  selection bias。
- clusters / flows / 2000ms overlap blocks：
  `39928 / 10536 / 9`

Path/outcome：
- sparse exact reconstruction 的 count 与 boundary/source identity 攻击均
  fail closed。
- fixed-grid exact values、source age、source identity、degraded mask、
  no-new-information 和 direction-normalized risk gap 已由 full stream
  digest 与 hostile attacks 覆盖。
- event-count 的 distinct/existing source identity 已由 nonexistent 和
  existing-but-wrong 两类攻击覆盖。
- outcome direction/risk-gap/markout/first-event predicate/sign 全量重建
  mismatch=`0`；markout 与 first-event hostile 均 fail closed。
- cross-segment mismatch：`0`
- cross-epoch mismatch：`0`
- interval/right/segment/quality/epoch semantics：
  `627406 / 177826 / 316 / 18 / 0`
- point-coerced interval：`0`

Feature/limitations：
- feature ledger A/B：
  `23092892 / 15310944`
- unavailable feature rows：`2863855`
- future feature observation mismatch：`0`
- formal
  `underlying_market_state=unknown_calendar_state`
- `trade_quantity_source=binance_trade_q`
- `rpi_adjustment_status=unavailable_historical_feed`
- `depth_stream=depth@0ms`
- `collection_topology_fingerprint` 和
  `hyperliquid_feed_cadence_regime` 已进入 feature contract。
- 对应 contract 位置：
  `frozen_episode_v3_contract.json:327-353,439-456`。

Hard boundary：
- manifest boundary 十项均为 `false`。
- forbidden input rows：`0`
- forbidden schema fields：`0`
- package 内 own-order/fill/PnL/model/score/actionability/network fields：`0`
- 未读取禁止的 legacy/later-session event-row roots。

## Tests、Static、Integrity 和 Atomic Publication

Focused/relevant inherited：
- Stage 4 focused：
  `17 passed in 0.08s`
- Stage 1/2/3 relevant inherited：
  `216 passed in 133.42s`
- R0/alignment/recovery relevant inherited：
  `83 passed in 0.45s`
- unique suite evidence：`316 passed`
- strict-pre targeted subset rerun：
  `2 passed in 0.08s`

Static/integrity：
- Ruff：`All checks passed!`
- external `PYTHONPYCACHEPREFIX` compileall：
  `48` bytecode files，`rc=0`
- scoped `git diff --check`：`rc=0`
- deterministic gzip integrity：`96/96`
- builder/admission `--help`：均 `rc=0`

Atomic partial publication：
- QA monkeypatch `_process_segment`，先写 partial marker 再抛错。
- injected failure 被观察到。
- output directory 不存在。
- staging directories：`0`
- partial markers：`0`

## Zero-Write 和 Input Identity

默认 Python source/archive admission：
- source：`rc=0`, `verified=true`,
  `source_semantic_verified=true`
- archive：`rc=0`, `verified=true`,
  `source_semantic_verified=true`
- 两者返回正式 core/full 和 A/B/rejected counts。

正式 package QA 前后：
- entries：`128 / 128`
- files：`107 / 107`
- bytes：`1561201692 / 1561201692`
- relative path/type/size/raw SHA/mtime_ns/ctime_ns exact identical：`true`
- package `.pyc/__pycache__`：`0`

输入 QA 前后：
- rows：`394 / 394`
- bytes：`1412604724 / 1412604724`
- identity：
  `b64e3db82746297b4d9684ab1013d6c657221467a3a1083cc82b2124df3205a6`
- exact identical：`true`

## Performance Caveat

- Full source-semantic verifier 是 CPU-bound，会重放高基数 grid/event/outcome
  source truth。
- 本轮并发 hostile runs 均正常终止；观察到单进程 RSS 约
  `220-675 MB`，无持续失控增长。
- APFS clones 为 production-size logical package；未写 accepted package
  或 accepted inputs。
- 验证较慢可接受，但不改变 correctness 判定。

## 验收结论

- `未通过`
- 第一轮 confirmation provenance、frozen burst、Candidate left truncation
  和 strict-pre defect 已独立关闭。
- 第一轮明确的 anchor/path/outcome hostile classes 也已 fail closed。
- 但 source-semantic trust boundary 仍未覆盖完整 Family A/B decision
  feature ledger；本轮 production-size coherent mutation 被 source 和
  archive 同时接受，是唯一但决定性的 P1。
- Stage 5 必须继续锁定。
- 本轮不修改 `.workflow/tasks/0815T003.md` 状态，也不修改
  `task_plan.md`、`progress.md`、`findings.md`、实现、测试或 package。

通过项：
1. Fresh rebuild 与正式 package 逐文件完全一致，四个冻结 identity 已独立确认。
2. 第一轮四个数据语义缺陷中的 confirmation、burst、first-event、
   strict-pre 已全量或最小 synthetic 独立关闭。
3. Family A/B population、outcome coverage、path/outcome exact streams、
   hard boundary、测试、静态检查、gzip、atomic publication 和 zero-write
   均通过。
4. Source roots、dependency bindings、runtime verifier/tests 与 current
   immutable identities 的绕过攻击均 fail closed。

不通过项：
1. 完整 decision feature ledger/source-semantic admission trust boundary
   未闭合。

缺陷清单：
1. P1-1：非 confirmation Family A/B decision feature 和 view payload
   没有完整 immutable source-derived exact projection；已证明
   `pre_binance_bid_px` coherent mutation 被 source/archive 错误接受。

阻塞项：
- 无外部阻塞；需要业务线程有界返修。

建议总控下一步：
1. 保持 `0815T003` 未通过语义和 Stage 5 锁定。
2. 派发有界返修，只扩展完整 feature/view source-semantic projection、
   contract 和 hostile regression，不扩展到 Stage 5。
3. 返修后完整原子重建正式 package，并启动第三轮全新独立 QA。

提交信息：
- commit：无
