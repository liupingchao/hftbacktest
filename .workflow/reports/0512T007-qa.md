# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0512T007

状态：
- 已通过

更新时间：
- 2026-05-12 23:06 Asia/Shanghai

验收线程：
- QA验收线程

验收对象：
- 测试线程 + 0512T007

验收方式：
- 正常验收

验收范围：
- 基于 `0512T006` 计划，对既有 `0512T005` Stage 6J replay CSV/audit 输出做只读 attribution / experiment implementation，解释 pure toxic timing candidates 为什么有 action-path coverage 但没有 replay risk improvement。本任务不改策略代码、不运行新 Stage 6J replay、不设计更严格候选规则、不启动 live。

验收步骤：
1. 读取 `.workflow/tasks/0512T007.md`。
2. 读取 `.workflow/reports/0512T007-business.md`。
3. 检查任务状态、业务回报、verify 证据、done 结论和阻塞项。
4. 写入 `.workflow/reports/0512T007-qa.md` 并刷新看板。

实际结果：
- 任务文件存在：.workflow/tasks/0512T007.md
- 业务回报状态：待验收
- 业务回报存在：.workflow/reports/0512T007-business.md

验收结论：
- 已通过
- 结论说明：
  - QA 通过。T007 已完成只读 attribution，并明确排除以下错误方向：1. 排除“pure toxic timing 没有 action-path coverage”，因为 100ms blocked rows 为 594；2. 排除“reduce-side 被误伤或靠 reduce-side 抑制制造结果”，因为 blocked reduce-side 为 0；3. 排除“只调 window_ms 50/100/200ms 就可能改变结论”，因为三档 unique blocked key sets 等价率 100%，原因是 blocked reason 被 pending_cancel+target_move 主导，window_ms 在这些 replay 样本中不是实际区分变量；4. 排除“主样本缺少触发量”，因为 5-11-night-active blocked 299，但 true submit removed 为 0；5. 排除“pure toxic timing 已经过滤到 Stage 6J replay adverse-selection/cancel-fill 风险订单”，因为 true submit removals 与 baseline cancel-fill risk event order id overlap 为 0/56，且 baseline 与 candidate 100ms risk order ids 不变；6. 排除“replay risk 不改善只是 blocked rows 不够多”，因为 blocked rows 足够但大部分是 eligibility-only coverage，538/594 没有真正删除 baseline submit；7. 排除“当前 T007 可以作为 live source-path improvement proof”，因为它仍是 replay-model attribution，不是 live-derived source-path proof。T007 不授权 stricter candidate redesign、new replay、default enable 或 live micro test。

通过项：
1. 业务回报已进入待验收状态
2. 任务声明需要 QA 验收
3. 业务回报包含 verify 证据
4. QA 通过。T007 已完成只读 attribution，并明确排除以下错误方向：1. 排除“pure toxic timing 没有 action-path coverage”，因为 100ms blocked rows 为 594；2. 排除“reduce-side 被误伤或靠 reduce-side 抑制制造结果”，因为 blocked reduce-side 为 0；3. 排除“只调 window_ms 50/100/200ms 就可能改变结论”，因为三档 unique blocked key sets 等价率 100%，原因是 blocked reason 被 pending_cancel+target_move 主导，window_ms 在这些 replay 样本中不是实际区分变量；4. 排除“主样本缺少触发量”，因为 5-11-night-active blocked 299，但 true submit removed 为 0；5. 排除“pure toxic timing 已经过滤到 Stage 6J replay adverse-selection/cancel-fill 风险订单”，因为 true submit removals 与 baseline cancel-fill risk event order id overlap 为 0/56，且 baseline 与 candidate 100ms risk order ids 不变；6. 排除“replay risk 不改善只是 blocked rows 不够多”，因为 blocked rows 足够但大部分是 eligibility-only coverage，538/594 没有真正删除 baseline submit；7. 排除“当前 T007 可以作为 live source-path improvement proof”，因为它仍是 replay-model attribution，不是 live-derived source-path proof。T007 不授权 stricter candidate redesign、new replay、default enable 或 live micro test。

不通过项：
1. 无

缺陷清单：
1. 无

阻塞项：
- 无

业务回报阻塞项：
- 无执行阻塞。
- 晋级阻塞仍存在：没有 live-derived source-path proof；不能授权 live micro test。
- 后续如要改规则，必须另建规则设计任务，且不能把 T007 attribution 直接当成 live improvement proof。

建议总控下一步：
1. 总控可以将该任务视为验收通过。
2. 如存在后续任务，可按前置条件派发下一任务。

提交信息：
- commit：无
