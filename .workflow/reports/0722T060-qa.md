# QA 验收结果

## Findings（按严重性）

- P0：无。
- P1：无。
- P2：无。

执行线程：
- QA验收线程

任务ID：
- 0722T060

状态：
- 已通过

更新时间：
- 2026-07-22 15:35 Asia/Shanghai

验收线程：
- QA验收线程

验收对象：
- 业务执行线程 `0722T060`
- implementation commit
  `f73c1480e1ad47a41533a15db5d6e4db706b87cd`
- workflow/business commit
  `a6ec59c407f431a4cec67da9c51df9cc2dd13662`

验收范围：
- 严格 offline-only 独立 hostile review。
- 覆盖 known legacy source 的 existing parent/final symlink、missing tail、
  safe current same-relative target、relative/current destination、
  traversal、missing-only skip 与 malformed-existing failure。
- 核对 production watcher/manager 未被 T060 修改。
- 所有 pytest 和 py_compile 均使用
  `conda run -n hftbacktest ...`。
- 未修改 implementation/test 文件。

验收步骤：
1. 阅读 workflow-kit、T060 task/business 和 T059 failed QA，核对固定提交
   及范围。
2. 审查 resolver 实现、测试 diff 和 production 文件边界。
3. 在临时 current/legacy/outside roots 中运行独立 hostile matrix。
4. 运行 focused、deadline hostile 20 次、连续两次 full Hyperliquid。
5. 运行 Conda py_compile、skip reason、diff 和工作树检查。

实际结果：
- Conda 环境为 Python `3.12.13`，解释器
  `/Users/liu/.local/conda/envs/hftbacktest/bin/python`，pytest `9.0.3`。
- Known legacy 分支现在无条件执行 `Path.resolve(strict=False)` 和
  resolved legacy-root containment，不再依赖 final child 的
  `exists()/is_symlink()`。
- 独立 hostile matrix `11/11 passed`：
  - 合法 current relative/absolute、合法 missing/existing legacy source
    relocation 通过。
  - Legacy lexical traversal、relative traversal、legacy final symlink
    existing/missing target、legacy parent symlink single/multi-component
    missing tail、current destination symlink 均 fail-closed。
- 上轮 P1 反例已关闭：legacy parent 是 root 外 symlink、final child
  缺失且 current checkout 有安全同名文件时，resolver 抛出
  `RowLevelGeneratorError`，不会返回 current target。
- True missing historical packages 仅产生两个预期 skip；malformed existing
  source contract 继续抛错，不被 availability helper 吞掉。
- 两个 skip reason 精确为：
  - `0609T002 historical pricing-signal source package is absent`
  - `0602T001 synchronized public sample package is absent`
- Focused：
  `21 passed, 2 skipped in 0.40s`。
- Deadline hostile repeated：
  `20/20 passed`。
- Full Hyperliquid run 1：
  `1260 passed, 2 skipped in 58.11s`。
- Full Hyperliquid run 2：
  `1260 passed, 2 skipped in 58.02s`。
- 四个相关文件 Conda `py_compile` 通过。
- Implementation/workflow `git diff --check` 通过。
- Production public watcher 在 T059 QA commit 与当前 HEAD 的 SHA-256 均为
  `395411f43673d759a0b8f290eef51d210422af4f244ecbce3cf9476267a0ed4f`。
- 工作树中的既存 `.DS_Store` 未修改、未暂存。
- 未执行 network、remote、private/account、order/cancel、live 或
  service 操作。

验收结论：
- 已通过。
- 结论说明：
  - T059 的 parent-symlink + missing-final-child P1 已关闭；source 与
    destination containment、missing-vs-malformed、deadline 和 full-suite
    验收全部通过。

通过项：
1. Known legacy source containment 对 final child existence 无条件成立。
2. Existing parent/final symlink 与 missing-tail hostile 均 fail-closed。
3. Safe current/legacy relocation 和 destination containment 保持正确。
4. Missing-only skip 与 malformed-existing failure 保持严格区分。
5. Deadline `20/20`、两轮 full suite、py_compile 和 diff checks 全部通过。
6. 未改变 production strategy、timestamp、deadline、quote、risk 或
   lifecycle 行为。

不通过项：
1. 无。

缺陷清单：
1. 无。

阻塞项：
- 无。

建议总控下一步：
1. 结束 fixture portability repair 链，按既定顺序派发 basis regression
   alpha 的正式实现与验收任务。
2. 保持 basis regression 先 offline/public-only、leave-one-window-out、
   train-fold-only normalization，再决定是否进入 production-equivalent
   public shadow。

提交信息：
- QA commit：由本报告提交后的线程回报提供。
- Implementation commit：
  `f73c1480e1ad47a41533a15db5d6e4db706b87cd`
- Workflow/business commit：
  `a6ec59c407f431a4cec67da9c51df9cc2dd13662`
