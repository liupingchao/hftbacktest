# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0719T004

状态：
- 未通过

更新时间：
- 2026-07-19 23:59 CST

验收线程：
- QA验收线程

验收对象：
- implementation commit `a739a78cfb4cc58a23644e67644a4289ad5789af`
- business report commit `80a9763d7b173e96efc06e7d0965a8702cfdd3e9`

验收范围：
- Redaction-safe reference token、严格 attempt parser、producer/acceptance 独立重建、standalone/manager writer、历史对抗回归及结构性 fail-closed。

验收步骤：
1. 审阅任务、业务报告、prior `0719T003` QA、implementation/report commits。
2. 独立生成 standalone multi-attempt 和 two-sided manager producer artifacts，经真实 writer/redaction 回读并重建。
3. 重放 token、attempt、partial/conflicting target、forged summary、ambiguous response 和 missing raw proof 对抗矩阵。
4. 检查 producer/acceptance helper independence，运行 focused/full tests、compile/help/diff checks。

## Findings

1. P1：malformed cancel success response 可伪造权威取消证明并通过完整 acceptance。
   - `hyperliquid_tiny_live_real_order_executor.py:913-929` 对字典状态只检查存在 `success` 键，不验证其值。
   - `hyperliquid_tiny_live_m2_fill_window.py:1966-1973` 因此把 `{"success": false/null/0/""/{}/[]}` 全部计为 authoritative success。
   - `cross_exchange_t024_same_window_acceptance.py:291-314` 独立实现了相同的结构漏洞。
   - QA 将 `statuses` 改为 `[{"success": false}]`，同步 producer/proof summaries 后，producer 和 acceptance rebuild 均为 `pass`、exact equal，完整 acceptance 错误返回 mechanism pass。
2. P2：超长规范 attempt 字符串导致 producer 和 acceptance 抛出未处理异常。
   - `hyperliquid_tiny_live_m2_fill_window.py:162-169`。
   - `cross_exchange_t024_same_window_acceptance.py:213-220`。
   - 5000 位纯数字字符串通过正则后，`int(value)` 触发 Python 位数限制并抛出 `ValueError`，没有返回结构化 `fail_closed`。

实际结果：
- Standalone multi-attempt 和 two-sided manager 真实 writer 写盘后，raw oid/cloid 被遮蔽，token 保留，acceptance rebuild 与 manifest/proof summary exact equal。
- Reference key 不含 raw oid/cloid；token 为 kind-bound SHA-256 格式。
- Producer/acceptance 分别验证 token 格式、raw/token conflict 和 all-token same-reference。
- Acceptance 生产模块未 import/call producer reconciliation/token helper。
- `1.1/1.9` alias、bool、所有 float、zero、negative、NaN/Infinity、scientific notation、空白及非规范字符串均 fail-closed；`1`、`"1"` 通过。
- Partial/conflicting target、forged summary、unrelated raw target、ambiguous generic response、missing raw lists、cross-attempt 和 redundant generic 回归通过。
- Focused 5-file regression：`185 passed in 22.33s`。
- Writer/nominal acceptance 定向验证：`3 passed in 2.21s`。
- Full `python -m pytest examples/hyperliquid -q -p no:cacheprovider`：`526 passed in 32.28s`。
- Modified Python `py_compile`、三个 CLI `--help`、implementation/report `git diff --check` 通过。
- 未运行任何 live/private/account/order/cancel/network/remote/service 操作。

验收结论：
- 未通过
- 结论说明：
  - 核心 token/redaction 和 fractional-attempt 修复成立，但 raw cancel response 仍存在可构造完整机制通过的 P1 fail-open。

通过项：
1. Persisted redaction-safe token contract 与 producer-written artifact exact reconstruction。
2. Strict ordinary attempt matrix 和 `1.1/1.9` alias fail-closed。
3. All-token same-reference、raw/token conflict、summary/raw contradiction 和 helper independence。
4. 既有 quote、strategy、risk envelope 和 activation behavior 未改变。

不通过项：
1. False/null/empty `success` value 被当作 authoritative cancel success。
2. 超长 canonical digit attempt 没有 fail-closed，而是抛出未处理异常。

缺陷清单：
1. P1：malformed cancel success payload 可使完整 acceptance fail-open。
2. P2：超长 attempt 数字字符串导致未处理异常。
3. 缺少 malformed success value 和超长 attempt 的直接回归测试。

阻塞项：
- 不得创建或启动新的 live task。
- Principal Task 12 与 Task 10 single-level two-sided lifecycle gate 继续保持未关闭。

建议总控下一步：
1. 新建 offline repair task，producer 和 acceptance 独立要求 cancel status 精确符合允许的成功结构和值，并拒绝多状态歧义。
2. 增加 `false/null/0/empty/object/list` success payload 的 producer、rebuild 和完整 acceptance regressions。
3. 为 attempt 增加明确长度/范围限制及异常捕获后重跑 focused/full QA。

提交信息：
- implementation commit：`a739a78cfb4cc58a23644e67644a4289ad5789af`
- business report commit：`80a9763d7b173e96efc06e7d0965a8702cfdd3e9`
- QA commit：无
