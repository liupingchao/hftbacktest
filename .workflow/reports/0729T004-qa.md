# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0729T004

状态：
- 已通过

更新时间：
- 2026-07-29 14:03 CST

验收线程：
- QA验收线程

验收对象：
- 业务线程-python/public-collector + 0729T004

验收范围：
- Binance snapshot/WebSocket bootstrap、bridge、stale depth 丢弃、
  depth continuity、reconnect、reader shutdown、deadline tail drain、
  manifest replay-ready 及相关离线回归测试。
- 未执行网络、AWS、远端或交易所操作。

验收步骤：
1. 审查最新任务、业务报告、采集实现和测试差异。
2. 复核 reader shutdown、reconnect 门禁、RuntimeError 优先级和 manifest。
3. 运行 focused pytest、关键边界测试、py_compile、CLI help、diff check
   及过滤既有环境项后的目录回归。

实际结果：
- WebSocket reader 在 REST snapshot 期间持续 drain。
- queue 和 retained pre-bridge buffer 均有界，溢出路径 fail closed。
- stale depth 在 `u < lastUpdateId` 时被计数并丢弃，不写入 raw。
- 第一条有效 bridge 满足 `U <= lastUpdateId <= u`。
- bridge 后每条 depth 强制 `pu == previous u`。
- reconnect 仅在旧 reader clean shutdown 后发生，并重新
  snapshot/bridge。
- close+join 后 reader 仍存活时，记录
  `reader_shutdown_timeout_count/events`，禁止 reconnect，
  `depth_replay_ready=false`，并优先抛出 shutdown RuntimeError。
- deadline 成功收尾会等待 reader 并 drain 已接收的队列尾部。
- 业务报告与最新工作树测试计数一致。
- focused pytest：`18 passed`。
- 四条关键边界测试：`4 passed, 14 deselected`。
- 目录回归：`1309 passed, 2 skipped`。
- py_compile、两个 CLI help 和 git diff --check 均通过。

验收结论：
- 已通过
- 结论说明：
  - 此前 reader shutdown/reconnect 生命周期缺陷及 stale depth 测试缺口
    均已关闭；本轮离线 collector contract 满足任务验收条件。

通过项：
1. snapshot/WebSocket bootstrap、bridge 和 stale depth 处理符合任务要求。
2. depth continuity、buffer overflow 和 missing bridge 均 fail closed。
3. reconnect 前完成旧 reader shutdown；shutdown timeout 禁止 reconnect。
4. manifest replay-ready 包含 snapshot、bridge、continuity、shutdown 和
   正常 deadline 门禁。
5. focused、关键边界和目录回归全部通过。

不通过项：
1. 无

缺陷清单：
1. 无

阻塞项：
- 无

建议总控下一步：
1. 将 0729T004 更新为已通过。
2. 后续如需证明真实 exact Binance 或双交易所订单簿回放，另行授权并运行
   canary/2H 数据采集；本次通过仅代表离线实现和回归验收通过。

提交信息：
- commit：无
