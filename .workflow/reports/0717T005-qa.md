# QA 验收结果

Recovery notice:
- Original QA date: 2026-07-17.
- Reconstructed on 2026-07-28 from `task_plan.md`, `progress.md`,
  `findings.md`, and the downstream `0717T006`-`0717T011` repair chain.
- This is a historical record, not a new QA execution or authorization.

执行线程：
- QA验收线程

任务ID：
- 0717T005

状态：
- 未通过

更新时间：
- 2026-07-17 Asia/Shanghai（reconstructed record）

验收线程：
- QA验收线程

验收对象：
- `d4af427..c9547f2` remote-update code review

验收范围：
- Watcher lifecycle, checksum sealing, fill attribution, runtime envelope,
  and account/service provenance.

验收步骤：
1. Review durable controller findings for the code range.
2. Cross-check each recorded defect against the task scope subsequently
   implemented by `0717T007`-`0717T010`.
3. Confirm no live/private/order action was part of this review.

实际结果：
- Focused tests passed, but test success did not close the evidence defects.
- Signal handling recorded abort intent without terminating/reaping the active
  watcher child.
- Failed-run SHA-256 manifests could be stale.
- Fill attribution was not window/attempt stable or idempotent.
- Multi-window artifact identity retained a hardcoded window assumption.
- Account/service isolation remained unresolved.
- The runtime max-loss/max-position finding was later removed from the
  immediate repair route by a separate controller scope decision.

验收结论：
- 未通过
- 结论说明：
  - The reviewed update was not ready for another live evidence attempt; an
    offline, sequential repair plan was required.

通过项：
1. Review scope and focused-test evidence were recorded.
2. No live/private/order/cancel action occurred.

不通过项：
1. Watcher termination and reap were incomplete.
2. Terminal artifact integrity was not reliable.
3. Fill attribution and multi-window identity were not deterministic.

缺陷清单：
1. Missing process-group-aware child termination and timeout.
2. Stale failed-run checksum manifest.
3. Non-idempotent, non-attempt-bounded fill attribution.
4. Hardcoded multi-window artifact identity.
5. Missing account/service isolation.

阻塞项：
- Further live evidence collection required an accepted offline repair route.

建议总控下一步：
1. Create `0717T006 / LIVE-EVIDENCE-INTEGRITY-REPAIR-PLAN`.
2. Execute the repair phases sequentially with independent QA.

提交信息：
- commit：无
