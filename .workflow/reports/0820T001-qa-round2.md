# QA 验收结果

执行线程：
- 独立 QA 验收线程

任务ID：
- `0820T001`

状态：
- 未通过

更新时间：
- 2026-08-21 12:04 CST

验收线程：
- 独立 QA 验收线程

验收对象：
- `0820T001 / RESEARCH-PACKAGE-TRUST-KERNEL-LAYERED-IDENTITY-AND-STAGE4-PARITY`
- repair commit：
  `268b3795874c6487d3fadbd1a5ea812b803ded46`
- evidence/candidate commit：
  `6548e8722618df01d59371a8ba88246c341ab0e8`
- QA candidate：
  `/home/molly/project/durable_archives/research_package_trust_kernel/qa_candidates/0820T001/6548e8722618df01d59371a8ba88246c341ab0e8`

验收范围：
- 第二轮独立复核第一轮 QA 的五项返修：
  current/frozen negative topology、future accepted registry/pin、
  canonical relocation、QA-specific full-admission outputs、Surface Matrix
  stable-code execution、final archive/cleanup binding。
- 按执行方案重新检查 Gate 0-7，并独立运行一次 Stage 4 完整只读
  source-semantic admission。
- accepted registry 未写入，Stage H0-A 未启动，99 个 accepted research
  files 和正式 Stage 4 package 只读。

## Findings First

P0/P1/P2/P3：
- `0 / 1 / 1 / 0`

### P1-1：final archive receipt 不满足明示的严格时序

- 任务要求 exact 验证：
  `hostile < first-full < parity < final archive < cleanup final-envelope attestation`。
- 业务 evidence 的时间为：
  - hostile completed：
    `2026-08-21T03:13:31.813992Z`
  - first-full started：
    `2026-08-21T03:13:39.232975Z`
  - parity completed：
    `2026-08-21T03:28:16.895742Z`
  - final archive started：
    `2026-08-21T03:28:16.895742Z`
  - final archive completed：
    `2026-08-21T03:28:26.863661Z`
  - cleanup final-envelope attested：
    `2026-08-21T03:28:31.624512Z`
- `parity completed == final archive started`，不是严格 `<`。
- runner
  `.workflow/runners/0820T001_archive_stage4_to_amdserver.sh`
  在 refresh 和 initial archive 两条路径中均把
  `started_at_utc` 直接赋值为 `parity["completed_at_utc"]`，没有记录实际
  archive/envelope operation start，也没有执行
  `parity completed < archive started` 的结构化断言。
- 业务报告把这两个相等时间写成严格 `<`，与 receipt 机器事实不一致。
- Archive exact tree、R/C/E/composite 和 cleanup post-final attestation
  本身均通过；失败点是任务明确要求的 load-bearing chronology proof
  不成立。

### P2-1：QA_ENTRYPOINT 的 full-admission 命令与 frozen portability 声明矛盾

- `QA_ENTRYPOINT.md` 指示在 amdserver candidate workspace 对 durable
  archive package 直接运行 `--full-admission`。
- 执行方案和 archive receipt 同时冻结：
  `full_source_semantic_replay_portable=false`，完整 replay 仍依赖 Mac 上
  exact Stage 1/2/3 和 Jul30 inputs。
- 按 entrypoint 原命令执行时，legacy verifier 尝试打开
  `/Users/liu/Documents/.../skhynix_trigger_aligned_episode_research_v1/research_manifest.json`
  并失败；该命令还会先写 first-full start receipt，不能原样重试。
- QA 最终必须转到 Mac accepted local inputs，并显式使用 formal package
  的 frozen runtime source 才完成 full admission。
- 这不否定 Gate 3 的实际 parity 结果，但第二轮交接入口仍不是可直接执行
  的完整 QA runbook。

## Positive Evidence

- Candidate detached clean HEAD
  `6548e8722618df01d59371a8ba88246c341ab0e8`；源分支与
  `origin/episode-research` exact，工作树干净。
- Bundle verify 通过；bundle SHA256：
  `aff03a4ea15a7be074628040c463fc71e7c99725752a49e5ce99bb238dc9bf7d`。
- 127-file gate inventory 的 path/bytes/mode/SHA mismatch=`0`；
  canonical self SHA256：
  `3a10e850d55b8204de6306d635f2c57f91c69a063efb4eb0c2ed2082cdaeaada`。
- Gate 0 validator：
  `7 surfaces / 27 artifacts / 10 declared+executed mutations / EC1-EC7`。
- Trust Kernel/workflow focused suite：`50 passed`。
- current formal / amdserver archived Stage 4 focused suite：
  `97 passed / 97 passed`。
- Ruff：`All checks passed!`；`bash -n`、`py_compile`、
  `git diff --check` 通过。
- 独立 hostile preflight：
  - receipt：
    `26e276b26a7f3a9b660b57d9895cc723871abcb6f003589cb4365cbf0bbd1047`
  - source/frozen：
    `cee2395afad9420c38235ba195bf030e92330015e1a15937ebc22fa707c80203`
  - topology：
    `49 x current/frozen`、`6 attacks x 3 boundaries x current/frozen`、
    `6 production attacks x current/frozen`
  - counts：
    `98/36/12/4/10`，fail-open=`0`
  - 10 个 Surface Matrix mutation 的 declared/observed stable code
    全部 exact。
- macOS `/tmp -> /private/tmp` alias 下 hostile preflight 通过，
  canonical relocation finding 已关闭。
- 第二轮 QA fresh full source-semantic admission：
  - first-full receipt：
    `81796c989c74a41b94b27bfd99e3f3bbb1714ce3e33313a065f356092fd98565`
  - parity report：
    `337c7c387fd9e639e03a0a4492bbcdf371edae7fbc4cc829d49fcfcc28c3bd68`
  - hostile `<` first-full `<` QA parity 为严格顺序
  - legacy/kernel source semantic verified=`true/true`
  - files/dirs/artifacts/bytes：
    `107/21/106/1,561,307,420`
  - full rebuild/package mutation=`0/0`
  - pre/post metadata exact=`true`
  - R：
    `bb5aed2099b1a97da5b476d4b09dfe4a7bac06f0bf331f8ca864a88daf5c9232`
  - C：
    `f1c84731b71400254a6742b8620db7f096349dac7e4e7d3de906d0f66bc9a579`
  - E：
    `7320058cf2021a132fd1869d8864727b33be8a99e054d75856b772f70d04535a`
  - composite：
    `50680a81b02f7cedac3fef7881d560d075e7b8b110ba2cceeb0d33ab50ebb128`
- amdserver archive 独立全树重算：
  `107 files / 21 dirs / 128 entries / 1,561,307,420 bytes`；
  entries/type/mode/bytes/SHA 与 receipt exact。
- Archive tree identity：
  `f7348e4d456a7cfd7014d73590c79b87b04b4f3e21219070422e156d95b71a6e`；
  file identity：
  `669fb7d12f25cfa7828aec0fb1546398b1def754952de2290cd19784a477a433`。
- Archive kernel-only admission独立通过，R/C/E/composite 与 fresh full
  admission exact，pre/post metadata exact。
- Cleanup old receipt self-hash
  `b446c5946753d74b71ce66b9ab97e37f32083d6d32f31c5738e297f56b178911`
  与 final receipt 的 `pre_binding_receipt_sha256` exact；
  Phase A/B、allowlist、inode/dev、mutation count 和禁止 recursive/glob
  字段保持不变。
- Final archive receipt
  `6e1d42f197d916fab5bcc8b610958b81d0c8487e810d05470fdafcb3cc5fbdb4`
  与 cleanup receipt
  `c4dd06d2efa502163f08010d996a2e484aae67662c067a87513924fe11dc6413`
  canonical self-hash及 R/C/E/composite binding通过。
- Formal package仍存在；七个 duplicate empty dirs均不存在；
  task processes remaining=`0`。
- Registry保持 revision `0`、`versions=[]`，raw SHA256：
  `d4e045a5aeca78288ce38d66497ace3baca1962058c583adfc33ac5644b0d285`。

## Gate 0-7 结论

1. Gate 0：通过。Matrix/schema/EC1-EC7/10 executed mutations exact。
2. Gate 1：通过。Purity、source/frozen、future registry package binding、
   append-only prior revision 和 accepted pin tests通过；registry仍为空。
3. Gate 2：通过。真实 current/frozen topology、production CLI、stable
   code、zero-write和 hostile-first QA evidence通过。
4. Gate 3：通过。独立 fresh full source-semantic admission exact parity，
   正式包 zero-write。
5. Gate 4：通过。Workflow positive/negative enforcement通过。
6. Gate 5：未通过。Archive内容和 identity exact，但
   `parity completed < archive started` 的严格 evidence 不成立。
7. Gate 6：通过。Cleanup final-envelope binding、两相历史证据和
   post-final absence/formal-package attestation通过。
8. Gate 7：通过。Research bytes/semantics未变，无 rebuild、model/order/live
   工作和残留 task process。

实际结果：
- 第一轮五项核心代码缺陷均已得到实质修复。
- 第二轮仍有一项 P1 chronology proof 缺陷和一项 P2 QA handoff/runbook
  缺陷。
- 当前 candidate 不能发布为 accepted Trust Kernel v1。

验收结论：
- 未通过
- 结论说明：
  - Trust Kernel 行为和 Stage 4 parity 已通过独立验证，但 archive
    receipt 不满足任务明示的严格时序，且 QA_ENTRYPOINT 与 frozen
    portability contract 矛盾，因此不能进入 registry promotion。

通过项：
1. 第一轮 P1-1 到 P1-4 的 kernel/workflow/relocation/output 修复。
2. 第一轮 P2 final cleanup identity binding 修复。
3. 独立 hostile、fresh full parity、双端 Stage 4 tests 和 archive exact
   tree。

不通过项：
1. Final archive chronology strict-order proof。
2. QA_ENTRYPOINT full-admission host/package 选择。

缺陷清单：
1. P0：无。
2. P1：1 项，见 P1-1。
3. P2：1 项，见 P2-1。
4. P3：无。

阻塞项：
- 无外部 blocker；需要同一任务的窄范围 evidence/runner 返修。

建议总控下一步：
1. 保持 `0820T001` 为 `未通过`；accepted registry继续 revision 0 /
   versions empty，Stage H0-A继续锁定。
2. Runner 在 archive/envelope operation 真正开始时采集独立
   `started_at_utc`，并在写 receipt 前 exact断言
   `parity.completed_at_utc < archive.started_at_utc <
   archive.completed_at_utc`。
3. 重建 final archive receipt 和 cleanup final-envelope attestation；
   不需要重传或重建 1.5GB package，但必须重新闭合 self-hash、identity
   binding 和严格时间顺序。
4. 修正 `QA_ENTRYPOINT.md`：Gate 3 指向 Mac accepted formal package、
   Python 3.10+ 和 frozen runtime source；amdserver archive只提供
   kernel/package admission命令。
5. 返修后重新进行一轮独立 QA；无需修改 research bytes、Stage 4
   semantics 或 accepted registry。

报告文件一致性：
- 第一轮 `.workflow/reports/0820T001-qa.md` 保持原样。
- 本报告原样镜像到 `docs/qa-acceptance-report.md`。
- 写入后执行 `cmp -s` 和 SHA256 确认 byte-identical。

提交信息：
- commit：无
