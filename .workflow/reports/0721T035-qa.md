# QA 验收结果

## Findings

1. **P0：无。**

2. **P1 - container 类型的 raw `method` 导致验收异常退出，而不是 fail-closed。**
   - Producer 在 `hyperliquid_tiny_live_m2_fill_window.py:2671`、independent helper 在 `cross_exchange_t024_same_window_acceptance.py:3162` 直接执行集合成员判断。
   - 当 `method=[]` 或 `{}` 时，两者均抛出 `TypeError: unhashable type`。
   - Task 12 端到端使用 `method=[]` 时同样异常退出，未生成 blocked manifest。
   - 这违反了 hostile container method 必须结构性 fail-closed 的合同。

3. **P1 - malformed historical result envelope 未产生显式 mismatch，audit 甚至可以通过。**
   - 两套 mismatch helper 只比较 `result.status`，未验证 historical `orders` envelope。
   - Classifier 在 producer `:2579-2599`、independent `:3068-3090` 将 malformed envelope 归类为 `unknown`；当持久化 `query_status=unknown` 时不会产生 status mismatch。
   - 以下 payload 均使 producer/independent audit 返回 `status=pass / reasons=[]`：
     - `{"status":"historical_orders"}`
     - `{"status":"historical_orders","orders":null}`
     - `{"status":"historical_orders","orders":{}}`
     - `{"status":"historical_orders","orders":[null]}`
   - Task 12 测试 fixture 最终仍被 terminal-reference 语义层 blocked，分层本身正确；但未满足任务要求的显式 `terminal_audit_query_method_result_mismatch`。

4. **P2：无。**

执行线程：
- 独立 QA 验收线程

任务ID：
- 0721T035

状态：
- 未通过

更新时间：
- 2026-07-21 14:29:28 CST

验收线程：
- 独立 QA 验收线程

验收对象：
- implementation `180d24bd520e68487cdf843385b3940f426b0dc8`
- business report `ec08644e94895130da17c2f08fe3e1eaa2a4e400`

验收范围：
- Raw historical semantics trigger、method/result mismatch、malformed attempt、legacy compatibility、Task 12 rollout。
- T033 exact timing、T034 marker trigger、manager/quarantine 和历史边界。
- Live executor、watcher、manager、orchestrator、quote、risk、submission 和 activation 不变性。

验收步骤：
1. 核验 workflow、T035 task、T034 QA、提交哈希及 committed diff。
2. 审查 producer、independent rebuild 和 Task 12 分层。
3. 执行 method/result、canonical/attempt、marker、malformed row hostile matrix。
4. 运行 focused、targeted 和 full Hyperliquid regression。
5. 在 `/tmp` 重跑 T031/T026/T016/T022 历史边界。

实际命令与结果：
- `git rev-parse 180d24bd520e68487cdf843385b3940f426b0dc8 ec08644`：哈希解析正确。
- `python -m pytest ...test_hyperliquid_tiny_live_m2_fill_attribution.py ...test_cross_exchange_t024_same_window_acceptance.py -q`：`414 passed in 8.88s`。
- Targeted delayed/marker/manager/quarantine/Task12 pytest：`35 passed, 379 deselected in 0.59s`。
- Combined four-file focused pytest：`537 passed in 27.73s`。
- `python -m pytest examples/hyperliquid -q`：`1040 passed in 47.96s`。
- Python compile 到 `/tmp/t035-qa-*.pyc`、implementation `git diff --check`：通过。
- Exact constants：producer/independent 均为 `4.0 / 0.5 / 5 / 5.0 / 1`。
- Inline hostile matrix：string/empty/bool unknown method fail-closed；list/dict method 抛 `TypeError`；malformed historical envelopes audit 错误通过。
- Task 12 probes：direct+nested-history blocked；malformed envelope rebuilt-summary case由 terminal-reference 层 blocked；container method 异常退出。
- T031 estimator replay至 `/tmp/0721T035-qa-t031-estimator.Im8ZPw`：exit `1`，exposure `6/6`、censor `0/2`、quarantine `1/0`、`snapshot_match=false`。
- T031 acceptance至 `/tmp/0721T035-qa-t031-acceptance.t8XtKT`：exit `2`，`112/112`、`72/72`、`43/43`、lifecycle `55 pass / 23 fail`。
- T026 acceptance至 `/tmp/0721T035-qa-t026-acceptance.UM4YvT`：exit `0`，lifecycle `78/78 pass`。
- T016/T022 acceptance：均 exit `2`，lifecycle 均为 `66 pass / 12 fail`。

验收结论：
- **未通过**
- 正常 raw historical trigger 和历史边界未回退，但 container method 可使验收崩溃，malformed historical envelope 也未按合同显式 fail-closed。

通过项：
1. Top-level/nested historical semantics 在合法 dict rows 中于 valid-count normalization 前触发。
2. Direct method+nested history、historical method+non-historical/missing result产生一致 mismatch。
3. Missing/bool/zero/list/dict attempt id 保留 protocol、count、snapshot/call-timing失败。
4. Marker-only、单 delayed field、canonical-only、attempt-only、重复 canonical 和 canonical/attempt 分歧均被阻断。
5. Consistent legacy direct-only v4 保持通过；non-dict rows不虚构 historical semantics且整体失败。
6. T033 timing、T034 marker、manager/quarantine、Task 12 independent comparison及历史边界未回退。
7. Implementation 仅修改两套 audit 和测试，未修改 live 行为路径。

不通过项：
1. List/dict `method` 使 producer、independent helper 和 Task 12 抛异常。
2. Historical status 搭配缺失或 malformed `orders` envelope 未产生 mismatch，audit 可返回 pass。

缺陷清单：
1. P1：raw method 未先验证为 string，集合成员判断不具备 total/fail-closed 性质。
2. P1：method/result coherence 只验证 status 标签，未验证 method-specific result envelope。
3. 现有回归缺少 container method 和 malformed historical envelope hostile cases。

阻塞项：
- T035 修复并通过新的独立 QA 前，不得启动 private read 或新的 live window。
- Adaptive、dynamic spread 和 multi-level activation继续锁定。

残余风险：
- 完整 `4s` delayed-history 路径仍未由修复后的新真实 live reference 验证。
- Task 12 当前可通过其他语义层阻断部分 malformed result，但不能依赖该偶然分层替代 audit 合同。

建议总控下一步：
1. 对 raw `method` 先做严格 string/schema validation，再进行集合成员判断。
2. Historical mismatch 检查应验证 result 为 dict、status exact、orders 为 list，并明确处理 malformed rows。
3. 增加 container method、missing/wrong-type orders、malformed history rows及 Task 12 端到端回归后重新 QA。

提交信息：
- commit：无
- QA 未修改仓库、未提交、未修改 `local_live_analysis`，未执行 live/private/account/order/cancel/network/remote/service 操作。
