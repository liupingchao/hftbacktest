# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0529T004

状态：
- 已通过

更新时间：
- 2026-05-30 23:27 CST

验收线程：
- QA验收线程

验收对象：
- 业务线程-python 0529T004

验收范围：
- 验收 Hyperliquid read-only public market-data evidence hardening 是否满足任务合同。
- 检查 fresh public sample 是否包含 `l2Book`、`trades`、subscription ack、session/connection/reconnect metadata、startup public Info `l2Book` recovery snapshot 和 raw sha256 provenance。
- 检查 alignment artifacts 是否完整，且分类是否达到 `passes_pricing_research_market_view`。
- 检查边界：不实现 private connector、order submit/cancel、fill lifecycle、strategy live logic、parameter search、default-on、tiny-live、promotion，也不引入 Binance `U/u/pu`、`lastUpdateId`、`bookTicker` 语义。

验收步骤：
1. 阅读 `.workflow/tasks/0529T004.md`。
2. 阅读 `.workflow/reports/0529T004-business.md`。
3. 检查 collector、alignment runner、focused tests 和 T004 artifacts。
4. 检查 `collection_manifest.json`、`recovery_snapshots.jsonl`、alignment `metrics.json` 和 required output file list。
5. 复跑 CLI help、py_compile、focused pytest、raw sha256、metrics JSON assertions。
6. 使用 committed fresh `raw.gz` 复跑 alignment 到 `/tmp/qa_0529T004_alignment`。
7. 检查 top-N sidecar header，确认未出现 Binance update-id/bookTicker 字段。
8. 核对提交范围：
   - `c79d7af` `Add Hyperliquid public sample evidence`
   - `c58594a` `Record 0529T004 business report`

实际结果：
- Collector 已实现：`examples/hyperliquid/hyperliquid_public_sample.py`。
- Focused tests 已实现：
  - `examples/hyperliquid/test_hyperliquid_public_sample.py`
  - `examples/hyperliquid/test_hyperliquid_raw_alignment.py`
- Required top-level sample artifacts 已生成：
  - `raw.gz`
  - `raw.sha256`
  - `collection_manifest.json`
  - `recovery_snapshots.jsonl`
- Required alignment artifacts 已生成：
  - `run_manifest.json`
  - `converter_manifest.json`
  - `data.npz`
  - `raw_provenance.csv`
  - `raw_to_npz_mapping.csv`
  - `topn_sidecar.csv`
  - `synthetic_joined_views.csv`
  - `metrics.json`
  - `acceptance_report.md`
- Fresh sample collection evidence:
  - requested duration `120.0s`
  - actual duration `120.100474382s`
  - coin `BTC`, network `mainnet`
  - `l2Book=222`
  - `trades=111`
  - `subscriptionResponse=2`
  - subscription ack by channel: `l2Book=1`, `trades=1`
  - session id `hl-66959ecb09e941cab61b020ac1e06419`
  - connection attempts `1`
  - reconnect count `0`
  - close reason `duration_elapsed`
  - raw sha256 `137018ef937b3692a5de0c12ee009c4a93a0e6d62ff15321061c377fc514389c`
- Recovery snapshot evidence:
  - startup public Info `l2Book` snapshot count `1`
  - status `ok`
  - best bid/ask `73681.0 / 73682.0`
  - bid/ask level count `20 / 20`
- Alignment metrics:
  - raw parse errors `0`
  - trade events `418`
  - `data.npz` rows `4279`
  - event-order validation `passed`
  - top-N coverage `1.0`
  - synthetic join coverage `1.0`
  - future joins `0`
  - missing joins `0`
  - join-age p99 `1114.135914ms`
  - l2Book cadence p99 `1317.185171ms`
  - final classification `passes_pricing_research_market_view`
- QA 复跑命令结果：
  - `python examples/hyperliquid/hyperliquid_public_sample.py --help` 通过。
  - `python examples/hyperliquid/hyperliquid_raw_alignment.py --help` 通过。
  - `python -m py_compile py-hftbacktest/hftbacktest/data/utils/hyperliquid.py examples/hyperliquid/hyperliquid_public_sample.py examples/hyperliquid/hyperliquid_raw_alignment.py` 通过。
  - `python -m pytest examples/hyperliquid/test_hyperliquid_raw_alignment.py examples/hyperliquid/test_hyperliquid_public_sample.py -q` 通过，`6 passed`。
  - `sha256sum local_live_analysis/hyperliquid_public_sample_0529T004/raw.gz` 与 `raw.sha256` / manifest 一致。
  - JSON assertion 检查通过：classification、parse errors、message counts、npz row count、top-N coverage、join coverage、future/missing joins、event-order validation、subscription/recovery/session/private/order boundary fields 均符合合同。
  - alignment 复跑到 `/tmp/qa_0529T004_alignment` 通过，输出 `classification=passes_pricing_research_market_view`、`l2Book=222`、`trade_events=418`、`npz_rows=4279`、`join_coverage=1.000000`。
  - `git diff --check` 通过。
- top-N sidecar header 为 Hyperliquid 字段：`raw_seq,line_no,channel,coin,local_ts,event_ts,bid_topn_px,...,session_id,connection_attempt,recovery_crossed`，未发现 Binance `U/u/pu`、`lastUpdateId`、`bookTicker` 字段。
- Manifest 明确记录：
  - `no_private_keys=true`
  - `no_private_account_endpoints=true`
  - `no_order_endpoints=true`
  - `no_strategy_process=true`
  - `no_remote_deploy=true`
- QA 注意到本地 `websockets` 包不可用，业务线程未安装新依赖，而是使用已存在的 `websocket-client` fallback 并在 manifest 中显式记录；这未扩大到 Rust collector 或依赖安装，按当前任务的 public-only 证据目标接受。
- 当前工作树中存在不属于 T004 的未提交 T005/Binance 改动；QA 未触碰、未纳入本次验收。

验收结论：
- 已通过
- 结论说明：
  - `0529T004` 满足 fresh Hyperliquid public market-data evidence hardening 验收合同：采集、session/subscription/recovery provenance、raw-to-npz、top-N sidecar、synthetic as-of join 和 final classification 均通过；结果仅证明 public market-data market-view research readiness，不授权 private/order/strategy/live/promotion。

通过项：
1. Collector CLI、focused tests、fresh raw artifacts 和 manifest/recovery evidence 完整。
2. Fresh raw gzip 同时包含 `l2Book` 和 `trades` public messages，并有 subscription ack 证据。
3. Session id、connection attempt count、reconnect count、start/end timestamps、raw sha256 和 close reason 已记录。
4. Startup public Info `l2Book` recovery snapshot 已记录且状态为 `ok`。
5. Converter 生成非空 `data.npz`，event-order validation 通过。
6. Top-N sidecar / provenance 未使用 Binance update-id 或 bookTicker 语义。
7. Synthetic as-of join coverage `1.0`，future/missing joins 为 `0/0`。
8. Final classification 为 `passes_pricing_research_market_view`。

不通过项：
1. 无

缺陷清单：
1. 无

阻塞项：
- 无

建议总控下一步：
1. 可将 `0529T004` 标记为 `已通过`。
2. 若继续 Hyperliquid 方向，下一步应仍保持 read-only/public market-data consumer 或 planning/design 边界；不得直接进入 private connector、order lifecycle、strategy live、parameter search、default-on、tiny-live 或 promotion。
3. Binance `0529T005` 与 T004 独立；T004 QA 结论不应混入 Binance Stage 9L 实现。

提交信息：
- business/artifact commit：`c79d7af` `Add Hyperliquid public sample evidence`
- business report commit：`c58594a` `Record 0529T004 business report`
