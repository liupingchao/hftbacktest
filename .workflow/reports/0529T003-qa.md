# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0529T003

状态：
- 已通过

更新时间：
- 2026-05-29 16:22 CST

验收线程：
- QA验收线程

验收对象：
- 业务线程-python 0529T003

验收范围：
- 验收 `0529T003` 是否按 Hyperliquid read-only market-data alignment 边界完成本地 raw sample / converter / top-N sidecar / synthetic join 证据链。
- 检查是否明确区分 `l2Book` 与 `trades`，并复用现有 Hyperliquid converter 产出非空 `data.npz`。
- 检查 top-N sidecar 和 raw provenance 是否避免 Binance `U/u/pu`、`lastUpdateId`、`bookTicker` 假设。
- 检查分类是否如实反映本地样本缺少 subscription/session/recovery 证据，而不是过度声明 fresh market-view 通过。
- 检查是否未越界到 Hyperliquid private connector、order lifecycle、strategy live、parameter search、default-on、tiny-live 或 promotion。

验收步骤：
1. 阅读 `.workflow/tasks/0529T003.md`。
2. 阅读 `.workflow/reports/0529T003-business.md`。
3. 检查 `local_live_analysis/hyperliquid_raw_alignment_0529T003/metrics.json`、`run_manifest.json`、`collection_manifest.json`、`acceptance_report.md`。
4. 检查 T003 实现提交范围：
   - `8dfff8b` `Add Hyperliquid raw alignment artifacts`
   - `ff1ca4b` `Record 0529T003 business report`
5. 复跑：
   - `python examples/hyperliquid/hyperliquid_raw_alignment.py --help`
   - `python -m pytest examples/hyperliquid/test_hyperliquid_raw_alignment.py -q`
   - `python -m py_compile py-hftbacktest/hftbacktest/data/utils/hyperliquid.py`
   - `python examples/hyperliquid/hyperliquid_raw_alignment.py --input-gzip examples/hyperliquid/btcusd_20250126.gz --output-dir /tmp/qa_0529T003_alignment --source-label qa_existing_local_sample`
6. 检查 top-N sidecar header 和 CSV 行数，确认生成物存在且字段为 Hyperliquid sidecar 语义。
7. 检查 business report 中对 dashboard skip 的解释：当时工作区存在 unrelated parallel `0529T002` task file，跳过 dashboard 生成是为了避免任务混入；QA 不将其作为 T003 内容不通过项。
8. 当前工作区已无 unrelated parallel T002 未跟踪文件后，运行 `python3 .workflow/build_dashboard.py` 回写 dashboard / dispatch。

实际结果：
- 业务报告存在并结束在 `待验收`。
- 任务文件、runner、focused tests、artifact directory、business report 均存在。
- 产物完整：
  - `run_manifest.json`
  - `collection_manifest.json`
  - `converter_manifest.json`
  - `data.npz`
  - `raw_provenance.csv`
  - `raw_to_npz_mapping.csv`
  - `topn_sidecar.csv`
  - `synthetic_joined_views.csv`
  - `metrics.json`
  - `acceptance_report.md`
- `metrics.json` 关键指标：
  - raw messages: `55`
  - `l2Book` messages: `46`
  - `trades` messages: `9`
  - trade events: `64`
  - converted npz rows: `413`
  - raw parse errors: `0`
  - top-N coverage: `1.0`
  - synthetic join coverage: `1.0`
  - future joins: `0`
  - missing joins: `0`
  - event-order validation: `passed`
  - sample classification: `limited_pricing_research`
- `collection_manifest.json` 正确记录本次是 `existing_local_sample`，并如实保留 `subscription_ack_count=0`、`recovery_snapshot_count=0`、空 session/connection fields。
- `topn_sidecar.csv` header 为 `raw_seq,line_no,channel,coin,local_ts,event_ts,bid_topn_px,bid_topn_ticks,bid_topn_qtys,bid_topn_n,ask_topn_px,ask_topn_ticks,ask_topn_qtys,ask_topn_n,session_id,connection_attempt,recovery_crossed`，未出现 Binance `U/u/pu`、`lastUpdateId`、`bookTicker` 字段。
- CSV 行数符合样本规模：
  - `raw_provenance.csv`: `56` 行，含 header
  - `raw_to_npz_mapping.csv`: `56` 行，含 header
  - `topn_sidecar.csv`: `47` 行，含 header
  - `synthetic_joined_views.csv`: `52` 行，含 header
- QA 复跑命令结果：
  - `python examples/hyperliquid/hyperliquid_raw_alignment.py --help` 通过。
  - `python -m pytest examples/hyperliquid/test_hyperliquid_raw_alignment.py -q` 通过，`3 passed`。
  - `python -m py_compile py-hftbacktest/hftbacktest/data/utils/hyperliquid.py` 通过。
  - runner 复跑到 `/tmp/qa_0529T003_alignment` 通过，输出 `classification=limited_pricing_research`、`l2Book=46`、`trade_events=64`、`npz_rows=413`、`join_coverage=1.000000`。
- QA 重建 dashboard 通过：`python3 .workflow/build_dashboard.py` loaded `75` tasks and `147` reports, wrote `.workflow/dashboard.html` and `.workflow/dispatch_suggestions.md`。
- 未发现策略行为、Binance strategy/acceptance rule、Hyperliquid private connector、order submit/cancel、fill lifecycle、live trading、remote deploy、parameter search、default-on、guard relaxation、tiny-live 或 promotion 改动。

验收结论：
- 已通过
- 结论说明：
  - `0529T003` 按合同完成了 Hyperliquid 第一阶段本地只读 market-data alignment 证据链：raw parse、converter、npz、raw provenance、raw-to-npz mapping、top-N sidecar、synthetic as-of join 和 classification 均可复现。
  - 任务正确将本地样本分类为 `limited_pricing_research`，没有把缺失 subscription/session/recovery evidence 的样本过度声明为 `passes_pricing_research_market_view`。
  - 后续已通过 `0529T004` 收敛到 fresh read-only public sample / collection manifest / recovery snapshot 证据补全方向。

通过项：
1. Runner 可从 CLI 复现，并可在无 private credentials 的情况下处理现有本地 Hyperliquid raw gzip。
2. `l2Book` 和 `trades` 被分别解析、计数和验证。
3. 现有 Hyperliquid converter 产出非空 `data.npz`，且 event-order validation 通过。
4. raw provenance、raw-to-npz mapping、top-N sidecar、synthetic join、metrics 和 acceptance report 均存在。
5. Hyperliquid top-N sidecar 使用 `l2Book` snapshot 中的 px/size/`n` 信息，没有继承 Binance diff-depth continuity 或 bookTicker 假设。
6. Classification 与证据强度一致：本地样本足够支持 `limited_pricing_research`，但不足以支持 fresh market-view session/recovery 通过。
7. 业务报告明确记录未做 private connector、order lifecycle、strategy live、parameter search、default-on、tiny-live 或 promotion。

不通过项：
1. 无

缺陷清单：
1. 无

残余风险：
- 本任务使用既有本地样本，不包含 subscription ack、session id、connection attempt、reconnect/recovery snapshot 证据，因此不能作为 Hyperliquid fresh public collection 完整验收。
- `run_manifest.json` 中 `git_commit` 记录的是生成 artifacts 时的前置 HEAD；业务报告和 git history 已记录实际实现提交 `8dfff8b` 与报告提交 `ff1ca4b`，不影响 artifacts 内容验收。
- `python3 .workflow/build_dashboard.py` 未在业务线程执行；QA 接受其原因是当时存在 unrelated parallel `0529T002` task file，重跑会污染 T003 dashboard/dispatch 输出边界。

建议总控下一步：
1. 可将 `0529T003` 标记为 `已通过`。
2. 后续执行 `0529T004`：fresh read-only public `l2Book + trades` sample collection，补齐 subscription ack、session/connection attempt、reconnect count、startup/reconnect public Info `l2Book` recovery snapshot 和 raw sha256 证据。
3. 在 `0529T004` 通过前，不要进入 Hyperliquid private connector、order lifecycle、strategy port、live trading、parameter search、default-on、tiny-live 或 promotion。

提交信息：
- business/artifact commit：`8dfff8b` `Add Hyperliquid raw alignment artifacts`
- business report commit：`ff1ca4b` `Record 0529T003 business report`
- QA report commit：待提交
