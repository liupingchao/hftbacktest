# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0805T003

状态：
- 已通过

更新时间：
- 2026-08-05 23:59 CST（星期三）

验收线程：
- QA验收线程

验收对象：
- 业务线程-python/research-postprocess + cross-exchange-signals + 0805T003

验收范围：
- point-in-time basis/dislocation state、strict as-of、reconnect epoch
  isolation、exact masks、trailing-only features 和 deterministic output。
- R0/R1 provenance admission、build-duration immutability、pipeline cache、
  report capability boundaries 和 Aug05 amdserver evidence。
- `basis-research` profile 与 repo-scoped Skill。

验收步骤：
1. 审查 basis builder、pipeline/profile/report、tests、文档和 Skill。
2. 执行 artifact descriptor、R0/R1 binding、input mutation、reconnect、
   epoch-history、as-of、mask、identity 和 determinism hostile tests。
3. 在 amdserver 独立核对 accepted R0 descriptor、双构建、pipeline
   resume、validator 和 source/R0/R1 immutability。
4. 检查 lead-lag、maker 和 hierarchy capability boundary。

实际结果：
- 第一轮 QA 发现 consumed R0 hot artifacts 未校验 manifest SHA/row
  count 的 P1；业务线程已修复并重新生成全部 AMD 证据。
- mask、Binance hot、Hyperliquid hot 的缺 SHA、缺 row count、SHA
  mismatch、row-count mismatch 共 `12` 种 hostile case 全部 fail closed。
- 构建期间修改 R0 manifest、R1 manifest、mask 或任一 hot artifact
  全部 fail closed。
- R1 未绑定当前 R0 manifest 时拒绝构建。
- recovery 同 timestamp 的旧 epoch BBO 被抑制；缺 higher-epoch BBO
  时拒绝；Aug05 抑制旧 epoch BBO `3` 条，old-state leaks `0`。
- 100ms change 不跨 state group；rolling median/MAD/z 按 group；
  reconnect 后第 899 秒不可用，第 900 秒才满足 15min warmup。
- strict as-of、同 timestamp 顺序、inclusive masks、spread identity、
  crossed-book 和 epoch-regression gates 均通过。
- 第二轮 QA 聚焦测试 `19` 项通过，扩大相关回归 `98` 项通过。
- 两次 Aug05 isolated builds 的 `6/6` 文件 byte-identical，inventory
  SHA 均为
  `ad424495322006063ae43199e911d40167c67bed4a435909dda8ce8431319f77`。
- R0 descriptor 对账：mask `6` 行、Binance hot `3,907,048` 行、
  Hyperliquid hot `206,989` 行，SHA 与 row count 全部匹配。
- 最终 pipeline validator `passes=true`、`failures=[]`、`5` stages；
  resume 复用 `5/5` stages。
- basis 输出 `2,641,172` 行，其中 feature eligible `2,250,643` 行；
  future join、crossed book、epoch regression 和 old-state leak 均为 `0`。
- source campaign、accepted R0、accepted R1 inventories 保持不变。
- `basis-research` 可执行；lead-lag、maker diagnostics 和 hierarchy
  均未被误报为完成。

验收结论：
- 已通过
- 结论说明：
  - 第一轮 P1 provenance 缺口与跨 epoch 派生历史风险均已关闭；
    point-in-time basis stage 满足可执行、可恢复、可审计和来源只读要求。

通过项：
1. strict receipt-time as-of 和 deterministic same-timestamp ordering。
2. reconnect reset、higher-epoch recovery proof 和 old-state isolation。
3. epoch-aware change、rolling state 和 feature warmup。
4. exact inclusive masks、source age 和 spread identity。
5. R0 descriptor SHA/row-count admission 与 R1 binding。
6. build-duration input immutability 和 deterministic gzip。
7. `basis-research` pipeline cache、resume、validator 和 reports。
8. Skill 与 capability boundaries。

不通过项：
1. 无。

缺陷清单：
1. P0：无。
2. P1：无。
3. P2：无。
4. P3：mask descriptor 缺 `sha256` 时会抛出原生 `KeyError`，而不是
   统一 `BasisDislocationError`；仍 fail closed，不阻塞本次验收。

阻塞项：
- 无。

建议总控下一步：
1. 关闭 `0805T003`。
2. 下一正式任务迁移 `lead_lag`，输入只允许使用本任务发布的 canonical
   basis state。
3. lead-lag 验收后再迁移 `maker_diagnostics`。
4. Atom/Episode/Motif/Regime 继续保持 deferred，除非出现新的 alpha
   证据。
5. 后续小修可统一 mask descriptor 缺字段时的错误类型。

提交信息：
- commit：无
