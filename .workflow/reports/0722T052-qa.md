# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0722T052

状态：
- 已通过

更新时间：
- 2026-07-22 09:54 Asia/Shanghai

验收线程：
- QA验收线程

验收对象：
- Workflow commit `287164222ad5120f0af45d7bcd14cca0c6724649`
- Runtime source commit `e423b442f549935f6a181160ba348feec07fe0c8`
- Evidence root
  `local_live_analysis/principal_alignment_bounded_fill_feedback_0722T052/`

验收范围：
- 单窗口 exact envelope、source/checksum、进程和账户终态、逐 reference
  lifecycle/cancel reconciliation、estimator/fill-feedback replay、same-window
  mechanism/evidence acceptance，以及 unsupported claims 和 multi-level 边界。

## Findings

### P0

- 无。

### P1

- 无。

### P2

- 无。

验收步骤：
1. 独立读取 preflight、run、account、lifecycle、reconciliation 和 terminal
   manifest 原始证据，并核对唯一 window 和 exact envelope。
2. 将 runtime provenance 的 `63` 个文件逐项与 source commit Git blob
   比较，独立重算 terminal manifest 的文件集合和 SHA-256。
3. 将 estimator、fill-feedback replay 和 same-window acceptance 重放到
   `/tmp`，不覆盖 sealed evidence。

实际命令与结果：
- `git show` / `git cat-file` source identity 与逐文件 blob 校验：
  runtime source `63/63`，start/postrun verification 均 `63/63 pass`。
- 独立 terminal manifest 文件集合和 SHA-256 校验：`110/110 pass`，无
  missing、unexpected、duplicate、路径或 digest 错误。
- `python examples/hyperliquid/cross_exchange_online_estimators.py
  --replay-kind estimator ...`：exit `0`，event rows `5673`，confirmed
  exposure `4`，censor `1`，quarantine `0`，snapshot match `true`。
- `python examples/hyperliquid/cross_exchange_online_estimators.py
  --replay-kind fill-feedback ...`：exit `0`，lifecycle rows `2`，
  activation `true`，snapshot match `true`。
- `python examples/hyperliquid/cross_exchange_t024_same_window_acceptance.py
  ... --expected-task-id 0722T052 --expected-source-commit
  e423b442f549935f6a181160ba348feec07fe0c8
  --expected-window-seconds 1800 --expect-fill-feedback-enabled`：exit `0`；
  provenance `113/113`、config `83/83`、decision `43/43`、lifecycle
  `78/78`、economics `6/6`、optimism `6/6`。
- `git diff --check 2871642^ 2871642`：通过。
- 本次 QA 未执行任何 live、private、account、network、remote、service、
  order 或 cancel 操作。

实际结果：
- 证据只包含一个 live window。Envelope 为 `0.005 BTC/order`、`0.01 BTC`
  position、`1 USDC` loss、`2` submissions、`1800s`；profile 为
  `two-sided-fill-feedback-manager`，lead source 为
  `binance_public_book_ticker`。Dynamic spread、inventory skew 和
  multi-level 均关闭。
- Child return code `0`，已 reaped，未请求 termination，无 SIGKILL。
  Pre/post open orders 均 `0`，BTC position 均 `0.0`；kill-switch clear，
  service inactive，conflicting process count `0`。
- Attempt 1 buy 由交易所 authoritative post-only reject；attempt 2 sell
  进入 resting 后通过 authoritative oid cancel。两个 submitted reference
  均独立 terminal proven，per-reference reconciliation 和 shutdown proof
  均 `pass`。
- `cancel_retry_used=false`，本窗口只记录一次 `identity_kind=oid` cancel。
  因此 T051 exact-cloid fallback 仍只有离线 action-path 证据，本任务不提供
  该 retry 的 live action-path proof。
- Fill-feedback 已启用但 target 未配置，candidate 为
  `unavailable_neutral`，fallback fixed，实际 quote behavior 未改变。
- Final recommendation 为
  `principal_task12_mechanism_and_evidence_integrity_passed`。

验收结论：
- 已通过。
- T052 满足单层双边 manager 的 source、execution-safety、account、
  evidence-integrity、replay 和 same-window mechanism gate，可关闭本任务
  的 Principal Task 12 机制/证据完整性验收边界。

通过项：
1. Exact one-window envelope、Binance lead 和 adaptive feature-off 边界成立。
2. Source `63/63`、terminal checksum `110/110`、进程和账户终态成立。
3. 两个 submitted reference 均有 authoritative、精确绑定的 terminal proof。
4. Estimator/fill-feedback replay snapshot exact match。
5. Same-window 六组 acceptance matrix 全部通过。

不通过项：
- 无。

缺陷清单：
- 无。

阻塞项：
- 无。

Unsupported claims：
- 本窗口 `0 fills`，不支持 stable PnL、fill rate、fee/rebate、queue
  priority、maker viability、promotion 或 final MVP 结论。
- Exact-cloid retry 未发生，不支持该 fallback 已获 live action-path proof。
- Multi-level 未启用，也未因 T052 自动解锁。

建议总控下一步：
1. 将 `0722T052` 关闭为已通过的单层、双边、fill-feedback neutral-fallback
   mechanism/evidence baseline。
2. 保持经济性、exact-cloid retry live proof 和 multi-level 边界锁定；下一
   formal task 由总控另行派发，不从本窗口外推。

提交信息：
- QA commit：无。
- Workflow commit：`287164222ad5120f0af45d7bcd14cca0c6724649`
- Runtime source commit：`e423b442f549935f6a181160ba348feec07fe0c8`
