# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0729T005

状态：
- 未通过

更新时间：
- 2026-07-29 16:18 CST

验收线程：
- QA验收线程

验收对象：
- 业务线程-remote-public-data + 0729T005

验收范围：
- T004 修复源码部署身份、60 秒 SKHYNIX canary、严格 canary gate 与
  30 分钟启动边界。

验收步骤：
1. 核对本地和远端关键源码 SHA256。
2. 检查 transient systemd 终态、两个 child return code 和日志。
3. 检查 Binance bootstrap manifest、Hyperliquid fast-L2 ACK 和本地拉回产物。

实际结果：
- 修复源码哈希在本地与远端完全一致。
- Hyperliquid 运行满 60 秒，fast L2 `112`、trades `334`、ACK 完整、
  reconnect/disconnect `0`。
- Binance 四次连接均取得有效 snapshot，但 snapshot 均落后于第一条
  buffered diff，bridge count `0`，`depth_replay_ready=false`。
- Binance child return code `1`，父 unit `ExecMainStatus=2`。
- 业务线程遵守停止条件，没有启动 30 分钟窗口。

验收结论：
- 未通过
- 结论说明：
  - 部署和 canary gate 行为正确，但 Binance live bootstrap 无法在
    snapshot 过旧时保留当前 buffer 并重新获取 snapshot，目标未完成。

通过项：
1. 远端运行源码身份准确。
2. Hyperliquid 高频 L2 采集和 ACK 通过。
3. Canary fail-closed，30 分钟窗口未越过失败门禁。
4. 失败证据和产物已拉回。

不通过项：
1. Binance snapshot bridge 未建立。
2. 60 秒双交易所 canary 未通过。
3. 30 分钟验证未执行。

缺陷清单：
1. P1：snapshot 过旧时错误地重连 WebSocket；应在同一 buffered stream
   上重新执行 REST snapshot 步骤。

阻塞项：
- 需要先完成同连接 snapshot refresh 修复及离线回归。

建议总控下一步：
1. 派发独立 offline repair 任务。
2. 修复通过后使用新的 live validation 任务重新执行 60 秒 gate。

提交信息：
- commit：无
