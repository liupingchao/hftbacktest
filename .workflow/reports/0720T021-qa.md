# QA 验收结果

## Findings

- **未发现 P0、P1 或 P2 缺陷。**
- T020 指出的 position row identity 与 boolean numeric parsing 问题已关闭。
- 未发现 strategy、pricing、quote、risk cap、order lifecycle、activation、multi-level、spread 或 inventory skew 漂移。

执行线程：
- 独立只读 QA 验收线程

任务ID：
- `0720T021`

状态：
- **已通过**

更新时间：
- `2026-07-20 12:43 CST`

验收对象：
- implementation commit `fa1095af12415d46dda80894e78a5e7f266281b4`
- business report commit `d24cee82fb8cf5a6eccc1ab5a53916c9babe61ff`

验收范围：
- Position parser identity validation、numeric parsing、manager/finalizer fail-closed 状态。
- T017-T020 回归、T016 monotonic replay及策略边界。
- 未执行 live、private/account endpoint、真实 order/cancel、network、remote 或 service 操作。

关键命令与结果：
- Focused executor/manager/watcher/fill/acceptance：
  - `451 passed in 16.51s`
- Full Hyperliquid：
  - `python -m pytest -q -p no:cacheprovider examples/hyperliquid`
  - `790 passed in 36.67s`
- T017-T020 状态机与证据定向回归：
  - `81 passed, 195 deselected`
- Position parser 独立对抗矩阵：
  - `20` 个 invalid、`8` 个 valid，全部通过。
- Manager/finalizer 独立端到端矩阵：
  - `12` 个 invalid、`5` 个 valid，全部通过。
- 修改文件 `py_compile`：通过。
- Acceptance CLI `--help`：通过。
- `git show --check fa1095af...`、`git diff --check`：通过。
- 验收前后 dirty worktree内容一致；未编辑、暂存、提交或清理文件。

通过项：
1. Identity validation发生在 symbol filter之前。
2. Missing、empty、whitespace、non-string和boolean coin均fail closed。
3. Direct/nested coin conflict fail closed；相同identity正确接受。
4. Boolean、missing、None、NaN、Inf `szi`和duplicate BTC均fail closed。
5. Valid empty、明确foreign-only、direct/nested BTC及finite numeric/string数量正确解析。
6. Invalid position在final status中显示unknown，并产生`final_position_snapshot_unavailable`。
7. Position失效时，2个已知open orders、`0.01 BTC` working exposure和cancel audit保持可见。
8. T020 semantic status propagation及T017-T019行为未回归。

T016 replay：
- Offline acceptance exit code：`2`，符合历史证据仍被阻塞的预期。
- Decision：`43 pass`。
- Lifecycle：`49 pass / 12 fail`。
- Provenance：`112 pass`；config：`72 pass`。
- Attempt 2仍为`cancel_or_full_fill_terminal_proof_missing`，未被升级或掩盖。
- 输入共`118`个文件；回放前后hash均为：
  - `a5443f8f9cb777509ffde21f9caed3837a9ef58f31b25cd82acef2d6a3991398`
- Replay boundary确认`offline_only=true`；network/private/order/cancel/remote/new-live全部为`false`。
- Task 12仍为`principal_task12_same_window_acceptance_blocked`，multi-level activation仍锁定。

验收结论：
- **已通过**
- T021满足position evidence parser、manager/finalizer evidence-completeness及回归要求。

阻塞项：
- T021无阻塞。
- T016历史attempt 2 terminal proof仍阻塞Task 12，但不构成T021验收失败。

建议总控下一步：
1. 将`0720T021`更新为`已通过`并复制本报告。
2. 可派发一个新的保守bounded-live任务，用新代码收集双reference terminal proof。
3. 在新lifecycle证据通过前，继续锁定Task 8及adaptive/multi-level activation。

提交信息：
- commit：无；本次为独立只读验收。
