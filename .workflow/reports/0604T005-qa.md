# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0604T005

状态：
- 已通过

更新时间：
- 2026-06-04 09:56 CST

验收线程：
- QA验收线程

验收对象：
- 业务线程-research + 0604T005

验收范围：
- `0604T005` 规定的 read-only canonical evidence source lock / guard hardening、focused tests、task-scoped source-lock artifacts，以及对 `0604T003` canonical source / synthetic diagnostic comparison 的真实输入复验。

验收步骤：
1. 阅读 `.workflow/tasks/0604T005.md` 与 `.workflow/reports/0604T005-business.md`，核对任务边界、验收标准、业务结论与提交信息。
2. 核对 commit `494eb58` 的改动范围，确认包含 task、business report、loader/test 修改和 task-scoped artifacts。
3. 复跑最小验证命令：`--help`、`py_compile`、focused pytest。
4. 对 `local_live_analysis/event_mode_canonical_pricing_signal_0604T003/` 运行 source-lock guard 到 `/tmp/qa_0604T005_source_lock/`，并同时传入 synthetic diagnostic comparison 做 negative validation。
5. 解析 `/tmp/qa_0604T005_source_lock/` manifest / CSV，核对 canonical sample count、diagnostic rejection count、source-lock metadata、negative validation rows 和 boundary flags。
6. 运行 `git diff --check`，确认无格式问题。

实际结果：
- `git show --stat --oneline 494eb58` 确认提交为 `0604T005 canonical evidence source lock guard`，包含 task、business report、`canonical_event_mode_evidence.py`、focused tests 和 source-lock artifacts。
- `python examples/hyperliquid/canonical_event_mode_evidence.py --help` 通过。
- `python -m py_compile examples/hyperliquid/canonical_event_mode_evidence.py examples/hyperliquid/test_canonical_event_mode_evidence.py` 通过。
- `python -m pytest examples/hyperliquid/test_canonical_event_mode_evidence.py` 通过，结果为 `10 passed`。
- QA 复跑 source-lock 输出 `/tmp/qa_0604T005_source_lock/`，结果 `canonical_sample_count=3`、`diagnostic_rejection_count=0`、`guard_status=accepted_formal_canonical_evidence`。
- QA 复跑 manifest 明确记录 `formal_evidence_task_id=0604T003`、`foundation_task_id=0604T004`、`no_formal_synthetic_fixed_grid_evidence=true`。
- QA 复跑 negative validation 报告包含两行：formal evidence 使用 synthetic diagnostic source 被 `rejected_formal_evidence`，显式 negative validation 为 `diagnostic_only_validation`；两行均为 `canonical_sample_count=0`、`diagnostic_rejection_count=3`。
- `python -m json.tool /tmp/qa_0604T005_source_lock/canonical_source_lock_manifest.json` 通过。
- `git diff --check` 通过。

验收结论：
- 已通过
- 结论说明：
  - `0604T005` 达成了 narrow read-only source-lock / guard hardening：formal evidence 被锁定到 `0604T003` canonical event-mode artifacts 和 `0604T004` foundation，synthetic fixed-grid diagnostics 不能作为 formal evidence 进入后续 ranking / horizon / regime worker。

通过项：
1. Guard 接受 `0604T003` canonical event-mode aggregate，且 canonical sample count 精确为 `3`。
2. Guard 对 synthetic diagnostic comparison 的 formal-evidence 使用会拒绝；negative validation 路径能稳定报告 `canonical_sample_count=0`、`diagnostic_rejection_count=3`。
3. Source-lock manifest 明确记录 `0604T003` formal source、`0604T004` foundation、required `decision_mode=event`、required `canonical_status=canonical_event_mode` 和禁止 formal synthetic fixed-grid evidence 的边界 flag。
4. Focused tests 覆盖 accepted canonical source、rejected diagnostic source、missing source-lock metadata、zero-canonical formal-evidence failure、downstream-worker-style guard call。
5. 边界保持正确：未引入 collection、signal ranking、regime selection、strategy/private/order/live/default-on/tiny-live/parameter search/promotion。

不通过项：
1. 无

缺陷清单：
1. 无

阻塞项：
- 无

建议总控下一步：
1. 将 `0604T005` 记为 `已通过`，后续 `0604T006` ranking 与 `0604T007` horizon/regime diagnostics 应继续使用该 source-lock guard。
2. 后续 QA `0604T006` / `0604T007` 时，应检查它们是否拒绝 diagnostic-only synthetic inputs，并且不把 fixed-grid diagnostics 当作 formal evidence。

提交信息：
- commit：494eb58
