# QA 验收结果

## Findings

1. **P1：语义无效的 final user-state 未传播 position fail-closed 状态。**
   - [hyperliquid_tiny_live_m2_public_watcher.py](/home/molly/project/hftbacktest/examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py:7941) 只要 `dict(user_state)` 不抛异常，就将 `post_state_snapshot_valid` 设为 `True`。
   - `{}`、`{"assetPositions": None}`、`{"assetPositions": {}}` 和 `[]` 均可通过该检查。
   - Manager 会正确返回 `position_snapshot_status=fail_closed`，但 watcher 在 [line 8000](/home/molly/project/hftbacktest/examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py:8000) 忽略返回值，仅依据外层布尔值决定是否增加 `final_position_snapshot_unavailable`。
   - 对抗探针中四种无效形状均产生：
     - `owned_open_order_count=2`
     - working exposure `0.01 BTC`
     - 订单状态 `resting`
     - operator-facing `position_btc=0.0`
     - **缺少** `final_position_snapshot_unavailable`
   - 因此旧值或默认零值无法与 exchange-verified 零仓位区分，不满足“position uncertainty 显式 fail-closed，不能伪造零仓位”。
   - 无 P0、无其他 P1、无 P2。

执行线程：
- 独立只读 QA 验收线程

任务ID：
- `0720T019`

状态：
- **未通过**

更新时间：
- `2026-07-20 12:06 CST`

验收对象：
- implementation commit `b0115d7cfe09f4612f68581481215f79026f0bfc`
- business report commit `6e63fd86d9cc341438f75c38887e52d4178c2ef6`

验收范围：
- Final open-order/position snapshot 解耦、cancel-pending 重现、T017/T018 回归、T016 monotonic replay及策略漂移。
- 未执行 live、private/account endpoint、order/cancel、network、remote 或 service 操作。

关键命令与结果：
- Focused manager/watcher/fill/acceptance：`365 passed in 16.15s`。
- Full Hyperliquid：`758 passed, 1 skipped in 36.15s`；skip 为隔离 clone 缺少 gitignored 0713 evidence，映射只读 fixture 后对应文件 `3 passed`。
- Malformed/query-filled/v2-v3/foreign/cardinality targeted：`30 passed`。
- `py_compile`、acceptance `--help`、`git show --check`、`git diff --check`：通过。
- T016 replay：预期 exit `2`；decision `43 pass`，lifecycle `49 pass / 12 fail`，Task 12 保持 blocked。
- T016 输入 `118` 个文件，回放前后 hash 均为 `62da666bbb575b01b61aecdde594d994973ddaf50df7342321e24c4f76ca88b8`。

通过项：
1. Valid final open-order rows 能独立恢复 owned count 和 working exposure。
2. Supplied final row 能将 `cancel_requested` 恢复为 `resting/partial_fill`，并保留 `cancel_requested_at_ms`。
3. 普通实时 reconcile 仍保留 `cancel_requested` 语义。
4. Empty/mixed/foreign snapshots、malformed status、classifier exception、query-filled unresolved 均保持保守行为。
5. T017 v2/v3、submitted cardinality 和 T016 历史 blocker 未回归。
6. 无 pricing、signal、risk cap、activation、multi-level 或 quote-policy 漂移。

验收结论：
- **未通过**
- 订单状态修复成立，但 final position evidence 对语义无效 user-state 仍可能静默显示默认/旧仓位值，T019 尚未满足 evidence-completeness gate。

建议总控下一步：
1. 依据 `reconcile_supplied_snapshot()` 返回的 `position_snapshot_status` 增加 blocker，不要仅依赖 `dict(...)` 是否成功。
2. 在最终 status 中显式输出 position snapshot status/reason，或将未验证仓位标记为 unknown。
3. 增加 `{}`、`assetPositions=None/object`、空列表以及 valid-empty-open-orders 的 watcher 回归测试。
4. 修复通过独立 QA 前，继续锁定 bounded live、Task 8 和 adaptive/multi-level activation。

提交信息：
- commit：无；本次只读验收未编辑或提交仓库文件。
