# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0719T002

状态：
- 未通过

更新时间：
- 2026-07-19 23:59 CST

验收线程：
- QA验收线程

验收对象：
- 总控 auto-loop / 业务实现线程 0719T002

验收范围：
- per-attempt/per-reference cancel proof、standalone/manager producer artifacts、same-window acceptance 独立性和 offline regression。

验收步骤：
1. 审阅任务、业务报告、prior `0719T001` QA、implementation commit `7235372bb2a2d6bde043a1fee3d4abc16bb5067d` 和 report commit `772aaa8`。
2. 独立构造跨 attempt、same-reference、unknown/missing/duplicate/cross-token 和 conflicting-token cancel evidence。
3. 检查 standalone multi-attempt、two-sided manager artifacts 及 acceptance 对 producer/cancel proof 的核验。
4. 运行 focused tests、`py_compile`、CLI `--help` 和 diff checks。

## Findings

1. P1：acceptance 没有从原始 cancel proof 独立重建 reference/cancel binding，可接受伪造 summary。
   - `cross_exchange_t024_same_window_acceptance.py:459-513` 信任 producer row 中的 `reference_key`、`matched_reference_key` 和 `authoritative_success`，没有按 attempt/oid/cloid 重算 key，也没有解析原始 exchange response。
   - `cross_exchange_t024_same_window_acceptance.py:639-644` 只比较 fill manifest 与 `cancel_shutdown_proof` 内复制的 reconciliation object 是否相等，没有核对 proof 的原始 `tracked_refs` 和 `cancel_results`。
   - QA 将 evidence row 改成 unrelated `attempt=2, oid=999, cloid=forged-target`，仅保留指向 attempt 1 的 `matched_reference_key`，并同步复制到 proof；acceptance 仍返回 mechanism pass。
   - QA 保留 pass summary，但把 proof 原始 cancel row 改成 ambiguous-only；acceptance 同样返回 mechanism pass。
2. P1：producer 的 target matcher 接受部分冲突 identity。
   - `hyperliquid_tiny_live_m2_fill_window.py:1858-1864` 使用任意 token 交集匹配。
   - 对 reference `attempt=1, oid=101, cloid=a`，cancel evidence `attempt=1, oid=101, cloid=unknown-cloid` 仍返回 `status=pass`。
   - 当 cancel row 同时携带 oid 和 cloid 时，所有已提供 token 应共同指向同一 reference；未知或冲突 token 不应被另一个正确 token 掩盖。

实际结果：
- 原 QA 反例 `attempt 1 authoritative success + attempt 2 ambiguous-only` 已返回 `fail_closed`。
- 两个 reference 分别由 oid/cloid success 证明时通过；same-reference success 后 redundant generic 仍通过。
- 完全 unknown target、missing target、duplicate reference 和两个已知 reference 间的 cross-token mapping 均 fail closed。
- Standalone multi-attempt artifact 保留 attempt 1/2 两个 refs，cancel rows 带 target identity，nominal reconciliation pass。
- Two-sided manager artifact 保留 attempt 1/2 两个 refs，每侧 cancel evidence 独立绑定，nominal reconciliation pass。
- Focused regression：`123 passed in 22.36s`。
- `py_compile`、三个 CLI `--help`、implementation commit diff check 和当前 diff check 通过。
- QA 未运行 full `examples/hyperliquid` suite：聚焦审阅已得到确定性 blocking reproduction；业务线程报告为 `476 passed`，本轮不重复宽泛测试。
- 未调用 live/private/account/order/cancel/network/remote/service。

验收结论：
- 未通过
- 结论说明：
  - 原跨 attempt any-success 缺陷已修复，但 producer malformed-target 边界和 downstream acceptance 仍可 fail-open，尚未满足独立、逐引用、不可伪造的证据 gate。

通过项：
1. 原 `0719T001` counterexample 已关闭。
2. nominal standalone multi-attempt 和 two-sided manager producer artifacts。
3. same-reference authoritative success 加 redundant generic 的兼容语义。
4. focused regression 和离线 compile/help/diff checks。

不通过项：
1. acceptance 未从 proof 原始 refs/cancel responses 独立重建并验证 reconciliation。
2. producer 未拒绝同时携带正确 token 与未知/冲突 token 的 cancel target。

缺陷清单：
1. P1：acceptance 可被同步伪造 manifest/proof summary 绕过。
2. P1：producer 使用 token OR matching，部分冲突 target 可被接受。
3. 缺少 forged-both-copies、raw-proof contradiction 和 partial-conflicting-token 回归。

阻塞项：
- 不得创建或启动新的 live task。
- Principal Task 12 仍未关闭。
- Task 10 multi-level 仍未解锁。

建议总控下一步：
1. 新 offline formal task 让 acceptance 从 `cancel_shutdown_proof.tracked_refs/cancel_results` 独立解析 authoritative response 并重建 deterministic reconciliation，再与两个 producer summary 比较。
2. Producer 对 cancel row 的全部非空 target tokens 要求共同唯一映射到同一 attempt/reference；任一未知或冲突 token fail closed。
3. 增加上述三个 adversarial regressions；focused 和 full offline tests 通过后重新独立 QA。

提交信息：
- implementation commit：`7235372bb2a2d6bde043a1fee3d4abc16bb5067d`
- report commit：`772aaa8`
