# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0801T006

状态：
- 已通过

更新时间：
- 2026-08-01 17:15 UTC

验收线程：
- QA验收线程

验收对象：
- 业务线程-python/cross-exchange-research / 0801T006 第三轮 QA

验收范围：
- 第二轮唯一 P1：existing-v2 calibration contract 冻结 discovery atom
  count 与 canonical row SHA。
- 第一、二轮 required-output、candidate drift、phase conservation hostile
  matrix 全量回归。
- 正式 rolling phase、discovery-only sensitivity、membership/output SHA、
  repeated build 和历史 v1 immutability。
- 不验收 baseline、motif、regime、held-out outcome 或策略结果。

验收步骤：
1. 重新阅读最新任务、业务报告、实现、测试和 Goal 2 freeze 合同。
2. 在真实八段副本中修改 discovery atom unused field，并合法更新 atom
   catalog/manifest SHA；监控 timeline loader、cluster/phase builder 和
   publisher 是否在拒绝前被调用。
3. 回归参数、算法标签、discovery split、calibration contract、required
   outputs、candidate outputs 和 phase conservation hostile matrix。
4. 独立全量复算正式 membership、rolling phase、discovery canonical SHA、
   sensitivity、output SHA 和 v1 hashes。
5. 修改 `segment_0004` atom 验证 discovery isolation，并连续执行两次真实
   existing-v2 rebuild 验证 byte determinism。
6. 运行 focused pytest、ruff、`py_compile`、CLI 和
   `git diff --check`。

实际结果：
- Focused tests: `22 passed`。
- ruff、`py_compile`、主 CLI help、`--stage episode-v2 --help` 和
  `git diff --check` 均通过。
- Discovery unused-field 注入：
  - 修改 `segment_0001-000001.basis_mid_bps`；
  - atom catalog 和 atom manifest SHA 合法同步更新；
  - builder 报错 `episode_boundary_v2 discovery input SHA drift`；
  - timeline loader、cluster builder、phase builder、publisher 调用次数均为
    `0`；
  - frozen v2、历史 v1 和 manifest SHA 全部不变；
  - `episode_v2.tmp` 不存在。
- Existing freeze 正常合同精确返回六项 outputs 和完整 calibration
  contract。
- 参数、算法标签、discovery split、calibration 缺失/额外字段、scope、
  count 类型、SHA 格式、outcome-read 和 held-out-calibration 漂移全部
  fail closed。
- Required output 缺失、额外、path、schema、row_count、SHA 和 record
  字段漂移全部 fail closed，validator 不修改 package。
- Candidate path、row_count、SHA 漂移全部 fail closed。
- 真实八段 phase-result drift 在发布前失败；frozen v2/v1 不变，无临时
  目录残留。
- 删除 `segment_0001-E000005` 最后一个 phase 后，builder 报错
  `v2 phases do not conserve membership`；旧 v2 不变。
- 正式全量复算：
  - atoms/membership `141,768`；
  - clusters `48,777`；
  - episodes `12,677`；
  - phases `26,428`；
  - boundary audit `48,769`；
  - sensitivity rows `13`；
  - rolling phase mismatch `0`；
  - two-atom confirmation error `0`。
- Discovery atom count 为 `60,820`，canonical SHA 为
  `bce395180f2773011d323a7647efa7513709cdf4190aa89c53572c2f4d220b2d`。
- 修改 `segment_0004` 后，discovery SHA 与 sensitivity SHA
  `311ac58111152c4900a0fb989a0caaaed607debae5a165a0a6040000cd97dd90`
  均不变。
- 正式 manifest SHA 为
  `be375c83fe3568c9d595037c615b6030798ee0635b565f08ea66bce6f4f53033`；
  所有 output SHA 与历史 v1 七个 SHA 均通过。
- 同一路径连续两次真实 rebuild 的全部 `episode_v2` 文件 byte-identical。

验收结论：
- 已通过
- 结论说明：
  - T006 的 rolling phase、discovery-only calibration、same-version
    calibration/output freeze、phase conservation 和 immutable publication
    合同均通过三轮独立 QA。

通过项：
1. 第二轮 discovery calibration input freeze P1 已关闭。
2. 第一轮 required-output 与 candidate-output freeze P1 均保持关闭。
3. 第一轮 phase conservation 测试缺口保持关闭。
4. 正式数据的 rolling phase、two-atom confirmation、membership、output
   SHA 和 discovery sensitivity 全量复算通过。
5. Held-out 结构变化不污染 discovery calibration。
6. 历史 v1 不变，重复构建确定，全部验证命令通过。

不通过项：
1. 无。

缺陷清单：
1. 无。
- P0：无。
- P1：无。
- P2：无。
- P3：无。

阻塞项：
- 无。

建议总控下一步：
1. 将 `0801T006` 标记为 `已通过`。
2. 解锁顺序修复任务 `0801T007`，继续保持 `0004-0008` 为
   post-selection/in-sample-adjusted，不得恢复正式 held-out 标签。

提交信息：
- commit：无
