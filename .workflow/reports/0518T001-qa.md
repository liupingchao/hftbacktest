# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0518T001

状态：
- 已通过

更新时间：
- 2026-05-18 14:36 CST

验收线程：
- QA验收线程

验收对象：
- 业务线程-python + 0518T001

验收范围：
- 验收 `0518T001` 是否按 repair-design-only 合同完成 conservative queue proxy gate 设计，是否清楚描述 `4948` case，是否明确当前不实施 replay queue/touch fill repair，以及是否避免把 top5 / trade proxy 写成 exact queue-position proof。

验收步骤：
1. 检查 `.workflow/tasks/0518T001.md` 与 `.workflow/reports/0518T001-business.md`。
2. 复核 `0516T001` / `0516T002` 结论是否被正确引用。
3. 检查设计合同是否覆盖 gate 输入字段、阈值、data-quality guard、gating semantics、failure modes、implementation prerequisites 和 non-goals。
4. 确认本任务没有修改 replay、策略、live collection、sample policy 或 generated sample artifacts。

实际结果：
- `0518T001` 保持了 design-only 边界：未实现 gate，未修改 replay fill model，未修改 queue / priority / touch fill 逻辑，未修改策略，未补样本，未启动 live。
- `4948` case 被清楚记录：
  - submit_key `28940|sell`
  - order_id `4948`
  - live canceled / replay filled
  - same-price trade qty `8.884`
  - submit visible qty `21.143`
  - replay-fill visible qty `15.633`
  - same-price qty / visible qty ratios `0.4202` and `0.5683`
  - order-at-touch share `1.0`
- 结论保持保守：
  - `4948` 是 queue-ahead proxy suspicious case，不是 hidden trigger unknown。
  - live no-fill 合理，因为 visible queue proxy 可吸收 observed same-price trade qty。
  - replay fill 可疑，因为 replay 可能把 touch-level trades 过度近似成我方订单可成交。
- gate 设计覆盖了输入字段、候选阈值、top1 visible qty decay / unexplained shrink 处理、latency / join-age / stale-data guards、diagnostic-first semantics、failure modes 和 false-negative risk。
- 业务回报明确当前不实施修复：`0516T002` 只证明 queue-ahead no-fill pattern 重复出现，但 replay-fill false-positive 仍只有 `4948` 一条。

验收结论：
- 已通过
- 结论说明：
  - `0518T001` 完成了 conservative queue proxy gate 的 repair-design-only 合同；它只授权后续诊断/验证方向，不授权当前实现 replay queue/touch fill repair。

通过项：
1. 明确描述了 `4948` 的 evidence package。
2. 给出了 conservative queue proxy gate 的设计边界、输入、阈值、guard、语义和风险。
3. 明确当前不实施修复，并要求更多 current-format samples / replay false-positive cases 后再考虑 implementation。
4. 没有越界修改 replay、策略、live 或样本。

不通过项：
1. 无

缺陷清单：
1. 无

阻塞项：
- 无

建议总控下一步：
1. 将 `0518T001` 视为已通过的 design-only 合同。
2. 下一步不要直接实现 queue/touch repair；应先开更多 current-format sample / cross-sample validation 任务，验证 replay-fill false-positive 是否重复出现。

提交信息：
- commit：f906fdb
