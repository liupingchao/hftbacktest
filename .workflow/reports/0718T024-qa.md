# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0718T024

状态：
- 未通过

更新时间：
- 2026-07-19 08:58 CST

验收线程：
- QA验收线程

验收对象：
- 总控 auto-loop / 业务实现线程 0718T024

验收范围：
- T023 exact envelope/identity repair、no-network preflight、single-window tiny-live、runtime source provenance、fill reconciliation、terminal account/open-orders/checksum 和 same-window acceptance。

## Findings

1. P1：runtime source provenance 可 fail-open。
   - sealed run 内没有 `source_commit.txt` 或 runtime-critical source hash manifest。
   - acceptance 在缺少 run-time source marker 时，仅凭 preflight 与 run 使用相同 `remote_repo` 路径就接受 source commit。
   - 相同目录路径不能证明 watcher 启动时的源码内容与 preflight commit 相同，也不能证明运行期间没有代码变化。
2. P1：same-window acceptance 忽略 live producer 的 fill reconciliation blocker。
   - `m2_fill_window_manifest.json` 明确包含 `fill_reconciliation_required_no_fill_unproven`，producer final recommendation 为 blocked。
   - 当前 acceptance 没有检查该 blocker 或产生它的 ambiguous cancel payload，却仍给出 mechanism/evidence integrity pass。
   - 这会把 producer 的 fail-closed 结果在下游重新解释为 pass。

## 通过项

1. task/window/attempt identity 已精确为 `0718T024 / window_01 / 0718T024:window_01:attempt_*`。
2. runtime config 精确执行 `0.005 BTC` max order、`0.01 BTC` max position、`1 USDC` max loss、`2` submissions。
3. 真实 BTC post-only `Alo` buy `0.005 @ 64770` 达到 `resting`，随后 tracked cancel。
4. final owned open orders `0`，独立 private proof `0`，post BTC position `0.0`，estimated loss `0.0`。
5. child return code `0`、reaped、no SIGKILL；status writer healthy。
6. dynamic spread、fill feedback、inventory skew、multi-level 和 actual quote behavior change 均未激活。
7. sealed run checksum `61/61`，missing `0`，mismatch `0`；preflight/post-live proof remote/local hash 一致。
8. business-thread full `examples/hyperliquid` regression 为 `461 passed`；QA focused checks 为 `42 passed`。

## QA 结论

`0718T024` 未通过。

本窗口可以保留为真实 submit/resting/cancel/final-open-orders/account 诊断证据，但不能作为 Principal Task 12 accepted baseline。Task 12 的机制/证据完整性 gate 不能关闭。

下一任务必须：

1. 在 orchestrator 启动 watcher 前，将 exact full source commit 和 runtime-critical file hashes 写入 sealed run，并由本地 expected commit blob hashes 独立校验。
2. 修复 cancel/fill reconciliation，使成功 cancel 后的冗余 cancel generic message 不被误判为 fill ambiguity；或者保留 blocker，并要求 acceptance fail-closed。
3. 新的 acceptance 必须显式拒绝任何未分类 producer blocker，不能覆盖 producer final recommendation。
4. 使用新的 formal repair/re-run task 和新的 clean live window；不得复用 T024 作为 accepted baseline。

## Unsupported Claims

- accepted zero-fill reconciliation；
- stable PnL；
- fill-rate；
- fee/rebate calibration；
- queue priority；
- maker viability；
- multi-level activation；
- promotion；
- final MVP pass。

## Multi-Level

- 未解锁。
- T021 prerequisite gate 继续 default-off；T024 是单边 fresh-touch lifecycle，也不满足原计划要求的已验收单层双边 manager lifecycle。

## 提交信息

- implementation/live source：`4c32d99`
- acceptance repair：`d4fdc9d`
- business report：`c239c20`
- live artifact：`local_live_analysis/principal_alignment_task12_repair_0718T024/`
