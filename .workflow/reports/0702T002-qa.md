# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0702T002

状态：
- 已通过

更新时间：
- 2026-07-06 10:39 CST

验收线程：
- QA验收线程

验收对象：
- 业务线程-collector-fix 0702T002

验收范围：
- 验收 Binance REST depth snapshot 限流失败硬门禁修复，包括 task/report、collector code、focused tests、CLI 参数、fail-fast 行为、manifest 证据和 no-live/no-private/no-order 边界。

验收步骤：
1. 复核 `.workflow/tasks/0702T002.md`、`.workflow/reports/0702T002-business.md`、0702T001 root cause 和本任务 hard boundary。
2. 复核 `examples/hyperliquid/synchronized_public_collection.py` 和 `examples/hyperliquid/test_synchronized_public_collection.py` 的修复点。
3. 运行 focused pytest、py_compile、两个 CLI help 检查和 task-scoped diff whitespace 检查。

实际结果：
- 修复将 Binance depth snapshot 默认 limit 降为 `100`。
- HTTP `418/429` 被标记为 rate-limited，并记录 `http_status`、`rate_limited`、`Retry-After`、attempt count 和 validity。
- 新增 bounded low-frequency retry/backoff，避免 tight loop。
- 无有效 `lastUpdateId/bids/asks` snapshot 时，collector 写 manifest 证据后 fail fast，避免生成 Binance top5 必空的成功样本。
- `collect` 和 `collect-binance-public` 均暴露 snapshot limit/retry 参数。
- 业务报告明确说明 Binance `2400 requests per minute` 文案是 IP-level limit 描述，不是本进程已发出 `2400/min` 的直接证据。
- 本任务未修改 0702T001 raw 数据，未补造 snapshot，未补造 Binance top5，未修改 alignment/join/sample expansion 统计口径。

验收结论：
- 已通过
- 结论说明：
  - 0702T002 满足 collector fail-fast 修复验收标准：之后再遇到无有效 Binance snapshot 时会在采集阶段失败并保留证据，而不是产出 top5 全空的可后处理样本。

通过项：
1. Snapshot default limit、retry/backoff、rate-limit evidence 和 validity gate 已实现。
2. Missing/invalid snapshot 已成为 hard collection failure。
3. Focused regression coverage 覆盖 429 backoff 和 missing snapshot fail-fast。
4. No-live/no-private/no-order/no-canary/no-promotion 边界保持。

不通过项：
1. 无

缺陷清单：
1. 无

阻塞项：
- 无

建议总控下一步：
1. 后续重新采集前使用 0702T002 修复后的 collector；若 AWS IP 仍持续 429，应在采集阶段 fail-fast，而不是进入 downstream。
2. 继续推进已授权的 no-submit `0625T008-PREFLIGHT` packet；不要从 0702T001 数据集解锁 signal/replay。

提交信息：
- commit：无
