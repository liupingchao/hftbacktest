# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0828T004

状态：
- 已通过

更新时间：
- 2026-08-28 10:42 CST

验收线程：
- QA验收线程

验收对象：
- SKHYNIX OBI Reversal Track A0 业务线程
- implementation commit：`663c14bd`
- business report commit：`1e394f22`

验收范围：
- 验收 29 个历史 captures 的 source/cache closure 和 zero-target input
  boundary。
- 验收 equal-weight L1-L5 OBI、causal reversal state machine、
  non-reversal controls、common risk-set support 和 threshold freeze。
- 验收 support-selected `tau_max`、first-passage target contract、H0/H1
  tuple 和 numeric gates。
- 验收 artifact closure、determinism evidence、测试和 exact-path git scope。
- 验收研究结论没有越过 A0 support/tuple-freeze 边界。

验收步骤：
1. 核对 implementation 和 business report commits 的 changed paths。
2. 执行 focused pytest、Python compile、`git diff --check` 和 Markdown
   fence parity。
3. 核对 21 个正式 artifacts 的 size/SHA closure。
4. 核对 classification、all-gates result、source raw-hash re-verification
   和 outcome-access ledger。
5. 核对 OBI threshold trace、reversal/control support 和 date
   concentration。
6. 核对 reusable common risk-set coverage 与 unique no-reuse matching
   diagnostic 的 estimand 区分。
7. 核对 follow-up completeness 和 `tau_max=120s` 选择。
8. 核对 plan 中记录了实际结果、方法修正和下一阶段不可改动参数。

实际结果：
- `663c14bd` 只包含 0828T004 task、canonical plan、A0 runner、focused
  tests 和 21 个 compact artifacts；`1e394f22` 只包含业务回报。
- `pytest` 为 `5 passed`；Python compile、`git diff --check` 和 Markdown
  fence parity 通过。
- 21/21 artifacts 的 size/SHA closure 通过；此前两个独立临时构建的
  manifests 也为 21/21 hashes 一致。
- 29 captures、9 dates、35.9172 hours 完成 source/cache closure；
  `raw_hashes_reverified_now=true`，depth gaps 为 0。
- 23 captures 具有有效 OBI variation；6 个 low-variation captures
  仅保留 provenance。
- threshold trace 中 `0.40` 因 162.318/hour 超出 rate cap 未通过；
  `0.50` 和 `0.60` 通过，按最小合格 threshold 冻结 `0.50`。
- 识别 4,021 reversals，111.952/hour，最大单日占比 0.2502。
- 生成 3,162 controls；4,005 reversals 位于 common support，整体覆盖
  0.9960，最低单日 0.8764。
- unique no-reuse matching 为 2,801 pairs、覆盖 0.6966，只作为有限
  control allocation diagnostic；primary hazard estimand 使用 full
  reusable common risk set。
- 新增回归测试证明一个 control state 可提供 full common support，即使
  no-reuse matching 只能分配给一个 reversal。
- `120s` follow-up 仍有 0.9724 overall 和 0.8276 minimum-date
  completeness；`300s` 降至 0.9363 和 0.6897，因此冻结
  `tau_max=120s`。
- classification 为 `A0_support_and_tuple_frozen`，全部 7 个 gates
  为 true。
- outcome-access ledger 中 future price fields 为 `[]`、follow/fail
  labels 未物化、aligned future-price plots 未创建。
- plan 明确 A0 pass 只授权新 task 下的 A1/A2，不证明 OBI reversal
  predictive value，也不构成 prospective evidence。

验收结论：
- 已通过
- 结论说明：
  - A0 已在零目标访问条件下完成数据支持检查、state/control contract、
    H0/H1 tuple、horizon 和 numeric gates 冻结。`已通过`不表示 OBI
    reversal 的统计增量已成立。

通过项：
1. Source、cache 和 artifact identities 闭合。
2. OBI 和 reversal state machine 具有 causal detection semantics。
3. Support-only threshold selection 无 future-target tuning。
4. Common risk-set overlap 足够，且与 no-reuse matching 诊断正确区分。
5. `tau_max=120s` 由 follow-up geometry 冻结。
6. Future target contract 已冻结但 outcomes 尚未物化。
7. Zero-target access、tests、determinism 和 git scope 通过。
8. Claim boundary 和 prospective boundary 正确。

不通过项：
1. 无。

缺陷清单：
1. 无。

阻塞项：
- 无 A0 验收阻塞。
- OBI reversal effect 是否存在仍须在后续 A1/A2/A3 检验。
- 最终确认仍依赖 protocol-frozen prospective sessions。

建议总控下一步：
1. 新建正式任务执行 A1 state-ledger freeze 和 A2 symmetric one-tick
   first-passage target materialization。
2. 目标物化后先完成 H0-only baseline 与 target diagnostics，再运行
   H1 incremental competing-risk comparison。
3. 禁止根据 target 结果修改 OBI formula、threshold、dwell、
   `detected_at`、barrier、censoring 或 `tau_max`。

提交信息：
- commit：待 QA 事实源提交
