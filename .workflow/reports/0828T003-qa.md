# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0828T003

状态：
- 已通过

更新时间：
- 2026-08-28 10:16 CST

验收线程：
- QA验收线程

验收对象：
- SKHYNIX OBI Reversal Track A 方案更新业务线程
- plan commit：`eeaec22f`
- business report commit：`34454727`

验收范围：
- 验收 `OBI_REVERSAL_V1` 是否作为 canonical Track A 的唯一 primary
  hypothesis 写入。
- 验收 OBI formula、causal reversal state machine、alignment、
  current-OBI controls、first-passage targets 和 H0/H1 path-dependence
  comparison。
- 验收 target delay、null、historical/prospective claim boundary 和 generic
  successor isolation。
- 验收 git exact-path scope，确认未混入工作树已有无关文件。

验收步骤：
1. 核对 plan 和 business report commit 的 changed paths。
2. 执行 `git diff --check` 和 Markdown fence parity。
3. 核对 equal-weight L1-L5 OBI 与 robustness hierarchy。
4. 核对 `cross_at`、`detected_at` 和 pre-detection transition。
5. 核对 reversal/non-reversal controls 与 H0 current-OBI requirement。
6. 核对 follow/fail first-passage hazard、proper scores、null 和 gates。
7. 核对 generic composite M-state 不能救回 OBI primary failure。

实际结果：
- `eeaec22f` 仅包含 0828T003 task 和 canonical Track A 方案更新；
  `34454727` 仅包含业务回报。
- `git diff --check` 通过；方案为 1277 行，114 个 fenced-code markers，
  数量为偶数。
- Primary OBI 为 equal-weight L1-L5 displayed-quantity ratio；其他 depth
  projections 仅为 robustness，不能救回 primary。
- Reversal state machine 包含 accepted old-state dwell、neutral crossing、
  opposite-state confirmation、quality/ambiguity 和 hysteresis。
- Primary decision/alignment time 为 `reversal_detected_at`；
  `reversal_cross_at` 仅为结构诊断。
- Cross-to-detection 期间发生的价格转移进入
  `pre_detection_transition`，不能被宣称为可因果捕捉。
- Common risk set 比较相同 current OBI region 下的 reversal 和
  non-reversal arrival paths。
- H0 必须包含 current OBI 与普通 snapshot context；H1 只增加 reversal
  indicator `R_i`，正确隔离 path-dependence increment。
- Target 为 pressure-oriented `n_follow/n_fail` symmetric first passage；
  无事件与数据边界进入 right censoring。
- Proper scoring、session-block uncertainty、current-OBI-stratified null、
  stronger-H0 slope diagnostic、no-refit transport 和 prospective gate
  已冻结。
- Generic withdrawal/flow/refill composite 被隔离为 independently
  versioned successor，不能救回 OBI primary failure。
- 初次自动 QA 的 generic-isolation 断言使用了不存在的长英文短语而失败；
  核对原文后改为精确文本断言，完整语义检查全部通过。该失败属于检查脚本
  字符串错误，不是方案缺陷。

验收结论：
- 已通过
- 结论说明：
  - OBI reversal 已被正确转化为低自由度、current-state-controlled、
    causally aligned 的 path-dependence hypothesis。`已通过`只表示研究
    设计与流程闭合，不表示经验已被数据确认。

通过项：
1. OBI reversal 成为唯一 primary hypothesis。
2. Alignment、confirmation delay 和 pre-detection 语义因果闭合。
3. Current-OBI-matched controls 与 H0/H1 comparison 能区分 state/path。
4. First-passage target 允许数据表达时间分布而不挑 favorable horizon。
5. Null、transport、actionability 和 prospective gates 可执行。
6. Generic successor 与 primary result 隔离。
7. Git scope 干净，未混入已有无关 staged/untracked files。

不通过项：
1. 无。

缺陷清单：
1. 无。

阻塞项：
- 无文档验收阻塞。
- 最终正结论仍依赖未来另行授权的 prospective 数据。

建议总控下一步：
1. 派发
   `SKHYNIX-BINANCE-OBI-REVERSAL-TRACK-A0-SUPPORT-AND-TUPLE-FREEZE`。
2. A0 保持 zero future-price target access，只冻结 OBI、state machine、
   controls、quote/barrier、H0/H1、time basis 和 numeric gates。

提交信息：
- commit：待 QA 事实源提交
