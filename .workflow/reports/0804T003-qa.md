# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0804T003

状态：
- 已通过

更新时间：
- 2026-08-04 10:51 CST

验收线程：
- QA验收线程

验收对象：
- 业务线程-python/tutorial-notebooks + 0804T003

验收范围：
- 九个原始 notebook 的 Run All 活动区和原教程 reference 区。
- `notebook_support.py` 的实验映射、环境检测、共享缓存和 manifest。
- `refresh_notebooks.py` 的 nbformat 结构化生成与确定性。
- amdserver 九本 notebook 的真实 Jupyter 执行产物。
- Tardis-only/no-key/no-download/private API 边界和 L3 语义。

验收步骤：
1. 审查被验 commit `4f22b610` 的 notebook、helper、生成器和测试。
2. 检查九本 notebook 的 cell 类型、ID、活动/参考边界和 slug 映射。
3. 在隔离环境连续生成两次并比较九本 notebook SHA256。
4. 运行本机和 amdserver focused pytest、nbformat 校验和生成器检查。
5. 检查 amdserver executed notebooks 的 execution count 和 error output。
6. 检查九份 manifest 的共享数据身份、实验状态和 L3 事件统计。
7. 对 QA 发现的 README P3 表述矛盾进行修复并补回归测试。

实际结果：
- 九本 notebook 均只有 `4` 个活动 code cell，reference heading 后没有
  code cell。
- 九本实验 slug、文件映射和顺序正确。
- amdserver executed notebooks 均通过 nbformat 校验，execution count
  为 `[1, 2, 3, 4]`，error output 为 `0`。
- 执行副本的活动 cell source、ID 和类型与被验 notebook 一致。
- 生成器连续执行两次，九本 notebook SHA256 完全一致。
- 默认 amdserver/Mac 路径、默认日期、300 秒窗口和四个环境变量覆盖
  逻辑正确。
- 活动执行链只读取已有 Tardis 文件，未发现 key、下载、网络请求或
  交易所 private API。
- 九份 manifest 共享相同 output root、fused NPZ 和原始数据 identity；
  fused events 为 `563454`。
- 实验状态为 `5 passed / 3 adapted / 1 blocked_expected`。
- L3 MBO add/modify/cancel/fill 均为 `0`，L2 depth events 为 `540213`；
  notebook 明确返回 `blocked_expected` 和 L2 control。
- focused tests 初验为 `5 passed`。
- QA 发现 README 仍写“original notebooks are not modified”的 P3
  文档矛盾；commit `676815ba` 已修正表述，并新增活动/参考 cell 边界、
  slug、唯一 ID 和无 wget/curl 的回归测试。
- 修复后本机与 amdserver focused tests 均为 `6 passed`，生成器
  `--check` 通过。
- amdserver 最终 HEAD 为
  `676815ba59c52dfbcb62a3b6a7d5627fcc6ad5ef`。

验收结论：
- 已通过
- 结论说明：
  - 九个 notebook 已可在 amdserver 使用现有 Tardis 数据直接 Run All；
    执行结果、共享缓存、适配状态和 L3 数据边界均符合任务要求。

通过项：
1. 九本 notebook 标准 Run All 和真实 nbconvert 执行。
2. 活动代码与原教程 reference 的安全边界。
3. 实验映射、共享缓存和 manifest。
4. Mac/amdserver 数据布局和环境变量覆盖。
5. Tardis-only/no-key/no-download/private API 边界。
6. L2/L3 能力边界和 `blocked_expected`。
7. 生成器确定性和提交内回归测试。

不通过项：
1. 无。

缺陷清单：
1. README P3 表述矛盾，已由 `676815ba` 修复。

阻塞项：
- 无。

残余风险：
- 当前实际执行范围为 `BTCUSDT`、2025-08-01、300 秒窗口。
- 真实 L3/MBO 数据未挂载，因此无法展示真实 L3 成交队列结果。

建议总控下一步：
1. 将 `0804T003` 收口为 `已通过`。
2. 用户可直接在 amdserver 的
   `/home/molly/project/hftbacktest/examples/` 打开九个 notebook 测试。

提交信息：
- notebook commit：4f22b6106f9d938a1f5780e8ea12e3db1f25c730
- QA fix commit：676815ba59c52dfbcb62a3b6a7d5627fcc6ad5ef

文档占用说明：
- `docs/qa-acceptance-report.md` 当前仍包含另一并行任务的未提交修改；
  本任务未覆盖该文件，最新结论保存在本任务专属 QA 报告中。
