# QA 验收结果

## Findings

1. P0：无。
2. P1：无。
3. P2：无。

执行线程：
- 独立 QA 验收线程

任务ID：
- 0721T043

状态：
- 已通过

更新时间：
- 2026-07-21 CST

验收对象：
- implementation commit `238a2c4dd253d52196dc9d44094b302cb79b7137`
- business/report HEAD `654c90e9e34a44918855e40deb2cf5f1489cc271`

验收范围：
- Persisted nested representation gate 与 generic token semantics 的执行顺序。
- Shared `historical_result_envelope_valid()` 入口的短路保护。
- Invalid nested、outer-row hostile 和 producer-redacted positive 合同。
- Raw nested、immutable T040、六个 T040 hostile。
- Focused、combined、完整 Hyperliquid regression。
- T037/T031/T026/T022/T016 historical exact boundaries 与 offline-only 边界。

验收步骤：
1. 审查任务、T043 business report、T042 QA、implementation/report commits。
2. 审查所有 `historical_reference_tokens()` 和 `historical_result_envelope_valid()` 调用入口。
3. 运行 focused、targeted、combined 和完整 Hyperliquid regression。
4. 独立回放 raw nested、immutable T040 和六个原 hostile。
5. 使用显式 `1800s` expectation 回放五份 historical exact boundary。
6. 进行 parser/shared-validator 进程内调用计数审计。

实际命令与结果：
- `python -m pytest examples/hyperliquid/test_cross_exchange_delayed_history_probe_acceptance.py -q`
  - `70 passed in 0.15s`
- 五个关键合同测试定向执行：
  - `20 passed in 0.09s`
- verifier/watcher/orchestrator combined regression：
  - `239 passed in 22.99s`
- `python -m pytest examples/hyperliquid -q`
  - `1213 passed in 51.66s`
- `py_compile`、implementation diff check：通过。
- implementation commit 到 HEAD 的两个代码文件无后续差异；implementation 是 HEAD 祖先。

代码审查结果：
- `summarize_history_envelope()` 先调用
  `persisted_history_order_evidence_issues()`，仅在无 representation reason 时调用
  `historical_reference_tokens()`。
- Invalid nested representation 设置 `nested_representations_valid=false`、
  `tokens={}` 和 `classification=malformed`。
- 后续 `not nested_representations_valid or ...` 使用短路语义，确保 invalid nested
  路径不会进入 shared `historical_result_envelope_valid()`。
- Outer-row identity hostile 不清空合法 nested tokens，但会将该行分类为 malformed
  并使 acceptance blocked。
- Producer-redacted canonical/alternative aliases 精确重建 persisted
  `oid_token`/`cloid_token`。

调用计数审计：
- Invalid nested：generic parser `0` 次，shared validator `0` 次，
  `tokens={}`，classification `malformed`。
- Outer-row hostile：shared validator `1` 次，parser `2` 次，合法 nested tokens
  保留，classification `malformed`。
- Producer-redacted alternative alias：shared validator `1` 次，parser `2` 次，
  token map 精确重建，classification `foreign`。

证据回放结果：
- T041 `raw_nested_identity`：exit `2`，`12 pass / 2 fail`，blocked；
  `tokens={}`、classification `malformed`。
- Immutable T040：exit `0`，`14/14 pass`，`blocking_checks=[]`。
- 六个 T040 hostile 均 exit `2` 并 blocked：
  - `conflicting_alias`
  - `direct_error`
  - `direct_extra_orders`
  - `false_attempt`
  - `foreign_history_target`
  - `forged_history_classification`

Historical exact boundary：
- T037：exit `2`，decision `39/4`，lifecycle `78/0`，blocked。
- T031：exit `2`，decision `43/0`，lifecycle `55/23`，blocked。
- T026：显式 `--expected-window-seconds 1800`，exit `0`，
  decision `43/0`，lifecycle `78/0`，pass。
- T022：exit `2`，decision `43/0`，lifecycle `66/12`，blocked。
- T016：exit `2`，decision `43/0`，lifecycle `66/12`，blocked。
- 五份 manifest 均为 `offline_only=true`。
- `network/private/order/cancel/remote/new_live/credentials_read` 全部为 `false`。

验收结论：
- 已通过
- T042 的 representation-before-token-semantics P2 已关闭。
- 未发现新的 P0/P1/P2 或测试缺口。
- T043 仅关闭 verifier ordering repair；不改变 T037 blocked 结论，也不单独解锁
  adaptive、dynamic 或 multi-level activation。

通过项：
1. Representation gate 覆盖逐行重建和 shared envelope validator 入口。
2. Invalid nested representation parser 零调用、tokens 为空并分类为 malformed。
3. Outer-row hostile blocked 且保留合法 nested tokens。
4. Producer-redacted aliases 精确重建。
5. Raw nested、immutable T040、六个 hostile 边界通过。
6. Focused、combined、full regression 完整通过。
7. 五份 historical exact boundary 和 offline-only 合同保持。

不通过项：
1. 无

缺陷清单：
1. 无

阻塞项：
- 无

建议总控下一步：
1. 将 `0721T043` 标记为 `已通过`，同步最新结果至
   `docs/qa-acceptance-report.md`。
2. 后续如需关闭 T037 evidence gate，应创建新的固定报价、单窗口、保守 envelope
   tiny-live 任务；不得复用 T037 immutable window 作为已接受基线。
3. 新 live QA 通过前继续锁定 adaptive 和 multi-level activation。

提交信息：
- implementation：`238a2c4dd253d52196dc9d44094b302cb79b7137`
- business/report：`654c90e9e34a44918855e40deb2cf5f1489cc271`
- QA commit：无；本线程只读，未修改仓库或 immutable evidence。
