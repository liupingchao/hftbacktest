# QA 验收结果

## Findings（按严重性）

- P0：无。
- P1：legacy root 重定位存在 repo containment fail-open。
  `_resolve_recorded_artifact_path()` 在未规范化 recorded path 的情况下
  使用 `relative_to()`，随后只验证 candidate 存在；已知旧根下的 `..`
  可以映射到当前 repo 外部的任意已存在路径。
- P1：historical availability probe 把“数据包存在但合同损坏”误判为
  “外部数据包缺失”。`historical_source_artifacts_available()` 捕获
  `RowLevelGeneratorError`、`KeyError`、`TypeError` 和 `ValueError`，
  因而错误 canonical count、损坏 JSON 或非法 sample contract 会导致
  exact historical integration test 被 skip，而不是失败。
- P2：任务记录的 implementation 完整 SHA
  `d5e5318b9603d809ea90294a3fbfd54c65bad85c` 不是有效 Git 对象；实际
  implementation commit 是
  `d5e5318baaade3031f439b78aa9a7263ab0d85a6`。
- P2：full Hyperliquid regression 两次均在一个未被 T056 修改的 watcher
  时序测试失败；该测试单独运行通过。T056 focused 范围通过，但正式
  full-green 验收条件未达到。

执行线程：
- QA验收线程

任务ID：
- 0722T056

状态：
- 未通过

更新时间：
- 2026-07-22 14:22 Asia/Shanghai

验收线程：
- QA验收线程

验收对象：
- 业务执行线程 `0722T056`
- actual implementation commit
  `d5e5318baaade3031f439b78aa9a7263ab0d85a6`
- workflow/business commit
  `50ccf132a20fa0a7b2b70751360c71acf9d5eb6c`

验收范围：
- 严格 offline-only 独立 code review 和临时目录 hostile
  counterexample。
- 覆盖 legacy amdserver path relocation、unknown absolute path、
  repo containment、artifact existence、historical skip qualification、
  skip reason 和 exact historical assertion 保留。
- 核对 implementation diff 是否影响 strategy、alpha、quote、risk、
  order lifecycle 或 live 行为。
- 不修改四个 implementation/test 文件。

验收步骤：
1. 阅读 workflow-kit、task、business report、`task_plan.md`、
   `progress.md` 和 `findings.md`，核对任务范围和固定提交。
2. 审查 implementation diff，并检查 recorded absolute path 到当前
   checkout 的每个路径转换和 existence/contract validation 边界。
3. 运行独立 hostile counterexamples：
   - 已知旧根路径使用 `..` 逃出当前 repo。
   - 所有声明文件存在但 source manifest canonical contract 损坏。
   - 0602T001 全部 required paths 存在但内容损坏的对照。
4. 运行 focused tests 和精确 skip reason 输出。
5. 两次运行 full `examples/hyperliquid` regression，并单独重跑唯一
   failing watcher test。
6. 运行四文件 `py_compile`、implementation/workflow
   `git diff --check`、implementation 文件范围检查和工作树检查。

实际结果：
- 正常已知旧路径在对应 repo-relative artifact 存在时可重定位。
- 未知绝对路径 `/tmp/another-checkout/...` 保持原路径，不会被普通路径
  前缀规则重定位。
- Hostile traversal
  `/home/molly/project/hftbacktest/../outside/secret.json` 被解析为临时 repo
  外部的已存在文件，`traversal_escaped_repo=True`。实现未验证 resolved
  candidate 仍属于 `PROJECT_ROOT`。
- 构造的 T006、T003 和 source manifest 文件全部存在，但
  `canonical_sample_count=6` 时
  `historical_source_artifacts_available()` 返回 `False`。按当前
  `pytest.mark.skipif`，这会以
  `0609T002 historical pricing-signal source package is absent` 跳过，
  隐藏真实合同失败。
- 0602T001 对照中，全部 required paths 存在但内容损坏时
  `required_sample_artifacts_available()` 返回 `True`，因此测试会执行而
  不是 skip；该 helper 没有相同的 semantic-error fail-open。
- Focused：
  `12 passed, 2 skipped in 0.13s`。
- Skip reason 精确为：
  - `0609T002 historical pricing-signal source package is absent`
  - `0602T001 synchronized public sample package is absent`
- Full Hyperliquid 第一次：
  `1 failed, 1250 passed, 2 skipped in 56.70s`。
- Full Hyperliquid 第二次：
  `1 failed, 1250 passed, 2 skipped in 56.08s`。
- 两次 full 的唯一失败均为
  `test_manager_resting_interval_contract_uses_submit_end_to_cancel_request`；
  该测试单独运行 `1 passed in 0.11s`。T056 diff 未修改其实现或测试文件。
- 四个指定文件 `py_compile` 通过。
- `git diff --check`、`git diff --check d5e5318b^..HEAD` 和 workflow
  commit 未触碰四个 implementation/test 文件的检查通过。
- Implementation diff 仅修改两个历史 public research/data helper 和
  两个对应测试文件；未修改 strategy、alpha、quote、risk、order
  lifecycle 或 live 执行路径。
- 工作树中的既存 `.DS_Store` 未被修改或纳入本任务。
- 未执行 network、remote、private/account、order/cancel、live 或
  service 操作。

验收结论：
- 未通过。
- 结论说明：
  - Clean-clone 的两个原始 collection failure 已被转换为精确 skip，
    但重定位 containment 和 semantic-error skip 两个 fail-open 违反
    本任务明确安全边界；full regression 也未取得独立全绿结果。

通过项：
1. 当前 clean clone 的两个外部历史数据依赖具有精确 skip reason。
2. 正常已知 amdserver root relocation 和普通未知绝对路径拒绝具有
   self-contained regression。
3. 0602T001 partial fixture 不会被误判为完整。
4. 原有 `3545` 和 `3599` exact assertions 未被删除或弱化。
5. 策略和 live 行为文件未改变，offline-only 边界保持。

不通过项：
1. 已知 legacy root 可通过 `..` 或等价 resolved containment 逃逸映射到
   当前 repo 外部。
2. 已存在但格式或合同错误的 0609T002 package 会被错误 skip。
3. Full Hyperliquid regression 未取得通过结果。
4. Workflow/business 记录的 implementation 完整 SHA 无效。

缺陷清单：
1. P1：对 legacy recorded path 和 resolved candidate 同时执行严格
   containment 检查；拒绝 `..`、symlink escape 和任何不属于 resolved
   `PROJECT_ROOT` 的 candidate。
2. P1：availability probe 只允许真正缺失的 external artifact 触发
   skip；JSON、schema、canonical count、sample mode 和其他 semantic
   错误必须让 integration test 执行并失败。
3. P2：增加上述 traversal、symlink/containment 和 malformed-existing
   package hostile regressions。
4. P2：修正 task/business/controller tracking 中的 exact
   implementation SHA，并在重新送验前取得 full regression 全绿结果。

阻塞项：
- 无外部阻塞；需要新的 offline-only repair task。

建议总控下一步：
1. 派发最小 offline repair：严格 project-root containment，并把
   missing-artifact qualification 与 semantic validation 分离。
2. 保留两条现有精确 skip reason，但增加 malformed-existing package
   必须 fail 的回归。
3. 调查并消除 full-suite watcher 时序/状态泄漏，或至少在同一固定提交
   上取得可重复 full-green 证据后重新送 QA。

提交信息：
- QA commit：待本报告提交后填写于回报。
- Actual implementation commit：
  `d5e5318baaade3031f439b78aa9a7263ab0d85a6`
- Workflow/business commit：
  `50ccf132a20fa0a7b2b70751360c71acf9d5eb6c`
