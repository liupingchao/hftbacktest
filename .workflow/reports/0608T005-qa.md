# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0608T005

状态：
- 已通过

更新时间：
- 2026-06-08 16:19 CST

验收线程：
- QA验收线程

验收对象：
- 业务线程-research / 0608T005

验收范围：
- 验收 `0608T005` 是否按任务边界完成 read-only basis-context visibility / lineage diagnosis，仅评估 `regime_011_1000_spread_10_20_ticks` 下的 `context_basis_mid_ticks > 0`，并确认未输出 strategy、case-library、shadow decision、private/order endpoint、live/default-on/tiny-live 或 promotion claim。

验收步骤：
1. 读取 `.workflow/tasks/0608T005.md` 和 `.workflow/reports/0608T005-business.md`，确认任务边界、业务回报、artifact 路径和验证命令。
2. 解析 `local_live_analysis/canonical_basis_context_visibility_0608T005/basis_visibility_manifest.json`。
3. 检查 required artifacts 是否存在。
4. 读取 `basis_lineage_audit.csv`、`basis_timestamp_join_audit.csv`、`basis_positive_pattern_by_sample.csv`、`basis_horizon_persistence.csv`、`basis_contract_decision.csv`。
5. 复跑 `python examples/hyperliquid/canonical_basis_context_visibility.py --help`。
6. 复跑 `python -m py_compile examples/hyperliquid/canonical_basis_context_visibility.py examples/hyperliquid/test_canonical_basis_context_visibility.py`。
7. 复跑 `python -m pytest examples/hyperliquid/test_canonical_basis_context_visibility.py`。
8. 复跑正式 runner 到 `/tmp/qa_0608T005_basis_visibility`，避免改写正式 artifact 时间戳。
9. 检查 `basis_context_visibility_report.md` 与 boundary flags。
10. 运行 `git diff --check`。

实际结果：
- Manifest 解析通过，`task_id=0608T005`，`schema_version=canonical_basis_context_visibility_v1`。
- Assessment 限定为 `regime_011_1000_spread_10_20_ticks` 和 `context_basis_mid_ticks > 0`。
- Canonical guard 结果为 `canonical_sample_count=3`、`diagnostic_rejection_count=0`。
- T004 prerequisite 验证通过：basis-positive row 在 T004 中为 `invalid_not_decision_visible`，且该分类来自 contract caveat。
- Basis 公式审计为 `(binance_mid_px - hyperliquid_mid_px) / 0.1`。
- Lineage 审计 `57/57` 行通过，最大公式误差 `0.0`。
- Timestamp/as-of 审计 `57/57` 行为 `timestamp_clean_asof`，future input joins `0`，missing input joins `0`。
- Pattern 覆盖 `57` 行、`3` 个 canonical samples，最大样本占比 `0.38596491`。
- Per-sample hit rate 为 `0.90909091 / 0.94736842 / 1`。
- Horizon persistence 未反转：`1000ms` hit rate `0.94736842`、mean `97.54385965` ticks；`5000ms` hit rate `0.84210526`、mean `134.03508772` ticks；`10000ms` hit rate `0.71929825`、mean `122.63157895` ticks。
- Contract decision 为 `upgrade_to_context_only_supported`，理由为 basis as-of clean 且公式可解释；保留 execution-PnL caveat，但移除 decision-visibility blocker。
- Focused pytest 通过：`5 passed`。
- `/tmp` 复现输出一致：`final_contract_decision=upgrade_to_context_only_supported`。
- `git diff --check` 通过。
- QA 未发现本任务访问 private/order endpoint、实现策略/quote/order 行为、启动 live/default-on/tiny-live、运行参数搜索、生成 case-library/shadow decision、输出交易指令或 promotion claim。

验收结论：
- 已通过
- 结论说明：
  - `0608T005` 已按任务边界完成 read-only basis-context visibility / lineage diagnosis。`context_basis_mid_ticks > 0` 可从 diagnostic-only visibility caveat 升级为后续只读研究中的 context-only supported 字段；该结论不授权策略实现、case-library、shadow decision、private/order endpoint、live/default-on/tiny-live、parameter search 或 promotion。

通过项：
1. Runner 使用 accepted canonical source-lock path，并拒绝 diagnostic-only synthetic formal input。
2. Runner 验证 T004 prerequisite。
3. Runner 从 `multi_sample_manifest.json` `samples[].pricing_signal_rows` 解析 row-level inputs。
4. Assessment 限定为 `regime_011_1000_spread_10_20_ticks` 和 `context_basis_mid_ticks > 0`。
5. Basis lineage、timestamp/as-of、sample distribution、horizon persistence 和 contract decision artifacts 全部存在并可解析。
6. Final contract decision 使用任务允许 taxonomy。
7. Focused verification 全部通过。

不通过项：
1. 无

缺陷清单：
1. 无

阻塞项：
- 无

建议总控下一步：
1. 可将 `0608T005` 标记为 `已通过`。
2. 在后续只读研究中，可把 `context_basis_mid_ticks` 作为 decision-time context-only supported 字段使用，但继续保留 execution-PnL caveat。
3. 不应继续把 `regime_011_1000_spread_10_20_ticks` 本体作为 maker 或 directional 主线；若继续，应新建只读 basis-positive 稳健性诊断任务，脱离 Regime 011 壳层检查跨 spread/join-age/volatility/venue-state 稳定性。

提交信息：
- commit：`03f05f8`
