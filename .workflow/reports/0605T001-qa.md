# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0605T001

状态：
- 已通过

更新时间：
- 2026-06-05 11:39 CST

验收线程：
- QA验收线程

验收对象：
- 业务线程-python / 0605T001

验收范围：
- 验收 `0604T016` QA 指出的 shutdown terminal proof 漏洞是否已修复：`PARTIALLY_FILLED` 本地订单必须被识别为 active，不能被误判为 absent / terminal confirmed；同时保持 T016 已通过的 wait-result 分类、summary counter 和禁止越界边界。

验收步骤：
1. 读取 `.workflow/tasks/0605T001.md`，确认本任务是正常 QA，范围限于 `live_tick_mm.py` shutdown terminal proof、focused tests、任务文件和业务报告。
2. 读取 `.workflow/reports/0605T001-business.md`，核对业务报告、验证命令、commit id 和 forbidden 边界。
3. 检查提交 `4d2624c` 的文件范围，确认修复只触及任务文件、`live_tick_mm.py` 和 focused tests；检查提交 `b8a5b2e` 为业务报告提交。
4. 复查 `_confirm_shutdown_terminal_state_from_local_orders(...)` 是否直接遍历 `hbt.orders(asset_no)` snapshot，并按状态分类 active / terminal / unknown。
5. 复查 focused regression 是否覆盖 `wait_result=3 + local PARTIALLY_FILLED same order_id`。
6. 复跑 focused pytest：`/home/molly/anaconda3/envs/hftbacktest/bin/python -m pytest examples/binance_tick_mm/test_backtest_tick_mm.py -k "live_shutdown or shutdown_cancel or wait_order_response or terminal_confirmed or order_response_received or partially_filled"`。
7. 复跑 package-level test 文件集合：`/home/molly/anaconda3/envs/hftbacktest/bin/python -m pytest examples/binance_tick_mm/test_*.py`。
8. 运行 `git diff --check`。

实际结果：
- 提交 `4d2624c` 文件范围为 `.workflow/tasks/0605T001.md`、`examples/binance_tick_mm/live_tick_mm.py`、`examples/binance_tick_mm/test_backtest_tick_mm.py`。
- 提交 `b8a5b2e` 写入 `.workflow/reports/0605T001-business.md`。
- 未发现 py binding、Rust live bot/backtest、connector、production config、normal trading loop cancel、audit schema、strategy quote/submit/cancel 行为、REST private/order endpoint、live/default-on/tiny-live 或 promotion 相关越界改动。
- `SHUTDOWN_ACTIVE_LOCAL_ORDER_STATUSES` 包含 `NEW` 和 `PARTIALLY_FILLED`；`SHUTDOWN_TERMINAL_LOCAL_ORDER_STATUSES` 包含 `EXPIRED`、`FILLED`、`CANCELED`、`REJECTED`。
- `_confirm_shutdown_terminal_state_from_local_orders(...)` 直接遍历 `hbt.orders(asset_no)` 的本地订单 snapshot；同一 `order_id` 为 `PARTIALLY_FILLED` 时返回 `terminal_confirmed=False` / `final_order_status=local_active_order:partially_filled`。
- Focused regression `test_shutdown_wait_three_with_partially_filled_local_order_is_not_terminal_confirmed` 覆盖了 T016 QA 缺陷。
- Focused pytest 通过：`14 passed, 173 deselected`。
- Package-level `test_*.py` 通过：`302 passed`。
- `git diff --check` 通过，无输出。

验收结论：
- 已通过
- 结论说明：
  - T016 QA 指出的 `PARTIALLY_FILLED` active local order 误判缺陷已被修复，terminal proof 现在按本地订单状态直接分类，且未扩大到 REST、binding、Rust、connector、normal loop 或 live/promotion 范围。

通过项：
1. `PARTIALLY_FILLED` 本地订单被视为 active，`wait_result=3` 只设置 `order_response_received=True`，不会单独确认 terminal。
2. `NEW` / `PARTIALLY_FILLED` 与 terminal 状态分类清晰；unknown / status unavailable 不确认 terminal；local absent proof 保留。
3. T016 已通过的 wait-result 分类和 shutdown summary counter 口径未被破坏。
4. 指定 focused 和 package-level pytest 均通过。
5. 未触碰 forbidden 文件或 live/default-on/promotion 边界。

不通过项：
1. 无

缺陷清单：
1. 无

阻塞项：
- 无

建议总控下一步：
1. 可将 `0605T001` 标记为 `已通过`。
2. 如继续 shutdown safety 方向，下一步应由总控单独决定是否需要 REST/open-order reconciliation 或 audit tail proof；本 QA 不授权扩大范围。

提交信息：
- commit：`4d2624c` / `b8a5b2e`
