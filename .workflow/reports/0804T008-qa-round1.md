# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0804T008

状态：
- 未通过

更新时间：
- 2026-08-04

验收线程：
- QA验收线程

验收对象：
- 业务线程-python/three-session-commonality-execution + 0804T008

验收范围：
- 本轮仅验收四项 directional-BBO formal gates、正式分层、确定性、
  provenance closure 和 claim boundaries。
- broader structural commonality controls 不在本轮通过声明内。

验收步骤：
1. 读取 workflow 规范、任务、业务报告、研究计划和全部正式 manifests。
2. 只读审查 effect-retention、first-after、fast-L2 secondary 和
   full multi-track surrogate 实现。
3. 独立重算行数、coverage、BH、999 slots、C1/C2/C3 和 75-record SHA。
4. 核验远端/本地 SHA，并在同一 AMD 环境重跑 surrogate 0。
5. 运行任务相关的 91 项联合回归、py_compile、gzip、pip check 和
   scoped git diff 检查。

实际结果：
- 前三项门禁和所有数值对账通过。
- surrogate 文件为 119,880 rows 和 5,994 quality rows；999 IDs 每个精确
  60 primary + 60 secondary slots、单一 lag、无 invalid slot。
- 独立重算当前结果为 43 C1 / 15 C2 / 2 C3，p/q 与正式表一致。
- remote/local SHA、same-AMD replay、75-record closure 和 91 项回归通过。
- 但 full multi-track 实现没有查询或重建 standard-L2、asset-context、
  main allMids、target-dex allMids 的状态和值，仅移动其时间戳并统计
  retained-count。
- 远端正式运行依赖的 commonality source SHA 未被正式 package 精确归档。
- 正式 package 同时保留一份声称“四门禁未完成、全部 formal C1”的旧报告。

验收结论：
- 未通过
- 结论说明：
  - 前三项门禁及数值对账通过，但第四项完整多轨道 surrogate 未达到
    冻结合同，正式 C3 分层暂不能验收。

通过项：
1. adjusted/unadjusted effect-retention。
2. first-after-target。
3. separate fast-L2 secondary family。
4. 999 x 60+60 slot、BH 和 tier 算术对账。
5. remote/local SHA、same-AMD replay、gzip、测试和 75-record closure。
6. claim boundaries 未声称因果领导、exact fill、可执行套利或 PnL。

不通过项：
1. full Hyperliquid multi-track surrogate 未实际消费四条辅助轨道的状态和值。
2. 正式运行依赖源码 SHA 未形成精确、可移植的 provenance closure。
3. 正式发布包包含互相矛盾的 directional-BBO 报告。

缺陷清单：
1. P1：辅助轨道只做时间戳 retained/order 审计，未按 t+lag 查询状态并
   进入依赖特征或资格重建。
2. P1：归档 runtime source 不包含远端正式运行使用的 commonality
   dependency SHA。
3. P2：directional_bbo_confirmation.md 未标记 superseded，仍发布旧结论。

阻塞项：
- 无外部阻塞；需要业务线程修复后重新运行第四项 gate 和 finalizer。

建议总控下一步：
1. 解析并冻结 standard-L2、asset-context 和两条 allMids 的完整 payload，
   对每个 surrogate 在 t+lag 严格 as-of 查询并重建相关状态/资格。
2. 将实际远端运行的全部源码依赖按 SHA 归档并绑定到 surrogate manifest。
3. 删除、归档或明确标记旧 directional_bbo_confirmation.md 为 superseded。
4. 重跑 999 surrogates、formal finalizer 和独立 QA。

提交信息：
- commit：无
