# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0719T008

状态：
- 未通过

更新时间：
- 2026-07-19 23:59 CST

验收线程：
- QA验收线程

验收对象：
- implementation commit `57c4d9346836c5ae73e4c1358fc51a17b469c7dc`
- business report commit `abe8112e8dc56aa2097808e8b23a98c2ce045561`

验收范围：
- 重新攻击 T007 三个 P1，并验证 fresh manager-watcher zero-fill producer 路径。

验收步骤：
1. 审阅 task、business report、T007 QA、implementation diff 和 producer/acceptance 实现。
2. 运行四组 focused tests 和 full Hyperliquid regression。
3. 独立构造 raw fill、all-token、argv/path、checksum 和 fresh producer 对抗反例。

## Findings

1. P1：同步篡改 raw fill 成交价及全部派生证据后，acceptance 仍可通过。
   - Acceptance 只要求价格为正，没有执行限价语义校验。
   - Terminal proof 检查数量、maker role 和 reference，但不检查成交价。
   - Raw、ledger、attribution、role、summary 相互一致不足以证明成交价符合订单语义。
   - 独立反例：
     - buy intent `64000`，raw fill 改为 `70000`；
     - sell intent `66000`，raw fill 改为 `60000`；
     - 同步重建 ledger、attribution、role、summary。
   - 实际结果：`principal_task12_mechanism_and_evidence_integrity_passed`，所有 acceptance checks 均为 pass。
   - 这两个成交价均违反相应限价约束，可作为伪造 full-fill terminal proof。
2. P1：acceptance 只信任已有 checksum verification JSON，没有独立重新验证 terminal SHA-256 manifest。
   - 同步 post-seal 变异无法由现有 acceptance 阻断。

实际结果：
- raw pullback 文件缺失：blocked。
- raw pullbacks 为空但填充派生 CSV：blocked。
- raw side、quantity、role 与派生证据不一致：blocked。
- correct oid + conflicting cloid：producer 无 attribution，acceptance blocked。
- token/raw identity 冲突和 raw alias 冲突：`ValidationError`。
- unknown、duplicate、equals、multiple-mode、缩写 argv：全部 blocked。
- 同步伪造 Python、watcher script、run root、output-dir：blocked。
- 删除预制 inner artifact 后，fresh manager watcher 生成 26 个 producer 文件，2 次 mock submit、2 次 mock cancel、零 fill，完整 acceptance pass。
- Acceptance 未 import/call producer helper。
- 未执行 live/private/account/order/cancel/network/remote/service。

命令与结果：
- Focused：
  `PYTHONDONTWRITEBYTECODE=1 python -m pytest -p no:cacheprovider -q <4 focused test files>`
  结果：`269 passed in 15.29s`
- Full：
  `PYTHONDONTWRITEBYTECODE=1 python -m pytest -p no:cacheprovider -q examples/hyperliquid`
  结果：`651 passed in 35.24s`
- 独立 `/tmp` 内联 Python 对抗重放：发现上述 impossible-price acceptance pass。
- `git diff --check`、commit ancestry 和只读工作区检查通过。

验收结论：
- 未通过
- 结论说明：
  - T007 的 all-token 和 canonical argv P1 已关闭，但 raw fill 同步字段篡改仍可构造伪造 full-fill terminal proof。

通过项：
1. Missing/empty raw pullback fail closed。
2. oid/cloid all-token 和 alias/token conflict fail closed。
3. Canonical argv、runtime provenance 和 physical output root 绑定。
4. Fresh zero-fill manager-watcher producer 路径。

不通过项：
1. Raw fill price 未与订单限价执行 side-aware 独立校验。
2. Terminal checksum 未由 acceptance 独立重新计算。

缺陷清单：
1. P1 impossible-price forged full-fill terminal acceptance。
2. P1 stale checksum-summary acceptance。

阻塞项：
- 0719T008 不能关闭。
- Principal Task 12 机制/证据 gate 不能关闭。
- 新 tiny-live 和 Task 10 multi-level 继续锁定。
- stable PnL、fill-rate、maker viability、promotion 等 claims 仍 unsupported。

建议总控下一步：
1. Acceptance 和 producer 对 buy fill 强制 `fill_px <= limit_px + tolerance`，sell fill 强制 `fill_px >= limit_px - tolerance`。
2. Acceptance 独立重算 terminal SHA256 manifest，不信任已有 verification summary。
3. 增加同步篡改 raw price/fill identity、派生文件及 stale checksum JSON 的确定性反例。

提交信息：
- QA commit：无
- 文件修改：无
