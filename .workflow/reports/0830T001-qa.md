# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0830T001

状态：
- 已通过

更新时间：
- 2026-08-30 15:04 CST（星期日）

验收线程：
- QA验收线程

验收对象：
- SKHYNIX Fixed Epoch Suppression V1 Baseline
- branch `codex/fixed-causal-epoch-mstate-a-minus1`
- candidate HEAD `98d53a3e63835d296789547f33f29f3e80dc7308`
- baseline implementation `256f3d676eaaceef043e463c62ec656eca7eac42`
- accepted authority `f06eb5cb012cb62b2a778ad90d433c4083f9ba14`

验收范围：
- 验收 authority manifest、六个冻结文件、9 个 callable AST、60s fixed
  epoch constants、Git ancestry、tracked evidence snapshot、poison
  attestation、verifier、恢复文档和 recovery tag 状态。
- 复验 canonical A、Build B、poison P 的 live non-cache evidence，但不运行
  29-cache，不读取 future outcomes。
- 不修改 plan、runner、tests、正式产物、task 或 execution report。
- 本轮只写本报告和 `docs/qa-acceptance-report.md`。

严重度：
- P0: 0
- P1: 0
- P2: 0
- P3: 0

验收步骤：
1. 从 Git object database 独立复算 authority file SHA256、blob OID、
   normalized AST SHA256、constants 和 ancestry。
2. 独立读取 tracked archive，核对 member safety、25-file closure、
   canonical byte equality、run manifest 和 tree SHA256。
3. 核对 tracked poison attestation 与正式 attestation 的完整字节身份及
   29/15/435/0 语义。
4. 运行 verifier 正常路径与 working drift、archive mutation、extracted
   output mutation 三类隔离 hostile checks。
5. 运行 74-test combined regression、Ruff、隔离 py_compile，并检查 tag、
   cache exclusion 和非破坏恢复说明。

实际结果：
- 六个冻结文件在 working tree、authority commit 和 manifest 三方完全
  一致；file SHA256 与 Git blob OID 均可独立复算。
- 9 个 frozen callable normalized AST SHA256 全部匹配。
- Frozen constants 精确为：checkpoint `20ms`、epoch `60s`、core
  `[15s,45s)`、每完整 epoch `3000` checkpoints。
- `f0110d40 -> 781a7cd0 -> b583d02b -> 8463eae6 -> f06eb5cb`
  ancestry 全部成立。
- `evidence_noncache.tar.gz` 为 tracked Git blob；SHA256 为
  `c9167c59e518ac22b89c874aae80b6c2c99869a253296f73cbb21ef954ba5740`。
- Archive 含精确 25 个 regular-file artifacts，另有 3 个目录项；路径
  唯一且无 absolute/parent traversal。无 cache payload。
- Archive 25 项与 canonical 正式产物 path set 和文件 bytes 全部一致；
  difference count 为 `0`。
- Non-cache tree SHA256 为
  `2d5505b531b2da97a928284ce9fd80696d79ee9870cf290507cf37d94e63049f`；
  archive 内 run manifest 的 24 个 self-excluding entries、size 和 SHA256
  全部正确。
- Tracked poison attestation 与 `/tmp` 正式 attestation 字节一致，SHA256
  为 `3eaca61d3769f2dee6c50aea45a95109fc622b2ce557277451b504c8aca8942b`；
  记录 29 caches、15 unconsumed fields、435 changed instances、
  0 consumed mismatch。
- Source inventory 精确列出 29 个唯一 cache names；这些名称与 Git tracked
  files 零重叠，archive 中对应 cache payload 数为 `0`。
- Verifier `--check-working-tree` 加 live A/B/P roots 返回 `PASS`、
  `artifact_count=25`、`difference_count=0`。
- 隔离 hostile checks 均 fail closed：
  - frozen task drift：
    `working_tree_sha256_mismatch:.workflow/tasks/0829T003.md`
  - archive byte mutation：
    `snapshot_sha256_mismatch:.../evidence_noncache.tar.gz`
  - extracted output mutation：
    `non_cache_tree_sha256_mismatch`
- Combined baseline/current/predecessor regression 为 `74 passed`；Ruff 与
  隔离 py_compile 通过。
- `skhynix-fixed-epoch-suppression-v1` 是 annotated tag，tag object 为
  `13019e3afbbbd47d0b7dfa2cc6820d69548e0f72`，精确 peel 到
  `f06eb5cb012cb62b2a778ad90d433c4083f9ba14`。
- 恢复文档使用新 worktree，不包含 destructive reset；明确 29 个原始
  caches 不在 Git，并要求 successor 新建 plan/runner/tests 且不得修改六个
  frozen authority files。
- `skhynix-fixed-epoch-research-kit-v1` 尚未创建，符合冻结流程：应在本次
  QA 被提交后，由总控指向包含 QA 的 closure commit。
- Archive 内科学分类保持
  `Aminus1_structural_support_not_estimable`；confirmatory A0、
  exploratory A0 execution 和 future-target access 均为 `false`。

验收结论：
- 已通过
- 结论说明：
  - 0830T001 已把 0829T003 封为可恢复、可验证的研究底座，未改变其负面
    科学结果或权限边界。

通过项：
1. Git authority、frozen files、AST、constants 和 ancestry 通过。
2. Tracked 25-artifact snapshot、manifest、canonical equality 和 poison
   attestation 通过。
3. Verifier 正常路径、live triad 和三类 fail-closed mutation 通过。
4. 74-test regression、Ruff、py_compile 和 annotated authority tag 通过。
5. 恢复文档、cache exclusion、successor immutability 和 tag sequencing
   通过。

不通过项：
1. 无

缺陷清单：
1. 无

阻塞项：
- 无

建议总控下一步：
1. 提交本次 QA 两文件，并将 0830T001 更新为 `已通过`。
2. 在该 closure commit 上创建 annotated tag
   `skhynix-fixed-epoch-research-kit-v1`，随后运行 verifier
   `--require-tags` 完成最终 tag 验证。
3. 继续保持 A0、future outcome 和 live/private/order 权限关闭。

提交信息：
- commit：无
