# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0729T002

状态：
- 已通过

更新时间：
- 2026-07-29 09:48 CST

验收线程：
- QA验收线程

验收对象：
- 业务执行线程 `0729T002`
- T001 runtime run `0729T001-20260729T005823Z`
- branch `cross-exchange`，HEAD `aee70284ad67`

验收范围：
- 核验 T001 QA 的 finalizer mutation 顺序、success/failure focused
  coverage、staged diff hygiene 和 rack-level 命名修复。
- 核验 `runtime_control_source.tar.gz` 内 T001 runtime source 与 manifest
  `runtime_source_hashes`，以及当前 staged source 与
  `post_qa_repair.source_hashes`。
- 核验 T001 runtime 结果、raw state、archives 和终态 snapshot 字节未变。
- 重跑 Python/Rust/compile/shell/fmt/clippy/diff checks。
- 以 AWS 只读查询确认当前双机、六台 loser、winner protection、
  placement group 和 bucket 404。
- 本 QA 未执行任何 AWS mutation；仓库内只写入并 stage 本报告与
  `docs/qa-acceptance-report.md`。

验收步骤：
1. 读取 T002 task、business report、QA template 和完整 staged diff。
2. 审阅 `finalize_winner`、failure cleanup 与新增 focused tests。
3. 独立解包 runtime source archive，逐文件计算 SHA256 并与 manifest
   runtime source identity 比较。
4. 对当前 working tree 和 Git index 分别计算 post-QA repair source
   hashes。
5. 对 T001 compact/runtime evidence、7 个 full archives 和 running
   snapshot 重新计算并比较既有哈希。
6. 重跑 Python focused、Python compile、shell syntax、Rust
   fmt/test/clippy、worktree/staged diff checks。
7. 仅使用 AWS describe/head 类只读命令复核当前终态。

实际结果：
- finalizer 当前 mutation 顺序为：
  1. 给 winner 写 `HuntDisposition`、`RetainedByTask` 和 Name tags；
  2. 设置 `DisableApiTermination=true`；
  3. `describe-instance-attribute` 读回并强制验证为 true；
  4. 记录 `winner_protected_before_loser_cleanup`；
  5. 才允许 terminate 六台 losers、等待 terminated 并删除 bucket；
  6. 最终再次读回 winner protection 并纳入 final receipt。
- protection 首次读回不为 true 时，`finalize_winner` 在
  `terminate-instances` 前抛错；focused failure test 明确断言该操作未
  发生。任务允许外层 failure cleanup 在整体失败后清理 task candidates，
  但 protected baseline 不进入 candidate mutation 集合。
- success focused test 精确验证：
  - winner 为 `i-candidate-07`；
  - loser termination 集合恰好为 `i-candidate-01..06`；
  - winner 和 `i-awsserver1` 均不在 loser 集合；
  - protection set < protection readback < loser terminate < bucket delete；
  - active task instances 仅有 winner；
  - retained PG 为 `pg-test`、bucket discovery 为空；
  - protection-before-cleanup 为 true。
- failure cleanup regression 只把 state 中的 task candidate IDs 传给
  disable-protection/cleanup；`awsserver1` 不在该集合。
- rack test 已更名为
  `test_spread_group_is_rack_level_and_launches_labeled_candidates`，语义与
  `spread-level=rack` 一致。
- runtime source archive：
  - archive SHA256 为
    `b609a8361bb2176dd617c26e44523c73462b4b845cb7b02dc9d345b31b143480`；
  - archive 只含任务声明的 6 个 control/analyzer/remote/test 文件；
  - 6/6 文件哈希精确匹配 manifest `runtime_source_hashes`，并匹配 T001
    QA 修复前已观察到的 source identity。
- post-QA repair provenance：
  - manifest 中 6/6 `post_qa_repair.source_hashes` 与当前 working tree
    精确一致；
  - 6/6 同时与 Git index 中 staged bytes 精确一致；
  - 仅 `ec2_spread_hunt.py` 和 `test_ec2_hunt.py` 与 runtime bytes 不同，
    其余 4 文件保持 runtime identity，边界陈述准确。
- T001 runtime evidence 保持不变：
  - `hunt_results.json`
    `bafccad731391d4504e343653629fe036faec55b4839f794f222f206b43fe277`；
  - `hunt_results.csv`
    `87f0332d3e86acf64a228f3fc6e4f19e81bccc9b00a3acc5950a8b36b2a12677`；
  - `final_receipt.json`
    `32224134b6acc807462ef5c1f22615698d1ad1b73e35fac38a0709527ffa9415`；
  - raw `state.json`
    `f03abe99e42219b7301e3635fd856a00d0be56db03f1930a9d81b8a077438422`；
  - raw `candidate_instances.tsv`
    `c315eb94c2cf95536eebfaaf3a9ab8a2d8a8493434540216167caf8c47c2da02`；
  - 7/7 full archive hashes仍与 T001 `hunt_results.json` 一致；
  - `running_instances.json`
    `e03ccfe1a3159ba93f28252aa1a976626ea3277e6f7d9309e57aeecdc0f83527`；
  - tracked results/CSV/final receipt 与本地 authoritative files 字节一致。
- execution manifest 和 README 仅增加 provenance 分层说明；没有把
  T002 repaired code 冒充为 T001 runtime code。
- QA 重跑结果：
  - Python focused：`24 passed`；
  - Rust：`15 passed`；
  - Python compile：通过，pycache 定向到 `/tmp`；
  - `bash -n latency-probe/ec2_hunt_remote.sh`：通过；
  - Rust fmt：通过；
  - clippy `-D warnings`：通过；
  - `git diff --check`：通过；
  - `git diff --cached --check`：通过。
- Git scope：
  - T002 task、business、两处源码、runtime archive、manifest、README
    和 controller 文件均已 staged；
  - raw 目录继续由 `.gitignore` 排除，没有 raw 文件进入 Git；
  - 既有 `.DS_Store` 未 staged；
  - 无关 `docs/qa-acceptance-report 3.md` 仍未跟踪且未被修改。
- AWS 只读终态：
  - `awsserver1 / i-02c64c088f311cbc1 / c7i.large` running；
  - winner `i-0a962e47210528526 / c6in.xlarge` running；
  - 两台 system/instance status 均为 `ok`；
  - 区域内 running 实例恰好为上述两台；
  - 六台 T001 losers 均为 `terminated`；
  - winner `DisableApiTermination=true`；
  - retained group 为 `available / spread / rack`；
  - 临时 bucket HeadBucket 明确返回 `404 Not Found`。
- T002 是纯离线源码、测试和 provenance 修复；T001 state/evidence hash 与
  AWS 双机终态均未变化。本 QA 也只执行了 AWS read-only 查询。
- CloudTrail `LookupEvents` 权限缺失是既知审计限制，不属于 T002
  修复范围，也不影响本轮离线修复验收。

验收结论：
- 已通过
- 结论说明：
  - T001 QA 的 P1、P2、P3 均已关闭；未来 finalizer 使用先保护并读回、
    后清理 loser 的顺序，success/failure 回归和双层源码 provenance
    完整，T001 runtime evidence 与当前 AWS 双机终态保持不变。

通过项：
1. winner tag/protect/readback true 严格先于 loser terminate 和 bucket
   delete。
2. protection false 时 finalizer 不发生任何 loser mutation。
3. success test 精确覆盖六 losers、winner、`awsserver1` 排除、PG、
   bucket、唯一 active winner 和 protection。
4. runtime archive 6/6 hashes 匹配 `runtime_source_hashes`。
5. working tree/index 6/6 hashes 匹配
   `post_qa_repair.source_hashes`。
6. T001 results/raw/final receipt/full archives/running snapshot bytes 不变。
7. Python 24、Rust 15、compile/shell/fmt/clippy 全部通过。
8. worktree/staged diff checks 和 Git scope 通过。
9. AWS 双机终态只读复核通过，T002 offline-only 边界成立。

不通过项：
1. 无。

缺陷清单：
1. **P0：无。**
2. **P1：无。**
3. **P2：无。**
4. **P3：无。**

阻塞项：
- 无。

建议总控下一步：
1. 将 `0729T002` 标记为 `已通过`，以修复后的 staged controller 作为未来
   hunting 控制面基线。
2. 保持当前 winner 与 `awsserver1` 均 running；在 8 小时 burn-in 和迁移
   验收前不要 stop/start winner，也不要释放 `awsserver1`。

提交信息：
- commit：无
