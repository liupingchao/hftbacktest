# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0722T054

状态：
- 未通过

更新时间：
- 2026-07-22 11:11 Asia/Shanghai

验收线程：
- QA验收线程

验收对象：
- 业务执行线程 `0722T054`
- implementation commit `4db5fb6f061106d13f5cab16269aa1bd58926507`
- workflow/business commit `9bf370ae6551e368f9bce43e6266e6f403c78a27`

验收范围：
- 严格 offline-only 独立 code review 和 fake/local 验证。
- 覆盖 active multi-level runtime config、unknown submit stop、per-submit
  halt、normal watcher ladder/config/status、standing cap、restart provenance
  以及 single-level/T052/dynamic/fill regression。
- 审核业务报告的 exact implementation commit 和 unsupported
  live/economics/restart claims。
- 不修改实现代码、task 文件或 task 状态。

验收步骤：
1. 阅读 workflow-kit、task、business report、`task_plan.md`、`progress.md`
   和 `findings.md`，核对两个提交的完整 SHA。
2. 审查 manager submit mutation 顺序、unknown batch stop、restart recovery
   和 watcher ladder/config/status 调用链。
3. 运行任务指定 hostile subset，并增加两个独立本地反例：
   - 第二次 `before_submit` halt 后检查未提交 logical key 的 generation。
   - active ladder config hash 下检查 watcher early status 的 multi-level
     snapshot。
4. 运行 focused manager/ladder/event watcher、related watcher/shared/executor、
   full Hyperliquid、py_compile 和 implementation commit diff check。

实际结果：
- P1-1 成立：`_submit()` 在执行 `before_submit` 前调用会写状态的
  `_next_generation()`。第二次 callback halt 后只有 `1` 次 fake order
  call、`submissions_used=1`，但第二个未提交 logical key 已留下
  `generation=0` 且没有对应 order；后续重试会跳到 generation 1。该 mutation
  发生在 halt gate 前，违反任务的“mutation 不提前”，也与业务报告
  “callback failure 发生在 generation mutation 之前”的声明冲突。
- P1-2 成立：normal inline watcher 的启动、disconnect 和候选等待 status
  使用包含 active `QuoteLadderConfigV1(levels=2)` 的 config hash，但未传
  matching multi-level snapshot。`task7_status_payload()` 因而报告
  `requested_levels=1`、`activation_enabled=false` 和
  `multi_level_not_requested`。同一 status 内 config identity 与 activation
  state 不一致。
- 通过：active multi-level omitted runtime config 在 endpoint 前失败。
- 通过：首个 unresolved unknown 后仅 `1` 次 fake order call，剩余
  additions blocked，unknown leaves 保留在 working exposure。
- 通过：active 四层目标在 standing submission cap `2` 下零 order calls。
- 通过：recovered owned order 可 hold/cancel，cancel-confirmed 后 readd 被
  provenance gate 阻止；该路径未生成或复用新 cloid。
- 通过：default-off 仍输出单层；active ladder 输出两层/side；active
  multi-level 与 dynamic/fill activation 互斥。
- hostile subset：
  `6 passed in 0.11s`。
- focused manager/ladder/event watcher：
  `259 passed in 21.05s`。
- related public watcher/shared kernel/executor：
  `93 passed in 0.26s`。
- full：
  `python -m pytest examples/hyperliquid -q`
  -> `1250 passed in 51.56s`。
- py_compile：本任务 manager、watcher 和两个 focused test 文件通过。
- `git diff --check 4db5fb6^ 4db5fb6`：通过。
- 全部执行均为 local fake、静态检查或离线测试；没有 live、
  private/account、真实 order/cancel、network、remote 或 service 操作。

验收结论：
- 未通过。
- 结论说明：
  - 两个确定性 P1 分别违反 per-submit halt mutation ordering 和
    watcher status/config identity 一致性；全量回归通过不能抵消显式验收
    contract 的失败。

通过项：
1. T053 omitted-runtime-config 和 unresolved-unknown 两个 P1 已修复并有
   hostile regression。
2. standing cap `2`、default-off、active/adaptive 互斥和 restart
   hold/cancel-only gate 符合本轮边界。
3. single-level/T052/dynamic/fill 相关回归及 full Hyperliquid suite 通过。
4. 业务报告记录了 exact implementation commit，并明确不支持
   multi-level live、economics、promotion 和完整 restart readiness。
5. py_compile、diff whitespace 检查和 offline-only 边界通过。

不通过项：
1. 第二次 per-submit halt 前已提前消耗未提交 logical key 的 generation。
2. active ladder 的 watcher early status 与其 config hash 报告不同的
   multi-level activation/config identity。

缺陷清单：
1. P1：将 generation allocation/commit 移到最终 `before_submit` gate
   之后，或保证 callback failure 对 generation/order/submission 状态完全
   无 mutation，并增加精确断言。
2. P1：所有携带 ladder-aware config hash 的 watcher status 都必须使用由
   同一 `QuoteLadderConfigV1` 派生的 multi-level snapshot；启动、
   disconnect、候选等待和 terminal 状态不得退回隐式单层 snapshot。

阻塞项：
- 无环境阻塞；未通过原因是上述实现缺陷。

Unsupported claims：
- 本轮不支持 multi-level live、private/account、真实 order/cancel、
  network/remote/service、stable PnL、fill rate、fee/rebate、queue priority、
  maker viability、promotion 或 final MVP 结论。
- restart 只验收到 recovered owned-order 的 hold/cancel-only fail-closed
  路径；不支持 durable historical generation/submission provenance 或完整
  restart readiness 声明。

建议总控下一步：
1. 派发最小 offline-only repair，修复两个 P1 并加入精确 hostile
   regressions。
2. 修复经独立 QA 接受前，不关闭 Principal Task 10，也不启动
   multi-level live 或扩大 standing envelope。

提交信息：
- QA commit：无。
- Implementation commit：
  `4db5fb6f061106d13f5cab16269aa1bd58926507`
- Workflow/business commit：
  `9bf370ae6551e368f9bce43e6266e6f403c78a27`
