# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0717T007

状态：
- 已通过

更新时间：
- 2026-07-17 23:08 UTC

验收线程：
- QA验收线程

验收对象：
- 业务线程-window-attempt-identity 0717T007

验收范围：
- 验收 `0717T006` Phase 1 window/attempt identity contract。
- 检查 orchestrator -> watcher -> inline/standalone artifact -> fill/evidence row 的 identity 传播。
- 本 QA 不运行 live，不访问 credential/private/order/cancel endpoints。

验收步骤：
1. 审查任务、业务回报和实现 commits `66ba588`、`0271d99`。
2. 检查 `--artifact-window-id` CLI 和 orchestrator 每窗口参数传播。
3. 检查 stable attempt key、manifest、fill/evidence row、copied path 和 standalone attempt matrix。
4. 运行 orchestrator、fill-loop、fill-attribution 和 event-driven watcher focused tests。
5. 运行 `py_compile`、watcher `--help` 和 `git diff --check`。
6. 搜索 inline production range 的硬编码 `window_id=1` / `window_1`。

实际结果：
- Orchestrator fake windows 分别传入 `artifact_window_id=1` 和 `2`。
- Inline manifest、run intent、copied path 和 window result 使用 `window_01` / `window_02`。
- Fill、liquidity-role 和 quote-attempt rows 包含 `window_id`、`attempt_id` 和 `attempt_key`。
- Attempt key 格式为 `<task_id>:window_<two-digit-id>:attempt_<id>`，不同窗口不会冲突。
- Standalone fill-window 的多-attempt pullback 使用真实 attempt id，不依赖默认 attempt 1。
- 单窗口调用默认仍为 `artifact_window_id=1` / `window_01`。
- Focused tests: `83 passed`.
- `py_compile`、CLI help、`git diff --check` 通过。
- Inline production range 未发现硬编码窗口 1。

验收结论：
- 已通过
- 结论说明：
  - Phase 1 identity contract 已在 orchestrator、inline 和 standalone fill-window 路径闭环，可进入 Phase 2。

通过项：
1. Multi-window authority id 从 orchestrator 正确传到 watcher。
2. Manifest、fill、attempt 和 copied artifact identity 一致。
3. Single-window compatibility 保持。
4. No-live/no-private 边界保持。

不通过项：
1. 无

缺陷清单：
1. 无

阻塞项：
- 无

建议总控下一步：
1. 创建唯一下一任务 `0717T008 / IDEMPOTENT-FILL-ATTRIBUTION-REPAIR`。
2. Phase 2 使用本任务的 attempt namespace，实现 stable fill id、time-bounded fallback 和 window-level deduplication。

提交信息：
- implementation commits：`66ba588`, `0271d99`
- workflow commits：`91bcc75`, `fc57872`, `b17e7ee`
