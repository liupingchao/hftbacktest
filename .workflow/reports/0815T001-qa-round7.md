# QA 验收结果

执行线程：
- QA验收线程（第七轮全新独立验收）

任务ID：
- 0815T001

状态：
- 未通过

更新时间：
- 2026-08-15 18:12 CST（星期六）

验收线程：
- QA验收线程

验收对象：
- 业务线程-python/research + 0815T001 Round 6 bounded repair
- 正式包：
  `local_live_analysis/skhynix_trigger_aligned_episode_research_v1_stage02_density/`
- 声称 core SHA256：
  `cc6a10cc022d97a4fda01a9988a3dfd7a8c713a243d6d8eaacb6a21d309a095c`
- Build A：
  `/tmp/0815T001-r6-density-a.VfHvoG/package`
- Build B：
  `/tmp/0815T001-r6-density-b.Gc72PR/package`

验收范围：
- 独立复核 accepted Stage 1 exact resolved root 是否同时被 source
  constant、canonical contract、builder、input bindings 和 verify-only
  冻结。
- 独立检查 Stage 1 scope 的规范绝对路径、实际 Stage 1
  manifest/artifact/core/14-file full inventory revalidation。
- 在新的 `/tmp` package copies 中构造 root、path 和 metadata 一致性
  变化，并分别使用 current source CLI 和副本内 archived CLI 验证。
- 复核 Round 1-5 的 projection v2、session order、candidate/sensitivity
  coupling、small CSV closure、binding enum、Aug07 ledger exception、
  canonical report、manifest、exact float、runtime 和 boundary。
- 运行 focused tests、静态检查、formal verify-only、zero-write、
  Formal/A/B 比较，以及 123 个 source 和 14 个 Stage 1 文件的实际复算。
- 普通代码审查按 P0-P3 分级；任何影响 Stage 3 门禁可信度的问题判
  `未通过`。

验收步骤：
1. 阅读 `AGENTS.md`、workflow manual/templates、任务文件、
   business-r6、qa-round6、主实施方案、`task_plan.md`、`progress.md`
   和 `findings.md`。
2. 静态审查 current source、archived runtime、frozen contract、
   input bindings 和 Stage 1 verifier，不直接采信既有结论。
3. 在 `/tmp/0815T001-qa7-negative-e5c4hob9/` 创建独立 package copies，
   修改 payload 后刷新 artifact records 和 core hash，再由两套 CLI
   验证。
4. 独立运行 builder root 变化、Stage 1 identical clone、metadata-only
   clone 和 artifact drift 检查。
5. 运行完整回归、Ruff、外置 pycache compileall、CLI help 和
   `git diff --check`。
6. 对正式包执行 source/archive verify-only 和 zero-write；比较
   Formal/A/B 全部 relative paths、bytes 和 SHA256。
7. 使用独立 stdlib CSV/gzip/JSON/SHA 脚本流式复算 membership、
   small CSV、input bindings、Stage 1 closure、runtime 和 boundary。
8. 对 Round 6 QA 对象逐文件比较，并进行普通代码审查。

运行命令与结果：
1. `python -B -m pytest -q
   examples/hyperliquid/test_cross_exchange_trigger_density_core.py
   examples/hyperliquid/test_cross_exchange_candidate_episode_merging.py
   examples/hyperliquid/test_cross_exchange_trigger_density_inputs.py
   examples/hyperliquid/test_cross_exchange_trigger_density_admission.py`
   - `145 passed in 8.68s`
2. `python -B -m pytest -q
   examples/hyperliquid/test_cross_exchange_trigger_density_admission.py`
   - `94 passed in 2.52s`
3. `python -B -m pytest -q
   examples/hyperliquid/test_cross_exchange_liquidity_response_case_hierarchy.py
   -k 'merges_dense_clusters_without_recovery or
   stops_at_recovery_checkpoint or
   fails_closed_on_missing_recovery_evidence'`
   - `3 passed, 19 deselected in 0.04s`
4. `python -m ruff check` 对四个 Stage 2 实现和四个测试文件。
   - `All checks passed!`
5. 使用外置
   `PYTHONPYCACHEPREFIX=/tmp/0815T001-qa7-pycache.bIKuXg`
   运行 `python -m compileall -q`。
   - `rc=0`
6. 对相同八个 Stage 2 文件运行 `git diff --check`。
   - `rc=0`
7. Source/archived `--help`。
   - 均为 `26` 行、`rc=0`，输出逐字节一致。
8. Source/archived 正式包 `--verify-only`。
   - 均为 `rc=0`、`verified=true`、`artifact_count=15`、
     `candidate_membership_rows=463612`、`confirmed_rows=267554`、
     `sensitivity_membership_rows=463612`、`session_count=3`。
9. Source CLI 分别对 Build A 和 Build B 执行 `--compare-to`。
   - 两次均为 `identical=true`，core 为声称值。
10. 独立负向矩阵：
    `/tmp/0815T001-qa7-negative-e5c4hob9/`。
    - positive control：source/archive 均 `rc=0`。
    - 不存在 root、内容相同 clone root、含 `..` path、root 外 path、
      单行 root 变化、Stage 1 binding metadata 变化：
      source/archive 共 `12/12` 次 `rc=2`。
    - Stage 1 path 原始文本加入 `/./`：
      source/archive 均非预期 `rc=0`、`verified=true`。
    - Stage 1 path 原始文本加入重复 `/`：
      source/archive 均非预期 `rc=0`、`verified=true`。
11. Builder/Stage 1 clone 证据：
    `/tmp` clone 的 source/archive builder 对 identical clone 和
    nonexistent root 共 `4/4` 次 `rc=2`，且未创建输出目录。
    - identical Stage 1 clone 可复算为 `14` files，core 为 accepted
      Stage 1 core。
    - Stage 1 artifact bytes 变化被 manifest artifact closure 拒绝。
    - metadata-only manifest 变化不改变 Stage 1 artifact core，但完整
      14-file inventory 与 frozen binding 不再相等；Stage 2 的 actual
      full-inventory 比较可识别该变化。
12. 独立数据审计：
    - 全量读取 `463612` 条 candidate 和 `463612` 条 sensitivity。
    - 实际读取并重哈希全部 `123` 个 source 文件和 frozen root 下
      `14` 个 Stage 1 文件。
    - 比较 Formal/A/B 全部 `16` 个文件。

独立正向证据：
- Source constant、contract `accepted_stage1.resolved_root`、
  `verification_closure.accepted_stage1_resolved_root` 和正式 binding
  root 均精确为：
  `/Users/liu/Documents/hftbacktest-0814t001-skhynix-episode-research/
  local_live_analysis/skhynix_trigger_aligned_episode_research_v1`。
- Builder 在读取 Stage 1 前核对 resolved root；不存在 root 和
  identical clone root 均失败关闭。
- 正式包每一行 `stage1_package_path` 都是 frozen root；正式 Stage 1
  scope 的 `path` 原始文本当前均为规范绝对路径且位于该 root 下。
- Verify-only 实际调用 Stage 1 verifier，并复验：
  - 目录存在；
  - manifest artifact closure；
  - core SHA256
    `9c2756f966a1a05816f9e6a91d7361d76cfea10fc3c587944ca2f0c1d2eb7c96`；
  - 完整 `14` 文件、`1,401,387` bytes；
  - full inventory SHA256
    `c540cc056313716b3bdd2b9c0fe076cda15a7f152b399ae6a1283b3aa8aa6590`；
  - actual full inventory 与 binding before/after 精确相等。
- Source input before/after 均为 `123` unique files、
  `169,677,903` bytes、SHA256
  `94bad85bfd4981b351f84c53628099468ec27f13d308402ac7125a9d582a6644`；
  实际文件 size/SHA mismatch 为 `0`。
- Formal、Build A、Build B 均为 `16` files、`19,834,631` bytes；
  全部 relative path、bytes、逐文件 SHA256 完全一致。
- Source/archive verify-only 前后正式包文件 tuple 完全一致；无
  `__pycache__`、`.pyc` 或新增路径。
- 正式 manifest `15` 条 artifact records 与实际文件完全一致，规范
  pretty JSON 和 exact key set 均成立；独立重算 core 为声称值。
- 五个 runtime source 文件的 current source、archived bytes 和
  manifest SHA 三方一致。
- Projection version 为 `candidate_membership_projection_v2`，完整
  `19` fields 与 candidate schema 相等，session order 精确为
  `jul30 -> aug03 -> aug04`。独立重算：
  - Jul30：`268522` rows，
    `cd731b5b011183366fd3cb04e0809d082a26bde85d7f1446ff4754016c3551f9`
  - Aug03：`127622` rows，
    `04cc9a13c85e530291bcf3c584e78ea029ed67fe1653bcdd7efa13e14294fda0`
  - Aug04：`67468` rows，
    `bf5911989a07f9b1242b9eef12e72a40093549f98c516c846df6caae353db820`
- Candidate unique key duplicate、candidate/sensitivity pair mismatch、
  row-width error、window-bound error、Family A/B coupling error和 session
  re-entry 均为 `0`。
- 按 `(session_id, structural_id)` 复算 `110234` clusters、`43384`
  flows、`247` overlap blocks；跨 segment/epoch 和 cluster-flow parent
  mismatch 均为 `0`。
- Small CSV exact primary-key/cardinality/row-width：
  - density：`6/6`；
  - inter-trigger：`18/18`；
  - episode summary：`3/3`；
  - sensitivity summary：`36/36`；
  - ESS：`74/74`；
  - missing、extra、duplicate 和 row-width error 均为 `0`。
- Source verifier 对正式包的 canonical report、exact small-summary
  text/float、binding phase/scope、Aug07 control exception 和 boundary
  检查均通过；永久回归覆盖这些 Round 1-5 修复。
- 相对 Round 6 QA 对象
  `/tmp/0815T001-r5-density-a.hwOreI/package`，仅以下 `3` 个文件变化：
  `density_manifest.json`、`frozen_density_contract.json`、
  `runtime_source/cross_exchange_trigger_density_admission.py`。
  研究数值、membership、sensitivity、density、merging 和 ESS artifacts
  均未变化。

验收结论：
- 未通过
- 结论说明：
  - Round 6 的 P1 已关闭：accepted Stage 1 exact resolved root 已成为
    source、contract、builder、binding 和 verify-only 的外部不变量；
    不存在 root、clone root、root 外 path、`..` path 和 actual inventory
    drift 均失败关闭。
  - 但 Stage 1 `path` 的“规范绝对路径文本”仍未精确冻结。
    `Path(row["path"])` 会先把 `/./` 和重复 `/` 归一化，后续比较只看到
    规范化后的 `Path` 对象。两种非规范原始文本均可 coherent-rehash 后
    获得 `verified=true`。
  - 该问题不改变实际文件目标，也不能逃出 frozen root，因此定级 P2
    而非 P1；但它违反本轮明确承诺的 canonical absolute path contract，
    使同一 provenance 接受多个非规范 package identities。按本任务
    Stage 3 门禁规则，本轮仍必须判 `未通过`。

通过项：
1. Round 6 P1 的 exact root、builder root gate、actual Stage 1
   manifest/core/full-inventory revalidation 已独立关闭。
2. Round 1-5 的 projection、order、small CSV、coupling、report、
   manifest、exact float、runtime、inventory 和 boundary 均通过。
3. 正式包、Build A、Build B 逐文件一致；source/archive verify-only、
   zero-write、tests 和静态检查全部通过。
4. 当前正式包自身的 Stage 1 path 文本均规范，未发现实际 root、内容、
   研究数值或边界漂移。

不通过项：
1. P2：Stage 1 binding path 的原始 canonical text closure fail-open。

缺陷清单：
1. P0：无。
2. P1：无；Round 6 P1 已关闭。
3. P2：Stage 1 path 原始文本未被精确校验。
   - 文件/行号：
     `examples/hyperliquid/cross_exchange_trigger_density_admission.py:
     2018-2031`。
   - 原因：
     `Path(row["path"])` 在检查前归一化 `.` 和重复分隔符；
     `str(path) == str(path.resolve())` 与最终 `Path` equality 因此无法
     观察原始 CSV 文本漂移。
   - 合同冲突：
     `_canonical_contract()` 的 `accepted_stage1.binding` 明确声明
     `canonical_resolved_root_and_absolute_paths`，业务报告也声称每条
     Stage 1 scope path 都是 canonical absolute text。
   - 独立复现：
     `/tmp/0815T001-qa7-negative-e5c4hob9/
     dot_component_stage1_path/package` 和
     `duplicate_separator_stage1_path/package`。
   - 结果：
     两个 package 均刷新了 artifact records/core；source/archive
     `4/4` 次 `rc=0`、`verified=true`。新 core 分别为
     `0930c2b8660a28bec5d59cc0980814de7303178560448396a546678a78c4ce7c`
     和
     `b80d6a6f0518821bb0ba12cbca48c3a90134cd95b96f216650cb60ee16aa7c2c`。
   - 回归缺口：
     当前测试覆盖 relocated root 和 `..`，没有覆盖 raw `/./` 或
     duplicate separator。
   - 修复建议：
     先从 frozen root 和实际 14-file inventory 生成每行唯一 expected
     canonical path text，再要求 `row["path"] == expected_text`；不要只
     比较已经由 `Path` 归一化的对象。增加 production-sized `/./`、
     duplicate separator 和等价冗余文本回归。
4. P3：无。

边界结论：
- 正式 manifest 七项 boundary 全部为 `false`。
- 正式 bindings 中唯一 Aug07 路径是 accepted Stage 1
  `consumption_ledgers/aug07_access_ledger.json` 的 before/after 两行；
  scope、role、root、relative path、bytes 和 SHA 均正确。
- Ledger 记录 `event_rows_opened=false`、`event_row_read_count=0`。
- Membership headers 没有 response/outcome/markout/PnL/score/fill/
  actionability 字段；source bindings 没有 Aug07 event-row 路径。
- 本轮 QA 未读取 Aug07 raw/R0/R1/basis event rows，未运行 response、
  outcome、model、score、PnL、actionability 或 private/order/cancel
  工作。
- 本发现仅证明 canonical path 文本语法可接受等价非规范表示，不表示
  当前正式包读取了错误文件或越过了研究边界。
- Stage 3 detector parity、Episode v3、outcome/model/actionability 和
  Aug07 event-row first-read 继续锁定。

建议总控下一步：
1. 保持 `0815T001` 为 `未通过`，不得解锁 Stage 3。
2. 在同一任务范围内补齐 raw canonical path text 的 exact comparison，
   不重跑研究计算。
3. 修复后重新发布 manifest/contract/archived verifier，并由第八轮新的
   独立 QA 复核；其余已通过项只需做无回归确认。

报告文件一致性：
- 本报告与 `docs/qa-acceptance-report.md` 使用同一正文机械复制。
- 发布后执行 `cmp -s` 和 `shasum -a 256` 验证 byte-identical。
- 为避免文件自嵌自身哈希造成自引用变化，最终 SHA256 记录在 QA 完成
  回复中。

提交信息：
- commit：无
