# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0617T006

状态：
- 已通过

更新时间：
- 2026-06-17 23:16 CST

验收线程：
- QA验收线程

验收对象：
- 业务线程-research 0617T006

验收范围：
- `Hyperliquid tiny-live read-only optimistic PnL proxy`。
- 本轮验收 `0617T006` 业务报告、任务文件、optimistic PnL proxy runner、focused test、manifest、sample-set reconciliation、fixed-horizon / oracle summary、row-level audit、设计文档和 no-live/no-order/no-private/no-real-PnL 边界。
- 不验收真实 Hyperliquid 私有接口、账号、凭证、签名、nonce、user stream、下单、撤单、改单、live bot、真实 fill、真实 PnL、手续费/返佣结算、queue priority、post-only reject 行为或 maker viability。

验收步骤：
1. 阅读 `.workflow/tasks/0617T006.md` 和 `.workflow/reports/0617T006-business.md`。
2. 复核 `docs/hyperliquid_tiny_live_optimistic_pnl_proxy.md`、`optimistic_pnl_proxy_manifest.json`、`sample_set_reconciliation.csv`、`diagnostic_interpretation.csv` 和关键 CSV 行数。
3. 复跑 focused test、optimistic PnL proxy generator、JSON manifest validation 和 `git diff --check`。
4. 检查 manifest boundary flags 和报告文本，确认未越界到 live/order/private/real-PnL/fill/maker-viability claim。

实际结果：
- 指定解释器 `/home/liushuai/workspace/hftbacktest/.conda-envs/hft-py38/bin/python` 在本机不存在；本轮记录实际使用 `/home/molly/anaconda3/bin/python`，`Python 3.13.5`。
- `python -m pytest examples/hyperliquid/test_hyperliquid_tiny_live_optimistic_pnl_proxy.py -q` 通过，结果为 `1 passed`。
- `python examples/hyperliquid/hyperliquid_tiny_live_optimistic_pnl_proxy.py` 通过并重新生成 optimistic PnL proxy artifacts。
- `python -m json.tool local_live_analysis/hyperliquid_tiny_live_optimistic_pnl_proxy_0617T006/optimistic_pnl_proxy_manifest.json` 通过。
- `git diff --check` 通过。
- `optimistic_pnl_proxy_manifest.json` 记录 `official_sample_set=canonical_7`、`final_recommendation=hyperliquid_tiny_live_optimistic_pnl_proxy_ready_for_qa`、`assumption_set=unconstrained_all_intents`。
- `sample_set_reconciliation.csv` 明确记录 `requested_six` 已被用户选择的 `canonical_7` 口径替代，`canonical_7` 为 official computed sample set，`0617T005_8_input` 为 diagnostic comparison set。
- `canonical_7` 计算规模为 `139914` pricing rows、`23353` decision rows；`75` ticks / persistence `2` 下有 `7238` eligible decisions，`581` buy / `6657` sell。
- `diagnostic_interpretation.csv` 中 `canonical_7` 在 `75` ticks / persistence `2` / `1000ms` 下为 `materially_positive`，`7/7` samples positive，固定 horizon 乐观 proxy 为 `295.985 USDC`，mean `40.932789` ticks per intent。
- `fixed_horizon_pnl_summary.csv`、`aggregate_fixed_horizon_pnl_summary.csv`、`oracle_best_horizon_summary.csv`、`row_level_audit_sample.csv`、`sample_set_membership.csv`、`sample_set_reconciliation.csv` 均非空。
- Oracle-best-horizon 输出标记为 `non_tradeable_oracle_upper_bound`；`canonical_7` 在 `75` ticks / persistence `2` 下为 `7238` intents、`1098.535 USDC` optimistic proxy，但不得解释为可交易策略结果。
- boundary flags 显示 credentials、private endpoint、account query、order placement/cancel/amend、live bot、real fill、real PnL、fill probability、queue priority、maker viability claim 均为 false。

验收结论：
- 已通过
- 结论说明：
  - `0617T006` 满足 read-only optimistic public-data PnL proxy 验收要求；`canonical_7` 作为正式口径下的理论上界为 materially positive，但这仍不证明真实成交、真实 PnL、执行质量或 live readiness。

通过项：
1. 样本集口径已按用户确认切换为 `canonical_7`，并保留 `requested_six` superseded 和 `0617T005_8_input` diagnostic comparison 记录。
2. 输出包含 machine-readable manifest、sample-set reconciliation、fixed-horizon PnL summary、oracle upper-bound summary、row-level audit 和解释文档。
3. 主公式、side mapping、threshold/persistence、tick/size/USDC conversion 与任务要求一致。
4. 官方 `canonical_7` 主参数结果为 `materially_positive`，并按 sample / side / threshold / persistence / horizon 输出。
5. no-private/no-credential/no-account/no-order/no-live/no-real-fill/no-real-PnL/no-maker-viability 边界成立。

不通过项：
1. 无

缺陷清单：
1. 无

阻塞项：
- 无

建议总控下一步：
1. 将 `0617T006` 作为已验收的 read-only optimistic upper-bound evidence 使用，正式样本口径为 `canonical_7`。
2. 后续如果进入 execution / live 相关任务，仍必须单独验收成交概率、排队、post-only reject、手续费/返佣、账户库存、真实 PnL 和 live authorization 边界。
3. 不要把 oracle-best-horizon 结果解释为可交易策略表现。

提交信息：
- commit：f577b8d
