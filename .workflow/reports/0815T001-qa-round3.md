# QA 验收结果

执行线程：
- QA验收线程（第三轮全新独立验收）

任务ID：
- 0815T001

状态：
- 未通过

更新时间：
- 2026-08-15 15:10 CST（星期六）

验收线程：
- QA验收线程

验收对象：
- 业务线程-python/research + 0815T001 Round 2 bounded repair
- 正式包：
  `local_live_analysis/skhynix_trigger_aligned_episode_research_v1_stage02_density/`
- 业务声称 core SHA256：
  `6ee9bc7f78d484b42243f74d9973ecded8062136a9b5ab741fed66c42735353a`
- 隔离构建：
  `/tmp/0815T001-r2-density-a.5tyH0q/package`
  和 `/tmp/0815T001-r2-density-b.wWx3mh/package`

验收范围：
- 独立复现 Round 2 P1：五个 small CSV 的 duplicate-before、
  duplicate-after、extra-unique-key 和 missing-key coherent-rehash 攻击。
- 独立确认 density、inter-trigger、episode summary、sensitivity summary
  和 ESS 的完整 expected key sets，尤其 structural、session-total
  Bartlett 和 per-segment Bartlett ESS keys。
- 重新确认 Round 1 的完整 session 删除、provenance omission、
  recovery/decision category 伪造、非法 evidence taxonomy 和 Jul30
  segment role 漂移仍 fail closed。
- 运行 Stage 2 focused tests、admission suite、reused hierarchy recovery
  regressions、Ruff、compileall、CLI help、source/archived verify-only、
  archived 零写入和 `git diff --check`。
- 比较 formal/Build A/Build B 的全部 relative paths、bytes 和 SHA256；
  实际重哈希 source bindings 与 accepted Stage 1 inventory。
- 检查 Aug07、response/outcome/model/PnL/actionability/private/order/cancel
  边界，并继续 code review 寻找新的 P0-P3。

验收步骤：
1. 阅读 workflow-kit、任务文件、前两轮 QA、Round 2 业务报告、主实施方案
   及 controller tracking files，不采用业务结论作为验收事实源。
2. 审查 production `verify_package()` 和新增 primary-key closure，独立构造
   production-sized package 副本并同步刷新 artifact/core hashes。
3. 对五个 small CSV 各执行 duplicate forged row 位于合法行之前、之后，
   extra unique key 和 missing key；source/archived CLI 分别验证。
4. 对 ESS 额外抽掉 structural、session-total Bartlett 和 per-segment
   Bartlett key，检查精确 missing-key 拒绝。
5. 复跑 Round 1 五类 coherent-rehash 攻击并核对首个拒绝原因。
6. 运行完整回归、静态检查、CLI、零写入和三包/输入 inventory 验证。
7. 将当前正式 membership 与三个 frozen trigger audit 逐行对账，并继续
   对 candidate-level 字段和 CSV row grammar 做 hostile code review。

实际结果：
- 正式包 source CLI 与 archived CLI `--verify-only` 均返回 `rc=0`：
  `artifact_count=15`、`candidate_membership_rows=463612`、
  `confirmed_rows=267554`、`sensitivity_membership_rows=463612`、
  `session_count=3`、`verified=true`，core SHA256 与业务声称一致：
  `6ee9bc7f78d484b42243f74d9973ecded8062136a9b5ab741fed66c42735353a`。
- 五个 small CSV 当前 row/unique/expected key counts 精确为：
  density `6/6/6`、inter-trigger `18/18/18`、episode summary `3/3/3`、
  sensitivity summary `36/36/36`、ESS `74/74/74`；missing/extra 均为 `0`。
- ESS expected key set 独立构造结果：
  structural session-total keys `9`、population session-total Bartlett keys
  `6`、per-segment Bartlett keys `38`，总 ESS keys `74`。
- Round 2 P1 的 production-sized small-CSV 攻击为 `20/20` fail closed。
  两个 CLI 的 `rc`、首个错误和目标 invariant 完全一致，没有被 artifact
  hash、core hash 或 runtime-source drift 抢先拒绝。
- 20 组攻击根目录：
  `/tmp/0815T001-qa3-smallcsv-zqelhmnt/`。
  - density：
    `density-duplicate_before/package` 和
    `density-duplicate_after/package` 均返回
    `ERROR: duplicate density primary-key rows`；
    `density-extra_unique/package` 返回
    `density primary-key closure drift ... forged_population`；
    `density-missing_key/package` 返回缺失
    `('jul30','family_a_all_candidates')`。
  - inter-trigger：
    `inter_trigger-duplicate_before/package` 和
    `inter_trigger-duplicate_after/package` 均返回
    `ERROR: duplicate inter_trigger primary-key rows`；
    `inter_trigger-extra_unique/package` 返回 extra
    `forged_relation`；
    `inter_trigger-missing_key/package` 返回缺失 Jul30 Family A `all`。
  - episode summary：
    `episode_summary-duplicate_before/package` 和
    `episode_summary-duplicate_after/package` 均返回
    `ERROR: duplicate episode_summary primary-key rows`；
    `episode_summary-extra_unique/package` 返回 extra `forged_session`；
    `episode_summary-missing_key/package` 返回缺失 `jul30`。
  - sensitivity summary：
    `sensitivity_summary-duplicate_before/package` 和
    `sensitivity_summary-duplicate_after/package` 均返回
    `ERROR: duplicate sensitivity_summary primary-key rows`；
    `sensitivity_summary-extra_unique/package` 返回 extra
    `forged_sensitivity`；
    `sensitivity_summary-missing_key/package` 返回缺失 Jul30 Family A
    `impact_ge_050`。
  - ESS：
    `effective_support-duplicate_before/package` 和
    `effective_support-duplicate_after/package` 均返回
    `ERROR: duplicate effective_support primary-key rows`；
    `effective_support-extra_unique/package` 返回 extra `forged_metric`；
    `effective_support-missing_key/package` 返回缺失 Jul30 Family A
    `raw_candidate_count`。
- ESS 三类关键 missing-key 攻击位于
  `/tmp/0815T001-qa3-round1-4cekrs4l/`：
  - `ess-missing-structural/package`：两个 CLI 均 `rc=2`，精确报告缺失
    Jul30 structural `complete_time_block_count_60s`。
  - `ess-missing-session-total-bartlett/package`：两个 CLI 均 `rc=2`，
    精确报告缺失 Aug03 Family B session-total Bartlett。
  - `ess-missing-per-segment-bartlett/package`：两个 CLI 均 `rc=2`，
    精确报告缺失 Aug04 Family A `segment_0001` Bartlett。
- Round 1 五类攻击复验全部 fail closed，复现根目录同为
  `/tmp/0815T001-qa3-round1-4cekrs4l/`：
  - `drop-complete-aug04/package`：删除两个 membership 和所有 small CSV
    的 Aug04，并同步修改 manifest counts/hashes；两个 CLI 均 `rc=2`，
    `density primary-key closure drift`，缺失 Aug04 Family A/B。
  - `omit-provenance-rows/package`：删除 Jul30 trigger manifest 与 Stage 1
    `data_admission/data_admission.md` before/after rows，并同步刷新 inventory/
    artifact/core hashes；两个 CLI 均 `rc=2`，
    `frozen provenance inventory contract drift`。
  - `forge-recovery-decision-categories/package`：三 session 写入总数守恒的
    forged recovery/decision maps；两个 CLI 均 `rc=2`，
    `frozen episode-merging result drift`。
  - `forge-illegal-evidence-taxonomy/package`：恢复非法 Jul30/Aug03
    aggregate labels；两个 CLI 均 `rc=2`，精确命中
    `trigger_density.evidence_label drift`。
  - `forge-jul30-segment-role/package`：把 Jul30 `segment_0005` 从
    `historical_internal_validation` 改为允许枚举内的
    `historical_discovery`；两个 CLI 均 `rc=2`，精确命中
    `frozen_density_contract...segment_0005: canonical value drift`。
- 完整 Stage 2 focused suite：
  `python -B -m pytest -q`
  四个 Stage 2 test files，结果 `95 passed in 7.49s`。
- Admission suite：
  `python -B -m pytest -q
  examples/hyperliquid/test_cross_exchange_trigger_density_admission.py`，
  结果 `44 passed in 1.05s`。
- Reused hierarchy recovery regressions：
  dense no-recovery merge、accepted recovery checkpoint、missing recovery
  fail-closed，结果 `3 passed in 0.05s`。
- Ruff 对四个实现和四个测试返回 `All checks passed!`。
- compileall 使用外置
  `/tmp/0815T001-qa3-pycache.kPzx6a`，返回成功。
- Source/archived CLI help 各 `26` 行且 `cmp` 相同；验收前
  `git diff --check` 返回 `0`。
- Archived CLI 正式包零写入：
  before/after 均为 `16` files、`19,819,261` bytes，QA tuple-list digest
  均为
  `e4e066f4f3bc81c709fe59969f24aaaadffedd1978c9416ccecbee0ae408cc84`；
  mismatch `0`，无 `__pycache__`、`.pyc` 或新增路径。
- Formal、Build A、Build B 均为 `16` files、`19,819,261` bytes；
  全部 relative path、bytes、逐文件 SHA256 mismatch 为 `0`，manifest
  core SHA256 均为
  `6ee9bc7f78d484b42243f74d9973ecded8062136a9b5ab741fed66c42735353a`。
  Source CLI 对 A/B 的 `--compare-to` 均返回 `identical=true`。
- 按 `input_bindings.csv` 实际重哈希：
  - source before/after 均为 `123` unique files、`169,677,903` bytes、
    inventory SHA256
    `94bad85bfd4981b351f84c53628099468ec27f13d308402ac7125a9d582a6644`；
  - accepted Stage 1 before/after 均为 `14` unique files、
    `1,401,387` bytes、inventory SHA256
    `c540cc056313716b3bdd2b9c0fe076cda15a7f152b399ae6a1283b3aa8aa6590`；
  - missing、size mismatch、SHA mismatch 均为 `0`；
  - Stage 1 core SHA256 保持
    `9c2756f966a1a05816f9e6a91d7361d76cfea10fc3c587944ca2f0c1d2eb7c96`。
- 当前正式 candidate membership 与三个 frozen trigger audit 逐行独立
  对账：Jul30 `268522`、Aug03 `127622`、Aug04 `67468`；candidate ID、
  primary flag、rejection reason、aggressor side、direction、shock/decision
  timestamp、impact ratio mismatch 均为 `0`。
- 当前正式包所有 plain/gzip CSV 的 unnamed extra-cell rows 和 missing
  cells 均为 `0`；两个 membership 各 `463612` rows。
- 当前 membership schemas 没有 response/outcome/markout/PnL/model/score/
  action/private/account/order/cancel/fill 字段。Source binding roles 仅为
  frozen trigger audit/manifest、timeline、public collector manifests 和
  accepted Stage 1 package；source-input 精确 forbidden-path policy 命中
  `0`。Manifest boundary 七项均为 `false`。
- 尽管当前正式字节与 source 对账正确，继续 code review 发现一个新 P1
  和一个新 P2；两者均可在同步刷新所有 package hashes 后被 source CLI
  与 archived CLI 以 `verified=true` 接受。

验收结论：
- 未通过
- 结论说明：
  - Round 2 small-CSV primary-key/cardinality 修复已被独立确认关闭，
    Round 1 缺陷也仍按目标 invariant fail closed；当前正式包的数值、
    source 对账、构建确定性、输入 immutable 和范围边界均有充分正向证据。
  - 但 production verify-only 未把 candidate-level authoritative trigger
    projection 精确锚定到外部 frozen identity，任意 rejection category 和
    threshold 内 impact ratio 可被篡改后放行；同时 CSV reader 未拒绝数据
    行的 unnamed trailing cells。正式包仍存在 coherent-rehash verifier
    fail-open，因此不能作为后续 Stage 3 门禁。

通过项：
1. 五个 small CSV 的 exact key set/cardinality 修复通过全部 `20/20`
   production-sized coherent-rehash 攻击；duplicate before/after、extra
   unique key 和 missing key 均精确 fail closed。
2. ESS structural、session-total Bartlett、per-segment Bartlett key sets
   精确正确，三类定向 missing key 均 fail closed。
3. Round 1 完整 session、provenance、recovery/decision categories、
   taxonomy 和 Jul30 segment role 攻击均继续 fail closed。
4. Focused tests、admission suite、recovery regressions、Ruff、compileall、
   CLI help、source/archived verify-only 和 diff check 全部通过。
5. Formal/A/B 全部 paths/bytes/SHA 一致；archived CLI 零写入；source 与
   accepted Stage 1 实际文件重哈希完全匹配。
6. 当前正式 membership 与 authoritative trigger audit 的关键投影逐行
   一致，当前 CSV row width 也干净。
7. 本轮未打开 Aug07 event rows，未运行 response/outcome/model/PnL/
   actionability，未访问 private/account/order/cancel endpoint。

不通过项：
1. P1：candidate-level trigger-audit projection verification fail-open。
2. P2：CSV unnamed trailing-cell schema verification fail-open。

缺陷清单：
1. P0：无。
2. P1：candidate-level authoritative projection 未被 production
   verify-only 精确锚定。
   - 文件/行号：
     `examples/hyperliquid/cross_exchange_trigger_density_admission.py:2020-2202`
     仅验证 candidate 字段的局部关系和派生 sensitivity；
     `:2929-3006` 只验证 frozen provenance inventory identity；
     `:3039-3040` 随后直接验证 package membership。没有外部冻结的
     per-session candidate projection digest/row fingerprint，也没有在
     verify-only 中重放 trigger audit。
   - 复现 A：
     `/tmp/0815T001-qa3-new-hostile-tubjl4id/forged-rejection-reason/package`。
     将 candidate `jul30:segment_0001:6` 的 `rejection_reason` 从
     `same_direction_dedup_50ms` 改为 `forged_rejection_reason`，保持所有
     其他行和 derived summaries 不变，并同步刷新 artifact/core hashes。
     Source CLI 与 archived CLI 均返回 `rc=0`、`verified=true`，core
     SHA256
     `b45f5ec20f1f39f5790e66374015acf20b4ea1194318e137fb72c9409bc64ecd`。
   - 复现 B：
     `/tmp/0815T001-qa3-impact-drift-z46z9z5b/package`。
     将 candidate `jul30:segment_0001:1` 的 `impact_ratio` 从
     `7.516129032258064` 改为 `7.517129032258064`；该变化保持
     `impact_ge_050/070` membership 不变。同步重哈希后两个 CLI 均
     `rc=0`、`verified=true`，core SHA256
     `e816a1e01c06af847631231f2e3b69c714bd4f02088689cde30a1db8f3a1677b`。
   - 复现命令形态：
     独立 stdlib 脚本复制正式包、重写
     `candidate_episode_membership.csv.gz`、重算 manifest artifacts/core，
     然后分别运行：
     `python -B examples/hyperliquid/cross_exchange_trigger_density_admission.py
     --verify-only --output-dir <hostile-package>`
     与包内 archived CLI 的同一命令。
   - 影响：
     业务合同明确 trigger audit 对 rejection reason、candidate identity、
     severity 和 timing 为 authoritative，且不得重解释 rejection reasons。
     当前 verifier 只能证明篡改后的行内部自洽，不能证明它仍是 frozen
     trigger audit 的精确投影。后续 sensitivity、Episode v3 或审计消费方
     可能读取被伪造的分类或 severity。
   - 修复建议：
     在 runtime source/canonical contract 外部冻结每 session 的完整
     candidate projection row count 和 canonical SHA256，字段至少覆盖
     candidate ID、primary flag、rejection reason、side/direction、
     shock/decision timestamp、impact ratio、pre-state identity和 segment；
     verify-only 必须从 membership 重算并精确比较。补充 rejection reason、
     impact ratio、timestamp/pre-state 等单行 coherent-rehash regressions。
3. P2：CSV 数据行 row width 未被精确验证，unnamed trailing cell 可进入
   verified package。
   - 文件/行号：
     `examples/hyperliquid/cross_exchange_trigger_density_admission.py:488-495`
     只比较 `DictReader.fieldnames`，没有拒绝 `None in row` 或缺失 cell；
     `:2765-2780` 的五个 small CSV 均经该 helper 读取。
     Membership readers `:2832-2840` 也只比较 header fieldnames。
   - 复现：
     `/tmp/0815T001-qa3-extra-cell-wurpxql4/density-extra-cell/package`。
     保持 `trigger_density_by_session.csv` header 不变，在首个合法数据行
     末尾附加额外 cell `future_outcome=forged`，同步重算 artifact/core
     hashes。Source CLI 与 archived CLI 均返回 `rc=0`、`verified=true`，
     core SHA256
     `3fa4f23c54406c9c6ff0b34df7c589d8cef1c4983cb61d4437c94be6778d9289`。
   - 影响：
     artifact 可包含 header schema 之外的未验证 payload；不同 CSV
     consumer 对超宽行可能忽略、错位或暴露额外值，破坏 exact schema 和
     fail-closed consumption。当前正式包本身没有此类行，但 verifier
     不能拒绝 coherent-rehash 变体。
   - 修复建议：
     所有 plain/gzip CSV reader 对每行要求 key set 精确等于 header、
     `None not in row` 且所有 expected fields 的值不为 `None`；将该检查
     应用于 input bindings、五个 small CSV 和两个 membership，并增加
     leading/middle/trailing extra cell 与 missing cell 回归。
4. P3：无。

阻塞项：
- 无。两个缺陷均可在 0815T001 范围内 bounded repair。

边界结论：
- 当前正式包本身没有 Aug07 event rows、response/outcome、model、score、
  PnL、actionability、private/account/order/cancel 数据或执行。
- 当前 source bindings 与 accepted Stage 1 均只读且实际 SHA 未变。
- 由于 P1/P2 verifier fail-open，Stage 3 detector parity、Episode v3、
  outcome/model/actionability 和 Aug07 event-row first-read 继续锁定。

建议总控下一步：
1. 保持 `0815T001` 为 `未通过`，不得消费当前 Stage 2 包作为后续正式门禁。
2. 增加外部冻结的完整 candidate projection canonical digest，并在
   source/archived verify-only 中强制比较。
3. 为所有 CSV 实施 exact row-width/cell closure，补充 P1/P2 的
   production-sized coherent-rehash 永久回归。
4. bounded repair 完成后由新的独立 QA 线程重新验收，不自动沿用本轮结论。

提交信息：
- commit：无
