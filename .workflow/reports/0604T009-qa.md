# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0604T009

状态：
- 已通过

更新时间：
- 2026-06-04 13:46 CST

验收线程：
- QA验收线程

验收对象：
- 业务线程-research + 0604T009

验收范围：
- `0604T009` 规定的 canonical signal + horizon/regime read-only synthesis runner、focused tests、task-scoped synthesis artifacts，以及 candidate/watch/reject 定义是否满足显式 allowlist、short-horizon、non-canonical input refusal 和 no-maker-action 边界。

验收步骤：
1. 阅读 `.workflow/tasks/0604T009.md` 与 `.workflow/reports/0604T009-business.md`，核对任务边界、验收标准、业务结论与提交信息。
2. 抽查 commit `b6bbd83` 与 `fb02bf4`，确认改动范围只包含 T009 task/report、runner/test 与 task-scoped artifacts，以及业务报告提交字段回填。
3. 复跑最小验证命令：`--help`、`py_compile`、focused pytest、相关 canonical focused pytest 合集。
4. 对 accepted canonical inputs 运行 synthesis runner 到 `/tmp/qa_0604T009_regime_synthesis/`。
5. 解析 `/tmp/qa_0604T009_regime_synthesis/canonical_regime_synthesis_manifest.json` 与正式 `local_live_analysis/canonical_regime_synthesis_0604T009/canonical_regime_synthesis_manifest.json`。
6. 对 QA 复现产物与正式产物同时运行 scripted boundary check，确认 candidate 仅允许 `binance_mid_move_ticks_from_prev` 为 primary anchor，且没有 `100/250ms` promoted candidate。
7. 运行 `git diff --check`，确认无格式问题。

实际结果：
- `git show --stat --summary b6bbd83` 确认提交为 `0604T009 add canonical regime synthesis`，改动仅包含 `.workflow/tasks/0604T009.md`、`.workflow/reports/0604T009-business.md`、`canonical_regime_synthesis.py`、focused tests 与 `local_live_analysis/canonical_regime_synthesis_0604T009/` task-scoped artifacts。
- `git show --stat --summary fb02bf4` 确认只回填了业务报告中的 `commit id` 与 `提交信息` 字段。
- `python examples/hyperliquid/canonical_regime_synthesis.py --help` 通过。
- `python -m py_compile examples/hyperliquid/canonical_regime_synthesis.py examples/hyperliquid/test_canonical_regime_synthesis.py` 通过。
- `python -m pytest examples/hyperliquid/test_canonical_regime_synthesis.py` 通过，结果为 `6 passed`。
- `python -m pytest examples/hyperliquid/test_canonical_event_mode_evidence.py examples/hyperliquid/test_canonical_signal_quality_ranking.py examples/hyperliquid/test_canonical_horizon_regime_diagnostics.py examples/hyperliquid/test_canonical_regime_synthesis.py` 通过，结果为 `28 passed`。
- QA 复跑 runner 到 `/tmp/qa_0604T009_regime_synthesis/`，结果 `canonical_sample_count=3`、`candidate_count=1`。
- `python -m json.tool /tmp/qa_0604T009_regime_synthesis/canonical_regime_synthesis_manifest.json` 与正式 manifest 解析均通过。
- Scripted official-vs-QA artifact consistency check 通过：两套产物均为 `candidate_count=1`、`summary_count=18`，classification counts 一致：`candidate_for_milestone3_executability=1`、`watch_needs_more_samples=6`、`reject_unstable_direction=9`、`reject_concentrated_or_aliased=2`。
- Candidate boundary check 通过：`candidate_rows 1`，`bad []`；唯一 candidate 的 `primary_anchor_feature=binance_mid_move_ticks_from_prev`，`horizon_ms=1000`，secondary context fields 仅为 `binance_top5_imbalance|binance_top5_bid_qty|binance_microprice_minus_mid_ticks`，context fields 仅为 `hyperliquid_context_quality|hyperliquid_join_age_bucket|hyperliquid_spread_bucket`。
- QA 未发现 candidate definitions 中出现 maker side、quote placement、order behavior、strategy action、default-on、tiny-live 或 promotion 文本。
- `git diff --check` 通过。

验收结论：
- 已通过
- 结论说明：
  - `0604T009` 满足任务边界与验收标准：runner 只消费 accepted canonical evidence，候选定义严格受 allowlist 和 decision-time-visible context 约束，短周期 `100/250ms` 未被 promoted，且输出只将 `regime_011_1000_spread_10_20_ticks` 作为后续 Milestone 3 executability assessment 的 read-only candidate，而未越界到 maker action 或策略行为。

通过项：
1. Runner 通过 accepted loader/source-lock path 工作，并拒绝 non-canonical / diagnostic-only synthetic input。
2. Candidate/watch/reject outputs 字段齐全，包含 row count、sample count、direction consistency、sample concentration、effective future-row-delta support、horizon support 和 reason。
3. 唯一 promoted candidate 使用正确 primary anchor：`binance_mid_move_ticks_from_prev`。
4. `100/250ms` short-horizon rows 保持 watch，不存在 short-horizon promoted candidate。
5. Secondary features `binance_top5_imbalance`、`binance_top5_bid_qty`、`binance_microprice_minus_mid_ticks` 只作为 context，不作为 promoted primary anchor。
6. 边界保持正确：无 new collection、maker side、quote behavior、order behavior、strategy/private/order/live/default-on/tiny-live/parameter search/promotion 越界修改。

不通过项：
1. 无

缺陷清单：
1. 无

阻塞项：
- 无

建议总控下一步：
1. 将 `0604T009` 记为 `已通过`。
2. 后续如继续 Milestone 3，应把 `regime_011_1000_spread_10_20_ticks` 仅当作 executability assessment 输入，而不是策略动作授权。

提交信息：
- commit：b6bbd83（实现与 artifact）；fb02bf4（业务报告回填）
