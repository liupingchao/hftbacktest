# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0717T010

状态：
- 已通过

更新时间：
- 2026-07-18 00:44 UTC

验收线程：
- QA验收线程

验收对象：
- 业务线程-terminal-seal 0717T010

验收范围：
- 验收 `0717T006` Phase 4 terminal artifact ordering、relative-path checksum manifest、immediate verification 和 post-seal mutation detection。
- 本 QA 不执行 live，不访问 credential/private/order/cancel/network/remote/service endpoints。

验收步骤：
1. 审查任务、业务回报和 implementation commit `b5247d9`。
2. 检查 success/failure 是否都在最终 status/event row 与 heartbeat stop/join 后只生成一次 manifest。
3. 检查 manifest 是否只写 run-root-relative paths，并排除 manifest 与 verification summary。
4. 检查 verification summary 的 entry/verified/missing/mismatch/status 字段和 mutation mismatch 行为。
5. 运行 T010 focused tests、T008/T009/T010 联合回归、`py_compile` 和 commit diff check。

实际结果：
- success 和 failure 路径均先完成 window/root evidence、最终 `run_status.json` 和 event-log row，再停止并 join heartbeat，之后单次生成 manifest 并立即验证。
- manifest 使用 run-root-relative paths；`remote_sha256_manifest.txt` 与 `remote_sha256_verification.json` 均不进入 manifest。
- success 不再二次改写 `run_complete.json` 或二次生成 manifest；failure 不再在最终 failed status 写入前 seal。
- verification summary 正确记录 `manifest_entry_count`、`verified_count`、`missing_count`、`mismatch_count` 和 `status`。
- 修改已 seal 的 `run_complete.json` 后，verification 返回 `status=fail` 且报告 mismatch。
- T010 focused tests: `13 passed`。
- T008/T009/T010 combined regression: `109 passed`。
- `py_compile`、`python ... --help`、`git diff --check` 和 implementation commit diff check 通过。
- 未执行 live、credential、private、order、cancel、network、remote 或 service 动作。

验收结论：
- 已通过
- 结论说明：
  - Phase 4 terminal artifact seal contract 已闭环，可进入 Phase 5 integrated offline acceptance。

通过项：
1. Success/failure final artifact ordering 一致。
2. Heartbeat stop/join 发生在 manifest seal 前。
3. Manifest 单次生成、相对路径和 checksum-file exclusion 正确。
4. Immediate verification 和 post-seal mutation detection 正确。
5. Existing watcher termination、fill attribution 和 identity regression 保持通过。
6. No-live/no-private/no-remote 边界保持。

不通过项：
1. 无

缺陷清单：
1. 无

阻塞项：
- 无

建议总控下一步：
1. 创建唯一下一任务 `0717T011 / LIVE-EVIDENCE-INTEGRATED-OFFLINE-ACCEPTANCE`。
2. 仅使用 offline fake-watcher/fill fixtures 集成验证四个 repair phases；不要执行 live 或 private endpoint。

提交信息：
- implementation commit：`b5247d9`
- workflow commits：`29fb06e`, `c1f3837`
