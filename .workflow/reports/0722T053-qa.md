# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0722T053

状态：
- 未通过

更新时间：
- 2026-07-22 10:45 Asia/Shanghai

验收线程：
- QA验收线程

验收对象：
- 业务执行线程 `0722T053`
- implementation commit `68416c180ca02d53de455f3971fbeb9d5cb5ac4b`

验收范围：
- 严格 offline-only 验收 active multi-level manager 的 submission cap
  和 ambiguous/unknown 后续 submit 行为。
- 同时执行任务要求的 focused/full pytest、py_compile 和 commit diff
  whitespace 检查。
- 不修改实现代码、task 文件或 task 状态。

验收步骤：
1. 阅读 `.workflow/tasks/0722T053.md`、`.workflow/reports/0722T053-business.md`
   和 QA/workflow 模板，并核对 `68416c1`。
2. 用独立 fake 复现：
   - A：active multi-level manager 不传 `runtime_config`。
   - B：首个 fake order response ambiguous，oid/cloid query 均返回 unknown。
3. 运行 focused pytest、full Hyperliquid pytest、py_compile 和
   `git diff --check 68416c1^ 68416c1`。

实际结果：
- A 成立：未传 `runtime_config` 时 manager effective
  `max_real_order_submissions=30`；formal default 为 `2`。四层 desired
  实际产生 `order_calls=4`，四个 action 均为 `submitted`，最终
  `submissions_used=4`。
- B 成立：首个 response 为 ambiguous，oid query 与 cloid query 均返回
  `unknownOid`；四层 desired 仍产生 `order_calls=4`、`query_calls=4`，
  四个 action/state 均为 `unknown`，最终 `submissions_used=4`。预期应在
  首个 unresolved unknown 后停止剩余 order calls。
- focused：
  `python -m pytest examples/hyperliquid/test_hyperliquid_maker_order_manager.py
  examples/hyperliquid/test_cross_exchange_quote_ladder.py -q`
  -> `114 passed in 0.11s`。
- full：
  `python -m pytest examples/hyperliquid -q`
  -> `1240 passed in 51.72s`。
- py_compile：
  `python -m py_compile` 对本任务四个修改/测试文件通过。
- `git diff --check 68416c1^ 68416c1`：通过。
- 全部复现和测试均为本地 fake/静态/离线操作；没有 live、private/account、
  真实 order/cancel、network、remote 或 service 操作。

验收结论：
- 未通过。
- 结论说明：
  - A/B 均为 P1：A 违反 standing `2 submissions/window` 边界，B 在
    unresolved unknown exposure 下继续扩张提交。回归测试通过不能抵消
    已独立复现的 P1。

通过项：
1. focused manager/ladder regression `114 passed`。
2. full `examples/hyperliquid` regression `1240 passed`。
3. py_compile 和目标 commit 的 diff whitespace 检查通过。
4. 本轮保持 offline-only，未执行任何 live/private/order/cancel/network/
   remote/service 操作。

不通过项：
1. omitted `runtime_config` 的 active multi-level manager 使用 cap `30`
   而非 standing cap `2`。
2. 首个 ambiguous 且 oid/cloid 均 unknown 后仍继续四次 order calls。

缺陷清单：
1. manager 未传 `runtime_config` 时必须保持 formal standing cap `2`，并在
   任何 order call 前 fail-closed。
2. 批量 submit 中任一订单最终 unresolved/unknown 后，必须停止剩余 additions，
   保留 unknown exposure 并等待 authoritative reconcile。

阻塞项：
- 无环境阻塞；未通过原因是上述实现缺陷。

Unsupported claims：
- 本轮不支持 multi-level live、live order/cancel、private/account、
  network/remote/service、stable PnL、fill rate、fee/rebate、queue priority、
  maker viability、promotion 或 final MVP 结论。
- restart/recovery 的完整 provenance、historical submission count 和
  generation provenance 不在本轮独立验收范围内，不能据此宣称 restart
  readiness。

建议总控下一步：
1. 派发 offline-only repair task，修复 A/B 并加入对应 hostile regressions。
2. 修复经独立 QA 接受前，不启动任何 multi-level live 或扩大 standing
   submission envelope。

提交信息：
- QA commit：无。
- Implementation commit：
  `68416c180ca02d53de455f3971fbeb9d5cb5ac4b`
