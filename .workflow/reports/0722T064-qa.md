# QA 验收结果

## Findings（按严重性）

- P0：无。
- P1：`source_evidence_snapshot.json` 只记录两个 SSM command IDs、10 个
  hashes 和派生摘要；checkout 中没有对应命令回执或源文件，独立 QA 无法
  重算 hashes、distance/counts 或 eligible lifecycle。
- P1：source validator 只检查 hash 字符串长度和字段数量，未检查 expected
  filenames、hex、command receipt、file content hash 或从 CSV/JSON 重建
  counts。
- P2：manifest 中两个 eligibility、blocking reasons 和 recommendation
  被固定写入，未从 matrix/threshold 计算；eligible-source 反例可能产生
  matrix/manifest 矛盾。

执行线程：
- QA验收线程

任务ID：
- 0722T064

状态：
- 未通过

更新时间：
- 2026-07-22 17:06 Asia/Shanghai

验收线程：
- QA验收线程

验收对象：
- 业务执行线程 `0722T064`
- implementation commit
  `e96051850c7622b453dd1e94da07df9f16a133d5`

验收范围：
- Watcher candidate/evidence ordering。
- T047/T052 source evidence provenance、hash、counts、distance variation、
  lifecycle eligibility。
- Derived reachability/recommendation 和 no-live boundary。

验收步骤：
1. 阅读 T064 task/business、implementation、official artifacts 和 T047/T052
   historical reports。
2. 独立审查 watcher source timeline。
3. 尝试从 checkout 复核两个 SSM command receipts、10 file hashes 和派生
   evidence。
4. 构造伪 command/hash/count inputs 检查 validator fail-closed。
5. 运行 focused tests。

实际结果：
- Watcher ordering 结论通过：adaptive candidate 在 manager call 参数求值，
  dynamic exposure 在 manager 返回后观察，fill-feedback lifecycle 在循环
  finalization 后生成。
- 基于 committed snapshot，combined dynamic buy 为
  `4 observations / 1 distance`，fill feedback 为 `0 eligible / 0s`。
- Historical reports 粗粒度支持 T047 `8 exposure / 2 lifecycle` 和 T052
  `4 exposure / 2 lifecycle`，但不足以独立证明 committed 细分数据。
- 两个 SSM command IDs、10 hashes 只出现在 source snapshot，缺命令回执与
  源文件。
- Validator 接受伪 command ID、非 hex 64 字符 hash 和不一致 variation
  count。
- Eligibility/recommendation 为 hard-coded current-case output。
- Focused：
  `3 passed in 0.03s`，exit `0`。
- No-live/private/order/credential boundary 通过。

验收结论：
- 未通过。
- 结论说明：
  - Reachability 方向可信，但 source package 还不能独立重算，derived
    recommendation 也未真正由 evidence 计算。

通过项：
1. Watcher candidate-before-evidence ordering。
2. Current snapshot 内部的 matrix/recommendation方向。
3. No-live safety boundary 和 focused tests。

不通过项：
1. Source provenance/content/hash 不可独立复核。
2. Counts/variation/eligibility 未从真实文件重建。
3. Final eligibility/recommendation 硬编码。

缺陷清单：
1. Pull back structured SSM receipts and the 10 redacted source files。
2. Verify expected filenames、hex hashes、content hashes 和 receipt identity。
3. Parse source CSV/JSON to derive observations、distance variations、
   eligible lifecycle/exposure。
4. Compute eligibility、blocking reasons and recommendation from derived facts。
5. Add eligible-source positive regression。

阻塞项：
- T064 repair QA 通过前不得派发 dynamic seed implementation。

建议总控下一步：
1. 创建唯一 offline/read-only source-materialization repair task。
2. 不执行 live/private/order/cancel。

提交信息：
- QA commit：由本报告提交后的线程回报提供。

