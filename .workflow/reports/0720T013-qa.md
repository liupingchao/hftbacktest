# QA 验收结果

执行线程：
- 独立 QA验收线程

任务ID：
- 0720T013

状态：
- 未通过

更新时间：
- 2026-07-20 09:05 CST

验收对象：
- implementation commit `5b482929a09c4402b5682fc9c7c130aafbb25977`
- business report commit `a6fbaa4`
- 审查范围：`5b482929^..a6fbaa4`
- 实现代码范围：`5b482929^..5b482929`

验收范围：
- decision event/attempt identity 严格解析。
- trigger、anti-drift、immediate-guard、edge、attempt 因果连接及授权一致性。
- exact T011 legacy bridge 限制。
- two-sided shared authorized event 和 distinct attempt identities。
- source/checksum/fill/cancel/path portability 回归边界。
- 全程只读；未执行 live、private、account、order、cancel、network、remote 或 service。

## Findings

1. P2：超长 canonical-looking `attempt_key` 可令 acceptance 抛出异常，未能 fail closed 为 blocked manifest。
   - 位置：`examples/hyperliquid/cross_exchange_t024_same_window_acceptance.py:2231-2238`
   - `attempt_key` 正则中的 attempt 数字长度不受限制，匹配后直接执行：
     - `int(attempt_key_match.group(3))`
   - 该转换没有长度上限、协议范围检查或异常捕获，与同文件 `raw_strict_positive_attempt()` 的 bounded parsing 不一致。
   - 可复现构造：
     - 保留 `attempt=1`、`attempt_id=1`；
     - 将 top-level 和 nested `quote_attempt_matrix.csv` 的 `attempt_key` 改为 `<task>:window_01:attempt_` 加 5000 位数字；
     - 同步重建 producer summaries 并重新 seal；
     - acceptance 会在 attempt-key 解析处发生超长整数转换异常，而不是返回包含 `attempt_key_mismatch` 的 blocked 结果。
   - 影响：
     - malformed attempt identity 不能稳定转换为验收失败；
     - 不满足任务要求的“malformed attempt identity fails closed and cannot be skipped”。
   - 修复建议：
     - 对 key suffix 直接调用 bounded strict parser，或在转换前实施与 `RAW_MAX_CANCEL_REFERENCE_ATTEMPT_DIGITS` 相同的长度和范围限制；
     - 增加超长 attempt-key regression，断言 acceptance 不抛异常且最终 recommendation 为 blocked。

实际结果：
- malformed/missing event 和普通 malformed attempt 路径已有严格 validation reason。
- legacy guard bridge 仅在显式 flag、task `0719T011`、source `d8e22c2d9288fef86707d9b26f7791d7d8711c09` 同时成立时授权。
- 新 artifact 删除 guard event 列不能通过普通 legacy flag 降级。
- orphan、duplicate、ambiguous event joins 在实现中有显式拒绝路径。
- 零 authorized trigger 加两个 submissions 有 focused regression。
- trigger/anti-drift/guard/edge/attempt status/reason 和 anti-drift phase 有行级检查。
- actual two-sided writer fixture 包含 distinct attempt identities，并共享一个 authorized event。
- 未发现 P0 或 P1 finding。
- QA 在用户要求停止扩展测试后，未继续运行 full suite、额外 `/tmp` 反例或 T011 byte-exact replay。

命令与结果：
- 分支检查：`cross-exchange`，HEAD 为 `a6fbaa4`。
- 工作树存在验收前已有的无关修改和未跟踪文件；QA 未修改或清理。
- focused acceptance/watcher/orchestrator/manager：
  - `181 passed in 5.15s`
- 修改模块及测试 `py_compile`：
  - pass；pycache 定向至 `/tmp/0720T013-pycache`
- acceptance CLI `--help`：
  - pass；包含 `--allow-legacy-guard-identity-bridge`
- watcher CLI `--help`：
  - pass
- implementation commit `git show --check`：
  - pass
- business report commit `git show --check`：
  - pass
- `git diff --check 5b482929^..a6fbaa4`：
  - pass
- Full `python -m pytest -q examples/hyperliquid`：
  - 未运行；依照用户最新指令，在当前 focused 命令结束后立即收口。
- T011 byte-exact replay：
  - 未独立运行；业务报告中的 `validation_reasons=[]`、`112/112 provenance` 和 lifecycle blocked 未由本 QA 重放确认。

验收结论：
- 未通过
- 结论说明：
  - 已知 T012 的普通 malformed identity、authorization gap 和 cross-matrix drift 路径已得到修复，但超长 attempt-key 仍可令验收器异常退出，未满足 malformed identity 必须稳定 fail closed 的任务契约。

通过项：
1. 普通 malformed/missing event 与 attempt identity 不再静默跳过。
2. Legacy bridge 具有显式 flag 和 exact task/source 双重限制。
3. 零授权 trigger 与双 submission 冲突会被拒绝。
4. 两侧 distinct attempt keys 共享一个 authorized event 的 producer fixture 通过 focused regression。
5. source/checksum/fill/cancel/path 相关 focused regression 未出现失败。
6. 编译、CLI 和 commit whitespace 检查通过。

不通过项：
1. 超长 attempt-key suffix 未使用 bounded parser，可能异常退出而非返回 blocked manifest。

缺陷清单：
1. P2：unbounded `attempt_key` integer conversion。

剩余风险：
1. Full Hyperliquid suite 未由本 QA 独立运行。
2. T011 byte-exact artifact 未由本 QA 使用 explicit legacy flag 重放。
3. 本轮未继续构造 `/tmp` synchronized/resealed 反例，超长 key finding 来自确定性代码路径审查。

阻塞项：
- malformed attempt-key fail-closed 缺陷修复并通过独立 QA 前，不应派发新的 bounded live window。

建议总控下一步：
1. 派发最小离线修复：bounded parse attempt-key suffix，并增加超长数字回归。
2. 修复后重跑 focused、full `examples/hyperliquid` 和 T011 byte-exact replay。
3. 继续保持 live、multi-level、dynamic spread、fill feedback 和 inventory skew 锁定。

提交信息：
- implementation commit：`5b482929a09c4402b5682fc9c7c130aafbb25977`
- business report commit：`a6fbaa4`
- QA commit：无
- QA 文件修改：无
