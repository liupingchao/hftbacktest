# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0719T001

状态：
- 未通过

更新时间：
- 2026-07-19 23:59 CST

验收线程：
- QA验收线程

验收对象：
- 总控 auto-loop / 业务实现线程 0719T001

验收范围：
- runtime source provenance、structured no-fill reconciliation、producer blocker classification、唯一 T025 live window、终态安全、Task 12 gate 和 Task 10 multi-level prerequisite。

验收步骤：
1. 审阅任务、业务报告、`82f4a4d798d4012286082e9242df237ed8e86901`、T024 prior QA 和实现/测试。
2. 独立比较 62 个 runtime source SHA-256/bytes 与 exact Git commit blobs，并验证 pre-start/postrun/source marker/terminal manifest。
3. 重放 same-window acceptance，检查 producer blocker、零提交事实、进程/account/open-orders/caps/activation artifacts。
4. 运行 focused regression，并构造多 attempt cancel/no-fill 最小复现。

## Findings

1. P1：no-fill reconciliation 未把 authoritative cancel success 绑定到每个 submitted attempt/tracked reference。
   - `hyperliquid_tiny_live_m2_fill_window.py:1773-1781` 只检查整个 `cancel_results` 是否存在任意一次成功。
   - `hyperliquid_tiny_live_m2_fill_window.py:2365` 每次提交后覆盖 `tracked_refs`，但 `cancel_results` 保留所有 attempt。
   - 因此 attempt 1 cancel 成功、attempt 2 只有 `already canceled, or filled` ambiguous response 时，当前 helper 仍返回 `no_fill_reconciled`。
   - QA 最小复现得到 `status=no_fill_reconciled`、`mechanism_status=pass`、`ambiguous_redundant_cancel_tolerated=true`。
   - 现有测试只覆盖同一单引用的 success 后 redundant ambiguous，以及完全没有 success；缺少跨 attempt/reference 绑定回归。

实际结果：
- runtime provenance 子修复通过：seal 在 watcher `Popen` 前完成，exact commit 和 62/62 Git blob hashes/bytes 独立一致，pre-start/postrun 均 pass。
- terminal checksum `65/65`，JSON/CSV 和 acceptance checksum 检查通过。
- acceptance 已显式拒绝 unclassified/mechanism producer blockers；对应 focused tests 通过。
- 唯一实际 live window submissions/order/cancel/fills 均为 `0`，producer blocker 为 `fresh_touch_session_gate_no_eligible_candidate`，分类为 `mechanism_or_evidence`。
- 两个 candidate attempts 均因 `outside_quality_a_b_queue_bands` 跳过；acceptance 正确返回 `principal_task12_same_window_acceptance_blocked`。
- 没有第二个 live window；首次 carrier failure 发生在 executable/watcher 前且 `counts_as_live_window=false`。
- final/independent open orders 均为 `0`，post-live BTC position `0.0`；child `rc=0`、reaped、no SIGKILL，writer healthy，kill switch clear。
- 实际 caps 为 `0.005 BTC / 1 USDC / 0.01 BTC / 2 submissions / 900s`；dynamic spread、fill feedback、inventory skew、multi-level 和 actual quote behavior change 均关闭。

验收结论：
- 未通过
- 结论说明：
  - provenance 和 blocker-classification 修复可接受，但 structured no-fill reconciliation 仍存在跨 attempt/reference fail-open，未完全关闭 T024 的 P1。
  - 零提交 T025 window 只能作为安全 stop-condition 证据，不能关闭 Principal Task 12 的机制/证据完整性 gate。

通过项：
1. runtime source seal、independent Git blob reproduction、pre-start/postrun verification 和 terminal checksum。
2. acceptance 对未分类/mechanism producer blocker 的 fail-closed contract。
3. 单窗口限制、终态 open-orders/account/process/status-writer 安全和 exact caps/activation-off。
4. QA focused regression：`108 passed in 13.09s`。

不通过项：
1. 多 attempt/reference no-fill cancel reconciliation 不能证明每个提交订单均有对应 authoritative terminal cancellation。
2. replacement live baseline 为零提交，未形成 submit/resting/cancel/fill-reconciliation lifecycle。

缺陷清单：
1. P1：按 attempt 和 oid/cloid 关联并验证 cancel terminal evidence；任一 submitted reference 仅有 ambiguous response 时必须 `no_fill_unproven`。
2. 缺少“attempt 1 success + attempt 2 ambiguous 必须 fail-closed”及多 reference 覆盖测试。

阻塞项：
- Principal Task 12 仍未关闭。
- Task 10 multi-level 未解锁；仍无 QA-accepted single-level two-sided manager lifecycle。

## Unsupported Claims

- accepted no-fill reconciliation；
- Principal Task 12 mechanism/evidence completion；
- accepted single-level two-sided manager lifecycle；
- stable PnL、fill rate、fee/rebate calibration、queue priority、maker viability；
- multi-level activation、promotion、final MVP pass。

建议总控下一步：
1. 新 formal task 先离线修复 per-attempt/per-reference cancel proof，保留全部 submitted refs，并让 producer/acceptance 对任一未证明终态 fail-closed。
2. 增加多 attempt、多 oid/cloid 和 unrelated-success regression；离线 QA 通过前不启动新 live。
3. 修复通过后，仅允许一个新隔离窗口走现有 single-level two-sided exchange-reconciled manager path；保持 exact caps，multi-level/dynamic/fill-feedback/skew 全关闭。

提交信息：
- implementation/live source：`82f4a4d798d4012286082e9242df237ed8e86901`
- business report：`3c9eb700`
- live artifact：`local_live_analysis/principal_alignment_task12_repair_0719T001/`
