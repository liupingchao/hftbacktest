# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0529T005

状态：
- 已通过

更新时间：
- 2026-05-30 23:40 CST

验收线程：
- QA验收线程

验收对象：
- 业务线程-python 0529T005

验收范围：
- 验收 Stage 9L fill-quality rejection decomposition / bucket coarsening runner 是否按 `0529T005` read-only 边界实现。
- 检查 required artifacts 是否完整生成，并能复现 key verdict。
- 检查 coarsened bucket sample coverage 是否来自 row-level `sample_id`，而不是 Stage 9K aggregate coverage 加总。
- 检查 churn hard-gate sensitivity / warning variants 是否不产生 Shape A / Shape B candidate，并且未被表述成生产 guard relaxation。
- 检查是否未改策略、未 run live、未做 replay semantics / parameter search / default-on / guard relaxation / promotion。

验收步骤：
1. 阅读 `.workflow/tasks/0529T005.md`。
2. 阅读 `.workflow/reports/0529T005-business.md`。
3. 检查 runner 与测试：
   - `examples/binance_tick_mm/fill_quality_rejection_decomposition.py`
   - `examples/binance_tick_mm/test_fill_quality_rejection_decomposition.py`
4. 检查 Stage 9L 输出目录：
   - `local_live_analysis/stage9l_fill_quality_rejection_decomposition_0529T005/`
5. 复跑 CLI、focused pytest、py_compile、manifest JSON parse、runner 到 `/tmp/qa_0529T005_stage9l`。
6. 对比 committed artifacts 与 QA 复跑 artifacts 的关键计数。
7. 核对提交范围：
   - `fc73c0e` `Add Stage 9L rejection decomposition runner`
   - `2965843` `Record 0529T005 business commit`

实际结果：
- Runner 已实现：`examples/binance_tick_mm/fill_quality_rejection_decomposition.py`。
- Focused tests 已实现：`examples/binance_tick_mm/test_fill_quality_rejection_decomposition.py`。
- Required artifacts 已生成：
  - `run_manifest.json`
  - `rejection_reason_decomposition.csv`
  - `churn_gate_sensitivity.csv`
  - `coarsened_trigger_bucket_metrics.csv`
  - `coarsened_shape_candidates.csv`
  - `sample_gap_by_regime.csv`
  - `stage9l_recommendation.md`
- Manifest 可正常解析，且 boundary 明确：
  - `read_only=true`
  - `default_off=true`
  - `live_run=false`
  - `replay_run=false`
  - `strategy_change=false`
  - `parameter_search=false`
  - `guard_relaxation=false`
  - `promotion=false`
- Manifest 明确记录：
  - `row_level_reaggregation=true`
  - `coarsened_sample_coverage_source=row_level_sample_id`
  - forbidden trigger inputs include future fill / markout / spread capture, `fill_after_cancel_bucket`, same-sample PnL feedback, exact queue position, and hidden queue assumptions.
- QA 复跑到 `/tmp/qa_0529T005_stage9l` 通过，复现：
  - `final_classification=needs_targeted_clean_fills`
  - `shape_candidate_count=0`
  - coarsened clean-only buckets:
    - `ready_for_policy_design=0`
    - `needs_more_clean_fills=310`
    - `reject_quality_negative=662`
    - `not_decisionable=0`
- QA 复核 churn sensitivity committed 与复跑结果一致：
  - `stage9k_original_hard_gate`: `0` ready, `68` needs-more, `246` reject, `0` shape candidates
  - `recent_reject_throttle_warning`: `0` ready, `197` needs-more, `117` reject, `0` shape candidates
  - `fast_cancel_cancel_readd_warning`: `0` ready, `154` needs-more, `160` reject, `0` shape candidates
  - `non_true_reject_churn_warning`: `0` ready, `283` needs-more, `31` reject, `0` shape candidates
- QA 复核 top gap committed 与复跑结果一致：
  - `churn_warning_coarsened / large_skew_or_low_score / add_side / step_back_gt1 / edge_non_adverse / market_view_usable / post_only_clean / warning_churn_context`
  - `2605` rows, `34` fills, `7` samples, `6` fill samples, `6` additional clean fills needed.
- QA 复跑命令结果：
  - `python examples/binance_tick_mm/fill_quality_rejection_decomposition.py --help` 通过。
  - `python -m pytest examples/binance_tick_mm/test_fill_quality_rejection_decomposition.py -q` 通过，`4 passed`。
  - `python -m py_compile examples/binance_tick_mm/fill_quality_rejection_decomposition.py` 通过。
  - `python -m json.tool local_live_analysis/stage9l_fill_quality_rejection_decomposition_0529T005/run_manifest.json` 通过。
  - `python examples/binance_tick_mm/fill_quality_rejection_decomposition.py --input-dir local_live_analysis/stage9k_fill_quality_bucket_synthesis_0529T002 --output-dir /tmp/qa_0529T005_stage9l` 通过。
  - `git diff --check` 通过。
- 当前未发现 Binance strategy behavior、live config、replay lifecycle semantics、queue/touch repair、parameter search、default-on、guard relaxation、tiny-live 或 promotion 改动。

验收结论：
- 已通过
- 结论说明：
  - `0529T005` 按合同完成 read-only Stage 9L rejection decomposition / bucket coarsening runner、focused tests 和 required artifacts；结论明确为 `needs_targeted_clean_fills`，Shape A / Shape B 仍无 ready candidate，churn warning sensitivity 不产生 ready candidate。本任务不授权策略实现、参数搜索、live/default-on、guard relaxation、tiny-live 或 promotion。

通过项：
1. Runner CLI、focused tests、required artifacts 和 business report 完整。
2. 输出能复现业务报告关键结果：final classification `needs_targeted_clean_fills`、Shape A/B candidate rows `0`、coarsened ready buckets `0`、needs-more `310`、reject `662`。
3. Coarsened sample coverage 使用 row-level `sample_id` 重新计算，未以 Stage 9K aggregate coverage 加总替代。
4. Churn hard-gate sensitivity 明确为 offline evidence sensitivity，未变成生产 guard relaxation。
5. Manifest 和 recommendation 明确记录 read-only/default-off/no-live/no-replay/no-parameter-search/no-guard-relaxation/no-promotion 边界。
6. 复跑命令通过。

不通过项：
1. 无

缺陷清单：
1. 无

阻塞项：
- 无

建议总控下一步：
1. 可将 `0529T005` 标记为 `已通过`。
2. 后续不要进入 policy design、strategy implementation、parameter search、live/default-on、guard relaxation、tiny-live 或 promotion。
3. 如继续 Binance fill-quality-first 路线，下一步应由总控决定是否围绕 Stage 9L top gap 做 targeted clean-fill collection，或继续 read-only evidence refinement。

提交信息：
- business/artifact commit：`fc73c0e` `Add Stage 9L rejection decomposition runner`
- business report commit：`2965843` `Record 0529T005 business commit`
