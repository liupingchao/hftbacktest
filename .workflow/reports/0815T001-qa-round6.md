# QA 验收结果

执行线程：
- QA验收线程（第六轮全新独立验收）

任务ID：
- 0815T001

状态：
- 未通过

更新时间：
- 2026-08-15 17:38 CST（星期六）

验收线程：
- QA验收线程

验收对象：
- 业务线程-python/research + 0815T001 Round 5 bounded repair
- 正式包：
  `local_live_analysis/skhynix_trigger_aligned_episode_research_v1_stage02_density/`
- 声称 core SHA256：
  `f29597985d41e0f33588513a5a940469d0bb6188ff0f33acbdb50ca115a7a638`
- Build A：
  `/tmp/0815T001-r5-density-a.hwOreI/package`
- Build B：
  `/tmp/0815T001-r5-density-b.wu3uaX/package`

验收范围：
- 独立检查 `input_bindings.csv` phase/scope 全表枚举、研究边界路径规则，
  以及 accepted Stage 1 Aug07 控制账本例外的 scope、role、root 和相对
  路径闭包。
- 在全新的 `/tmp` 正式包副本中构造 coherent-rehash 不一致样例，分别
  使用 current source CLI 和副本内 archived CLI 验证。
- 独立复算 Round 1-4 的 projection v2、session order、small CSV 主键/
  cardinality/row width、candidate/sensitivity coupling、runtime closure、
  input inventory 和研究边界。
- 运行 focused tests、Admission tests、recovery regressions、Ruff、
  外置 pycache compileall、CLI help、`git diff --check`、source/archive
  verify-only、zero-write 和 formal/A/B 比较。
- 普通代码审查重点覆盖 Round 5 `verification_closure` 与运行时行为是否
  一致，以及任何会削弱 Stage 3 门禁可信度的 P0-P3。

验收步骤：
1. 阅读 `AGENTS.md`、workflow manual/templates、任务文件、
   business-r5、qa-round5、主实施方案及 controller tracking。
2. 直接审查 source 与 archived verifier，不采信业务或前五轮 QA 的通过
   判断。
3. 从正式包创建全新 `/tmp` 副本，修改 payload 后同步刷新 artifact
   records 和 core hash，再由两套 CLI 分别验证。
4. 使用独立 stdlib CSV/gzip/JSON/SHA 脚本流式重算高基数 membership、
   小表 key closure、输入文件和构建库存。
5. 运行完整回归、静态检查、help、verify-only、zero-write 和 A/B 比较。
6. 对 Round 5 QA 对象执行逐文件比较，确认研究 artifacts 是否变化。
7. 进行普通代码审查并按 P0-P3 分级。

运行命令与结果：
1. `python -B -m pytest -q
   examples/hyperliquid/test_cross_exchange_trigger_density_core.py
   examples/hyperliquid/test_cross_exchange_candidate_episode_merging.py
   examples/hyperliquid/test_cross_exchange_trigger_density_inputs.py
   examples/hyperliquid/test_cross_exchange_trigger_density_admission.py`
   - `142 passed in 8.56s`
2. `python -B -m pytest -q
   examples/hyperliquid/test_cross_exchange_trigger_density_admission.py`
   - `91 passed in 2.44s`
3. `python -B -m pytest -q
   examples/hyperliquid/test_cross_exchange_liquidity_response_case_hierarchy.py
   -k 'merges_dense_clusters_without_recovery or
   stops_at_recovery_checkpoint or
   fails_closed_on_missing_recovery_evidence'`
   - `3 passed, 19 deselected in 0.04s`
4. `python -m ruff check` 对四个 Stage 2 实现和四个测试文件。
   - `All checks passed!`
5. 使用外置 `PYTHONPYCACHEPREFIX=/tmp/0815T001-qa6-pycache.*`
   运行 `python -m compileall -q`。
   - `rc=0`
6. 对相同八个 Stage 2 文件运行 `git diff --check`。
   - `rc=0`
7. Source/archived `--help`。
   - 均为 `26` 行、`rc=0`，输出逐字节一致。
8. Source/archived 正式包 `--verify-only`。
   - 均为 `rc=0`、`verified=true`、`artifact_count=15`、
     `candidate_membership_rows=463612`、`confirmed_rows=267554`、
     `sensitivity_membership_rows=463612`、`session_count=3`。
9. Source CLI 分别对 Build A 和 Build B 执行 `--compare-to`。
   - 两次均为 `identical=true`，core 为声称值。
10. 独立负向矩阵根目录：
    `/tmp/0815T001-qa6-negative-r0eqjh80/`。
    - `11` 类应拒绝样例由 source/archive 共验证 `22` 次，全部按预期
      `rc=2`。
    - `1` 类 Stage 1 根目录整体替换样例由两套 verifier 共验证 `2` 次，
      两次均非预期地 `rc=0`、`verified=true`。
11. 独立数据审计根目录：
    `/tmp/0815T001-qa6-audit-_69mtdmy/`。
    - 全量流式读取 `463612` 条 candidate 和 `463612` 条 sensitivity。
    - 实际读取并重哈希所有 `123` 个 source 文件和 `14` 个 Stage 1 文件。
    - 比较 formal/A/B 全部 relative path、bytes 和 SHA256。

独立正向证据：
- 正式 manifest 的 `15` 条 artifact records 与实际文件完全一致；独立
  重算 core 为
  `f29597985d41e0f33588513a5a940469d0bb6188ff0f33acbdb50ca115a7a638`。
- Formal、Build A、Build B 均为 `16` files、`19,831,761` bytes；
  relative path/bytes/SHA mismatch 均为 `0`。按业务所用 compact canonical
  JSON inventory 算法，三者 full-directory SHA256 均为
  `ea1aeee9aae8a38cdeb3eed0471b94c45248785e241f65a3e8e120f8dc2baa29`。
- Archived verify-only 前后正式包均为 `16` files、`19,831,761` bytes，
  全文件 tuple 完全一致；无 `__pycache__`、`.pyc` 或新增路径。
- Projection version 为 `candidate_membership_projection_v2`，完整 `19`
  字段与 frozen contract 精确一致。独立重算：
  - Jul30：`268522` rows，
    `cd731b5b011183366fd3cb04e0809d082a26bde85d7f1446ff4754016c3551f9`
  - Aug03：`127622` rows，
    `04cc9a13c85e530291bcf3c584e78ea029ed67fe1653bcdd7efa13e14294fda0`
  - Aug04：`67468` rows，
    `bf5911989a07f9b1242b9eef12e72a40093549f98c516c846df6caae353db820`
- Candidate 和 sensitivity session order 均为
  `jul30 -> aug03 -> aug04`；重入 fragment 为 `0`，逐行
  `(session_id,candidate_id)` mismatch 为 `0`。
- Candidate unique-key duplicate、candidate/sensitivity row-width error、
  window bound mismatch 均为 `0`。按 `(session_id, structural_id)` 复算：
  `110234` clusters、`43384` flows、`247` overlap blocks 均未跨
  segment/epoch，cluster-flow parent mismatch 为 `0`。
- Small CSV exact primary-key/cardinality/row-width：
  - density：`6/6`；
  - inter-trigger：`18/18`；
  - episode summary：`3/3`；
  - sensitivity summary：`36/36`；
  - ESS：`74/74`；
  - missing、extra、duplicate 和 row-width error 均为 `0`。
- Source input before/after 均为 `123` unique files、`169,677,903` bytes、
  SHA256
  `94bad85bfd4981b351f84c53628099468ec27f13d308402ac7125a9d582a6644`。
- Accepted Stage 1 before/after 均为 `14` unique files、`1,401,387` bytes、
  SHA256
  `c540cc056313716b3bdd2b9c0fe076cda15a7f152b399ae6a1283b3aa8aa6590`；
  core 为
  `9c2756f966a1a05816f9e6a91d7361d76cfea10fc3c587944ca2f0c1d2eb7c96`。
- 所有 `137` 个实际输入文件均存在，size/SHA mismatch 为 `0`。
- 五个 runtime source 文件的 current source、archived bytes 和 manifest
  SHA 三方一致。
- 相对 Round 5 QA 对象
  `/tmp/0815T001-r4-density-a.Ok1hG1/package`，仅以下 `3` 个文件变化：
  `density_manifest.json`、`frozen_density_contract.json`、
  `runtime_source/cross_exchange_trigger_density_admission.py`。研究数值、
  membership、sensitivity、density、merging 和 ESS artifacts 未变化。
- Round 5 其余修复独立通过：
  - unknown phase/scope 和全表 Aug07 path：source/archive 均拒绝；
  - 控制账本 role 或相对路径变化：均拒绝；
  - 控制账本 bytes/SHA 变化：命中 frozen inventory drift；
  - 报告计数变化：命中 canonical report drift；
  - manifest extra key、missing key、非规范 JSON：均拒绝；
  - small-summary 浮点文本增加 `5e-10`：命中 exact field drift。

验收结论：
- 未通过
- 结论说明：
  - 正式包当前内容、研究数值、Round 1-4 闭包、R5 的 report/manifest/
    exact-float/phase-scope 修复、测试质量和双构建确定性均通过。
  - 但业务声称的“Stage 1 控制账本例外精确绑定正确 root”未实现。
    `stage1_package_path` 只要求全表单值，未与 accepted Stage 1 根目录或
    frozen contract 外部锚比较；将全套 Stage 1 binding 一致改写到一个
    不存在且带 `outcome_shadow` 的 `/tmp` 根目录后，两套 verifier 仍为
    package 背书。该 fail-open 影响 provenance 和研究边界门禁可信度，
    因此 Stage 3 不得解锁。

通过项：
1. Round 5 的 phase/scope 枚举、全行 Aug07 检查、scope/role/相对路径、
   控制账本 identity fields、canonical report、manifest schema/bytes 和
   exact float 修复均独立关闭。
2. Round 1-4 的 projection v2、session order、small CSV closure、
   candidate/sensitivity coupling、runtime closure 和输入 inventory 均通过。
3. 正式包、Build A、Build B 逐文件一致；source/archive verify-only、
   zero-write、tests 和静态检查全部通过。
4. 当前正式包自身只绑定正确的 accepted Stage 1 根目录；当前数据没有
   发现实际越界或研究数值错误。

不通过项：
1. P1：accepted Stage 1 根目录不是 verifier 或 frozen contract 的外部
   不变量，可被全表一致替换后 coherent-rehash 并继续 `verified=true`。

缺陷清单：
1. P0：无。
2. P1：Stage 1 root closure fail-open。
   - 文件/行号：
     `examples/hyperliquid/cross_exchange_trigger_density_admission.py:
     1940-1968` 使用每行自报的 `stage1_package_path` 判断 Aug07 控制账本
     例外；`:3222-3241` 只要求根目录 cardinality 为 `1`，没有与 accepted
     根目录常量或 frozen contract 比较；`:1906-1937` 在 inventory identity
     中把绝对根目录归一化掉。
   - Contract：
     `:891-906` 的 `verification_closure` 只冻结控制账本相对路径，没有
     冻结 accepted Stage 1 root。
   - 回归缺口：
     `test_cross_exchange_trigger_density_admission.py:878-898` 只单测
     任意临时 root 下的 scope/role/相对路径，不覆盖 production-sized
     全根替换。
   - 独立复现：
     `/tmp/0815T001-qa6-negative-r0eqjh80/
     ledger_root_substitution/package`。
     将所有 `stage1_package_path` 改为
     `/tmp/0815T001-qa6-negative-r0eqjh80/root_substitution/
     outcome_shadow/accepted_stage1`，并将 `accepted_stage1_package` 行的
    绝对 `path` 同步平移，保持相对路径、bytes、SHA、role、Stage 1 core
     和 before/after inventory 不变，再刷新 artifact records/core。
   - 结果：
     source/archived 均 `rc=0`、`verified=true`，新 core 为
     `7a57c971bd62014b1bfca9dfe8c9890b15666b89d21e4c057fc2d8d26555b86e`。
   - 影响：
     verified `input_bindings.csv` 可以声称消费另一个甚至不存在的 Stage 1
     根目录；控制账本 Aug07 例外也随自报 root 平移。内容相对 inventory
     虽未变化，但绝对 provenance 和业务承诺的边界根不再可信。
   - 修复建议：
     在 source 常量和 canonical contract 中冻结 accepted Stage 1 resolved
     root；验证全表每行 `stage1_package_path` 精确等于该 root，并验证
     Stage 1 scope 的每个绝对 `path` 等于
     `frozen_root / frozen_relative_path`。新增全根替换、不存在 root、
     outcome-like root component 的 production-sized coherent-rehash
     回归；如确需可搬迁包，应删除“精确 root”承诺并改为显式、版本化的
     content-addressed relocation contract。
3. P2：无。
4. P3：无。

边界结论：
- 正式 manifest 七项 boundary 全部为 `false`。
- 正式 `input_bindings.csv` 当前只有 accepted Stage 1
  `consumption_ledgers/aug07_access_ledger.json` 两条 before/after Aug07
  路径；scope、role、当前 root、相对路径、bytes 和 SHA 均正确。
- 被绑定账本记录 `event_rows_opened=false`、
  `event_row_read_count=0`，且来源为实际成功 policy content reads。
- Membership schema 没有 response/outcome/markout/PnL/score/fill/
  actionability 字段；正式 source binding 没有 Aug07 event-row 路径。
- 本轮 QA 未读取 Aug07 raw/R0/R1/basis event rows，未运行 response、
  outcome、model、score、PnL、actionability 或 private/order/cancel
  工作。
- 本发现证明 verifier 可接受错误 provenance root，不表示当前正式包
  已经读取 Aug07 event rows。
- Stage 3 detector parity、Episode v3、outcome/model/actionability 和
  Aug07 event-row first-read 继续锁定。

建议总控下一步：
1. 保持 `0815T001` 为 `未通过`，不得解锁 Stage 3。
2. 在同一任务范围内补齐 accepted Stage 1 root 的 canonical contract 和
   verifier 外部锚，并加入 production-sized root-substitution regressions。
3. bounded repair 后由新的独立 QA 线程重新验收；其余 R5 修复不需要重做
   研究计算，但必须重新确认正式包和双构建 bytes。

报告文件一致性：
- 本报告与 `docs/qa-acceptance-report.md` 使用同一正文机械复制；发布后
  执行 `cmp -s` 和 `shasum -a 256` 验证 byte-identical。
- 完整文件 SHA256 在写入完成后才能计算；为避免文件自嵌自身哈希造成
  自引用变化，最终两个路径的 SHA256 记录在本 QA 线程完成回复中。

提交信息：
- commit：无
