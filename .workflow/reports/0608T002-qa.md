# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0608T002

状态：
- 已通过

更新时间：
- 2026-06-08 13:53 CST

验收线程：
- QA验收线程

验收对象：
- 业务线程-research / 0608T002

验收范围：
- 验收 `0608T002` 是否按任务边界实现并验证 read-only maker executability assessment，仅评估 `regime_011_1000_spread_10_20_ticks` 的 public-data proxy maker 可执行性，并确认未输出策略动作、maker side、quote/order 行为、private/order endpoint、live/default-on/tiny-live 或 promotion claim。

验收步骤：
1. 读取 `.workflow/tasks/0608T002.md`，确认任务状态为 `待验收`、QA 方式为正常验收、范围限定为 read-only maker executability assessment。
2. 读取 `.workflow/reports/0608T002-business.md`，核对方法、输入 source-lock、artifact 路径、验证命令、commit id 和边界声明。
3. 复查 `examples/hyperliquid/canonical_maker_executability.py`，确认通过 `canonical_event_mode_evidence.guard_canonical_event_mode_evidence()` 使用 canonical source-lock，且 `allow_diagnostic_validation=False`。
4. 复查实现中 `TARGET_CANDIDATE_ID`、T009 candidate manifest 校验、T006/T007 prerequisite manifest 校验和 final recommendation taxonomy。
5. 复查 `examples/hyperliquid/test_canonical_maker_executability.py`，确认覆盖 canonical input acceptance、diagnostic-only synthetic refusal、candidate-id filtering、final taxonomy 和 watch path。
6. 检查 `local_live_analysis/canonical_maker_executability_0608T002/` 是否包含任务要求的 7 个 artifacts。
7. 复跑 `python examples/hyperliquid/canonical_maker_executability.py --help`。
8. 复跑 `python -m py_compile examples/hyperliquid/canonical_maker_executability.py examples/hyperliquid/test_canonical_maker_executability.py`。
9. 复跑 `python -m pytest examples/hyperliquid/test_canonical_maker_executability.py`。
10. 复跑正式 runner 命令，确认输出仍为 `reject_not_maker_executable`；复跑只临时改变 manifest 的 `generated_at` / `git_commit`，QA 已恢复该 transient diff，未把复跑时间戳写回业务 artifact。
11. 运行 `python -m json.tool local_live_analysis/canonical_maker_executability_0608T002/maker_executability_manifest.json`。
12. 检查 `maker_executability_report.md` 边界文本，确认只有 proxy caveat 和禁止项，没有策略动作、private/order、live/default-on/tiny-live 或 promotion claim。
13. 运行 `git diff --check`。

实际结果：
- `maker_executability_manifest.json` 解析通过，`task_id=0608T002`，`schema_version=canonical_maker_executability_v1`。
- manifest 限定 `assessed_candidate_id=regime_011_1000_spread_10_20_ticks`。
- canonical guard 结果为 `canonical_sample_count=3`、`diagnostic_rejection_count=0`。
- final recommendation taxonomy 存在并包含 `candidate_for_case_library`、`watch_needs_execution_evidence`、`watch_needs_more_public_samples`、`reject_not_maker_executable`、`reject_public_data_insufficient`。
- 任务要求的 artifacts 均存在：
  - `maker_executability_manifest.json`
  - `regime_executability_summary.csv`
  - `fill_opportunity_proxy.csv`
  - `spread_capture_adverse_selection.csv`
  - `quote_churn_post_only_latency.csv`
  - `inventory_exposure_what_if.csv`
  - `maker_executability_report.md`
- Summary 结果为 `public_proxy_row_count=242`、`candidate_definition_row_count=242`、`sample_count=3`。
- Fill opportunity proxy 为 `fillable_proxy_supported`，per-sample trigger counts 为 `39 / 99 / 104`，sample concentration 为 `0.42975207`。
- Spread/adverse result 为 `spread_capture_negative` / `adverse_selection_reject`，half-spread `10` ticks，adverse selection proxy `72.08677686` ticks，net spread-capture proxy `-62.08677686` ticks。
- Quote churn 为 `manageable_churn`，但 post-only / latency 为 `reject_post_only_risk` / `latency_reject`。
- Final recommendation 为 `reject_not_maker_executable`，符合任务 taxonomy。
- Report 明确写明所有 fill、spread、adverse-selection、post-only、latency、inventory 结论都是 read-only public-data proxies。
- Report 明确写明不证明 real fill probability、real post-only reject rate、private order lifecycle、inventory behavior 或 live execution。
- Report 明确禁止 maker side、quote price、size、cancel rule、strategy action、private/order endpoint、order lifecycle、live/default-on/tiny-live、parameter search 和 promotion。
- `--help` 通过。
- `py_compile` 通过。
- Focused pytest 通过：`5 passed`。
- 正式 runner 复跑通过并输出 `final_recommendation=reject_not_maker_executable`。
- `python -m json.tool` 通过。
- `git diff --check` 通过。
- QA 未发现本任务访问 private/order endpoint、实现策略/quote/order 行为、启动 live/default-on/tiny-live、运行参数搜索、修改 connector/core/schema、输出交易指令或 promotion claim。

验收结论：
- 已通过
- 结论说明：
  - `0608T002` 已按任务边界完成 read-only maker executability assessment。结论 `reject_not_maker_executable` 有对应 public proxy artifacts 支撑；该结论不授权策略实现、private/order endpoint、order lifecycle、live/default-on/tiny-live、parameter search、case-library implementation、shadow decisions 或 promotion。

通过项：
1. Runner 使用 accepted canonical source-lock path，并拒绝 diagnostic-only synthetic formal input。
2. Assessment 限定为 `regime_011_1000_spread_10_20_ticks`。
3. Required artifacts 全部存在并可解析。
4. Final recommendation 使用任务允许 taxonomy，结果为 `reject_not_maker_executable`。
5. Report 边界声明充分，未输出 maker side、quote price、size、cancel rule、strategy action、live 或 promotion claim。
6. Focused verification 全部通过。

不通过项：
1. 无

缺陷清单：
1. 无

阻塞项：
- 无

建议总控下一步：
1. 可将 `0608T002` 标记为 `已通过`。
2. `0608T002` 不支持 maker case-library / live / promotion；若继续研究用户提出的单边动量思路，应以另一个 read-only directional momentum viability task 处理，并继续禁止策略实现和 live。

提交信息：
- commit：`0196149` / `c59647e`
