```md
# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0526T002

状态：
- 已通过

更新时间：
- 2026-05-26 11:15 CST

验收线程：
- QA验收线程

验收对象：
- 测试线程 0526T002

验收范围：
- 验收 `0526T002` 是否完成一个 active current-format no-rule/default-off 1H control sample 的采集、归档、audit replay、maker acceptance、sidecar/join、Stage 5/5C/6/9B/9D 后处理，并保持 no-promotion / no-guard-relaxation / no-candidate-enable 边界。

验收步骤：
1. 阅读 `.workflow/tasks/0526T002.md` 与 `.workflow/reports/0526T002-business.md`。
2. 检查 `local_live_analysis/5-26-active-minmove-control-60min-a/**`、archive 和 aggregate Step 9D artifacts 是否存在。
3. 抽查 raw gzip、archive gzip 与 archive sha256：
   - `gzip -t local_live_analysis/5-26-active-minmove-control-60min-a/raw_market_data/btcusdt_20260526.gz`
   - `gzip -t local_live_analysis/archive/5-26-active-minmove-control-60min-a.tar.gz`
   - `sha256sum -c local_live_analysis/archive/5-26-active-minmove-control-60min-a.tar.gz.sha256`
4. 抽查 maker acceptance、market-view acceptance、T009 joined decision metrics、Stage 5 labels、Step 5C counters、Stage 6 calibration、Step 9B summary、Step 9D aggregate recommendations。
5. 确认业务报告未把该样本解释为 tiny-live / default-on / promotion 证据。

实际结果：
- 采集样本存在，包含 deployment manifest、start/stop marker、audit、raw gzip、logs、replay outputs、sidecar/join、Stage 5/5C/6/9B/9D artifacts。
- raw gzip 检查通过。
- archive gzip 检查通过。
- archive sha256 检查通过：
  - `e5e4fcbc47c17292937d16a1b15429e1f5b81b46e8915bd669d92b40aae28403`
- preflight / deployment:
  - deployed commit `76130f4`
  - remote worktree `/home/admin/hft_live/worktrees/0526T002-active-minmove-60min`
  - baseline config source `5-26-active-minmove-control-30min-a`
  - config diff 仅包含 run id / output path / audit csv / connector config path 替换
  - live audit rows / fields `150756 / 159`
  - stop marker exit code `0`
- base maker acceptance 通过：
  - `passed=true`
  - hard failures `[]`
  - action/planned/reject/throttle match `1.0`
  - strict replay lag breaches/drops/failures `0/0/0`
- market-view acceptance 未作为 strict clean 通过：
  - `passed=false`
  - classification `limited_pricing_research`
  - only hard failure: `top5_join_age_ms_p99 = 69.99774251999995ms > 50ms`
- 但 sidecar/join 结构质量达标：
  - first valid update aligned `true`
  - depth `pu` mismatch `0`
  - decision join coverage `1.0`
  - future/gap/missing join `0/0/0`
  - stale join rate `0.015264663443282788`
  - bookTicker/depth BBO mismatch rate `2.2243642025654334e-05`
  - top5 tick/qty match `0.8929608127721336 / 0.8486093765821716`
- Stage 5:
  - submits `5095`
  - fills `164`
  - fill-after-cancel `81`
  - fill@100/500/1000/5000ms `22/73/92/137`
- Step 5C:
  - decision rows `118509`
  - bookTicker anchor rows `89099`
  - guarded depth fallback rows `28679`
  - missing anchor rows `0`
  - stale anchor rows `731`
  - post-only risk after recheck `0`
- Stage 6:
  - decision state `methodology_valid_single_sample`
  - live/replay submits `5095 / 5095`
  - matched submits `5095`
  - live/replay fills `164 / 163`
  - live/replay fill-after-cancel `81 / 80`
  - `Not Aligned: none`
- Step 9B:
  - classification `promising_but_single_sample`
  - submit orders `5095`
  - missing T006 field count `0`
- Step 9D aggregate:
  - caveated samples: `5-19-night-active-30min-a`, `5-26-active-minmove-control-60min-a`
  - `min_move_quote_age_churn_guard`: stable promising `19`, reject `1`, parameter-sweep seed buckets `2`
  - seed buckets: `inventory_state=large_skew_or_low_score`, `latency_stale_age=stale_latency_medium`
  - `inventory_reservation_shift_band`: seed `0`, reject `19`
  - `size_reduction_or_add_side_suppression_pressure`: seed `0`, reject `20`
- Fill mass:
  - 本样本新增 `164` natural fills。
  - 如果计入 caveated 样本，总 fills 约 `550/500`，数字上超过 Step 9C fill mass 门槛。
  - 但 strict clean-only fill mass 仍约 `386/500`，仍缺约 `114` fills。
  - 因此该样本只可作为 caveated evidence，不可作为 strict-clean tiny-live readiness proof。
- 未发现 live/default-on/promotion/guard relaxation/strategy behavior change。

验收结论：
- 已通过
- 结论说明：
  - `0526T002` 完成了 1H active current-format no-rule/default-off control sample 的采集、归档和重跑验证；QA 接受该样本作为 caveated evidence，但不接受它作为 strict-clean market-view sample 或 `ready_for_tiny_live_design` 证据。

通过项：
1. 采集、拉回、归档和后处理完整。
2. raw/archive integrity 检查通过。
3. base maker acceptance 通过，action/reject/throttle 对齐为 `1.0`。
4. sidecar/join future/gap/missing 为 `0/0/0`，top5 tick/qty 达标。
5. Stage 5/5C/6/9B/9D required artifacts 齐全。
6. no-live-promotion / no-default-on / no-guard-relaxation 边界保持成立。

不通过项：
1. 无

缺陷清单：
1. 无

阻塞项：
- 无

建议总控下一步：
1. 不要进入 Step 10 tiny-live design；`0526T002` 不是 strict-clean market-view sample。
2. 可以基于当前证据新建 narrow `min_move_quote_age_churn_guard` parameter-sweep design 任务，seed 只围绕 `inventory_state=large_skew_or_low_score` 和 `latency_stale_age=stale_latency_medium`。
3. 如果总控坚持 strict-clean `500` fill gate，则下一步应再采一个 strict-clean active current-format no-rule/default-off 样本，而不是放宽 guard。

提交信息：
- commit：待总控收尾提交
```
