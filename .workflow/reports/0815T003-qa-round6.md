# QA 验收结果

执行线程：
- QA验收线程（第六轮全新独立 Stage 4 验收）

任务ID：
- `0815T003`

状态：
- 已通过

更新时间：
- 2026-08-16（星期日；authoritative task date）

验收线程：
- QA验收线程

验收对象：
- 业务线程-python/research + `0815T003` 第五轮有界返修
- 正式 package：
  `local_live_analysis/skhynix_trigger_aligned_episode_research_v1_stage04_jul30_episode_v3/`
- current source/tests 与 package archived runtime/tests
- durable pre-repair inventory：
  `.workflow/reports/0815T003-round4-pre-repair-research-inventory.csv`
- 本轮全新 QA 根：
  `/tmp/0815T003-qa-round6.xXfNdv/`

验收范围：
- 仅 Jul30 immutable public-data inputs。
- 不解析 Jul30 legacy
  `skhynix_liquidity_response_0730T017/episodes/*.csv.gz`。
- 不读取 Aug03/Aug04 future event rows 或任何 Aug07 event rows。
- 不运行 case/model/score/actionability/orders/network/live config。
- accepted Stage 1/2/3、Jul30 raw/R0/R1 和正式 package 只读。
- 未复用任何先前业务/QA `/tmp` 证据。
- 除本报告及其 mirror 外，未修改 task、tracking、实现、测试、正式
  package、pre-repair inventory、accepted dependencies 或 inputs。
- Stage 5 在本轮明确验收前保持锁定。

## Findings First

普通代码审查：
- P0：`0`
- P1：`0`
- P2：`0`
- P3：`0`

### P0 Findings

- 无。

### P1 Findings

- 无。
- Round5 dangling-symlink artifact-closure P1 已由 source-owned exact
  `lstat` tree universe 和 production package attacks 独立关闭。

### P2 Findings

- 无。

### P3 Findings

- 无。

## Exact Tree Implementation Review

source-owned implementation：
- `_tree_entry_type_contract()`、`_lstat_entry_type()` 和
  `_exact_tree_entries()` 定义在 current builder source 内。
- current builder source 与 archived builder source raw bytes exact。
- frozen contract 的 `tree_entry_type_contract` 与 source helper exact。
- package root 先 `lstat()`，只接受真实 directory。
- descendant 逐项 `lstat()`，只接受 `stat.S_ISREG` /
  `stat.S_ISDIR`。
- symlink、FIFO、Unix socket、character/block device 和其他 special
  type 均 fail closed。
- scanner 只从 `os.scandir()` 取 entry name，再对构造出的 path
  `lstat()`；不使用 `DirEntry.is_file()/is_dir()` 或
  `Path.is_file()/is_dir()` 构造 identity universe。
- `verify_package()` 使用 `os.path.abspath()`，不会提前跟随 root
  symlink；CLI 中的 `.resolve()` 只发生在完整 `verify_package()`
  已成功之后的结果汇总，不会抹掉 admission 的 root identity。
- `_directory_inventory()`、`_artifact_records()`、
  `_verify_artifact_closure()` 和 `_fsync_tree()` 都先消费同一个
  `_exact_tree_entries()` universe。

独立 scanner behavior：
- nested tree：通过。
- empty descendant directory：进入 exact directory universe。
- result sorting：按 relative path 稳定排序。
- missing root：fail closed，`missing directory`。
- entry 在 `scandir` 后、`lstat` 前消失：fail closed，
  `package tree entry disappeared during scan`。
- valid tree 的 file inventory 只含真实 regular files，不含 dirs。

正式 package 完整 `lstat` universe：
- root：`1` 个真实 directory，mode `0755`。
- descendants：`128`。
- regular files：`107`。
- descendant directories：`21`。
- directories including root：`22`。
- symlink/special：`0`。
- artifact path allowlist / artifact records：`106 / 106`。
- exact directory allowlist：`21`。

## Direct Boundary Attack Matrix

对 current 和 archived runtime 分别直接攻击：
- `_directory_inventory()`
- `_artifact_records()`
- `_verify_artifact_closure()`

descendant attacks：
- dangling symlink。
- symlink -> existing regular file。
- symlink -> directory。
- FIFO。
- Unix socket（创建、保持打开完成调用、随后安全关闭）。

root attack：
- root symlink -> real package directory。

结果：
- current：`18/18` fail closed。
- archived：`18/18` fail closed。
- total：`36/36` fail closed。
- fail-open：`0`。
- unsupported entry 下三个 load-bearing boundary 均抛错，不返回原
  file_count/core/full identity。

## Production Package Attack Matrix

每个 descendant attack 都从正式 package 创建全新 APFS CoW clone，
再分别运行 current/archived standalone admission。root symlink 使用
指向全新正式包 clone 的 symlink root。

| attack | current | archived | exact failure |
| --- | ---: | ---: | --- |
| dangling symlink | rc=2 | rc=2 | `type=symlink` |
| symlink -> manifest file | rc=2 | rc=2 | `type=symlink` |
| symlink -> `anchors/` | rc=2 | rc=2 | `type=symlink` |
| FIFO | rc=2 | rc=2 | `type=fifo` |
| Unix socket | rc=2 | rc=2 | `type=socket` |
| root symlink | rc=2 | rc=2 | root is not a real directory |

结果：
- current：`6/6` fail closed。
- archived：`6/6` fail closed。
- total：`12/12` fail closed。
- fail-open：`0`。
- 所有 payload 都是 `verified=false`，不输出可信 file/core/full
  identity。
- max wall：`0.068s`；全部在 source-semantic replay 前快速拒绝。
- Round5 决定性 dangling symlink 已按真实 production package 重放并
  关闭。
- attack clone 和 special fixture 均已清理；本轮 QA root 最终 special
  entry 为 `0`。

## Fresh Full Rebuild And Identity

命令：

```text
/usr/bin/time -l python -B
examples/hyperliquid/cross_exchange_trigger_aligned_episodes.py
--output-dir /tmp/0815T003-qa-round6.xXfNdv/fresh/package
```

结果：
- 8 个 Jul30 segments 全部构建。
- build 内完整 source-semantic replay 通过。
- wall：`1317.25s`。
- max RSS：`1023721472 bytes`。
- swaps：`0`。
- residual staging dirs：`0`。
- fresh `.pyc/__pycache__`：`0`。
- fresh symlink/special：`0`。
- files/artifacts/bytes：`107 / 106 / 1561307420`。

Formal/Fresh 独立完整比较：
- relative path/type/mode/file bytes/raw SHA exact：`true`。
- file inventory exact：`true`。
- manifest semantic/raw bytes exact：`true`。
- root type/mode exact：`true`。
- core：
  `78be6559c7ac5aaec4d5411b042f7ddcce522473f60f987237bcaa9d2dd5e157`
- full：
  `669fb7d12f25cfa7828aec0fb1546398b1def754952de2290cd19784a477a433`
- contract：
  `b80b9bae2d6cf18cb6e7be4f133467138527fec20eef7054ab38b7f8c70cefde`
- manifest raw：
  `2c802336c1446eaf50eaf5ef546b115046abe3a1b69fdd1fcaea31d22a8e64a6`
- 四项均与 provisional package identities exact。

performance caveat：
- fresh build 前段只与短时 direct attacks、focused tests 和静态检查有
  轻量重叠；没有与另一个 full replay、inherited suite、gzip 或 data
  audit 并行。wall/RSS 是本轮实测值，不作为独立性能回归 gate。

## Durable 99-File Research Closure

durable pre-repair inventory：
- file raw SHA256：
  `07521dbc2c8bfe5f41a3ab14ac5493591dad878d52053b2d74aad251b9f022df`
- data rows：`99`。
- research bytes：`1560514934`。
- canonical inventory SHA256：
  `bb5aed2099b1a97da5b476d4b09dfe4a7bac06f0bf331f8ca864a88daf5c9232`

同口径：
- package 内所有 `.csv` / `.csv.gz`。
- 排除 `input_bindings.csv`。
- exact relative path、bytes、raw SHA256。

三方结果：
- Durable：`99 / 1560514934 / bb5aed...9232`。
- Formal：`99 / 1560514934 / bb5aed...9232`。
- Fresh：`99 / 1560514934 / bb5aed...9232`。
- Durable == Formal：`true`。
- Durable == Fresh：`true`。
- Formal == Fresh：`true`。
- missing/extra/changed：`0 / 0 / 0`。

## Aggregate Evidence V2 Regression

source-owned frozen contract：
- version：
  `skhynix_jul30_source_semantic_aggregate_evidence_v2`
- projections：`12`。
- aggregate-output bindings：`28`。
- unique aggregate-output fields：`28`。
- observed manifest key set与 derived expected key set exact：`true`。
- manifest/contract canonical pretty JSON：`true / true`。
- manifest contract SHA binding：`true`。

独立 current/archive negative matrix：
- 每侧完整 positive evidence：通过。
- 每侧 negative calls：`49`。
- current：`49/49` fail closed。
- archived：`49/49` fail closed。
- total：`98/98` fail closed。
- fail-open：`0`。

覆盖：
- semantic evidence missing/not-dict/false/extra。
- scope not-dict/value drift。
- projection missing/extra/non-dict。
- entry missing/extra/non-dict。
- fields type/value/order drift。
- expected/observed rows value/bool/string。
- digest mismatch/malformed/uppercase/non-string。
- mismatch nonzero/bool/string。
- exact counts missing/extra/value/bool/string/non-dict。
- aggregate counts missing/extra/value/bool/string/non-dict。
- binding duplicate/empty/bool/string/missing-key/extra-key。

Round4 决定性 extra-key：
- 只增加
  `manifest["aggregate_output_counts"]["unexpected"]=0`。
- current/archive 均 fail closed。
- exact failure：
  `manifest aggregate_output_counts key universe drift`。

## True Positive Admission And Zero-Write

命令：

```text
/usr/bin/time -l python
examples/hyperliquid/cross_exchange_jul30_episode_v3_admission.py
--verify-only --package-dir <Formal>
```

```text
PYTHONPATH=<Formal>/runtime_source /usr/bin/time -l python
<Formal>/runtime_source/cross_exchange_jul30_episode_v3_admission.py
--verify-only --package-dir <Formal>
```

默认 Python、无 `-B`：

| verifier | rc | verified | source semantic | wall | max RSS |
| --- | ---: | --- | --- | ---: | ---: |
| current | 0 | true | true | 925.72s | 860602368 |
| archived | 0 | true | true | 925.64s | 842104832 |

两侧共同：
- artifact/files/bytes：`106 / 107 / 1561307420`。
- core/full：provisional identities exact。
- Family A/B/rejected：`268522 / 141768 / 126754`。
- exact projections：`12`。
- aggregate mismatch sum：`0`。

zero-write / immutability：
- admission 前后完整 package path/type/mode/bytes/mtime/ctime/symlink-target
  metadata SHA：
  `177c0b53ffc5f28dd28ad97031c2fab3d553b09d055da705ab41f89d41dc68bb`
  / exact unchanged。
- default-Python admission 未创建 package/repo bytecode。
- input actual identity前后 exact：
  `d827fd88094e6b788ad2c52cbe14a2a71e69cc01f352bddf23898616926ada17`。
- accepted dependency identities前后 exact。
- actual input path/bytes/raw SHA mismatch：`0`。

performance caveat：
- current 与 archived true-positive admissions 同时运行，wall/RSS 是双侧
  并行实测值，不是 serial performance baseline。

## Dependencies And Input Immutability

accepted dependencies 独立重算：
- Stage 1：
  - `14` files / `1401387` bytes。
  - core：
    `9c2756f966a1a05816f9e6a91d7361d76cfea10fc3c587944ca2f0c1d2eb7c96`
  - full：
    `c540cc056313716b3bdd2b9c0fe076cda15a7f152b399ae6a1283b3aa8aa6590`
- Stage 2：
  - `16` files / `19834728` bytes。
  - core：
    `7b3d06c3225f77c9929cdd3fa40d69c0866d83dbb75dd584dcb7db6db22ad3f8`
  - full：
    `bdade16c53aed7bba54fdb9408ba3a8a03036d183720f589bda2a762448a3833`
- Stage 3：
  - `24` files / `40504469` bytes。
  - core：
    `4939d1c1addce493edb2f368297d56b37edd0b123de01497dcdee6e77637eb9b`
  - full：
    `ff8e3434672226371051151cea838503877dca79ac7860cf179256362d75e404`

Stage 4 input bindings：
- published rows：`788`。
- before/after：`394 / 394`。
- before/after payload exact：`true`。
- bytes：`1412652259`。
- manifest input inventory SHA：
  `31beb3ad8879842e597bddbd6784113e3413bf6d27350c61a1804b1d4f95879f`。
- scopes：
  - accepted Stage 1：`14`。
  - accepted Stage 2：`16`。
  - accepted Stage 3：`24`。
  - Jul30 campaign：`287`。
  - Jul30 R0/R1：`49`。
  - Stage 4 fixed source/test：`4`。
- actual mismatch：`0`。
- forbidden input path rows：`0`。

current/archive source/test bindings：
- builder source：
  `e9ead424e44f4837d55011944b2f5499be31b09bd27b630ec02155711aa7b25a`
- builder focused tests：
  `e87925f5786fe2f866833683af54b3a5f75b6ecaf0ffa0058479a78014444f84`
- admission source：
  `95b2b7e459344b7411e22cea76c82b8631277dac7c21cf9abe4c3a7ea87b6b12`
- admission focused tests：
  `6e4aa01220eaf9bb775ede586faac14d9843ed8ea824eada497512d3d11f74be`
- 四项 current/archive bytes与 manifest bindings exact。

## Full Data Semantics Audit

独立流式读取正式 package，corrected contract audit：
- wall：`141.11s`。
- max RSS：`304332800 bytes`。
- `all_checks_pass=true`。

population/linkage：
- anchors / Family A / outcomes：
  `268522 / 268522 / 268522`。
- Family B / confirmed：
  `141768 / 141768`。
- rejected：`126754`。
- Family A/B missing/extra：`0/0 / 0/0`。
- rejected non-null confirm：`0`。
- confirmed ordering/detector-confirm mismatch：`0`。
- clusters / flows / overlap blocks：
  `39928 / 10536 / 9`。

features：
- Family A/B：
  `23092892 / 15310944`。
- Family A/B feature-name universe：
  `86 / 108`。
- unavailable：`2863855`。
- 14 confirmation features：
  `1984752`。
- future observation：`0`。
- available provenance error：`0`。
- unavailable value/source fill：`0`。
- confirmation timeline provenance error：`0`。

paths：
- sparse A/B：
  `268522 / 141768`。
- fixed grid A/B：
  `4564874 / 2410056`。
- event count A/B：
  `805566 / 425304`。
- ID universe、landmark、epoch、multiplicity error：全部 `0`。

first-event outcomes：
- interval-censored：`627406`。
- right-censored：`177826`。
- segment-censored：`316`。
- quality-censored：`18`。
- epoch-censored：`0`。
- interval lower `< Candidate`：`0`。
- point/empty interval：`0`。
- interval missing source event：`0`。
- censor upper/source fabrication：`0`。

targeted regressions包含在 current/archive focused suites：
- rejected null confirm。
- feature observed-at after landmark fail closed。
- unavailable-to-zero fail closed。
- fixed-grid forward-fill no-new-information。
- interval-not-point。
- same-receipt trade observation interval。
- strict-pre equal-receipt outcome baseline。
- detector-state/frozen-burst confirmation features。
- distinct event-count source events。

source-semantic replay进一步对 12 个完整 ordered projections逐行 exact
重建，mismatch sum `0`；因此 strict-pre、burst membership、view payload、
feature provenance、path与outcome rows均无回归。

audit caveat：
- 第一版 QA audit 自行增加了“所有 censored rows 的 lower bound 必须为空”
  的非契约假设，因此将 `177826 + 316 = 178142` 个合法 right/segment
  censored last-non-event lower bounds误计为错误。
- 抽样核对 frozen contract 和实际 rows 后，修正为：censor 可保留
  Candidate 后的 last-non-event lower bound，但不得伪造 interval upper
  或 source event；v2 全量重跑 `all_checks_pass=true`。
- 这是 QA 审计器 caveat，不是 package defect。

## Tests And Static Verification

Pytest：
- current Stage 4 focused：`97 passed in 0.14s`。
- archived Stage 4 focused：`97 passed in 0.13s`。
- current Stage 1/2/3 inherited：`216 passed in 132.16s`。
- R0/alignment/recovery inherited：`83 passed in 0.64s`。
- current unique related total：`97 + 216 + 83 = 396 passed`。

其他：
- independent tree direct calls：`36/36` fail closed。
- production package tree attacks：`12/12` fail closed。
- aggregate negative calls：`98/98` fail closed。
- Ruff current/archive source/tests 8 files：
  `All checks passed!`
- external `PYTHONPYCACHEPREFIX` compileall：
  `52` bytecode files，全部位于本轮 `/tmp`。
- deterministic gzip integrity/header：
  - Formal：`96/96`。
  - Fresh：`96/96`。
- builder/admission `--help`：
  - builder：`18` lines。
  - source/archive admission：`12 / 12` lines，bytes exact。
- scoped `git diff --check`：通过。

## Atomic Publication

独立注入：
- monkeypatch `_process_segment()` 在 staging 写入 `partial-marker` 后抛出
  `qa injected failure`。

结果：
- injected failure observed：`true`。
- output visible：`false`。
- residual staging dirs：`0`。
- partial markers：`0`。
- wall：`2.21s`。

## Hard Boundary And Residual Scan

Formal/Fresh：
- manifest 十项 boundary 全部为 `false`。
- later-session/own-order/fill/PnL/model/score/actionability artifact paths：
  `0`。
- forbidden input path rows：`0`。
- symlink/special：`0 / 0`。
- `.pyc/__pycache__`：`0 / 0`。
- staging dirs：`0 / 0`。

本轮 QA：
- 未读取 Jul30 legacy episode rows。
- 未读取 Aug03/Aug04 future event rows。
- 未读取任何 Aug07 event rows。
- 未运行 case/model/score/actionability/orders/network/live。
- QA root final symlink/special：`0`。
- repo pycache path set before/after：`54 / 54`，new/missing `0 / 0`。
- 后台 build/admission/test sessions：`0`。

## Load-Bearing Commands

主要命令及证据：
1. fresh full rebuild：
   `/usr/bin/time -l python -B ... --output-dir
   /tmp/0815T003-qa-round6.xXfNdv/fresh/package`。
2. current/archive default-Python standalone admission：
   两条 `/usr/bin/time -l python ... --verify-only --package-dir <Formal>`。
3. current/archive Stage 4 focused pytest：
   `python -B -m pytest -q -p no:cacheprovider` 对各自两份 focused tests。
4. Stage 1/2/3 inherited pytest：7 个 inherited test modules。
5. R0/alignment/recovery pytest：5 个 inherited test modules。
6. Ruff：current/archive source/tests 8 files，`ruff check --no-cache`。
7. compileall：外置 `PYTHONPYCACHEPREFIX`，8 个 current/archive files。
8. Formal/Fresh gzip full decompression + deterministic header scan。
9. custom independent `lstat` tree scanner、direct boundary matrix、
   production CoW-clone matrix、aggregate v2 matrix、Formal/Fresh inventory、
   durable inventory、pre/post admission metadata/input inventory、full data
   semantics和residual scan；全部证据写入本轮 QA root。
10. atomic failure injection。
11. scoped `git diff --check`。

## 验收结论

- `已通过`
- P0/P1/P2/P3：`0/0/0/0`。
- Round5 dangling symlink/special-entry P1 已由 current/archive direct
  boundaries、正式 package standalone attacks 和 exact `lstat` universe
  三层独立关闭。
- Formal/Fresh identity、durable 99-file research closure、aggregate v2、
  true-positive source semantics、zero-write、atomic、dependencies/inputs、
  hard boundaries和完整 research semantics全部通过。
- `0815T003` 可以由总控关闭；Ordered Research Queue Stage 5 可以解锁。
- 本 QA 线程不自行派发或执行 Stage 5。

通过项：
1. exact tree source/contract/runtime identity和完整 special-entry closure。
2. direct `36/36`、production `12/12` tree attacks fail closed。
3. Formal/Fresh完整 tree与四项 identities exact。
4. durable/Formal/Fresh 99-file逐文件 exact。
5. aggregate v2 `12 projections / 28 exact keys` 与 `98/98` negative calls。
6. current/archive true-positive admission与默认 Python zero-write。
7. Family A/B、observed-at、confirmation/burst、strict-pre、interval
   censoring、paths/outcomes完整语义无回归。
8. `396` current related tests、`97` archived focused tests、Ruff、
   compileall、Formal/Fresh gzip全部通过。
9. atomic publication、dependencies/input immutability和hard boundaries通过。

不通过项：
1. 无。

缺陷清单：
1. P0：无。
2. P1：无。
3. P2：无。
4. P3：无。

阻塞项：
- 无。

建议总控下一步：
1. 将 `0815T003` 置为/保持 `已通过`并关闭。
2. 解锁 Ordered Research Queue Stage 5。
3. Stage 5 仍需由总控另行正式派发；本 QA 不自行启动。

报告文件一致性：
- 本报告原样镜像到 `docs/qa-acceptance-report.md`。
- 写入后执行 `cmp -s` 和 `shasum -a 256` 确认 byte-identical。
- 最终 report/mirror SHA256 在 QA 完成回复中给出，避免自嵌哈希。

提交信息：
- commit：无
