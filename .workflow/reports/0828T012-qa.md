# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0828T012

状态：
- 已通过

更新时间：
- 2026-08-28 15:56 CST

验收线程：
- 第六次独立只读 QA 验收线程

验收对象：
- FLOW_INTERNAL_DIRECTIONAL_ALPHA_V1 A0 Contract 业务线程
- fifth remediation commit：`e29a735d`
- commit record：`28b07cae`

验收范围：
- 复核第五次 QA 的两项 P1 findings。
- 终局审计 A0 population、gates、classification、A1 target 和 A3
  primary Delta_NLL/pass gate。
- 本轮只验收方案合同，不执行 A0。

验收步骤：
1. 核对 commit ancestry、变更范围、文件身份和 SHA。
2. 检查 A0-3/A0-4 gate statistic formulas。
3. 检查 `primary_tau` 的唯一生成和下游只读规则。
4. 复核 anchor/control population、target、model、score、bootstrap 和
   classification 完整链路。
5. 执行 source hash、Markdown、Git diff 和静态存在性检查。

实际结果：
- `e29a735d` 是 record commit `28b07cae` 的祖先。
- Plan SHA256：
  `6278d6b5f0d0dfd855b2d2ee121fa4fbebf6b3a864acecc4a0f5368e558aeeed`。
- Predecessor commit `91cc0770` 的四个 source-authority SHA 全部复算
  通过。
- Markdown fence 数量 `322`，配对通过；无 trailing whitespace。
- `git diff --check f2331f38..28b07cae` 通过。
- `detector_ready_hours`、`active_flow_hours`、两类 anchor rate、raw
  qualifying checkpoint、inter-anchor median、5s burst、direction/type
  shares 和 component-pair family 均有唯一公式。
- `primary_tau` 仅由 A0-7 按八项 geometry gates 产生；A1/A2/A3
  明确只读并禁止重选。
- A0 population、global no-reuse matching、multi-failure classification、
  A1 target、A3 preprocessing/model/score/bootstrap 均未发现剩余
  P0/P1/P2 自由度。
- 缺陷统计：`P0/P1/P2/P3 = 0/0/0/0`。

验收结论：
- 已通过
- 结论说明：
  - FLOW_INTERNAL_DIRECTIONAL_ALPHA_V1 A0 方案合同已达到可实现、
    可复现、可执行的冻结状态。
  - 本次通过只表示方案合同通过，不表示 detector 已实现或历史数据
    支持该 hypothesis。

通过项：
1. Exact causal feature、calibration 和 source authority contract 完整。
2. Total state transition、matching、dependence 和 A0 gate contract
   完整。
3. Primary one-tick target、`primary_tau`、censoring 和 event ordering
   完整。
4. H0/H1 estimand、fold-local preprocessing、ridge、solver、Delta_NLL 和
   PCG64 bootstrap 完整。
5. Zero-outcome boundary、H2 non-rescue 和 cost/execution boundary 完整。

不通过项：
1. 无

缺陷清单：
1. 无

阻塞项：
- 无方案合同阻塞。
- A0 尚未实现或执行；数据支持仍未知。

建议总控下一步：
1. 另行派发独立 A0 implementation/execution task。
2. 严格按冻结合同生成 contracts、support、summary 和 classification。
3. A0 执行结果仍需独立 QA，且不得用 outcome 调整 V1 tuple。

提交信息：
- commit：待提交
