# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0828T007

状态：
- 已通过

更新时间：
- 2026-08-28 12:00 CST

验收线程：
- QA验收线程

验收对象：
- SKHYNIX Liquidity Break Onset A0 Contract 业务线程
- initial contract commit：`9d941862`
- business report commit：`176df3b6`
- normalization remediation commit：`37a56393`

验收范围：
- 验收 `LIQUIDITY_BREAK_ONSET_V1` 与失败的 `OBI_REVERSAL_V1` 版本边界。
- 验收 raw-message event-driven start anchor、active-lock end anchor 和
  equal-timestamp ordering。
- 验收三个 interpretable pressure components、robust normalization、
  outcome-blind controls、target stub 和 H0/H1 information boundary。
- 验收 A0 zero-outcome gates、A1 timeliness stop gate 和 forbidden rescue
  operations。
- 验收 contract 可执行性、静态一致性、SHA 和 Git path scope。

验收步骤：
1. 对照 methodology kernel、phase-alignment negative result 和 OBI
   reversal A0-A3 canonical result。
2. 核对 `start_anchor` 不使用 future dwell、offline change point 或
   backdating。
3. 核对 `end_anchor` 仅用于 duplicate suppression，不冒充 recovery
   target。
4. 核对 detector predicate 不使用 midpoint、microprice、OBI reversal
   或 future price。
5. 核对 Revision 2 normalization：
   - 每条 eligible message 后仍执行 detector；
   - 使用最近完成的 20ms checkpoint；
   - equal timestamp 的未来 message 不进入 checkpoint；
   - rolling median 与 `IQR/1.349` 可确定性执行。
6. 核对 control matching 不使用 future anchor 或 outcome。
7. 核对 A1 `pre_detection_adverse_fraction <= 0.30` 在 target access 前已
   冻结。
8. 执行 Markdown fence parity、constant presence、`git diff --check`
   和 SHA closure。

实际结果：
- `LIQUIDITY_BREAK_ONSET_V1` 被明确注册为新版本，不允许作为
  `OBI_REVERSAL_V1` rescue。
- Event ordering 冻结为
  `(local_receive_ts_ns,event_seq_in_file)`。
- Primary detector 为 raw-message event driven；50ms window、L1-L5
  `1/l` weights、`z_star=3.0`、two-of-three coherence 和 score `6.0`
  均冻结。
- `start_anchor` 为 structural predicate 首次成立的当前 message，禁止
  backdate。
- `end_anchor` 为 causal release detection，仅用于 active interval
  管理。
- Revision 1 的 exact rolling MAD 执行风险在 outcome access 前被发现；
  Revision 2 改为最近完成 checkpoint 上的 median/IQR，并保留逐消息
  onset detection。
- Zero-outcome boundary、A0 support gates、A1 timeliness gate 和
  downstream claim boundary 完整。
- Markdown fence count 为 `134`，配对完整。
- `git diff --check` 通过。
- Final contract SHA256 为
  `b69146b92411a425e9d78d9deb88ac5347ccc0b2ad5b6f2ac32167b9eecb1141`。
- 三个相关 commits 仅包含 task、contract 和 business report 路径；
  未纳入工作树已有无关文件。

验收结论：
- 已通过
- 结论说明：
  - Revision 2 A0 design contract 在信息边界、状态机、统计门槛和执行
    算子上闭合，可作为独立 A0 implementation/execution task 的唯一
    authority。

通过项：
1. 新 hypothesis/version boundary 清晰。
2. Causal start/end anchor semantics 闭合。
3. Primary onset 不使用未来价格或完成态确认。
4. Robust normalization 可确定性执行。
5. Controls、target stub、H0/H1 和 stop gates outcome-blind 冻结。
6. 静态验证、SHA 和 Git scope 通过。

不通过项：
1. 无。

缺陷清单：
1. 无。

阻塞项：
- 无合同验收阻塞。
- A1 仍受 A0 execution passing gate 约束。

建议总控下一步：
1. 派发独立 A0 implementation/execution task。
2. A0 只生成 anchors、controls 和 support geometry，不读取 target。
3. 只有 `A0_causal_anchor_contract_supported` 才派发 A1 target support
   audit。

提交信息：
- commit：待 QA 事实源提交
