# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0617T005

状态：
- 已通过

更新时间：
- 2026-06-17 17:55 CST

验收线程：
- QA验收线程

验收对象：
- 业务线程-research 0617T005

验收范围：
- `Hyperliquid tiny-live read-only signal / quote replay`.
- 本轮验收 `0617T005` 业务报告、任务文件、replay runner、focused test、manifest、calibration/source/audit CSV、设计文档和 no-live/no-order/no-private/no-PnL 边界。
- 不验收真实 Hyperliquid 私有接口、账号、凭证、签名、nonce、user stream、下单、撤单、改单、live bot、真实 fill、真实 PnL、queue priority、post-only reject 行为或 maker viability。

验收步骤：
1. 阅读 `.workflow/tasks/0617T005.md` 和 `.workflow/reports/0617T005-business.md`。
2. 复核 `docs/hyperliquid_tiny_live_signal_quote_replay.md`、`replay_manifest.json`、`calibration_summary.csv` 和关键 CSV 行数。
3. 复跑 focused test、replay runner、JSON manifest validation 和 `git diff --check`。
4. 检查边界文本和 manifest boundary flags，确认未越界到 live/order/private/PnL/fill/maker-viability claim。

实际结果：
- `python -m pytest examples/hyperliquid/test_hyperliquid_tiny_live_signal_quote_replay.py -q` 通过，结果为 `1 passed`。
- `python examples/hyperliquid/hyperliquid_tiny_live_signal_quote_replay.py` 通过并重新生成 replay artifacts。
- `python -m json.tool local_live_analysis/hyperliquid_tiny_live_signal_quote_replay_0617T005/replay_manifest.json` 通过。
- `git diff --check` 通过。
- `replay_manifest.json` 记录 `pricing_rows_input_count=8`、`pricing_rows_replayed=161455`、`pricing_decision_rows_evaluated=26948`、`final_recommendation=hyperliquid_tiny_live_signal_quote_replay_ready_for_qa`。
- `source_availability.csv`、`threshold_sensitivity.csv`、`calibration_summary.csv`、`row_level_basis_distribution_by_sample.csv`、`row_level_audit_sample.csv` 均非空。
- `calibration_summary.csv` 中 primary candidate 为 `75` ticks / persistence `2`，共 `654` accepted theoretical intents，`313` buy / `341` sell，`2.4269%` intent rate，`8/8` samples with intent。
- stricter fallback 为 `75` ticks / persistence `3`，共 `327` intents，`148` buy / `179` sell，`1.2134%` intent rate。
- boundary flags 显示 credentials、private endpoint、account query、order placement/cancel/amend、live bot、real fill、PnL claim 均为 false。

验收结论：
- 已通过
- 结论说明：
  - `0617T005` 满足 read-only signal / quote replay 和 threshold calibration 验收要求；它可以作为后续 `0617T006` optimistic PnL proxy 的已验收前置，但仍不授权 `0616T008` live execution。

通过项：
1. Replay 正确消费 `0617T004` protocol rule source，并覆盖本机可用的 `8` 个 pricing-signal 输入。
2. 输出包含 source availability、threshold sensitivity、calibration summary、basis distribution 和 row-level audit。
3. 主候选和 fallback threshold/persistence 选择有明确样本覆盖和 intent-rate 证据。
4. no-private/no-credential/no-account/no-order/no-live/no-real-fill/no-real-PnL/no-maker-viability 边界成立。

不通过项：
1. 无

缺陷清单：
1. 无

阻塞项：
- 无

建议总控下一步：
1. 按已创建的 `0617T006` 任务继续执行 read-only optimistic PnL proxy。
2. `0617T006` 必须继续区分 optimistic public-data upper bound 与真实 PnL / execution viability。
3. 在 `0617T006` QA/controller ratification 前，不创建或执行 `0616T008` live execution。

提交信息：
- commit：34dd3ce / 0087ece
