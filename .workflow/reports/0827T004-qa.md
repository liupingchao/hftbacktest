# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0827T004

状态：
- 已通过

更新时间：
- 2026-08-27 CST

验收线程：
- QA验收线程

验收对象：
- SKHYNIX Binance Phase Alignment Track A0-A4 业务线程
- base execution commit：`974a88c1`
- minute-scale robustness commit：`7d26fa9a`
- latest business report commit：`f2ce4803`

验收范围：
- 验收现有历史数据是否按 outcome-blind、read-only 边界完整执行 A0-A4。
- 验收输入身份、top-5 重建、模型与基线、fail-closed gate、在线 replay、
  artifact closure 和 git scope。
- 本验收判断执行是否正确完成，不将负研究结论改写为正结果，也不把历史
  replay 宣称为 prospective。

验收步骤：
1. 重跑 focused pytest 与 Python compile。
2. 对 `track_a_manifest.json` 中全部 artifact 复算 size 和 SHA256。
3. 核对 primary classification、gate precedence、duration/grid sensitivity
   和 online metrics。
4. 核对 outcome/private/order/new-collection access ledger。
5. 核对 execution/report commit 的 exact path scope 和工作树隔离。

实际结果：
- Focused pytest 为 `5 passed`，`py_compile` 与 `git diff --check` 通过。
- Manifest 为 `92 artifacts / 92 size+SHA256 exact / 0 mismatch`。
- A0 接受 29 个 capture、9 个研究日期、35.917 小时和 1,292,945 个有效
  100ms reconstruction rows；全部 capture `valid_fraction=1.0`，零
  sequence gap。
- 状态 observation grid 为 200ms，高于最大 capture depth
  interarrival p99 `155.858ms`；400ms replay 在 validation/replay 的
  label-matched agreement 约为 `0.996`。
- K=6 Student-t HSMM validation mean log density 为 `-149.570`；
  continuous ridge VAR(1) 为 `-127.672`。Primary state gate 正确失败，
  classification 为 `continuous_state_no_discrete_phase_support`。
- Duration support 从 12.8s 扩到 25.6s、51.2s 后 score 均约
  `-149.483535211`，未改变结论。
- 分钟尺度 robustness 使用 1s grid、13 维固定 L1-L5 投影、shared
  emission scale、K=2/3/4 和每状态 2 个负二项 duration 参数。K=3
  被选中，共 66 参数。
- 60s/120s/300s support 的 validation density 分别约为
  `-37.1204 / -37.1221 / -37.1226`，扩大到 5 分钟没有改善。
- 39 参数 diagonal AR(1) 的 validation density 为 `-34.7476`，仍比
  66 参数分钟 HSMM 高 `2.3750`/row；不能用“连续 VAR 参数更多”解释
  离散模型失败。
- 排除 capture/chunk 首尾 right-censored runs 后，Q0/Q1/Q2 的完整
  `>=300s` dwell 数为 `0 / 8 / 11`，分钟 duration tail 未在全部状态
  上被识别。
- A3 被正确标记为
  `not_eligible_upstream_state_gate_failed`，没有把离散 decoder 内的
  recurrent grammar 当作正式通过。
- A4 no-refit replay run recall 为 `0.365`，late-detection fraction 为
  `0.529`，正确失败。
- Outcome access 为零：无 future join、无 private API、无订单、无新采集。
- Execution commit 严格包含 task、方案、runner 和紧凑结果包；business
  report commit 仅包含本任务回报。未混入原有已暂存 staleness 文档。

验收结论：
- 已通过
- 结论说明：
  - A0-A4 已按冻结边界执行、验证并 fail closed。`已通过`表示执行任务
    完成且证据闭合；科学结果本身为负，不授权 N/S/P/R mapping、Track B
    或 positive phase-grammar claim。

通过项：
1. 数据准入、因果特征、模型基线、duration/grid sensitivity 和在线 replay
   均有可审计 artifact。
2. 连续模型优于离散模型时正确触发 primary failure classification。
3. 下游 grammar 没有越过 upstream gate 获得正式通过资格。
4. 92-file artifact closure 与 zero-outcome-access 边界通过。
5. Git 提交 scope 干净，完整缓存保留在本地 ignored artifact root，紧凑
   结果包进入版本历史。

不通过项：
1. 无执行验收不通过项。

缺陷清单：
1. 无。

阻塞项：
- 无。

建议总控下一步：
1. 关闭当前离散 HSMM phase-cycle 路线，不进入 Track B。
2. 如继续验证 broad alignment hypothesis，应新立任务研究 continuous
   latent trajectory alignment 或 change-point-localized excursions。
3. 新任务仍需独立 prospective data；不得重命名 8 月 26/27 历史数据。

提交信息：
- commit：待 QA 事实源提交
