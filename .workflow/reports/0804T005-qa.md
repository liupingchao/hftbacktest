# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0804T005

状态：
- 已通过

更新时间：
- 2026-08-04 12:33 CST

验收线程：
- QA验收线程

验收对象：
- 业务线程-python/multi-market-queue-notebook-copies + 0804T005

验收范围：
- 五个 notebook 副本与五个原始 notebook 不可变边界。
- 多市场数据准备、contract multiplier、queue model 和 queue signal。
- point-in-time order quantity/ROI、权益时间对齐和 future mutation。
- Tardis-only/no-key/no-download/private API 边界。
- 本机与 amdserver tests、manifests 和 executed notebooks。

验收步骤：
1. 审查任务文件、实验 helper、共享映射、生成器、测试和五个副本。
2. 核对五个原始 notebook 的 HEAD、工作树和 SHA256。
3. 检查五交易所 source/staged/fused identity、tick/lot/multiplier。
4. 独立复算 flow base quantity 和多市场墙钟 as-of 组合。
5. 对 order qty/ROI 和 queue signal 做 future-mutation/hostile 检查。
6. 检查 amdserver 五个 manifests 和五本 executed notebooks。
7. 对三轮 QA 缺陷及修复做回归验收。

首轮缺陷：
1. P1：OKX/Gate flow quantity 仍为合约单位，不能与 BTC quantity
   直接跨 venue 比较。
2. P1：多市场权益删除 NaN 后按数组下标截断，没有按墙钟时间对齐。
3. P1：order qty 和 ROI 使用完整回测窗口中位价，存在 future look-ahead。
4. P2：future-mutation 测试未覆盖实际 Numba queue-signal helper。

第二轮缺陷：
1. P2：初始 mid helper 未限制 `DEPTH_EVENT` 且没有处理 `qty=0`
   depth 删除；hostile 输入可把成交或已删除价位误作初始 BBO。

修复结果：
- flow quantity 第 3:7 列统一乘 contract multiplier，Parquet 使用
  `*_qty_base`，manifest 使用 `quantity_unit=base_asset`。
- 多市场权益保留 timestamp，在共同 overlap 上按 epoch 100ms grid
  backward as-of。
- PreparedMarket 固化从首个完整可见 depth BBO 得到的 initial mid；
  order qty/ROI 不再读取全窗口中位价。
- queue signal 生产策略和 future-mutation 路径共同调用
  `_queue_signal_values`。
- initial mid helper 只处理 `DEPTH_EVENT`，维护 bid/ask price maps，
  支持 qty 更新与删除。
- 新增 trade-before-depth 和 delete-before-opposite-side hostile tests。
- 每轮代码修复后均重新执行受影响副本；最终修复后五本全部重跑。

实际结果：
- P0/P1/P2/P3 findings 均为 `0`。
- 本机和 amdserver focused tests 均为 `24 passed`。
- 五个原始 notebook 的 Git diff 为空，SHA256 与任务开始时一致。
- 五个副本均只有四个活动 code cell，原始教程代码仅位于 raw reference。
- 五本 executed notebook execution count 均为 `[1,2,3,4]`，
  error output 为 `0`。
- 五份 manifest 状态为 `1 passed / 4 adapted / 0 failed`。
- 五市场均实际读取：
  Binance、Bybit、OKX、Bitget、Gate。
- Gate buy quantity 为 `108.5931 BTC`，OKX 为 `223.5038 BTC`；
  base-unit 独立汇总误差仅为浮点精度。
- 多市场组合为 `2994` 个 epoch-100ms 对齐行；diversification ratio
  独立复算约 `1.482788874`，与 manifest 一致。
- 修改 Binance fused 数据未来半段价格不改变首个可见 mid
  `115697.35`。
- hostile cases：
  trade-before-depth `101.0`，delete-before-opposite-side `101.0`。
- queue signal future mutation 对历史前缀最大影响为 `0.0`。
- Tardis-only、无 key、无下载、无 private/account API 边界通过。
- amdserver 最终执行 HEAD：
  `23b045519f324e9cbd75b7a7863a003615d5fabc`。

验收结论：
- 已通过
- 结论说明：
  - 首轮三个 P1、两个阶段的 P2 均通过代码、hostile/future-mutation
    测试和服务器五本全量重跑闭合。

通过项：
1. 原 notebook 不变与副本 Run All。
2. 五市场真实 Tardis 输入和输入身份。
3. 跨 venue base-asset quantity。
4. epoch 100ms causal wall-clock equity alignment。
5. point-in-time initial BBO/order qty/ROI。
6. Queue Model 单变量对比。
7. causal book pressure/trade impulse/thin-queue backoff。
8. 本机与 amdserver tests/manifests/executed notebook 证据。

不通过项：
1. 无。

缺陷清单：
1. 首轮 P1 x3 / P2 x1，均已修复。
2. 第二轮 P2 x1，已修复。

阻塞项：
- 无。

残余风险：
- 正式执行仅覆盖 2025-08-01 前 300 秒。
- 同一 BTC 跨 venue 不等于原教程多资产组合。
- BTC queue signal 不等价于 CRVUSDT large-tick 行为。
- 统一费用和延迟是教学假设，不是各交易所生产参数。
- 短窗 PnL、ratio 和 Sharpe 类指标不构成生产收益结论。

建议总控下一步：
1. 将 `0804T005` 收口为 `已通过`。
2. 用户可在 amdserver 的副本目录打开五本 notebook 测试。

提交信息：
- implementation：`c44b024b534b763f65d1809afb962fcf80533426`
- conversion fix：`d27dfdc22e3cb6e2bacd777098276c13b54b64bc`
- finite-row fix：`baedf7cf56e15c8311bdf58701fc297ba96cd538`
- point-in-time/base-unit fix：
  `5e8352185ff33457d10169b98494011df8513697`
- wall-clock alignment：
  `602faefb1d55f8cc1119c224e48bb56e011db0e3`
- depth-visible initial price：
  `23b045519f324e9cbd75b7a7863a003615d5fabc`

文档占用说明：
- `docs/qa-acceptance-report.md` 仍含另一并行任务未提交的既有修改；
  本任务未覆盖该文件，最新结论保存在本任务专属 QA 报告中。
