# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0622T005

状态：
- 已通过

更新时间：
- 2026-06-22 22:42 CST

验收线程：
- QA验收线程

验收对象：
- 业务线程-live 0622T005

验收范围：
- 验收 M2 event-driven inline reprice / post-only reject repair 的实现、focused tests、远端刷新、final gate、public-only waiting boundary、inline reprice submit path、post-only reject retry matrix、tracked cancel / final open-orders proof、T008 ledger fail-closed、artifact 完整性和 redaction。

验收步骤：
1. 复核 `.workflow/tasks/0622T005.md`、`.workflow/reports/0622T005-business.md`、`task_plan.md`、`progress.md`、`findings.md`。
2. 复核 `local_live_analysis/hyperliquid_tiny_live_m2_inline_reprice_0622T005/` 下 watcher、inline reprice、order intent、latency、post-only reject、shutdown、independent open-orders、final gate 和 T008 ledger artifacts。
3. 运行 focused tests、`py_compile`、CLI help、artifact required/non-empty check、redaction scan 和 `git diff --check`。

实际结果：
- Implementation commits `741b5b2` and `c4faf36` implemented the inline watcher-local reprice / submit path; report/status commit `491aefd` recorded the formal live result.
- Focused regression passed: `test_hyperliquid_tiny_live_m2_event_driven_watcher.py` -> `5 passed`; `test_hyperliquid_tiny_live_m2_public_watcher.py` -> `4 passed`; `test_hyperliquid_tiny_live_m2_fill_loop.py` plus `test_hyperliquid_tiny_live_m2_pnl_ledger.py` -> `26 passed`.
- `py_compile` passed for `hyperliquid_tiny_live_m2_public_watcher.py`, `hyperliquid_tiny_live_m2_fill_window.py`, and `hyperliquid_tiny_live_m2_fill_loop.py`; three CLI help checks passed; `git diff --check` passed.
- Required artifacts were present and non-empty; artifact scan found `112` files and `0` empty files.
- Redaction scan found `0` suspicious 64-hex secret-shaped hits; all `8` 64-hex matches were allowed checksum/sha256 manifest entries.
- Remote refresh succeeded from `f6487c063e0d895c6bc118f6a1619d1cfbb855fb` to `c4faf36b7a60342f195238041d2b711ca300233e`; remote branch was `cross-exchange`, dirty count `0`, SDK availability `true`, and final gate returned `allow_create_0617T008=true` with `blocking_reasons=[]`.
- Waiting phase stayed public-only: `public_waiting_phase_private_or_order_endpoint_called=false`, selected public candidate recorded `no_private_or_order_endpoint=true`, and private/order endpoints were called only after trigger.
- The watcher ran `82.208056s`, evaluated `48` current candidates, triggered once, and stayed in the same process for inline submit (`same_process_trigger=true`, `controller_pullback_before_order=false`).
- Inline path submitted exactly `2` real order attempts, matching the task cap. Both used `Alo`, buy side, limit `64144.0`, size `0.00004 BTC`, and stayed below the unchanged `<=0.005 BTC` hard cap.
- Both inline guards passed with current/submit BBO `64144/64145`, `quality_a`, current touch match, post-only non-crossing proof, top order count `1`, and top-depth multiple `4.25x`; candidate ages were `0.559s` and `0.826s`.
- Attempt 1 latency split was `trigger_to_open_orders_start=0.000255s`, `open_orders_elapsed=0.2836s`, `open_orders_end_to_reprice=0.000136s`, `reprice_to_order_submit=0.000045s`, and exchange response `0.358896s`.
- Attempt 2 latency split was `trigger_to_open_orders_start=0.000193s`, `open_orders_elapsed=0.04255s`, `open_orders_end_to_reprice=0.000084s`, `reprice_to_order_submit=0.000043s`, and exchange response `0.413227s`.
- Hyperliquid rejected both attempts through post-only protection: first BBO moved to `64143@64144`, then to `64142@64143`. Retry handling obeyed the task contract: attempt 1 set `retry_allowed=true` with `wait_next_public_event_reprice`, attempt 2 set `retry_allowed=false` after the cap.
- No order rested or filled: `order_status_types=error,error`, `fill_count=0`, `maker_fill_count=0`, and `ledger_fill_rows=0`.
- Shutdown evidence passed: tracked cancel by cloid was attempted, `final_open_orders_count=0`, `shutdown_proof_status=pass`, and independent remote open-orders check returned `final_open_orders_empty=true` with no order/cancel endpoint call in that independent check.
- T008 ledger returned `live_realized_pnl_proof=false` and `realized_pnl_proof_status=fail_closed_no_realized_live_pnl`; no M2 completion, stable PnL, M3 readiness, cap relaxation, taker/crossing, `Ioc`, or one-tick-back claim was made.

验收结论：
- 已通过
- 结论说明：
  - `0622T005` meets the implementation, safety-boundary, artifact, and fail-closed QA criteria for the inline reprice / post-only reject repair. This is a task-level pass only; M2 remains blocked because T008 did not prove live maker fill / fee / inventory / realized PnL.

通过项：
1. Inline watcher-local reprice/submit path exists and does not use the full `fill_window.run_window` path for the triggered submit.
2. Public waiting boundary, remote refresh, final gate, `Alo`, `<=0.005 BTC`, max `2` submissions, tracked cancel, final open-orders proof, independent open-orders proof, redaction, and T008 fail-closed behavior all passed.
3. Post-only reject retry behavior followed the task contract: re-evaluate on the next public event, allow at most one maker-only retry, and do not switch to taker/crossing or loosen caps.

不通过项：
1. 无

缺陷清单：
1. 无 task-scoped safety defect found.

阻塞项：
- 任务级阻塞项：无
- M2 里程碑仍阻塞：`live_trigger_without_t008_realized_pnl_proof` / `ledger_no_live_realized_pnl_proof_after_trigger`

建议总控下一步：
1. Do not enter M3 and do not claim stable PnL.
2. Treat T005 as a completed repair that narrowed the bottleneck: post-`open_orders` reprice-to-submit is effectively immediate, while observed remaining failure is exchange-side fast BBO drift before post-only validation.
3. If continuing M2, the next task should target the remaining maker-only fill-acquisition problem without weakening `Alo`, `<=0.005 BTC`, T008 fail-closed, final open-orders proof, or redaction boundaries.

提交信息：
- commit：c74bad3
- commit：741b5b2
- commit：c4faf36
- commit：491aefd
