# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0706T005

状态：
- 已通过

更新时间：
- 2026-07-06 14:30 CST

验收线程：
- QA验收线程

验收对象：
- 业务线程-scoped-replay-acceptance 0706T005 / `0625T010-SCOPED`

验收范围：
- 验收 `0625T010-SCOPED Supported-Fact Same-Window Replay Acceptance` 是否只基于 `0706T002` 和 `0706T003` 本地 artifact，保守表达一单 submit/resting/cancel/final-open-orders 事实，并保持 unsupported 参数 fail-closed，没有新增 live/private/order/remote 行为。

验收步骤：
1. 复核 `.workflow/tasks/0706T005.md` 和 `.workflow/reports/0706T005-business.md`。
2. 检查 scoped replay acceptance artifacts：manifest、supported fact comparison、unsupported fail-closed matrix、optimism check matrix、boundary manifest、validation report、sha256 manifest。
3. 验证 JSON/CSV 可解析，所有 acceptance rows 为 `pass`，boundary flags 保持 no-live/no-remote/no-private/no-order，并运行 `git diff --check`。

实际结果：
- Output package exists at `local_live_analysis/cross_exchange_mvp_t010_scoped_replay_acceptance_0706T005/`.
- Final recommendation is `scoped_same_window_replay_acceptance_passed`.
- Source artifacts are limited to:
  - `local_live_analysis/cross_exchange_mvp_t008_live_submit_calibration_0706T002/pulled_back_awsserver1/`
  - `local_live_analysis/cross_exchange_mvp_t009_execution_outcome_calibration_0706T003/`
- Supported fact comparison passed `12/12`, covering order intent, `Alo`, submit path, resting status, primary tracked cancel, shutdown proof, and independent final open-orders count `0`.
- Unsupported fail-closed matrix passed `11/11`, covering submit/ack latency, resting duration, cancel latency, cancel-fill race, fill horizon, fill probability, fee/rebate, inventory transition, realized PnL, stable PnL, and maker viability.
- Optimism checks passed `8/8`; no fill probability, fill horizon, fee/rebate, inventory, realized PnL, reject-rate-zero, zero-latency, or maker viability assumption is inferred.
- Boundary manifest confirms no network, remote/AWS, credential read, private/account/order/cancel endpoint, live submit, market-data collection, strategy config change, production config change, PnL claim, maker viability claim, promotion, full T010 unlock, or T011 unlock.
- JSON parse, CSV schema/row checks, and `git diff --check` passed.

验收结论：
- 已通过
- 结论说明：
  - 0706T005 passes the scoped replay acceptance gate: replay can conservatively represent the observed one-order submit/resting/cancel/open-orders case without inventing unsupported economics or viability.

通过项：
1. Uses only accepted local `0706T002` and `0706T003` artifacts.
2. Supported facts match live/calibration evidence.
3. Unsupported execution/economics/PnL/viability domains remain fail-closed.
4. Optimism checks prevent invented fill, fee, inventory, PnL, reject-rate, latency, or maker viability assumptions.
5. No new live/private/order/remote behavior occurred.

不通过项：
1. 无

缺陷清单：
1. 无

阻塞项：
- 无 for scoped replay acceptance.

建议总控下一步：
1. Treat `0625T010-SCOPED` as complete.
2. Do not treat this as full `0625T010`, `0625T011`, stable PnL, maker viability, promotion, or final MVP pass.
3. Any next MVP-forward live evidence acquisition, full same-window replay acceptance, repeated-window task, or fill-seeking run requires a new formal task and explicit authorization.

提交信息：
- commit：无
