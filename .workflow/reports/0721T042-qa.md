# QA 验收结果

## Findings

1. P0：无。
2. P1：无。
3. P2：Representation gate 的执行顺序未完全符合任务合同。
   - `cross_exchange_delayed_history_probe_acceptance.py:1323` 先调用 `historical_reference_tokens()` 解释 identity/token semantics。
   - `cross_exchange_delayed_history_probe_acceptance.py:1329` 随后才调用 `persisted_history_order_evidence_issues()` 检查六种 alias 是否 exact 为 `<redacted>`。
   - 当前 hostile 矩阵均能 fail closed，没有形成已知 fail-open；但没有实现任务要求的“representation 通过后才允许 token semantics 重建”。
   - 建议先执行 representation 检查；存在任何 representation issue 时，不使用 raw identity 重建 token semantics。

执行线程：
- 独立 QA 验收线程

任务ID：
- 0721T042

状态：
- 未通过

更新时间：
- 2026-07-21 CST

验收线程：
- 独立 QA 验收线程

验收对象：
- implementation `21e0d2b120f100981aa7bd48e9c7bea2942bfff1`
- business report `8ffa9655e2edbd3aa1c1327a147dbc9a2f985cbe`

验收范围：
- Persisted nested 六种 identity alias 的 exact `<redacted>` representation。
- Raw/null/empty/partial marker、matching token/alias hostile。
- Producer-redacted canonical/alternative/dual positive rows。
- T041 raw_nested_identity、immutable T040、六个 T040 hostile。
- T037/T031/T026/T022/T016 exact historical boundaries。
- Focused/full regression及offline-only边界。

验收步骤：
1. 审查任务、业务报告、implementation/report commit及相关 verifier/redactor。
2. 独立运行 focused acceptance tests。
3. 构造限定的六 alias representation hostile/positive矩阵。
4. 复验 T041 raw_nested_identity、immutable T040及六个T040 hostile。
5. 复跑五个historical exact boundaries。
6. 启动full Hyperliquid regression；该命令在线程收敛时被中断，未产生最终汇总。

实际结果：
- Focused acceptance：`69 passed in 0.15s`。
- Full `examples/hyperliquid`：
  - 业务报告记录：`1212 passed in 51.11s`。
  - 本独立QA重跑未产生可采信的完成数字。
- 限定 identity 矩阵：
  - Hostile：`27/27 blocked`。
  - Producer-redacted positive：`9/9 pass`。
  - Hostile覆盖六种alias各自的raw/null/empty/partial marker，以及canonical/camel/snake三种dual identity。
  - Positive覆盖OID-only、cloid-only的六种alias及三种dual producer-redacted rows。
- T041 `raw_nested_identity`：
  - blocked。
  - `12 pass / 2 fail`。
  - Blocking checks：`history_result_envelope_contract`、`history_unknown_contract`。
- Immutable T040：
  - `14/14 pass`。
  - `blocking_checks=[]`。
- 六个T040 hostile全部blocked。

历史 exact boundary：
- T037：exit `2`，decision `39/4`，lifecycle `78/0`，blocked。
- T031：exit `2`，decision `43/0`，lifecycle `55/23`，blocked。
- T026：exact `1800s`，exit `0`，decision `43/0`，lifecycle `78/0`，pass。
- T022：exit `2`，decision `43/0`，lifecycle `66/12`，blocked。
- T016：exit `2`，decision `43/0`，lifecycle `66/12`，blocked。
- 五次manifest均为`offline_only=true`。
- `network/private/order/cancel/remote/new_live=false`，`credentials_read=false`。
- 本QA未运行live、private、account、order、cancel、network、remote或service操作。

验收结论：
- 未通过
- 已确认T041 raw nested identity fail-open关闭，全部限定hostile均blocked，合法producer-redacted rows和immutable T040保持通过。
- 但implementation没有严格实现“representation gate先于token semantics”的任务合同，且独立full regression未完成。

通过项：
1. 六种nested identity alias均要求exact `<redacted>`并能fail closed。
2. Raw/null/empty/partial marker及matching-token hostile全部blocked。
3. 合法canonical/alternative/dual producer-redacted rows全部通过。
4. T041 raw_nested_identity已关闭。
5. Immutable T040 `14/14 pass`，六个T040 hostile全部blocked。
6. 五个historical exact boundaries保持且全部offline-only。

不通过项：
1. Representation检查没有在token semantics调用之前执行。
2. 独立full Hyperliquid regression没有产生最终完成数字。

缺陷清单：
1. 将`persisted_history_order_evidence_issues()`移到`historical_reference_tokens()`之前；representation失败时不得从raw identity重建token。
2. 修复后重新完成focused及full `examples/hyperliquid` regression。

阻塞项：
- 无外部环境阻塞；当前为实现合同及独立验证未完成。

建议总控下一步：
1. 新建最小offline-only repair，调整representation/token semantic执行顺序。
2. 保留现有27个hostile和9个positive边界。
3. 完成独立full regression后重新进入QA；无需新live或修改immutable T040 evidence。

提交信息：
- implementation：`21e0d2b120f100981aa7bd48e9c7bea2942bfff1`
- report：`8ffa9655e2edbd3aa1c1327a147dbc9a2f985cbe`
- QA：无提交；未修改repo或immutable evidence，仅写入`/tmp`验收输出。
