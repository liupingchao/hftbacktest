# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0625T003

状态：
- 已通过

更新时间：
- 2026-07-05 20:10 CST

验收线程：
- QA验收线程

验收对象：
- 业务线程-research 0625T003

验收范围：
- 验收 `0625T003 Out-of-sample signal acceptance` 的任务文件、业务报告、signal acceptance runner/tests、`0627T001` QA-accepted input gate、row-level effective-horizon filter、leave-one-window-out split、candidate allowlist、side mapping stability、adjusted edge proxy、warning bucket treatment、boundary manifest、deterministic reproduction 和 git hygiene。

验收步骤：
1. 复核 `.workflow/tasks/0625T003.md`、`.workflow/reports/0625T003-business.md`、`task_plan.md`、`progress.md`、`findings.md`、`docs/cross_exchange_maker_mvp_plan.md` 和 `docs/cross_exchange_mvp_auto_loop_plan.md` 的任务边界与 auto-loop 条件。
2. 复跑 focused pytest、`py_compile`、runner CLI help、JSON/CSV artifact validation、deterministic reproduction、business commit diff check 和当前工作区 `git diff --check`。
3. 独立校验 `local_live_analysis/cross_exchange_mvp_signal_acceptance_0625T003/` 的 required artifacts、input gate、effective-horizon row counts、train/evaluation split integrity、held-out performance、warning bucket 和 no-live/no-private/no-order boundary flags。

实际结果：
- T003 business commit `1f79c1c` 存在于当前 `cross-exchange` 分支，提交信息为 `0625 accept cross exchange signal contract`。
- 当前工作区在 QA 开始时存在与 `0702T001/0702T002` 相关的未提交改动和未跟踪文件；这些不属于 T003。无 unresolved conflict，`git diff --check` 通过，T003 business commit 范围 `git diff --check 1f79c1c^..1f79c1c` 通过。
- Focused tests 通过：`python -m pytest examples/hyperliquid/test_cross_exchange_signal_acceptance.py -q` -> `3 passed`。
- `python -m py_compile examples/hyperliquid/cross_exchange_signal_acceptance.py` 通过。
- `python examples/hyperliquid/cross_exchange_signal_acceptance.py --help` 通过。
- Output package `local_live_analysis/cross_exchange_mvp_signal_acceptance_0625T003/` 的 12 个 required artifacts 全部存在且非空。
- `python -m json.tool` 可解析所有 output JSON；结构化校验通过 `signal_acceptance_manifest.json`、`input_gate_report.json`、`train_eval_split_manifest.json`、`accepted_signal_contract.json` 和 `boundary_manifest.json`。
- Input gate 通过：source task 为 `0627T001`，source recommendation 为 `sample_contract_ready_for_signal_acceptance`，`t003_creation_unlocked=true`。
- Effective-horizon-valid rows 为 `3587/3567/3550`，aggregate `10704`，三窗均满足 `>=20/window` 和 `>=100 aggregate`。
- Row condition 明确为 `valid_for_1000ms_signal_acceptance=true` 且 `1000ms <= effective_future_age_ms <= 1250ms`，nominal horizon 为 `1000ms`。
- Train/evaluation split 为 `leave_one_window_out`，三折 held-out window 均不在 train set 中，`same_window_threshold_backfill=false`。
- Accepted signal contract 为 `binance_lead_composite`，feature schema 为 `input_binance_top5_imbalance`、`input_binance_microprice_minus_mid_ticks`、`input_binance_mid_move_ticks_from_prev`。
- Normalization 为 `train_fold_z_score_mean_std`，threshold 为 `abs(z) >= 1.0`，side mapping 为 `positive_signal_buy_negative_signal_sell`，三折一致且没有 flip。
- Held-out active rows 为 `433/284/403`，max window contribution 为 `0.38660714`，未被单窗口主导。
- Held-out adjusted edge proxy 为 `13.52309469/1.89788732/4.02109181` ticks，三窗均为正；nonzero direction hit 为 `0.76530612/0.87234043/0.96428571`。
- `regime_stability.csv` 有且仅有一个 negative adjusted-proxy warning bucket：`xemm_0627_t001_hlfast_utc17_b / binance_source_age_mid`，active rows `89`，adjusted proxy `-0.93820225`。该 bucket 已在 manifest warning 中保留，应进入后续 T004/T005 诊断视野，但不构成 T003 acceptance blocker。
- Boundary manifest 确认 public-only/offline local processing/no network collection/no AWS execution/no remote alignment/no credentials/no private/account/order/cancel endpoint/no live client/no live orders/no shadow execution/no watcher strategy change/no canary/no promotion/future labels not decision inputs 均为 true。
- Deterministic reproduction 通过：重新运行 runner 到 `/tmp/0625T003_qa_repro.mtGw0i` 后，5 个 JSON、6 个 CSV 和 `recommendation.md` 在排除 generated timestamp 与 absolute paths 后一致。

验收结论：
- 已通过
- 结论说明：
  - 0625T003 满足 out-of-sample signal acceptance 的 input gate、effective-horizon gate、allowlist、leave-one-window-out、side mapping stability、held-out adjusted edge、boundary 和 deterministic reproduction 要求；QA 接受 business recommendation `signal_contract_accepted_for_shadow`。

通过项：
1. T003 使用 QA-accepted `0627T001` HL fast package，未使用 repaired-but-invalid ordinary T002 package 作为 signal acceptance rows。
2. Row-level effective-horizon gate 通过，aggregate valid rows `10704`，per-window `3587/3567/3550`。
3. Leave-one-window-out split 和 no same-window threshold backfill 证据成立。
4. Accepted contract `binance_lead_composite` 的 feature schema、normalization、threshold、side mapping、horizon、effective-horizon row condition 和 edge formula 均明确。
5. Held-out adjusted edge proxy 三窗均为正，side mapping 三折稳定，单窗口贡献未支配结果。
6. Negative source-age bucket 被保留为 warning，未被隐藏。
7. No-live/no-private/no-order/no-shadow/no-canary/no-promotion 边界成立。

不通过项：
1. 无

缺陷清单：
1. 无

阻塞项：
- 无

建议总控下一步：
1. 可按 `docs/cross_exchange_mvp_auto_loop_plan.md` 自动创建 `0625T004 Shared Signal and Quote-Intent Kernel`。
2. T004/T005 必须保留 T003 warning bucket 的可见性；T003 QA 不授权 watcher/live strategy change、private/order endpoints、live orders、shadow execution、canary、promotion 或 Hyperliquid replay/alignment 结论。

提交信息：
- commit：无
