# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0728T071

状态：
- 未通过

更新时间：
- 2026-07-28 15:33 CST

验收对象：
- 业务执行线程 / `0728T071 EC2-LATENCY-HUNT`

验收范围：
- 实现、10 台 EC2 采集证据、结果一致性、AWS 资源清理及 Git scope。

验收步骤：
1. 审阅任务、协议、业务报告、probe、远端脚本、analyzer 和 evidence。
2. 运行 Python、Shell、Rust 验证及全量 raw 重建。
3. 独立计算 Chrony clock error bound。
4. 通过 AWS CLI 核验实例、placement group 和临时桶。
5. 检查最新工作树、索引及 diff hygiene。

实际结果：
- pytest：`2 passed`。
- `bash -n`、Rust fmt、test、clippy：通过；Rust `14 passed`。
- 10 个 archive SHA、binary SHA 和 analyzer 结果全部一致。
- 本机 raw 重建：`20/20` 逐字节一致。
- 20 份 raw 的 measurement span 约 `899.566-899.964s`。
- Hyperliquid `feed_network` 已明确降级，未进入 ranking。
- AWS 无 T071 活跃实例，候选和 builder 均 terminated。
- T071 placement groups 为 `0`；临时 bucket 已删除。
- CSV LF 和 x86_64 `xlarge` 口径修正已进入索引。
- `git diff --check` 与 `git diff --cached --check` 通过。

验收结论：
- 未通过
- 采集、完整性、重建和实际资源清理成立，但 Binance
  `feed_network` 排名使用了未满足可信误差界的时钟证据，胜出结论不能
  成立。

通过项：
1. 10 台双 venue 900 秒采集、warmup、完整性和 binary hash。
2. `20/20` raw 独立重建及 archive 内容寻址。
3. Hyperliquid feed-network 降级正确。
4. 本次 AWS 资源确已清理。
5. 最新 CSV、任务单和 Git diff hygiene 正确。

不通过项：
1. 时钟门禁只检查 `System time <=100us`，未约束完整 clock error bound。
2. `metadata-before/after` 使用同一份 setup 阶段 Chrony 快照，不能证明
   900 秒窗口内的时钟状态。
3. 仓库没有可复核的 launch/state/tag-fallback cleanup 编排器。

缺陷清单：
1. P1：`c6in.xlarge` clock bound 约 `1.965s`，但其 Binance feed 指标
   仍参与推荐；另有多台候选存在同类问题。
2. P1：analyzer 只检查 `clock_gate == pass`，错误实例仍为 eligible。
3. P2：首次失败 builder 未进入本地 state file，失败留痕不耐久。
4. P2：clock bound 和 cleanup 故障路径缺少自动化测试。

阻塞项：
- 无外部阻塞；需要实现修复并重新采集可信 feed-network 证据。

建议总控下一步：
1. 采集前后实时执行 `chronyc tracking` 并计算完整 clock error bound。
2. 为 error bound 设置阈值；未通过实例不得进入 feed ranking。
3. 保留当前 local pipeline 结论，撤销 Binance feed 排名和综合推荐。
4. 实现受测的 Git 跟踪 EC2 launch/state/cleanup 编排器后重跑。

提交信息：
- commit：无
