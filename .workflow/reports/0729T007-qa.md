# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0729T007

状态：
- 已通过

更新时间：
- 2026-07-29 17:08 CST

验收线程：
- QA验收线程

验收对象：
- 业务线程-remote-public-data + 0729T007

验收范围：
- T006 runtime source identity、60 秒 canary、30 分钟 SKHYNIX 双交易所
  public-data 窗口、snapshot bridge、连续性、fast-L2 ACK 与本地完整性。

验收步骤：
1. 核对本地、远端和两个窗口中的 runtime source SHA。
2. 检查两个 transient systemd unit 终态和采集 manifest。
3. 独立解析两个 venue 全量 raw，重建 Binance bridge/continuity 事实。
4. 检查 Hyperliquid ACK、L2 shape、逐分钟密度和 gap。
5. 使用远端逐文件 SHA 清单在本地重新验证传输完整性。

实际结果：
- 两个窗口运行源码均为 T006 SHA
  `3e1335c4a7560ed850d6cfc99c09934f407f886828cd7f4f434f0a4441dcb310`。
- Canary 和 30 分钟 unit 均 `Result=success / ExecMainStatus=0`。
- 两个 Binance 窗口都在第一次 snapshot 过旧后，于同一连接第二次
  snapshot 成功 bridge；reconnect `0`。
- 30 分钟 Binance depth `58,845`，manifest 和独立 raw continuity gap
  均为 `0`，`depth_replay_ready=true`。
- 30 分钟 Hyperliquid L2 `3,317`、trades `5,570`，ACK 明确包含
  `fast=true`，29 个完整分钟均为 `107-112` L2。
- 双边 overlap `1799.999199135s`。
- 两个窗口远端/本地逐文件 SHA、gzip、JSONL、timestamp 和 manifest
  count reconciliation 全部通过。

验收结论：
- 已通过
- 结论说明：
  - 60 秒 canary 和 30 分钟 live validation 均满足冻结门禁；T005 暴露的
    snapshot 过旧 live 缺陷已由真实市场数据验证关闭。

通过项：
1. Runtime source provenance 一致。
2. Binance snapshot refresh、bridge 和连续性通过。
3. Hyperliquid fast-L2 ACK、密度、shape 和 symbol 通过。
4. 双边时长、overlap、单连接和零重连通过。
5. 远端到本地完整性及全量 parse 通过。

不通过项：
1. 无

缺陷清单：
1. P3：成功 run manifest 未显式保留两个 child return code；后续
   supervisor/observability 任务应补齐。

阻塞项：
- 无

建议总控下一步：
1. 将目标1标记完成。
2. 下一阶段进入 public collector supervisor、heartbeat/status 和 8H
   分段采集能力。

提交信息：
- commit：无
