# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0807T002

状态：
- 已通过

更新时间：
- 2026-08-07 15:26 CST

验收线程：
- QA验收线程

验收对象：
- 业务线程-python/research-postprocess + 0807T002

验收范围：
- Binance recovered reconnect admission 与严格计数对账。
- R0/R1 epoch、degraded sidecar、exact masks 和 label v4。
- basis 双 venue reset、历史隔离和 strict-as-of。
- raw immutability、pipeline fingerprint/runtime/artifact SHA 和 resume。
- compact evidence 的能力边界。

验收步骤：
1. 复核 reconnect count 缺失/伪造 0 hostile cases 及六类计数合同。
2. 独立扫描完整 R0、R1 和 basis 产物的 epoch、sidecar、mask 与 reset。
3. 重算 raw inventory、23 个 stage artifacts、runtime source 和 state
   fingerprint，并检查 resume 后 validator。
4. 对比本机 compact 与 AMD 完整产物的对应文件 checksum。

实际结果：
- P0/P1/P2/P3 findings 均为 0；第一轮 Binance reconnect count
  fail-open 缺口已关闭。
- Binance reconnect=`2`，attempt/subscription/bridge/bootstrap=`3`，
  disconnect=`2`；raw 内嵌 snapshot 实际计数为 `3`。
- disconnect equality 在 timeline 与 R0 均使用
  `disconnect_ts <= event_ts`。
- 独立扫描全部 `3,998,895` 条 R0 Binance hot rows：
  epoch mismatch=`0`、degraded sidecar mismatch=`0`，epochs 为 `0/1/2`。
- 独立扫描全部 `714,063` 条 R1 v4 labels：
  Binance sidecar mismatch=`0`、eligible timeline epoch mismatch=`0`、
  八个 horizon exact-mask mismatch=`0`、future source error=`0`、
  cross-epoch true=`0`、timestamp regression=`0`。
- basis Binance/Hyperliquid fast reset 为 `2/1`；旧 epoch BBO 抑制
  `816/4`，双 venue epoch regression 和 old-state leak 均为 `0`。
- raw before/after 均为 `39` 文件，inventory SHA256 均为
  `a30654f32cea375c49e4b26e39ed3c0de436e6d1f08de959601db749f262a003`。
- 独立重算 `23` 个 stage artifacts、共 `2,605,963,037` bytes：
  size/SHA mismatch=`0`，四个 runtime source mismatch=`0`。
- resume `reused_stage_count=4`，最终 validator 通过，pipeline manifest
  SHA256 为
  `6d5e7ec31a461569dec9392945263a4e7e1a4221d3a5f8ef1206691ebcffc4d0`。
- compact 对应文件与 AMD checksum 一致，README 准确说明其不能独立
  运行完整 validator。
- 本机和 AMD 回归均为 `108 passed`；QA 未修改文件、未启动采集、未
  访问 private/account/order/cancel endpoint。

验收结论：
- 已通过
- 结论说明：
  - 第一轮 P1 已关闭，R0/R1/basis、raw immutability 和 pipeline
    provenance 构成完整闭环，没有 P0-P3 缺陷或阻塞项。

通过项：
1. Binance recovered reconnect admission 与 count contract 通过。
2. R0/R1 v4 exact sidecar/mask/epoch closure 通过。
3. basis 双 venue reset 与 old-state isolation 通过。
4. raw/source/artifact/runtime/fingerprint provenance closure 通过。
5. resume 与最终 validator 通过。

不通过项：
1. 无

缺陷清单：
1. 无

阻塞项：
- 无

建议总控下一步：
1. 将 `0807T002` 状态更新为 `已通过`。
2. 保留当前能力边界，不从本任务推导 lead-lag、maker fill、PnL 或
   live readiness。

提交信息：
- commit：无
