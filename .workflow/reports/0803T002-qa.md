# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0803T002

状态：
- 已通过

更新时间：
- 2026-08-03 19:11 CST

验收线程：
- QA验收线程

验收对象：
- 业务线程-python/cross-exchange-research / 0803T002

验收范围：
- Aug03 十段 diagnostic M1、ShockAtom、ShockCluster /
  ContinuousFlowEpisode、baseline/post-selection、motif/prototype 和
  temporary regime 全管线。
- 0803T001 失败 R1 的 provenance、reconciliation/coverage 边界和
  `formal_eligible=false` 传播。
- row/SHA、segment conservation、motif BH、regime surrogate、独占锁、
  consumption reason、双 full build determinism。
- 本轮未修改实现、任务状态或业务产物；仅写入两份 QA 文档。

## Findings

- P0：无。
- P1：无。
- P2：无。
- P3：无。

验收步骤：
1. 阅读 workflow 规范、0803T002 task/business report、0803T001 R1
   失败事实、Jul30 T006-T009 QA 和 case hierarchy 方法计划。
2. 审计 Aug03 R1、diagnostic replay root manifest、五层
   `diagnostic_status.json`、stage manifest 和 frozen contract。
3. 独立复算 input/output row/SHA、atom/cluster/episode/phase
   conservation、split 和跨 segment 约束。
4. 独立重算 19 个 prototype 的 empirical p、BH q 和 classification；
   复核 regime `999 x 3` surrogate、candidate p 和 publication。
5. 审查并测试同输出目录独占锁、module configuration restore 和
   diagnostic consumption reason。
6. 在 `/tmp` 从同一 Aug03 R0/R1 输入完整构建两次，比较 55 个核心文件
   SHA。
7. 运行 focused pytest、正式 validators、py_compile、CLI help 和
   `git diff --check`。

实际结果：
- Source R1 / diagnostic boundary：
  - alignment manifest SHA：
    `5639173056fcd0cbbe92d3bbe2e2abb7e59d055081ead6772a77ad2ccb4ee702`。
  - `passes=false`、`reconciliation_pass=false`、
    `accepted_primary_horizons_ms=[]`。
  - M1 保留 `source_alignment_passes=false`、
    `source_alignment_reconciliation_pass=false`、
    `formal_eligible=false`、`diagnostic_mode=true`。
  - atom、episode-v2、baseline-v2、motif-v2、regime-v2 的 SHA-bound
    companion status 均为 `formal_eligible=false`、
    `classification_ceiling=diagnostic_only`，且
    `source_r1_failure_overridden=false`。
  - replay root manifest 同样为 `formal_eligible=false` /
    `diagnostic_only`；未声明 signal、arbitrage、exact fill、maker identity
    或 maker PnL。
- Provenance / artifact closure：
  - 独立复核 `109` 个 input provenance 条目和 `46` 个 manifest output
    条目；适用的实际 path、row count 和 SHA 全部一致。
  - root manifest 的七个 stage manifest SHA 和四个 Jul30 method
    reference SHA 全部一致。
  - M1：`127,622` candidates、`82,533` primary atoms，十段 episode
    row count 与 atom segment count 完全一致。
- Segment conservation：
  - `82,533` atoms 均唯一映射到 membership，无重复、无遗漏、无跨段。
  - `48,790` clusters 的 atom count 合计 `82,533`。
  - `24,040` ContinuousFlowEpisodes 的 atom/cluster count 分别合计
    `82,533 / 48,790`。
  - `29,465` phases 覆盖全部 `24,040` episodes，phase atom count 合计
    `82,533`；long-flow cases 为 `26`。
  - baseline features 完整分为 `6,486 discovery + 17,554
    post_selection = 24,040`，segment set 分别严格为 `0001-0003` 和
    `0004-0010`。
  - regime membership 对 `24,040` episodes 一对一，每行 decision
    timestamp 均命中同 segment 的唯一 frozen interval。
- Motif / prototype：
  - discovery eligible `6,462`，post-selection eligible `17,552`，
    membership `18,587`，prototypes `19`。
  - 每个 prototype 的 surrogate count 均为 `199`。
  - 独立 BH 重算得到 empirical p-value 范围 `0.98-1.0`，全部
    `q=1.0`。
  - prototype 与 surrogate 两份文件的 p/q/classification 逐项一致；
    19 个 prototype 全部为 `not_supported`。
- Temporary regime：
  - `300` 个 one-minute context windows，boundary audit `250` 行。
  - frozen contract 为 primary `5min x 999`，并包含
    `3/5/7min` 各 `999` 次 block-shuffle diagnostics。
  - real spacing candidates 为 `5` 个，family-wise p-value 为
    `0.827 / 0.139 / 1.0 / 1.0 / 1.0`。
  - primary global boundary-count p-value 为 `0.336`。
  - significant candidate 为 `0`，published data-driven boundary 为
    `0`；十个已发布 boundary/interval 全部为机械 segment boundary /
    `context_only`。
- Lock / consumption repair：
  - `_exclusive_output_lock()` 使用输出目录集合的 order-independent
    identity 和 non-blocking `flock`；同目录并发 writer 被拒绝。
  - focused tests 覆盖 concurrent writer rejection 和目录顺序交换。
  - consumption manifest 为 `formal_heldout_authorized=false`、
    `label=post_selection`。
  - reason 精确为
    `Aug03 R1-unaccepted diagnostic evaluation; no formal held-out claim`。
  - 同路径第二次 full build 保留相同 consumption run identity/time；
    baseline、motif、regime validators 均通过。
- Determinism：
  - 两次独立 full build 分别耗时 `787.21s` 和 `786.58s`。
  - 每轮均成功重建完整 M1 到 regime 管线，核心计数与正式包一致。
  - 两轮各提取恰好 `55` 个核心文件；两份 SHA 清单 `diff` 为空。
  - 两份 SHA 清单摘要均为
    `093b3f837395ea55cec957303fe23d4a16693a94dbbcc1181b8e5a1d5fe0c899`。
- 验证结果：
  - Focused combined pytest：`70 passed in 144.31s`。
  - baseline discovery/post、motif-v2、regime-v2 正式 validators：通过。
  - provenance/row/SHA/segment/statistical independent check：通过。
  - 六个实现模块及 replay test `py_compile`：通过。
  - replay CLI `--help`：通过。
  - `git diff --check`：通过。
  - 最终 diff/status 检查确认 QA 前既有工作区改动未被本线程修改；
    本线程仅新增本报告并替换最新 QA 结论文档。

验收结论：
- 已通过
- 结论说明：
  - Aug03 diagnostic hierarchy 在失败 R1 上按冻结 Jul30 v2 方法完整、
    可追溯且确定地重放；所有层级保持 diagnostic-only，统计结果不支持
    motif 或 data-driven regime boundary，也未越权形成正式信号结论。

通过项：
1. Source R1 failure 和五层 diagnostic/formal-ineligible 边界传播。
2. M1 至 regime 的 provenance、row/SHA 和 segment conservation。
3. 19 prototype 的 p/BH q/classification 独立复算。
4. `999 x 3` regime surrogate 和零 data-driven boundary。
5. 独占锁与 diagnostic consumption reason 修复。
6. 两次完整构建的 55 核心文件 byte determinism。
7. Focused tests、validators、compile、CLI 和 diff check。

不通过项：
1. 无。

缺陷清单：
1. 无。

阻塞项：
- 无。

建议总控下一步：
1. 将 `0803T002` 收口为 `已通过`。
2. 保持 Aug03 结果为 diagnostic evidence；在 0803T001 R1
   reconciliation/coverage 被独立修复并通过前，不得升级为 formal
   signal、arbitrage 或 maker 结论。

提交信息：
- commit：无
