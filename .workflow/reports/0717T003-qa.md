# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0717T003

状态：
- 已通过

更新时间：
- 2026-07-17 14:19 CST

验收线程：
- QA验收线程

验收对象：
- 业务线程-live-safety-audit 0717T003

验收范围：
- 验收本轮是否完成 `trade_logs/0717trade_history.csv` 对 0717T002 live artifacts、代码 symbol guard、以及 awsserver1 只读账户作用域的安全审计。
- 本 QA 不验收 live retry、quote-policy 改动、fee/PnL、maker viability、T004 public shadow、promotion 或 final MVP pass。

验收步骤：
1. 检查 0717 trade history 是否按实际表头解析。
2. 检查 0717 CSV rows 与 0717T002 window/order intent 的时间、symbol、side、price、size 对齐关系。
3. 检查 0717T002 artifacts 的 submitted/resting/reject/fill ledger/role evidence。
4. 检查 Hyperliquid live runner 代码是否存在 WTIOIL symbol route。
5. 检查 SSM read-only account-scope audit 是否没有提交/撤单并给出账户视图结果。

实际结果：
- `trade_logs/0717trade_history.csv` 成功解析 `293` 行。
- 0717 CSV 中两笔 BTC long 如果按上海时间解释，分别落在 0717T002 window 01 和 window 02，且与 runner 的 BTC buy intent 精确匹配：
  - `BTC 0.00067 @ 63422`
  - `BTC 0.005 @ 63150`
- 0717 CSV 中 `WTIOIL (xyz) Open Short 1.14 @ 78.51` 如果按上海时间解释，落在 0717T002 window 03。
- 0717T002 window 03 artifacts 只有两个 `BTC buy` intent，且均为 post-only immediate-match reject；reject 文本的 `asset=0` 经 meta 审计为 `BTC`。
- 代码审计未发现 Hyperliquid live runner 可在该路径下提交 WTIOIL 的生产路径；当前 intent validation 和 fill attribution 都有 BTC symbol guard。
- SSM read-only account-scope audit 显示 awsserver1 env account 与 wallet-from-private-key 为同一地址，但该地址在 0717T002 相关 UTC 时间段的 fills/recent fills/open orders/positions 均为 `0`/empty。

验收结论：
- 已通过
- 结论说明：
  - 本审计任务已完成并给出明确安全结论：0717T002 的原始 no-fill 结论被 0717 trade history 反证，且当前存在账户/成交来源 provenance mismatch；后续 live 必须暂停到 account/fill source identity 闭环。

通过项：
1. 0717 trade history 被按正确表头解析。
2. BTC fills 与 runner intents 的强匹配关系已记录。
3. WTIOIL short 未被归因到 repo runner，证据边界已记录。
4. awsserver1 read-only account-scope mismatch 已记录。
5. 未进行 live、下单或撤单。

不通过项：
1. 无。

缺陷清单：
1. 0717T002 artifacts 的 `live_fill_ledger.csv` 和 `fill_liquidity_role_evidence.csv` 为空，但外部 0717 trade history 显示至少两笔 BTC fill 与 runner intents 精确匹配，因此 0717T002 的 no-fill 结论不可继续使用。
2. 当前 live artifact 没有足够强的 account/vault/API-wallet provenance 字段来证明 order submission account 与 fill pullback account 是同一个交易账户视图。
3. `run_intent_marker.json` 和部分 inline finalize path 仍显示 `window_id=1`，这会降低多窗口/多 attempt 审计清晰度。

阻塞项：
- 后续 live test、T004 public shadow、fee/PnL calibration、maker viability 均应阻塞，直到 account provenance / fill source identity guard 被修复并通过 QA。

建议总控下一步：
1. 派发一个修复任务，为 live order artifacts 增加 account provenance guard：order-submit account、fill-pullback account、post-state account、wallet/account/vault hash 必须一致或 fail-closed。
2. 修复 multi-window artifact id/window id 的硬编码问题。
3. 修复后用 0717 trade history 和 0717T002 artifacts 做 offline regression，先不要再跑 live。

提交信息：
- commit：无
