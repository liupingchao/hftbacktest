# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0728T070

状态：
- 已通过

更新时间：
- 2026-07-28 12:56 CST

验收线程：
- QA验收线程

验收对象：
- 业务执行线程 `0728T070`
- 当前 Git index 中 staged 的 16 个文件

验收范围：
- 统一延迟合同、Rust probe、逐 trace benchmark、完整性与 drop 统计。
- 零有效消息 fail-closed、queue full/disconnected 回归。
- 内容寻址 evidence、源码和 artifact 哈希、summary 确定性重建。
- Workflow 状态、任务范围、staged scope 和引用完整性。
- 本轮严格只读，未修改、暂存或提交仓库文件。

验收步骤：
1. 从全新 Git index 快照读取源码、任务、业务报告、契约和 evidence。
2. 独立计算 manifest 中全部 source/artifact SHA-256。
3. 使用 staged release binary 从两家 raw NDJSON 重算 summary 并逐字节比较。
4. 运行 Rust 1.93.0 fmt、test、clippy、CLI、synthetic 和 exact benchmark。
5. 运行 Binance 与 Hyperliquid 小样本只读公共 smoke。
6. 检查 staged scope、状态、引用和 diff。

实际结果：
- staged scope 为 16 个文件；`.DS_Store` 未 staged。
- `git diff --cached --check` 通过。
- manifest 中 4 个 source SHA-256 和 5 个 artifact SHA-256 全部匹配。
- 两份 raw NDJSON 均为 100 行，trace ID 为唯一连续的 `1..100`。
- Binance 与 Hyperliquid raw 均为 `latency-trace-v1`，venue/channel/symbol
  正确。
- 两家 ACK、fill 实际记录数均为 0，未伪造私有订单生命周期。
- 两份 summary 从 raw 重算后均与 staged summary 逐字节一致。
- 接受基线一致：Binance P50 `7.271 us`、P99 `66.089 us`、feed invalid
  `45`；Hyperliquid P50 `42.521 us`、P99 `109.273 us`。
- 保存 benchmark 为 `batch_size=1`、P50 `334 ns`、P99 `500 ns`、drop
  `0`。
- 本轮 exact benchmark：P50 `333 ns`、P99 `458 ns`、drop `0`、门禁通过。
- 1,000 条 synthetic trace 重建逐字节一致，完整率 100%，
  duplicate/out-of-order/drop 为 0。
- Binance 和 Hyperliquid 公共 smoke 各 `10/10` complete、drop `0`。
- `cargo +1.93.0 test -p latency-probe`：12 个 library 测试和 1 个 binary
  测试通过。
- `cargo +1.93.0 clippy -p latency-probe --all-targets -- -D warnings`：通过。
- `cargo +1.93.0 fmt -p latency-probe -- --check`：通过，仅有仓库
  nightly-only 配置 warning。
- CLI 和 `public --help` 通过，`--connect-ip`、`--tls12-only` 可见。

验收结论：
- 已通过
- 结论说明：
  - 上轮零样本、批次均摊 benchmark、drop 回归和证据 provenance 问题均已
    关闭；当前 index 可构建、可复算、可审计，并满足 T070 验收标准。

通过项：
1. 15-stage、14 interval 和 Unix/monotonic 时钟域语义正确。
2. P50/P90/P99/P99.9/max、missing、invalid 统计可靠且 deterministic。
3. trace association、duplicate、out-of-order 和 drop 统计完整。
4. benchmark 使用真实逐 trace 样本，`batch_size=1`。
5. bounded handoff 不隐藏 full/disconnected drop，相关回归通过。
6. 零有效 BBO 运行 fail-closed。
7. Binance/Hyperliquid parser 和公共 probe 未隐藏未知协议消息。
8. `--connect-ip` 保留 hostname、SNI 和 HTTP Host。
9. ACK/resting/fill/cancel 未被公共 probe 伪造。
10. 内容寻址 evidence 与当前 staged 源码闭合。
11. Task、business report、contract、progress 和 findings 状态及基线一致。
12. staged scope、引用和 diff check 通过。

不通过项：
1. 无。

缺陷清单：
1. 无。

阻塞项：
- 无。

建议总控下一步：
1. 将 `0728T070` 状态更新为 `已通过`。
2. 将本 QA 结果写入 `.workflow/reports/0728T070-qa.md` 和
   `docs/qa-acceptance-report.md`。
3. 提交当前 staged T070 源码、工作流文档和内容寻址 evidence。
4. 下一正式任务进入真实 connector/策略路径埋点，不沿用公共 loopback
   数据宣称生产订单延迟。

提交信息：
- commit：无
