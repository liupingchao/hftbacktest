# QA 验收结果

执行线程：
- QA验收线程（第二轮全新独立验收）

任务ID：
- 0815T002

状态：
- 未通过

更新时间：
- 2026-08-15 20:45 CST（星期六）

验收线程：
- QA验收线程

验收对象：
- 业务线程-python/research + `0815T002` bounded repair
- 当前 source/tests：
  - `examples/hyperliquid/cross_exchange_liquidity_response_trigger.py`
  - `examples/hyperliquid/cross_exchange_liquidity_response_episodes.py`
  - `examples/hyperliquid/cross_exchange_trigger_parity_admission.py`
  - 三个对应 Stage 3 focused test files
- 正式 Stage 3 package：
  `local_live_analysis/skhynix_trigger_aligned_episode_research_v1_stage03_detector_parity/`
- Build A：
  `/tmp/0815T002-r1-parity-a.AQc2i0/package`
- Build B：
  `/tmp/0815T002-r1-parity-b.Wce87m/package`
- QA 独立 raw-input replay：
  `/tmp/0815T002-qa-r2-replay.X4QYAh/package`

验收范围：
- 独立复验首轮 QA 的六项 bounded repair，不继承首轮或业务结论。
- 审查 shared detector、historical builder integration、source/archive
  runtime identity、package self-containment、exact artifact/binding
  closure、zero-write、canonical serialization、atomic publication和 hard
  boundary。
- 独立比较全部 Jul30/Aug03/Aug04 candidates，包括 rejected population、
  36-field text、row/segment/session order、candidate sequence、
  attribution、rejection、primary selection 和 19 个 segment digest。
- 从 detector/pre-state raw inputs 完成一套新的 production replay。
- 构造 production-size coherent-rehash/self-report/path/symlink/parser
  攻击副本；不修改业务代码、正式 package、accepted dependencies、
  历史数据、task/controller tracking，不开始 Stage 4。

普通代码审查：
- P0：`0`
- P1：`1`
- P2：`0`
- P3：`1`

P1 finding：
1. `parity_manifest.json` 的四个 Stage 1/2 dependency identity 字段只是
   schema 成员，没有与 accepted anchors 做值比较；同一个 accepted core
   可以携带伪造的依赖声明并得到 `verified=true`。
   - Expected constants 已固定在
     `cross_exchange_trigger_parity_admission.py:50-60`。
   - `verify_package()` 把四个字段列入 expected keys
     `:1912-1915`，但 `:1921-1929` 的 identity checks 没有检查它们。
   - `_verify_bindings()` 会在 `:1821-1849` 重新验证真实 Stage 1/2
     package 和 exact binding rows，但没有把结果与 manifest 的四个
     dependency claims 交叉比较。
   - QA 在生产大小副本
     `/tmp/0815T002-qa-r2-manifest-deps-fast.dMEiCF/package` 中，仅将：
     - `stage1_core_sha256`
     - `stage1_full_inventory_sha256`
     - `stage2_core_sha256`
     - `stage2_full_inventory_sha256`
     全部改成 `000...000`，并保持 canonical manifest bytes。
   - 没有改 artifact、bindings、accepted dependencies 或 core。
   - Source 和 archived CLI 均返回 `rc=0`、`verified=true`，并继续报告
     accepted core
     `994c12f98dadb7201ad03b67d3c116caa85778233b9c1bc58fccc6b799124fb4`。
   - 因 manifest 自身不进入 core artifact list，这个假 dependency
     identity 与 accepted core 共存；仅依赖 verifier/core 的 Stage 4
     consumer 会收到一个被标记为 verified 的错误 provenance 声明。
   - 该问题直接影响 Stage 4 dependency credibility，按本轮裁决规则
     不允许放行。

P3 finding：
1. `frozen_trigger_contract.json` 只做 semantic JSON equality，没有冻结
   canonical raw bytes。
   - `_read_json()` 在 `cross_exchange_trigger_parity_admission.py:404-411`
     只解析 JSON；contract verification 在 `:1930-1934` 使用该 reader。
   - QA 将正式 contract 改为语义相同的 minified JSON，并同步重算
     contract SHA、artifact records、core 和 canonical manifest。
   - Source/archive verifier 均返回 `rc=0`、`verified=true`；替代
     contract SHA 为
     `b3adf20dc0059ef46db381c5cdeabc0897617f40e2f406b89cd91262e740cc75`，
     替代 core 为
     `6ce2acaebd8bb2dc6c33a70233050d9f6e1a62a642e2b1c36dfa3a8b89ecd62e`。
   - 该攻击会改变 core/full identity，不能伪装成当前 accepted package，
     且不改变 detector semantics，因此列为 P3；建议同一 bounded repair
     将 contract reader 升级为 canonical raw-byte check。

首轮六项修复复验：
1. Self-contained baseline：通过。
   - QA 将 `/tmp/0815T002-pre-extraction.4r9BcF` 原子移动到隔离名称，
     确认原路径不存在后运行 source/archive 默认 verify-only。
   - 两者均 `rc=0`、输出 byte-identical，随后在 `finally` 语义下恢复
     baseline。
   - `226` bindings 中 ephemeral baseline references 为 `0`。
2. Archived verifier identity：通过。
   - 修改 archived admission source，随后 coherent refresh runtime
     binding、artifact records 和 core。
   - Source/archive 均 `rc=2`：
     `fixed worktree source/test archive identity drift`。
3. Exact artifact universe和 hard boundary：通过。
   - 在 production-size copies 中分别加入：
     - `unexpected.txt`
     - `outcomes/future_outcome.csv`
     - `aug07/events.csv`
     - `fixture/pre/episodes/segment_0002.csv.gz`
   - 每项均同步重算 artifacts/core，并扩大 manifest self-reported
     allowlist；source/archive 仍全部 `rc=2`。
   - 额外空目录和 package symlink 也分别 `rc=2`。
4. Default zero-write：通过。
   - 默认 `python`、无 `-B` 的 source/archive verify-only 均 `rc=0`。
   - QA 对 package root、全部 directories/files 比较 path、type、
     bytes、SHA、`mtime_ns`、`ctime_ns`，前后完全一致。
   - 无 `.pyc` 或 `__pycache__`。
5. Current tests archive/binding：通过。
   - 三个 runtime source 和三个 current tests 的 fixed worktree bytes、
     package archive bytes、manifest SHA maps 和 input bindings 精确闭合。
   - 修改 archived admission test 并 coherent refresh test SHA、
     artifacts/core 后，source/archive 均 `rc=2`。
6. Canonical manifest：通过。
   - Semantically identical minified `parity_manifest.json` 在 source/archive
     两侧均 `rc=2`：
     `parity manifest canonical bytes drift`。

独立 detector/parity 证据：
1. Shared detector 保持纯、版本化。
   - `CONTRACT_VERSION=cross_exchange_queue_shock_trigger_v1`。
   - `AUDIT_FIELDS` 精确 `36`。
   - Shared module 仅使用 Python stdlib 和传入的 normalized states/trades；
     无文件、网络、subprocess、outcome、markout、PnL、Aug07、
     private/account/order/cancel access。
2. Historical builder 继续真正复用 shared detector。
   - constants、state records、audit schema 和 candidate helper 均来自
     shared module。
   - Segment path 调用 `shared_trigger.detect_candidates()`；未发现第二份
     candidate/primary state-machine implementation。
3. QA 独立逐行比较正式 projection 与三个 historical trigger audits。
   - `463612` candidates。
   - `267554` primary。
   - `196058` rejected。
   - `36` audit fields；projection 加 `session_id` 后 header 为 `37`。
   - Field-text/order mismatch：`0`。
   - Candidate-sequence mismatch：`0`。
   - Historical omission/extra projection rows：`0`。
   - Segment count：`19`；每个 segment count/primary/row-stream SHA
     与 frozen anchors 一致。
4. Session-level exact results：
   - Jul30：
     `268522 / 141768 / 126754`，row-stream SHA
     `e2b4e998b87c81c1f8350a92bd1253db35fcb396cabe557773364535190ceed4`。
   - Aug03：
     `127622 / 82533 / 45089`，row-stream SHA
     `73b8e5ae2ad6652c811e26c7df60d5cacba1a4febe812c0960450a7ea88fc309`。
   - Aug04：
     `67468 / 43253 / 24215`，row-stream SHA
     `fa537846e50760a50ee38882c8d39884d6f240f3f844c3ae8b1b66fe10367dcf`。
   - Attribution、rejection reason 和 session/segment order 均与正式
     summaries 精确一致。
5. QA 独立 raw-input full replay：
   - 输出：
     `/tmp/0815T002-qa-r2-replay.X4QYAh/package`。
   - 从 frozen detector/pre-state inputs 重放全部 `463612` candidates；
     未读取历史 episode response rows 或 Aug07 event rows。
   - QA replay 与 Formal 的全部 `24` paths/bytes/SHA byte-identical。
   - Core：
     `994c12f98dadb7201ad03b67d3c116caa85778233b9c1bc58fccc6b799124fb4`。
   - Full：
     `7bff0caf92856fdda0115fabd5fdc066bee49ea4cfba7f2091a5ccc63b38386e`。

正式 package identity：
- Formal、Build A、Build B 和 QA replay 均：
  - `24` files
  - `40,499,445` bytes
  - Core SHA256：
    `994c12f98dadb7201ad03b67d3c116caa85778233b9c1bc58fccc6b799124fb4`
  - Full inventory SHA256：
    `7bff0caf92856fdda0115fabd5fdc066bee49ea4cfba7f2091a5ccc63b38386e`
- Formal/A/B 全部 relative paths、bytes、逐文件 SHA 和 raw file bytes
  一致。
- Formal manifest：
  - `23` artifacts + manifest 自身；
  - manifest SHA256：
    `16c5a3fb2d0ee7503ccf0c4cba7890fbb15a8035445023f4a16ac6f2bf197260`。
- Frozen research artifacts：
  - Candidate projection：
    `68e65a612d7bce2911a4e3c96fd10528914290ca045c8912e6be45045fd0415f`。
  - Trigger contract：
    `a894079e405073400d86f8471fd20e2a3a7116d3b804952bab8db56587f93b3d`。
  - Session CSV：
    `67b8035aa5b8ce0cf650c6e8465aa83284e741f6d8ba3c92f492274c7267f534`。
  - Segment CSV：
    `c4ab6a2a23e6349c5a77a9f30373b09f1ffa3724d278afe49f24047444a736f9`。

Binding、dependency和 immutability 证据：
- `input_bindings.csv`：
  - `226` exact-width rows；
  - before/after 各 `113`，逐行镜像；
  - `226` full rows 全部唯一；
  - accepted dependency `2`；
  - historical opaque inventory `3`；
  - historical trigger truth `6`；
  - detector input `79`；
  - current source/test `6`；
  - package-internal baseline `11`；
  - package-internal source/test `6`。
- 独立检查 `108` file bindings：存在、bytes、SHA 均一致，mismatch `0`。
- Unknown scope、role、content-mode、raw `/./` path、CSV extra cell 和
  missing cell 攻击在 source/archive 两侧全部 `rc=2`。
- Package-internal paths 均为 canonical relative paths；external paths 均为
  canonical absolute paths。
- Accepted Stage 1：
  - exact root preserved；
  - `14` files / `1,401,387` bytes；
  - core
    `9c2756f966a1a05816f9e6a91d7361d76cfea10fc3c587944ca2f0c1d2eb7c96`；
  - full
    `c540cc056313716b3bdd2b9c0fe076cda15a7f152b399ae6a1283b3aa8aa6590`。
- Accepted Stage 2：
  - exact root preserved；
  - `16` files / `19,834,728` bytes；
  - core
    `7b3d06c3225f77c9929cdd3fa40d69c0866d83dbb75dd584dcb7db6db22ad3f8`；
  - full
    `bdade16c53aed7bba54fdb9408ba3a8a03036d183720f589bda2a762448a3833`。
- Historical opaque package inventories unchanged：
  - Jul30：`11` files / `79,979,087` bytes /
    `603565e840ff99fdf050f9c862c382524934ef8e2034cf5bc668bfdf8d7a6387`。
  - Aug03：`13` files / `46,747,139` bytes /
    `50a95175db0e0abc36604e1f6d3481d094270ee9ceebf24259356dfe8479e648`。
  - Aug04：`4` files / `24,531,687` bytes /
    `1a182c9ae346e6a3c2989734d18316ffc78e26bc3f40aeadc335f1a625c856c3`。

运行命令与结果：
1. Required Stage 3 suite：
   `python -B -m pytest -q
   examples/hyperliquid/test_cross_exchange_liquidity_response_trigger.py
   examples/hyperliquid/test_cross_exchange_liquidity_response_episodes.py
   examples/hyperliquid/test_cross_exchange_trigger_parity_admission.py`
   - `43 passed in 118.28s`
2. Alignment/density regressions：
   `python -B -m pytest -q
   examples/hyperliquid/test_cross_exchange_trigger_aligned_episode_contract.py
   examples/hyperliquid/test_cross_exchange_trigger_density_core.py
   examples/hyperliquid/test_cross_exchange_candidate_episode_merging.py
   examples/hyperliquid/test_cross_exchange_trigger_density_inputs.py
   examples/hyperliquid/test_cross_exchange_trigger_density_admission.py`
   - `183 passed in 8.86s`
3. Merging/recovery regressions：
   `python -B -m pytest -q
   examples/hyperliquid/test_cross_exchange_candidate_episode_merging.py
   examples/hyperliquid/test_cross_exchange_liquidity_response_case_hierarchy.py`
   - `33 passed in 0.16s`
4. Ruff：
   - `All checks passed!`
5. 外置 `PYTHONPYCACHEPREFIX` compileall：
   - pass；未在 source/package 写 bytecode。
6. Formal 全部 gzip：
   - `gzip -t` pass。
7. Scoped `git diff --check`：
   - pass。
8. Source/archive `--help`：
   - 均 `18` lines、bytes identical。
9. Source/archive default verify-only：
   - 均 `rc=0`、positive output byte-identical；
   - path/type/bytes/SHA/mtime_ns/ctime_ns zero-write。
10. Formal/A/B：
    - `diff -rq` 无差异；
    - independent full-inventory comparison byte-identical。

Atomic publication：
- Focused suite 的 injected pre-publication failure 保留原 accepted marker，
  且未留下 temporary output。
- Producer 在 publication 前验证 complete temporary package，并使用
  same-parent atomic directory exchange；本轮未发现新的 partial-output
  fail-open。

边界结论：
- 未读取任何历史 `episodes/*.csv.gz` response/outcome rows。
- 对历史 package 仅做 opaque compressed-byte inventory/hash；仅允许的
  fixture episode bytes 用于 pre/post byte comparison。
- 未读取 Aug07 raw/R0/R1/basis event rows。
- 未构建 Episode v3、feature、outcome、case distribution、model、score、
  calibration、incremental test 或 actionability。
- 未访问网络、AWS、SSH、private/account/order/cancel endpoint。
- 未启动采集、下单或修改 live/active config。
- 未修改业务代码、正式 package、accepted dependencies、历史数据、
  `.workflow/tasks/0815T002.md`、`task_plan.md`、`progress.md` 或
  `findings.md`。

验收结论：
- 未通过
- 结论说明：
  - Shared detector extraction、historical builder integration、首轮六项
    bounded repair、全量 `463612`-candidate parity、Formal/A/B 和 QA
    independent replay 均成立。
  - 但 manifest 的 Stage 1/2 dependency identities 可以在 accepted core
    不变时被伪造，并由 source/archive verifier 标记为 verified。该 P1
    直接影响 Stage 4 对 Stage 3 provenance 的信任，因此本轮不能关闭
    Stage 3。

通过项：
1. 首轮六项 bounded repair 全部独立复验通过。
2. Shared detector purity/versioning、historical builder shared-module
   integration 通过。
3. 全 candidates、全部 rejected、36-field、row/order/sequence、
   attribution/rejection/primary 和 19-segment parity 通过。
4. Formal/A/B/QA replay determinism、core/full identity 通过。
5. Bindings、actual Stage 1/2、historical/input immutability 和 hard
   boundary 通过。
6. Required tests、regressions、Ruff、compileall、gzip、diff check 和
   zero-write 通过。

不通过项：
1. Manifest Stage 1/2 dependency claims 未与 accepted anchors 交叉验证，
   可在 accepted core 不变时 coherent falsify。

缺陷清单：
1. P0：无。
2. P1：Stage 1/2 dependency identity manifest self-report fail-open。
3. P2：无。
4. P3：Frozen trigger contract canonical raw bytes 未冻结。

阻塞项：
- 无外部阻塞；可在同一任务内 bounded repair。

建议总控下一步：
1. 不关闭 Stage 3，不派发 Stage 4；将 `0815T002` 保持在同任务 bounded
   repair。
2. 在 `verify_package()` 中把 manifest 的四个 Stage 1/2 identity 字段
   与 hard-coded accepted constants 及 `_verify_dependency_packages()`
   的实际结果同时做 exact equality。
3. 新增 production-size regression：四个 dependency claims 分别和组合
   coherent mutation 时，source/archive 必须 fail closed，且特别断言
   accepted core 不变也不能通过。
4. 同轮将 `frozen_trigger_contract.json` 改用 canonical raw-byte reader，
   或显式绑定 accepted contract SHA
   `a894079e405073400d86f8471fd20e2a3a7116d3b804952bab8db56587f93b3d`。
5. 重新原子发布 Formal/A/B，生成新的 core/full 后再派全新独立 QA。

报告文件一致性：
- 本报告正文机械复制到 `docs/qa-acceptance-report.md`。
- 写入后执行 `cmp -s` 和 SHA256 验证 byte-identical。
- 为避免报告自嵌自身哈希造成递归变化，最终 report/mirror SHA256 记录在
  QA 完成回复中。

提交信息：
- commit：无
