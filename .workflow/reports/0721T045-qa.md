# QA 验收结果

## Findings

1. P0：无。
2. P1：无。
3. P2：`(event_sequence, attempt)` exact join没有真正保持优先。
   - Bridge在`cross_exchange_t024_same_window_acceptance.py:5711-5735`先对所有canonical manager event强制要求event只有一条freshness row。
   - Exact join直到`:7066-7074`才执行。
   - 为T044 attempt 2增加一条合法、逐字段匹配的exact freshness row后，attempts 1/2均可exact join，但仍产生`manager_batch_public_state_freshness_row_count:1896:2`。
   - 关闭bridge后，同一双exact fixture无freshness validation reason。
4. P2：manager bridge资格没有完整证明submitted和完整window identity。
   - `canonical_manager_batch_attempt_rows_by_event()`只要求`order_endpoint_called=true`，未拒绝`order_status_types=skipped`或空值。
   - `window_id`为空时也被允许，只在非空时比较。
   - 独立调用确认`skipped_status=true`、`empty_status=true`、`missing_window=true`均可取得canonical manager batch资格。
5. P2：T045业务报告曾记录错误的implementation SHA；已在本QA记录前更正为实际对象
   `3fcb883e69431f16f264f0e4f0ef1707a78137a8`。

执行线程：
- 独立 QA 验收线程

任务ID：
- 0721T045

状态：
- 未通过

更新时间：
- 2026-07-21 23:03 UTC

验收对象：
- Implementation commit `3fcb883e69431f16f264f0e4f0ef1707a78137a8`
- Business/report commit `3f452117e1100a6450f6f7bb156d3efba6cae565`
- Immutable T044 evidence

验收范围：
- Exact join优先级、strict manager bridge、hostile fail-closed、legacy边界、immutable T044 replay及完整Hyperliquid回归。

实际结果：
- Immutable T044 acceptance exit `0`：decision `43/43`、lifecycle `78/78`、mechanism/evidence `pass`。
- Provenance `113/113`、config `72/72`、economics `6/6`、optimism `6/6`。
- Boundary为`offline_only=true`；network/private/order/cancel/remote/new-live/credentials-read均为false。
- Terminal manifest独立重算`108/108 pass`；immutable evidence未重封。
- Missing、真实duplicate identity、cross-event、wrong task/window、endpoint-false、wrong side、non-consecutive、empty projection和mismatch均产生fail-closed reason。
- Legacy `0719T001`保持non-strict；旧two-sided acceptance测试通过。
- Multi-level仍为locked。

实际命令与结果：
- Immutable T044 acceptance：exit `0`，decision `43/0`、lifecycle `78/0`。
- 三个核心测试：`3 passed in 3.58s`。
- Acceptance测试文件：`244 passed in 9.36s`。
- Watcher测试文件：`133 passed in 20.69s`。
- Full `examples/hyperliquid`：`1214 passed in 51.67s`。
- `py_compile`、implementation/report/current diff check：通过。
- 独立hostile矩阵发现上述exact-two-row false-negative及skipped/empty-status/missing-window bridge资格问题。

验收结论：
- 未通过。
- 当前T044 artifact能够通过修复后的verifier，但T045没有满足exact join优先和strict bridge资格两个明确合同。
- 在缺陷修复并重新QA前，不应将T045标记为通过或正式关闭same-window baseline gate。

通过项：
1. Immutable T044目标回放正确且offline-only。
2. 大部分身份、projection和cross-boundary hostile保持fail-closed。
3. Legacy `0719T001`边界未变化。
4. Focused及完整回归通过。

不通过项：
1. 双per-attempt exact freshness被错误拒绝。
2. Skipped/empty-status和missing-window rows可取得manager bridge资格。

缺陷清单：
1. 仅在某个canonical submitted attempt缺少exact join时计算bridge；若两个attempt均有exact row，不得执行event-level唯一row检查。
2. Bridge资格要求`order_status_types`非空且不为`skipped`。
3. Bridge资格要求`window_id`显式存在且与expected window exact匹配。
4. 增加双exact rows通过、skipped、empty status、missing window和真实duplicate identity回归。

阻塞项：
- T045验收。
- Bounded dynamic-spread后续任务、multi-level及其他adaptive activation继续锁定。

建议总控下一步：
1. 创建新的offline-only最小修复任务，不启动新live window。
2. 修复后只读重跑同一immutable T044和完整Hyperliquid回归。
3. 通过QA后再决定是否关闭same-window baseline gate。

提交信息：
- QA commit：无；本线程未修改仓库或immutable evidence。
