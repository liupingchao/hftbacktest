# QA 验收结果

## Findings

1. **P0：无。**

2. **P1 - Anti-drift 独立重建仍可被同步派生标签绕过。**
   - Acceptance 信任 `current_cross_risk` 和 `adverse_flow_status`，没有从 `side/limit_px/current_bid/current_ask` 及 adverse/favorable quantity、ratio、adverse BBO 重新计算。
   - 离线反例设置：buy `limit_px=ask=101`、adverse `0.04 BTC`、favorable `0`、ratio `inf`、adverse BBO=true，同时将两个派生标签改为 pass。
   - Helper 返回 `('pass', '')`；同步修改 anti row 和 submit row 后，完整 decision summary 仍返回 `validation_reasons=[]`。
   - 这违反“从 raw quantitative evidence 独立重建、同步 stage 标签篡改必须 fail closed”的验收标准。

3. **P1 - Late halt 覆盖并丢失原始 immediate-guard evidence。**
   - Watcher 在 `guard_rows.append(event_guard)` 后原地将同一 dict 改成 `persistent_kill_switch_gate`。
   - 因此 late halt 场景只保留 kill-switch 行，之前的 immediate guard pass/status/reason/source 被覆盖。
   - 这直接违反任务要求“各 stage 原始 evidence 独立保留，不得覆盖或删除”。
   - 应增加独立 late-halt stage row，而不是修改 immediate guard row。

4. **P2 - Immediate guard 的 independent reason reconstruction 与 producer 不完全一致。**
   - Producer 使用 `elif`：报价不等于 bid 时只记录 `selected_quote_not_current_touch`。
   - Acceptance 使用两个独立判断，还会追加 `post_only_buy_would_cross_current_ask`。
   - 离线探针中 producer reason 为一个 atom，independent reason 为两个 atom，会错误阻断合法的 fail-closed evidence。

执行线程：
- 独立 QA 验收线程

任务ID：
- 0721T038

状态：
- 未通过

更新时间：
- 2026-07-21 18:27:28 CST

验收线程：
- 独立 QA 验收线程

验收对象：
- implementation `c436ce00cee50a03c3accadc01517691880a9927`
- business/report `792c6dbc538482ffb53b821eb71e4b2f5764e357`

验收范围：
- Canonical primary precedence、stage evidence、public-state binding、late halt、manager hold shutdown v2 和 T038 rollout。

验收步骤：
1. 阅读 workflow、任务、业务报告及 T037 QA。
2. 审查两个提交及四个实现/测试文件。
3. 运行 compile/diff、focused regression 和 hostile inline probes。
4. 发现足以阻断验收的 P1 后停止扩展测试并返回结果。

实际结果：
- Commit hash 和提交范围正确。
- 四个关键 Python 文件内存 compile 通过。
- Implementation `git diff --check`、cached diff check 通过。
- Watcher focused：`109 passed in 20.75s`。
- Acceptance focused：`231 passed in 12.31s`。
- Canonical helper的 immediate > anti > edge > pass 基本路径成立。
- Hold shutdown v2 已加入 strict boolean、source close、thread exit、ack、observer/cancel timeline检查。
- 但上述两个 P1 证明 mechanism/evidence integrity 合同仍未闭合。
- QA 未继续运行 full Hyperliquid 和五组历史 replay；已有 P1 足以作出未通过结论。

验收结论：
- **未通过**
- 正常回归通过，但 anti-drift quantitative reconstruction 可被绕过，且 late halt 会覆盖原始 immediate evidence。

通过项：
1. Trigger、attempt、submit-decision canonical primary outcome 主路径已统一。
2. Public-state freshness/source/cardinality 和 attempt projection 检查已增加。
3. Hold shutdown v2 的时间链和 cancel cross-artifact 检查已实现。
4. T038 rollout predicate 未直接追溯应用到 T037。

不通过项：
1. Anti-drift raw quantitative evidence 未被真正独立重建。
2. Late halt 覆盖 immediate guard 原始证据。
3. Immediate reason reconstruction 与 producer 不完全相同。

阻塞项：
- T038 mechanism/evidence integrity 未通过。
- 新 live/private read、adaptive 和 multi-level activation继续锁定。

建议总控下一步：
1. 派发 offline repair，按底层数量重建 anti-drift cross-risk 和 pressure。
2. 将 late halt 建模为独立 stage，保留原始 immediate guard row。
3. 对 immediate helper逐分支复用同一纯函数或增加 producer/independent property test。
4. 修复后重新执行 focused、full Hyperliquid 和五组历史 replay。

提交信息：
- commit：无
- QA严格只读；未修改文件、未提交、未执行live/private/account/order/cancel/network/remote/service。
