# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0514T003

状态：
- 已通过

更新时间：
- 2026-05-14 10:31 Asia/Shanghai

验收线程：
- QA验收线程

验收对象：
- 业务线程-python + 0514T003

验收方式：
- 正常验收

验收范围：
- 基于 `0514T002` 已通过的计划，实现只读 Stage 4 pricing-model research runner，并在 `5-13-day-control-30min` 上全量运行。任务只产出研究指标和候选/拒绝信号报告，不改策略、不启动 live、不改 core/connector/schema。

验收步骤：
1. 读取 `.workflow/tasks/0514T003.md`。
2. 读取 `.workflow/reports/0514T003-business.md`。
3. 检查任务状态、业务回报、verify 证据、done 结论和阻塞项。
4. 写入 `.workflow/reports/0514T003-qa.md` 并刷新看板。

实际结果：
- 任务文件存在：.workflow/tasks/0514T003.md
- 业务回报状态：待验收
- 业务回报存在：.workflow/reports/0514T003-business.md
- `0514T002` plan 已由 QA runner 验收通过。
- `0514T003` read-only pricing research runner 已实现并在 `5-13-day-control-30min` 全量运行。
- 研究产物已生成到 `local_live_analysis/5-13-day-control-30min/stage4_pricing_research_0514T003/`。
- 当前任务进入 QA。

验收结论：
- 已通过
- 结论说明：
  - 0514T003 QA 通过：read-only pricing research runner 已实现并在 5-13-day-control-30min 全量运行，产物包含 summary、candidate metrics CSV/JSON、bucket tables、markout_by_horizon、rejected_signals 和 run_manifest；验证通过 pytest 4 passed、--help、全量 runner、dashboard、git diff --check。样本口径：decision rows 47499，primary non-stale rows 47067，stale excluded 432，future/missing/gap-crossed/startup rows 均为 0。按重要性排序的信号分组如下：第一组 strongest pricing candidates 为 top5_imbalance、top1_imbalance、top1_microprice_edge_ticks、top5_microprice_edge_ticks，最佳 500ms raw future-mid markout abs Spearman 约 0.68-0.71，top-bottom spread 约 63-69 ticks；第二组 depth-size / liquidity candidates 为 top5_depth_imbalance_qty、top5_ask_qty、top5_bid_qty，abs Spearman 约 0.65-0.66；第三组 existing model / recent move diagnostics 为 fair_edge_ticks、recent_mid_move_ticks、audit_bbo_mid_edge_ticks，abs Spearman 约 0.33-0.48；第四组 weaker but usable follow-up signals 为 top5_ofi_proxy、top1_ofi_proxy、top5_liquidity_concentration、audit_feed_latency_ms，abs Spearman 约 0.11-0.23。重复信号已明确降级：reservation_edge_ticks duplicates fair_edge_ticks，audit_mid_edge_ticks duplicates audit_bbo_mid_edge_ticks，book_view_stale_ms 与 latency_signal_ms duplicates audit_feed_latency_ms。弱或不稳定信号包括 spread_ticks、bookticker_mid_edge_ticks、bookticker_depth_age_ms、vol_bps、bookticker_join_age_ms、max_join_age_ms、inventory_score、spread_bps、depth_join_age_ms、top5_join_age_ms。边界清楚：T003 只证明这些信号对 raw future top5 mid markout 的研究相关性，不证明策略 PnL、side-adjusted execution outcome、full L2、exact queue/fill、live readiness，也不授权策略实现或 live。

通过项：
1. 业务回报已进入待验收状态
2. 任务声明需要 QA 验收
3. 业务回报包含 verify 证据
4. 0514T003 QA 通过：read-only pricing research runner 已实现并在 5-13-day-control-30min 全量运行，产物包含 summary、candidate metrics CSV/JSON、bucket tables、markout_by_horizon、rejected_signals 和 run_manifest；验证通过 pytest 4 passed、--help、全量 runner、dashboard、git diff --check。样本口径：decision rows 47499，primary non-stale rows 47067，stale excluded 432，future/missing/gap-crossed/startup rows 均为 0。按重要性排序的信号分组如下：第一组 strongest pricing candidates 为 top5_imbalance、top1_imbalance、top1_microprice_edge_ticks、top5_microprice_edge_ticks，最佳 500ms raw future-mid markout abs Spearman 约 0.68-0.71，top-bottom spread 约 63-69 ticks；第二组 depth-size / liquidity candidates 为 top5_depth_imbalance_qty、top5_ask_qty、top5_bid_qty，abs Spearman 约 0.65-0.66；第三组 existing model / recent move diagnostics 为 fair_edge_ticks、recent_mid_move_ticks、audit_bbo_mid_edge_ticks，abs Spearman 约 0.33-0.48；第四组 weaker but usable follow-up signals 为 top5_ofi_proxy、top1_ofi_proxy、top5_liquidity_concentration、audit_feed_latency_ms，abs Spearman 约 0.11-0.23。重复信号已明确降级：reservation_edge_ticks duplicates fair_edge_ticks，audit_mid_edge_ticks duplicates audit_bbo_mid_edge_ticks，book_view_stale_ms 与 latency_signal_ms duplicates audit_feed_latency_ms。弱或不稳定信号包括 spread_ticks、bookticker_mid_edge_ticks、bookticker_depth_age_ms、vol_bps、bookticker_join_age_ms、max_join_age_ms、inventory_score、spread_bps、depth_join_age_ms、top5_join_age_ms。边界清楚：T003 只证明这些信号对 raw future top5 mid markout 的研究相关性，不证明策略 PnL、side-adjusted execution outcome、full L2、exact queue/fill、live readiness，也不授权策略实现或 live。

不通过项：
1. 无

缺陷清单：
1. 无

阻塞项：
- 无

业务回报阻塞项：
- 无

建议总控下一步：
1. 总控可以将该任务视为验收通过。
2. 如存在后续任务，可按前置条件派发下一任务。

提交信息：
- commit：065b7e5
