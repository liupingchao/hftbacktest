# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0719T003

状态：
- 未通过

更新时间：
- 2026-07-19 23:59 CST

验收线程：
- QA验收线程

验收对象：
- 总控 auto-loop / 业务实现线程 0719T003

验收范围：
- producer all-token matching、acceptance raw-proof independent rebuild、persisted producer artifacts、malformed raw identity 和 nominal regression。

验收步骤：
1. 审阅任务、业务报告、prior `0719T002` QA、implementation commit `ba220c52a4e0cfa6f045cabcbc1ab034acd3f2f9` 和 report commit `9561f58`。
2. 独立重放 partial/conflicting target、forged summaries、ambiguous raw response、missing/malformed raw inputs。
3. 从 standalone multi-attempt 和 two-sided manager 实际写盘 artifacts 重建 reconciliation，并与 producer summary 比较。
4. 检查 acceptance helper independence，运行 focused tests、compile/help/diff checks。

## Findings

1. P1：真实 producer artifact 经 redaction 写盘后，valid raw proof 无法与 producer summary exact equal。
   - `hyperliquid_tiny_live_m2_fill_window.py:98-100` 在写 JSON 时递归 redacts `oid/cloid` 字段。
   - Producer 在 redaction 前生成的 `reference_key` 保留实际 oid 和另一种 cloid redaction；写盘后的 raw refs/cancel rows 则是 `oid=<redacted>, cloid=<redacted>`。
   - QA 对 standalone multi-attempt 和 two-sided manager 的实际写盘 `cancel_shutdown_proof.json` 独立重建：producer 与 rebuilt 均为 `status=pass`，但 `exact_equal=false`。
   - Standalone producer keys 为 `attempt_*|oid=61800100*|cloid=<redacted_hex32>`，rebuilt keys 为 `attempt_*|oid=<redacted>|cloid=<redacted>`；manager artifact 同样不相等。
   - 将该 nominal persisted proof 放入完整 acceptance fixture 后，acceptance 返回 mechanism fail。当前 acceptance 因此会误拒绝未来真实 producer evidence。
2. P1：malformed fractional attempt identity 被静默截断并可跨 attempt 证明成功。
   - Producer `safe_int()` 和 acceptance `raw_safe_int()` 都直接调用 `int(value)`。
   - Reference `attempt=1.1` 与 cancel evidence `attempt=1.9` 均被归一化为 `1`；producer reconciliation 返回 pass。
   - QA 同步写入独立重建的两份 summary 后，acceptance 也返回 mechanism pass。
   - 这违反 raw refs/cancel lists malformed 必须 blocked，以及 exact attempt identity 不能别名化的要求。

实际结果：
- Correct oid + unknown cloid：producer fail closed。
- Correct cloid + unknown oid：producer fail closed。
- Oid/cloid 分别指向两个已知 refs：`cancel_result_conflicting_target`。
- 两份 pass summary 保持不变但 raw target unrelated：acceptance blocked。
- 两份 pass summary 保持不变但 raw response ambiguous-only：acceptance blocked。
- Raw lists missing、wrong container type、non-dict rows：acceptance blocked。
- Valid unredacted in-memory raw proof 的 independent rebuild 与 producer output exact equal。
- 原 cross-attempt any-success 保持 fail closed；same-reference success + redundant generic 保持 pass。
- Standalone multi-attempt 和 two-sided manager nominal producer tests 通过，但其实际写盘 proof 无法通过 exact-summary integration gate。
- Acceptance production module 未 import producer module，也未调用 producer reconciliation helper。
- Focused regression：`130 passed in 22.31s`；nominal artifact generation：`2 passed in 2.16s`。
- `py_compile`、CLI `--help`、implementation/current diff checks 通过。
- QA 未运行 full suite：确定性 P1 已足以裁决；业务线程报告 `483 passed`，本轮未独立重跑。
- 未调用 live/private/account/order/cancel/network/remote/service。

验收结论：
- 未通过
- 结论说明：
  - T002 的 partial-token 和 synchronized-summary fail-open 已关闭，但 persisted evidence integration 与 malformed attempt identity 仍不满足 fail-closed contract。

通过项：
1. Producer all-token same-reference matching覆盖用户要求的三类 target 冲突。
2. Acceptance 从 raw proof 独立重建并阻断 unrelated target、ambiguous-only response 和缺失/明显 malformed containers。
3. Acceptance 与 producer helper 解耦。
4. 原 cross-attempt、same-reference、standalone 和 two-sided manager nominal逻辑未发生单元回归。

不通过项：
1. Valid producer-written proof 因 redaction representation mismatch 无法与 producer summary exact equal。
2. Fractional attempts 被截断后可形成错误的跨 attempt identity match。

缺陷清单：
1. P1：persisted raw proof 与 producer summary 使用不一致的 redacted identity representation。
2. P1：attempt parser 接受并截断 fractional numeric values。
3. 缺少直接把 producer `write_json` artifacts 输入 acceptance 的 integration test，以及 fractional/bool attempt malformed tests。

阻塞项：
- 不得创建或启动新的 live task。
- Principal Task 12 仍未关闭。
- Task 10 multi-level 仍未解锁。

建议总控下一步：
1. 新 offline formal task 定义可写盘、非敏感且稳定的 reference identity token，使 raw refs/cancel rows、producer summary 和 independent rebuild 在 redaction 后仍 exact equal。
2. Producer/acceptance 使用严格正整数 attempt parser，拒绝 bool、fractional、NaN/Infinity 和非规范 identity。
3. 增加真实 producer-written standalone/manager artifact acceptance integration 与 malformed attempt matrix，再跑 focused/full offline QA。

提交信息：
- implementation commit：`ba220c52a4e0cfa6f045cabcbc1ab034acd3f2f9`
- report commit：`9561f58`
