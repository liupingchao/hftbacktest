# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0718T020

状态：
- 已通过

更新时间：
- 2026-07-18 23:00 CST

验收线程：
- QA验收线程

验收对象：
- 总控 auto-loop / 业务实现线程 0718T020

验收范围：
- Principal Alignment Task 9：T018/T019 artifact 兼容的 quote lifecycle、censoring、partial fill、fill identity、exposure-weighted aggregate、bounded observe-only feedback、restart checksum、watcher/status artifact 和 deterministic replay。
- 不验收 fill-feedback activation、dynamic spread activation、multi-level、profitability、maker viability 或 promotion。

验收步骤：
1. 审查 commit `f04333d` 的 estimator、watcher integration、专项测试和 workflow task。
2. 复核 attempt/fill identity、rejected/never-resting 排除、short hold/run-end/forced-cancel/coverage 缺失 censoring、partial ratio 和冲突 fail-closed。
3. 复核 pooled exposure-weighted quantity statistic、target 未配置时 neutral、hysteresis/rate-limit/anti-windup/bounds/version 和 checksum/schema restore。
4. 运行 focused、watcher、fill attribution、manager 和仓库 hyperliquid 回归。
5. 复核两次远端 public-only attempt、endpoint boundary、artifact checksum、feedback replay 和 estimator replay。

实际结果：
- lifecycle 以 `attempt_key` 作为订单尝试身份，以 ledger `fill_id` 作为成交身份；重复同身份同 payload 幂等，冲突进入 quarantine 并从统计中排除。
- rejected/never-resting 不计普通 no-fill；short hold、run-end、forced cancel、missing terminal public coverage 和身份冲突均保留为 censored/fail-closed evidence。
- partial fill 使用 `filled_qty/original_qty`；统计按 `original_qty * exposure_seconds` 加权，初期只输出 pooled aggregate。
- target fill ratio 不硬编码；未由 live evidence/config 提供时 candidate 为 `unavailable_neutral`，activation 和 quote behavior 均为 false。
- controller 实现 hysteresis、rate limit、anti-windup、hard bounds、version；state checksum/schema/controller/config 校验失败时回到 neutral。
- watcher 三条收尾路径均输出 feedback lifecycle/aggregate/quarantine/candidate/state/snapshot/manifest，并将 snapshot 写入 status。
- focused feedback `5 passed`，existing estimator `8 passed`，watcher `58 passed`，fill attribution `23 passed`，manager `12 passed`，hyperliquid directory `446 passed`。
- remote attempt 1 使用 `/usr/bin/python3` 因 websocket 依赖缺失在 `1.500913s` fail-closed；无 endpoint side effect。
- remote attempt 2 使用既有 SDK venv，`60.000712s` 收到 `113` L2、`85` trades、`148` evaluations、无 reconnect；`0` quote exposure、`0` lifecycle、feedback neutral；credentials/private/account/order/cancel 全 false。
- 第二次 live artifact remote/local `29/29` 文件逐项 SHA-256 一致；feedback replay 和 estimator replay 均 `snapshot_match=true`。

验收结论：
- 已通过
- 结论说明：
  - T020 observe-only lifecycle/feedback contract、证据完整性、replay determinism 和 no-order boundary 均通过；由于没有真实 resting lifecycle，任何 fill calibration 或 feedback activation 仍然不成立。

通过项：
1. Lifecycle schema、attempt/fill identity、状态转换和 fail-closed censoring。
2. Partial fill、重复身份幂等、冲突隔离和 exposure-weighted aggregate。
3. Neutral target boundary、hysteresis、rate-limit、anti-windup、bounds 和 restart checksum。
4. Watcher/status/artifact integration，且未改变 quote/order behavior。
5. Focused/regression tests、public-only live evidence、terminal checksum 和 exact replay。

不通过项：
1. 无

缺陷清单：
1. 无未修复实现缺陷。

阻塞项：
- 真实 resting/fill lifecycle 仍缺失；该项不阻塞 T020 observe-only 验收，但阻塞 feedback activation、dynamic spread activation、Task 10 multi-level 和任何经济性/promotion 结论。

建议总控下一步：
1. 创建唯一下一任务 `0718T021 / P3-MULTI-LEVEL-PREREQUISITE-GATE`，先验证 Task 10 的真实单层 lifecycle 前置条件和 default-off ladder contract。
2. 不在 Task 10 中绕过真实 lifecycle 前置条件，不与 dynamic spread 或 feedback activation 同时启用 levels。

提交信息：
- commit：`f04333d`
