# QA 验收结果

执行线程：
- 独立 QA 验收线程

任务ID：
- 0804T001

状态：
- 已通过

更新时间：
- 2026-08-04 11:52 CST

验收线程：
- 第二轮独立 QA 验收线程

验收对象：
- 业务线程-python/public-data-infra + cross-exchange-research / 0804T001

验收范围：
- 第一轮 P1/P2 整改、hard stale gate、phase-specific runtime source、
  control-plane reconciliation、R0/R1 重建及原有 alignment 语义。

验收步骤：
1. 检查正式 timeline、segment、campaign、R0、R1 manifest。
2. 独立重算 collection/postprocess/R0/R1 源码归档 SHA、大小和路径。
3. 重算 control-plane、原 collection-only manifest 和历史 events SHA。
4. 比较 embedded final manifest 与原 collection-only manifest。
5. 重算 R0/R1 provenance、输出 SHA、行数及全部冻结标签。
6. 运行 focused tests 和 `git diff --check`。

实际结果：
- Focused tests：`72 passed`；`git diff --check` 通过。
- 正式 timeline 使用默认 hard gate：
  `fast_stale_interval_policy.enabled=false`。
- Binance、Hyperliquid fast、standard 三条轨道 stale row 均为 0；
  degraded interval 为 0，timeline `passes=true`。
- Collection runtime 四个归档文件实际 SHA 与原云端 runtime manifest
  完全一致。
- Postprocess runtime 四个归档文件与执行版本、当前源码完全一致。
- R0 builder archive SHA 为 `ef0bd4f8...`，与执行源码一致。
- R1 builder archive SHA 为 `3915528d...`，与执行源码一致。
- 所有 archive 均为普通文件，不是符号链接；未发现路径越界。
- Control-plane reconciliation SHA 与最终 campaign manifest 的引用一致。
- 原 collection-only manifest SHA 为 `44d975b3...`，历史 events SHA 为
  `b210053e...`，实际文件重算一致。
- 历史文件 mtime/birth time 早于第一轮 QA，未在整改时重写。
- 6 行历史 events 原样保留，错误 mode 仍为
  `collect_and_postprocess`，没有被篡改隐藏。
- Embedded final manifest 与归档 collection-only manifest 完整相等。
- 唯一 collector command 的脚本、profile、7200s、task ID 均正确，
  并且包含一次 `--skip-alignment`。
- Control-plane reconciliation 不引用最终 campaign manifest，不存在
  manifest/reconciliation 自引用环。
- R0/R1 的 10 个 provenance 输入重新计算 SHA 和行数均无差异。
- 新 R1 共 `529,003` 行：零 future join、零跨 segment、零 segment
  外 primary、零 eligible primary 缺失。
- R0、R1、reconciliation、八个 horizon 均 `passes=true`。

验收结论：
- 已通过
- 结论说明：
  - 第一轮 P1/P2 已真实闭合，未发现伪造、自引用、路径越界、SHA
    缺口或门禁放宽导致的虚假通过。

通过项：
1. Hard stale gate 整改闭合。
2. Collection/postprocess/R0/R1 源码 provenance 闭合。
3. 历史控制面误标得到保留、绑定和正确解释。
4. R0/R1 重建及 alignment 语义通过独立重算。
5. 云端仅采集、本地计算边界成立。

不通过项：
1. 无

缺陷清单：
1. 无

阻塞项：
- 无

建议总控下一步：
1. 将 `0804T001` 更新为 `已通过`并收口。
2. 后续研究继续遵守当前边界：本结果不构成 signal、arbitrage、
   exact fill、maker identity 或 PnL 结论。

提交信息：
- commit：无
