# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0608T003

状态：
- 已通过

更新时间：
- 2026-06-08 14:53 CST

验收线程：
- QA验收线程

验收对象：
- 业务线程-research / 0608T003

验收范围：
- 验收 `0608T003` 是否按任务边界完成 read-only directional momentum viability assessment，仅评估 `regime_011_1000_spread_10_20_ticks`，并确认未输出 executable trading instruction、actual order side、quote/order 行为、private/order endpoint、live/default-on/tiny-live 或 promotion claim。

验收步骤：
1. 读取 `.workflow/tasks/0608T003.md`，确认任务状态为 `待验收`、QA 方式为正常验收、范围限定为 read-only directional momentum viability assessment。
2. 读取 `.workflow/reports/0608T003-business.md`，核对 method、input source-lock、T002 prerequisite check、artifact 路径、验证命令、commit id 和边界声明。
3. 解析 `local_live_analysis/canonical_directional_momentum_viability_0608T003/directional_momentum_manifest.json`。
4. 检查 T003 required artifacts 是否存在。
5. 复跑 `python examples/hyperliquid/canonical_directional_momentum_viability.py --help`。
6. 复跑 `python -m py_compile examples/hyperliquid/canonical_directional_momentum_viability.py examples/hyperliquid/test_canonical_directional_momentum_viability.py`。
7. 复跑 `python -m pytest examples/hyperliquid/test_canonical_directional_momentum_viability.py`。
8. 复跑 `python -m json.tool local_live_analysis/event_mode_canonical_pricing_signal_0604T003/multi_sample_manifest.json`。
9. 复跑正式 runner 命令，确认输出仍为 `reject_directional_edge_unstable`；复跑只临时改变 manifest 的 `generated_at` / `git_commit`，QA 已恢复该 transient diff，未把复跑时间戳写回业务 artifact。
10. 检查 `directional_momentum_report.md` 与业务报告边界文本。
11. 运行 `git diff --check`。

实际结果：
- `directional_momentum_manifest.json` 解析通过，`task_id=0608T003`，`schema_version=canonical_directional_momentum_viability_v1`。
- manifest 限定 `assessed_regime_id=regime_011_1000_spread_10_20_ticks`。
- manifest 记录 `row_level_input_policy=multi_sample_manifest.samples[].pricing_signal_rows`。
- canonical guard 结果为 `canonical_sample_count=3`、`diagnostic_rejection_count=0`。
- T002 prerequisite 验证通过：`maker_prerequisite_final_recommendation=reject_not_maker_executable`。
- 任务要求 artifacts 均存在：
  - `directional_momentum_manifest.json`
  - `base_regime_directionality.csv`
  - `feature_directionality_summary.csv`
  - `cost_latency_adjusted_edge.csv`
  - `tail_risk_summary.csv`
  - `directional_candidate_watch_reject.csv`
  - `directional_momentum_report.md`
- Base regime result 为 `row_count=242`、`sample_count=3`、positive/negative future counts `124/118`、direction hit rate `0.51239669`、mean signed future move `-0.92975207` ticks。
- Per-sample signed edge 为 `10.51282051 / -9.04040404 / 2.5` ticks，样本间方向不稳。
- Cost proxy 结果为 gross edge `0.92975207` ticks、fee/slippage/latency proxies `2/2/5` ticks、net edge `-8.07024793` ticks，classification `net_edge_negative_proxy`。
- Tail risk 结果为 wrong-way rate `0.48760331`、p95 wrong-way loss `205.75` ticks、max wrong-way loss `275` ticks，classification `tail_risk_reject`。
- Final recommendation 为 `reject_directional_edge_unstable`，符合 T003 taxonomy。
- Report 明确写明该结果只是 read-only public-data proxy assessment，不是 executable strategy PnL 或 private execution proof。
- Report 明确禁止 executable trading instruction、actual order side、quote price、size、leverage、stop rule、take-profit rule、private/order endpoint、order lifecycle、live/default-on/tiny-live、parameter search、case-library implementation、shadow decision generation、deployment recommendation 和 promotion。
- `--help` 通过。
- `py_compile` 通过。
- Focused pytest 通过：`5 passed`。
- canonical `multi_sample_manifest.json` 解析通过。
- 正式 runner 复跑通过并输出 `final_recommendation=reject_directional_edge_unstable`。
- `git diff --check` 通过。
- QA 未发现本任务访问 private/order endpoint、实现策略/quote/order 行为、启动 live/default-on/tiny-live、运行参数搜索、修改 connector/core/schema、输出交易指令或 promotion claim。

验收结论：
- 已通过
- 结论说明：
  - `0608T003` 已按任务边界完成 read-only directional momentum viability assessment。结论 `reject_directional_edge_unstable` 有对应 public proxy artifacts 支撑；该结论不授权策略实现、private/order endpoint、order lifecycle、live/default-on/tiny-live、parameter search、case-library implementation、shadow decisions 或 promotion。

通过项：
1. Runner 使用 accepted canonical source-lock path，并拒绝 diagnostic-only synthetic formal input。
2. Runner 验证 T002 maker rejection prerequisite。
3. Runner 从 `multi_sample_manifest.json` `samples[].pricing_signal_rows` 解析 row-level inputs。
4. Assessment 限定为 `regime_011_1000_spread_10_20_ticks`。
5. Required artifacts 全部存在并可解析。
6. Final recommendation 使用任务允许 taxonomy，结果为 `reject_directional_edge_unstable`。
7. Focused verification 全部通过。

不通过项：
1. 无

缺陷清单：
1. 无

阻塞项：
- 无

建议总控下一步：
1. 可将 `0608T003` 标记为 `已通过`。
2. `0608T003` 不支持 directional case-library / shadow decision / strategy / live / promotion。
3. 若继续研究 T003 中局部 feature-conditioned pattern，应新建 read-only validity diagnosis task，检查这些 pattern 是否跨样本稳定、decision-time-visible、非共线/非泄漏，并在 cost/tail proxy 后仍成立。

提交信息：
- commit：`e1dc1b9` / `4e62773`
