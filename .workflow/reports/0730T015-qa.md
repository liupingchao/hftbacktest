# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0730T015

状态：
- 未通过

更新时间：
- 2026-07-30 23:59 CST

验收线程：
- QA验收线程

验收对象：
- 业务线程-python/cross-exchange-research + 0730T015

验收范围：
- R1 alignment builder、focused tests、真实八段 alignment 输出、R0
  provenance/mask closure，以及新增采集授权边界。

## Findings

1. [P1] `passes` 没有执行 R1 必需的 raw count/hash 和 exact mask
   closure。`cross_exchange_alignment_acceptance.py:402-408` 只核对 segment
   manifest 自身 SHA，不核对其 `source_files`、`outputs` 的 SHA/row count；
   `:507-516` 只统计 mask type 数量，不核对 mask index SHA、campaign/profile、
   segment、track、起止时间、policy 或 degraded interval identity；
   `:539-546` 仍可据此发布 `passes=true`。独立反例把 source/output SHA 和
   row count 全部写错，并把唯一 segment mask 改成错误 segment/track/time，
   builder 仍 `passes=true`。这违反研究计划 `:262` 的 raw count/hash
   reconciliation 和 `:267` 的 exact mask 要求。
2. [P1] 没有产出计划要求的逐 decision、逐 label 冻结研究表。
   `cross_exchange_alignment_acceptance.py:281-297` 只累计 coverage 和
   effective-horizon quantile；`:518-529` 只发布四个聚合 CSV。计划
   `skhynix_cross_exchange_research_plan.md:225-241` 要求每个 label 持久化
   nominal/effective horizon、source timestamp、decision-time source age、
   update/no-new-information，并同时保留 primary response 和 diagnostic
   wall-clock 两种 label。当前输出无法逐行审计 response join，也不能作为
   R2/R3 共享的 frozen accepted research table。
3. [P1] 低 exact top match 被限制为 asynchronous diagnostics 的措辞是
   合理边界，但当前证据不足以满足“quantified and explainable”，且完全未接入
   pass gate。`cross_exchange_alignment_acceptance.py:317-330` 对每个
   standard trigger 直接比较 as-of fast top-5，没有 near-time threshold；
   `:350-364` 只输出 count 和 price distance，没有计划 `:203` 要求的
   mismatch duration、comparison source age 或 stale/update-order
   decomposition；`:539-546` 不检查任何 reconciliation 结果。真实输出中
   Binance exact match 仅 `1.085%-3.531%`，HL fast/BBO 为
   `5.643%-13.912%`，fast/standard 为 `0%-99.104%`，最大距离分别达到
   `40.580bps`、`49.335bps`；一句“public feeds are asynchronous”不能解释
   跨 segment 的巨大离散。独立反例让三类 reconciliation 全部 `0%` match、
   最大距离超过 `8,600bps`，builder 仍 `passes=true`。
4. [P2] warmup/eligible denominator 没有要求 decision 前已有 Hyperliquid
   BBO state。`cross_exchange_alignment_acceptance.py:244-271` 在 timeline
   as-of 成功后立即增加 eligible，随后才把缺失 BBO 记为 freshness missing。
   真实数据有 `55` 个此类 decision：segment 2/5/7 分别 `41/12/2`，但报告
   warmup excluded 只有 `2`。这与计划 `:166-167` 的 decision view 必须
   as-of join 最新 HL BBO 不闭合。
5. [P2] focused tests 未覆盖核心 acceptance 行为。
   `test_cross_exchange_alignment_acceptance.py:22-104` 只覆盖 quantile、
   freshness boundary、pre-publication clean failure 和 nonempty output；
   没有覆盖 decision dedupe、same-segment as-of/response join、warmup
   denominator、horizon tolerance、95% per-segment gate、exact mask、
   provenance closure 或 reconciliation pass gate。上述 P1/P2 因而没有
   regression protection。

- P0: none
- P1: 3
- P2: 2
- P3: none

验收步骤：
1. 静态审查 builder、tests、T015 task/business report 和研究计划 R1 gate。
2. 运行 focused tests、py_compile、CLI help 和 diff check。
3. 独立复算八段 decision dedupe、local-time as-of/response join、warmup、
   horizon coverage、freshness closure、output SHA/row count。
4. 复核真实 R0 的 24 个 output gzip、56 个 source SHA、8 个 timeline 和
   exact mask interval。
5. 重放 atomic publish failure、错误 provenance/mask 和极端 reconciliation
   counterexample。
6. 核对本地-only 和 R6 新增采集授权边界。

实际结果：
- Focused tests：`8 passed`。
- `py_compile`、CLI `--help`、`git diff --check`：pass。
- Atomic clean：
  - build-stage failure 保留旧产物；
  - 独立注入 final publish rename failure 后，旧产物逐字节恢复，tmp/backup
    无残留。
- 真实八段独立复算：
  - decision dedupe `2,366,631`；
  - 当前 builder 口径 eligible `2,366,629`，warmup `2`；
  - timestamp regression/future response join `0/0`；
  - 八个 horizon 的 covered/outside/missing/future count 全部闭合；
  - 逐段 `>=95%` 的 horizon 只有 `1000ms` 和 `2000ms`；
  - 最低逐段 coverage 分别为 `95.561449%` 和 `95.256891%`。
- Freshness count closure 通过；aggregate primary：
  - HL BBO `96.461211%`
  - fast L2 `90.008404%`
  - standard L2 `55.817029%`
- 四个 alignment CSV 的 row count/SHA 与 manifest 一致：
  `8/64/24/24`。
- 独立 temporary rebuild 的四个 CSV 和 acceptance markdown 与真实产物
  SHA 完全一致。
- 当前真实 R0 数据本身完整：
  - `24/24` output gzip SHA/row count
  - `56/56` source SHA
  - `8/8` timeline row count
  - mask index SHA/row count、8 个 segment epoch、2 个 degraded interval
    identity 全部 exact
- Builder 无 network/AWS/SSH/collector 路径，本轮也未执行新增采集。
- R6 仍只能建议新增采集；启动必须有独立任务、用户明确授权和用户确认的
  活跃交易时段。

验收结论：
- 未通过
- 结论说明：
  - 真实产物可复算不等于 builder fail closed。三个 P1 允许缺失必需研究表、
    错误 provenance/mask 或无法解释的 top reconciliation 仍发布
    `passes=true`，R1 暂不能解锁 R2/R3。

通过项：
1. Decision BBO-state dedupe 和 same-segment local-time join 的真实复算。
2. Horizon tolerance、逐段 95% gate 和 freshness tier 统计。
3. Atomic clean preservation。
4. 当前真实 output SHA/row count 和 R0 source/mask 完整性。
5. 本地-only 与新增采集授权边界。

不通过项：
1. Builder provenance/count/hash 和 exact mask fail-closed gate。
2. 逐 decision/label frozen table 与 diagnostic wall-clock labels。
3. Top reconciliation quantified/explainable gate。
4. Complete-feature warmup denominator。
5. 核心 acceptance counterexample tests。

缺陷清单：
1. P1 x3，P2 x2；详见 Findings。

阻塞项：
- 无；需要业务线程修复后重新 QA。

建议总控下一步：
1. 将 T015 标记为`未通过`，不得据此解锁 R2/R3 主研究。
2. 创建 T015 repair：补齐 provenance/mask/top gate、逐 label artifact、
   complete-feature warmup 和对应反例。
3. 保持 R6 新增采集授权边界不变。

提交信息：
- commit：无
