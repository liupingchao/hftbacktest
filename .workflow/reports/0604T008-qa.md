# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0604T008

状态：
- 已通过

更新时间：
- 2026-06-04 10:44 CST

验收线程：
- QA验收线程

验收对象：
- 业务线程-research + 0604T008

验收范围：
- `0604T008` 规定的 T006 signal ranking report bucket consistency 窄修复、focused regression test、刷新后的 T006 task-scoped report/manifest artifact，以及 report/CSV bucket 一致性复验。

验收步骤：
1. 阅读 `.workflow/tasks/0604T008.md` 与 `.workflow/reports/0604T008-business.md`，核对任务边界、验收标准、业务结论与提交信息。
2. 核对 commit `c78cec3` 的改动范围，确认只包含 T008 task/report、T006 ranking runner/test 和刷新后的 T006 report/manifest artifact。
3. 复跑最小验证命令：`--help`、`py_compile`、focused pytest。
4. 对 `local_live_analysis/event_mode_canonical_pricing_signal_0604T003/` 运行 ranking runner 到 `/tmp/qa_0604T008_signal_quality/`。
5. 解析 `/tmp/qa_0604T008_signal_quality/signal_quality_ranking_manifest.json`。
6. 对 `/tmp/qa_0604T008_signal_quality/` 和正式 `local_live_analysis/canonical_signal_quality_ranking_0604T006/` 同时运行 scripted report/CSV consistency check。
7. 运行 `git diff --check`，确认无格式问题。

实际结果：
- `git show --stat --oneline c78cec3` 确认提交为 `0604T008 repair signal ranking report consistency`，包含 `.workflow/tasks/0604T008.md`、`.workflow/reports/0604T008-business.md`、`canonical_signal_quality_ranking.py`、focused tests、T006 report/manifest artifacts。
- `python examples/hyperliquid/canonical_signal_quality_ranking.py --help` 通过。
- `python -m py_compile examples/hyperliquid/canonical_signal_quality_ranking.py examples/hyperliquid/test_canonical_signal_quality_ranking.py` 通过。
- `python -m pytest examples/hyperliquid/test_canonical_signal_quality_ranking.py` 通过，结果为 `7 passed`。
- QA 复跑 runner 到 `/tmp/qa_0604T008_signal_quality/`，结果 `canonical_sample_count=3`、`ranked_feature_count=4`。
- `python -m json.tool /tmp/qa_0604T008_signal_quality/signal_quality_ranking_manifest.json` 通过。
- Scripted consistency check 通过：临时复现 artifact 为 `checked_features 4 missing []`，正式 artifact 为 `checked_features 4 missing []`。
- 临时复现 report 和正式 report 均写明：`binance_mid_move_ticks_from_prev` is `keep_for_read_only_research`；`binance_top5_imbalance`、`binance_top5_bid_qty`、`binance_microprice_minus_mid_ticks` 均为 `watch_regime_dependent`。
- `git diff --check` 通过。
- QA 未发现 ranking scoring、allowlist、source-lock guard、canonical loader、strategy/live/promotion 等越界修改。

验收结论：
- 已通过
- 结论说明：
  - `0604T008` 已修复 `0604T006` QA 指出的 report bucket 文案不一致问题；report interpretation 现在由实际 `ranking_rows` 生成，复现 artifact 与正式 artifact 均和 `signal_quality_ranking.csv` 的 `final_bucket` 一致。

通过项：
1. T006 report 的 `Controller Interpretation Check` 不再使用会分叉的静态 keep/watch 文案。
2. Focused regression test 已覆盖 report interpretation 与实际 bucket 一致性，并防止 `binance_top5_imbalance` watch bucket 被写成 kept。
3. 正式与临时复现 artifact 的四个 feature 均通过 report/CSV consistency check。
4. Ranking CSV 语义未改变：mid move 为 keep，其余三个 feature 为 watch。
5. 边界保持正确：无 scoring change、allowlist change、source-lock guard change、canonical loader change、new collection、regime selection、strategy/private/order/live/default-on/tiny-live/parameter search/promotion。

不通过项：
1. 无

缺陷清单：
1. 无

阻塞项：
- 无

建议总控下一步：
1. 将 `0604T008` 记为 `已通过`。
2. 可据此认为 `0604T006` 的已知 QA 缺陷已由后续窄修复任务闭环；如需重新关闭 T006 本体，应由总控决定是否单独更新 T006 状态或以 T008 作为修复通过记录。

提交信息：
- commit：c78cec3（实现与 artifact）；768465f（业务报告回填）
