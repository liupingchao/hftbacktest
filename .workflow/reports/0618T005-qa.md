# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0618T005

状态：
- 已通过

更新时间：
- 2026-06-18 11:40 CST

验收线程：
- QA验收线程

验收对象：
- 业务线程-research 0618T005

验收范围：
- M0 evidence-chain baseline read-only verification.
- 验收 `0617T005` replay、`0617T006` optimistic proxy、`0618T004` accepted canary artifacts、M0 final gate rerun、M0 artifact、边界声明、无下单/无凭证/无私有接口/无 live bot。
- 本轮不验收 M1 live/canary task，不验收 real PnL、stable PnL、maker viability、promotion、default-on、scale-up。

验收步骤：
1. 阅读 `.workflow/tasks/0618T005.md` 和 `.workflow/reports/0618T005-business.md`。
2. 复核 `local_live_analysis/m0_evidence_chain_baseline_0618T005/signal_quote_replay/replay_manifest.json` 和 `calibration_summary.csv`。
3. 复核 `local_live_analysis/m0_evidence_chain_baseline_0618T005/optimistic_pnl_proxy/optimistic_pnl_proxy_manifest.json` 和 `diagnostic_interpretation.csv`。
4. 复核 `local_live_analysis/hyperliquid_tiny_live_real_order_canary_0618T004/pulled_back_awsserver1/executor_manifest.json` 和 `cancel_shutdown_proof.json`。
5. 复核 `local_live_analysis/m0_evidence_chain_baseline_0618T005/final_go_no_go_gate/final_go_no_go_manifest.json` 和 `remote_state_gate_matrix.csv`。
6. 运行 focused tests、JSON manifest 校验、artifact 非空检查、credential / secret pattern scan 和 `git diff --check`。

实际结果：
- `python -m pytest examples/hyperliquid/test_hyperliquid_tiny_live_signal_quote_replay.py examples/hyperliquid/test_hyperliquid_tiny_live_optimistic_pnl_proxy.py examples/hyperliquid/test_hyperliquid_tiny_live_final_go_no_go_gate.py -q` 通过，`6 passed`。
- M0 replay manifest 可解析，记录 `pricing_rows_input_count=8`、`pricing_rows_replayed=161455`、`pricing_decision_rows_evaluated=26948`。
- M0 replay calibration preserved primary candidate `75` ticks / persistence `2`: `654` intents, `313` buy / `341` sell, `2.4269%` intent rate, `8/8` samples with any intent。
- M0 optimistic proxy manifest 可解析，记录 official sample set 为 `canonical_7`，仍为 `unconstrained_all_intents` optimistic upper-bound assumption。
- M0 optimistic proxy diagnostic preserved `canonical_7` at `75` ticks / persistence `2` / `1000ms`: `295.985 USDC` optimistic proxy, mean `40.932789` ticks per intent, `7/7` samples positive。
- Accepted `0618T004` canary artifact still records `order_submission_attempted=true`、`order_status_types=resting`、`real_order_endpoint_called=true`、`real_cancel_endpoint_called=true`、`shutdown_proof_status=pass`、`credentials_written=false`、`secret_values_written=false`、`raw_signatures_written=false`。
- `cancel_shutdown_proof.json` records `final_open_orders=[]` and `proof_status=pass`。
- M0 final gate rerun is read-only and records boundary flags all false for credentials/private/order/account/live behavior.
- M0 final gate rerun intentionally fails closed with `allow_create_0617T008=false`, `final_recommendation=tiny_live_needs_missing_precondition`, blocker `remote_execution_checkout_not_synced_or_invalid`。
- `remote_state_gate_matrix.csv` explains the drift: expected current local gate commit `d37438e`, actual saved remote state `52b5b9541`; branch/path/python/dirty-count gates pass.
- Artifact non-empty check passed.
- Credential / secret scan excluding SHA256 checksum rows found no `HL_PRIVATE_KEY=`, raw signature, nonce assignment, raw private-key assignment, or raw address-shaped values.
- `git diff --check` passed.

验收结论：
- 已通过
- 结论说明：
  - `0618T005` 按 M0 边界完成了只读证据链基线复核；M0 完成不等于 live 授权，且本轮正确发现 M1 前必须刷新/同步 remote checkout 后再跑 gate。

通过项：
1. M0 使用一个业务任务完成 replay / proxy / canary artifact / final gate 的只读基线复核。
2. `0617T005` primary threshold candidate 仍可复核。
3. `0617T006` `canonical_7` optimistic proxy 仍可复核，并保留非真实 PnL 边界。
4. `0618T004` 历史 canary 的 order/cancel/shutdown/redaction 证据仍完整。
5. M0 过程中没有下单、没有读凭证、没有调用 private/account/order endpoint、没有启动 live bot。
6. M0 捕捉到 remote checkout drift，并明确将其作为 M1 前置处理项。

不通过项：
1. 无

缺陷清单：
1. 无

阻塞项：
- 无 M0 验收阻塞。
- M1 前置阻塞：必须刷新/同步 `/home/admin/hftbacktest-cross-exchange` remote checkout 状态，并重跑 final gate；当前 M0 gate 结果为 `allow_create_0617T008=false`。

建议总控下一步：
1. 将 M0 标记为完成的基线复核。
2. 在创建任何 M1 tiny-live/canary 任务前，先派发 remote checkout sync / final gate refresh 任务，直到 gate 重新返回 go。
3. 继续保持 `schedule_cancel` 不可依赖边界，M1 仍应使用 tracked cancel + final open-orders proof。

提交信息：
- commit：无
