# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0513T007

状态：
- 未通过

更新时间：
- 2026-05-13 23:09 Asia/Shanghai

验收线程：
- QA验收线程

验收对象：
- 业务线程-python + 0513T007

验收方式：
- 正常验收

验收范围：
- 实现“不改标准 npz 主事件，新增 provenance/top5 sidecar，并把它 join 到 decision audit”的数据可追溯层。必须建立 `raw_seq -> npz rows -> reconstructed top5 book -> decision rows` 的显式映射和 join-age 验收，解决 T006 暴露的 Binance provenance 缺失问题。T007 的研究边界是 top5-only，不做 full L2 provenance，也不做精确 queue-position 证明。

验收步骤：
1. 读取 `.workflow/tasks/0513T007.md`。
2. 读取 `.workflow/reports/0513T007-business.md`。
3. 检查任务状态、业务回报、verify 证据、done 结论和阻塞项。
4. 写入 `.workflow/reports/0513T007-qa.md` 并刷新看板。

实际结果：
- 任务文件存在：.workflow/tasks/0513T007.md
- 业务回报状态：待验收
- 业务回报存在：.workflow/reports/0513T007-business.md
- T007 保留标准 hftbacktest `data` npz 主事件 schema 不变，未修改 core `Event`、py `event_dtype`、connector、本地 `hbt.depth(0)` API、strategy 行为、`align_live_run.py`、canonical `audit_schema.py` 或 live audit CSV schema。

验收结论：
- 未通过
- 结论说明：
  - T007 QA 未通过：T007 的 standalone sidecar/mapping/join 结构方向正确，且没有修改标准 npz 主事件 schema；但在 5-13-day-control-30min full-run 验收中发现 Binance local book snapshot bootstrap bug，导致 T007 不能作为 top5 microprice / OFI / queue proxy 的可靠基础。具体证据：snapshot raw_seq=6 的 lastUpdateId=10537138804218，按 Binance local book 规则第一条可用 depth update 必须满足 U <= lastUpdateId+1 <= u，即覆盖 10537138804219。raw 中 snapshot 前已有 buffered depth raw_seq=5：U=10537138802913, u=10537138805036, pu=10537138802699，正好覆盖 10537138804219；snapshot 后 raw_seq=7：U=10537138805242, u=10537138807174, pu=10537138805036，正好接上 raw_seq=5 的 u。但当前 T007 逻辑没有在 snapshot 到达后回放/使用 snapshot 前已缓冲的 depth update，而是把 raw_seq=7 当作 first valid update，导致 first_valid_update_aligned=false，并把 sync_gap 保持为 true，最终 5-13-day-control-30min 的 joined decisions 全部 gap_crossed=47499/47499。该问题不是采集时间不够长，而是 sidecar reconstructed book bootstrap 逻辑缺陷。下一步应新建修复任务：buffer snapshot 前 depthUpdate，snapshot 后选择并应用满足 U <= lastUpdateId+1 <= u 的 buffered/update，再按 pu == previous_u 连续应用；用 5-13-day-control-30min 重新生成 sidecar/join 验收。

通过项：
1. 业务回报已进入待验收状态
2. 任务声明需要 QA 验收
3. 业务回报包含 verify 证据

不通过项：
1. T007 QA 未通过：T007 的 standalone sidecar/mapping/join 结构方向正确，且没有修改标准 npz 主事件 schema；但在 5-13-day-control-30min full-run 验收中发现 Binance local book snapshot bootstrap bug，导致 T007 不能作为 top5 microprice / OFI / queue proxy 的可靠基础。具体证据：snapshot raw_seq=6 的 lastUpdateId=10537138804218，按 Binance local book 规则第一条可用 depth update 必须满足 U <= lastUpdateId+1 <= u，即覆盖 10537138804219。raw 中 snapshot 前已有 buffered depth raw_seq=5：U=10537138802913, u=10537138805036, pu=10537138802699，正好覆盖 10537138804219；snapshot 后 raw_seq=7：U=10537138805242, u=10537138807174, pu=10537138805036，正好接上 raw_seq=5 的 u。但当前 T007 逻辑没有在 snapshot 到达后回放/使用 snapshot 前已缓冲的 depth update，而是把 raw_seq=7 当作 first valid update，导致 first_valid_update_aligned=false，并把 sync_gap 保持为 true，最终 5-13-day-control-30min 的 joined decisions 全部 gap_crossed=47499/47499。该问题不是采集时间不够长，而是 sidecar reconstructed book bootstrap 逻辑缺陷。下一步应新建修复任务：buffer snapshot 前 depthUpdate，snapshot 后选择并应用满足 U <= lastUpdateId+1 <= u 的 buffered/update，再按 pu == previous_u 连续应用；用 5-13-day-control-30min 重新生成 sidecar/join 验收。

缺陷清单：
1. T007 QA 未通过：T007 的 standalone sidecar/mapping/join 结构方向正确，且没有修改标准 npz 主事件 schema；但在 5-13-day-control-30min full-run 验收中发现 Binance local book snapshot bootstrap bug，导致 T007 不能作为 top5 microprice / OFI / queue proxy 的可靠基础。具体证据：snapshot raw_seq=6 的 lastUpdateId=10537138804218，按 Binance local book 规则第一条可用 depth update 必须满足 U <= lastUpdateId+1 <= u，即覆盖 10537138804219。raw 中 snapshot 前已有 buffered depth raw_seq=5：U=10537138802913, u=10537138805036, pu=10537138802699，正好覆盖 10537138804219；snapshot 后 raw_seq=7：U=10537138805242, u=10537138807174, pu=10537138805036，正好接上 raw_seq=5 的 u。但当前 T007 逻辑没有在 snapshot 到达后回放/使用 snapshot 前已缓冲的 depth update，而是把 raw_seq=7 当作 first valid update，导致 first_valid_update_aligned=false，并把 sync_gap 保持为 true，最终 5-13-day-control-30min 的 joined decisions 全部 gap_crossed=47499/47499。该问题不是采集时间不够长，而是 sidecar reconstructed book bootstrap 逻辑缺陷。下一步应新建修复任务：buffer snapshot 前 depthUpdate，snapshot 后选择并应用满足 U <= lastUpdateId+1 <= u 的 buffered/update，再按 pu == previous_u 连续应用；用 5-13-day-control-30min 重新生成 sidecar/join 验收。

阻塞项：
- 无

业务回报阻塞项：
- 无。

建议总控下一步：
1. 总控应将任务退回执行线程补证或修正。
2. 补齐后重新生成业务回报并再次 QA。

提交信息：
- commit：cb4aabe
