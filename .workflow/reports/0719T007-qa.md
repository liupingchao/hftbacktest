# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0719T007

状态：
- 未通过

更新时间：
- 2026-07-19 23:59 CST

验收线程：
- QA验收线程

验收对象：
- implementation commit `5239af62d67381c5b3584c0873b58e6a0b246cfe`
- business report commit `3fccb300f74acfd3e514b30c47630e63bf980e60`

验收范围：
- T006 五项 finding，以及 fill token conflict、额外 unbound fill、canonical run-id、真实 manager-watcher integration 和 regressions。

验收步骤：
1. 审阅 task、business report、T006 QA、implementation diff 和 producer/acceptance 实现。
2. 运行现有 focused tests，并构造 argv、fill identity、raw pullback 和 artifact-path 对抗反例。
3. 删除预制 inner artifact，验证真实 manager watcher 可重新生成 canonical `window_01` producer tree。

## Findings

1. P1：完整 fill terminal 仍未从原始 pullback 独立重建。
   - Acceptance 只消费汇总和 `live_fill_ledger.csv`，未解析 `user_fills_pullback_audit.json` 中的原始 pullback rows。
   - 确定性反例：原始 pullback 数量为 `0`，手工构造两侧 full-fill ledger/attribution/role 行后，acceptance 仍返回 pass。
   - 因此伪造的 fill CSV 仍可作为 terminal proof。
2. P1：fill 的 oid/cloid 未执行 all-token 一致性。
   - Producer 在 oid 匹配后直接归因，不检查同时存在的 cloid 是否指向同一 attempt。
   - Acceptance 同样以 oid 匹配或 cloid 匹配接受。
   - 确定性反例：fill 使用正确 oid 和无关 cloid，producer 仍生成 `matched_tracked_oid`，full-fill acceptance 通过。
3. P1：canonical argv 仍可被 argparse 缩写和未绑定路径绕过。
   - Watcher 使用默认 `ArgumentParser`，长参数缩写默认开启。
   - Acceptance 只检测完全相同的重复 flag，没有 canonical allowlist。
   - `--max-real-order-submissions 2 --max-real-order-sub 3` 被 argparse 解析为 `3`，但 acceptance 返回 pass。
   - `--output-dir` 仅要求非空；伪造 output 路径或使用 `--out` 缩写仍可通过。
   - Python/watcher executable 路径未绑定 runtime provenance，stale producer artifact 仍可能被接受。

实际结果：
- Acceptance 测试：`89 passed in 1.86s`。
- Manager watcher focused：`5 passed, 58 deselected`。
- Orchestrator focused：`3 passed, 17 deselected`。
- Fresh manager-watcher integration：2 order、2 cancel、canonical inner tree 重新生成且 acceptance 通过。
- Near-cap 在首个 order call 前阻断；raw response 空/重复/错绑和 extra unbound fill 的现有反例通过。
- 上述三个 P1 反例均返回 acceptance pass。
- QA 未运行 full `examples/hyperliquid`：确定性 P1 已足以否决；业务报告记录的 `631 passed` 不改变结论。
- `git diff --check` 通过；工作区状态与验收前一致。
- 未执行 live/private/account/order/cancel/network/remote/service。

验收结论：
- 未通过
- 结论说明：
  - Raw response 和 near-cap 修复有效，但 fill terminal 独立证据及 canonical command/producer-path 闭环仍可绕过。

通过项：
1. Raw order response 的 side/attempt/intent/ref 绑定。
2. Partial fill、failed/unrelated terminal proof 和 extra unbound fill 阻断。
3. Exact near-cap 首单前阻断。
4. 真实 manager watcher 可生成 canonical producer artifact。

不通过项：
1. Full-fill 原始 pullback独立重建。
2. Fill oid/cloid all-token 一致性。
3. Argparse 缩写、可执行文件和 output-dir canonical binding。

缺陷清单：
1. P1 forged fill terminal acceptance。
2. P1 conflicting fill reference acceptance。
3. P1 abbreviated/unbound runner argv acceptance。

阻塞项：
- 新 tiny-live、Principal Task 12 closure 和后续 multi-level 解锁继续阻塞。

建议总控下一步：
1. Watcher parser 设置 `allow_abbrev=False`；acceptance 使用完整 flag allowlist，并精确绑定 watcher script、runtime provenance 和 canonical output-dir。
2. Acceptance 从原始 fill pullback 独立重建 fill identity/quantity/role，并要求与 ledger/attribution/role 完全一致。
3. Producer 和 acceptance 对同时存在的 oid/cloid 执行 all-token same-reference 校验。
4. 增加上述三个确定性反例后重新 QA。

提交信息：
- QA commit：无
