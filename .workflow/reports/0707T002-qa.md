# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0707T002

状态：
- 已通过

更新时间：
- 2026-07-07 12:06 CST

验收线程：
- QA验收线程

验收对象：
- 业务线程-local 0707T002 / `T010-POST-OPEN-ORDERS-PUBLIC-STATE-RESYNC-REPAIR`

验收范围：
- 验收 Task B 是否修复 `post_open_orders_public_state_timeout` 的 fixed-short-wait brittleness，同时保持 stale/no-proof cases fail-closed，并确认未改 edge source binding、anti-drift / touch-stability thresholds、quote envelope、size 或 live authorization。

验收步骤：
1. 复核 `.workflow/tasks/0707T002.md` 和 `.workflow/reports/0707T002-business.md`。
2. 检查 `observe_post_open_orders_l2_state` 与新增 `post_open_orders_public_state_timeout_seconds`。
3. 检查 deterministic tests 覆盖 pass、fail-closed、cadence-aware timeout scaling。
4. 复核本地 artifact `local_live_analysis/cross_exchange_t010_post_open_orders_resync_0707T002/`。
5. 复核 pytest、py_compile、CLI help、artifact parse、`git diff --check` 结果。

实际结果：
- Default post-open-orders L2 wait is now cadence-aware and capped:
  - base timeout `0.2s`
  - max timeout `6.0s`
  - recent L2 cadence can scale the effective timeout up to the bounded max
- Resync pass condition remains strict:
  - L2 local receive timestamp must be greater than `open_orders_end_ns`
- Focused tests passed:
  - `test_post_open_orders_resync_passes_when_l2_arrives_after_open_orders`
  - `test_post_open_orders_resync_blocks_without_after_open_orders_l2`
  - `test_post_open_orders_resync_timeout_scales_with_recent_l2_cadence`
- Full focused pytest passed: `41 passed`.
- `python -m py_compile examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py` passed.
- `python examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py --help` passed.
- Artifact manifest exists at `local_live_analysis/cross_exchange_t010_post_open_orders_resync_0707T002/post_open_orders_resync_manifest.json`.
- Artifact positive case:
  - `positive_status=pass`
  - `positive_state_observed_after_open_orders_end=true`
  - `post_open_orders_public_state_pass_count=1`
- Artifact negative case:
  - `negative_status=block`
  - `negative_reason=public_source_exhausted_before_post_open_orders_l2`
  - `negative_state_observed_after_open_orders_end=false`
  - `post_open_orders_public_state_block_count=1`
- Artifact endpoint boundary:
  - `real_order_endpoint_called=false`
  - `real_cancel_endpoint_called=false`
  - `live_submit_authorized=false`
- `git diff --check` passed.
- No edge source binding, anti-drift threshold, touch-stability threshold, quote-envelope, size, or live authorization change was made.

验收结论：
- 已通过
- 结论说明：
  - Task B passes: the post-open-orders public-state resync guard can pass with valid post-open-orders L2 proof, remains fail-closed without such proof, and no live-submit or threshold/quote/size change was introduced.

通过项：
1. Formal task file exists and stayed within Task B scope.
2. The brittle fixed `0.2s` default was replaced by bounded cadence-aware wait logic.
3. Fresh post-open-orders L2 proof is still mandatory.
4. Focused tests and local artifacts cover positive and negative cases.
5. No live-submit, order/cancel endpoint, threshold, edge-source, quote-envelope, or size change was introduced by this task.

不通过项：
1. 无

缺陷清单：
1. 无

阻塞项：
- Full `0625T010` remains blocked.
- Trigger frequency and anti-drift / touch-stability distribution still need Task C diagnosis before any threshold decision.
- This QA pass does not authorize live-submit or a controlled live evidence rerun.

建议总控下一步：
1. Create and execute Task C: `0707T003 / T010-ANTI-DRIFT-TOUCH-STABILITY-LIVE-DISTRIBUTION-DIAGNOSIS`.
2. Do not create a threshold-change or controlled live evidence task until Task C receives QA acceptance.

提交信息：
- commit：54fac02 / Repair T010 post-open-orders resync
