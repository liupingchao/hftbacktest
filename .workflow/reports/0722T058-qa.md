# QA 验收结果

## Findings（按严重性）

- P0：无。
- P1：existing legacy source symlink 仍未 fail-closed。
  `_resolve_recorded_artifact_path()` 对 known legacy absolute path 只做
  lexical `relative_to()`，随后直接验证 current-checkout candidate。
  它没有解析或验证 recorded legacy source path 本身。因此，当旧 checkout
  中真实存在的 recorded path 是指向 repo 外部的 symlink，而当前 checkout
  中同名 relative target 是普通合法文件时，resolver 会接受并返回当前文件，
  没有拒绝 hostile legacy symlink。
- P2：T058 的 symlink regression 没有覆盖上述分支。
  `test_recorded_amdserver_path_rejects_symlink_escape` 创建的是当前
  `PROJECT_ROOT` 下的 symlink，并没有创建已存在的 legacy-root symlink；
  因此测试只能证明 relocation target containment，不能证明 legacy source
  path containment。
- P2：第二次 full Hyperliquid regression 出现一个未被 T058 修改的
  watcher shutdown-wait 边界时序失败：
  `0.260058s > 0.260000s`。该测试单独重跑通过，但正式的连续两次
  full-green 条件未达到。

执行线程：
- QA验收线程

任务ID：
- 0722T058

状态：
- 未通过

更新时间：
- 2026-07-22 15:00 Asia/Shanghai

验收线程：
- QA验收线程

验收对象：
- 业务执行线程 `0722T058`
- implementation commit
  `16d9db34ab977efcc36db5a2676865413dce1dc6`
- workflow/business commit
  `ea09b36d651bd9872a036875f50f925cd8106c9e`

验收范围：
- 严格 offline-only 独立 hostile review。
- 覆盖 existing legacy traversal、existing legacy symlink、relative
  traversal、relative symlink、current-root symlink、unknown external
  absolute、合法 current-root relative/absolute 和合法 legacy mapping。
- 覆盖 true missing skip 与 malformed-existing fail。
- 运行 focused、两次 full Hyperliquid、`py_compile` 和 diff checks。
- 不修改 implementation/test 文件。

验收步骤：
1. 阅读 T056/T057 failed QA、T058 task/business、workflow-kit 和 controller
   tracking，核对提交、范围和验收标准。
2. 审查 resolver 全部分支，并确认 implementation/workflow commit 文件
   范围。
3. 在临时 current/legacy/outside roots 中运行独立 hostile matrix：
   - existing legacy traversal；
   - existing legacy symlink 指向外部，同时 current checkout 同名目标合法；
   - relative traversal/symlink；
   - current-root absolute symlink；
   - unknown external absolute existing/missing；
   - 合法 current-root relative/absolute 和合法 legacy mapping。
4. 独立验证 true-missing design/source/sample 与 malformed JSON/contract。
5. 运行 focused portability、historical join 和 exact watcher interval。
6. 连续两次运行 full `examples/hyperliquid` regression，并单独重跑第二次
   full 的唯一失败。
7. 运行四文件 `py_compile`、工作树和 implementation/workflow
   `git diff --check`。

实际结果：
- 合法 current-root relative、current-root absolute 和合法 legacy mapping
  均返回预期 current-checkout artifact。
- Existing legacy traversal、relative traversal、relative symlink、
  current-root absolute symlink、unknown external absolute existing/missing
  均 fail-closed。
- Existing legacy source symlink hostile case被错误接受：
  legacy `data/legacy-link.json` 指向 repo 外部，current checkout 中
  `data/legacy-link.json` 是普通文件；resolver 返回 current 文件而没有
  抛出 `RowLevelGeneratorError`。
- True missing design manifest、source manifest 和 sample CSV 均返回
  unavailable；malformed existing JSON 和 canonical contract 均传播异常，
  不会被 absent-package skip 隐藏。
- Focused：
  `19 passed, 2 skipped in 0.26s`。
- Skip reason 精确为：
  - `0609T002 historical pricing-signal source package is absent`
  - `0602T001 synchronized public sample package is absent`
- Full Hyperliquid 第一次：
  `1257 passed, 2 skipped in 56.43s`。
- Full Hyperliquid 第二次：
  `1 failed, 1256 passed, 2 skipped in 56.13s`。
- 第二次 full 的唯一失败是
  `test_task7_manager_cycle_cancels_by_deadline_when_public_source_blocks`；
  shutdown wait 仅比允许边界高约 `58` 微秒。单独重跑：
  `1 passed in 3.75s`。
- 四个指定文件 `py_compile` 通过。
- `git diff --check`、implementation diff check 和 workflow diff check
  通过。
- Implementation commit 只修改 resolver、对应测试和 workflow/tracking；
  workflow/business commit 只修改 workflow/tracking。
- 工作树中的既存 `.DS_Store` 未被修改或纳入本任务。
- 无环境阻塞，未执行 network、remote、private/account、order/cancel、
  live 或 service 操作。

验收结论：
- 未通过。
- 结论说明：
  - Destination containment、relative/current-root 路径和
    missing-vs-malformed 均已修复，但明确要求的 existing legacy source
    symlink 仍被接受；同时未取得连续两次 full-green 结果。

通过项：
1. Existing legacy traversal、relative traversal/symlink、current-root
   symlink 和 unknown external absolute 均 fail-closed。
2. 合法 current-root relative/absolute 和合法 legacy mapping 均通过。
3. True missing 与 malformed existing 已严格分离，两个 skip reason 精确。
4. Focused、第一次 full、`py_compile` 和所有 diff checks 通过。
5. 未改变 strategy、alpha、quote、risk、production timing、order
   lifecycle 或 live 行为。

不通过项：
1. Existing legacy source symlink 没有在 lexical relocation 前被验证和
   拒绝。
2. 新增测试没有真正构造 legacy-root 中已存在的 symlink。
3. 第二次 full Hyperliquid regression 未全绿。

缺陷清单：
1. P1：known legacy source path 在映射前必须验证 resolved source 仍属于
   resolved legacy root；任何 existing parent/final symlink escape 必须
   fail-closed。
2. P2：新增真实临时 legacy root regression，同时创建 legacy symlink
   escape 和安全的 current same-relative target，证明不能靠 destination
   containment 掩盖 hostile source。
3. P2：稳定 watcher shutdown-wait 边界测试，避免数十微秒调度抖动阻塞
   full-green；不得放宽 production deadline 语义。

阻塞项：
- 无外部或环境阻塞；需要新的 offline-only repair task。

建议总控下一步：
1. 派发最小 legacy-source containment repair，只补 source resolution
   invariant 和对应 hostile regression。
2. 保持当前 destination containment、missing-only skip 和 test-only
   clock 修复不变。
3. 修复 watcher shutdown-wait 测试的时钟/容差表达后，再运行同一 hostile
   matrix、focused 和连续两次 full Hyperliquid。

提交信息：
- QA commit：待本报告提交后由线程回报提供。
- Implementation commit：
  `16d9db34ab977efcc36db5a2676865413dce1dc6`
- Workflow/business commit：
  `ea09b36d651bd9872a036875f50f925cd8106c9e`
