# QA 验收结果

## Findings

1. P0：无。
2. P1：无。
3. P2：same-window verifier 存在离线 false-negative。
   - Producer 在 `hyperliquid_tiny_live_m2_public_watcher.py:10359-10368` 每个 manager batch 只执行一次 freshness refresh。
   - 随后在 `:10842-10901` 将同一 freshness projection 写入 manager attempts `1/2`。
   - Verifier 在 `cross_exchange_t024_same_window_acceptance.py:6847-6871` 按 `(event_sequence, attempt)` 精确 join，错误地将 attempt `2` 判为 unbound。
   - 当前测试 `test_cross_exchange_t024_same_window_acceptance.py:3437` 使用 rollout 前的 `TASK_ID=0719T001`，没有覆盖 `0721T038+` 的 strict decision-evidence 路径。

执行线程：
- 独立 QA 验收线程

任务ID：
- 0721T044

状态：
- 已通过

更新时间：
- 2026-07-21 16:58 UTC

验收对象：
- Source/dispatch commit `6142b5e9b9ce9c2189cc2d4bbdbc358632de1107`
- Business/report commit `b3b312f9663509b91be7a0642e5c3c48f313d5ea`
- Exact live source `260f8964812eb20506eb221333e6aca17da20b4d`
- Immutable evidence `local_live_analysis/principal_alignment_estimator_observe_only_0721T044/`

验收范围：
- Source/runtime/checksum/account、唯一窗口、reference-bound lifecycle、resting exposure、estimator replay及same-window verifier归因。

实际结果：
- Runtime provenance在watcher前sealed；独立对比Git blobs为`63/63`，mismatch `0`；start/postrun均pass。
- Terminal manifest独立重算`108/108`，missing/mismatch/unexpected均为`0`。
- 仅有`window_01`：`2026-07-21T16:27:18Z`至`16:34:45Z`；child `rc=0`且已reap。
- Final owned/account open orders均为`0`，BTC position `0.0`，kill-switch clear；writer `395`次成功、`0`失败。
- Buy attempt 1为reference-bound rejection；sell attempt 2 exchange-confirmed resting并由相同oid/cloid cancel success；两条reference全部证明。
- Confirmed interval/exposure/censor/quarantine为`1/3/1/0`。
- Estimator replay exit `0`，event rows `3261`，source/replay SHA均为`3b70839b...4fef`，activation和quote behavior change均为false。
- Same-window acceptance独立重跑exit `2`：provenance `113/113`、config `72/72`、lifecycle `78/78`、economics `6/6`、optimism `6/6`；decision `39 pass / 4 fail`。
- 四项失败仅由`attempt_public_state_freshness_projection_unbound:16:1896`产生。Event `1896`只有一个freshness row；attempts `1/2`均精确投影`1920/1920/true`。
- 所有QA命令保持offline-only；未调用live/private/account/network/remote/service/order/cancel。

验收结论：
- 已通过
- T044满足任务定义的替代完成条件：唯一窗口安全完成，首个formal stop condition被准确记录，没有第二窗口或乐观升级。
- 此结论仅接受T044的安全执行、证据保存和fail-closed处理；不接受其为same-window accepted baseline。

通过项：
1. Exact source、runtime、checksum、唯一窗口和账户终态。
2. 两侧authoritative reference-bound lifecycle。
3. Resting exposure、censor/quarantine和estimator exact replay。
4. Adaptive、dynamic及multi-level activation始终关闭。
5. Same-window failure已独立归因为P2 verifier join缺陷。

不通过项：
1. Principal Task 8 same-window mechanism/evidence integrity gate仍为fail。
2. T044不得升级为accepted baseline。

缺陷清单：
1. Verifier需支持唯一batch freshness向同事件的two-sided manager attempts投影。
2. 增加`0721T038+` strict-path two-sided batch freshness端到端测试。

阻塞项：
- Stable A/k、fill-rate、fee/rebate calibration、queue priority、maker viability、PnL、promotion、final MVP和multi-level仍不受支持。
- Multi-level未解锁。

建议总控下一步：
1. 新建offline-only verifier repair任务；不得修改或重封T044 immutable evidence，也不需要新增live window。
2. 修复必须要求：同事件freshness唯一、manager batch身份明确、attempts/side/key完整且投影字段exact match。
3. 对missing、duplicate、conflicting、cross-event、cross-window、stale/block freshness及空projection继续fail closed；不得放宽submit-decision和其他逐attempt joins。
4. 修复经独立QA后，在同一immutable T044 evidence上重跑acceptance，再决定是否关闭baseline gate。

实际命令与结果：
- Same-window acceptance：exit `2`，`39 pass / 4 fail`。
- Estimator replay：exit `0`，snapshot match。
- Runtime Git blob检查：`63/63 pass`。
- Terminal manifest独立检查：`108/108 pass`。
- Focused watcher test：`1 passed, 132 deselected`。
- Focused acceptance test：`1 passed, 242 deselected`。
- `git diff --check`及commit diff检查：通过。

提交信息：
- QA commit：无；本线程只读，未修改仓库或immutable evidence。
