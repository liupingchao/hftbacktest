```md
# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0625T005

状态：
- 已通过

更新时间：
- 2026-07-05 21:18 CST

验收线程：
- QA验收线程

验收对象：
- 业务线程-shadow 0625T005

验收范围：
- 验收 `0625T005 Multi-Window Production Shadow Acceptance` 的 task file、business report、offline public-shadow runner、focused tests、formal output package、boundary manifest、counterfactual markout diagnostics、deterministic reproduction 和 no-submit/no-live boundary。

验收步骤：
1. 复核 `.workflow/tasks/0625T005.md`、`.workflow/reports/0625T005-business.md`、T003 QA accepted signal contract、T004 QA accepted shared kernel、`progress.md`、`findings.md`、`task_plan.md` 和 auto-loop plan 的 T005 边界。
2. 复跑 T005 focused pytest、combined T003/T004/T005 pytest、`py_compile`、CLI help、JSON parse、formal runner reproduction 和 `git diff --check`。
3. 独立校验 `local_live_analysis/cross_exchange_mvp_production_shadow_0625T005/` 的 required artifacts、CSV schema/row counts、would-submit counts、per-window edge proxy、warning-bucket visibility、boundary flags 和 deterministic reproduction。

实际结果：
- T005 created `examples/hyperliquid/cross_exchange_production_shadow.py` and `examples/hyperliquid/test_cross_exchange_production_shadow.py`.
- T005 consumes the QA-accepted `0627T001` public rows, the T003 accepted signal contract, and the T004 shared pure signal/fair-mid/quote-intent kernel.
- Runner remains offline/public-only/no-submit and uses future labels only for counterfactual markout, not for decision inputs.
- Frozen normalization stats are sourced from all valid `0627T001` public-row feature distributions without future labels.
- Formal output package exists at `local_live_analysis/cross_exchange_mvp_production_shadow_0625T005/`.
- Raw input rows: `10747`; valid shadow rows: `10704`; would-submit rows: `1098`.
- Would-submit by window: `429 / 291 / 378`.
- Per-window mean adjusted counterfactual edge ticks: `13.54662005 / 2.02233677 / 4.33333333`.
- Aggregate mean adjusted counterfactual edge ticks: `7.32058288`; max window contribution: `0.39071038`.
- T003 warning bucket remains visible: `1231` warning-bucket decisions and `91` warning-bucket would-submit rows.
- Final recommendation is `production_shadow_accepted_for_replay_contract`.
- Focused tests passed: `python -m pytest examples/hyperliquid/test_cross_exchange_production_shadow.py -q` -> `3 passed`.
- Combined T003/T004/T005 focused tests passed: `python -m pytest examples/hyperliquid/test_cross_exchange_signal_acceptance.py examples/hyperliquid/test_cross_exchange_shared_signal_kernel.py examples/hyperliquid/test_cross_exchange_production_shadow.py -q` -> `9 passed`.
- `py_compile` passed for the T003/T004/T005 runner modules and T005 focused test.
- CLI help passed: `python examples/hyperliquid/cross_exchange_production_shadow.py --help`.
- JSON parse passed for `production_shadow_manifest.json` and `boundary_manifest.json`.
- Formal runner reproduction passed to `/tmp/0625T005_qa_repro.cpDj7Z`.
- Structured artifact validation passed: 2 JSON, 9 CSV and 1 markdown artifact matched after excluding manifest output paths.
- `git diff --check` passed.
- Boundary manifest confirms offline/public-only/no network/no AWS/no remote/no credentials/no private/account/order/cancel/no user stream/no live client/no live orders/no submit/no watcher strategy change/no production config change/no signal feature search/no threshold tuning/no side mapping change/no horizon change/no canary/no promotion.
- Caveat remains visible for T006: median adjusted counterfactual edge is `-1.5` ticks because many 1000ms rows have zero mid move after subtracting the `1.5` tick buffer.

验收结论：
- 已通过
- 结论说明：
  - 0625T005 satisfies the multi-window production-equivalent no-submit public-shadow acceptance scope and accepts `production_shadow_accepted_for_replay_contract` for the next audit/replay-contract task.

通过项：
1. Offline public-shadow runner calls the T004 shared kernel and preserves the T003 accepted signal contract.
2. Would-submit sample size is not thin and is distributed across all three windows.
3. Per-window and aggregate adjusted counterfactual edge means are positive, with no single-window domination.
4. T003 warning bucket remains visible in diagnostics.
5. Boundary flags confirm no live/private/order/canary/promotion behavior.

不通过项：
1. 无

缺陷清单：
1. 无

阻塞项：
- 无

建议总控下一步：
1. 可按 `docs/cross_exchange_mvp_auto_loop_plan.md` 创建 `0625T006 Hyperliquid MVP Audit and Replay Contract`。
2. T006 必须保留 T005 median adjusted edge caveat、T003 warning bucket 和 no-live/no-private/no-order boundary。
3. T006 不得授权 live submit；首次 live-submit boundary 仍需在 T008 前由总控显式确认。

提交信息：
- commit：无
```
