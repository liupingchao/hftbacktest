# QA 验收结果

执行线程：
- 独立 QA验收线程

任务ID：
- 0720T014

状态：
- 已通过

更新时间：
- 2026-07-20 09:27 CST

验收对象：
- implementation commit `33975476d629e1abc1f62324276785ac0908093f`
- business report commit `19f3fbf6d3c1f3bdb5544528ce3266f388d4c07e`
- 审查范围：`33975476^..19f3fbf6`
- 实现代码范围：`33975476^..33975476`

## Findings

1. 未发现 P0、P1 或 P2 缺陷。
2. `attempt_key` suffix 已改用 `raw_strict_positive_attempt()`：
   - 转换前限制最大 10 位；
   - 仅接受 canonical 正整数；
   - 范围限制为 `1..2147483647`；
   - 不再对攻击者控制的超长数字直接执行 `int()`。
3. Exact task、`window_01`、row window、`attempt_id`/legacy attempt/suffix equality 检查均保留。
4. 实现提交只修改 acceptance parser 与其测试，未改变 strategy、threshold、risk、activation 或 producer 行为。

验收范围：
- T014 bounded attempt-key suffix parser repair。
- T013 strict trigger、anti-drift、guard、edge、attempt joins 回归。
- Exact T011 legacy guard bridge 与 byte-exact replay。
- 全程只读；生成型输出仅写入 `/tmp`。

实际结果：
- 独立 synchronized/resealed 5000 位 suffix：
  - 未抛异常；
  - final recommendation 为 `principal_task12_same_window_acceptance_blocked`；
  - validation reasons 为 `attempt_key_mismatch:0:1`、`attempt_key_mismatch:1:2`。
- 独立 synchronized/resealed `2147483648` suffix：
  - 未抛异常；
  - final recommendation blocked；
  - 同样产生两条 `attempt_key_mismatch`。
- 两个反例均保持 provenance `112/112`、config `71/71`、lifecycle `61/61`，说明阻塞来自 decision identity validation。
- Valid actual two-sided writer fixture、普通 strict joins、zero-authorization guard 和 stale attempt-key 用例通过。
- Legacy bridge 的授权条件为显式 flag、task `0719T011`、source `d8e22c2d9288fef86707d9b26f7791d7d8711c09` 三者 conjunction。
- Exact T011 replay带 flag：exit `2`、bridge authorized、validation reasons `[]`、provenance `112/112`、config `70/71`、decision `16/43`、lifecycle `34/61`，final blocked。
- Exact T011 replay不带 flag：exit `2`、bridge unauthorized，并产生 21 条 legacy-not-authorized reason。
- 显式 flag但非 exact task/source 的 downgrade 拒绝测试通过。

命令与结果：
- Focused acceptance/event-driven watcher/orchestrator/manager：
  - `245 passed in 16.99s`
- 条目定向回归：
  - `11 passed in 0.58s`
- `python -m pytest -q -p no:cacheprovider examples/hyperliquid`：
  - `708 passed in 35.51s`
- 修改模块及测试 `py_compile`：
  - pass；pycache 定向至 `/tmp/0720T014-pycache`
- Acceptance CLI `--help`：
  - pass；包含 `--allow-legacy-guard-identity-bridge`
- Implementation/business commits `git show --check`：
  - pass
- `git diff --check 33975476^ 19f3fbf6`：
  - pass
- 当前工作树 `git diff --check`：
  - pass

安全边界：
- 本 QA 未执行 live、private、account、order、cancel、network、remote 或 service 命令。
- 反例和 T011 manifests 均记录 `offline_only=true`，相关 endpoint/network/remote boundary 字段全部为 false。
- 验收前后分支保持 `cross-exchange`，HEAD 保持 `19f3fbf6`；已有无关工作树改动未变化，QA 未编辑、清理、stash、恢复或提交文件。

验收结论：
- 已通过
- 结论说明：
  - T013 唯一遗留的 unbounded attempt-key integer conversion 已修复；恶意 suffix 现在稳定 fail closed 为 blocked manifest，未发现既有严格 join 或 legacy bridge 回归。

通过项：
1. Bounded suffix parser 与 fail-closed 反例。
2. Valid two-sided fixture 和 T013 strict joins。
3. Exact T011 legacy bridge 与 byte-exact blocked replay。
4. Focused、full suite、compile、CLI 和 commit/range integrity checks。

不通过项：
- 无

缺陷清单：
- 无

阻塞项：
- 无

剩余风险：
- 本结论仅接受 T014 离线 parser repair，不构成 live、策略收益、multi-level、risk 或 producer 行为验收。
- 工作树仍包含验收前已有的无关修改和未跟踪文件。

建议总控下一步：
1. 将 `0720T014` 更新为 `已通过`。
2. T014 parser blocker 可关闭；后续 live 或策略任务仍须按独立任务边界和既有门禁派发。

提交信息：
- implementation commit：`33975476d629e1abc1f62324276785ac0908093f`
- business report commit：`19f3fbf6d3c1f3bdb5544528ce3266f388d4c07e`
- QA commit：无
- QA 文件修改：无
