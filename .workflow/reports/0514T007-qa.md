# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0514T007

状态：
- 已通过

更新时间：
- 2026-05-14 19:18 Asia/Shanghai

验收线程：
- QA验收线程

验收对象：
- 业务线程-python + 0514T007

验收方式：
- 正常验收

验收范围：
- 验收 `0514T007` 是否按 `0514T006` 已通过的 Stage 6A 合同，实现了只读 replay/live execution outcome calibration runner，是否正确使用 matched submit opportunities 作为 comparison unit，是否在 `5-13-day-control-30min` 上生成 required calibration artifacts，并清楚区分对齐与未对齐的 lifecycle / strata 指标。

验收步骤：
1. 读取 `.workflow/tasks/0514T007.md`。
2. 读取 `.workflow/reports/0514T007-business.md`。
3. 检查实现是否保持 read-only 边界：不启动 live、不生成新 replay sweep、不改策略、不改 core/connector/schema。
4. 核对 comparison unit、required artifacts、verify 证据和业务结论。
5. 写入 `.workflow/reports/0514T007-qa.md` 并同步 `docs/qa-acceptance-report.md`。

实际结果：
- 任务文件存在：`.workflow/tasks/0514T007.md`。
- 业务回报存在：`.workflow/reports/0514T007-business.md`。
- 业务回报状态：`待验收`。
- 代码文件存在：
  - `examples/binance_tick_mm/execution_outcome_calibration.py`
  - `examples/binance_tick_mm/test_execution_outcome_calibration.py`
- 输出目录存在，并包含 required artifacts：
  - `execution_calibration_summary.md`
  - `submit_key_coverage.csv`
  - `fill_horizon_gap.csv`
  - `time_to_fill_gap.csv`
  - `final_state_gap.csv`
  - `cancel_race_gap.csv`
  - `markout_gap.csv`
  - `placement_strata_gap.csv`
  - `inventory_strata_gap.csv`
  - `latency_strata_gap.csv`
  - `coverage_gap.csv`
  - `run_manifest.json`
- comparison unit 符合 `0514T006`：业务实现明确使用 `submit_strategy_seq + order_side` 构造 normalized submit key，并单独输出 submit matching coverage。
- 真实样本结果与业务回报一致：
  - matched submit orders：`2516`
  - live filled orders：`53`
  - replay filled orders：`172`
  - live fill-after-cancel orders：`16`
  - replay fill-after-cancel orders：`133`
  - decision_state：`diagnostic_only_gap_too_large`
- 业务回报清楚区分了：
  - 已对齐：submit coverage、short-horizon fill rates、fast-cancel churn
  - 未对齐：final order state、fill-after-cancel-request、cancel-to-fill delay、部分 long-horizon fill / time-to-fill
  - 差异集中 bucket：placement / edge / inventory / latency
- 实现保持 read-only 边界：未启动 live、未新跑 replay sweep、未改策略、未改 core/connector/schema。

验收结论：
- 已通过
- 结论说明：
  - `0514T007` 已满足 Stage 6B 的单样本 methodology / diagnostic 目标。comparison unit、artifacts、验证证据和结论边界都成立；当前合理结论就是 `diagnostic_only_gap_too_large`，而不是 promotion / exact queue proof。

通过项：
1. comparison unit 正确：先做 matched submit coverage，再解释 lifecycle gaps。
2. required artifacts 完整。
3. pytest、CLI help、real dataset full run、`git diff --check` 证据完整。
4. 业务回报明确说明什么对齐、什么没对齐，以及差异集中在哪些 strata。
5. observed-only proxy / no exact queue proof 的边界清楚。
6. 单样本 decision state 合理，没有越权解释成 promotion 或 live readiness。

不通过项：
1. 无

缺陷清单：
1. 无

阻塞项：
- 无

建议总控下一步：
1. 将 `0514T007` 视为 QA 通过。
2. 不要基于当前单样本结果直接进入 quote-adjustment promotion；下一步应先决定是否补充更多 current-format samples，还是在现有 gap 结论下回到更早层处理 replay fill/cancel lifecycle mismatch。

提交信息：
- commit：无
