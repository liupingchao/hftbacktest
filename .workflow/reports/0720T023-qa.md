# QA 验收结果

执行线程：
- 独立只读 QA 验收线程

任务ID：
- 0720T023

状态：
- 未通过

更新时间：
- 2026-07-20 17:23 CST

验收线程：
- QA验收线程

验收对象：
- Implementation commit：`b8474af3b94a0d5145398a9c82e577e5ab038d51`
- Business report commit：`1550961b54cc6287b27968d7b596fd8e2e47c1e8`
- 业务执行线程 / 0720T023

验收范围：
- Official nested orderStatus、bounded direct/history recovery、v4 audit 和 canonical result
- Post-history snapshot、exact OID recovery、SDK readiness、task-aware title
- T016/T022 immutable offline replay
- 全程只读；未访问 network、private/account/order/cancel/remote/service

验收步骤：
1. 核对任务、业务报告、提交祖先关系、文件范围和工作树。
2. 静态审查 manager、producer、acceptance 三层 exact-reference contract。
3. 运行聚焦测试、完整 Hyperliquid 测试、语法、CLI 和 Git checks。
4. 在 socket/request 强制拒绝条件下运行 SDK readiness。
5. 离线重放 T016/T022 并核对边界、标题、结论和输入哈希。
6. 构造 conflicting/malformed historicalOrders hostile counterexample。

实际结果：
- Implementation 是 business report commit 的直接祖先；任务范围内文件与 implementation commit 完全一致。
- Focused tests：`523 passed in 19.12s`。
- Full tests：`861 passed in 39.18s`。
- 12 个修改模块/测试的 `py_compile`：通过，缓存写入 `/tmp`。
- Acceptance/readiness `--help`：通过。
- Implementation 和 report 的 `git show --check`、commit-range `git diff --check`：通过。
- SDK readiness：`hyperliquid-python-sdk==0.24.0`，`historical_orders` surface 通过，constructor timeout 和 write/read/restore 通过。
- SDK guarded probe 的 endpoint attempt count 为 `0`；credential、HTTP/WS、private/account/order/cancel flags 均为 false。
- T016 replay exit `2`：decision `43/43`，lifecycle `49 pass / 12 fail`，标题 `0720T016`，保持 blocked。
- T022 replay exit `2`：decision `43/43`，lifecycle `49 pass / 12 fail`，标题 `0720T022`，保持 blocked。
- 两次 replay 均为 `offline_only=true`，network/private/order/cancel/remote/new-live 全部 false。
- T016 输入文件 `118` 个，前后 hash：
  `a5443f8f9cb777509ffde21f9caed3837a9ef58f31b25cd82acef2d6a3991398`。
- T022 输入文件 `118` 个，前后 hash：
  `71c9ba35f62e6657d0d48f63abb451ad538f14d85d7113a567b16dfe2b92833c`。
- Hostile counterexample 显示：包含同一 reference 的 `filled` 冲突行和 `canceled` exact 行时，manager、producer、acceptance 均接受 canceled terminal proof。

验收结论：
- 未通过
- 结论说明：
  - 大部分 bounded recovery、安全和回放契约成立，但核心 historical exact-identity gate 未拒绝同一 reference 的 conflicting/malformed rows，不满足任务验收标准。

通过项：
1. Official nested direct status、五轮/五秒边界、每 reference 一次 history 上限。
2. direct_round、query sequence、canonical source 和 post-history snapshot 审计。
3. unknown/account absence/filled-without-raw-fill 保持 fail closed。
4. Exact OID symbol/side/price/size/alias recovery 检查。
5. SDK readiness、task-aware title、T016/T022 immutable replay。

不通过项：
1. Mixed conflicting/malformed exact-reference historical response 可被接受。

缺陷清单：
1. [P1] Historical scan 把共享 expected oid/cloid 但未完整匹配的行当作 foreign row 跳过。

阻塞项：
- Private historical recovery、新 bounded live、Task 8 和 adaptive/multi-level activation继续锁定。

建议总控下一步：
1. 将历史行分类为 exact、fully-disjoint foreign、reference-conflicting 和 malformed-reference。
2. 只有 fully-disjoint foreign 可忽略；任何共享 expected oid/cloid 的非 exact 行必须使该 reference fail closed。
3. 在 manager、producer、acceptance 三层增加 alias conflict、same-oid/different-cloid、same-cloid/different-oid 与 conflicting filled+canceled 测试。
4. 修复后重新派发独立离线 QA。

提交信息：
- commit：无；本次验收未编辑、暂存、提交或清理任何文件。
