# QA 验收结果

执行线程：
- QA验收线程（第四轮全新独立 Stage 4 验收）

任务ID：
- `0815T003`

状态：
- 未通过

更新时间：
- 2026-08-16 22:20 CST（星期日）

验收线程：
- QA验收线程

验收对象：
- 业务线程-python/research + `0815T003` 第三轮有界返修
- 正式 package：
  `local_live_analysis/skhynix_trigger_aligned_episode_research_v1_stage04_jul30_episode_v3/`
- 实现：
  - `examples/hyperliquid/cross_exchange_trigger_aligned_episodes.py`
  - `examples/hyperliquid/cross_exchange_jul30_episode_v3_admission.py`
  - 两个对应 focused test files
- 本轮全新 QA 根：
  `/tmp/0815T003-qa-round4.UjUS70/`

验收范围：
- 仅 Jul30 immutable public-data inputs。
- 不解析 Jul30 legacy `episodes/*.csv.gz`。
- 不读取 Aug03/Aug04 future event rows 或任何 Aug07 event rows。
- 不运行 case/model/score/actionability/orders/network/live config。
- accepted Stage 1/2/3 package 和 Jul30 raw/R0/R1 只读。
- 未复用任何先前 QA/业务 `/tmp` 证据。
- 除本报告及其 mirror 外，不修改 task、tracking、实现、测试、正式
  package 或 accepted inputs。
- Stage 5 在本轮 `已通过` 前保持锁定。

## Findings First

普通代码审查：
- P0：`0`
- P1：`1`
- P2：`1`
- P3：`0`

### P0 Findings

- 无。

### P1 Findings

#### P1-1 standalone attestation 接受未冻结的 manifest aggregate count extra key

判定：
- 未通过。
- 对 source 和 archived admission 分别 monkeypatch
  `verify_package()`，返回完整、正向、12-projection source-semantic
  evidence，但在 `manifest["aggregate_output_counts"]` 额外加入：

```text
unexpected = 0
```

- 两侧均错误返回：

```text
rc=0
verified=true
source_semantic_verified=true
```

- 这违反本轮明确要求的 manifest aggregate count exact key universe。

代码证据：
- `examples/hyperliquid/cross_exchange_trigger_aligned_episodes.py:4493-4495`
  只要求 `aggregate_output_counts` 是 `dict`。
- `examples/hyperliquid/cross_exchange_trigger_aligned_episodes.py:4544-4562`
  只逐 projection 检查 required count bindings 是否存在且等于 frozen
  row count，没有拒绝未冻结的额外 count key。
- 相比之下，`exact_counts` 在
  `examples/hyperliquid/cross_exchange_trigger_aligned_episodes.py:4480-4491`
  已执行 exact key set 检查。
- 当前 permanent negative matrix
  `examples/hyperliquid/test_cross_exchange_jul30_episode_v3_admission.py:113-148`
  覆盖 aggregate count missing/value/bool/string，但没有
  `aggregate_count_extra`。

独立矩阵：
- 每个 target 共 `45` 个 negative cases，source/archive 共 `90` calls。
- source：`44/45` fail closed。
- archived：`44/45` fail closed。
- 总计：`88/90` fail closed，`2/90` 被错误接受。
- 唯一错误接受的 case：
  `aggregate_count_extra`，source/archive 各一次。
- 两侧完整 positive aggregate 均 `rc=0`。
- 第三轮 QA 的 forged aggregate 已关闭：
  source/archive 均 `rc=2`，错误为
  `source-semantic aggregate projection universe drift`。

已通过的 negative categories：
- aggregate missing/extra projection/non-dict；
- entry missing/extra/non-dict；
- fields 内容、顺序、类型漂移；
- expected/observed count value/bool/string；
- digest mismatch、malformed、uppercase、non-string；
- mismatch nonzero/bool/string；
- scope drift/non-dict；
- semantic top-level extra/missing、verified false/non-dict；
- exact count missing/extra/value/bool/string/non-dict；
- aggregate count missing/value/bool/string/non-dict；
- projection binding contradiction。

影响：
- 当前正式 package 的真实 `verify_package()` 仍会在完整 source replay 后
  拒绝这一 extra key；本 finding 不表示正式 Episode rows 已损坏。
- 但 standalone admission 的二次 attestation 仍会在其受信
  `verify_package()` 返回了附加、未冻结 manifest count evidence 时过度
  声明 `source_semantic_verified=true`。
- Stage 5 若把 standalone attestation 当作 durable trust boundary，会
  接受比 frozen contract 更宽的 evidence surface。

必须返修：
1. 冻结 `aggregate_output_counts` 的 exact key universe。
2. `_validate_source_semantic_verification_evidence()` 在成功前要求
   `set(aggregate_output_counts) == expected_key_set`。
3. 新增 source/archive `aggregate_count_extra` permanent regression。
4. 不改 Episode v3 research rows、feature/view/outcome semantics。

### P2 Findings

#### P2-1 第三轮到第四轮的 99-file research inventory 没有 durable 独立重放入口

判定：
- 本轮独立证明了当前正式 package 与本轮 fresh rebuild 的全部 `99`
  个 research CSV/GZ 文件逐文件 bytes/raw SHA 相等，`changed=0`。
- 但第三轮 QA candidate package 只保存在先前 `/tmp`，本轮明确禁止
  复用；repository 内也没有持久化第三轮 `99` 个 relative path/bytes/raw
  SHA inventory。
- `.workflow/reports/0815T003-business-r3.md` 声称 cross-round
  `changed=0`，但没有发布可由独立 QA 重算的旧 inventory 明细或 digest
  binding。业务线程自报不能替代独立证据。

当前可独立证明：
- current formal vs fresh：
  - research files：`99`
  - bytes：`1560514934`
  - changed：`0`
  - canonical inventory SHA256：
    `bb5aed2099b1a97da5b476d4b09dfe4a7bac06f0bf331f8ca864a88daf5c9232`
- current key cardinality/outcome statistics 与第三轮 QA 报告值一致。

不能独立证明：
- Round 3 accepted candidate package 的 `99` 个逐文件 raw SHA 与当前
  formal 的逐文件 equality；旧明细没有 durable archive。

影响：
- 没有发现 research data 实际变化。
- 这是跨返修证据留存缺口，而不是当前数据面 mismatch。

建议：
1. 本次返修前先将当前 `99`-file inventory 作为 durable artifact/report
   evidence 保存。
2. 下一轮 QA 直接比较返修前后 inventory，不依赖 ephemeral `/tmp`。

### P3 Findings

- 无。

## Fresh Full Rebuild And Identity

命令：
- `/usr/bin/time -l python -B
  examples/hyperliquid/cross_exchange_trigger_aligned_episodes.py
  --output-dir
  /tmp/0815T003-qa-round4.UjUS70/rebuild/package`

结果：
- 8 个 segment 全部完成。
- fresh package：
  - files：`107`
  - symlinks：`0`
  - bytes：`1561285739`
  - artifacts：`106`
  - Family A/B/rejected：`268522 / 141768 / 126754`
- formal/fresh 全部 relative path、type、size、raw SHA：
  exact identical。
- independently recomputed identities：
  - core：
    `95dac8f94b6d8223d51b91c95cdea74963e62b9a940526f740fd104b062b9f7b`
  - full：
    `49cab05e39a74e6fd120088d19fcbf657739e64f8dadba33b267edb4c8d9272c`
  - contract：
    `ff2b80e627415b8fc88d05a1f08c7d740c612cfdf13a26915293bc15e862b867`
  - manifest：
    `7b165ff841d3829b88362bd7ea36e6546d415ec4b9e4b2637b4489cd9ee3b30b`
- formal/fresh manifest/core/contract/full 均与待验值一致。
- fresh rebuild wall：`1787.18s`。
- fresh rebuild max RSS：`740687872 bytes`。

## Aggregate Contract Audit

独立结果：
- source contract == archived contract：`true`。
- source contract == frozen artifact aggregate contract：`true`。
- frozen contract raw JSON canonical：`true`。
- manifest raw JSON canonical：`true`。
- manifest contract SHA binding：`true`。
- current/archived/manifest runtime source-test bindings：全部一致。
- aggregate evidence contract version：
  `skhynix_jul30_source_semantic_aggregate_evidence_v1`。
- exact projections：`12`：
  - anchors
  - views_family_a
  - views_family_b
  - features_family_a
  - features_family_b
  - sparse_range_family_a
  - sparse_range_family_b
  - fixed_grid_family_a
  - fixed_grid_family_b
  - event_count_family_a
  - event_count_family_b
  - outcomes
- 每个 projection 的 ordered fields、frozen rows 和 manifest bindings
  与 code/artifact 一致。
- canonical integer policy 正确拒绝 bool/string。
- digest policy 正确要求 lowercase 64-hex 且 expected==observed。
- mismatch policy 正确要求 canonical int `0`。

## Positive Admission

默认 Python、无 `-B`：

| verifier | rc | verified | source semantic | wall | max RSS |
| --- | ---: | --- | --- | ---: | ---: |
| current source | 0 | true | true | 1306.63s | 714276864 |
| archived runtime | 0 | true | true | 1298.00s | 688275456 |

两侧共同：
- files/bytes：`107 / 1561285739`
- core/full：
  `95dac8f94b6d8223d51b91c95cdea74963e62b9a940526f740fd104b062b9f7b`
  /
  `49cab05e39a74e6fd120088d19fcbf657739e64f8dadba33b267edb4c8d9272c`
- exact 12-projection aggregate；
- expected/observed rows 和 digest 全部相等；
- mismatch rows 全部 `0`。

## Production-Size Coherent-Rehash

每项均从本轮正式 `1.56 GB` package 创建新的 APFS clone，修改
load-bearing artifact/manifest 后重算受影响 artifact records、core、
contract/runtime bindings 和 canonical manifest。

| attack | source | archived | failure boundary |
| --- | ---: | ---: | --- |
| Family A `pre_binance_bid_px` value | 2 | 2 | exact feature value |
| frozen contract scope shrink | 2 | 2 | frozen contract drift |
| manifest exact count | 2 | 2 | manifest exact count drift |
| manifest aggregate count extra | 2 | 2 | manifest aggregate output count drift |
| archived source + test coherent rehash | 2 | 2 | fixed worktree runtime identity |

- production calls：`10/10` fail closed。
- representative feature attack：
  - source/archive wall：`57.02s / 57.45s`
  - source/archive max RSS：`533118976 / 495403008`
- contract attack：
  - wall：`1367.19s / 1377.24s`
  - max RSS：`618954752 / 620396544`
- manifest exact attack：
  - wall：`1363.77s / 1371.93s`
  - max RSS：`608583680 / 615432192`
- manifest aggregate attack：
  - wall：`1366.55s / 1361.80s`
  - max RSS：`609681408 / 558039040`
- archived runtime attack：
  - wall：`1.28s / 1.27s`
  - max RSS：`63471616 / 64520192`
- 前轮 32 类 data attacks 的 implementation boundary 未见漂移；本轮以
  99-file current formal/fresh raw equality、379 tests、representative
  data mutation 和四类 trust-boundary production attacks形成独立充分
  回归证据，没有重复无意义的全部 48GB clone matrix。

## Data Cardinality And Semantics

- anchors / Family A / outcomes：
  `268522 / 268522 / 268522`
- Family B / confirmed：
  `141768 / 141768`
- rejected：
  `126754`
- rejected non-null confirm：
  `0`
- feature A/B：
  `23092892 / 15310944`
- view A/B：
  `268522 / 141768`
- sparse A/B：
  `268522 / 141768`
- fixed grid A/B：
  `4564874 / 2410056`
- event count A/B：
  `805566 / 425304`
- unavailable features：
  `2863855`
- auxiliary/core degraded grid：
  `4316 / 0`
- first-event status totals：
  - interval-censored：`627406`
  - right-censored：`177826`
  - segment-censored：`316`
  - quality-censored：`18`
  - epoch-censored：`0`
- interval lower `< Candidate`：`0`
- point/empty interval：`0`
- interval source-event missing：`0`
- future feature observation：`0`
- synthetic rejected confirm：`0`
- anchor ordering mismatch：`0`
- cross-segment / cross-epoch：`0 / 0`

## Tests And Static Verification

Pytest：
- Stage 4 focused：`80 passed in 0.12s`
- Stage 1/2/3 inherited：`216 passed in 131.60s`
- R0/alignment/recovery：`83 passed in 0.48s`
- total：`379 passed`

其他：
- Ruff：`All checks passed!`
- external `PYTHONPYCACHEPREFIX` compileall：
  `48` bytecode files，全部写入本轮 `/tmp`。
- deterministic gzip integrity：`96/96`。
- scoped diff-check：passed。
- package `.pyc/__pycache__`：`0`。
- worktree existing `examples/hyperliquid/__pycache__` mtime 为
  `2026-08-16 03:33:37 CST`，早于本轮 QA，且本轮未新增 pyc。

## Atomic Publication

- 注入 `_process_segment` failure。
- injected failure observed：`true`。
- output visible：`false`。
- residual staging directories：`0`。
- partial markers：`0`。

## Zero-Write And Input Immutability

正式 package 在 source/archive 默认 Python admission 前后：
- files：`107 / 107`
- bytes：`1561285739 / 1561285739`
- full inventory：
  `49cab05e39a74e6fd120088d19fcbf657739e64f8dadba33b267edb4c8d9272c`
  / same
- relative path/bytes/raw SHA exact identical：`true`
- package `.pyc/__pycache__`：`0`

Accepted inputs：
- unique rows：`394`
- bytes：`1412635729`
- before/after identity：
  `1473c267c06c7c13c82298c11a92a4a3427353c9db6e37b1271e7792deab4348`
  / same
- exact identical：`true`

Research data within current round：
- files：`99`
- bytes：`1560514934`
- formal/fresh changed：`0`
- inventory SHA：
  `bb5aed2099b1a97da5b476d4b09dfe4a7bac06f0bf331f8ca864a88daf5c9232`

## Hard Boundary

- manifest 十项 boundary 全部 `false`。
- input binding rows：`788`，unique inputs：`394`。
- exact forbidden input path rows：`0`。
- accepted Stage 1 的 `aug07_access_ledger.json` 只作为 dependency
  inventory hash 两阶段各出现一行；未读取 Aug07 event rows。
- package forbidden later-session artifact paths：`0`。
- runtime network imports：`0`。
- 未读取 Jul30 legacy episode rows。
- 未读取 Aug03/Aug04 future event rows。
- 未读取任何 Aug07 event rows。
- 未运行 case/model/score/actionability/orders/network/live。
- 未修改 `.workflow/tasks/0815T003.md`、`task_plan.md`、`progress.md`、
  `findings.md`、实现、测试、正式 package 或 accepted inputs。

## 验收结论

- `未通过`
- 当前正式 package 的 data plane、source-semantic replay、deterministic
  rebuild、runtime binding、positive admission 和真实 production package
  mutation boundary均通过本轮独立检查。
- 但 standalone attestation 仍会接受
  `aggregate_output_counts` 的未冻结 extra key，并过度声明
  `source_semantic_verified=true`。这是决定性 P1。
- 此外，Round 3 到 Round 4 的 99-file cross-round inventory 没有 durable
  independent replay artifact，记为 P2 证据留存缺口。
- `0815T003` 不能置为 `已通过`。
- Stage 5 必须继续锁定。

通过项：
1. Fresh rebuild 与正式 package 107 文件逐字节一致。
2. 正向 source/archive complete source-semantic admission 通过。
3. 12-projection contract 的 code/archive/artifact identity 通过。
4. 生产规模 5 类攻击 source/archive `10/10` fail closed。
5. current formal/fresh 99 research files `changed=0`。
6. cardinality、confirmation、interval-censoring 和 prior semantic repairs
   无回归。
7. `379` tests、Ruff、compileall、96 gzip、atomic、zero-write、input
   immutability 和 hard boundary 通过。

不通过项：
1. standalone manifest aggregate count extra-key fail-open。
2. 第三轮旧 99-file inventory 未持久化，cross-round data immutability
   不能由本轮独立重放。

缺陷清单：
1. P1-1：`aggregate_output_counts` 没有 exact key-universe assertion。
2. P2-1：缺少 durable pre-repair 99-file research inventory。

阻塞项：
- 无外部 blocker；需要同一任务的第四轮有界返修和第五轮全新独立 QA。

建议总控下一步：
1. 将 `0815T003` 保持/置为 `未通过`，Stage 5 继续锁定。
2. 返修只增加 aggregate output count exact key contract/assertion/test。
3. 返修前持久化当前 99-file inventory，返修后由第五轮 QA逐文件比较。
4. 不改 Episode v3 rows、detector、feature/view/outcome semantics。

提交信息：
- commit：无
