# QA 验收结果

执行线程：
- QA验收线程（第四轮全新独立验收）

任务ID：
- 0815T001

状态：
- 未通过

更新时间：
- 2026-08-15 15:53 CST（星期六）

验收线程：
- QA验收线程

验收对象：
- 业务线程-python/research + 0815T001 Round 3 bounded repair
- 正式包：
  `local_live_analysis/skhynix_trigger_aligned_episode_research_v1_stage02_density/`
- 业务声称 core SHA256：
  `982011eae0851112747ee8b98a9308edf3843c2ac7505105ee2c750951dbda02`
- Build A：
  `/tmp/0815T001-r3-density-a.kKplZL/package`
- Build B：
  `/tmp/0815T001-r3-density-b.46adRv/package`

验收范围：
- 独立复现 Round 3 P1：authoritative candidate projection 的 exact
  rejection、threshold-preserving impact、coherent pre-state、side/direction
  和整段 shock/decision/segment time-translation 变体。
- 独立复现 Round 3 P2：`input_bindings.csv`、五个 small CSV、candidate
  gzip、sensitivity gzip 的 leading/middle/trailing unnamed extra cell 和
  missing cell，共 `32` 组 production package coherent-rehash。
- 重新确认 Round 2 五表 duplicate-before/after、extra/missing key 和 ESS
  structural/session-total/per-segment 三类 keys。
- 重新确认 Round 1 session deletion、provenance omission、forged recovery/
  decision categories、evidence taxonomy 和 Jul30 segment role。
- Stage 2 四文件 tests、admission suite、reused hierarchy regressions、
  Ruff、compileall、CLI help、source/archived verify-only、zero-write、
  formal/A/B 比较、source/Stage1 实际重哈希和 `git diff --check`。
- 继续审查 projection framing、session ordering、CSV row grammar、
  fixture-only bypass、canonical contract consumption、verify ordering 和
  candidate-level structural membership。
- Stage 2 禁止边界：不读取 Aug07 event rows，不运行 response/outcome/
  model/PnL/actionability，不访问 private/account/order/cancel。

验收步骤：
1. 阅读 workflow-kit manual/templates、任务文件、前三轮 QA、R3 业务报告、
   主实施方案及 `task_plan.md`、`progress.md`、`findings.md`。
2. 从正式 candidate gzip 独立重算 projection version、字段序列、每 session
   row count 和 canonical SHA，并分别比较 source、archived source 和
   canonical contract anchors。
3. 在 `/tmp` 正式包副本中同步修改 payload、artifact records 和 core hash，
   分别运行 source CLI 与包内 archived CLI；检查首个拒绝是否为目标
   invariant，而不是 stale artifact/runtime drift。
4. 对八个 CSV 入口执行完整 `32/32` row-width 攻击；另验证 quoted comma
   和 blank-but-present cell 不被 row-cell closure 当作 missing。
5. 重跑 Round 1/2 production-sized coherent-rehash，运行完整测试和静态
   检查，比较 formal/A/B，并按 binding rows 实际读取和重哈希 source 与
   accepted Stage 1。
6. 继续构造 structural membership 和 session-order 变体，检查当前
   verifier 是否真正消费 `episode_merging_v1` candidate-level 结构。

运行命令与实际结果：
1. Stage 2 完整 focused suite：
   `python -B -m pytest -q
   examples/hyperliquid/test_cross_exchange_trigger_density_core.py
   examples/hyperliquid/test_cross_exchange_candidate_episode_merging.py
   examples/hyperliquid/test_cross_exchange_trigger_density_inputs.py
   examples/hyperliquid/test_cross_exchange_trigger_density_admission.py`
   - `130 passed in 8.32s`
2. Admission suite：
   `python -B -m pytest -q
   examples/hyperliquid/test_cross_exchange_trigger_density_admission.py`
   - `79 passed in 2.09s`
3. Reused hierarchy recovery regressions：
   `python -B -m pytest -q
   examples/hyperliquid/test_cross_exchange_liquidity_response_case_hierarchy.py
   -k 'merges_dense_clusters_without_recovery or
   stops_at_recovery_checkpoint or
   fails_closed_on_missing_recovery_evidence'`
   - `3 passed, 19 deselected in 0.05s`
4. Ruff：
   `python -m ruff check` 对四个 Stage 2 实现和四个测试文件。
   - `All checks passed!`
5. compileall：
   外置 `PYTHONPYCACHEPREFIX=/tmp/0815T001-qa4-pycache.ISVU4j`
   运行四个实现和四个测试文件。
   - `compileall_rc=0`
6. CLI help：
   source/archived `--help` 各 `26` 行，`cmp` 返回 `0`。
7. 正式包 source/archived `--verify-only`：
   - 两者均 `rc=0`、`verified=true`
   - `artifact_count=15`
   - `candidate_membership_rows=463612`
   - `confirmed_rows=267554`
   - `sensitivity_membership_rows=463612`
   - `session_count=3`
   - core SHA256 与业务声称一致：
     `982011eae0851112747ee8b98a9308edf3843c2ac7505105ee2c750951dbda02`
8. Formal 对 Build A/B：
   `python -B ...cross_exchange_trigger_density_admission.py --verify-only
   --compare-to <build> --output-dir <formal>`
   - A/B 均 `identical=true`
9. `git diff --check`
   - 返回 `0`

实际结果：
- 独立 projection 重算确认正式包 session block 为
  `jul30 -> aug03 -> aug04`，anchors 精确为：
  - Jul30：`268522` rows，
    `155d3b74898e10d56344f07808da4cea0abcec2696bd9bb6174e32c1bb8e2416`
  - Aug03：`127622` rows，
    `20028e23d1e4393a819c1d2882ce212a5cba96d8b57d4e7b55bd51d9a3d313d6`
  - Aug04：`67468` rows，
    `7266e1ac9f3654b74b4802eca01b31e1332c5499cd2b91c24cfc4f3938796bcb`
- 独立字段序列精确覆盖：
  `session_id,candidate_id,primary_episode,rejection_reason,aggressor_side,
  direction_sign,shock_ts_ns,decision_ts_ns,impact_ratio,pre_state_ts_ns,
  pre_state_age_ms,segment_id,connection_epoch_id,segment_start_ts_ns,
  segment_end_ts_ns`。
- 上述 version、fields、row counts 和 SHA 与 source constants、archived
  constants、`frozen_density_contract.json` 三方完全一致。
- Projection canonical framing 使用 version 行、JSON 字段数组行和逐行 JSON
  value array；JSON escaping 和换行 framing 无可利用拼接歧义。
- Round 3 P1 的五组 production-sized 变体全部精确 fail closed：
  hostile 根目录
  `/tmp/0815T001-qa4-projection-0ebpfnds/`。
  - `rejection_reason_quoted_comma`：把 rejected reason 改为含 quoted comma
    的 `forged,rejection_reason`
  - `impact_ratio_same_bucket`：`7.516129032258064` 改为
    `7.517129032258064`
  - `pre_state_pair_coherent`：`pre_state_ts_ns` 与
    `pre_state_age_ms` 成对自洽平移
  - `side_direction_segment_isomorph`：Jul30 segment_0001 全部 `48683`
    rows 同时翻转 side/direction
  - `shock_decision_segment_time_translation`：同一 segment 全部 `48683`
    rows 同时平移 shock、decision、pre-state、segment bounds 和 window end
  - 五组 source/archived CLI 均 `rc=2`，首个错误均为
    `frozen_candidate_trigger_projection.jul30.sha256: canonical value drift`；
    未被 artifact hash、runtime copy、inter-trigger、sensitivity、ESS 或
    structural summary 抢先拒绝。
- Round 3 P2 的八入口 `32/32` 攻击全部精确 fail closed：
  hostile 根目录
  `/tmp/0815T001-qa4-csvcells-laht1_kr/`。
  - 每个入口均执行 `extra_leading`、`extra_middle`、`extra_trailing`、
    `missing`
  - source/archived CLI 全部 `rc=2`
  - 首个错误全部为对应 artifact 的
    `row cell/schema drift at row 2`
  - 无 artifact/runtime drift 抢先拒绝。
- Row grammar 单独 probe 确认 `"quoted,comma"` 被解析为一个合法 cell，
  blank-but-present value 为 `""` 且通过 cell closure；正式包已有合法空
  `rejection_reason`/binding cells，source/archived verify-only 均通过。
- 正式八个 CSV 当前 row grammar 扫描：
  - input bindings `274` rows
  - density `6`
  - inter-trigger `18`
  - episode summary `3`
  - sensitivity summary `36`
  - ESS `74`
  - candidate membership `463612`
  - sensitivity membership `463612`
  - unnamed/key drift `0`，missing cell `0`
- Round 2 五表攻击 `20/20` 全部重新 fail closed，hostile 根目录
  `/tmp/0815T001-qa4-round2-97ukscrk/`：
  - 每表 duplicate-before/after 均命中
    `duplicate <label> primary-key rows`
  - extra unique 与 missing key 均命中 exact primary-key closure drift。
- ESS 三类 key 重新确认：
  - structural：
    Jul30 `complete_time_block_count_60s`
  - session-total Bartlett：
    Aug03 Family B `session_total`
  - per-segment Bartlett：
    Aug04 Family A `segment_0001`
  - 三组 source/archived CLI 均精确报告缺失 key。
- Round 1 五类攻击全部重新 fail closed，hostile 根目录
  `/tmp/0815T001-qa4-round1-hpga2a9f/`：
  - 删除完整 Aug04：density primary-key closure 缺失 Aug04 A/B
  - omission source/Stage1 rows：frozen provenance inventory contract drift
  - forged recovery/decision categories：frozen episode-merging result drift
  - 非法 evidence taxonomy：`trigger_density.evidence_label drift`
  - Jul30 segment_0005 role：canonical value drift
- Archived verify-only 零写入：
  - before/after 均 `16` files、`19,826,238` bytes
  - QA tuple-list digest 均
    `2dac446fa57cef51bd34b619136bc9da6f11f7d8193cb3659cd6edbe3526ab3a`
  - mismatch `0`；无 `__pycache__`、`.pyc` 或新增路径。
- Formal、Build A、Build B 均为 `16` files、`19,826,238` bytes；
  全部 relative paths、bytes 和逐文件 SHA256 mismatch 为 `0`，core 均为
  `982011eae0851112747ee8b98a9308edf3843c2ac7505105ee2c750951dbda02`。
- 按 `input_bindings.csv` 实际读取并重哈希：
  - source before/after 均 `123` unique files、`169,677,903` bytes、
    inventory SHA256
    `94bad85bfd4981b351f84c53628099468ec27f13d308402ac7125a9d582a6644`
  - accepted Stage 1 before/after 均 `14` unique files、`1,401,387` bytes、
    inventory SHA256
    `c540cc056313716b3bdd2b9c0fe076cda15a7f152b399ae6a1283b3aa8aa6590`
  - missing、size mismatch、SHA mismatch 均为 `0`
  - Stage 1 core 保持
    `9c2756f966a1a05816f9e6a91d7361d76cfea10fc3c587944ca2f0c1d2eb7c96`
- 与 R2 Build A
  `/tmp/0815T001-r2-density-a.5tyH0q/package` 逐文件比较，仅以下 `3`
  个文件变化：
  - `density_manifest.json`
  - `frozen_density_contract.json`
  - `runtime_source/cross_exchange_trigger_density_admission.py`
  其余 `13` 个文件，包括两个 membership gzip 和全部研究 summary，
  逐字节不变。
- Runtime source 五文件与当前 source 逐字节相等；projection fixture
  monkeypatch 仅存在于 pytest fixture，production source constants 只有
  一个 assignment site，未发现 fixture-only production bypass。
- Canonical contract 在 artifact closure 与 runtime byte closure 后被 exact
  consumption；本轮所有定向 projection/CSV 攻击均到达目标 invariant，
  未发现 verify ordering 抢先误拒绝。
- 尽管以上旧缺陷均关闭，新的 candidate-level structural membership
  coherent-rehash 被 source/archived CLI 接受为 `verified=true`。
- Whole-session block permutation 同样被 source/archived CLI 接受，说明
  per-session projection 未冻结全局 session block 顺序。

验收结论：
- 未通过
- 结论说明：
  - Round 3 P1/P2、Round 2 和 Round 1 缺陷均已被独立确认关闭，正式包
    当前 bytes、projection、输入 provenance、构建确定性和范围边界也正确。
  - 但 production verify-only 没有精确验证 `episode_merging_v1` 的
    candidate-level cluster/flow/overlap membership。攻击者可制造 cluster
    与 flow 的父子关系冲突，更新自描述 summaries 和全部 hashes 后仍获
    `verified=true`。该 P1 破坏 Stage 2 primary structural unit，当前包
    不能作为 Stage 3 门禁。

通过项：
1. Candidate authoritative projection 的 fields/count/SHA anchors 正确，
   五组 production-sized coherent variants 均由 source/archived 精确
   fail closed。
2. 八个 CSV 入口 `32/32` extra/missing cell 攻击全部关闭；quoted comma
   和 blank-present 处理正确。
3. Round 2 五表 exact key/cardinality 与 ESS 三类 key 全部关闭。
4. Round 1 session/provenance/category/taxonomy/segment-role 缺陷继续关闭。
5. Tests、Ruff、compileall、CLI help、source/archived verify-only、
   zero-write、formal/A/B 和 `git diff --check` 全部通过。
6. Source `123` files 和 accepted Stage 1 `14` files 实际重哈希完全匹配，
   相对 R2 仅 manifest/contract/archived verifier 改变。
7. 当前正式包没有 Aug07 event rows、response/outcome/model/PnL/
   actionability/private/account/order/cancel 数据或执行。

不通过项：
1. P1：candidate-level `episode_merging_v1` structural membership
   verification fail-open。
2. P2：全局 session block canonical order 未冻结，完整 session permutation
   可被 source/archived verifier 接受。

缺陷清单：
1. P0：无。
2. P1：candidate-level cluster/flow/overlap exact structural membership
   未被重放或外部锚定。
   - 文件/行号：
     `examples/hyperliquid/cross_exchange_trigger_density_admission.py:2264-2285`
     只检查 ID prefix，并按 published ID 聚合；
     `:2427-2505` 只冻结 group counts、quantiles 和 aggregate category maps；
     `:3099-3104` 的 projection digest 不包含 `cluster_id`、
     `continuous_flow_episode_id` 或 `overlap_block_id`。
     正确 producer mapping 位于
     `examples/hyperliquid/cross_exchange_candidate_episode_merging.py:507-522`，
     但 verify-only 未重放该 mapping，也未验证 cluster-to-flow parent、
     `<=100ms` cluster 连续性、recovery bridge 或 overlap connectivity。
   - 复现：
     `/tmp/0815T001-qa4-structural-membership-fw6utvor/
     swap-two-cluster-memberships/package`。
   - 变体：
     交换 `jul30:segment_0001:1` 与
     `jul30:segment_0001:6` 的 `cluster_id`，authoritative projection
     fields 全部不变；同步更新 cluster quantiles、
     sensitivity selected-cluster counts、artifact/core hashes。
   - 独立结构检查：
     正式包 `clusters_with_multiple_flows=0`；hostile 包产生
     `segment_0001-0-C000004` 同时包含
     `E000001` 和 `E000002`，`clusters_with_multiple_flows=1`。
   - 实际结果：
     source/archived CLI 均 `rc=0`、`verified=true`，hostile core
     `28983dd28dbe72881a64d0018cd20b8dcff02d9f82997bcdda549f87393bc7e7`。
   - 影响：
     下游有效支持、first-flow sensitivity、flow-block resampling 和 later
     Episode v3 可消费错误的 primary structural membership；aggregate
     counts 正确不能证明每个 candidate 被分配到正确 cluster/flow/block。
3. P2：whole-session block order 未纳入 canonical projection。
   - 文件/行号：
     `examples/hyperliquid/cross_exchange_trigger_density_admission.py:564-624`
     为每个 session 建立独立 hasher；
     `:3021-3047` 只拒绝已关闭 session 再次出现，不要求 frozen
     `jul30 -> aug03 -> aug04` 顺序。
   - 复现：
     `/tmp/0815T001-qa4-session-order-ypk7fa74/
     swap-session-blocks/package`。
   - 变体：
     同步把两个 membership gzip 的完整 block 改为
     `aug03 -> jul30 -> aug04`，每 session 内 row projection 完全不变，
     并刷新全部 hashes。
   - 实际结果：
     source/archived CLI 均 `rc=0`、`verified=true`，hostile core
     `6fd550a6fe09cb48d8a88c6506a4a7525eb6f0ef2d96bcc45aa70f15543e39a9`。
   - 影响：
     verifier 可为非 canonical row sequence 背书；显式按 session_id
     分组的消费方数值不变，因此严重度低于 structural membership P1，
     但流式或顺序敏感消费不再具有 frozen package ordering 保证。
4. P3：无。

阻塞项：
- 无。缺陷可在 0815T001 范围内 bounded repair。

边界结论：
- 两个 membership schemas 的 forbidden fields 均为 `0`。
- `246` 条 source binding rows 中 Aug07/outcome-like forbidden paths 为
  `0`。
- Manifest 七项 boundary flags 全部为 `false`。
- 本轮未读取 Aug07 raw/R0/R1/basis event rows，未运行 response、
  outcome、model、score、PnL 或 actionability，未访问 private/account/
  order/cancel endpoint。
- Stage 3 detector parity、Episode v3、outcome/model/actionability 和
  Aug07 event-row first-read 继续锁定。

建议总控下一步：
1. 保持 `0815T001` 为 `未通过`，不得把当前 Stage 2 包作为后续正式门禁。
2. 对 candidate-level `cluster_id`、`continuous_flow_episode_id`、
   `overlap_block_id` 和 `window_end_ts_ns` 增加外部 canonical digest，
   或在 verify-only 中从 authoritative candidate projection、segment
   bounds 和 frozen recovery evidence 独立重放 `episode_merging_v1`。
3. 至少精确验证 cluster 连续 gap、cluster-to-flow 单一父关系、flow bridge
   decision、overlap connectivity 和每 candidate exact structural IDs；
   增加单行交换/迁移后 coherent-rehash 的 production regression。
4. 冻结并验证 membership session block order，或在 contract 中明确声明
   sessions 为无序分区并让所有消费 API canonicalize 后再使用。
5. bounded repair 后由新的独立 QA 线程重新验收，不自动沿用本轮结论。

提交信息：
- commit：无
