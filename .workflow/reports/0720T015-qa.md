# QA 验收结果

执行线程：
- 独立 QA验收线程

任务ID：
- 0720T015

状态：
- 已通过

更新时间：
- 2026-07-20 09:48 CST

验收对象：
- implementation commit `35ceea2a5ca861a9329e384a2faeca11d9615cf7`
- business report commit `2ecb2f604c926839e0e88013be47c33927f88b64`
- 实现范围：exact-envelope duration authorization/acceptance contract
- 实现审查范围：`35ceea2^..35ceea2`
- 完整审查范围：`35ceea2^..2ecb2f6`

## Findings

1. 未发现 P0、P1 或 P2 缺陷。
2. Orchestrator exact cap 已从 `900s` 调整为 `1800s`，同时保留 single window、现有风险上限、submission budget、profile、manager、fast-L2 和 private-proof 要求。
3. Acceptance 使用外部 `expected_window_seconds` 授权；artifact、preflight 或 watcher 自报 duration 均不能替代外部值。
4. 实现提交未改变 strategy formula、signal freshness、edge threshold、risk cap、activation、manager lifecycle 或 evidence identity。

验收范围：
- Orchestrator exact two-sided-manager preflight。
- Acceptance external expected duration、exact joins 与 standing cap。
- 正反 acceptance fixtures。
- Exact T011 legacy replay。
- Focused、full suite、compile、CLI 和 Git 完整性检查。
- 全程只读；生成型输出仅写入 `/tmp`。

实际结果：
- `cross_exchange_live_remote_orchestrator.py:55` 将 exact maximum 设为 `1800.0`。
- `validate_args()` 仍要求：
  - `windows=1`
  - `0.005 BTC/order`
  - `1 USDC max loss`
  - `0.01 BTC max position`
  - `2 submissions`
  - `quote hold=3s`
  - `wait=10s`
  - `live_open_orders`
  - fast L2、edge-gate mode、two-sided manager、requote attempts `2`
- Acceptance external expected validation要求 finite、positive、`<=1800`；默认 `900` 仅用于历史兼容。
- Preflight duration exact-match external expected；runtime argv exact-match完整 preflight command，并独立受 `1800s` cap。
- Manifest 持久化 `expected_window_seconds`；CLI 暴露 `--expected-window-seconds`。

独立 no-start preflight：
- `1800s`：exit `0`、status `pass`、single window，preflight/runtime duration 为 `1800.0/1800.0`。
- watcher、credential、private、account、order、cancel activity 全为 `false`。
- dynamic spread、fill feedback、inventory skew、multi-level、actual quote behavior 全为 `false`。
- 未生成 run root、watcher/window/private output。
- `1800.001`、`NaN`、`Inf`：均以 `exact_envelope_mismatch:window_seconds` fail closed。
- `0`：以 `window_seconds_must_be_positive` fail closed。
- 四个失败 case 均未生成 preflight、run root 或 window output。

独立 acceptance fixtures：
- artifact `1800` / external `1800`：通过，config `72/72` pass，manifest expected 为 `1800.0`。
- artifact `900` / external `1800`：blocked，config `71 pass / 1 fail`。
- artifact `1800` / external `900`：blocked，config `71 pass / 1 fail`。
- artifact/external `1800.001`：blocked，config `70 pass / 2 fail`。
- external `0`、`NaN`、`Inf`：均 blocked。
- 精确 config checks/reasons：
  - `expected_window_seconds_within_standing_cap`: externally supplied duration is positive and no longer than 1800 seconds
  - `preflight_window_seconds`: preflight duration equals the externally authorized task duration
  - `watcher_seconds_matches_preflight`: runtime duration equals the preflight envelope
  - `watcher_seconds_within_cap`: runtime duration is positive and no longer than 1800 seconds

Exact T011 legacy replay：
- 显式 `--expected-window-seconds 900` 和 legacy bridge。
- exit `2`，bridge authorized，final recommendation blocked。
- provenance `112/112` pass。
- config `71 pass / 1 fail`。
- decision `16 pass / 27 fail`。
- lifecycle `34 pass / 27 fail`。
- `validation_reasons=[]`。
- 输入树 replay 前后均为 `121` 个文件，SHA-256 均为 `82b90e19072225a28d6c73ffaede3843e3e8803a79a10604f63f7b777df96bbf`，byte-exact 未变化。

命令与结果：
- Focused orchestrator + acceptance：`171 passed in 8.02s`。
- `python -m pytest -q -p no:cacheprovider examples/hyperliquid`：`712 passed in 40.04s`。
- 4 个修改 Python 文件 `py_compile`：pass，字节码定向至 `/tmp`。
- Orchestrator 与 acceptance `--help`：pass。
- Implementation/business `git show --check`：pass。
- Implementation diff、implementation-to-business range 和当前 worktree `git diff --check`：pass。

安全与工作树：
- 未执行 live、private、account、order、cancel、network、remote 或 service 行为。
- Acceptance manifests 的 offline/network/remote/private/order/cancel boundary 全部符合离线要求。
- 验收前后分支均为 `cross-exchange`，HEAD 均为 `2ecb2f604c926839e0e88013be47c33927f88b64`。
- 既有 1 个已修改文件和 10 个未跟踪文件状态未变化；QA 未编辑、提交、清理、stash 或恢复文件。
- 仓库内未产生新的 `.pytest_cache` 或 `__pycache__`。

验收结论：
- 已通过
- 结论说明：
  - Exact orchestrator 与 acceptance 已统一到 externally bound duration contract 和 standing `1800s` maximum；artifact 无法自授权不同 duration，历史 T011 `900s` replay 保持确定性 blocked。

通过项：
1. Orchestrator exact `1800s` cap 与 no-start fail-closed boundary。
2. Acceptance external expected、preflight/runtime exact joins 和 manifest persistence。
3. `1800s` 正例、双向 mismatch、over-cap 和 non-finite 反例。
4. Exact T011 legacy replay。
5. Focused/full/compile/CLI/Git 完整性检查。

不通过项：
- 无

缺陷清单：
- 无

阻塞项：
- 无

剩余风险：
- 本结论仅接受离线 duration contract，不构成 live lifecycle、策略收益、multi-level 或 adaptive controller 验收。
- 未来 `1800s` 任务必须显式传入 `--expected-window-seconds 1800`；省略时兼容默认仍为 `900s`。

建议总控下一步：
1. 将 `0720T015` 更新为 `已通过`。
2. 仅可按下一 formal task 的 exact envelope 和既有 QA gate 决定是否派发新的单窗 live 任务。

提交信息：
- implementation commit：`35ceea2a5ca861a9329e384a2faeca11d9615cf7`
- business report commit：`2ecb2f604c926839e0e88013be47c33927f88b64`
- QA commit：无
- QA 文件修改：无
