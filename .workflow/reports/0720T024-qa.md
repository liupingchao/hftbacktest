# QA 验收结果

执行线程：
- 独立只读 QA 验收线程

任务ID：
- 0720T024

状态：
- 未通过

更新时间：
- 2026-07-20 18:22 CST

验收线程：
- QA验收线程

验收对象：
- Implementation commit：`4b9d98452168f082599c033e41e461a8538ab467`
- Business/report commit：`9aafea412a6058ecbce14cb2e45b0fdc47043e55`
- 业务执行线程 / 0720T024
- Branch：`cross-exchange`

## Findings

1. [P1] 不完整或 malformed foreign historical row 仍可被忽略。
   - Manager 在 `hyperliquid_maker_order_manager.py:421-469` 中，只要现有 identity 没有匹配 expected identity，就返回 `foreign`；没有要求 foreign row 同时提供 oid、cloid 且两者都与 expected reference 不同。
   - Producer 在 `hyperliquid_tiny_live_m2_fill_window.py:2268-2300`、独立 acceptance 在 `cross_exchange_t024_same_window_acceptance.py:864-896` 中存在相同逻辑。
   - 主动 probe 使用 clean exact row 加 `{"oid": 999}` 或 `{"cloid": "foreign"}` 的缺失 paired identity row，三层均将该 row 分类为 `foreign`，最终接受 `cancel_confirmed`。
   - 使用完整 foreign identity 但 `status=""` 的 row，三层同样分类为 `foreign` 并接受旁边的 clean exact row。
   - 这违反“只能忽略 well-formed fully-disjoint foreign row”以及“row cannot be proven fully disjoint 时 fail closed”的任务合同。

验收范围：
- Historical row 全量分类及唯一 exact selection。
- Raw/redacted alias、token、marker 和 canonical OID 安全。
- T023 bounded terminal recovery 语义回归。
- T016/T022 immutable offline replay。
- 提交祖先、文件范围和只读边界。

验收步骤：
1. 阅读 QA 模板、任务文件、业务报告、T023 QA 及两个固定 commit 的 diff/ancestry。
2. 静态审查 manager、producer、独立 acceptance 和 redactor。
3. 运行 focused/full tests、语法、CLI、Git checks 和 T023 定向回归。
4. 主动构造 partial foreign、malformed status、OID、redaction schema hostile probes。
5. 使用新 `/tmp` 目录重放 T016/T022，核对输入、标题、计数和离线边界。

实际结果：
- Implementation 是 report commit 的直接祖先。
- Implementation commit 恰好包含任务声明的 8 个 implementation/test 文件。
- Report commit 只包含 task、business report 和三个 tracker 文件。
- Focused tests：`577 passed in 18.94s`。
- Full Hyperliquid tests：`917 passed in 39.24s`。
- T023 重点语义回归：`11 passed in 0.36s`。
- 8 个修改文件 `py_compile`、acceptance `--help`、两个 commit 的 `git show --check` 和提交区间 `git diff --check` 均通过。
- Unicode digit、5000 位数字、leading zero 和 uint64 overflow 在四条 OID 路径均安全拒绝且未抛未捕获异常；uint64 最大值的 int/string 形式一致接受。
- Redactor per-alias map 和 aggregate token 一致，无 raw oid/cloid 泄漏，double-redaction 幂等。
- 伪 marker、coverage 缺失、非 bool marker、aggregate-token-only、token/alias conflict 在 producer 和 acceptance 均返回 `unknown`。
- T016 replay exit `2`：decision `43/43`、lifecycle `49 pass / 12 fail`，标题 `0720T016`。
- T022 replay exit `2`：decision `43/43`、lifecycle `49 pass / 12 fail`，标题 `0720T022`。
- 两组 replay 均为 `offline_only=true`；network/private/order/cancel/remote/new-live 全为 false。
- 两个输入 root 各 `118` 个文件，重放前后逐 path/file-SHA 映射一致，未改写输入。
- 初始与最终工作树状态一致；未修改 unrelated dirty/untracked 文件。
- 全程未执行 live/private/account/order/cancel/network/remote/service 操作。

验收结论：
- 未通过
- 结论说明：
  - 大部分 redaction、OID、bounded recovery 和 replay 合同成立，但核心 historical classification 仍会忽略无法证明 fully-disjoint 的 row，不满足 T024 fail-closed 验收标准。

通过项：
1. Same oid/different cloid、same cloid/different oid、共享 identity 缺失 paired identity、alias conflict 和非字符串 malformed status 均拒绝。
2. 完整、合法且 oid/cloid 均不同的 foreign row 可与唯一 clean exact row 共存。
3. Redaction schema、canonical OID、T023 bounded recovery、SDK/title 和 replay 未回归。

不通过项：
1. 仅包含不同 oid 或仅包含不同 cloid 的不完整 foreign row 可被忽略。
2. 空字符串 historical status 可作为 foreign row 被忽略。

缺陷清单：
1. [P1] Manager、producer、acceptance 未要求 foreign row 完整覆盖 expected identity kinds，也未充分验证 historical status 的 well-formedness。

阻塞项：
- 无 QA 判断阻塞；现有证据足以裁决未通过。
- Private historical recovery、新 bounded live、Task 8 和 adaptive/multi-level activation应继续锁定。

建议总控下一步：
1. 只有 row 同时提供全部 expected identity kinds、每个 identity 合法且全部不同，才能分类为 `foreign`。
2. 缺失任一 expected identity 的非 exact row 应分类为 malformed/indeterminate；historical status 应要求非空且符合明确合法状态合同。
3. 在 manager、producer、acceptance 三层增加 partial-foreign 和 empty/invalid-string-status 回归，再执行独立离线 QA。

提交信息：
- commit：无；本次验收未编辑、暂存、提交或清理任何文件。
