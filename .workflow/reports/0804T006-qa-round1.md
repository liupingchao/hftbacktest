# QA 验收回报（第一轮）

任务ID：
- 0804T006

状态：
- 未通过

结论：
- 方案的数据集盘点和总体研究方向成立，但确认性方法存在 outcome
  leakage、共识匹配循环论证和 session-level 外推过度三个关键问题。

必须修复：
- Jul30 现有 prototype-v2 schema 包含 `response_observed__*` 和
  `response_residual__*`，不得作为确认性 family assignment 特征。
- consensus matching 不得使用事后 response curve 或事后 basis path，
  再用同一结果验收 response/basis。
- 三次 session 是三个独立观察单位；分钟 block bootstrap 只能估计
  session 内不确定性，不能把三次 session 扩大成可外推总体的样本。
- Aug03 当前通过的 R1 v3 应绑定
  `skhynix_cross_exchange_research_0804T001_old5h_replay/alignment/`。
- Binance lead 只能表述为 local receipt-time precedence association，
  必须加入网络路径时延不对称、共同因子和时间偏移敏感性边界。
- C1/C2/C3 需要明确指标、零假设、效应门槛、alpha、失败条件。
- 所有 negative control 必须重跑完整的构建、匹配、筛选和检验流程。

验收动作：
- 修复后重新进行独立 QA。
