# QA 验收结果

执行线程：
- QA验收线程（第五轮全新独立验收）

任务ID：
- 0815T001

状态：
- 未通过

更新时间：
- 2026-08-15 16:46 CST（星期六）

验收线程：
- QA验收线程

验收对象：
- 业务线程-python/research + 0815T001 Round 4 bounded repair
- 正式包：
  `local_live_analysis/skhynix_trigger_aligned_episode_research_v1_stage02_density/`
- 预期 core SHA256：
  `536229d7a08db6048887cac29093ff137856424655563b578b361f6b37ac1a6f`
- Build A：
  `/tmp/0815T001-r4-density-a.Ok1hG1/package`
- Build B：
  `/tmp/0815T001-r4-density-b.5XtMXE/package`

验收范围：
- 独立重算 `candidate_membership_projection_v2` 的 version、完整 fields、
  session row count/SHA、framing 和全局 session order。
- 对 cluster/flow/overlap/window、cluster-flow parent、overlap grouping、
  session permutation/contiguity 和 candidate/sensitivity coupling 执行
  production-sized coherent-rehash。
- 使用当前 source/archived runtime 复验 Round 1-3 的全部 `65` 个历史
  production-sized fixture。
- 运行 focused/admission/recovery tests、Ruff、compileall、CLI help、
  source/archived verify-only、zero-write、formal/A/B、source/Stage 1
  实际重哈希、上一版差异和禁止边界检查。
- 普通 code review 重点覆盖 projection framing/order、candidate 与
  sensitivity 耦合、small summary exact semantics、manifest/report/input
  binding 外部锚、fixture-only path、`zip_longest` 和 verify 顺序。

验收步骤：
1. 阅读 workflow manual/templates、任务文件、Round 1-4 QA、business-r4、
   主实施方案及 controller tracking，不沿用业务结论。
2. 使用独立 stdlib CSV/gzip/JSON/SHA 脚本重算 v2 projection，并通过 AST
   分别读取 source 与 archived constants，再对 canonical contract。
3. 从正式包创建全新的 `/tmp` 副本，修改 payload 后同步刷新 artifact
   records/core hash，由当前 source 和当前 archived verifier 分别验证。
4. 将 Round 1-3 的旧 fixture currentize 为当前 runtime/contract，避免旧
   verifier、runtime drift 或 stale contract 抢先拒绝。
5. 独立比较 formal/A/B 全文件 tuple，并实际读取 `input_bindings.csv`
   所列 `123` 个 source 与 `14` 个 accepted Stage 1 文件重新计算 SHA。
6. 继续构造 unknown binding phase/scope、false report、small float drift
   和 manifest extra-key 攻击，作为不自动通过的普通 code review。

运行命令与结果：
1. `python -B -m pytest -q
   examples/hyperliquid/test_cross_exchange_trigger_density_core.py
   examples/hyperliquid/test_cross_exchange_candidate_episode_merging.py
   examples/hyperliquid/test_cross_exchange_trigger_density_inputs.py
   examples/hyperliquid/test_cross_exchange_trigger_density_admission.py`
   - `134 passed in 8.26s`
2. `python -B -m pytest -q
   examples/hyperliquid/test_cross_exchange_trigger_density_admission.py`
   - `83 passed in 2.09s`
3. `python -B -m pytest -q
   examples/hyperliquid/test_cross_exchange_liquidity_response_case_hierarchy.py
   -k 'merges_dense_clusters_without_recovery or
   stops_at_recovery_checkpoint or
   fails_closed_on_missing_recovery_evidence'`
   - `3 passed, 19 deselected in 0.04s`
4. `python -m ruff check` 对四个 Stage 2 实现和四个测试文件。
   - `All checks passed!`
5. 外置
   `PYTHONPYCACHEPREFIX=/tmp/0815T001-qa5-pycache.m0ly6d`
   运行 `python -m compileall -q`。
   - `compileall_rc=0`
6. Source/archived `--help` 各 `26` 行，`cmp=0`；
   `git diff --check` 返回 `0`。
7. Source/archived 正式包 verify-only 均返回 `rc=0`、`verified=true`、
   `artifact_count=15`、`candidate_membership_rows=463612`、
   `confirmed_rows=267554`、`sensitivity_membership_rows=463612`、
   `session_count=3` 和预期 core SHA。
8. Source CLI 对 Build A/B 的 `--compare-to` 均返回
   `identical=true`。
9. Production fixture 命令形态：
   `python -B <current-source-or-archived-cli> --verify-only
   --output-dir <fixture>/package`。每个副本均先同步刷新包内 artifact
   records 和 core hash。

实际结果：
- v2 projection version 为
  `candidate_membership_projection_v2`；完整 `19` fields 为：
  `session_id,candidate_id,primary_episode,rejection_reason,aggressor_side,
  direction_sign,shock_ts_ns,decision_ts_ns,impact_ratio,pre_state_ts_ns,
  pre_state_age_ms,segment_id,connection_epoch_id,segment_start_ts_ns,
  segment_end_ts_ns,cluster_id,continuous_flow_episode_id,overlap_block_id,
  window_end_ts_ns`。
- Projection fields 精确等于 `CANDIDATE_MEMBERSHIP_FIELDS`。Canonical
  framing 为 version 行、JSON fields 数组行、逐 row JSON value array
  加换行；未发现 JSON escaping 或行拼接歧义。
- 独立重算 session order 为 `jul30 -> aug03 -> aug04`，anchors 为：
  - Jul30：`268522` rows，
    `cd731b5b011183366fd3cb04e0809d082a26bde85d7f1446ff4754016c3551f9`
  - Aug03：`127622` rows，
    `04cc9a13c85e530291bcf3c584e78ea029ed67fe1653bcdd7efa13e14294fda0`
  - Aug04：`67468` rows，
    `bf5911989a07f9b1242b9eef12e72a40093549f98c516c846df6caae353db820`
- 上述 version、fields、order、counts 和 SHA 与 source constants、
  archived constants、`frozen_density_contract.json` 三方完全一致；
  source/archived verifier bytes 相等，SHA256 为
  `004dea18a1f175ef8923a5b947501286c6849e313587c9cd7eda36b6af7ef662`。
- Round 5 structural/order fixture 根目录：
  `/tmp/0815T001-qa5-final-negative-62_8icim/`。
  - Round4 指定候选 `jul30:segment_0001:1` 与
    `jul30:segment_0001:6` 交换 cluster membership，并刷新 cluster
    quantiles/sensitivity cardinalities；同时形成 cluster-flow parent
    不一致。两个 verifier 均命中 Jul30 v2 projection SHA drift。
  - Cluster、flow、overlap 和 window 各自的单行/多行合法格式重标共
    `8` 组，另有 overlap grouping 不一致 `1` 组；source/archived
    `18/18` 次均由对应 session 的 v2 projection SHA identity 拒绝。
  - 两个 gzip 完整 session block 置换均命中
    `frozen membership session order drift`。
  - 两个 gzip 在完整 Jul30/Aug03 后再次出现 Jul30 fragment，均命中
    `membership sessions are reordered`。
  - Candidate gzip 与 sensitivity gzip block order 不一致，均命中
    `candidate/sensitivity session drift`。
- Round 1-3 currentized fixture 根目录：
  `/tmp/0815T001-qa5-historical-current-h0myr2cj/`。
  - Round3 projection `5/5`：source/archived `10/10` 均命中 v2
    projection SHA drift。
  - Round3 CSV row-width `32/32`：source/archived `64/64` 均命中对应
    artifact `row cell/schema drift at row 2`。
  - Round2 small CSV/key/cardinality 与 ESS `23/23`：
    source/archived `46/46` 均命中 duplicate 或 exact primary-key
    closure drift；三类 ESS key 分别覆盖 structural、session-total
    Bartlett 和 per-segment Bartlett。
  - Round1 `5/5`：source/archived `10/10` 分别命中完整 session 缺失、
    provenance inventory drift、episode-merging category drift、
    evidence taxonomy drift和 Jul30 segment role canonical drift。
  - 合计 `65` fixtures、`130/130` 当前 verifier 调用 fail closed，
    source/archived 首个错误完全一致，无 unexpected pass。
- Archived verify-only zero-write：
  before/after 均 `16` files、`19,827,566` bytes，独立 QA JSON tuple-list
  digest 均为
  `e54fb8da71d6bb8c32bad207d9d4bd7cc13f46999df67fdd1f2818ab0482f36a`；
  mismatch `0`，没有 `__pycache__`、`.pyc` 或新增路径。
- Formal、Build A、Build B 均为 `16` files、`19,827,566` bytes；
  relative path、bytes 和逐文件 SHA mismatch 为 `0`，core 均为
  `536229d7a08db6048887cac29093ff137856424655563b578b361f6b37ac1a6f`。
- 按 binding rows 实际重哈希：
  - source before/after 均 `123` unique files、`169,677,903` bytes、
    SHA256
    `94bad85bfd4981b351f84c53628099468ec27f13d308402ac7125a9d582a6644`
  - accepted Stage 1 before/after 均 `14` unique files、
    `1,401,387` bytes、SHA256
    `c540cc056313716b3bdd2b9c0fe076cda15a7f152b399ae6a1283b3aa8aa6590`
  - missing、size mismatch、SHA mismatch 均为 `0`
  - Stage 1 core 为
    `9c2756f966a1a05816f9e6a91d7361d76cfea10fc3c587944ca2f0c1d2eb7c96`
- 与上一版
  `/tmp/0815T001-r3-density-a.kKplZL/package` 全文件比较，仅 `3` 个文件
  变化：`density_manifest.json`、`frozen_density_contract.json`、
  `runtime_source/cross_exchange_trigger_density_admission.py`。
- Fixture monkeypatch 仅位于 pytest 生命周期；production projection/order
  constants 各只有一个 assignment site。`zip_longest` 对 row-count 和
  candidate/sensitivity coupling 的 verify 顺序符合预期。
- 当前正式包两个 membership schema 的 forbidden fields 为 `0`；
  `246` 条正式 source binding rows 中 Aug07/outcome-like path 为 `0`；
  manifest 七项 boundary flags 全部为 `false`。
- 尽管上述修复和正式 bytes 正确，四个新的 coherent-rehash 变体被
  source/archived verifier 同时接受：
  - `/tmp/0815T001-qa5-final-negative-62_8icim/
    ignored-input-binding-unknown-phase-scope/package`
  - `/tmp/0815T001-qa5-final-negative-62_8icim/
    report-false-count/package`
  - `/tmp/0815T001-qa5-final-negative-62_8icim/
    small-summary-float-tolerance/package`
  - `/tmp/0815T001-qa5-final-negative-62_8icim/
    manifest-extra-key/package`

验收结论：
- 未通过
- 结论说明：
  - Round 4 structural projection/session-order 修复和 Round 1-3 历史修复
    均已独立确认关闭；正式包当前数据、构建确定性、输入实际 SHA 和禁止
    边界也正确。
  - 但 input binding verifier 会忽略未知 phase/scope 行，导致携带
    Aug07/outcome path 的合法宽度 binding row 可进入 `verified=true`
    package。另有 report、manifest 和 small-summary exact semantics
    闭包不足。存在新的 coherent-rehash verifier fail-open，不能接受为
    Stage 3 门禁。

通过项：
1. `candidate_membership_projection_v2` 完整 19 字段、三 session anchors
   和 frozen session order 正确，所有 structural/order 攻击 fail closed。
2. Round 1-3 共 `65` 个 production-sized fixture 在当前 source/archived
   verifier 下全部按目标 invariant fail closed。
3. Tests、Ruff、compileall、CLI help、formal verify-only、zero-write、
   formal/A/B、实际 source/Stage1 重哈希和上一版差异检查全部通过。
4. Candidate/sensitivity identity、session contiguity、fixture-only path、
   projection framing 和 `zip_longest` 未发现新的绕过。
5. 当前正式包本身未包含 Aug07 event rows、response/outcome/model/PnL/
   actionability 或 private/account/order/cancel 数据或执行。

不通过项：
1. P1：`input_bindings.csv` unknown phase/scope 行逃逸 inventory 与禁止路径
   验证。
2. P2：admission report 只做短语检查，可发布与已验证表格矛盾的数值。
3. P2：density manifest 没有 exact key-set closure，且额外键不进入 core
   identity。
4. P3：small CSV float 比较使用非零容差，不能保证 deterministic package
   的精确数值语义。

缺陷清单：
1. P0：无。
2. P1：input binding phase/scope closure 与全行 forbidden-path policy
   fail-open。
   - 文件/行号：
     `examples/hyperliquid/cross_exchange_trigger_density_admission.py:
     1858-1889` 仅选择四个已知 phase/scope 组合；
     `:1892-1907` 只对 `binding_scope=source_inputs` 检查禁止路径；
     `:3142-3184` 未要求每一行属于允许 enum，也未验证全表 exact row/key
     closure。
   - 复现：
     `ignored-input-binding-unknown-phase-scope/package` 追加合法 `10` 列行：
     `snapshot_phase=unknown_phase`、`binding_scope=unknown_scope`、
     `path=/tmp/0807/outcome_rows.csv.gz`、
     `role=forged_outcome_binding`、`session_id=aug07`，同步重哈希。
   - 结果：
     source/archived 均 `rc=0`、`verified=true`，core
     `53bfabf13ce26a7d48b216573d73fd9f02fccc85d0703b02ee4090e49ea52167`。
   - 影响：
     verified provenance artifact 可携带未锚定、未重哈希且明确越过 Stage 2
     边界的 Aug07/outcome binding；遍历全表的下游消费者与 verifier 对
     binding universe 的理解不一致。
   - 修复建议：
     冻结 `snapshot_phase={before,after}`、
     `binding_scope={source_inputs,accepted_stage1_package}`；要求每行恰好
     属于一个允许组合，全表 row/key cardinality 精确闭合，并在任何筛选前
     对所有 path 执行 forbidden policy。
3. P2：required admission report 未与 verified artifacts 精确绑定。
   - 文件/行号：
     `examples/hyperliquid/cross_exchange_trigger_density_admission.py:
     3254-3266` 只检查六个短语。
   - 复现：
     `report-false-count/package` 把 report Jul30 Family A count 从
     `268522` 改为 `1`，保留短语并同步重哈希。
   - 结果：
     source/archived 均 `rc=0`、`verified=true`，core
     `d890f08a9d63d7f0ffa7aedc7df5c427cdaf79bea370b930510e02ca4020b83b`。
   - 影响：
     正式 human-facing admission report 可与已验证 CSV/manifest 矛盾，
     但 verifier 仍为整个 package 背书。
   - 修复建议：
     从已验证 rows 重新生成 canonical report 并逐字比较，或结构化解析并
     精确比较所有表格数值与结论枚举。
4. P2：density manifest exact schema/key-set closure 缺失。
   - 文件/行号：
     `examples/hyperliquid/cross_exchange_trigger_density_admission.py:
     1522-1527` 从 core artifact records 排除 manifest；
     `:1922-1935` 和 `:3222-3253` 只读取已知键，没有拒绝未知键。
   - 复现：
     `manifest-extra-key/package` 只追加
     `"qa_status": "forged_passed"`，不刷新任何 artifact 或 core hash。
   - 结果：
     source/archived 均 `rc=0`、`verified=true`，并继续返回原 core
     `536229d7a08db6048887cac29093ff137856424655563b578b361f6b37ac1a6f`。
   - 影响：
     同一 core identity 可对应带有任意未验证 manifest metadata 的不同
     文件内容；下游若消费未知键，package identity 与语义不再唯一。
   - 修复建议：
     对 manifest 实施 exact top-level/nested key-set schema；未知键一律
     fail closed。需要扩展时升级 schema version。
5. P3：small-summary float exact semantics 使用宽容比较。
   - 文件/行号：
     `examples/hyperliquid/cross_exchange_trigger_density_admission.py:
     1938-1960` 使用 `math.isclose(rel_tol=1e-12, abs_tol=1e-9)`。
   - 复现：
     `small-summary-float-tolerance/package` 把首行
     `window_union_coverage_fraction` 从
     `0.9998232314807587` 改为 `0.9998232319807587` 并同步重哈希。
   - 结果：
     source/archived 均 `rc=0`、`verified=true`，core
     `6d984b6618d161aa8f0343b75eedbef21f137d3806d31bbfd60d86721f3a76e1`。
   - 影响：
     deterministic frozen summary 可存在多个不同数值文本和 core，
     verifier 仍认为语义相同；影响幅度受容差限制，故定级 P3。
   - 修复建议：
     对同一 Python/runtime 生成的 frozen CSV 使用 canonical numeric text
     或精确解析值比较；如业务确需容差，应将容差写入 contract 并明确该表
     不是 exact-identity surface。

阻塞项：
- 无。四个缺陷均可在 0815T001 范围内 bounded repair。

边界结论：
- 当前 formal package 自身 boundary 正向检查通过；P1 证明的是 verifier
  可接受越界 binding row，不代表当前正式构建实际读取了 Aug07。
- 本轮未读取 Aug07 raw/R0/R1/basis event rows，未运行 response、outcome、
  model、score、PnL 或 actionability，未访问 private/account/order/cancel。
- Stage 3 detector parity、Episode v3、outcome/model/actionability 和
  Aug07 event-row first-read 继续锁定。

建议总控下一步：
1. 保持 `0815T001` 为 `未通过`，不得把当前 Stage 2 package/verifier 作为
   后续正式门禁。
2. 优先修复 input binding enum/full-row closure 和 all-row forbidden
   policy；补充 unknown phase、unknown scope、合法宽度 extra row 及
   Aug07/outcome path 的 production regression。
3. 将 report 变为 canonical derived artifact，对 manifest 实施 exact
   schema key closure；为 small-summary 数值明确 exact 或 tolerance
   contract。
4. bounded repair 后由新的独立 QA 线程重新验收，不自动沿用本轮结论。

提交信息：
- commit：无
