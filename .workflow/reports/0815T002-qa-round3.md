# QA 验收结果

执行线程：
- QA验收线程（第三轮全新独立验收）

任务ID：
- 0815T002

状态：
- 已通过

更新时间：
- 2026-08-15 21:22 CST（星期六）

验收线程：
- QA验收线程

验收对象：
- 业务线程-python/research + `0815T002` 第二次 bounded repair
- 当前 source/tests：
  - `examples/hyperliquid/cross_exchange_liquidity_response_trigger.py`
  - `examples/hyperliquid/cross_exchange_liquidity_response_episodes.py`
  - `examples/hyperliquid/cross_exchange_trigger_parity_admission.py`
  - 三个对应 Stage 3 focused test files
- 正式 Stage 3 package：
  `local_live_analysis/skhynix_trigger_aligned_episode_research_v1_stage03_detector_parity/`
- QA fresh raw-input replay：
  `/tmp/0815T002-qa-r3.CjW0pb/replay/package`
- QA hostile evidence root：
  `/tmp/0815T002-qa-r3.CjW0pb/`

验收范围：
- 独立攻击第二轮 QA 剩余的四个 dependency claims 和 canonical trigger
  contract 问题。
- 独立复验首轮六项 admission repair，无条件继承项为零。
- 独立检查 shared detector 纯度、historical builder integration、全部
  candidate/rejected parity、fixture byte identity、package determinism、
  accepted dependency/input immutability 和 hard boundary。
- 从固定 Jul30/Aug03/Aug04 detector/pre-state inputs 建立 fresh replay。
- 运行最小但充分的 Stage 3、alignment/density、recovery、Ruff、
  compileall、gzip、zero-write 和 diff checks。
- 除本报告及其 mirror 外，不修改 task、controller tracking、业务代码、
  正式 package、accepted dependencies、历史数据或 Stage 4 文件。

验收步骤：
1. 阅读 workflow、任务、主方案、业务 r2 报告和前两轮 QA 报告。
2. 重新计算 Formal package、Stage 1/2、历史 package 和冻结 artifact
   身份。
3. 流式逐行比较正式 projection 与三个 historical trigger audits。
4. 对四个 dependency claims 分别和组合做 production-size mutation。
5. 对 trigger contract 做 semantic-equivalent minify + coherent rehash。
6. 复做 external baseline、archived verifier/test、allowlist、manifest、
   zero-write 和 source/test binding 攻击。
7. 从 raw detector/pre-state inputs 完成 fresh production replay。
8. 运行测试、静态检查和边界检查，形成独立裁决。

普通代码审查：
- P0：`0`
- P1：`0`
- P2：`0`
- P3：`0`

P0 findings：
- 无。

P1 findings：
- 无。

P2 findings：
- 无。

P3 findings：
- 无。

第二轮重点问题复验：
1. 四个 Stage 1/2 manifest dependency claims 已 fail closed。
   - Hard-coded accepted constants 位于 admission source
     `50-67` 行。
   - `_verify_dependency_packages()` 在固定 Stage 1/2 roots 重新读取
     manifest core 并计算完整 inventory，位于 `540-578` 行。
   - `verify_package()` 先将四个 manifest claims 与 accepted constants
     精确比较，再将 `_verify_bindings()` 返回的实际 identities 与 claims
     精确比较，位于 `1958-1962` 和 `2008-2010` 行。
   - QA 对下列 mutation 分别建立 production-size package copy：
     - `stage1_core_sha256`
     - `stage1_full_inventory_sha256`
     - `stage2_core_sha256`
     - `stage2_full_inventory_sha256`
     - 四字段组合
   - 每个 mutation 都保持 artifacts 和 accepted
     `core_package_sha256=4939d1c1addce493edb2f368297d56b37edd0b123de01497dcdee6e77637eb9b`
     不变，并保持 manifest canonical。
   - 五组攻击的 source API、source CLI、archived CLI 全部 `rc=2`，
     错误均为 `manifest accepted dependency identity drift`。
   - QA 另将 `_verify_bindings()` 的实际 Stage 2 inventory 结果注入为
     伪值；`verify_package()` 返回 `rc=2`：
     `manifest/actual dependency identity drift`。这确认最终判断消费了
     fixed-root 实测 identity，而非只检查 schema 或 constants。
2. `frozen_trigger_contract.json` canonical raw bytes 已 fail closed。
   - 通用 canonical pretty JSON reader 位于 `420-434` 行；contract 在
     `1963-1970` 行先做 raw-byte canonical check，再做语义比较。
   - QA 将 contract 改成语义完全相同的 minified JSON，并同步更新：
     - contract SHA：
       `b3adf20dc0059ef46db381c5cdeabc0897617f40e2f406b89cd91262e740cc75`
     - artifact bytes/SHA
     - coherent core：
       `14e4fd91e8cb8242af41ef747711020eff305f5884ce8901f0eed628a2da55ee`
     - canonical manifest
   - Source API、source CLI、archived CLI 全部 `rc=2`：
     `frozen trigger contract canonical bytes drift`。

首轮六项修复复验：
1. Self-contained baseline：通过。
   - `input_bindings.csv` 无 external ephemeral baseline scope/path。
   - QA 将 `/tmp/0815T002-pre-extraction.4r9BcF` 原子移动到隔离路径，
     确认原路径不存在后运行 source/archive 默认 verify-only。
   - 两者均 `rc=0`、输出同一 accepted core；随后恢复 external
     baseline。
2. Archived verifier exact source binding：通过。
   - QA 修改 package 内 archived admission source，并 coherent refresh
     runtime-source map、artifacts、core 和 canonical manifest。
   - Source API、source CLI、被修改的 archived CLI 全部 `rc=2`：
     `fixed worktree source/test archive identity drift`。
3. Exact artifact/file/directory allowlist：通过。
   - QA 分别加入并 coherent rehash：
     `unexpected.txt`、`aug07/events.csv`、`0807/events.csv`、
     `outcome/future.csv`、`response/future.csv`、
     `markout/future.csv`、`PnL/future.csv`、`private/future.csv`、
     `account/future.csv`、`order/future.csv`、`cancel/future.csv` 和
     unknown fixture episode path。
   - 每一项的 source API、source CLI、archived CLI 全部 `rc=2`，在
     package universe scan 处因 forbidden/unknown path fail closed。
4. Default Python zero-write：通过。
   - Source/archive 均使用默认 `python`、无 `-B` 执行 verify-only。
   - QA 对 Formal package root、directories 和 files 共 `34` 个 entries
     比较 path/type/bytes/SHA/`mtime_ns`/`ctime_ns`，前后完全一致。
   - Formal package 无 `.pyc` 或 `__pycache__`。
5. Current tests archive/binding：通过。
   - 三个 runtime source 和三个 current test 的 package bytes 与固定
     worktree bytes exact 相同。
   - 修改 archived admission test 并 coherent refresh test map、
     artifacts/core 后，source API/source CLI/archived CLI 全部 `rc=2`。
6. Canonical manifest raw bytes：通过。
   - 语义相同的 minified `parity_manifest.json` 在 source
     API/source CLI/archived CLI 全部 `rc=2`：
     `parity manifest canonical bytes drift`。
7. 未篡改 Formal 正向验证：通过。
   - Source/archive 默认 verify-only 均 `rc=0`、`verified=true`，
     artifact count `23`，candidate rows `463612`，primary rows
     `267554`。

Shared detector 与 historical builder：
1. Shared detector 纯度和版本：通过。
   - `CONTRACT_VERSION=cross_exchange_queue_shock_trigger_v1`。
   - `AUDIT_FIELDS` 精确 `36`。
   - AST 审查只发现 stdlib imports：
     `bisect`、`collections`、`dataclasses`、`math`、`typing`。
   - 无文件 I/O、网络、subprocess、future response/outcome/model、
     private/account/order endpoint 调用。
2. Historical builder 真正复用 shared detector：通过。
   - Builder 导入 alias `queue_shock_trigger`。
   - constants、`AUDIT_FIELDS`、`TimelineState`、`BboState`、
     side/candidate helpers 均直接 alias shared module。
   - `_process_segment()` 调用
     `queue_shock_trigger.detect_candidates()`。
   - Builder 未定义第二个 `detect_candidates()` state machine。
3. Source identities：
   - shared trigger：
     `69f6dd56b1e34a07f4073269cd3df11101cf630a78d72707af00dbfb2ef478ad`
   - historical builder：
     `7e8a2718b01d968d99843226a006ac451052a0ecd22dd086f458e49651452050`
   - pre-extraction builder：
     `a81dcf58dddf86d74f38a3c33dde28a299e6ed6b133362027181c94ff0695cb9`
   - pre-extraction test：
     `298f586cf589a33730143bab28cdeb1dd60db2dd34299b37e5e4ce62e4a0554b`
4. Fixture pre/post 四文件逐 byte 相同：
   - episode gzip：
     `072fb1806fd2eec41fbd716364e8dbd68f44cfbdb9b29addae9ad3b3fd95a86a`
   - manifest：
     `683807fcd04cfe6340eb5f23cf984ebd2733f915b143eca56bbbcffa2acf0b2a`
   - summary：
     `c286db6860796320e6353356f2fc5170f065c5f74b20a6bca9c167166e3d4a1b`
   - trigger audit：
     `7a9bc1f8ab814a1371e73f8ff9323edfdb2bd7f7d78a6646b1b524d24ea81709`

独立 detector parity：
1. QA 流式逐行比较全部 historical trigger audit 与正式 projection。
   - Candidates：`463612`
   - Primary：`267554`
   - Rejected：`196058`
   - Audit fields：`36`
   - Segments：`19`
   - Field-text mismatch：`0`
   - Row-order mismatch：`0`
   - Candidate-sequence mismatch：`0`
   - Historical omission/extra projection rows：`0`
2. Session exact results：
   - Jul30：
     `268522 / 141768 / 126754`，row-stream SHA
     `e2b4e998b87c81c1f8350a92bd1253db35fcb396cabe557773364535190ceed4`，
     trigger-audit gzip SHA
     `264b42e52e3d9ed0b30611e3aab1af0a51ea1674a58ab62823ebac422a3bf752`。
   - Aug03：
     `127622 / 82533 / 45089`，row-stream SHA
     `73b8e5ae2ad6652c811e26c7df60d5cacba1a4febe812c0960450a7ea88fc309`，
     trigger-audit gzip SHA
     `06b692b2c748db1e272c392e010fa9ca7da8742ccc9a8dda0bb534d484e65c0f`。
   - Aug04：
     `67468 / 43253 / 24215`，row-stream SHA
     `fa537846e50760a50ee38882c8d39884d6f240f3f844c3ae8b1b66fe10367dcf`，
     trigger-audit gzip SHA
     `f90dec5bcb02117fcd8c140d4229df3f5fbecbad42e8b0dbc82c8412d6b2e06c`。
3. `detector_parity_by_session.csv` 的 `3` 行和
   `detector_parity_by_segment.csv` 的 `19` 行全部 mismatch 为 `0`、
   `exact_parity=true`。

Fresh replay 与正式 package identity：
1. QA fresh replay：
   `/tmp/0815T002-qa-r3.CjW0pb/replay/package`
   - 从固定 detector/pre-state inputs 重放全部 candidates。
   - 未读取 Aug07 event rows 或历史 episode response/outcome rows。
2. Formal 与 QA replay：
   - `diff -rq` 无差异。
   - 全部 `24` relative paths、raw bytes 和逐文件 SHA byte-identical。
   - `24` files / `40,504,469` bytes。
   - `23` artifacts + manifest。
   - Core SHA256：
     `4939d1c1addce493edb2f368297d56b37edd0b123de01497dcdee6e77637eb9b`
   - Full inventory SHA256：
     `ff8e3434672226371051151cea838503877dca79ac7860cf179256362d75e404`
   - Manifest SHA256：
     `a73d4a15a6f58bd5c533944131a9cadae4e74d1c6f0c23a65d75c35973282fa1`
3. Frozen research artifact SHA 未变：
   - Candidate projection：
     `68e65a612d7bce2911a4e3c96fd10528914290ca045c8912e6be45045fd0415f`
   - Trigger contract：
     `a894079e405073400d86f8471fd20e2a3a7116d3b804952bab8db56587f93b3d`
   - Session CSV：
     `67b8035aa5b8ce0cf650c6e8465aa83284e741f6d8ba3c92f492274c7267f534`
   - Segment CSV：
     `c4ab6a2a23e6349c5a77a9f30373b09f1ffa3724d278afe49f24047444a736f9`

Dependencies、bindings 和 immutability：
1. Manifest claims、hard-coded constants 和 fixed-root actual identities
   三方精确一致。
   - Stage 1：`14` files / `1,401,387` bytes；
     core
     `9c2756f966a1a05816f9e6a91d7361d76cfea10fc3c587944ca2f0c1d2eb7c96`；
     full
     `c540cc056313716b3bdd2b9c0fe076cda15a7f152b399ae6a1283b3aa8aa6590`。
   - Stage 2：`16` files / `19,834,728` bytes；
     core
     `7b3d06c3225f77c9929cdd3fa40d69c0866d83dbb75dd584dcb7db6db22ad3f8`；
     full
     `bdade16c53aed7bba54fdb9408ba3a8a03036d183720f589bda2a762448a3833`。
2. `input_bindings.csv`：
   - `226` exact-width rows；
   - before/after 各 `113`，逐行镜像；
   - detector inputs 为 `79` files，current source/test 为 `6` files；
   - external ephemeral baseline reference：`0`；
   - Aug07/outcome/private/order-like binding：`0`。
3. Detector/pre-state inventories：
   - Jul30：`33` files / `354,524,468` bytes /
     `7c25cce265a215ac92f124f2f1d77f3bdb28fc7c1601fa4387dd73c0573c64a9`
   - Aug03：`41` files / `175,173,698` bytes /
     `073ccbfa977ddcbf4bebbbd29eab4749e0b1f949e4d1ba768eb6787b851bff3b`
   - Aug04：`5` files / `90,183,269` bytes /
     `f3d1433b6f0d242707e4df08720c01f8e9442ddcf4c2e8670e69bde5ccd031e8`
4. Historical package opaque inventories：
   - Jul30：`11` files / `79,979,087` bytes /
     `603565e840ff99fdf050f9c862c382524934ef8e2034cf5bc668bfdf8d7a6387`
   - Aug03：`13` files / `46,747,139` bytes /
     `50a95175db0e0abc36604e1f6d3481d094270ee9ceebf24259356dfe8479e648`
   - Aug04：`4` files / `24,531,687` bytes /
     `1a182c9ae346e6a3c2989734d18316ffc78e26bc3f40aeadc335f1a625c856c3`

运行命令与结果：
1. Full Stage 3 suite：
   `python -B -m pytest -q
   examples/hyperliquid/test_cross_exchange_liquidity_response_trigger.py
   examples/hyperliquid/test_cross_exchange_liquidity_response_episodes.py
   examples/hyperliquid/test_cross_exchange_trigger_parity_admission.py`
   - `49 passed in 118.48s`
2. Alignment/density regressions：
   `python -B -m pytest -q
   examples/hyperliquid/test_cross_exchange_trigger_aligned_episode_contract.py
   examples/hyperliquid/test_cross_exchange_trigger_density_core.py
   examples/hyperliquid/test_cross_exchange_candidate_episode_merging.py
   examples/hyperliquid/test_cross_exchange_trigger_density_inputs.py
   examples/hyperliquid/test_cross_exchange_trigger_density_admission.py`
   - `183 passed in 9.54s`
3. Merging/recovery regressions：
   `python -B -m pytest -q
   examples/hyperliquid/test_cross_exchange_candidate_episode_merging.py
   examples/hyperliquid/test_cross_exchange_liquidity_response_case_hierarchy.py`
   - `33 passed in 0.18s`
4. Ruff：
   - `hftbacktest` Conda Python 未安装 `ruff` module；
   - 使用已安装 `/Users/liu/.local/conda/bin/ruff check ...`：
     `All checks passed!`
   - 不是验收阻塞。
5. 外置 `PYTHONPYCACHEPREFIX` compileall：
   - pass；`50` 个 bytecode files 全部写入 QA `/tmp` prefix。
   - Formal package 内 `.pyc`/`__pycache__` 为 `0`。
6. Formal package 全部 gzip：
   - `gzip -t` pass。
7. Scoped `git diff --check`：
   - pass。
8. Source/archive `--help`：
   - 均 `18` lines，bytes identical。
9. Source/archive default verify-only：
   - 均 `rc=0`；
   - package path/type/bytes/SHA/mtime/ctime zero-write。
10. Formal/QA replay：
    - `diff -rq` 无差异；
    - independent full-inventory identity 完全相同。

边界结论：
- 本轮没有读取 Aug07 raw/R0/R1/basis event rows。
- 本轮只解析 Jul30/Aug03/Aug04 historical `trigger_audit.csv.gz`；
  historical package 中其他文件只做 opaque compressed-byte inventory/hash，
  未解析 `episodes/*.csv.gz` response/outcome rows。
- Package 内存在任务要求归档的历史 builder source 和小型 fixture，
  但没有新建 Episode v3、Family A/B outcome、response path、markout、
  PnL、model、score、calibration、actionability 或 Stage 4 artifact。
- 本轮没有访问网络、AWS、SSH、private/account/order/cancel endpoint。
- 本轮没有启动采集、修改 live/active config 或下单。
- 本轮没有修改业务代码、正式 package、accepted Stage 1/2、历史 v1/v2
  artifacts、`.workflow/tasks/0815T002.md`、`task_plan.md`、
  `progress.md` 或 `findings.md`。

验收结论：
- 已通过
- 结论说明：
  - 第二轮剩余的 dependency-claim P1 和 canonical-contract P3 均已在
    production-size attacks 下 fail closed。
  - 首轮六项 repair 无回归，shared detector extraction、historical
    integration、全量 `463612`-candidate parity、Formal/fresh replay
    determinism、dependency provenance、zero-write 和 hard boundary
    全部成立。
  - QA 允许总控将 `0815T002` 置为 `已通过` 并解锁 Ordered Research
    Queue Stage 4；QA 本身未修改 task 状态，也未派发或开始 Stage 4。

通过项：
1. 第二轮四 dependency claims 与 canonical contract 修复通过。
2. 首轮六项 admission repair 全部复验通过。
3. Shared detector purity/versioning 与 historical builder integration
   通过。
4. 全 candidates、全部 rejected、36-field、19-segment exact parity
   通过。
5. Formal 与 QA fresh replay 的 core/full/manifest/bytes determinism
   通过。
6. Stage 1/2、historical/input identities、source/test bindings 和 hard
   boundary 通过。
7. Required tests、regressions、Ruff、compileall、gzip、diff check 和
   zero-write 通过。

不通过项：
1. 无。

缺陷清单：
1. P0：无。
2. P1：无。
3. P2：无。
4. P3：无。

阻塞项：
- 无。

建议总控下一步：
1. 将 `.workflow/tasks/0815T002.md` 更新为 `已通过`。
2. 将本轮 accepted Stage 3 core/full/manifest identities 写入 controller
   tracking。
3. 按主方案只派发 Ordered Research Queue Stage 4：Jul30-only Family
   A/B Episode v3；继续保持 Aug03/Aug04/Aug07 和后续 outcome/model
   stages 锁定到各自 gate。

报告文件一致性：
- 本报告正文机械复制到 `docs/qa-acceptance-report.md`。
- 写入后执行 `cmp -s` 和 SHA256 验证 byte-identical。
- 为避免报告自嵌自身哈希造成递归变化，最终 report/mirror SHA256 记录在
  QA 完成回复中。

提交信息：
- commit：无
