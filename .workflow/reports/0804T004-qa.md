# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0804T004

状态：
- 已通过

更新时间：
- 2026-08-04 11:33 CST

验收线程：
- QA验收线程

验收对象：
- 业务线程-python/advanced-tutorial-notebooks + 0804T004

验收范围：
- 七个 Grid/GLFT/Alpha/Pricing notebook 的 Run All 活动区。
- advanced experiment 的策略公式、点时数据边界和适配声明。
- Tardis-only/no-key/no-download/private API 边界。
- 本机与 amdserver 测试、executed notebooks 和 manifests。

验收步骤：
1. 审查七本 notebook、advanced helper、共享映射、生成器和测试。
2. 核对固定 Grid、skew、Simplified GLFT、GLFT、OBI、Basis、APT 和
   Pricing 的核心计算与报价方向。
3. 对 Basis/APT/OBI、GLFT 和 Pricing 执行 future-mutation 检查。
4. 运行 focused pytest、py_compile、nbformat、AST 和生成器确定性检查。
5. 检查 amdserver 七个 executed notebook 和七份 manifest。
6. 对首轮 P1 修复 commit `379a2651` 做独立复验。

首轮缺陷：
1. P1：GLFT 使用完整 300 秒窗口拟合后从同一窗口起点回测，早期报价
   依赖未来成交。
2. P1：Pricing 使用全样本标准差构造交易信号，未来样本会改变历史信号。

修复结果：
- GLFT 改为每 5 秒使用截至当前时点、最多过去 30 秒的滚动前缀数据
  校准；最早约 10 秒开始有效拟合。
- 修改 150 秒后的 `9432` 条成交时，前 `1500` 行 GLFT 输出完全不变，
  截点后输出按新数据变化。
- Pricing 四因子改为 300 行 causal rolling z-score。
- 修改 150 秒后的 `215` 行 index price 时，前 `1500` 行 pricing
  components 完全不变，截点后输出按新数据变化。
- 提交内新增 prefix fit 和 future-component mutation 回归测试。

实际结果：
- 七本 notebook 均只有四个活动 code cell，reference 区无可执行代码。
- 七本 amdserver executed notebook 均为 execution count `[1,2,3,4]`，
  error output 为 `0`。
- 生成器连续执行 SHA256 一致。
- 活动代码只读取已有 Tardis depth/trades/book ticker/derivative ticker。
- 未发现 Tardis Key、下载、网络请求或交易所 private/account API。
- Basis/APT/Pricing 明确使用 index price 适配，未冒充 spot、FDUSD 或
  多资产面板。
- 修复后 focused tests 为 `11 passed`。
- 七份 manifest 状态为 `1 passed / 6 adapted / 0 failed`。
- 修复后 GLFT/Pricing notebook 已在 amdserver 重新生成执行副本，
  不是沿用旧产物。
- 修复后 GLFT 与 Pricing manifest 均为 `adapted`，关键数值有限。
- amdserver 修复执行时 HEAD 为
  `379a26516a175d758181923aea2d67a31adf89ac`。

验收结论：
- 已通过
- 结论说明：
  - 首轮两项 P1 look-ahead 已通过代码修复、future-mutation 和服务器
    notebook 重跑闭合；七项实验满足 Run All 和 point-in-time 边界。

通过项：
1. 固定 Grid、inventory skew 和波动率自适应 Grid。
2. GLFT point-in-time arrival-intensity/volatility rolling calibration。
3. OBI、Basis 和 APT point-in-time fair-price 计算。
4. Pricing causal rolling factor standardization。
5. Tardis-only 数据和适配声明。
6. 七本 notebook Run All 与 amdserver 执行证据。
7. focused tests 和 future-mutation 验证。

不通过项：
1. 无。

缺陷清单：
1. 首轮 P1 x2，均已由 `379a2651` 修复并复验通过。

阻塞项：
- 无。

残余风险：
- 当前实际执行范围仅为 BTCUSDT、2025-08-01、300 秒窗口。
- GLFT 最长校准窗口为 30 秒，因果边界正确，但不具备长周期统计稳定性。
- short-window PnL 和 IC 仅为教学诊断，不构成生产策略结论。

建议总控下一步：
1. 将 `0804T004` 收口为 `已通过`。
2. 用户可直接在 amdserver 的 examples 目录打开七本 notebook 测试。

提交信息：
- implementation：14a9a7442cc6dbd8c7887b0249c4de1c76d8b43b
- look-ahead fix：379a26516a175d758181923aea2d67a31adf89ac

文档占用说明：
- `docs/qa-acceptance-report.md` 仍含另一并行任务未提交的既有修改；
  本任务未覆盖该文件，最新结论保存在本任务专属 QA 报告中。
