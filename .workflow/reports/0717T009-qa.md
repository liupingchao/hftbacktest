# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0717T009

状态：
- 已通过

更新时间：
- 2026-07-18 00:16 UTC

验收线程：
- QA验收线程

验收对象：
- 业务线程-watcher-termination 0717T009

验收范围：
- 验收 `0717T006` Phase 3 watcher termination/timeout contract。
- 检查 signal handling、process-group lifecycle、timeout、SIGTERM/SIGKILL escalation、child reap、window suppression 和 proof ordering。
- 本 QA 不运行 live，不访问 credential/private/order/cancel/network/remote endpoints。

验收步骤：
1. 审查任务、业务回报和 implementation commit `9b00e4c`。
2. 检查阻塞 `subprocess.run()` 是否替换为 process-group-aware `Popen`。
3. 检查 signal handler 是否只更新内存状态，所有文件写入和终止动作是否在 polling flow 完成。
4. 运行 success、SIGTERM、SIGINT、timeout 和 SIGKILL fake-watcher tests。
5. 检查 child reap、proof-after-child-exit、abort lifecycle fields 和 next-window suppression。
6. 运行 T008/T009 联合回归、`py_compile` 和 commit diff check。

实际结果：
- Watcher 使用 `Popen(..., start_new_session=True)`，并保留 active child handle。
- `SIGTERM` / `SIGINT` handler 不执行文件 I/O；主 polling flow 记录终止状态并终止 child process group。
- Timeout 使用 `window_seconds + window_timeout_grace_seconds`，终止流程为 SIGTERM -> grace wait -> SIGKILL escalation -> `wait()` reap。
- Window status 和 root abort manifest 均包含 child pid/process-group、termination reason/signal、SIGKILL escalation、returncode、reaped、timeout budget 和 proof-after-child-exit 字段。
- Independent open-orders proof 在 child exit/reap 后执行。
- Abort 和 timeout 均不会启动下一窗口。
- 成功的两窗口路径保持通过，live watcher 参数、post-only 和订单边界未改变。
- T009 focused tests: `7 passed`。
- T008/T009 combined regression: `103 passed`。
- `py_compile`、`git diff --check` 和 `git diff --check 9b00e4c^..9b00e4c` 通过。
- fake watcher PID 在 signal、timeout 和 SIGKILL 测试后均已消失。
- 未执行 live、credential、private、order、cancel、network、remote 或 service 动作。

验收结论：
- 已通过
- 结论说明：
  - Phase 3 watcher termination/timeout contract 已闭环，可进入 Phase 4 terminal artifact seal repair。

通过项：
1. Signal/timeout 不再被阻塞式 child wait 吞掉。
2. Process group termination、grace、SIGKILL escalation 和 child reap 正确。
3. Abort/timeout 生命周期证据完整，且 proof ordering 明确。
4. 后续窗口不会在 abort/timeout 后启动。
5. Successful multi-window compatibility 保持。
6. No-live/no-private/no-remote 边界保持。

不通过项：
1. 无

缺陷清单：
1. 无

阻塞项：
- 无

建议总控下一步：
1. 创建唯一下一任务 `0717T010 / TERMINAL-ARTIFACT-SEAL-REPAIR`。
2. Phase 4 仅修复 terminal checksum manifest 的生成顺序和 final artifact immutability；不要提前执行 live 或 integrated offline acceptance。

提交信息：
- implementation commit：`9b00e4c`
- workflow commits：`221edc6`, `2c1d57a`
