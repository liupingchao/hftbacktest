# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0717T011

状态：
- 已通过

更新时间：
- 2026-07-18 01:05 UTC

验收线程：
- QA验收线程

验收对象：
- 业务线程-integrated-offline-acceptance 0717T011

验收范围：
- 验收 `0717T006` Phase 5 integrated offline acceptance，把 Phase 1-4 repair contracts 放入同一条三窗口 fake orchestration。
- 本 QA 不执行 live，不访问 credential/private/order/cancel/network/remote/service endpoints。

验收步骤：
1. 审查任务、业务回报和 implementation commit `6fadc95`。
2. 检查 window 1 重复 pullback、window 2 双 attempt/unique fill/ambiguous fill 和 window 3 timeout fixture。
3. 检查真实 `LiveFillLedger` 的 window/attempt identity、幂等、time-bounded attribution 和 ambiguous quarantine。
4. 检查真实 orchestrator 的 child timeout/reap、no-next-window、abort/open-orders proof 和 terminal checksum。
5. 运行 integrated test、Phase 1-4 full regression、`py_compile`、CLI help 和 diff check。

实际结果：
- 三窗口 fixture 运行成功：window 1/2 完成，window 3 timeout，window 4 未创建。
- window 1 同一 fill 两次 pullback 只保留一条 ledger row，数量为 `0.005 BTC`，并保留 duplicate pullback evidence。
- window 2 唯一 fill 归因到 `0717T011:window_02:attempt_2`；重叠时间窗口的 ambiguous fill 保持 `ambiguous_unattributed_fill`，reason 为 `multiple_candidate_attempts`。
- window 3 child 的 termination reason 为 `watcher_timeout`，child 已 reaped，open-orders proof 在 child exit 后执行。
- abort root open-orders proof 报告 empty；terminal Python verification status 为 `pass`。
- fixture 内真实 `sha256sum -c remote_sha256_manifest.txt` 返回成功。
- Integrated/Phase 1-4 regression: `110 passed`。
- `py_compile`、`python ... --help`、`git diff --check` 和 implementation commit diff check 通过。
- 未执行 live、credential、private、order、cancel、network、remote 或 service 动作。

验收结论：
- 已通过
- 结论说明：
  - `0717T006` 四个 repair phases 已通过集成离线验收，可进入 Principal Alignment Task 1 价格合法化。

通过项：
1. Window/attempt identity 在集成 fixture 中保持一致且跨窗口不冲突。
2. 重复 pullback 不重复累计 fill quantity。
3. 唯一时间归因与 ambiguous fail-closed 行为正确。
4. Timeout、process termination、reap 和 no-next-window 正确。
5. Abort/open-orders evidence 和 terminal checksum verification 完整。
6. No-live/no-private/no-remote 边界保持。

不通过项：
1. 无

缺陷清单：
1. 无

阻塞项：
- 无

建议总控下一步：
1. 创建唯一下一任务 `0718T012 / PRICE-NORMALIZATION-REPAIR`，实现 Principal Alignment Task 1。
2. 仅做离线价格合法化、post-only property tests 和 shared helper 接线；不要执行 live。

提交信息：
- implementation commit：`6fadc95`
- workflow commits：`d73b92b`, `9b7cc59`
