# QA 验收结果

执行线程：
- 独立只读 QA 验收线程

任务ID：
- 0720T025

状态：
- 已通过

更新时间：
- 2026-07-20 18:49 CST

验收线程：
- QA验收线程

验收对象：
- Implementation commit：`87c9639ec49717a4dcaaa3545e5165b0803aef5f`
- Business/report commit：`403f659ae1d9141b437603777421c6291dd5c77c`
- 业务执行线程 / 0720T025
- Branch：`cross-exchange`

## Findings

1. 未发现 P0/P1/P2 缺陷。

验收范围：
- Historical row supported-status 验证。
- Manager、producer、independent acceptance 的 complete-foreign exact-cover 分类。
- T023 bounded direct/history、T024 redaction/OID、SDK/title 回归。
- T016/T022 immutable offline replay。
- 提交祖先、文件范围和只读边界。

验收步骤：
1. 阅读 QA 模板、T025 task/business、T024 QA 和 workflow 规则。
2. 核对固定 commit 的 ancestry、diff 和文件范围。
3. 静态审查三层 historical classification 和 terminal selection。
4. 执行跨层 hostile probes、focused/full tests、语法、CLI 和 Git checks。
5. 核对已完成的 T016/T022 replay 结果、输入摘要和离线边界。
6. 确认无遗留 pytest 或 acceptance 进程。

实际结果：
- Implementation 是 report commit 的直接父提交。
- Implementation 恰好包含任务声明的 6 个代码/测试文件。
- Report commit 只包含 task、business report 和三个 tracker 文件。
- Hostile probes：`135/135` 通过，`0` failures。
- 双 identity 下的 partial foreign、invalid status、duplicate exact、shared identity conflict 和 alias conflict全部 fail closed。
- 单 identity expectation 下，缺失或额外 identity kind 全部 fail closed；精确 kind coverage 的 exact/foreign 正常。
- Complete valid fully-disjoint foreign 加 unique exact 在三层均返回 `cancel_confirmed`。
- Redacted hostile schema、Unicode digit、5000 位 OID、leading zero 和 uint64 overflow均安全拒绝。
- uint64 最大值的 int/string 形式均正常接受。
- Focused tests：`600 passed in 19.78s`。
- Full Hyperliquid tests：`938 passed in 43.72s`。
- 6 个修改文件 `py_compile`、acceptance `--help`、两个 commit 的 `git show --check` 和提交区间 `git diff --check` 均通过。
- T016 replay exit `2`：decision `43/43`、lifecycle `49 pass / 12 fail`，输入 `118` 个文件保持不变。
- T022 replay exit `2`：decision `43/43`、lifecycle `49 pass / 12 fail`，输入 `118` 个文件保持不变。
- 两组 replay 均保持 `offline_only=true`；network/private/order/cancel/remote/new-live 全为 false。
- 未修改 repository 文件，未触发 live/private/account/order/cancel/network/remote/service。
- 最终进程检查未发现遗留 pytest 或 acceptance 进程。

验收结论：
- 已通过
- 结论说明：
  - T025 已关闭 T024 的 partial-foreign、invalid-status 和 single-kind exact-cover 缺陷，且未发现回归。

通过项：
1. Manager、producer 和 independent acceptance 独立执行一致的 complete-foreign exact-cover 合同。
2. Supported status 在 identity 分类前验证，malformed row 无法被 clean exact row 掩盖。
3. T023 bounded recovery、T024 redaction/OID、SDK/title 和 T016/T022 replay保持。
4. 提交边界、测试结果和 offline-only 限制符合任务合同。

不通过项：
1. 无

缺陷清单：
1. 无

阻塞项：
- 无

建议总控下一步：
1. 将 `0720T025` 标记为 `已通过`。
2. 可按正式计划派发下一项保守、受限任务；Task 8 和 adaptive/multi-level activation仍应等待对应 lifecycle gate。

提交信息：
- commit：无；本次 QA 只读，未编辑、暂存或提交文件。
