# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0604T006

状态：
- 未通过

更新时间：
- 2026-06-04 10:11 CST

验收线程：
- QA验收线程

验收对象：
- 业务线程-research + 0604T006

验收范围：
- `0604T006` 规定的 read-only canonical signal quality ranking runner、focused tests、task-scoped artifacts、allowlist enforcement、canonical source-lock guard，以及 synthetic diagnostic comparison 输入拒绝复验。

验收步骤：
1. 阅读 `.workflow/tasks/0604T006.md` 与 `.workflow/reports/0604T006-business.md`，核对任务边界、验收标准、业务结论与提交信息。
2. 核对 commit `5f5d218` 的改动范围，确认包含 runner、focused tests、business report 和 task-scoped artifacts。
3. 阅读 `examples/hyperliquid/canonical_signal_quality_ranking.py`、focused tests、manifest/report/CSV 产物，确认 source-lock guard、allowlist、ranking buckets 和 boundary flags。
4. 复跑最小验证命令：`--help`、`py_compile`、focused pytest。
5. 对 `local_live_analysis/event_mode_canonical_pricing_signal_0604T003/` 运行 ranking runner 到 `/tmp/qa_0604T006_signal_quality/`，解析复现 manifest/report。
6. 对 `local_live_analysis/event_mode_canonical_pricing_signal_0604T003/synthetic_diagnostic_comparison/` 运行 ranking runner，确认 diagnostic-only synthetic input 被拒绝。
7. 运行 `git diff --check`，确认无格式问题。

实际结果：
- `git show --stat --oneline 5f5d218` 确认提交为 `Refine 0604T006 guarded signal ranking`，包含 runner、focused tests、business report 和 `local_live_analysis/canonical_signal_quality_ranking_0604T006/**` artifacts。
- 正式 artifact manifest 记录 `canonical_sample_count=3`、`diagnostic_rejection_count=0`、`ranked_feature_count=4`、`source_lock_task_id=0604T005`、`unknown_non_allowlist_features_seen=[]`。
- 正式 ranking CSV 包含且仅包含四个 allowlist features：`binance_mid_move_ticks_from_prev`、`binance_top5_imbalance`、`binance_top5_bid_qty`、`binance_microprice_minus_mid_ticks`。
- 正式 ranking CSV / reject-watch list 的 buckets 为：`binance_mid_move_ticks_from_prev=keep_for_read_only_research`，其余三个为 `watch_regime_dependent`。
- `python examples/hyperliquid/canonical_signal_quality_ranking.py --help` 通过。
- `python -m py_compile examples/hyperliquid/canonical_signal_quality_ranking.py examples/hyperliquid/test_canonical_signal_quality_ranking.py` 通过。
- `python -m pytest examples/hyperliquid/test_canonical_signal_quality_ranking.py` 通过，结果为 `6 passed`。
- QA 复跑 canonical 输入输出 `/tmp/qa_0604T006_signal_quality/`，结果 `canonical_sample_count=3`、`ranked_feature_count=4`。
- QA 复跑 manifest JSON parse 通过，复现 manifest 仍记录 `source_lock_task_id=0604T005`、`unknown_non_allowlist_features_seen=[]` 和完整 no collection / no strategy / no live / no promotion boundary flags。
- QA 对 synthetic diagnostic comparison 输入运行 runner，命令按预期失败并返回 `EvidenceValidationError: ... is diagnostic-only; pass allow_diagnostic_validation=True only for negative validation`，证明 diagnostic-only synthetic artifacts 不能作为 formal ranking input。
- `git diff --check` 通过。
- 但 QA 复跑生成的 `/tmp/qa_0604T006_signal_quality/signal_quality_ranking_report.md` 在 `Controller Interpretation Check` 中写道：`binance_top5_imbalance` “is kept for read-only research”；同一份复现 report 的 ranking section、CSV、manifest 和业务报告均把该 feature 标为 `watch_regime_dependent`。这使 report 的 keep/watch 解释与实际 ranking bucket 不一致。

验收结论：
- 未通过
- 结论说明：
  - `0604T006` 的 runner、guard、allowlist、focused tests 和核心 ranking CSV 可复现通过；但 report artifact generation 存在可复现的结论文案错误，导致 `binance_top5_imbalance` 的 keep/watch/reject 解释与实际 `watch_regime_dependent` bucket 不一致，未满足“Report includes a keep/watch/reject list with reasons”验收标准。

通过项：
1. Runner 通过 `0604T005` source-lock guard / `0604T004` loader path 消费 `0604T003` canonical event-mode evidence。
2. Runner 拒绝 diagnostic-only synthetic comparison 作为 formal ranking input。
3. Ranking 输出包含且仅包含四个 `0601T004` primary allowlist features，无非 allowlist primary feature。
4. Core ranking CSV 和 reject/watch list 可复现，核心排序为：mid move rank 1 keep，top5 imbalance / top5 bid qty / microprice-minus-mid 为 watch。
5. Focused tests 通过，覆盖 ranking order、watch classification、sample concentration penalty、short-horizon-only rejection、diagnostic-only input refusal 和 artifact generation。
6. 边界 flags 保持 read-only：无 collection、regime selection、case-library、shadow decisions、strategy/private/order/live/default-on/tiny-live/parameter search/promotion。

不通过项：
1. Report artifact generation 可复现地输出与 ranking bucket 不一致的 Controller Interpretation Check：`binance_top5_imbalance` 被 CSV/manifest/business report 标为 `watch_regime_dependent`，但复现 report 文案写成 “kept for read-only research”。

缺陷清单：
1. 修正 `examples/hyperliquid/canonical_signal_quality_ranking.py` 的 `_write_report` Controller Interpretation Check，使 `binance_top5_imbalance` 的描述与实际 bucket 一致，或从 `ranking_rows` 动态生成解释，避免静态文案和 CSV 分叉。
2. 修复后重新运行 T006 runner，刷新 `local_live_analysis/canonical_signal_quality_ranking_0604T006/signal_quality_ranking_report.md`，并复跑 focused tests / JSON parse / `git diff --check`。

阻塞项：
- 无环境阻塞；这是窄 report-generation 修复。

建议总控下一步：
1. 将 `0604T006` 记为 `未通过`，派回业务线程做窄修复。
2. 修复范围应限制在 report 文案/生成逻辑和刷新 T006 task-scoped report artifact；不需要改 ranking scoring、allowlist、guard、tests 边界，除非业务线程发现同源问题。

提交信息：
- commit：5f5d218
