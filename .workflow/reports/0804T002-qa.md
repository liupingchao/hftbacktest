# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0804T002

状态：
- 已通过

更新时间：
- 2026-08-04 10:24 CST

验收线程：
- QA验收线程

验收对象：
- 业务线程-python/tutorial-reproduction + 0804T002

验收范围：
- commit `a0c54391daa44b48df4baf1f4d5c0064d7fb65ee` 中
  `examples/tutorial_reproduction/` 的实现和测试。
- 九个 notebook 的映射、顺序、Tardis-only 输入边界、Mac/amdserver
  布局兼容、时间切片、转换安全性和结果状态语义。
- 本机 2025-01-01 与 amdserver 2025-08-01 的五分钟执行证据。
- L3/MBO 数据可用性和不得把 L2 冒充 L3 的硬边界。

验收步骤：
1. 审查 task、业务报告、runner、README 和 focused tests。
2. 核对九项实验列表与顶层 manifest 的串行顺序。
3. 运行 focused pytest、CLI help 和 `git diff --check`。
4. 检查本机与 amdserver 的 `reproduction_manifest.json`。
5. 在 amdserver 核对 HEAD、被验 commit 祖先关系和 `run.py` 文件哈希。
6. 检查 `/mnt/4t_sda1` 多交易所挂载是否存在可用于真实 L3 的 MBO 数据。

实际结果：
- 未发现影响验收的代码缺陷。
- 九个 notebook 映射及顺序正确。
- runner 仅读取已有 Tardis 文件，没有下载、Tardis Key、交易所私有接口
  或下单逻辑。
- 同时支持 Mac 扁平 `.csv.zst` 布局与 amdserver `YYYY/MM/DD` 日目录。
- 时间切片采用 `[start, end)`；测试和服务器边界检查通过。
- 转换按输入规模保守扩容，并执行事件顺序校验和快照生成。
- focused pytest 为 `2 passed`；CLI help 和 `git diff --check` 通过。
- 本机五分钟实验 `completed=true`、`failed=[]`，L3 为预期阻塞。
- amdserver 五分钟实验 `completed=true`、`failed=[]`；其余八项均为
  `passed` 或 `adapted`。
- amdserver L3 统计为 `DEPTH_EVENT=540213`，add/modify/cancel/fill 均为
  `0`；实现明确输出 `blocked_expected` 并只运行 L2 控制实验。
- `/mnt/4t_sda1` 检查到的五个交易所挂载均只有 L2、快照、成交等数据，
  没有可用的 MBO/L3 逐订单数据。
- amdserver HEAD 为 `7572f446a2ed90f26f2bbfd36ab01cb322c75b65`，
  被验 commit 是其祖先，服务器 `run.py` 哈希与被验 commit 一致。

验收结论：
- 已通过
- 结论说明：
  - 三个系列的核心实验已在本机 sample 和 amdserver 完整日数据挂载上
    按顺序复现；适配项与真实 L3 阻塞边界均如实记录。

通过项：
1. 九个 notebook 映射、串行顺序和独立结果发布。
2. Tardis-only/no-key/no-download 输入边界。
3. Mac 扁平布局与 amdserver 日目录布局兼容。
4. 数据切片、转换、融合、快照和延迟模型流程。
5. 本机和 amdserver 五分钟实跑及 manifest 证据。
6. L2/L3 能力边界和 `blocked_expected` 语义。

不通过项：
1. 无。

缺陷清单：
1. 无。

阻塞项：
- 无。

残余风险：
- 当前验证范围是单品种、单日、五分钟窗口。
- manifest 尚未内嵌 Git SHA 和依赖版本；本次已通过服务器代码哈希、
  commit 祖先关系和环境检查补充核验，不阻塞本轮验收。

建议总控下一步：
1. 将 `0804T002` 收口为 `已通过`。
2. 若后续取得真实 MBO/L3 数据，单独派发 Level-3 notebook 完整复现任务。

提交信息：
- commit：a0c54391daa44b48df4baf1f4d5c0064d7fb65ee

文档占用说明：
- `docs/qa-acceptance-report.md` 当前含另一个并行任务未提交的既有修改；
  本任务没有覆盖该文件，最新结果保存在本任务专属 QA 报告中。
