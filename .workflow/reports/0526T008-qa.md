# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0526T008

状态：
- 已通过

更新时间：
- 2026-05-27 10:53 CST

验收线程：
- QA验收线程

验收对象：
- 测试线程 0526T008

验收范围：
- 验收 `5-26-active-minmove-control-30min-b` 是否按任务要求完成 30min current-format no-rule/default-off live control 采集、拉回、raw 恢复、audit replay、maker acceptance、T009 sidecar/join、Stage 5、Step 5C、Stage 6、Step 9B、Step 9D 和 archive/checksum。
- 不验收任何 candidate enablement、guard relaxation、strategy change、parameter sweep、tiny-live、default-on 或 promotion claim。

验收步骤：
1. 检查 `.workflow/tasks/0526T008.md` 与 `.workflow/reports/0526T008-business.md`。
2. 检查本地 run dir、archive 和 sha256 是否存在。
3. 核对 maker acceptance、market-view gate、T009 sidecar/join、Stage 5、Step 5C、Stage 6、Step 9B、Step 9D 的关键指标。
4. 核对 raw gzip recovery、Stage 6 lifecycle-min input workaround 和 no-rule/default-off 边界是否有记录。

实际结果：
- 任务文件和业务报告均显示任务已进入 `待验收`，本次 QA 后任务切为 `已通过`。
- archive 存在：`local_live_analysis/archive/5-26-active-minmove-control-30min-b.tar.gz`。
- archive sha256：`90cb6279ddce0913d04567245b6a29ab49bf2fe580479faf8615046145f9926a`。
- remote preflight passed：commit `ff6f1b7`，git dirty `false`，compatibility passed `true`，audit field count `159`，missing T006 fields `[]`。
- raw gzip tail truncation 已处理：原始副本保留为 `.gz.corrupt`，recovered `btcusdt_20260526.gz` 通过 `gzip -t`。
- maker acceptance passed：`true`，hard failures `[]`；action/planned/reject/throttle match rate 均为 `1.0`。
- market-view acceptance passed：classification `passes_pricing_research_market_view`。
- T009 sidecar/join clean：final data row mapping coverage `1.0`，depth pu mismatch `0`，future/gap/missing `0/0/0`，decision join coverage `1.0`，top5 join age p99 `26.8948756ms`，top5 tick/qty match `0.9704893778786287 / 0.9474662196872514`。
- Stage 5：submit orders `11089`，filled orders `471`，fill-after-cancel orders `246`，partial fills `0`，missing lifecycle `1`。
- Step 5C：decision rows `340731`，bookTicker anchor rows `329579`，depth fallback rows `11152`，missing/stale anchor `0/0`，post-only risk after recheck `0`。
- Stage 6：decision state `methodology_valid_single_sample`，matched submits `11084/11089`，price/qty equality `11084/11084`，live/replay fills `471/480`，not-aligned list is `none`.
- Step 9B：classification `promising_but_single_sample`，candidate count `8`。
- Step 9D：single-sample stable promising bucket count `0`，candidate ids include `min_move_quote_age_churn_guard`、`inventory_reservation_shift_band`、`size_reduction_or_add_side_suppression_pressure`。
- Caveat 已记录：full audit replay CSV 因重复 `cancel_ack` lifecycle rows 膨胀到约 21GB，Stage 6 使用 lifecycle-minimized replay audit input 完成；原始 full replay audit 本地保留，后续问题已另建 `0527T001` 占位任务。

验收结论：
- 已通过
- 结论说明：
  - `0526T008` 完成 30min no-rule/default-off control 样本采集、恢复、后处理、归档和既有诊断链路；结果可作为 current-format control diagnostic input，但不授权 live/default-on/promotion 或参数搜索通过。

通过项：
1. 采集、拉回、raw recovery、archive 和 sha256 均完成。
2. Maker acceptance、market-view acceptance、T009 sidecar/join 均通过关键门槛。
3. Stage 5、Step 5C、Stage 6、Step 9B、Step 9D 产物均已生成并记录核心指标。
4. no-rule/default-off control 边界清楚，没有 candidate enablement、guard relaxation、strategy change、parameter sweep、tiny-live 或 promotion claim。
5. replay audit bloat caveat 已明确记录，并已拆出后续 `0527T001` 占位任务。

不通过项：
1. 无

缺陷清单：
1. 无。本次 QA 不把 replay audit full CSV 过大判为 `0526T008` 失败，因为 Stage 6 workaround 已记录且后续修复已单独立项。

阻塞项：
- 无

建议总控下一步：
1. 不应从 `0526T008` 的单样本 `promising_but_single_sample` 推导 live/default-on/promotion。
2. 后续应先决定是否继续 `0526T006` design-only，或细化 `0527T001` 来修 replay audit / Stage 6 input bloat。

提交信息：
- commit：无
