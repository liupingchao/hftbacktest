# QA 验收结果

执行线程：
- 独立 QA验收线程

任务ID：
- 0719T012 / LIVE-EVIDENCE-SUMMARY-PATH-PORTABILITY-REPAIR

状态：
- 未通过

更新时间：
- 2026-07-19 23:59 CST

验收对象：
- implementation commit `ab8ce0e6e803fd82ded91d054371bd0a2c3f6905`
- business report commit `ddbbfbf11896da2cf72768e6b6647490e3304eb9`
- T011 artifact `local_live_analysis/principal_alignment_single_level_two_sided_0719T011/`
- `f6526d8`、`4643003` 仅为日期元数据往返，不纳入验收范围；当前 business report 内容与 `ddbbfbf` 一致。

验收范围：
- decision evidence summary、endpoint 分类、remote/local path portability。
- strict boolean/status/event/attempt/join、reason atom counts。
- T011 离线重放、source/checksum/fill/cancel fail-closed、strategy/risk/activation 不变性。
- 未执行 live、private、account、order、cancel、network、remote 或 service 操作。

## Findings

1. P1：malformed event/attempt identity 可被最终 acceptance 接受。
   - `/tmp` 中把 immediate-guard、edge-gate 的 `attempt` 改为 `not-an-attempt`，并把 guard、edge、top+nested attempt 的 `event_sequence` 改为 `not-an-event`，重新 seal 后：
     - `validation_reasons=[]`
     - reconstruction validation、top/nested copy 均 pass
     - 最终 recommendation 仍为 mechanism/evidence pass。
   - `cross_exchange_t024_same_window_acceptance.py:1780-1785` 未校验 guard attempt/event。
   - `:1808-1822` 和 `:1862-1871` 对无法解析的 event 直接跳过 join 检查。
   - 不满足任务要求的 strict event/attempt/join fail-closed。

2. P1：零 order-authorized row 可与两个 submissions 同时被接受。
   - 将有效 fixture 的唯一 trigger 改为 `live_window_called=false`，同步重建 summaries 后：
     - `order_authorized_row_count=0`
     - `submitted_attempt_count=2`
     - `live_submissions=2`
     - `validation_reasons=[]`
     - 最终 mechanism/evidence acceptance 仍为 pass。
   - Acceptance 计算了 `order_authorized_row_count`，但没有要求 submitted attempt 按 event identity 连接到 authorized trigger。
   - 因此 raw trigger authorization 与真实 order endpoint evidence 可以互相矛盾。

3. P2：跨矩阵 status/reason 因果关联不完整。
   - 构造 trigger/anti-drift/guard/edge 使用互相冲突的 reason，重建仍返回 `validation_reasons=[]`。
   - anti-drift summary reason 来自 trigger `guard_reason`，没有与 anti-drift matrix `reason` 精确比较。
   - immediate-guard 和 edge reason 也未与对应 trigger 的 status/reason 做双向 exact join。
   - 当前能精确计数各自矩阵，但不能证明这些行属于同一个 trigger 因果链。

实际结果：
- T011 独立重建：
  - candidate evaluations `2564`；trigger rows `26`。
  - anti-drift `42 pass / 5 block`。
  - immediate guard `10 pass / 11 fail`；四个 reason atoms 各 `11`。
  - edge gate `0 pass / 10 block`；`7 fair_mid_source_stale / 3 edge_below_required_buffer`。
  - candidate attempt rows / distinct manager identity / submissions：`21 / 1 / 0`。
  - private read/order/cancel：`true / false / false`。
- T011 byte-exact replay：
  - provenance `112 pass / 0 fail`。
  - config `70 pass / 1 fail`；decision `16 pass / 27 fail`；lifecycle `34 pass / 27 fail`。
  - return code `2`，仍因零 submission、真实 edge/guard blocker 和缺失 two-sided lifecycle blocked。
- Remote/local physical roots 不同不再导致 provenance failure。
- 同步 remote-root rewrite 被外部 expected root 拦截：`107 pass / 5 fail`。
- local byte mutation：`109 pass / 3 checksum fail`。
- 重封装 runtime source digest mutation：`111 pass / 1 exact Git blob fail`。
- Strategy formula、threshold、risk cap、activation 行为未发现变化。

命令与结果：
- Focused acceptance/watcher/orchestrator/manager：`236 passed in 16.43s`。
- Full `python -m pytest -q examples/hyperliquid`：`699 passed in 35.21s`。
- source/checksum/fill/cancel 定向参数化回归：`15 passed in 0.37s`。
- 修改模块及测试 `py_compile`：pass。
- Acceptance/watcher `--help`：pass；acceptance 强制要求 `--expected-remote-run-root`。
- implementation/business `git diff --check`：pass。
- QA 未修改仓库；既有工作树改动保持不变。

验收结论：
- 未通过
- 路径可移植性、T011 数值重建和旧有 source/checksum/fill/cancel 边界基本成立，但新增 acceptance 对 event、attempt、authorization 和跨矩阵 reason 的关联仍可 fail-open，未达到任务明确要求的严格独立验收标准。

通过项：
1. External expected remote root 阻止同步路径自授权 rewrite，同时允许 remote/local physical roots 不同。
2. T011 `26 / 5 / 10 / 11`、edge reasons、reason atoms 和 `21 / 1 / 0` 重建正确。
3. T011 provenance 全通过且 lifecycle 继续正确 blocked。
4. Producer summary、standalone summary、inline summary 的 exact comparison 已建立。
5. Source、terminal checksum、fill、cancel 既有 fail-closed 未弱化。
6. Strategy/risk/activation 无变化；完整回归通过。

不通过项：
1. Malformed/missing event 和 guard/edge attempt identity 未 fail-closed。
2. Submitted attempts 未强制连接到 order-authorized trigger。
3. Trigger、anti-drift、guard、edge 的 status/reason 未完成双向 exact join。

缺陷清单：
1. 对每个 guard/anti-drift/edge/attempt row 严格解析 canonical positive event sequence 和 attempt identity。
2. 每个非 trigger row 必须唯一连接到现存 trigger；孤立、重复、缺失或 malformed join 均 fail closed。
3. Submission 必须连接到 `live_window_called=true` 的 authorized trigger；零授权不得存在 submission。
4. 跨矩阵 status、reason、attempt、event 必须精确一致，并增加上述三个反例的回归测试。

阻塞项：
- 0719T012 未通过独立 QA。
- 不得派发新的 bounded live window。
- Principal Task 7/12 lifecycle 及 multi-level unlock 继续阻塞。

建议总控下一步：
1. 派发仅限 acceptance strict join 的离线修复任务，不改 producer 策略行为。
2. 增加 malformed identity、零授权双 submission、跨矩阵 reason drift 对抗测试。
3. 修复后重跑 focused、完整 Hyperliquid 和 T011 byte-exact replay，再进入独立 QA。

提交信息：
- implementation commit：`ab8ce0e6e803fd82ded91d054371bd0a2c3f6905`
- business report commit：`ddbbfbf11896da2cf72768e6b6647490e3304eb9`
- QA commit：无
- 文件修改：无
