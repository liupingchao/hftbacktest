# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0601T005

状态：
- 已通过

更新时间：
- 2026-06-03 14:42 CST

验收线程：
- QA验收线程

验收对象：
- 业务线程-research 0601T005

验收范围：
- 验收 Binance-led Hyperliquid read-only pricing-signal runner 是否严格消费本地已验收 `0601T002/0601T003/0601T004` 产物、是否执行 `0601T004` 四特征 primary allowlist、是否分离 decision-time inputs 与 future labels、是否报告 nominal horizon 与 effective future age、是否生成全部 required artifacts，以及是否保持 no-private/no-order/no-live/no-strategy/no-parameter/no-default-on/no-tiny-live/no-promotion 边界。
- 本次 QA 不验收策略实现、私有/订单端点、order lifecycle、live/default-on/tiny-live、参数搜索、schema/connector/core API change 或 promotion。

验收步骤：
1. 阅读 `.workflow/tasks/0601T005.md` 和 `.workflow/reports/0601T005-business.md`。
2. 阅读 runner 和 focused tests：`examples/hyperliquid/binance_led_pricing_signal_runner.py`、`examples/hyperliquid/test_binance_led_pricing_signal_runner.py`。
3. 运行 help、py_compile、focused pytest、official manifest JSON parse 和 `git diff --check`。
4. 将 runner 复现输出到 `/tmp/qa_0601T005_pricing_signal_fc61757`，并与 official artifacts 的 row counts / allowlist / recommendation 对比。
5. 抽查 official `run_manifest.json`、`pricing_signal_feature_quality.csv`、`horizon_label_summary.csv` 和 `pricing_signal_rows.csv`。
6. grep private/order/live/strategy/parameter/default-on/tiny-live/promotion 相关命中，确认是否仅为禁止性说明、scope boundary 或 manifest flags。

实际结果：
- 业务报告状态为 `待验收`，符合进入 QA 的流程。
- `python examples/hyperliquid/binance_led_pricing_signal_runner.py --help` 通过。
- `python -m py_compile examples/hyperliquid/binance_led_pricing_signal_runner.py examples/hyperliquid/test_binance_led_pricing_signal_runner.py` 通过。
- `python -m pytest examples/hyperliquid/test_binance_led_pricing_signal_runner.py` 通过：`3 passed`。
- `/tmp` reproduction 通过，输出 `pricing_signal_rows=21541`，`primary_rows=3596`，`recommendation=keep_for_read_only_research`。
- `python -m json.tool local_live_analysis/binance_led_hyperliquid_pricing_signal_0601T005/run_manifest.json` 通过。
- `git diff --check` 通过。
- Official artifacts 与 `/tmp` reproduction 的 row counts 一致：input `3599`、primary `3596`、excluded `3`、pricing signal `21541`、feature quality `4`、horizon summary `30`、feature/regime `540`、venue conditioning `54`。
- Primary allowlist 抽查通过，仅包含 `binance_top5_imbalance`、`binance_microprice_minus_mid_ticks`、`binance_mid_move_ticks_from_prev`、`binance_top5_bid_qty`。
- `pricing_signal_feature_quality.csv` 只包含 4 个 `primary_allowlist` rows。
- `pricing_signal_rows.csv` 抽查通过：decision-time input fields 使用 `input_<feature>` / `input_<feature>_z`；future label fields 使用 `hyperliquid_future_*` / `basis_future_*`；未发现 `input_*future*` 字段。
- `horizon_label_summary.csv` 抽查通过：`30` rows = `5` labels x `6` horizons，且每个 horizon/label 都有 `effective_future_age_ms_min/mean/max`。
- Recommendation taxonomy 通过：official 与 `/tmp` reproduction 均为 `keep_for_read_only_research`。
- Boundary flags 全部为 true，QA 未发现 private/order/live/strategy/parameter/default-on/tiny-live/promotion 执行入口。

验收结论：
- 已通过
- 结论说明：
  - `0601T005` 满足 read-only pricing-signal runner implementation 任务合同；产物可作为继续只读 pricing research 的输入，但不授权策略实现、私有/订单端点、live/default-on/tiny-live、参数搜索或 promotion。

通过项：
1. Required outputs 齐全，official JSON/artifact checks 通过。
2. Runner 只读取 accepted local `0601T002/0601T003/0601T004` artifacts，未进行网络采集。
3. Primary rows 和 exclusion policy 符合 `0601T004` 合同。
4. Feature allowlist 严格限制为 4 个 Binance primary features。
5. Future labels 与 decision-time inputs 分离，未发现 future label 混入 input fields。
6. Horizon label summary 报告 nominal horizon 与 effective future age。
7. Recommendation 落在允许的 read-only taxonomy 内。
8. QA 未发现 private/order/live/strategy/parameter/default-on/tiny-live/promotion 越界。

不通过项：
1. 无

缺陷清单：
1. 无

阻塞项：
- 无

建议总控下一步：
1. 将 `0601T005` 标记为 `已通过`。
2. 继续保留 `single_public_sample_caveat=true`，不要把 `keep_for_read_only_research` 解读为 signal-ready 或 strategy-ready。
3. 如继续推进，下一步应是 read-only multi-sample public evidence expansion / robustness validation，仍不得进入 private/order、strategy/live/default-on/tiny-live、parameter search 或 promotion。

提交信息：
- business/artifact commit：`5362d3f` `Implement 0601T005 pricing signal runner`
- metadata commit：`fc61757` `Record 0601T005 commit metadata`
