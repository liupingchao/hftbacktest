# QA 验收结果

## Findings

1. P0：无。
2. P1：Direct-query producer/verifier 对 malformed result fail-open。`{"status":"unknownOid","orders":[...]}`、`{"status":"unknownOid","error":"endpoint failure"}` 和其他额外字段可被解释为 exact unknown，而任务要求 malformed result fail closed。
3. P1：History/reference metadata 未绑定到独立重建结果。History row 的 target token、alias token、`attempt` 和 `historical_row_classifications` 可与 deterministic synthetic reference/raw history冲突而仍通过。
4. P2：业务报告中的“无剩余 P-level findings”和“exact reconstruction”结论被本次独立 hostile QA 反例推翻。

执行线程：
- QA验收线程

任务ID：
- 0721T040

状态：
- 未通过

更新时间：
- 2026-07-21 21:57:37 CST

验收线程：
- QA验收线程

验收对象：
- 业务线程 `0721T040`
- implementation commit `76bea45046c21d99299b4801c82a262dec6b6e7d`
- report commit `2dac09314b967749bda4a5de8b3cf85e58441212`

验收范围：
- Delayed-history observe-only producer/verifier、exact profile launch boundary、live immutable evidence、账户安全、zero-side-effect proof和historical exact compatibility。

验收步骤：
1. 阅读 workflow manual、QA template、T040 task/business report、implementation/report commits及相关 producer/verifier/orchestrator/tests。
2. 独立运行 focused regression、完整 Hyperliquid regression和 live probe acceptance CLI。
3. 检查 source provenance、no-start preflight、pre/post account proof、service/child lifecycle、run checksum和transfer digest。
4. 对 direct result、history target、alias、attempt和classification构造 hostile artifacts并运行独立 verifier。
5. 复核 T037/T031/T026/T022/T016 historical exact boundaries和offline-only字段。

实际结果：
- Focused regression：`177 passed in 22.41s`。
- Full Hyperliquid regression：`1151 passed in 50.92s`。
- Live probe acceptance CLI exit `0`，原始 live artifact `14/14` checks pass。
- Live事实通过：五轮 raw `unknownOid`；history于`start+4.000137528s`开始且调用一次；empty history保持unknown；final snapshot于总预算`4.027979765s`完成。
- Pre/final open orders `0`；pre/post BTC position `0.0`；kill-switch clear。
- 唯一window；service success；child `rc=0`、reaped、无termination/SIGKILL。
- Source commit `76bea45` 的runtime `63` files hash exact；run checksum `14/14`；source/transfer archive digest与业务报告一致。
- Hostile direct fixtures `direct_extra_orders` 和 `direct_error` 均错误得到 `overall_acceptance=pass`、`blocking_checks=[]`。
- Hostile history fixtures `foreign_history_target`、`forged_history_classification`、`false_attempt` 和 `conflicting_alias` 均错误得到acceptance pass。
- Historical boundary复核：
  - T037：decision `39/4`、lifecycle `78/0`、blocked、offline-only。
  - T031：decision `43/0`、lifecycle `55/23`、blocked、offline-only。
  - T026：exact `1800s` invocation为decision `43/0`、lifecycle `78/0`、pass、offline-only。
  - T022/T016：decision `43/0`、lifecycle `66/12`、blocked、offline-only。
- QA初次记录的“T026 exit 2”使用了非exact `900s` expectation；总控以manifest/report的exact `1800s`参数复核后确认T026 boundary未改变。该项不列为实现缺陷。
- QA未执行任何live/private/account/network/remote/service操作；只读取immutable evidence并写入`/tmp` hostile输出。

验收结论：
- 未通过
- 结论说明：
  - Live execution/account safety和原始T040 artifact通过，但独立 verifier存在两个可确定复现的P1 fail-open，不能接受该机制证据合同。

通过项：
1. Exact source、archive、preflight、账户、service、child/reap和checksum evidence。
2. 原始live exact `5 direct -> 4.0s -> 1 history -> final snapshot`路径。
3. Zero order/cancel/submit/flatten/public-feed/manager/terminal participation和zero position delta。
4. Focused/full regressions及五份historical offline boundaries。

不通过项：
1. Direct raw result exact envelope。
2. History target/alias/attempt/classification independent binding。
3. 业务报告的no-finding结论。

缺陷清单：
1. Direct result必须使用exact allowlist，拒绝`error`、`order`、`orders`和任何未知字段。
2. History row必须验证`attempt=1`、target token/alias与synthetic reference一致，并从raw history独立重建classification后与producer字段exact比较。
3. 加入上述六个hostile regression，防止summary或metadata同步伪造。

阻塞项：
- T040不得升级为已通过。
- 不得启动第二个T040 window或任何dynamic spread/fill-feedback/inventory-skew/multi-level activation。

建议总控下一步：
1. 新建一个offline-only repair task，修复direct exact envelope和history metadata independent binding。
2. 使用immutable T040 artifact重跑修复后的verifier；不得重跑live。
3. 保持T037/T031/T026/T022/T016 exact boundaries，并在修复QA通过后再决定Task 8 completion。

提交信息：
- commit：implementation `76bea45046c21d99299b4801c82a262dec6b6e7d`；report `2dac09314b967749bda4a5de8b3cf85e58441212`
