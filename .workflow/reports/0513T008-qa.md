# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0513T008

状态：
- 已通过

更新时间：
- 2026-05-14 05:24 Asia/Shanghai

验收线程：
- QA验收线程

验收对象：
- 测试线程 + 0513T008

验收方式：
- 正常验收

验收范围：
- 验收 `5-13-day-control-30min` no-rule control 样本采集、T004 preflight/manifest、拉回归档、align/maker acceptance、T007 full-run sidecar/join 数据质量分类是否按任务完成。本 QA 不验收 T009 修复后的 sidecar 逻辑。

验收步骤：
1. 使用 `.workflow/runners/qa_task.py 0513T008 --pass ... --timezone Asia/Shanghai` 生成基础 QA 结果。
2. 读取 `.workflow/tasks/0513T008.md`。
3. 读取 `.workflow/reports/0513T008-business.md`。
4. 检查 preflight、manifest、采集、拉回、align、maker acceptance、T007 full-run sidecar/join、archive sha256 和样本分类证据。
5. 写入 `.workflow/reports/0513T008-qa.md` 并刷新看板。

实际结果：
- 任务文件存在：`.workflow/tasks/0513T008.md`
- 业务回报存在：`.workflow/reports/0513T008-business.md`
- 业务回报状态：`待验收`
- T004 preflight passed：`commit=f228950`，`dirty=false`，`compatibility.passed=true`
- start/stop marker 均存在，stop marker `exit_code=0`
- 采集样本：`5-13-day-control-30min`
- live rows：`63320`
- decision rows：`47499`
- `align_live_run.py` exit `0`
- `maker_acceptance.py` passed：`true`
- action/planned/reject/throttle match rate：`1.0 / 1.0 / 1.0 / 1.0`
- working-order semantic/blocking mismatch：`0 / 0`
- strict replay lag breach/drop/fail：`0 / 0 / 0`
- T007 full-run sidecar/join 已生成并报告质量指标。
- T007 full-run 负面质量结论已正确记录：`first_valid_update_aligned=false`，`gap_crossed_join_count=47499/47499`。
- 样本已正确分类为 limited `pricing_research_candidate`，仅用于 compressed action-path acceptance 和 limited BBO/bookTicker/compressed-mid sanity。
- archive sha256：`d7291afe0cb547663d4aa8e4cc9a175bfd06a3e7fcffea9d9eab82cc70b5c033`

验收结论：
- 已通过
- 结论说明：
  - 0513T008 QA 通过：该任务按 T004 标准完成 no-rule control 采集、拉回、align、maker acceptance、T007 full-run sidecar/join 和样本分类；它正确暴露了 T007 snapshot/bootstrap bug，并未错误升级样本为 top5 microprice / OFI proxy 可用。后续修复由 0513T009 验收。

通过项：
1. 业务回报已进入 `待验收` 状态。
2. 任务声明需要 QA 验收。
3. preflight/manifest/start-stop marker 证据完整。
4. live 样本、raw gzip、audit、logs、archive 和 sha256 证据完整。
5. align 与 maker acceptance hard gates 通过。
6. T007 full-run sidecar/join 指标已报告，且 `future_join_count=0`。
7. 对 `first_valid_update_aligned=false` 和 full-run `gap_crossed_join_count=47499` 的负面质量结论已明确记录，没有把样本错误解释为 top5 microprice / OFI proxy 可用。

不通过项：
1. 无

缺陷清单：
1. 无

阻塞项：
- 无

业务回报阻塞项：
- 无阻塞；但数据质量上存在后续必须解决的问题：T007 reconstructed top5 book 未通过 Binance first-valid snapshot/update alignment，导致所有 decision join 标记为 gap-crossed。该问题已进入并由 0513T009 修复。

建议总控下一步：
1. 将 `0513T008` 视为验收通过。
2. 继续以 `0513T009` QA 结果作为修复后 top5 sidecar 可用性事实源。

提交信息：
- commit：f228950
