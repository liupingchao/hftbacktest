# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0623T010

状态：
- 已通过

更新时间：
- 2026-06-24 15:32 CST

验收线程：
- QA验收线程

验收对象：
- 业务线程-live-awsserver1 0623T010

验收范围：
- 验收 `0623T010` 是否按 AWS public-only candidate funnel diagnosis 范围完成 600s no-submit public shadow、stage-level funnel 诊断、边界证明和产物校验；不验收 live order、private/order 或 canary。

验收步骤：
1. 复核 `.workflow/tasks/0623T010.md` 与 `.workflow/reports/0623T010-business.md`。
2. 复核 `local_live_analysis/hyperliquid_tiny_live_m2_aws_candidate_funnel_0623T010_20260623T064432Z/` 下的 public shadow manifest、boundary manifest、candidate audit 和 funnel summary。
3. 运行 focused watcher regression、`py_compile`、watcher CLI help、JSON/CSV/empty-file 检查和 `git diff --check`。

实际结果：
- AWS public-only no-submit run 完成 `600.000648s`，`duration_elapsed`，`subscription_ack_count=2`，`reconnect_count=0`。
- public stream counts：`l2Book=112`、`trades=1147`、`total_trade_event_count=3449`。
- candidate/shadow counts：`current_candidate_count=1259`、`shadow_evaluation_count=1259`、`shadow_would_submit_count=0`。
- funnel 诊断定位 first blocking stage 为 fresh-touch / dynamic-size gate：`fresh_touch_evidence_pass=2`，`fresh_touch_gate_allowed=0`。
- 主 blockers 为 `missing_touch_freshness_or_queue_reset_evidence=1257`、`missing_same_side_strict_through_support=960`、`missing_recent_same_side_at_or_through_throughput=205`。
- 因无 candidate 通过 fresh-touch，Binance freshness、fair-mid source 和 edge gate 没有被有效推进；`fair_mid_source_pass_count=0`、`edge_gate_pass_count=0` 是合理 fail-closed 结果。
- boundary manifest 记录 `credentials_read=false`、`private_endpoint_called=false`、`account_endpoint_called=false`、`order_endpoint_called=false`、`cancel_endpoint_called=false`、`live_client_initialized=false`、`real_orders_allowed=false`、`no_submit_enforced=true`、`next_real_canary_authorized=false`。
- 产物目录 `21` 个文件，空文件检查无输出；key JSON 校验通过。
- 验证通过：event watcher `37 passed`、`py_compile`、watcher CLI help、manifest JSON 校验、CSV 行数复核、空文件检查、`git diff --check`。

验收结论：
- 已通过
- 结论说明：
  - `0623T010` 满足任务级验收：AWS public-only candidate funnel diagnosis 已定位主阻塞层为 fresh-touch / BBO evidence，而非 Binance/fair-mid/edge；边界保持 no-submit/no-private/no-order。

通过项：
1. AWS public stream 有效，candidate funnel 产物可复核。
2. 第一阻塞层和 dominant reasons 明确。
3. 未放松 quote distance、cap、post-only 或 private/order/final-gate 边界。

不通过项：
1. 无

缺陷清单：
1. 无

阻塞项：
- forward blocker：`fresh_touch_gate_allowed=0`，需要后续检查 BBO history/cache visibility、queue-reset evidence 和事件排序/密度。

建议总控下一步：
1. 不应先放松 quote distance、cap、post-only 或 private/order 边界。
2. 应优先修复/诊断 public BBO history/cache continuity、queue-reset evidence 和 event ordering，然后再看 downstream edge。

提交信息：
- commit：无
