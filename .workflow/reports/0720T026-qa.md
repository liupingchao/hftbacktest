# QA 验收结果

执行线程：
- 独立只读 QA 验收线程

任务ID：
- 0720T026

状态：
- 未通过

更新时间：
- 2026-07-20 19:25 CST

验收对象：
- 业务线程 `0720T026`
- report commit `d518fabe877ebc96a7000689902676c4279ebbe0`
- source commit `40dc56a3225df4afb0f2185873c91f17b578f550`
- evidence root `local_live_analysis/principal_alignment_single_level_two_sided_0720T026/`

## Findings

1. **[P1] Producer/acceptance 无法接受同步明确的 post-only reject，错误阻塞完整生命周期。**
   - Buy response 是 authoritative terminal：`private_order_response_audit.json:7-38` 将 attempt `1`、side `buy`、attempt key、intent cloid token 与唯一同步响应绑定；响应外层 `status=ok`，唯一 status 是明确的 `Post only order would have immediately matched...`，manager action/state/query status 均为 `rejected`。
   - `order_intent_audit.csv:2` 的 cloid token 与上述 `intent_cloid_token` 一致。该绑定属于 request-context cloid binding；exchange payload 本身不回显 cloid，但来自单次同步 `client.order(intent)` 调用。
   - Manager 已把 `rejected` 定义为 terminal state：`hyperliquid_maker_order_manager.py:17-31,362-379,1483-1545`。
   - Producer 却把所有 endpoint attempts，包括 rejected buy，都加入 `tracked_refs`：`hyperliquid_tiny_live_m2_public_watcher.py:687-691`；随后只接受 cancel/history proof：同文件 `6703-6759`。
   - Acceptance 只解析 exact resting payload：`cross_exchange_t024_same_window_acceptance.py:530-597`；终态只允许 cancel/full-fill：`5214-5273`；并硬编码两侧 resting：`5338-5341`。
   - 结果是 `cancel_shutdown_proof.json:25-43` 将 buy 错报为 `authoritative_terminal_evidence_missing_for_reference`，尽管 sell 在 `98-120` 已有 exact oid/cloid-bound cancel success。
   - 同一缺口还导致 `m2_fill_window_manifest.json:251` 的 `post_only_reject_count=0`，与原始响应中的一个明确 reject 不一致。
   - 这是 P1 而非 P0：系统 fail-closed、账户终态安全，但正式证据合同误分类有效终态并阻塞 Principal Task 8。

2. **[P2] Fill-feedback 派生证据丢失真实 rejected buy。**
   - 实际 submitted buy 位于 `quote_attempt_matrix.csv:11`，status 为 `error`；但 `fill_feedback_lifecycle_matrix.csv:2` 将 attempt 1 标为 `not_submitted/excluded_not_submitted/rejected=false`。
   - `fill_feedback_quarantine.csv:2-10` 报告九个 `conflicting_duplicate_attempt_lifecycle`；`fill_feedback_snapshot.json:22` 因而记录 `rejected_count=0`。
   - 原因是 fill-feedback 直接读取包含多个候选评估的 `quote_attempt_matrix`（watcher `1597-1606`），normalizer 对同一 attempt key 保留第一行并隔离后续不同内容（online estimators `368-403`）。
   - 当前 activation 为 off，不影响本次下单；但在任何 fill-feedback/adaptive activation 前必须修复。

3. **[P2] Acceptance verdict 可复现，但 CSV 不是 byte-deterministic。**
   - `cross_exchange_t024_same_window_acceptance.py:4665` 直接序列化 `set(attempt_keys)`。
   - `PYTHONHASHSEED=1` 与 `5` 均 exit `2` 且计数相同，但 `decision_replay_comparison.csv` 中 attempt-key 顺序不同。
   - 应输出排序后的 list/稳定字符串，避免离线 acceptance checksum 随 hash seed 漂移。

## 实际结果

- Raw exchange facts 足以证明完整两侧终态：buy 为 request-bound explicit rejection；sell 为 resting 后 exact reference-bound cancel success。
- T026 满足 `done` 的“唯一窗口加首个 stop condition 如实保存”分支，但未满足正式 mechanism/evidence acceptance。
- Acceptance 可稳定复现 exit `2`：
  - provenance `112 pass / 0 fail`
  - config `72 pass / 0 fail`
  - decision `41 pass / 2 fail`
  - lifecycle `46 pass / 15 fail`
  - economics `6 pass / 0 fail`
  - mechanism/evidence integrity `fail`
  - `multi_level_activation_unlocked=false`
- Principal Task 8 不得解锁；不需要重跑 live，应先离线修复合同并复验同一不可变 evidence root。

## 安全与完整性通过项

1. Source：62 个 runtime 文件逐一匹配 source commit Git blob，mismatch `0`；source 到 `d518fab` 的 `examples/hyperliquid` diff 为空。
2. Envelope：exact one window、1800s cap、two-sided manager、Alo、0.005 BTC/order、0.01 BTC position、1 USDC loss、2 submissions、requote 2；config checks `72/72`。
3. Window：仅 `window_01`，运行 `2026-07-20T11:00:23Z` 至 `11:07:03Z`；无第二窗。
4. Account：pre/post open orders `0`、BTC position `0.0`、kill-switch clear、service inactive、conflicting process `0`。
5. Child/writer：child `rc=0`、reaped、无 SIGKILL；writer `351` successful、`0` failures。
6. Checksum：terminal manifest `104/104`，missing/mismatch `0`；本地 `sha256sum -c` exit `0`。
7. Offline boundary：network/remote/private/order/cancel 均为 `false`，未启动新 live window。

## 复跑命令与结果

```bash
python examples/hyperliquid/cross_exchange_t024_same_window_acceptance.py \
  --input-root local_live_analysis/principal_alignment_single_level_two_sided_0720T026 \
  --output-dir /tmp/0720T026-qa-acceptance.B6zLPg \
  --expected-task-id 0720T026 \
  --expected-source-commit 40dc56a3225df4afb0f2185873c91f17b578f550 \
  --expected-remote-run-root /home/admin/hftbacktest-cross-exchange-artifacts/principal_alignment_single_level_two_sided_0720T026/run \
  --expected-window-seconds 1800
```

- 结果：exit `2`，计数与已保存 acceptance 一致。

```bash
python -m pytest -q \
  examples/hyperliquid/test_hyperliquid_maker_order_manager.py \
  examples/hyperliquid/test_hyperliquid_tiny_live_m2_event_driven_watcher.py \
  examples/hyperliquid/test_cross_exchange_t024_same_window_acceptance.py \
  examples/hyperliquid/test_cross_exchange_fill_feedback.py
```

- 结果：`374 passed in 19.01s`。

```bash
cd local_live_analysis/principal_alignment_single_level_two_sided_0720T026/run
sha256sum -c remote_sha256_manifest.txt
```

- 结果：104 项全部 `OK`，exit `0`。

## 验收结论

- 未通过。
- T026 live 执行、安全终态和证据封存通过，但 P1 rejected-terminal 合同缺口使正式 mechanism/evidence acceptance 失败。
- Task 8、adaptive 和 multi-level 继续锁定。

## 建议总控下一步

1. 派发 offline-only repair，不修改 T026 raw artifacts、不启动新 live window。
2. 将每个 submitted attempt 接受为互斥终态路径：submit-response rejected、resting plus cancel、complete reference-bound fill，或 partial-fill plus cancel/fill reconciliation。
3. Rejected attempt 保留在 submitted-attempt ledger，但不得进入 cancel/history obligation；generic exception、malformed/multiple/conflicting status 仍保持 unknown。
4. 修复 reject count、fill-feedback canonical attempt selection 和 acceptance 稳定排序。
5. 增加 exact T026 mixed rejected/resting positive fixture及 malformed、token mismatch、multiple-status、forged-binding hostile tests，再对同一 evidence root 复跑 acceptance。

阻塞项：
- 无外部阻塞；P1 合同修复及独立 QA 通过前，Task 8 不得解锁。

提交信息：
- commit：无
- QA 未修改、暂存或提交仓库及 evidence root；离线复跑输出仅写入 `/tmp`。
