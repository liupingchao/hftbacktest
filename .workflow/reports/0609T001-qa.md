# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0609T001

状态：
- 已通过

更新时间：
- 2026-06-09 01:34 CST

验收线程：
- QA验收线程

验收对象：
- 业务线程-research / 0609T001

验收范围：
- 验收 `0609T001` 是否按任务边界完成 read-only basis-positive wrong-way decomposition and targeted sample design，并确认未采集新数据、未实现策略、未输出 case-library / shadow decision / private/order endpoint / live/default-on/tiny-live / promotion claim。

验收步骤：
1. 读取 `.workflow/tasks/0609T001.md` 和 `.workflow/reports/0609T001-business.md`。
2. 解析官方 artifact manifest：`local_live_analysis/canonical_basis_positive_wrong_way_decomposition_0609T001/basis_positive_wrong_way_manifest.json`。
3. 复跑 help、py_compile、focused pytest。
4. 复跑正式 runner 到 `/tmp/qa_0609T001_basis_wrong_way`。
5. 解析 `/tmp` manifest、baseline CSV 和 tail filter feasibility CSV。
6. 检查 business report、official recommendation 和 `/tmp` recommendation 的 forbidden-boundary 文本。
7. 运行 `git diff --check`。

实际结果：
- 官方 manifest 解析通过，`task_id=0609T001`，`schema_version=canonical_basis_positive_wrong_way_decomposition_v1`。
- T006 prerequisite 验证通过：`t006_final_recommendation=needs_more_samples`，`t006_scope_policy=not_limited_to_regime_011`，`t006_final_contract_decision=upgrade_to_context_only_supported`。
- Canonical source-lock 路径通过：`canonical_sample_count=3`，`diagnostic_rejection_count=0`。
- Baseline policy 为 `basis_positive_vs_basis_nonpositive_excluding_missing_or_non_numeric_basis`。
- Binance momentum policy 为 `input_binance_mid_move_ticks_from_prev_only_unavailable_bucket_if_missing`。
- 官方和 `/tmp` 复现结果一致：final recommendation 为 `targeted_collection_ready`。
- Baseline comparison：`basis > 0` rows `2425`，samples `3`，hit rate `0.94600939`，mean future move `45.67216495` ticks，wrong-way rows `69`，p95 wrong-way loss `138` ticks，max wrong-way loss `180` ticks。
- Baseline comparison：`basis <= 0` rows `7564`，samples `3`，hit rate `0.33853760`，mean future move `-16.94407721` ticks。
- Controlled support 通过：`binance_momentum_bucket=true`，`hl_book_state_bucket=true`。
- Top visible filter hypothesis 为 `basis_magnitude_bucket=basis_positive_small`，`wrong_way_count=42`，classification `promising_visible_filter`。
- Required artifact 文件齐全：manifest、baseline、magnitude bins、wrong-way rows、tail state decomposition、controlled effects、tail filter feasibility、targeted collection plan、next-step recommendation。
- Focused pytest 通过：`7 passed`。
- Boundary text check found required no-authorization language for no new collection and no executable/private/order/live/case-library/shadow/promotion authorization.
- `git diff --check` 通过。

验收结论：
- 已通过
- 结论说明：
  - `0609T001` 按任务边界完成只读 wrong-way decomposition 和 targeted sample design；`targeted_collection_ready` 被 artifact、报告和 `/tmp` 复现共同支持。该结论只允许后续单独派发采样任务设计/执行，不授权 T001 内采集、策略实现、private/order endpoint、case-library、shadow decision、live/default-on/tiny-live、parameter search 或 promotion。

通过项：
1. Runner 使用 accepted canonical event-mode artifacts 和 T006 artifacts，并验证 T006 prerequisite。
2. Missing / non-numeric basis 排除策略、Binance momentum 字段策略和 wrong-way 定义均写入 manifest。
3. Outputs 覆盖 required artifact 文件并使用允许的 final recommendation taxonomy。
4. Controlled checks、tail filter feasibility 和 targeted collection plan 均有 artifact 支撑。
5. Focused verification、`/tmp` 复现、boundary check 和 `git diff --check` 均通过。

不通过项：
1. 无

缺陷清单：
1. 无

阻塞项：
- 无

建议总控下一步：
1. 可将 `0609T001` 标记为 `已通过`。
2. 可正式派发 `0609T002`，但必须保持其边界：在 `awsserver1` 只采集 public raw data，拉回 local machine 后做本地 processing/testing。
3. 不要将 `basis > 0` 或候选 tail filter 提升为 strategy signal、case-library trigger、shadow decision 或 live/promotion basis。

提交信息：
- commit：`e4258b5`, `996c81c`
