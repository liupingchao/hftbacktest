# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0608T004

状态：
- 已通过

更新时间：
- 2026-06-08 16:19 CST

验收线程：
- QA验收线程

验收对象：
- 业务线程-research / 0608T004

验收范围：
- 验收 `0608T004` 是否按任务边界完成 read-only feature-conditioned signal validity diagnosis，仅评估 `regime_011_1000_spread_10_20_ticks` 的固定特征列表，并确认未输出 strategy、shadow decision、private/order endpoint、live/default-on/tiny-live 或 promotion claim。

验收步骤：
1. 读取 `.workflow/tasks/0608T004.md` 和 `.workflow/reports/0608T004-business.md`，确认任务边界、业务回报、artifact 路径和验证命令。
2. 解析 `local_live_analysis/canonical_feature_conditioned_signal_validity_0608T004/feature_validity_manifest.json`。
3. 检查 required artifacts 是否存在。
4. 复跑 `python examples/hyperliquid/canonical_feature_conditioned_signal_validity.py --help`。
5. 复跑 `python -m py_compile examples/hyperliquid/canonical_feature_conditioned_signal_validity.py examples/hyperliquid/test_canonical_feature_conditioned_signal_validity.py`。
6. 复跑 `python -m pytest examples/hyperliquid/test_canonical_feature_conditioned_signal_validity.py`。
7. 复跑正式 runner 到 `/tmp/qa_0608T004_feature_validity`，避免改写正式 artifact 时间戳。
8. 检查 `feature_conditioned_validity_report.md` 与 boundary flags。
9. 运行 `git diff --check`。

实际结果：
- Manifest 解析通过，`task_id=0608T004`，`schema_version=canonical_feature_conditioned_signal_validity_v1`。
- Assessment 限定为 `regime_011_1000_spread_10_20_ticks`。
- Canonical guard 结果为 `canonical_sample_count=3`、`diagnostic_rejection_count=0`。
- T002 prerequisite 为 `reject_not_maker_executable`，T003 prerequisite 为 `reject_directional_edge_unstable`。
- 固定特征范围为 `context_basis_mid_ticks`、`context_hyperliquid_top5_imbalance`、`context_hyperliquid_microprice_minus_mid_ticks`、`input_binance_top5_imbalance`、`input_binance_microprice_minus_mid_ticks`。
- Result 为 `final_recommendation=watch_needs_contract_visibility_clarification`、`valid_supported_pattern_count=0`、`valid_watch_pattern_count=0`、`invalid_pattern_count=21`。
- Strong `context_basis_mid_ticks > 0` pattern 有 `57` 行、`3` 个样本、`direction_hit_rate=0.94736842`、`net_edge_proxy_ticks=88.54385965`、`tail_risk_acceptable_proxy`，但在本任务中仍因合同 caveat 分类为 `invalid_not_decision_visible`。
- Hyperliquid context 和 Binance imbalance/microprice 强 pattern 因 redundancy 和/或 tail-risk proxy 被拒绝。
- Focused pytest 通过：`5 passed`。
- `/tmp` 复现输出一致：`final_recommendation=watch_needs_contract_visibility_clarification`。
- `git diff --check` 通过。
- QA 未发现本任务访问 private/order endpoint、实现策略/quote/order 行为、启动 live/default-on/tiny-live、运行参数搜索、生成 shadow decision、输出交易指令或 promotion claim。

验收结论：
- 已通过
- 结论说明：
  - `0608T004` 已按任务边界完成 read-only feature-conditioned signal validity diagnosis。结论 `watch_needs_contract_visibility_clarification` 有 artifact 支撑；该结论不授权 case-library、shadow decision、strategy、private/order endpoint、live/default-on/tiny-live、parameter search 或 promotion。

通过项：
1. Runner 使用 accepted canonical source-lock path，并拒绝 diagnostic-only synthetic formal input。
2. Runner 验证 T002/T003 prerequisite。
3. Runner 从 `multi_sample_manifest.json` `samples[].pricing_signal_rows` 解析 row-level inputs。
4. Assessment 限定为 `regime_011_1000_spread_10_20_ticks` 和固定特征列表。
5. Required artifacts 全部存在并可解析。
6. Final recommendation 使用任务允许 taxonomy。
7. Focused verification 全部通过。

不通过项：
1. 无

缺陷清单：
1. 无

阻塞项：
- 无

建议总控下一步：
1. 可将 `0608T004` 标记为 `已通过`。
2. `0608T004` 不支持 feature-conditioned case-library / shadow decision / strategy / live / promotion。
3. 可继续验收 `0608T005` 的 basis visibility / lineage diagnosis，以判断 `context_basis_mid_ticks` 的合同 caveat 是否能从 decision-visibility caveat 收窄为 execution-PnL caveat。

提交信息：
- commit：`6409f6b`
