# QA 验收结果

## Findings

1. P0：无。
2. P1：Verifier 接受 producer 不可能持久化的未脱敏 history 内层 identity。
   - Producer 会在 `examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py:738` 对 history rows 调用 `redact_with_reference_tokens`，因此有效 `oid/cloid` 必须持久化为 `<redacted>`。
   - Verifier 在 `examples/hyperliquid/cross_exchange_delayed_history_probe_acceptance.py:1315` 使用通用 `historical_reference_tokens`；`persisted_history_order_evidence_issues` 在 `:753` 仅检查 alias 是否存在，没有要求 alias 值必须为 `<redacted>`。
   - Hostile 复现：将有效 fixture 两份 history row 的 `order.oid/order.cloid` 从 `<redacted>` 改回原始值，同时保留匹配 token/alias map。
   - 结果：acceptance 错误返回 pass，`blocking_checks=[]`。
   - 复现输出：`/tmp/qa-0721T041-hostile/raw_nested_identity/acceptance/`。
3. P2：现有 verifier tests 未覆盖“有效 raw identity + 匹配 token/alias”形态，因此业务报告的“redaction/evidence surface 已 fail closed、无 P-level findings”结论不成立。

执行线程：
- 独立 QA 验收线程

任务ID：
- 0721T041

状态：
- 未通过

更新时间：
- 2026-07-21 22:57:19 CST

验收对象：
- implementation `2717d1c9a720dc6e8b399091f8008b840204e8eb`
- business report `9ee85c6145e93f2b2a9b9b7de0176df84276d46f`

验收范围：
- Direct exact envelope、strict attempt、target/alias、raw history classification、nested/outer identity、totality及producer/verifier parity。
- Immutable T040 evidence、六个指定 hostile fixture及五个历史 exact boundary。

实际结果：
- Focused regression：`187 passed in 20.78s`。
- Full `examples/hyperliquid`：`1197 passed in 51.08s`。
- Immutable T040 artifact：exit `0`，`14/14 pass`，`blocking_checks=[]`。
- 六个 T040 hostile fixture 均 exit `2` 并 blocked：
  - `direct_error`、`direct_extra_orders`：`direct_query_contract`
  - `foreign_history_target`、`forged_history_classification`：`history_result_envelope_contract`
  - `false_attempt`、`conflicting_alias`：direct/history contract blocked
- Non-dict、outer identity、false/null/empty marker/token presence均稳定 blocked。
- OID-only、`cloid=None`及`cloid=""` producer-to-verifier round-trip tests通过。
- 新增 QA hostile `raw_nested_identity` 错误通过，构成确定 P1。

历史 exact boundary：
- T037：exit `2`，decision `39/4`，lifecycle `78/0`，blocked。
- T031：exit `2`，decision `43/0`，lifecycle `55/23`，blocked。
- T026：exact `1800s`，exit `0`，decision `43/0`，lifecycle `78/0`，pass。
- T022：exit `2`，decision `43/0`，lifecycle `66/12`，blocked。
- T016：exit `2`，decision `43/0`，lifecycle `66/12`，blocked。
- 五次 manifest 均为 `offline_only=true`；network/private/order/cancel/remote/new-live均为false。

验收结论：
- 未通过
- Direct envelope、attempt、target/classification及指定 hostile 已修复，但 persisted history identity redaction合同仍可被绕过，不能关闭 evidence-integrity gate。

建议总控下一步：
1. 新建 offline-only repair task，要求每个 persisted nested identity alias 必须严格为 `<redacted>`，即使 raw value与token完全匹配也必须blocked。
2. 增加 raw OID、raw cloid及raw dual-identity verifier regressions，同时保持OID-only producer round-trip通过。
3. 使用 immutable T040 artifact复验，不需要第二个live window。

提交信息：
- implementation：`2717d1c9a720dc6e8b399091f8008b840204e8eb`
- report：`9ee85c6145e93f2b2a9b9b7de0176df84276d46f`
- QA：无提交；未修改仓库或immutable evidence，未运行live/private/network/remote/service。
