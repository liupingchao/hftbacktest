# QA 验收结果

## Findings

1. **P1 - Event `3053` 的 primary-cause evidence 合同不一致。**
   - Immediate guard 为 `fail_closed`，reason 为 `outside_quality_a_b_queue_bands;missing_intent_limit_px;missing_or_nonpositive_intent_size;missing_quality_bucket`。
   - Post-open-orders anti-drift 同时为 `block`，reason 为 `adverse_trade_pressure_with_recent_adverse_bbo`。
   - Trigger 和 attempt `guard_status/guard_reason` 选择 anti-drift；attempt 与 anti-drift-submit 的 `skip_reason` 选择 immediate guard。
   - 三个 validation reasons 均合理，分别检查 attempt/trigger、immediate-guard/trigger 和 anti-drift/trigger 的一致性；它们是同一个 producer precedence 缺陷的三个表现，不是三个独立缺陷。
   - 运行时保持 fail closed，没有导致未授权下单，但 mechanism/evidence integrity 因此失败。
   - 应修复 producer：集中定义 primary precedence，建议 `immediate fail_closed > anti-drift block > edge block > pass`，并在 trigger、attempt、submit-decision 全部复用。Acceptance 不应放宽；应在 producer 和 acceptance 测试中增加 simultaneous-failure 回归。如果改为显式 primary/secondary 字段，则需同步升级双方合同。

2. **P1 - 修复后的完整 delayed-history live endpoint path 未被验证。**
   - Raw terminal audit 中 direct query attempts 为 `0`，historical fallback attempts 为 `0`，历史调用和等待时间戳均不存在。
   - `4.0s/0.5s/5/1` 配置字段只能证明协议配置被持久化，不能证明等待、historical endpoint、result envelope 和 post-history snapshot 的真实 live 路径已经执行。
   - 因此 T037 不能声称完成 repaired delayed-history live validation。

3. **P2 - Hold observer 在 pump 尚未确认停止时仍记录 `pass`。**
   - Evidence 为 `pump_read_inflight_at_stop=true`、`pump_stop_acknowledged=false`、`pump_source_closed=false`。
   - 同时 deadline overrun 仅 `0.050477s`，低于 `0.25s` 合同上限；有 `18` 个公共事件、无 reconnect/disconnect，订单随后正确取消，child 最终已 reap。
   - 现有证据不能证明发生了 stop 后继续 reconnect 或跨线程修改 public state，因此不构成 T037 已发生的正式 stop condition。
   - 但 acceptance 完全忽略上述 pump 字段，不能证明 cancel 前 receive pump 已停止或 source 已关闭，属于独立 P2 evidence-contract gap。

4. **P0：无。**

执行线程：
- 独立 QA 验收线程

任务ID：
- 0721T037

状态：
- 未通过

更新时间：
- 2026-07-21 16:35:36 CST

验收线程：
- 独立 QA 验收线程

验收对象：
- Exact live source `597a5e478f291eea2c4e7178bd3bbe187b034589`
- Dispatch commit `3f92c7413a819c9a48ff1bd9e8f625189d2a1c4d`
- Business/report commit `eef70e56d4fe5b42deaab31fbe20f221166b562d`
- Immutable evidence `local_live_analysis/principal_alignment_estimator_observe_only_0721T037/`

验收范围：
- Exact source、single window、caps、activation-off、pre/post account proof、child/reap、terminal checksum 和 remote/local evidence equality。
- Raw submit/cancel/fill/account 的逐 reference terminal lifecycle。
- Confirmed interval、event-time exposure、leading censor、quarantine 和 estimator exact replay。
- Same-window acceptance、event `3053`、delayed-history实际调用边界和 hold pump shutdown evidence。
- 不支持 stable A/k、fill-rate、PnL、maker viability、dynamic activation、multi-level 或 promotion。

验收步骤：
1. 核验 workflow、任务、业务报告、前置 QA 和三个提交对象。
2. 独立核验 Git archive、runtime source blobs、single-window envelope、process/account proofs 和 checksums。
3. 从 raw response、intent、cancel、fill 和 account artifacts 重建两侧 terminal lifecycle。
4. 从 raw manager/public events 重建 interval、exposure、censor 和 quarantine。
5. 在 `/tmp` 重跑 exact estimator 和 same-window acceptance。
6. 审查 event `3053` producer/acceptance 逻辑及 hold observer shutdown 合同。
7. 运行 focused tests、内存 compile、Git diff/status checks。

实际命令与结果：
- `git archive --format=tar 597a5e... | sha256sum`：`d7d9c743...906564d`，与业务报告一致。
- Runtime provenance 与 exact Git blobs 独立比较：`62/62`，mismatch `0`；start/postrun verification 均为 pass。
- `sha256sum -c remote_sha256_manifest.txt`：`107/107` 通过；acceptance SHA manifest `8/8` 通过。
- Orchestrator events 仅有 window `01`；requested/completed 均为 `1`。Child `rc=0`、reaped、无 termination/SIGKILL，after-child open-orders proof 为 true。
- Envelope：`1800s` max、实际 `995.781109s`；`0.005 BTC/order`、`0.01 BTC` position、`1 USDC` loss、`2` submissions、`2` requotes、`3s` hold、`10s` wait均未超限。
- Pre/post proof：account scope一致，open orders `0`、BTC position `0.0`、kill-switch clear、冲突 service/process 为 `0`。
- Raw lifecycle独立重建：
  - Buy：exact post-only rejection，submit response直接提供reference-bound terminal proof。
  - Sell：exact resting oid/cloid，随后相同reference的cancel `success`。
  - References `2/2 pass`；未使用 account-wide absence 替代 reference proof。
  - Fill ledger `0`、pullback fills `0`、final open orders `0`、post position `0.0`。
- Manager/public rebuild：interval/exposure/censor/quarantine为 `1/3/1/0`；positive exposure `2.142s`，leading censor `438ms`，producer/independent exact-match。
- Estimator CLI 至 `/tmp/0721T037-qa-estimator.DilRPa`：exit `0`，event rows `7499`，snapshot SHA 双方均为 `6a61b876...f2358`，`snapshot_match=true`，activation/quote change均 false。
- Same-window acceptance 至 `/tmp/0721T037-qa-acceptance.VWmEeU`：exit `2`；provenance `112/112`、config `72/72`、decision `39 pass / 4 fail`、lifecycle `78/78`、economics `6/6`、optimism `6/6`。
- Four decision failures归并为 event `3053` 的三个 validation reasons及三个 producer summary 与独立重建不相等。
- Historical audit：direct `0`、history `0`；完整 delayed-history endpoint path未执行。
- Four-file focused pytest：`567 passed in 27.88s`。
- 四个关键 Python 文件内存 compile、`git diff --check`、cached diff check通过；工作区状态与验收前一致，immutable evidence未修改。

验收结论：
- **未通过**
- Execution/account safety、terminal reference proof、source provenance、single-window、estimator replay和activation-off均成立。
- 但 decision evidence primary-cause合同存在P1不一致，完整 delayed-history live path仍未实际执行；T037不能完成其主要 evidence gate。

通过项：
1. Exact source、single window、caps、pre/post account和process cleanup通过。
2. Buy submit rejection与sell cancel success均为authoritative reference-bound terminal proof。
3. `1/3/1/0` interval/exposure/censor/quarantine及snapshot exact replay通过。
4. Lifecycle `78/78`、economics和optimism边界通过。
5. Dynamic spread、fill feedback、inventory skew、multi-level和actual quote behavior change保持关闭。
6. 无稳定A/k、fill-rate、PnL、maker viability或promotion主张。

不通过项：
1. Event `3053` producer primary status/reason与skip reason不一致。
2. Historical fallback实际调用为零，不能证明完整delayed-history live path。

缺陷清单：
1. Producer没有统一 simultaneous guard failure 的primary-cause precedence。
2. Hold observer允许未acknowledge、未close的pump被记录为pass，acceptance也未校验这些字段。
3. Transfer archive digest `b6ab...` 仅见于业务报告；immutable evidence保留的是更细粒度的remote-sealed `107` 文件manifest及本地重验。

阻塞项：
- Mechanism/evidence integrity acceptance为 `fail`。
- Repaired delayed-history live endpoint path仍未验证。
- Dynamic activation、multi-level和Principal后续promotion继续锁定。
- T037不得启动第二个窗口。

残余风险：
- A/k两侧均不可用于稳定估计：buy insufficient observations，sell insufficient distance variation。
- Pump最终随child退出，但没有独立证据证明它在cancel前已acknowledge stop并关闭source。
- 本窗口零fill，不能支持fill-rate、fees/rebates、PnL或maker viability。

建议总控下一步：
1. 将 `0721T037` 标记为 `未通过`，保留immutable evidence。
2. 派发offline producer repair，统一primary precedence；acceptance保持fail closed，并增加dual-failure端到端测试。
3. 收紧hold合同：要求stop acknowledgment/source closure，或持久化可独立验证的bounded shutdown与post-stop no-reconnect proof。
4. 修复经独立QA后，另派fixed-quote single-window任务；可设计不参与terminal判定的delayed historical observe-only probe，确定性覆盖真实history endpoint、marker、timing和envelope。
5. 新live evidence通过前继续锁定所有adaptive和multi-level activation。

提交信息：
- commit：无
- QA严格只读；未修改仓库或immutable evidence，未提交，未执行private/account/order/cancel/remote/service/new-live操作。
