# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0604T011

状态：
- 已通过

更新时间：
- 2026-06-04 14:23 CST

验收线程：
- QA验收线程

验收对象：
- 业务线程-python + 0604T011

验收范围：
- `0604T011` 对 Binance live safety same-count open-order drift 的修复、`0604T010` case matrix 回归测试、业务报告与提交范围。验收重点是：同数量但 `open_order_diff` 非空的 side/price/qty/order-set drift 不再返回 `ok`，正常 quote key/id 边界不误报，且未扩大到策略、connector、生产配置、audit schema 或 `open_order_diff()` 元数据语义。

验收步骤：
1. 阅读 `.workflow/tasks/0604T011.md` 与 `.workflow/reports/0604T011-business.md`，核对任务边界、验收标准、业务结论与提交信息。
2. 抽查 commit `4d3c13c` 与 `b779caf`，确认改动范围只包含 T010/T011 task/report、`evaluate_live_safety()` gate 与 focused tests/report 回填。
3. 复跑 focused pytest：`python -m pytest examples/binance_tick_mm/test_backtest_tick_mm.py -k "evaluate_live_safety or open_order_diff"`。
4. 复跑 `0604T010` case matrix 一次性 Python 片段，确认前 7 个 diff 非空 case 进入 pending/confirmed mismatch，后 2 个边界 case 保持 `ok`。
5. 复跑常规测试文件集合：`python -m pytest examples/binance_tick_mm/test_*.py`。
6. 复跑任务指定的全目录命令：`python -m pytest examples/binance_tick_mm`，确认业务报告记录的环境阻塞是否复现。
7. 运行 `git diff --check`，确认无格式问题。

实际结果：
- `git show --stat --oneline 4d3c13c` 确认提交为 `fix binance live safety same-count order drift`，改动包含 `.workflow/tasks/0604T010.md`、`.workflow/reports/0604T010-business.md`、`.workflow/tasks/0604T011.md`、`.workflow/reports/0604T011-business.md`、`examples/binance_tick_mm/strategy_core.py`、`examples/binance_tick_mm/test_backtest_tick_mm.py`。
- `git show --stat --oneline b779caf` 确认只回填了 `0604T011` 业务报告中的 commit 字段。
- Focused pytest 通过：`19 passed, 138 deselected`。
- `0604T010` case matrix 复跑通过：
  - `single_side_drift_bid_vs_ask`: `open_order_mismatch_pending` / `open_order_mismatch`
  - `single_price_tick_drift`: `open_order_mismatch_pending` / `open_order_mismatch`
  - `single_qty_drift`: `open_order_mismatch_pending` / `open_order_mismatch`
  - `single_price_and_qty_drift`: `open_order_mismatch_pending` / `open_order_mismatch`
  - `two_orders_one_leg_price_drift`: `open_order_mismatch_pending` / `open_order_mismatch`
  - `two_orders_same_count_disjoint_set`: `open_order_mismatch_pending` / `open_order_mismatch`
  - `single_random_client_quote_key_different`: `open_order_mismatch_pending` / `open_order_mismatch`
  - `boundary_quote_key_same_id_different_expected_no_diff`: `ok` / `ok`
  - `boundary_status_exec_tif_different_same_quote_key`: `ok` / `ok`
- 常规 test 文件集合通过：`python -m pytest examples/binance_tick_mm/test_*.py` 结果为 `272 passed`。
- `git diff --check` 通过。
- `python -m pytest examples/binance_tick_mm` 复现业务报告所述环境/collection 阻塞：collect `examples/binance_tick_mm/run_env_test.py` 时 import `hftbacktest.data.utils.tardis`，numba cache 报 `RuntimeError: cannot cache function '_convert_depth': no locator available for file '/home/molly/anaconda3/lib/python3.13/site-packages/hftbacktest/data/utils/tardis.py'`。该错误发生在 test collection/import 阶段，早于本任务断言执行；常规 `test_*.py` 集合已覆盖 272 个测试并通过。

验收结论：
- 已通过
- 结论说明：
  - `0604T011` 满足任务验收标准：same-count open-order drift 已接入现有 live safety mismatch / confirmation 流程，`0604T010` 中 7 个 diff 非空 case 不再返回 `ok`，2 个边界 case 保持不误报，且未扩大 `open_order_diff()` 元数据语义或修改策略/connector/config/audit schema。全目录 pytest 的失败为当前环境 collection 阻塞，已由通过的 focused、case matrix 和 `test_*.py` 回归覆盖本任务验收范围。

通过项：
1. `evaluate_live_safety()` 在 `open_order_diff` 非空时进入现有 open-order mismatch 流程。
2. Pending 与 confirmed mismatch 均有回归测试覆盖。
3. `safety_detail` 保留非空 `open_order_diff`。
4. 正常 quote key 相同但 id/client id 不同的场景不误报。
5. status/exec/tif/cancel-request 等元数据字段仍保持本任务外边界，未被纳入 diff key 语义。
6. 未发现策略 quote/order、connector、生产配置、audit schema 或 compare 语义越界修改。

不通过项：
1. 无

缺陷清单：
1. 无

阻塞项：
- `python -m pytest examples/binance_tick_mm` 全目录命令受当前 Python/numba cache locator 环境问题阻塞；不影响本任务内容验收。可后续单独处理 `run_env_test.py` collection/import 环境问题。

建议总控下一步：
1. 将 `0604T011` 记为 `已通过`。
2. 如需扩展 status/exec/tif/cancel-request 等元数据 drift 语义，应另建任务，不要混入本次 same-count side/price/qty/order-set 修复。

提交信息：
- commit：4d3c13c（实现、任务与业务报告）；b779caf（业务报告 commit 字段回填）
