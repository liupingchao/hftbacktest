# QA 验收结果

执行线程：
- QA验收线程（第八轮全新独立验收）

任务ID：
- 0815T001

状态：
- 已通过

更新时间：
- 2026-08-15 18:44 CST（星期六）

验收线程：
- QA验收线程

验收对象：
- 业务线程-python/research + 0815T001 Round 7 bounded repair
- 正式包：
  `local_live_analysis/skhynix_trigger_aligned_episode_research_v1_stage02_density/`
- 声称 core SHA256：
  `7b3d06c3225f77c9929cdd3fa40d69c0866d83dbb75dd584dcb7db6db22ad3f8`
- Build A：
  `/tmp/0815T001-r7-density-a.hr0lg4/package`
- Build B：
  `/tmp/0815T001-r7-density-b.RTLgQF/package`

验收范围：
- 独立复核 Round 7 P2：Stage 1 scope 的 raw CSV path text 是否在
  `Path` normalization 前与 lexical rendering、resolved canonical
  text、frozen root、root/relative-path join 和实际 Stage 1 inventory
  共同闭合。
- 在全新 `/tmp` package copies 中构造 path/root 文本变化，刷新
  artifact records 和 core hash，再分别使用 current source CLI 与
  副本内 archived CLI 验证。
- 独立复核 Round 1-6 的 exact Stage 1 root、builder root gate、实际
  Stage 1 manifest/core/full inventory、projection v2、candidate/
  sensitivity coupling、small CSV、binding enum、Aug07 ledger exception、
  canonical report、manifest、exact float、runtime、source inventory 和
  boundary。
- 运行 focused tests、静态检查、formal verify-only、zero-write、
  Formal/A/B 比较、123 个 source 文件和 14 个 Stage 1 文件实际复算。
- 普通代码审查按 P0-P3 分级；影响 Stage 3 门禁可信度的问题必须判
  `未通过`。

验收步骤：
1. 阅读 `AGENTS.md`、workflow manual/templates、任务文件、
   business-r7、qa-round7、主实施方案和 controller tracking。
2. 静态审查 current source、archived runtime、frozen contract、
   input bindings、Stage 1 verifier 和 Round 7 新增回归。
3. 在 `/tmp/0815T001-qa8-path-matrix-final-p987ohbu/` 创建独立 package
   copies，逐案改写 path/root text 并重算 package identity。
4. 在 `/tmp/0815T001-qa8-prior-closure-o9ur3oma/` 独立抽查 Round 1-6
   admission closure。
5. 在 `/tmp/0815T001-qa8-builder-root-jbgw0v8t/` 独立检查 identical
   clone root 和 nonexistent root 的 builder fail-closed 行为。
6. 运行完整回归、Ruff、外置 pycache compileall、source/archive
   help 和 scoped `git diff --check`。
7. 使用独立 stdlib CSV/gzip/JSON/SHA 脚本流式复算 package、
   provenance、projection、membership、small CSV、boundary 和
   Formal/A/B identity。
8. 对正式包执行 source/archive verify-only，比较执行前后
   relative path、bytes、SHA256 和 mtime，确认 zero-write。
9. 对 Round 7 QA 对象逐文件比较并完成普通代码审查。

运行命令与结果：
1. `python -B -m pytest -q
   examples/hyperliquid/test_cross_exchange_trigger_density_core.py
   examples/hyperliquid/test_cross_exchange_candidate_episode_merging.py
   examples/hyperliquid/test_cross_exchange_trigger_density_inputs.py
   examples/hyperliquid/test_cross_exchange_trigger_density_admission.py`
   - `147 passed in 9.38s`
2. `python -B -m pytest -q
   examples/hyperliquid/test_cross_exchange_trigger_density_admission.py`
   - `96 passed in 3.11s`
3. `python -B -m pytest -q
   examples/hyperliquid/test_cross_exchange_liquidity_response_case_hierarchy.py
   -k 'merges_dense_clusters_without_recovery or
   stops_at_recovery_checkpoint or
   fails_closed_on_missing_recovery_evidence'`
   - `3 passed, 19 deselected in 0.07s`
4. `/Users/liu/.local/conda/bin/ruff check` 对四个 Stage 2 实现和四个
   测试文件。
   - `All checks passed!`
5. 使用外置
   `PYTHONPYCACHEPREFIX=/tmp/0815T001-qa8-pycache-compile`
   运行 `python -m compileall -q`。
   - `rc=0`
6. 对相同八个 Stage 2 文件运行 `git diff --check`。
   - `rc=0`
7. Source/archived `--help`。
   - 均为 `26` 行、`rc=0`，输出逐字节一致。
8. Source/archived 正式包 `--verify-only`。
   - 均为 `rc=0`、`verified=true`、`artifact_count=15`、
     `candidate_membership_rows=463612`、`confirmed_rows=267554`、
     `sensitivity_membership_rows=463612`、`session_count=3`。
9. 路径文本负向矩阵：
   `/tmp/0815T001-qa8-path-matrix-final-p987ohbu/`。
   - positive control：source/archive 均 `rc=0`。
   - 14 类变化、source/archive 共 `28/28` 次 `rc=2`：
     `/./`、重复分隔符、三重分隔符、尾随分隔符、尾随 `/.`、
     `..`、root 外绝对路径、相对路径、双前导分隔符，以及
     `stage1_package_path` 的 `/./`、重复分隔符、尾随分隔符、`..`
     和相对文本。
10. Round 1-6 生产大小 admission 抽查：
    `/tmp/0815T001-qa8-prior-closure-o9ur3oma/`。
    - binding unknown phase、unknown-row Aug07/outcome path、false report
      count、small-summary float drift、manifest extra key、noncanonical
      manifest JSON、boundary true 共 7 类。
    - source/archive 共 `14/14` 次 `rc=2`。
11. Builder root gate：
    `/tmp/0815T001-qa8-builder-root-jbgw0v8t/`。
    - identical Stage 1 clone 和 nonexistent Stage 1 root；
    - source/archive 共 `4/4` 次 `rc=2`；
    - 四次均未创建 output directory。
12. 独立数据审计：
    - 全量读取 `463612` 条 candidate 与 `463612` 条 sensitivity；
    - 实际读取并重哈希全部 `123` 个 source 文件与 frozen root 下
      `14` 个 Stage 1 文件；
    - 比较 Formal/A/B 全部 `16` 个文件；
    - formal verify-only 前后同时比较 bytes/SHA 和 mtime。

独立证据：
- Round 7 修复代码先保留 `raw_path`，再要求：
  - `path.is_absolute()`；
  - `raw_path == str(Path(raw_path))`；
  - `raw_path == str(Path(raw_path).resolve())`；
  - path 位于 frozen Stage 1 root；
  - path 等于 frozen root 与 relative path 的精确 join。
- `stage1_package_path` 在所有 binding rows 上先与 frozen root 原始文本
  精确比较；等价但非规范的 root 文本不能进入 inventory selection。
- 正式 Stage 1 scope 的每条 path 当前均为规范绝对文本，且实际文件
  inventory 与 binding before/after 精确相等。
- Source input before/after 均为 `123` unique files、
  `169,677,903` bytes、SHA256
  `94bad85bfd4981b351f84c53628099468ec27f13d308402ac7125a9d582a6644`；
  实际 size/SHA mismatch 为 `0`。
- Accepted Stage 1 before/after/actual 均为 `14` files、
  `1,401,387` bytes、full inventory SHA256
  `c540cc056313716b3bdd2b9c0fe076cda15a7f152b399ae6a1283b3aa8aa6590`；
  manifest artifact closure 成立，core 为
  `9c2756f966a1a05816f9e6a91d7361d76cfea10fc3c587944ca2f0c1d2eb7c96`。
- Formal、Build A、Build B 均为 `16` files、`19,834,728` bytes，
  full-directory inventory SHA256
  `bdade16c53aed7bba54fdb9408ba3a8a03036d183720f589bda2a762448a3833`，
  core 为声称值；全部 relative paths、bytes 和逐文件 SHA256 一致。
- 相对 Round 7 QA 对象
  `/tmp/0815T001-r6-density-a.VfHvoG/package`，仅变化：
  `density_manifest.json` 和
  `runtime_source/cross_exchange_trigger_density_admission.py`。
  `frozen_density_contract.json` 与其余 `14` 个文件逐字节不变。
- 五个 runtime source 文件的 current source、archived bytes 和
  manifest SHA 闭合为 `5/5`。
- Projection 为 `candidate_membership_projection_v2`，完整 `19` fields，
  session order 精确为 `jul30 -> aug03 -> aug04`。独立复算：
  - Jul30：`268522` rows，
    `cd731b5b011183366fd3cb04e0809d082a26bde85d7f1446ff4754016c3551f9`
  - Aug03：`127622` rows，
    `04cc9a13c85e530291bcf3c584e78ea029ed67fe1653bcdd7efa13e14294fda0`
  - Aug04：`67468` rows，
    `bf5911989a07f9b1242b9eef12e72a40093549f98c516c846df6caae353db820`
- Candidate/sensitivity identity、Family A/B eligibility、row grammar、
  sequential candidate IDs、window bounds、segment/epoch parentage均闭合；
  复算为 `110234` clusters、`43384` flows、`247` overlap blocks。
- Small CSV exact primary-key/cardinality/row-width：
  density `6`、inter-trigger `18`、episode summary `3`、sensitivity
  summary `36`、ESS `74`；无 missing、extra、duplicate 或 row-width error。
- Manifest exact key set、canonical pretty JSON、canonical report、exact
  numeric text、binding enum 和 Aug07 control-ledger exception 均由正式
  package verification、永久回归和生产大小负向抽查共同覆盖。
- 正式包 verify-only 前后 relative path、bytes、SHA256 和 mtime
  完全一致；无 package 内 `__pycache__`、`.pyc` 或新增路径。

普通代码审查：
- P0：无。
- P1：无。
- P2：无；Round 7 的 raw canonical path text fail-open 已关闭。
- P3：无。
- 剩余证据限制不是实现缺陷：本阶段仍只交付跨少数历史 session 的
  trigger-arrival/dependence structure，不提供 population replication、
  outcome、模型或 actionability 结论。

验收结论：
- 已通过
- 结论说明：
  - Round 7 P2 已关闭。Stage 1 path 的原始文本在 normalization 前被
    精确校验，`/./`、重复/尾随分隔符、`..`、相对路径、root 外路径和
    非规范 `stage1_package_path` 均无法 coherent-rehash 后保留
    `verified=true`。
  - Round 1-6 的 provenance、membership、summary、report、manifest、
    runtime、exact-value 和 boundary closure 均无回归。
  - 正式包、两次独立构建、实际 source/Stage 1 inventory 和完整
    membership projection 均复算一致，因此 `0815T001` 达到本阶段
    验收标准。

通过项：
1. Round 7 raw path text、resolved path、frozen root 和 actual inventory
   闭合通过。
2. Round 1-6 全部既有门禁与回归质量通过。
3. Formal/A/B determinism、source/archive verify-only、zero-write、
   runtime 和 provenance closure 通过。
4. 研究边界与统计证据限制保持不变。

不通过项：
1. 无。

缺陷清单：
1. P0：无。
2. P1：无。
3. P2：无。
4. P3：无。

阻塞项：
- 无。

边界结论：
- 正式 manifest 七项 boundary 全部为 `false`。
- Bindings 中唯一 Aug07 路径是 accepted Stage 1
  `consumption_ledgers/aug07_access_ledger.json` 的 before/after 两行；
  scope、role、root、relative path、bytes 和 SHA 均正确。
- Ledger 记录 `event_rows_opened=false`、`event_row_read_count=0`；
  Stage 1 manifest 记录 `aug07_full_event_rows_opened=false`。
- Membership headers 不含 response/outcome/markout/PnL/score/fill/
  actionability/private/order/cancel 字段。
- 本轮 QA 未读取 Aug07 raw/R0/R1/basis event rows，未运行 response、
  outcome、model、score、PnL、actionability 或 private/order/cancel
  工作。
- 本轮 `已通过` 只关闭 Ordered Research Queue 第 2 阶段；是否派发
  Stage 3 仍由总控依据本报告决定。

建议总控下一步：
1. 将 `0815T001` 记为 `已通过`。
2. 可按 Ordered Research Queue 派发独立 Stage 3 formal task：
   shared trigger contract 与 historical detector parity。
3. Stage 3 不得自动扩展到 Episode v3、outcome/model/actionability 或
   Aug07 event-row first-read。

报告文件一致性：
- 本报告与 `docs/qa-acceptance-report.md` 使用同一正文机械复制。
- 写入后执行 `cmp -s` 和 `shasum -a 256` 验证 byte-identical。
- 为避免文件自嵌自身哈希造成自引用变化，最终 SHA256 记录在 QA 完成
  回复中。

提交信息：
- commit：无
