# QA 验收结果

执行线程：
- 独立 QA 验收线程

任务ID：
- `0823T001`

状态：
- 已通过

更新时间：
- 2026-08-23 16:58 UTC

验收线程：
- 独立 QA 验收线程

验收对象：
- `SKHYNIX-H0B-PRIMARY-TUPLE-SUPERSESSION`
- candidate HEAD：
  `ecb99e52016375e73c5da4e55a29d16d495add74`
- business implementation：
  `912cd515eebcb258059158524e63bf53341b97c9`

验收范围：
- 在 fresh temporary worktree 中独立验证 reviewed plan、canonical
  Surface Matrix、dispatch pins、accepted H0-A、accepted `0822T002`
  latency measurement、frozen L1、tuple diff、Trust Kernel identity、
  exact package tree、atomic publication 和 zero-access boundary。
- business Build A/B 不作为 source-semantic oracle，仅在 fresh dispatch
  rebuild 完成后进行最终 byte-parity comparison。
- 不读取 H0-B/Stage 4 research outcomes、Aug07 event rows、raw market
  rows，不访问网络/private/order/cancel endpoint，不修改冻结合同或业务包。

P0/P1/P2/P3：
- `0 / 0 / 0 / 0`

验收步骤：
1. 核对候选 lineage、业务 commit 字段、dispatch commit
   `b9b3c02c1cbe774996982e5085df66b097a3255f`，并验证 plan/matrix/review
   SHA256 与任务 pins。
2. 使用业务 hostile evidence 运行 Gate 0，再生成 fresh current/frozen
   hostile receipt；额外逐项攻击五个 frozen L1 source SHA 和 full-path
   mapping key。
3. 在系统 `python3` 缺少 pytest 后，使用
   `/Users/liu/.local/conda/bin/python` 运行 focused suite、Ruff、
   compileall 和 diff check。
4. 仅复制三份 sealed L0 与五份 frozen runtime 到 isolated root，在
   `sandbox-exec` 的 `deny network*` 策略下运行 fresh L1 summarizer。
5. 使用 candidate source、fresh accepted inputs、fresh hostile evidence
   和独立 Git dispatch task snapshot 构建 fresh Build A/B/formal。
6. 独立检查 immutable H0-A fields、historical object SHA、完整 scenario
   roles、`1/2/14/0` diff、13-file/4-dir tree、R/C/E/composite 和
   fresh/business byte parity。
7. 对 authoritative formal package 和 fresh package 运行 zero-write
   admission；重试已存在的 final path，确认 fail-closed 且 tree 不变。

实际结果：
- Frozen pins 通过：
  - plan：
    `4e6aade687b1ce111412f00351f391b91ce2b7d51ec2d1a71acfeebd795cd20d`
  - matrix：
    `1c48e00744f3ec3b90592fdc270ef61fb91c1615fb00987fd651e2f1d3656467`
  - plan review：
    `73e4fbf41809e83bd3db415aaa3d839cbb888a9602f77bd9a4e7bf856cb74fa2`
  - dispatch task：
    `ecddc6045207701d0d4e83d2dadf63eda0ac7cf26fd5846361fef99dd653d75e`
- Gate 0 通过：`18 surfaces / 18 mutations / 7 exit criteria`；业务
  hostile negative evidence 和 fresh hostile evidence 均被接受。
- Fresh hostile 通过：`18 cases x current/frozen = 36/36`，
  fail-open `0`。五个 runtime dependency-SHA mutation 与 full-path key
  mutation 均返回
  `H0B_SUPERSESSION_L1_RUNTIME_DEPENDENCY_MISMATCH`。
- 系统 Python：
  `/Library/Developer/CommandLineTools/usr/bin/python3` 无 pytest。
  Conda Python 使用 pytest `8.4.2`，focused suite 为
  `8 passed in 14.88s`。Ruff、compileall、`git diff --check` 均通过。
- Accepted H0-A、`0822T002`、各自 QA/controller closure、kernel pins
  和 R/C/E/composite identities 全部逐字节匹配。
- 五文件 full-path runtime mapping canonical SHA256：
  `e29e0150e010d98dd5f196f596eaa6f288fe15243f95163f0fc7825ef338e8f7`。
- Isolated no-network L1 输出恰好四个文件，
  `latency_by_attempt.csv` SHA256 为
  `8ad174041046b3527eb65d969f976bcb5891f68b82568fd6a786a5d56285d4bd`，
  与 formal、L1-A、L1-B 字节一致。
- 独立 diagnostic 复算为
  `100 primary eligible / 81 retry_path=normal / rank 77 /
  833510us / 850ms`。
- 25 个 immutable H0-A fields 全部相等；historical
  `latency_observation_review` 和 `boundary` canonical SHA 分别为
  `871b501d...dd7`、`8350d5a2...680`。
- Tuple SHA256：
  `e3badf4c179a9e717ea49ff6617b637ec7e4c967c3dec365bebe78ef9399457c`。
  Diff 为 exact `1 primary-core / 2 scenario-role / 14 structural /
  0 undeclared`。
- `6600ms` 是唯一 primary；`850ms` 是 diagnostic-only 且
  `can_rescue_primary=false`；`100ms` 是 historical optimistic
  sensitivity 且非 primary。
- Fresh dispatch Build A/B/formal 均为
  `13 files / 4 dirs / 225987 bytes`，彼此完全一致，并与业务
  Build A/B/formal 的全部 13 个文件逐字节一致。
- Trust Kernel identities：
  - R：
    `08ada07165297f72dc05eec402bcfb70d748c6386986ec555b8c1b609e406079`
  - C：
    `a5f40d41226066291afcfc31d473cbadf8ed1edb322be6843b0f7aca45ea66b5`
  - E：
    `32ed6e541183683e2279860d9deef30ab7b0d230acff3ef84dd8e8f865632dc6`
  - composite：
    `5ec515e00ab2765a281084a64fbe0e1962727059e42734be33b7362258a22f76`
- Authoritative formal admission 和 fresh admission 均为
  `verified=true`、`zero_write=true`。
- Atomic publication 首次成功；第二次对已存在 final path 返回
  `PUBLICATION_FINAL_EXISTS`，package tree SHA
  `f94cbdfe...b611` 前后不变。
- Boundary 全部保持零/false：
  H0-B outcome、Stage 4、Aug07、raw market、network、private、order、
  cancel、new collection 和 live action 均未访问或执行。
- Business report 为 `待验收`，commit
  `912cd515eebcb258059158524e63bf53341b97c9`，message
  `research: publish h0b tuple supersession`，字段与 Git history 一致。

验收结论：
- 已通过
- 结论说明：
  - `0823T001` 的 outcome-blind tuple supersession 在独立 dispatch
    snapshot replay、frozen L1、tuple semantics、Trust Kernel identity、
    deterministic build、exact tree、atomic publication 和 zero-access
    boundary 上满足冻结合同。
  - 本 QA 只使结果具备 controller review 资格，不构成 controller
    closure，也不宣称 H0-B 已解锁。

通过项：
1. Gate 0、fresh hostile、focused tests、五文件 dependency mutations、
   isolated L1 和全部 frozen identity pins 通过。
2. Fresh Build A/B/formal 与业务 package 13/13 文件 exact byte parity。
3. `6600/850/100` 角色、`1/2/14/0` diff、R/C/E/composite、
   zero-write admission 与 atomic no-overwrite 通过。
4. 全程 zero outcome/network/private/order/cancel/live access。

不通过项：
1. 无

缺陷清单：
1. 无

阻塞项：
- 无

非阻塞观察：
- 当前 workflow task 已在业务 handoff 后从 dispatch 时的 `执行中`
  更新为 `待验收`。使用当前生命周期 task 重放会按预期改变 C/E identity；
  使用独立 Git dispatch snapshot
  `b9b3c02c1cbe774996982e5085df66b097a3255f` 后，13/13 package bytes
  与 authoritative business package 完全一致。

建议总控下一步：
1. 将本 QA 结果作为 `0823T001` 最新验收事实源。
2. 单独记录 controller closure，接受 exact tuple SHA 和
   R/C/E/composite 后，再判断 H0-B 是否可进入独立 reviewed plan/task。
3. 在 controller closure 前继续保持 H0-B outcome 锁定。

QA evidence：
- `.workflow/reports/0823T001-qa-evidence.json`
- `.workflow/reports/0823T001-qa-hostile-preflight.json`
- `.workflow/reports/0823T001-qa-input-pins.json`
- `.workflow/reports/0823T001-qa-build-receipt.json`
- `.workflow/reports/0823T001-qa-package-admission.json`

提交信息：
- commit：待本次 QA 提交
- message：`qa: accept 0823T001`
