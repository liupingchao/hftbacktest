# QA 验收结果

## Findings

1. **P0：无。**

2. **P1 - 嵌套 historical result 仍可绕过 producer 和 independent helper 的 delayed validation。**
   - Producer 在 `hyperliquid_tiny_live_m2_fill_window.py:3015-3025` 仅检查有效 historical 计数、canonical row 顶层 `method` 和 budget 字段。
   - Independent helper 在 `cross_exchange_t024_same_window_acceptance.py:3550-3560` 使用相同逻辑。
   - 将 direct query 的顶层 `method` 保持为 `query_order_by_cloid`，但把嵌套 `result.status` 改为 `historical_orders` 后，两者均返回 `status=pass / reasons=[]`。
   - 原因是 classifier 在 producer `:2566-2601`、independent `:3060-3092` 将该 method/result 冲突静默分类为 `unknown`，legacy direct-only 合同因此继续通过。
   - Historical attempt 的 `attempt` 缺失、bool、零或容器类型时，虽然整体 fail-closed，但不会产生 `terminal_audit_history_protocol_invalid`。Trigger 依赖已验证的 `historical_counts`，没有直接扫描任意 raw historical attempt。
   - 这不绕过 T034 Task 12 外层 rollout，但 producer/independent helper 仍未满足“任一 historical attempt/result 都触发 delayed validation”的任务合同。

3. **P2：无。**

执行线程：
- 独立 QA 验收线程

任务ID：
- 0721T034

状态：
- 未通过

更新时间：
- 2026-07-21 14:00:29 CST

验收线程：
- 独立 QA 验收线程

验收对象：
- implementation `fe8d725304ca8edf27b32de8f95322bce458154d`
- business report `3d7ed1b5c20cd8ca4eecac1e3beb5cabb63c2c03`

验收范围：
- Delayed validation trigger、marker fail-closed、Task 12 rollout、legacy direct-only compatibility。
- T033 exact timing、manager、quarantine 和历史边界。
- Live executor、quote、risk、submission 和 activation 不变性。

验收步骤：
1. 核验 workflow、任务、上一轮 QA、提交哈希及 committed diff。
2. 审查 producer、independent audit 和 Task 12 rollout。
3. 构造 marker、nested result、malformed attempt、非 dict row 和 attempt/result 冲突 probes。
4. 运行 focused、targeted 和 full Hyperliquid regression。
5. 在 `/tmp` 重跑 T031/T026/T016/T022 历史边界。

实际命令与结果：
- `git rev-parse fe8d725304ca8edf27b32de8f95322bce458154d 3d7ed1b`：哈希与验收对象一致。
- `python -m pytest examples/hyperliquid/test_hyperliquid_tiny_live_m2_fill_attribution.py examples/hyperliquid/test_cross_exchange_t024_same_window_acceptance.py -q`：`401 passed in 8.78s`。
- Marker/rollout targeted pytest：`6 passed, 206 deselected in 0.34s`。
- `python -m pytest examples/hyperliquid -q`：`1027 passed in 47.77s`。
- `python -m py_compile ...` 和 `git diff --check`：通过。
- Inline hostile matrix：legacy direct-only 通过；missing marker 加 delayed fields 明确失败且双方 reasons 一致；nested historical result 伪装为 direct 时双方错误通过。
- T031 estimator replay exit `1`：exposure `6/6`、censor `0/2`、quarantine `1/0`、`snapshot_match=false`。
- T031 acceptance exit `2`：provenance `112/112`、config `72/72`、decision `43/43`、lifecycle `55 pass / 23 fail`。
- T026 acceptance exit `0`：lifecycle `78/78 pass`。
- T016/T022 acceptance 均 exit `2`：lifecycle 均为 `66 pass / 12 fail`。

通过项：
1. Historical call 下 missing、empty、legacy、wrong、bool marker 的正常结构 case 均显式 fail-closed，producer/independent reasons 一致。
2. Zero history 但存在 delayed fields 时，missing/wrong marker 失败。
3. T034 Task 12 missing/wrong marker 和 zero-delay 均端到端 blocked，未发现 rollout 旁路。
4. 无 history、无 delayed fields 的 legacy direct-only v4 保持通过。
5. T033 exact `4.0/0.5/5/5.0/1`、manager、quarantine 及历史边界未回退。
6. 本提交未修改 watcher、executor、order manager、orchestrator、quote、risk、submission 或 activation 路径。

不通过项：
1. Direct method 下嵌套 `historical_orders` result 可通过两套 audit。
2. Malformed historical attempt 不会触发显式 protocol validation。

缺陷清单：
1. P1：Direct method 下嵌套 `historical_orders` result 可绕过 delayed validation。
2. P1：Malformed historical attempt 不会触发显式 protocol validation。

阻塞项：
- 修复通过独立 QA 前，不得启动新的 private read/live window。
- Adaptive 和 multi-level activation 继续锁定。

验收结论：
- **未通过**
- Task 12 最终 rollout 保持 fail-closed，但 producer 和 independent helper 仍存在 historical-result trigger fail-open。

建议总控下一步：
1. Trigger 应直接扫描全部 raw attempt/canonical rows，不依赖合法 attempt 计数。
2. `result.status=historical_orders` 与非 historical method 的组合应产生明确 method/result mismatch，并强制 delayed protocol validation。
3. 增加 nested result、malformed attempt、仅 marker、非 dict row 和 Task 12 集成回归后重新 QA。

残余风险：
- 完整 `4s` delayed-history 路径仍未由新的真实 live reference 验证。
- 修复通过前不得启动新的 private read/live window，不得解锁 adaptive 或 multi-level。

提交信息：
- commit：无
- QA 未修改仓库、提交或 `local_live_analysis`，未执行 live/private/account/order/cancel/network/remote/service 操作。
