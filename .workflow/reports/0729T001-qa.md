# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0729T001

状态：
- 未通过

更新时间：
- 2026-07-29 09:38 CST

验收线程：
- QA验收线程

验收对象：
- 业务执行线程 `0729T001`
- run `0729T001-20260729T005823Z`
- branch `cross-exchange`，HEAD `aee70284ad67`

验收范围：
- 严格核验任务边界、单 rack-spread group 的 7 台 `c6in.xlarge`、
  canary 与 900 秒/100 warmup 正式窗口、7/7 eligibility、双层
  raw-to-summary rebuild、冻结排名公式和真实胜者。
- 以 AWS 只读查询核验六台落选者、临时 bucket、胜者 protection、
  原 `awsserver1`、placement group 和最终 running 数量。
- 核验源码/证据哈希、Git staged scope、未跟踪 raw 边界、focused
  Python/Rust/shell/diff checks。
- 本 QA 未执行任何 AWS mutation，未 stop/start/reboot/terminate/tag
  任何实例。
- 本 QA 只写入本报告与 `docs/qa-acceptance-report.md`。

验收步骤：
1. 读取 `AGENTS.md`、workflow manual、QA template、任务文件、业务回报和
   controller 文档。
2. 核对 state journal、candidate TSV、full/smoke archives、compact
   manifests、source/artifact hashes 和 placement receipt。
3. 从 7 台 full raw 全量独立重建 Binance/Hyperliquid summary，并与原
   summary 做字节比较；另核对 remote rebuilt summary。
4. 独立重算五项冻结 rank、加权分和 tie break。
5. 使用 AWS CLI 只读查询实例、running 全集、placement group、
   termination protection、实例健康和 S3 HeadBucket。
6. 审阅 success/failure finalizer 与 focused test 覆盖。
7. 重跑 Python、Rust、shell syntax、fmt、clippy 和 diff checks。
8. 核对 staged 文件、未跟踪文件和 ignored raw 目录边界。

实际结果：
- task/run/region/AZ/机型/group 一致：7 台候选均为
  `c6in.xlarge`、`ap-northeast-1c`、同一
  `strategy=spread / spread-level=rack` group；启动顺序为 2 + 5。
- 两台 canary 均为 `60s / 10 warmup`，Binance traces 为
  `19,754/19,800`，Hyperliquid fast-L2 traces 均为 `111`；命令状态、
  完整性、零 drop 和双 venue remote rebuild 全部通过。
- full run 为 `7/7`、每台 `900s / 100 warmup`；所有 command、
  before/after clock、binary、metadata、benchmark 和双 venue integrity
  checks 均为 true。clock max bound 为 `412.394-655.988us`，均
  `<=750us`。
- Binance traces 为 `437,121-439,801`；Hyperliquid fast-L2 每台
  `1,662`；feed/local eligibility 均为 `7/7`。
- remote raw-to-summary 比较 `14/14` 字节一致；QA 使用本地 release
  probe 从全部 raw 再建，`14/14` 字节一致。
- 7 个 full archive 的 SHA256 与 bytes 同时匹配 state 下载 receipt 和
  `hunt_results.json`；compact evidence 与本地权威结果文件字节一致。
- 冻结公式和 tie break 与任务一致。QA 独立重算结果：
  `candidate-07 / i-0a962e47210528526 / score 31` 唯一第一；
  `candidate-05 / i-0e1ca020aedc8e712 / score 32` 第二。
- AWS 只读终态：
  - 原 `awsserver1 / i-02c64c088f311cbc1 / c7i.large` running，
    system/instance status 均为 `ok`；
  - 胜者 `i-0a962e47210528526 / c6in.xlarge` running，仍在原
    rack-spread group，winner tags 完整，`DisableApiTermination=true`，
    system/instance status 均为 `ok`；
  - 六台落选者均为 `terminated`；
  - `ap-northeast-1` running 实例恰好为上述两台；
  - 临时 bucket `hft-latency-hunt-0729t001-20260729t005823z`
    HeadBucket 明确返回 `404 Not Found`；
  - placement group 为 `available / spread / rack`。
- CloudTrail `LookupEvents` 只读查询因当前 IAM 身份缺少
  `cloudtrail:LookupEvents` 返回 AccessDenied；因此“全程无管理事件”未能
  通过 CloudTrail 做第三层证明，但 protected instance 前后 receipt、
  controller resource boundary 和当前 AWS 状态彼此一致。
- manifest 中 6 个 source hash、raw state/candidate TSV hash、结果和
  final receipt source hash均与当前文件一致。
- T001 task、business、实现、协议、compact evidence 和 controller
  文件均已 staged；1.4 GiB raw 目录受 `.gitignore` 管理且没有 raw 文件
  进入 Git。既有 `.DS_Store` 未 staged；无关
  `docs/qa-acceptance-report 3.md` 仍未跟踪且未纳入任务。
- QA 重跑结果：
  - Python focused：`22 passed`；
  - Rust：`15 passed`；
  - `bash -n`、Python compile、Rust fmt、clippy：通过；
  - worktree `git diff --check`：通过；
  - staged `git diff --cached --check`：失败，命中两处 EOF 空行。

验收结论：
- 未通过
- 结论说明：
  - 测量数据、冻结排名、真实胜者和当前 AWS 两服务器终态均成立，但
    success finalizer 的资源变更顺序违反任务合同，关键成功保留路径缺少
    指定 focused 回归，且 staged diff check 未通过；不能把本轮标记为
    `已通过`。

通过项：
1. 单一 regional rack-spread group、同 AZ/同机型 7 台和 2 + 5 启动事实。
2. canary 2/2、full 7/7、900s/100 warmup、完整 clock gate 和零 drop。
3. remote `14/14` 与 QA 本地独立 `14/14` raw rebuild。
4. 冻结公式、component ranks、tie break、score 31 和 candidate-07 胜者。
5. 六台落选者 terminated、bucket explicit 404、胜者 running/protected。
6. 原 `awsserver1` running，最终恰好两台 running，二者健康状态为 ok。
7. source/artifact hashes、compact evidence、ignored raw 边界和 staged
   任务文件范围。
8. Python/Rust/shell/fmt/clippy verification。

不通过项：
1. success finalizer 未按任务要求先标记并保护胜者、再终止六台落选者。
2. focused tests 未覆盖 success finalization 的唯一胜者保留、六台终止、
   winner tags/protection 和 protected baseline 排除。
3. staged diff check 失败，且业务回报错误声明 diff checks 已通过。

缺陷清单：
1. **P0：无。**
2. **P1：success finalizer 的 mutation 顺序违反冻结任务合同。**
   `latency-probe/ec2_spread_hunt.py:370-385` 先终止六台落选者并删除
   bucket，`387-406` 才给胜者写 tags 和启用 termination protection；
   任务要求顺序相反。当前 AWS 终态正确，但 staged 控制器仍不满足规定的
   exact-winner retention 安全顺序。
3. **P2：关键成功路径测试缺失。** 22 个 focused tests 没有调用
   `finalize_winner`，未验证成功时只保留唯一胜者、六台 loser
   termination、winner tags/protection、placement group 与
   `awsserver1` 排除；现有 `test_failure_cleanup_only_disables_task_candidate_protection`
   只覆盖 failure helper 的候选 ID 列表。
4. **P2：staged diff hygiene 失败且业务回报与事实不一致。**
   `git diff --cached --check` 报告
   `.workflow/reports/0729T001-business.md:88` 和
   `docs/evidence/ec2_latency_hunt_20260729/README.md:100`
   `new blank line at EOF`，而业务回报写为“diff checks：通过”。
5. **P3：测试名与实际 placement 语义不一致。**
   `latency-probe/test_ec2_hunt.py:87` 仍命名为
   `test_spread_group_is_host_level...`，实际断言和区域能力均为 rack
   level，容易误导后续审计。

阻塞项：
- 无。CloudTrail event-history 独立查询权限不足是审计限制，但不阻止对
  当前已知缺陷作出 `未通过` 裁决。

建议总控下一步：
1. 派发一个纯离线修复任务，不执行任何 AWS mutation，不改动或重启当前
   两台 running 实例。
2. 修正 finalizer 顺序并增加 success/failure 单元测试，明确断言
   `awsserver1` 永不进入 candidate cleanup/mutation 集合。
3. 清理两处 EOF 空行、修正业务回报和测试命名；重新运行 focused
   tests 与 staged diff check。
4. 修复后保留本次 runtime source hash 的真实 provenance，区分运行时源码
   与 QA 后离线源码；无需重跑 7 台 hunting，再走一次只读 QA。

提交信息：
- commit：无
