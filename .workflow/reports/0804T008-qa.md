# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0804T008

状态：
- 已通过

更新时间：
- 2026-08-04（星期二）

验收线程：
- QA验收线程

验收对象：
- 业务线程-python/three-session-commonality-execution + 0804T008

验收范围：
- 第二轮独立验收 directional-BBO 四项 formal gates。
- 重点复核第一轮三个缺陷：完整辅助状态轨、AMD runtime provenance
  closure、旧报告 superseded 边界。
- broader structural commonality controls 不在本轮 directional-BBO formal
  complete 声明内。

验收步骤：
1. 读取 workflow、任务、业务报告、研究计划、实现和全部正式 manifests。
2. 从冻结 raw 输入独立重建 v2 surrogate NPZ，并核对 payload/state SHA。
3. 独立重算 999 surrogate slots、七轨质量 accounting、BH 和正式 tier。
4. 核验 AMD runtime source、run manifest、remote/local SHA 和 surrogate 0。
5. 在隔离副本连续运行两次 finalizer，并核对全部 78 文件 SHA。
6. 运行 164 项联合回归、pip、gzip、Git diff 和 claim-boundary 检查。

实际结果：
- v2 解析 standard-L2、asset-context、main allMids 和 target-dex allMids
  完整 canonical state；每个 surrogate 在 native `t+lag` 使用 strict
  as-of 查询。
- 四条辅助状态轨的 availability 和 standard-L2 freshness 通过
  `_auxiliary_state_ready` 进入 `quality_eligible`，不是 retained-count-only
  审计。
- 从冻结 raw 独立 prepare 得到与正式输入相同的 NPZ SHA：
  `b25eba0bca870cf7620d5522417b8fae0cb176204a2ef05348ff1365bed8005c`。
- 正式输出包含 `119,880` hypothesis rows 和 `6,993` 七轨质量行；
  `999` IDs 每个精确 `60 primary + 60 secondary + 7 track rows`。
- 每个 ID 只有一个 lag；hypothesis invalid、track order、wrap、query、
  checksum 和 qualification-accounting failures 均为 `0`。
- missing state 独立重算为 `7,309`：main allMids `3,701`、target-dex
  allMids `3,608`；全部进入 qualification exclusion。
- qualification exclusions 独立重算为 `186,971`，其中 BBO stale
  exclusions 为 `179,662`，其余为上述 allMids 边界缺失。
- 独立重算正式 tier 为 `43 C1 / 15 C2 / 2 C3`；p/q 和正式表一致。
- 两个 C3 均为 `d_hb` level 对 `1000/2000ms` survival，p/q 分别为
  `0.007/0.042` 和 `0.005/0.0333333`。
- AMD 正式运行源码 SHA 精确闭合：
  worker `f266286...`、secondary `ce1010e...`、commonality dependency
  `23bac39...`；本地 archive、input manifest 和 run manifest 一致。
- finalizer 单独归档为 `65ef8b3...`；run manifest 精确绑定 input、
  hypothesis output、quality output 和三份 AMD runtime source SHA。
- remote/local input、hypothesis output、quality output 和 run manifest
  SHA 全部一致。
- 同 AMD 重跑 surrogate 0 后，`120` hypothesis rows 和 `7` quality rows
  与正式结果逐行一致。
- `commonality_manifest.json` 的 `77` 个非自引用记录全部通过 SHA/size
  对账；正式包共 `78` 个文件。
- 在隔离副本连续执行两次 finalizer，全部 `78` 文件 SHA 均无变化。
- `directional_bbo_confirmation.md` 只保留 superseded notice，并明确正式
  tier 仅来自 formal report 和绑定 manifests。
- 联合回归 `164 passed in 100.99s`；`pip check`、全部 gzip 和
  `git diff --check` 通过。
- claim boundaries 明确排除 causal venue leadership、exact fill、
  executable arbitrage、account-specific fee 和 PnL。
- `commonality formal_complete=false` 与
  `directional_bbo_formal_complete=true` 的边界清晰，不构成本轮阻塞。

验收结论：
- 已通过
- 结论说明：
  - 第一轮三个缺陷全部关闭，四项 directional-BBO formal gates 的实现、
    provenance、统计结果、确定性和声明边界满足本轮验收要求。

通过项：
1. adjusted/unadjusted effect-retention。
2. first-after-target。
3. separate Hyperliquid fast-L2 secondary family。
4. complete seven-track Hyperliquid multi-track surrogate v2。
5. AMD runtime source、run manifest 和 finalizer provenance closure。
6. `43 C1 / 15 C2 / 2 C3`、remote/local SHA 和重复构建确定性。
7. superseded report 与 claim boundaries。

不通过项：
1. 无。

缺陷清单：
1. P0：无。
2. P1：无。
3. P2：无。
4. P3：无。

阻塞项：
- 无。

建议总控下一步：
1. 关闭 0804T008。
2. 后续研究继续遵守 finite three-session receipt-time association 边界；
   不将 C3 扩展为因果领导、exact fill、可执行套利或 PnL 结论。

提交信息：
- commit：无
