# QA 验收结果

执行线程：
- 独立 QA 验收线程

任务ID：
- 0720T016

状态：
- 未通过

更新时间：
- 2026-07-20 10:20 CST

验收线程：
- 独立 QA 验收线程

验收对象：
- source commit `657bde21f590829fbe08e2fcba3126cec4010e9f`
- business report commit `991b8da18424c5f0767f7b95122ba27a3c891de4`
- `SINGLE-LEVEL-TWO-SIDED-1800S-BOUNDED-LIVE`

## 结论摘要

- 安全停止与账户终态：通过。
- single-level two-sided lifecycle：未接受。
- Task 8：不得解锁。
- 正式状态为“未通过”而非“阻塞”，因为现有证据足以确定验收标准未满足，不存在 QA 无法判断的外部条件。

## Findings

1. 严重：attempt 2 缺少 reference-bound authoritative terminal proof。
   - sell submission 确实到达 endpoint 并进入 resting。
   - cancel 结果为 `cancel_response_status_invalid`。
   - cancel reconciliation 为 `1/2`，`all_references_proven=false`，attempt 2 为 `fail_closed`。
   - 证据：`cancel_shutdown_proof.json:3-64,93-100`；`lifecycle_evidence_comparison.csv:26-27,41,45-46`。
   - account-wide open-orders=0 不能倒推出该特定引用获得了 authoritative cancel/full-fill 终态。
   - 直接违反任务文件第 119 行，因此 single-level lifecycle gate 未通过。

2. 高：最终 operator `live_status` 与独立账户终态不一致。
   - `live_status.json` 保留 sell `unknown`、`owned_open_order_count=1`、working sell `0.005`。
   - after-child proof 和 post-live account proof 均为 open orders `0`，BTC position `0.0`。
   - 证据：`live_status.json:346-401`；`independent_remote_open_orders_check.json:3-7`；`post_live_account_proof.json:5,16,34,47`。
   - 账户安全终态成立，但 operator 状态未完成 terminal reconciliation，不能作为最终控制面事实源。

3. 中：decision replay 的 attempt cardinality contract 自相矛盾。
   - `quote_attempt_matrix.csv` 有 4 行：2 行 no-submit candidate evidence、2 行真实 submitted attempts。
   - 同一 acceptance 已确认 `candidate_attempt_evidence_rows=4`、`manager_attempt_identity_count=2`、`submitted_attempt_count=2`，随后又直接用 `len(attempts)==2`，产生 false-negative。
   - 证据：`decision_replay_comparison.csv:13-18,30-31`；acceptance 实现 `cross_exchange_t024_same_window_acceptance.py:3509`。
   - 该问题不否定两侧真实 submission，但必须修复后重放，当前 same-window acceptance 仍为 fail。

4. 低：业务报告整体准确，但第 37 行的结束原因表述不够精确。
   - watcher 的 close reason 是 manager cycle complete；terminal reconciliation 随后产生 fail-closed stop condition。
   - 建议表述为“manager cycle 完成后，终态对账触发首个 fail-closed stop condition”。
   - 报告同时未记录自身 business report commit `991b8da`，只记录了 source commit 信息。

验收范围：
- Exact source、remote root、task/window、duration、envelope 和 activation。
- 唯一 live window、两侧 submission、cancel/fill/reference terminality。
- Child、writer、heartbeat、checksum 和独立账户终态。
- Same-window acceptance 独立重放与业务报告准确性。
- 全程只读；禁止 live、private/account endpoint、remote 和 service 操作。

实际结果：
- source commit 存在且是 business commit 祖先；相关运行/acceptance 代码在两者之间无差异。
- 唯一窗口、外部 duration `1800s`、两侧 manager、Alo、size/position/loss/submission caps 全部匹配。
- dynamic spread、fill feedback、inventory skew、multi-level 和 quote behavior activation 均未开启。
- 实际提交 buy/sell 各一笔，均为 `0.005 BTC`，均进入 resting；无 post-only reject、无 fill。
- child `rc=0` 且已 reap；未 SIGKILL。
- sealed post-child/account evidence 为 open orders `0`、BTC position `0.0`、estimated loss `0.0`。
- writer 自报 healthy，writes `173`、failures `0`；heartbeat 距 run completion 约 5 秒。
- terminal SHA-256 本地独立复算 `104/104` 全部通过。
- attempt rows `4`、submitted rows `2`、skipped rows `2`、intent sides `{buy,sell}`、cancel proof `1/2`。
- 禁用文件写入后的进程内 same-window acceptance 重放与保存结果一致：
  - provenance `112/0`；
  - config `72/0`；
  - decision `42/1`；
  - lifecycle `49/12`；
  - final recommendation `principal_task12_same_window_acceptance_blocked`。
- 未启动第二个窗口，首个 stop condition 被保守记录，未作经济性或 promotion 乐观推断。

验收结论：
- 未通过
- 结论说明：
  - T016 正确地按首个 stop condition 安全结束，但没有完成可接受的 single-level two-sided reference-bound lifecycle。

通过项：
1. Exact source/root/task/window/duration/envelope/activation。
2. 两侧真实 post-only submission 和账户终态安全。
3. Child/writer/heartbeat/checksum/provenance。
4. 无第二窗口、无策略放宽、无乐观经济性结论。

不通过项：
1. Attempt 2 缺少 authoritative reference-bound terminal proof。
2. Final operator status 未完成 terminal reconciliation。
3. Decision acceptance primary-attempt cardinality 使用了 candidate row 集合。

缺陷清单：
1. Cancel unknown 路径未保留足够的原始响应与 reference-bound terminal query 证据。
2. Child 退出前未用最终 reconciliation 强制刷新 `live_status`。
3. Acceptance `attempt_row_count` 对全部 candidate-attempt rows 执行 exact-two。

阻塞项：
- Task 8、dynamic spread activation、fill feedback activation、inventory-skew activation 和 multi-level 继续锁定。

建议总控下一步：
1. 派发 offline-only 修复：cancel raw result、按 oid/cloid 的 terminal status query、terminal `live_status` refresh、submitted-attempt cardinality。
2. 先重放 T016，证明 decision false-negative 消除；历史 attempt 2 仍不可补造 terminal proof。
3. 修复经独立 QA 后，使用新的正式 bounded-live 任务取得完整两侧 terminal proof，再决定是否解锁 Task 8。

提交信息：
- commit：无；本次为只读验收，未编辑、提交、访问远端/private endpoint 或启动 live。
