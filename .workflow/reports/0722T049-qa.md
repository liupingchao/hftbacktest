# 0722T049 QA Report

执行线程：
- QA验收线程（Franklin）

任务ID：
- 0722T049

状态：
- 阻塞

验收时间：
- 2026-07-22

## Findings

### P0

- 无。

### P1

1. Attempt 1 lacks authoritative terminal proof. The cancel evidence is an ambiguous generic match; five direct `oid/cloid` query rounds plus one historical query all remain `unknown`. The per-reference reconciliation therefore records `authoritative_terminal_evidence_missing_for_reference`, and the producer remains `no_fill_unproven` / `fill_reconciliation_required_no_fill_unproven`.
2. The exact fill-feedback profile records `lead_source=unspecified` in preflight. `two-sided-fill-feedback-manager` was not included in the Binance lead profile set, so acceptance correctly rejects the sealed preflight, which should have recorded `binance_public_book_ticker`.

### P2

- The focused fill-feedback orchestrator test does not assert `lead_source`, so it did not catch the profile contract defect.

## Verification

- Focused acceptance tests: `246 passed`.
- Full business-thread result: `python -m pytest examples/hyperliquid -q` -> `1223 passed`.
- Runtime source: `63/63`.
- Terminal checksum: `110/110`.
- Estimator replay: `299` events, exact snapshot match.
- Fill-feedback replay: `2` lifecycle rows, exact snapshot match.
- Acceptance: exit `2`; provenance `113/0`, config `82/1`, decision `43/0`, lifecycle `66/12`, economics `6/0`, optimism `6/0`.
- Live facts: one window, `2` submissions, `0` fills, final open orders `0`, BTC position `0.0`, loss `0.0 USDC`.
- Fill feedback was requested, but target was absent; candidate was `unavailable_neutral` with explicit target-missing reason and fixed fallback. Actual quote behavior did not change.

## Decision

`阻塞`

The fail-closed acceptance is correct. Final empty open orders/account state cannot substitute for a reference-bound terminal proof. The sealed T049 evidence must not be modified or reinterpreted, and no second live window should be started under T049. A new formal offline repair task is required for the lead-source contract/test gap; any new live must belong to a new formal task.
