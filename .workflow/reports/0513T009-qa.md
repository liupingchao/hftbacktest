# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0513T009

状态：
- 已通过

更新时间：
- 2026-05-14 05:24 Asia/Shanghai

验收线程：
- QA验收线程

验收对象：
- 业务线程-python + 0513T009

验收方式：
- 正常验收

验收范围：
- 验收 T007 standalone sidecar 的 Binance local book snapshot/bootstrap 修复：snapshot 到达后必须使用 snapshot 前已缓冲 depthUpdate 中满足 `U <= lastUpdateId + 1 <= u` 的第一条 update，并从该 update 开始按 `pu == previous_u` 连续应用。验收固定使用既有 `5-13-day-control-30min`，不重新采集 live。

验收步骤：
1. 使用 `.workflow/runners/qa_task.py 0513T009 --pass ... --timezone Asia/Shanghai` 生成基础 QA 结果。
2. 读取 `.workflow/tasks/0513T009.md`。
3. 读取 `.workflow/reports/0513T009-business.md`。
4. 检查 focused tests、T009 full-run sidecar metrics、decision join metrics、样本分类、禁止范围和提交信息。
5. 复核 `5-13-day-control-30min` 中 top5 sidecar/join 已对齐字段与未对齐字段。
6. 写入 `.workflow/reports/0513T009-qa.md` 并刷新看板。

实际结果：
- 任务文件存在：`.workflow/tasks/0513T009.md`
- 业务回报存在：`.workflow/reports/0513T009-business.md`
- 业务回报状态：`待验收`
- 代码提交：`6710b5f fix(binance): replay buffered depth bootstrap`
- workflow 回报提交：`e7fee40 docs(workflow): report T009 bootstrap fix`
- focused tests：`python -m pytest examples/binance_tick_mm/test_binance_top5_provenance.py` -> `8 passed`
- full raw gzip build-sidecars 已在 `local_live_analysis/5-13-day-control-30min/t009_fixed_sidecar/` 生成。
- decision join 已在 `local_live_analysis/5-13-day-control-30min/t009_fixed_sidecar/joined_decisions.csv` 生成。
- 没有启动 live，没有重新采集，没有策略行为变更，没有 core/connector/API 变更，没有 standard npz 主事件 schema 变更。

已对齐字段和链路：
- Binance first-valid snapshot/update bootstrap 已对齐：
  - snapshot `raw_seq=6`
  - `snapshot_lastUpdateId=10537138804218`
  - buffered first valid update `raw_seq=5`
  - `depth_U=10537138802913`
  - `depth_u=10537138805036`
  - 覆盖 `lastUpdateId + 1 = 10537138804219`
- 后续 depth 连续性已对齐：
  - snapshot 后 `raw_seq=7`
  - `pu=10537138805036`
  - 接上 buffered `raw_seq=5` 的 `depth_u=10537138805036`
- sidecar sync 状态已对齐：
  - `first_valid_update_aligned=true`
  - `depth_pu_mismatch_count=0`
  - full-run `sync_gap=true` rows：`0`
  - post-snapshot aligned depth rows 中 `sync_aligned=false` rows：`0`
- raw/npz mapping 已对齐：
  - final `data` row mapping coverage：`1.0`
  - standard npz dtype 仍为 `ev|exch_ts|local_ts|px|qty|order_id|ival|fval`
- decision as-of join 已对齐：
  - decision count：`47499`
  - joined decision count：`47499`
  - `decision_join_coverage=1.0`
  - `future_join_count=0`
  - `join_missing_count=0`
  - `gap_crossed_join_count=0`
  - top5 join age p50/p90/p99 ms：`13.602237 / 24.205795400000003 / 28.13446387999999`
- bookTicker/depth BBO 大体对齐但非完美：
  - bookTicker/depth BBO match/mismatch：`67307 / 11`
- provenance/source 字段存在且来源清晰：
  - live decision rows：`market_view_source=live_depth`
  - live decision rows：`top5_source=live_depth`
  - sidecar rows 保留 `raw_seq`、`depth_U`、`depth_u`、`pu`、`snapshot_lastUpdateId`、sync flags、startup flags、bookTicker/depth BBO match/age。

没有对齐或不能宣称对齐的字段：
- live audit top5 与 T009 sidecar reconstructed top5 的逐行精确字段没有完全对齐：
  - best bid tick match：`30787 / 47499`
  - best ask tick match：`30780 / 47499`
  - bid full top5 ticks match：`8898 / 47499`
  - ask full top5 ticks match：`12393 / 47499`
  - bid top5 qtys match：`8600 / 47499`
  - ask top5 qtys match：`11579 / 47499`
  - bid+ask full top5 ticks 全匹配：`3539 / 47499`
  - bid/ask ticks+qtys 四个 top5 字段全匹配：`3324 / 47499`
- `join_stale` 不是 0：
  - stale join count：`432`
  - max top5 join age ms：`251.033249`
  - max bookTicker join age ms：`603.705003`
  - stale 主要来自 `max_join_age_ms > 250`，尤其 bookTicker age。
- T009 不证明：
  - full L2 equivalence
  - exact queue position
  - queue/fill model correctness
  - live audit top5 与 sidecar top5 逐行完全一致
  - 策略收益或 live promotion readiness

验收结论：
- 已通过
- 结论说明：
  - 0513T009 QA 通过：T007 snapshot/bootstrap bug 已按 Binance local book 规则修复；T009 full-run sidecar 在 `5-13-day-control-30min` 上满足 `first_valid_update_aligned=true`、`depth_pu_mismatch_count=0`、`decision_join_coverage=1.0`、`future_join_count=0`、`gap_crossed_join_count=0`。该样本可升级为 top5 microprice / top5 OFI proxy / top5 imbalance pricing research candidate；但 live audit top5 与 sidecar reconstructed top5 的逐行 ticks/qtys 并未完全对齐，且本任务不证明 full L2、exact queue 或 queue/fill model。

通过项：
1. 业务回报已进入 `待验收` 状态。
2. 任务声明需要 QA 验收。
3. 业务回报包含 focused tests、full-run build-sidecars、join-decisions 和 dashboard 验证证据。
4. T007 fatal failure 已消除：`first_valid_update_aligned=true` 且 full-run `gap_crossed_join_count=0`。
5. sidecar 保留 required provenance fields：`raw_seq`、`depth_U`、`depth_u`、`pu`、`snapshot_lastUpdateId`、sync flags、startup flags、bookTicker/depth BBO match/age。
6. sample classification 已使用 fixed sidecar 更新，边界说明清楚。
7. 未违反禁止范围：无 live、无重新采集、无策略变更、无 core/connector/API 变更、无 standard npz schema 变更。

不通过项：
1. 无

缺陷清单：
1. 无

阻塞项：
- 无

业务回报阻塞项：
- 无

建议总控下一步：
1. 将 `0513T009` 视为验收通过。
2. 不建议继续在 Step 2 里追求 live audit top5 与 sidecar top5 逐行完全一致；建议进入 Step 3 market-view acceptance gate，把 top5 tick/qty match、BBO drift、source fields、stale age 和 startup-excluded quality 做成正式阈值与分类。
3. 后续如需让策略实时消费 update-id/top5 provenance，应另开 connector/core API 或 audit schema 集成任务。

提交信息：
- commit：6710b5f
