# QA 验收结果

执行线程：
- 独立 QA 验收线程

任务ID：
- 0804T001

状态：
- 未通过

更新时间：
- 2026-08-04 11:27 CST

验收线程：
- 独立 QA 验收线程

验收对象：
- 业务线程-python/public-data-infra + cross-exchange-research / 0804T001

验收范围：
- continuous single-segment、collection-only、startup warmup、target-time
  strict historical as-of 标签、focused tests、旧 5H replay、新 2H
  campaign/R0/R1，以及 SHA、degraded interval 和云端计算边界。

实际结果：
- 核心实现和数据语义通过；focused tests 为 `71 passed`。
- 旧 5H 与新 2H 均无 future join、跨 segment 或 provenance 缺口。
- 新 2H raw、R0 和 R1 数据内容成立。

验收结论：
- 未通过
- 结论说明：
  - 正式证据启用了零实际 interval 的 bounded stale waiver；最终源码
    与 collection runtime SHA 未做 phase-specific closure；历史云端
    execution-mode 误标未形成结构化 reconciliation。

通过项：
1. continuous collection 与 collection-only。
2. startup warmup reconciliation 修复。
3. target-time historical as-of 标签。
4. 旧 5H replay 与新 2H 数据内容。

不通过项：
1. 正式 postprocess 需关闭 stale waiver。
2. collection/postprocess/R0/R1 源码 provenance 需分别封存。
3. 云端历史 execution-mode 误标需保留并闭合。

缺陷清单：
1. P1：正式新 2H postprocess 证据启用了 bounded stale waiver。
2. P2：最终源码与 phase-specific runtime provenance 未闭合。
3. P2：云端历史状态的 execution mode 标注错误。

阻塞项：
- 无；同一批 raw data 可完成整改，无需重新采集。

建议总控下一步：
1. 默认 hard stale gate 重跑 postprocess。
2. 重建 R0/R1 并封存各阶段源码。
3. 对历史控制面误标生成 SHA-bound reconciliation。

提交信息：
- commit：无
