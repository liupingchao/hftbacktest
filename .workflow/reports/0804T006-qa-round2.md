# QA 验收回报（第二轮）

任务ID：
- 0804T006

状态：
- 未通过

结论：
- 第一轮 outcome leakage、consensus 循环验证和 session-level 外推问题
  已实质修复。
- 正式 C2/C3 仍缺 classification-time 专用 outcome contract 和可重复的
  stateful multi-track surrogate 定义，workflow 尚未进入合法待验收状态。

必须修复：
- Episode-end `classification_available_ts` 不能复用以 Binance
  bookTicker change 为 anchor 的现有 R1 horizon 资格；需要独立 label
  builder、coverage、source-age、boundary 和 reconciliation manifest。
- Hyperliquid fast/standard L2、BBO、trades、asset context、allMids 的
  surrogate 必须同步处理，并保持订单簿状态、timestamp、source age、
  degraded mask 和 segment 边界语义有效。
- 增加 Aug04 freeze 后首次读取的原子 consumption ledger。
- 固定 first-after 失败标准、bootstrap draw/CI/block attribution 和 bps
  单位。
- 消除 Track B 可能升级 C3 的文字矛盾。
- 进入 QA 前将 task/business 状态更新为 `待验收`，并同步最新 QA 到
  `docs/qa-acceptance-report.md`。

缺陷统计：
- P0：0
- P1：2
- P2：3
- P3：1

验收动作：
- 修复后进行第三轮独立 QA。
