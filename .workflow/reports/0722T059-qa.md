# QA 验收结果

## Findings（按严重性）

- P0：无。
- P1：legacy source containment 仍可由 existing parent symlink 加
  missing final child 绕过。
  `_resolve_recorded_artifact_path()` 仅在最终 `recorded.exists()` 或
  `recorded.is_symlink()` 为真时验证 source containment。当 legacy root
  内的中间父目录是指向 root 外部的 symlink、最终 child 不存在，而当前
  checkout 中同 relative target 是合法文件时，两个条件都为假，resolver
  跳过 source containment 并接受 current destination。
- P2：T059 新增回归覆盖 final component symlink escape 和合法 existing
  legacy file，但没有覆盖 existing parent symlink + missing child。

执行线程：
- QA验收线程

任务ID：
- 0722T059

状态：
- 未通过

更新时间：
- 2026-07-22 15:19 Asia/Shanghai

验收线程：
- QA验收线程

验收对象：
- 业务执行线程 `0722T059`
- implementation commit
  `70bf82eb552b7c463f67f0606fb01369e094f0f9`
- workflow/business commit
  `871dc8186b8900b2e620caf6fe8f8c509076b502`

验收范围：
- 严格 offline-only 独立 hostile review。
- 覆盖 existing legacy source final/parent symlink、legacy/current
  destination 双重 containment、relative/absolute/traversal/symlink、
  missing-only skip 与 malformed-existing failure。
- 核对 scheduler tolerance 严格限于 test-only。
- 所有 pytest 和 py_compile 均使用
  `conda run -n hftbacktest ...`。
- 不修改 implementation/test 文件。

验收步骤：
1. 阅读 workflow-kit、T056-T058 failed QA、T059 task/business 和
   controller tracking，核对固定提交及范围。
2. 审查 resolver 分支、测试 diff 和 production watcher/manager 文件
   哈希。
3. 在临时 current/legacy/outside roots 中运行独立 hostile matrix。
4. 运行 focused、deadline hostile 20 次、连续两次 full Hyperliquid。
5. 运行 Conda py_compile、skip reason、diff 和工作树检查。

实际结果：
- Conda 环境为 Python `3.12.13`，解释器
  `/Users/liu/.local/conda/envs/hftbacktest/bin/python`，pytest `9.0.3`。
- 合法 current relative/absolute 和合法 existing legacy mapping 通过。
- Relative traversal/symlink、current absolute symlink、unknown absolute
  existing/missing、legacy lexical traversal、legacy final symlink 和
  legacy parent symlink + existing child 均 fail-closed。
- 独立反例被错误接受：
  `legacy/linked-parent` 是指向 legacy root 外部的 existing directory
  symlink，`legacy/linked-parent/missing-child.json` 不存在，而
  `current/linked-parent/missing-child.json` 是合法文件；resolver 返回
  current 文件，没有抛出 `RowLevelGeneratorError`。
- True missing package 返回 unavailable；invalid JSON 抛出
  `JSONDecodeError`，malformed contract 抛出 `KeyError`。
- 两个 skip reason 精确为：
  - `0609T002 historical pricing-signal source package is absent`
  - `0602T001 synchronized public sample package is absent`
- Focused：
  `22 passed, 2 skipped in 3.89s`。
- Deadline hostile repeated：
  `20/20 passed`。
- Full Hyperliquid run 1：
  `1259 passed, 2 skipped in 56.22s`。
- Full Hyperliquid run 2：
  `1259 passed, 2 skipped in 55.96s`。
- 四个相关文件 Conda `py_compile` 通过。
- Implementation/workflow `git diff --check` 通过。
- Production watcher 文件前后 SHA-256 均为
  `395411f43673d759a0b8f290eef51d210422af4f244ecbce3cf9476267a0ed4f`；
  scheduler 变更仅是测试断言 `+0.05` 到 `+0.1`。
- 工作树中的既存 `.DS_Store` 未修改、未暂存。
- 未执行 network、remote、private/account、order/cancel、live 或
  service 操作。

验收结论：
- 未通过。
- 结论说明：
  - Final-component legacy symlink、destination containment、
    missing-vs-malformed 和 scheduler/full-suite 均通过，但 source
    containment 仍不是无条件不变量，existing parent symlink 可在 final
    child 缺失时绕过。

通过项：
1. Existing final-component legacy symlink escape 已拒绝。
2. Legacy/current destination、relative/current-root 和 unknown absolute
   的既有 hostile matrix 通过。
3. Missing-only skip 与 malformed-existing failure 通过。
4. Scheduler tolerance 严格 test-only，deadline `20/20` 和两轮 full
   suite 全绿。
5. 未改变 production strategy、timestamp、deadline、quote、risk 或
   lifecycle 行为。

不通过项：
1. Existing legacy parent symlink + missing final child 可绕过 source
   containment，并被安全 current target 掩盖。
2. 回归矩阵缺少上述分支。

缺陷清单：
1. P1：known legacy path 必须检查已存在的每个父级路径组件，或使用不会
   因 final child 缺失而跳过的 source containment 规则；任何 existing
   parent/final symlink escape 都必须 fail-closed。
2. P2：新增 parent symlink + missing child + safe current same-relative
   target 的 self-contained regression。

阻塞项：
- 无外部或环境阻塞；需要新的最小 offline-only repair。

建议总控下一步：
1. 派发最小 resolver repair，移除 source containment 对最终
   `exists()/is_symlink()` 的不完整依赖。
2. 保持 current destination containment、missing-only exception 和
   test-only scheduler tolerance 不变。
3. 修复后复跑同一 hostile matrix、focused、deadline repeated 和连续
   两次 full Hyperliquid。

提交信息：
- QA commit：由本报告提交后的线程回报提供。
- Implementation commit：
  `70bf82eb552b7c463f67f0606fb01369e094f0f9`
- Workflow/business commit：
  `871dc8186b8900b2e620caf6fe8f8c509076b502`
