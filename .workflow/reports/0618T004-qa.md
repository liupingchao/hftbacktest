# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0618T004

状态：
- 已通过

更新时间：
- 2026-06-18 10:33 CST

验收线程：
- QA验收线程

验收对象：
- 业务线程-research 0618T004

验收范围：
- `Hyperliquid tiny-live real-order canary interface validation`
- 本轮验收任务文件、业务报告、executor/self-test 产物、真实 canary pullback 产物、final gate 产物、focused tests、JSON manifest、artifact 非空、redaction scan 和 no-promotion 边界。
- 本轮不验收 PnL、maker viability、连续 live 策略、默认上线、放大仓位、后续 `0617T008` 执行或 scheduled-cancel 可用性。

验收步骤：
1. 阅读 `.workflow/tasks/0618T004.md` 和 `.workflow/reports/0618T004-business.md`。
2. 复核 `local_live_analysis/hyperliquid_tiny_live_real_order_canary_0618T004/pulled_back_awsserver1/executor_manifest.json`、`cancel_shutdown_proof.json`、`private_order_response_audit.json`、`credential_source_manifest.json` 和 `final_safety_summary.json`。
3. 运行 focused tests、`py_compile`、self-test、JSON manifest 校验、artifact 非空检查、redaction pattern scan 和 `git diff --check`。
4. 刷新 `awsserver1` 远端只读状态输入，重跑 `local_live_analysis/hyperliquid_tiny_live_final_go_no_go_gate_0618T004/` final gate。
5. 用断言复核 canary manifest、shutdown proof 和 final gate 的关键字段。

实际结果：
- `python -m pytest examples/hyperliquid/test_hyperliquid_tiny_live_real_order_executor.py examples/hyperliquid/test_hyperliquid_tiny_live_final_go_no_go_gate.py examples/hyperliquid/test_hyperliquid_tiny_live_sdk_readiness.py -q` 通过，`17 passed`。
- `python -m py_compile examples/hyperliquid/hyperliquid_tiny_live_real_order_executor.py` 通过。
- 本地 `--self-test` 通过，self-test manifest 记录 `executor_ready=true`、`shutdown_proof_status=pass`、`real_order_endpoint_called=false`。
- 真实 canary pullback manifest 记录 `task_id=0618T004`、`final_recommendation=hyperliquid_tiny_live_real_order_canary_ready_for_qa`、`order_submission_attempted=true`、`order_status_types=resting`。
- 真实 canary 已调用 `private_endpoint`、`real_order_endpoint`、`real_cancel_endpoint` 和 `schedule_cancel_endpoint`，且记录 `credentials_written=false`、`secret_values_written=false`、`raw_signatures_written=false`。
- `cancel_shutdown_proof.json` 记录 `proof_status=pass`、`final_open_orders=[]`，tracked cancel 成功。
- `Exchange.schedule_cancel` 被调用但返回账户交易量门槛错误；这证明接口可达，但该账户当前不能使用 scheduled-cancel / dead-man switch。
- `order_intent_audit.csv` 记录 `BTC,buy,0.01,63211.0,632.11,Alo,limit,false,true`，符合 `0.01 BTC`、`700 USDC`、post-only `Alo` 边界。
- `credential_source_manifest.json` 只记录 `.env` 路径和 key 名，未记录凭证值。
- artifact 非空检查通过。
- redaction pattern scan 未发现 raw `0x...` address/private-key/cloid/oid 长串、raw private-key 赋值、raw signature 或 nonce。
- 刷新远端状态后 final gate 记录 remote commit `52b5b9541`、dirty count `0`，输出 `final_recommendation=tiny_live_ready_for_controller_go`、`allow_create_0617T008=true`、`blocking_reasons=[]`。
- `git diff --check` 通过。

验收结论：
- 已通过
- 结论说明：
  - `0618T004` 满足真实订单 canary 接口验证的验收标准：order/cancel/private-read 主链路已打通，最终无遗留 open orders，产物脱敏边界成立。

通过项：
1. 真实 post-only `Alo` BTC canary order submission attempt 成功进入 `resting`。
2. tracked `Exchange.cancel` 成功，最终 `open_orders=[]`。
3. private read、order、cancel、cancel_by_cloid、schedule_cancel 接口均被真实调用并写入脱敏产物。
4. 未发现凭证值、raw signature、nonce、address、oid 或 cloid 泄漏。
5. 未运行连续 live 策略，未创建或执行 `0617T008`，未放松 `10min / 0.01 BTC / post-only / max loss cap`。
6. T004 final gate 刷新后为 go：`allow_create_0617T008=true`。

不通过项：
1. 无

缺陷清单：
1. 无

阻塞项：
- 无

风险 / 限制：
- `schedule_cancel` / dead-man switch 当前因账户交易量门槛不可用；后续 live task 不应依赖它，除非账户资格变化并重新验证。
- `Info.query_order_by_oid` / `Info.query_order_by_cloid` 在本轮 artifact 中返回 `unknownOid`，但 order response 和 cancel proof 已证明 tracked order lifecycle；后续若要把 order-status 查询作为强依赖，应单独加强状态查询时序。

建议总控下一步：
1. 若继续 tiny-live，应先创建并 QA `0617T008` 或新命名 live task，明确不依赖 scheduled-cancel，改用 tracked cancel + final open-orders proof。
2. 后续 live task 仍需保持 `10min / 0.01 BTC / post-only / max loss cap`，并把本轮 T004 产物作为接口链路证据，不得扩大为 PnL 或 maker viability 证明。

提交信息：
- commit：52b5b95 / 63f1761
