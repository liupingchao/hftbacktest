# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0730T012

状态：
- 已通过

更新时间：
- 2026-07-30 23:30 CST

验收线程：
- QA验收线程

验收对象：
- 业务线程-cross-exchange-research-design + 0730T012

验收范围：
- SKHYNIX 四小时双交易所研究方案、dataset facts、alignment、basis、
  lead-lag、arbitrage、Hyperliquid maker signal、执行顺序、能力边界和
  新增采集授权门禁。

验收步骤：
1. 对照 T011 本地 campaign manifests、timeline index 和八段 common L2
   timeline 独立复算核心数据事实。
2. 对照现有 cross-exchange MVP、shortfall、collection timeline 和 maker
   acceptance 文档检查研究方法与既有证据边界。
3. 检查 BTC coefficient 隔离、future leakage、segment boundary、
   degraded interval、economic equivalence、exact fill 和 promotion 边界。
4. 第一轮发现新增采集授权门禁缺失和 8 条 snapshot trigger 摘要遗漏，
   修复后进行同范围独立复验。
5. 运行 `git diff --check`。

实际结果：
- P0: none
- P1: none
- P2: none
- P3: none
- Timeline 独立复算为 `556,861` 行。
- Binance 共 `527,660` triggers：
  - depth update: `527,652`
  - snapshot: `8`
- Hyperliquid fast-L2 triggers: `26,519`。
- Hyperliquid standard-L2 triggers: `2,682`。
- Raw event counts、rates、source age、segment gap、basis 分布和 archive
  SHA 与方案一致。
- Alignment、basis、lead-lag、arbitrage、Binance lead 和 Hyperliquid
  maker signal 均有独立实验和验收门槛。
- BTC 已验收 coefficient 未被复用为 SKHYNIX coefficient。
- 预测 basis、economic equivalence、executable edge 和 maker PnL 已分层。
- R6 只能提出补采建议，不能启动采集。
- 新增 AWS/public-data 采集必须：
  - 另建 formal task
  - 获得用户明确授权
  - 只在用户确认的活跃交易时段启动
  - 冻结 symbol、venue mapping、host、duration、cost 和 output path
- 没有 exact queue、exact fill、exact maker PnL、确定套利或 production
  promotion 越权声明。
- `git diff --check` 通过。

验收结论：
- 已通过
- 结论说明：
  - T012 研究方案与本次数据事实、现有策略合约和能力边界一致，可以从
    R0 本地 research dataset builder 开始执行。

通过项：
1. Dataset-specific 事实和事件对账完整。
2. 五类用户指定研究重点完整。
3. 执行顺序、walk-forward、freshness 和统计门槛明确。
4. 新增采集授权与活跃交易时段门禁明确。
5. Exact fill/arbitrage/promotion 边界保持。

不通过项：
1. 无

缺陷清单：
1. 无

阻塞项：
- 无

建议总控下一步：
1. 派发 R0 research dataset builder，只使用已拉回的本地数据。
2. R0 独立 QA 通过后再进入 R1 alignment acceptance。
3. 若 R6 建议补采，停在授权门前等待用户明确批准。

提交信息：
- commit：无
