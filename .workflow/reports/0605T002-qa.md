# QA 验收结果

执行线程：
- QA验收线程

任务ID：
- 0605T002

状态：
- 已通过

更新时间：
- 2026-06-05 12:23 CST

验收线程：
- QA验收线程

验收对象：
- 业务线程-python / 0605T002

验收范围：
- 验收 `0605T002` 是否按诊断任务边界复现并定位 shutdown `local_absent_from_local_orders` local-only proof 与缺少 exchange open-orders reconciliation / audit tail final proof 的风险，且未提交 production 修复或越界改动。

验收步骤：
1. 读取 `.workflow/tasks/0605T002.md`，确认任务是正常 QA，范围为诊断/复现/定位，不允许修复 `live_tick_mm.py`、接入 REST、修改 binding/Rust/connector、启动 live 或 promotion。
2. 读取 `.workflow/reports/0605T002-business.md`，核对稳定复现矩阵、根因分层、验证命令、commit id 和未越界声明。
3. 检查提交 `272b49e` 的文件范围，确认只包含任务文件和 focused 诊断测试。
4. 检查提交 `1d90e1e` 的文件范围，确认只包含业务报告。
5. 复查 focused tests 是否覆盖 local absent 与 fake exchange open-orders 仍含同一 order id 的不一致场景。
6. 复查 `live_tick_mm.py` 当前 shutdown helper / summary 是否仍是 local-only proof 且没有 exchange reconciliation / audit tail final proof。
7. 复跑 focused pytest：`/home/molly/anaconda3/envs/hftbacktest/bin/python -m pytest examples/binance_tick_mm/test_backtest_tick_mm.py -k "shutdown_cancel or terminal_confirmed or exchange_reconciliation or local_absent or open_orders"`。
8. 复跑 package-level test 文件集合：`/home/molly/anaconda3/envs/hftbacktest/bin/python -m pytest examples/binance_tick_mm/test_*.py`。
9. 运行 `git diff --check`。

实际结果：
- 提交 `272b49e` 文件范围为 `.workflow/tasks/0605T002.md` 和 `examples/binance_tick_mm/test_backtest_tick_mm.py`；未修改 `live_tick_mm.py` production helper。
- 提交 `1d90e1e` 文件范围为 `.workflow/reports/0605T002-business.md`。
- 新增 fake/stub 诊断覆盖 `exchange_open_order_ids={101}` 但 helper 只调用 `hbt.orders(asset_no)` 的场景，结果仍为 `terminal_confirmed=True` / `final_order_status=local_absent_from_local_orders`。
- `ShutdownCancelResult` 当前没有 `exchange_reconciliation_checked`、`exchange_open_order_absent`、`exchange_confirmation_source`、`final_proof_level` 等 exchange-side final proof 字段。
- shutdown summary 当前只输出 attempts / sent / wait_requests / order_responses / terminal_confirmed / unknown_or_timeout / failed，没有 exchange/open-orders/reconciliation counter。
- graceful shutdown block 未写入 final no-open-order / exchange reconciliation audit tail record。
- Focused pytest 通过：`11 passed, 180 deselected`。
- Package-level `test_*.py` 通过：`306 passed`。
- `git diff --check` 通过，无输出。
- QA 未发现本任务修改 py binding、Rust live bot/backtest、connector、production config、normal trading loop cancel、audit schema、strategy quote/submit/cancel 行为、REST private/order endpoint、live/default-on/tiny-live 或 promotion。

验收结论：
- 已通过
- 结论说明：
  - `0605T002` 按诊断边界稳定复现并定位了风险 1 / 3：当前 local absent proof 是 local-only proof，不等价于 exchange-side no-open-order proof；这不是 `0605T001` 回归，而是更高 proof level 缺口。

通过项：
1. 明确判断风险 1 / 3 属实，并精确区分当前已修 bug 与更高 proof level 缺口。
2. 稳定复现 `local_absent_from_local_orders` 只来自本地 snapshot，且 local absent 与 fake exchange still-open 不一致时当前 helper 不会发现。
3. 证明 shutdown result / summary / audit tail 当前没有 exchange-side final proof。
4. 给出后续修复边界建议，包括 exchange proof 字段、local-only proof level、REST open-orders reconciliation 状态枚举和 audit tail final record。
5. 验证命令通过，且未提交 production 修复或越界改动。

不通过项：
1. 无

缺陷清单：
1. 无

阻塞项：
- 无

建议总控下一步：
1. 可将 `0605T002` 标记为 `已通过`。
2. 若近期准备恢复 live/tiny-live，建议派发独立窄修复任务补 exchange open-orders reconciliation / audit tail final proof；该修复仍应禁止策略 quote/submit/cancel 行为改变、参数搜索、live/default-on/tiny-live 和 promotion。
3. 若近期不恢复 live，本风险可作为 shutdown proof hardening 后续项，不阻塞当前只读研究任务。

提交信息：
- commit：`272b49e` / `1d90e1e`
