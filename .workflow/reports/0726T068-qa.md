# QA 验收结果

## Findings（按严重性）

- P0/P1/P2/P3：无。

执行线程：
- QA验收线程

任务ID：
- 0726T068

状态：
- 阻塞

更新时间：
- 2026-07-26 18:57 Asia/Shanghai

验收线程：
- QA验收线程

验收对象：
- Business/evidence commit
  `d27ecaf8a04c73148d86b137381c6fdaccb469aa`
- Deterministic CSV repair commit
  `cc4592094e2adb2d8b475d61634dc5f8eba499df`
- Exact live source
  `a0bc92898ecea43cbdc4219efc1acbcd69580969`

验收范围：
- 重新打开 T068 QA，验证 `cc459209` 的 CSV line-terminator repair。
- 从 repair commit 的 committed tar clean rebuild 三窗口聚合验收。
- 独立检查 source、window identity、duration/completion、BTC/Alo intent、
  strict seeded dynamic gate、reject/resting/cancel、fill/role、account
  continuity、terminal state、credential boundary 和 bundle integrity。
- 检查 submit、fill pullback、post-state 使用同一 watcher client 的源码
  contract。
- 按 T068 done 条件裁决 `阻塞` 或 `未通过`。

验收步骤：
1. 用 `git archive cc459209` 创建 clean source root，只从其中的 committed
   tar 解包，不使用工作区已有 extracted artifacts。
2. 校验 tar SHA-256、成员路径安全、bundle manifest 和三个 window
   manifests。
3. 将每个 runtime provenance 的 `69` 个文件逐一与 Git commit
   `a0bc928...` 对象重算比较。
4. 使用 conda env `hftbacktest` 重跑修复后的 `aggregate_acceptance.py`，
   对 JSON/CSV/Markdown 做逐字节比较和 SHA-256 校验。
5. 独立解析关键 JSON/CSV，直接断言三窗口 identity、时长、订单、动态门、
   cancel、fill/role、账户和终态。
6. 审查 exact watcher source 的 client 初始化及 submit/fill/post-state/
   open-orders 调用关系。
7. 运行 Python compile、remote shell syntax、diff whitespace 和 WTI/
   credential-name/path 检查。

实际结果：
- `d27ecaf8` 是 `cc459209` 的直接祖先。Repair 只为
  `csv.DictWriter` 固定 `lineterminator="\n"`，并同步业务报告中的 CSV
  hash；committed tar 和三份 aggregate output Git blobs 未改变。
- Tar SHA-256 精确为
  `5bd81ab915dac98b5141875cc932a9ce7aebe2f11f34741b84244b5cdfb3446d`；
  `377` 个 tar members 无 absolute path 或 `..` traversal。
- Bundle manifest `363/363` 通过；window manifests 分别
  `111/111`、`111/111`、`108/108`，无 missing/mismatch。
- 三个窗口的 runtime provenance 均为 source
  `a0bc92898ecea43cbdc4219efc1acbcd69580969`；每窗口 `69/69` 文件与该
  Git commit 的 blob SHA-256 和 byte count 完全一致。
- Global identity 为 `0726T068:window_01/02/03`，artifact window IDs
  `1/2/3`；三个 child 均 return code `0`、reaped，child exit 后 independent
  open-orders proof 为 empty。
- Window 01/02 分别运行 `1800.000864s`、`1800.000866s`，均无 submit/fill。
- Window 03 运行 `1693.889259s`；raw close reason 为
  `task7_manager_cycle_complete`，且 `live_submissions_count=2` 等于 cap
  `2`，因此 aggregate 的 `submission_cap_terminal` 路由成立。
- Window 03 strict seeded gate 为 `pass/allowed=true`，全部 checks 为 true；
  exact seed hash、`280` rows、current-market uncontaminated、candidate
  bounded/pass、no fallback 和 final quote change 均成立。
- 仅有两个 submitted intents：Hyperliquid `BTC`，一买一卖，均为
  post-only `Alo`、`0.005 BTC`；所有 submitted intent 中无 WTI/WTIOIL/
  `xyz:CL`。
- 买单为明确 post-only immediate-match reject；卖单为 resting，并以匹配
  oid/cloid token 获得唯一 authoritative `success` cancel；最终 open
  orders 为 `0`。
- 三窗口 `live_fill_ledger.csv` 和
  `fill_liquidity_role_evidence.csv` 均为零 data rows。Window 03 执行一次
  same-client `user_fills_by_time` pullback，但返回零 fill。
- `9/9` controller-external account guards 使用同一 account-scope token 和
  signer token，全部 source exact、open orders `0`、BTC position `0.0`、
  raw credentials written `false`；independent terminal guard 同样通过。
- Exact watcher source 只在 guarded initializer 中建立 live `client`；
  submit 使用 `run_order_once(client=client)`，fill pullback、user state、
  fees、cancel 和 final open orders 均使用该同一对象。外部 guards 以同一
  env-backed account/signer token 包围每个窗口。
- Remote secret scan artifact 被 bundle hash 覆盖，报告扫描 `359` files、
  raw identity match `0`、env-named file `0`；tar 本身不含 `.env`。由于 raw
  credentials 按要求未拉回本地，QA 不在本地重放真实 secret-value scan。
- Clean aggregate rebuild：
  `mechanism_status=pass`，
  `role_known_fill_status=blocked_no_fill`，
  `economics_status=blocked_no_role_known_fill`，
  `overall_status=blocked`。
- Clean rebuild 与 committed workspace outputs 三项均逐字节一致：
  - JSON：
    `7c75496ed5d0780642559d2ad1750f0014121b52dfbd74e978dbe44674a8ae23`
  - CSV：
    `d3241a73c898ba9d044aa38825d3a873b64791ab4e5c07bc5476fa01ec335662`
  - Markdown：
    `80f1601a665431ea36d21a6a9b43224c3c33dd07814524a092c1bc84da65cbf2`
- Rebuilt CSV 包含 `4` 个 LF、`0` 个 CRLF；已解决原 QA 的 P3。
- Repair commit clean `py_compile` 和 `git diff --check d27ecaf8 cc459209`
  通过。原 QA 的 tar、source、raw lifecycle、account 和 secret-boundary
  验收继续有效，因为对应 evidence blobs 未改变。
- QA 未访问 AWS、credentials、private endpoints 或执行 live/order/cancel。

验收结论：
- 阻塞。
- 结论说明：
  - Live seeded-dynamic mechanism、BTC-only order lifecycle、same-account
    evidence 和 terminal safety 均独立验收通过；但 T068 done 条件明确要求
    role-known fill evidence，本次三窗口没有任何 fill，maker/taker role 和
    economics 因此仍不可验收。该缺口来自本次有效窗口未产生成交，不是现有
    机制或证据链失败，所以状态应为 `阻塞`，不是 `未通过`。

通过项：
1. Exact source、三窗口 identity、duration/completion 和 artifact integrity。
2. Strict seeded dynamic final quote change before submit。
3. Hyperliquid BTC-only `Alo <= 0.005 BTC`，无 WTI contamination。
4. 一次 post-only reject、一次 resting 和 authoritative cancel。
5. Same-account/same-client contract 与 terminal zero orders/zero BTC。
6. Raw credential pullback boundary 和 clean deterministic aggregate rebuild。

不通过项：
1. 无机制或安全验收失败。

缺陷清单：
1. 无。

阻塞项：
- 三窗口总 fill 数为 `0`，liquidity-role rows 为 `0`。
- 无法形成 `confirmed_maker` 或 `confirmed_taker` 证据。
- Fee/rebate、fill-rate、markout/PnL 和经济性不可评估。
- T068 不满足“mechanism evidence + role-known fill evidence”done 条件。

建议总控下一步：
1. 保留本次 mechanism/lifecycle/account/terminal evidence，不重复修复已通过
   的证据链。
2. 新建后续 formal bounded live task，仅补 role-known fill 和 economics
   evidence；继续沿用 exact account guard、BTC-only、Alo 和 terminal proof。

提交信息：
- QA commit：无。
