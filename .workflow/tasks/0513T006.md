```md
执行线程：
- 业务线程-python

任务ID：
- 0513T006

标题：
- Step 2 read-only latency / market-data integrity analyzer 实施

简短描述：
- 按 `0513T005` 计划实施 Step 2 的 read-only analyzer，对现有样本生成 latency / market-data integrity / provenance / sample usability artifacts。只读取现有本地样本和现有代码，不启动 live、不修改策略、不运行新的策略 replay/sweep。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- 0513T005

规则更新提醒：
- 本任务是按照 `0513T005` 计划做 read-only 实施，不是重新规划。
- 不允许启动 live，不允许采集新样本，不允许修改策略、fair/target、risk guard、quote placement。
- 不允许把 action-path acceptance、audit overlay 或 top5 source 字段解释成 full L2 / queue / OFI / microprice 对齐证明。

按线程规则执行：
- .workflow/workflow-kit/thread-report-template.md
- docs/thread-playbook.md
- .workflow/reports/0513T005-business.md
- .workflow/reports/0513T005-qa.md

是否需要提交代码：
- 不需要

提交要求：
- 不需要提交
- 回报必须写 `commit：无`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

Allowed files：
- `.workflow/tasks/0513T006.md`
- `.workflow/reports/0513T006-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `.workflow/dashboard.html`
- `.workflow/dispatch_suggestions.md`
- `local_live_analysis/step2_market_data_baseline_0513T006/**`

Allowed read-only inputs：
- `local_live_analysis/5-13-day-control-15min/**`
- `local_live_analysis/5-11-night-active/**`
- `local_live_analysis/5-10-day-control-1h-06/**`
- `local_live_analysis/5-9-noon/**`
- `local_live_analysis/5-9-small/**`
- existing analysis scripts under `examples/binance_tick_mm/`

Forbidden files / areas：
- `examples/binance_tick_mm/*.py`
- `examples/binance_tick_mm/deploy/*.sh`
- strategy rule logic, fair/target formula, risk guards, quote placement
- core `hbt.depth(0)` API, converter/npz schema, connector local book management
- AWS/remote deployment state
- live run directories outside the output path
- archive tarballs and raw input artifacts

Requires separate task：
- 修改或新增长期维护的 analysis script。
- 修改 audit schema、converter/npz schema、connector 或 core depth API。
- 启动 fresh live no-rule run。
- 修改 `maker_acceptance.py` 的 market-view gate。
- 做 microprice / OFI / queue feature research。

Fatal Failures：
- 启动 live 或采集新样本。
- 修改策略代码、部署脚本、core/data schema、converter、connector 或 acceptance 脚本。
- 运行 Stage 6J candidate replay/sweep 或改变任何 replay candidate。
- 生成报告时伪造 raw update-id / bookTicker provenance。
- 把 existing action-path acceptance 当作 pricing/queue research 可用性证明。

action：
- 按 T005 计划实现 read-only 分析，可以使用临时 one-off 命令或 report-local helper，但不得修改仓库内 Python strategy/analysis 源码。
- 对样本：
  - `5-13-day-control-15min`
  - `5-11-night-active`
  - `5-10-day-control-1h-06`
  - `5-9-noon`
  - `5-9-small`
  生成 Step 2 artifacts。
- 输出目录：`local_live_analysis/step2_market_data_baseline_0513T006/`。
- 必须生成：
  - `sample_inventory.json`
  - `latency_summary.json`
  - `latency_summary.csv`
  - `market_data_integrity.json`
  - `top5_mismatch_buckets.csv`
  - `source_provenance_summary.json`
  - `sample_usability_matrix.csv`
  - `step2_baseline_report.md`
- 每个样本必须分类为：
  - `compressed_action_path_only`
  - `pricing_research_candidate`
  - `queue_fill_research_candidate`
  - `unusable`
- 如果 raw gzip 不能可靠提取 `U/u/pu`、`lastUpdateId` 或 bookTicker/depth consistency，必须在 artifact 中写 `requires_core_data_task` 或 `requires_raw_parser_task`，不能伪造字段。
- 明确 legacy/pre-T004 样本与 T004-standard manifest 样本的区别。

verify：
- 输出目录存在并包含全部 required artifacts。
- `python3 .workflow/build_dashboard.py`
- 人工检查：
  - 无 live。
  - 无 replay/sweep。
  - 无 strategy/deploy/core/converter/connector 源码修改。
  - 每个样本都有 usability classification 和不可用原因。

done：
- 回报必须说明生成了哪些 artifacts、每个样本的 classification、主要 latency/data-quality 结论、哪些字段需要后续 core/data task。
- 回报必须明确本任务是 read-only 实施，不是 live promotion，不授权策略变更。
- 回报必须写 `commit：无`。
```
