```md
执行线程：
- 业务线程-research

任务ID：
- 0625T003

标题：
- Out-of-sample signal acceptance

简短描述：
- 使用 `0627T001` QA-accepted HL fast synchronized public sample package，对 Binance lead / Hyperliquid lag 的候选信号做离线、样本外、public-only 验收。
- 本任务只判断是否能冻结 MVP v1 signal contract、side mapping、freshness limit 和 edge formula；不实现策略、不改 watcher、不下单、不进入 shadow/live。

状态：
- 待验收

人工检查状态：
- 总控已批准派发；业务线程执行完成并提交业务报告，待 QA 验收。

执行顺序：
- 当前唯一任务

前置任务：
- `0625T001` QA 已通过，结论为 `needs_more_public_samples`，明确 signal/edge 需要多窗口样本外证据。
- `0625T002` repaired result 为 `needs_more_public_samples`，原因是 ordinary HL `l2Book` nominal `1000ms` label 的 effective horizon 多数约 `5000ms`，不得作为 T003 输入。
- `0627T001` QA 已通过，最终 package 为 `local_live_analysis/cross_exchange_mvp_hl_fast_sample_expansion_0627T001/`，recommendation=`sample_contract_ready_for_signal_acceptance`，`t003_creation_unlocked=true`。

规则更新提醒：
- 请先阅读 `.workflow/workflow-kit/workflow-manual.md`、`.workflow/workflow-kit/task-dispatch-template.md`、`.workflow/workflow-kit/thread-report-template.md`、`.workflow/workflow-kit/qa-acceptance-template.md`、`task_plan.md`、`progress.md`、`findings.md`、`docs/cross_exchange_maker_mvp_plan.md` 后再执行本任务。

按线程规则执行：
- `AGENTS.md`
- `docs/thread-playbook.md`

是否需要提交代码：
- 需要

提交要求：
- 自测、离线 artifact 生成和业务报告完成后提交。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0625T003.md`
- `.workflow/reports/0625T003-business.md`
- `examples/hyperliquid/cross_exchange_signal_acceptance.py`
- `examples/hyperliquid/test_cross_exchange_signal_acceptance.py`
- `local_live_analysis/cross_exchange_mvp_signal_acceptance_0625T003/**`
- `task_plan.md`
- `progress.md`
- `findings.md`

read-only inputs：
- Required source package:
  - `local_live_analysis/cross_exchange_mvp_hl_fast_sample_expansion_0627T001/**`
- Required source QA/business context:
  - `.workflow/tasks/0627T001.md`
  - `.workflow/reports/0627T001-business.md`
  - `.workflow/reports/0627T001-qa.md`
  - `docs/qa-acceptance-report.md`
- Background signal context only; do not count as held-out acceptance rows:
  - `local_live_analysis/cross_exchange_mvp_alpha_edge_decomposition_0625T001/**`
  - `local_live_analysis/cross_exchange_mvp_sample_expansion_0625T002/**`
  - `local_live_analysis/cross_exchange_lead_lag_analysis_0601T003/**`
  - `local_live_analysis/binance_led_hyperliquid_pricing_signal_0601T005/**`

hard boundary：
- Offline/public-only/read-only.
- No new network collection, no AWS execution, no remote alignment, no live orders, no credentials, no private/account/order/cancel endpoints, no user stream, no live client initialization, no remote final gate, no T008 ledger claim.
- No watcher strategy change, no production config change, no quote-distance change, no cap relaxation, no post-only relaxation, no taker/crossing, no canary, no M3/stable-PnL/default-on/promotion.
- Future Hyperliquid values are labels only and must never be decision inputs.
- Do not use the repaired-but-invalid ordinary T002 package as signal acceptance rows.
- Do not reinterpret nominal `1000ms` labels as exact `1000ms` elapsed labels unless the effective horizon gate passes at row level.
- Do not use the same rows/window both to select formula/threshold/side mapping and to claim held-out acceptance.

input gate：
- Fail closed unless the `0627T001` package manifest says:
  - `recommendation=sample_contract_ready_for_signal_acceptance`
  - `t003_creation_unlocked=true`
- Fail closed unless required `0627T001` boundary manifest confirms public-only/offline/no-private/no-order/no-side-freeze/no-promotion.
- T003 signal rows may include only rows valid for nominal `1000ms` signal acceptance:
  - `valid_for_1000ms_signal_acceptance=true`, or equivalent reconstructed row condition
  - `1000ms <= effective_future_age_ms <= 1250ms`
  - `nominal_horizon_ms=1000`
- Report row counts before and after this effective-horizon filter for every window.

train/evaluation contract：
- Use the three accepted `0627T001` windows as separate evaluation units.
- Default split method: leave-one-window-out.
  - For each fold, select thresholds/normalization/side mapping using only the two train windows.
  - Evaluate the selected candidate on the held-out window.
- Any global threshold or formula taken from prior accepted artifacts must be declared as pre-registered before reading held-out performance.
- Same-window threshold backfill is forbidden.
- A candidate cannot be accepted if its direction/side mapping only works in aggregate but fails or flips in a held-out window.

candidate signal allowlist：
- Evaluate only decision-time-visible fields from the accepted package.
- Candidate components are limited to:
  - `input_binance_top5_imbalance`
  - `input_binance_microprice_minus_mid_ticks`
  - `input_binance_mid_move_ticks_from_prev`
  - `context_basis_mid_ticks`
  - `context_hyperliquid_top5_imbalance`
  - `context_hyperliquid_microprice_minus_mid_ticks`
  - source-age and liquidity/regime fields only as predeclared conditioning or rejection filters
- If a field is absent, stale, or outside the accepted package schema, report it as unavailable instead of substituting another field.
- Do not run an unconstrained feature search, model search, or broad parameter sweep.

label contract：
- Primary label:
  - Hyperliquid future mid move ticks at nominal `1000ms` with valid effective horizon.
- Optional diagnostic labels:
  - Hyperliquid future microprice move ticks at nominal `1000ms` with valid effective horizon.
  - Maker-touch alternative markout for preserved buy-touch/sell-touch alternatives, if already available in the package.
- Diagnostic labels must not replace the primary acceptance label unless the task explicitly reports `reject_current_signal_shape` or `needs_more_samples`.

side / edge contract：
- Side mapping must be learned or selected only from train folds or pre-registered prior evidence.
- Output must explicitly state the frozen mapping, for example:
  - positive signal -> buy HL maker touch
  - positive signal -> sell HL maker touch
  - no stable side mapping
- Edge formula must be explicit and audit-friendly:
  - `fair_mid_px = current_hyperliquid_mid + signed_expected_move_ticks * tick_size`
  - `buy_edge_ticks = (fair_mid_px - quote_px) / tick_size`
  - `sell_edge_ticks = (quote_px - fair_mid_px) / tick_size`
- Fee/adverse-selection buffers in T003 are read-only acceptance buffers only; changing production watcher buffers is out of scope.

metrics：
- Required aggregate and per-window metrics:
  - row count after effective-horizon filter
  - train/evaluation fold id
  - candidate id
  - signal coverage
  - selected threshold / normalization
  - side mapping
  - direction hit rate
  - mean/median future move ticks
  - mean/median signed label under mapped side
  - p05/p95 signed future move ticks
  - wrong-way rate
  - mean wrong-way loss ticks
  - fee/adverse-buffer-adjusted edge proxy
  - source-age bucket stability
  - basis/regime bucket stability
  - sample/window contribution concentration
- Report confidence or uncertainty conservatively, for example bootstrap CI or deterministic per-window stability bands.

acceptance line：
- `signal_contract_accepted_for_shadow` may be recommended only if all conditions pass:
  - Effective-horizon-valid rows are present in all three windows and remain above the `0627T001` unlock minimum: `>=20/window` and `>=100` aggregate.
  - The selected signal uses only allowlisted decision-time fields.
  - Train/evaluation split is documented and same-window threshold backfill is absent.
  - Side mapping is stable across held-out folds; no fold flips the accepted mapping.
  - Held-out signed label and direction metrics are not dominated by a single window.
  - Fee/adverse-buffer-adjusted edge proxy is not systematically negative in held-out evaluation.
  - Boundary manifest confirms no live/private/order/shadow execution.
- If these conditions do not pass, final recommendation must be `needs_more_samples` or `reject_current_signal_shape`.

required output package：
- `signal_acceptance_manifest.json`
- `input_gate_report.json`
- `train_eval_split_manifest.json`
- `effective_horizon_acceptance.csv`
- `candidate_signal_matrix.csv`
- `heldout_performance_by_window.csv`
- `side_mapping_evidence.csv`
- `threshold_sensitivity.csv`
- `regime_stability.csv`
- `accepted_signal_contract.json` or `signal_rejection_reasons.csv`
- `boundary_manifest.json`
- `recommendation.md`

final recommendation enum：
- `signal_contract_accepted_for_shadow`
- `needs_more_samples`
- `reject_current_signal_shape`

verify：
- `python -m pytest examples/hyperliquid/test_cross_exchange_signal_acceptance.py -q`
- `python -m py_compile examples/hyperliquid/cross_exchange_signal_acceptance.py`
- `python examples/hyperliquid/cross_exchange_signal_acceptance.py --help`
- Run the runner over `local_live_analysis/cross_exchange_mvp_hl_fast_sample_expansion_0627T001/`.
- Parse every output JSON with `python -m json.tool`.
- Validate required CSV schemas, non-empty artifacts, row counts, effective-horizon row filters, train/evaluation split integrity and boundary flags.
- Reproduce the task output into a temporary directory and compare deterministic artifacts, excluding generated timestamps / absolute output paths.
- `git diff --check`

done：
- Report the exact input package path and source manifests consumed.
- Report per-window row counts before and after `valid_for_1000ms_signal_acceptance`.
- Report the train/evaluation split, candidate allowlist actually used, thresholds, normalization and side mapping.
- Report held-out performance per window and aggregate, with explicit concentration/stability caveats.
- Report the final recommendation enum.
- If accepted, include the frozen `accepted_signal_contract.json` fields for feature schema, horizon, effective-horizon row condition, normalization, threshold, side mapping, freshness limit and edge formula.
- If rejected or inconclusive, list the minimum additional sample/evidence needed before retrying T003.
- Explicitly state no watcher/live strategy behavior changed, no private/order endpoints were used, no orders were placed, no shadow/canary/promotion was authorized.
- 回报 commit id 和提交信息。
```
