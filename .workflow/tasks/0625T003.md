```md
执行线程：
- 业务线程-research

任务ID：
- 0625T003

标题：
- Out-of-sample signal acceptance

简短描述：
- 在已接受的 `0625T002` 多窗口公共样本上运行同一个 signal runner，固定 train/evaluation 边界，验证方向、future move、markout、basis/context、source age 和 regime stability。
- 本任务只做离线样本外信号验收，不做 live 行为变更，不下单，不读凭据，不调用 private/order/cancel endpoint，不冻结 production shadow。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0625T002` 视为已通过（按总控前提）

规则更新提醒：
- 请先阅读 `.workflow/workflow-kit/workflow-manual.md`、`.workflow/workflow-kit/task-dispatch-template.md`、`.workflow/workflow-kit/thread-report-template.md`、`.workflow/workflow-kit/qa-acceptance-template.md`、`task_plan.md`、`progress.md`、`findings.md`、`docs/cross_exchange_maker_mvp_plan.md` 后再执行本任务。

按线程规则执行：
- `AGENTS.md`
- `docs/thread-playbook.md`

是否需要提交代码：
- 需要

提交要求：
- 自测和离线 artifact 生成通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0625T003.md`
- `.workflow/reports/0625T003-business.md`
- `examples/hyperliquid/binance_led_pricing_signal_runner.py`
- `examples/hyperliquid/test_binance_led_pricing_signal_runner.py`
- `local_live_analysis/binance_led_hyperliquid_pricing_signal_xemm_0625_t002_*/**`
- `local_live_analysis/cross_exchange_lead_lag_join_xemm_0625_t002_*/**`
- `local_live_analysis/cross_exchange_lead_lag_analysis_xemm_0625_t002_*/**`
- `local_live_analysis/cross_exchange_mvp_signal_acceptance_0625T003/**`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `docs/cross_exchange_maker_mvp_plan.md`

read-only inputs / reusable code：
- `local_live_analysis/cross_exchange_mvp_sample_expansion_0625T002/**`
- `local_live_analysis/cross_exchange_mvp_alpha_edge_decomposition_0625T001/**`
- `examples/hyperliquid/cross_exchange_lead_lag_join.py`
- `examples/hyperliquid/cross_exchange_lead_lag_analysis.py`
- `examples/hyperliquid/binance_led_pricing_signal_runner.py`
- `examples/hyperliquid/synchronized_public_collection.py`

hard boundary：
- Offline public-only no-submit.
- No live orders, no credentials, no private/account/order/cancel endpoints, no live client initialization, no remote refresh, no remote final gate, no T008 ledger claim.
- No strategy/live behavior change, no quote-distance/cap relaxation, no taker/crossing, no canary, no M3, no stable-PnL, no default-on, no promotion.
- Future Hyperliquid values are labels only; they must not be used as decision inputs.
- Do not silently expand T003 into production shadow or live calibration.

action：
- Run the same signal runner across the accepted T002 windows with a fixed train/evaluation boundary.
- Validate signal direction, effective horizon handling, source freshness, basis/context conditioning, regime stability and markout consistency.
- Freeze or reject only the signal contract inputs required for later shadow/replay work:
  - feature allowlist
  - horizon
  - signal normalization
  - side mapping
  - freshness limit
  - edge formula
- Compare out-of-sample direction/markout across the three accepted windows and report whether the signal contract is ready for shadow consumption.
- Keep future Hyperliquid rows as labels only and preserve explicit nominal vs effective horizon reporting.

acceptance criteria：
- One deterministic offline signal acceptance run over all accepted T002 windows.
- Train/evaluation boundary is fixed and no same-window threshold backfill is used.
- Direction, future move, markout, basis/context, source age and regime stability are all reported.
- The final recommendation is exactly one of:
  - `signal_contract_accepted_for_shadow`
  - `signal_contract_needs_repair`
  - `signal_contract_rejected`
- Only `signal_contract_accepted_for_shadow` may unlock creation of `0625T004`.

verify：
- `python -m pytest examples/hyperliquid/test_binance_led_pricing_signal_runner.py -q`
- `python examples/hyperliquid/binance_led_pricing_signal_runner.py --help`
- Run the signal runner over the accepted T002 local artifact set.
- Parse JSON manifests with `python -m json.tool`.
- Validate CSV schemas, non-empty files, row counts, boundary flags and recommendation enum.
- `git diff --check`

done：
- Report the accepted sample ids, train/evaluation boundary, horizon, direction/markout summary, basis/context and source-age summary, and regime stability result.
- State the exact final recommendation and whether T004 may be created.
- Explicitly state that T003 did not modify live behavior, did not place orders, did not read credentials, and did not authorize shadow/live promotion.
- End in `待验收` if execution and local verification complete; use `阻塞` if accepted T002 inputs are unavailable on the current machine; use `未通过` for invalid evidence or boundary violation.
- Include commit id and commit message.
```
