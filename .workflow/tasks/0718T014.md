# 0718T014 AGGREGATE-EXPOSURE-RUNTIME-ENVELOPE

执行线程：
- 总控 auto-loop / 业务实现线程

任务ID：
- 0718T014

标题：
- Aggregate exposure and runtime envelope enforcement

简短描述：
- Implement Principal Alignment Task 3: calculate worst-case aggregate long/short exposure across current position, working leaves, cancel-pending leaves, inflight submissions, and proposed quotes.
- Enforce the strictest configured-vs-global position, notional, and submission caps at the executor runtime boundary.

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- 0718T013

规则更新提醒：
- 无

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `docs/superpowers/plans/2026-07-17-principal-alignment-p0-p3.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files:
- `examples/hyperliquid/hyperliquid_tiny_live_real_order_executor.py`
- `examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- `examples/hyperliquid/hyperliquid_tiny_live_m2_fill_window.py`
- `examples/hyperliquid/test_hyperliquid_tiny_live_real_order_executor.py`
- `examples/hyperliquid/test_hyperliquid_kill_switch.py`
- `examples/hyperliquid/test_hyperliquid_tiny_live_m2_fill_loop.py`

action:
- Add typed `ProjectedExposure` and the `projected_exposure()` / `validate_runtime_envelope()` interfaces from the Principal Alignment plan.
- Count working, cancel-pending, and unknown/inflight leaves conservatively until exchange confirmation; aggregate all proposed quote quantities before validating caps.
- Keep reduce-side orders possible when they reduce current inventory, while preventing a sell-through or buy-through from creating a capped opposite-side position.
- Apply the stricter of task config and global defaults for position, notional, and submission caps.
- Require live callers to provide the runtime projection and prior submission count; current canary, watcher, and fill-window paths obtain a pre-submit private position/open-order snapshot and pass it to the executor.
- Preserve the existing anti-drift operational upper bound of 30 only when explicitly configured; formal tiny-live config remains the stricter default of 2.
- Integrate the runtime check into the executor submit boundary without changing quote policy or enabling any live endpoint.

verify:
- Focused executor tests for long/short worst-case aggregation, cancel-pending/inflight treatment, multi-level aggregate caps, reduce-side allowance, strict task caps, and submission budget.
- Existing executor and kill-switch regression tests.
- `python -m py_compile` for modified Python files.
- `git diff --check`.
- Explicitly confirm no live/private/order/cancel/network/remote/service action.

done:
- Report changed files, runtime envelope semantics, focused and related test counts, commit id, and explicit endpoint-action confirmation.
