# 0720T030 MANAGER-PUMP-DEMAND-ACK-STOP-LIFECYCLE-REPAIR

执行线程：
- 总控 auto-loop / 业务执行线程

任务ID：
- 0720T030

标题：
- Prevent manager public-event pump prefetch and acknowledge bounded shutdown

简短描述：
- Repair the single T029 QA P2 finding without changing the accepted cancel deadline or estimator replay behavior。
- Require one trading-thread demand for every pump read so disconnect/stop cannot resume the source or reconnect after the manager cycle。

状态：
- 执行中

执行顺序：
- 当前唯一任务

前置任务：
- `0720T029`（未通过；deadline/replay accepted，stop-lifecycle P2 only）

规则更新提醒：
- 本任务 offline-only。
- Observe-only tiny-live、dynamic spread、fill feedback、inventory skew 和 multi-level 继续锁定。
- 不得修改 T029 已接受的 estimator replay、quote、risk、submission 或 activation behavior。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `.workflow/reports/0720T029-qa.md`
- `docs/superpowers/plans/2026-07-17-principal-alignment-p0-p3.md`

是否需要提交代码：
- 需要

提交要求：
- 先提交 implementation。
- 再提交 task/business/tracker report。
- 独立 QA 单独验收。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

source：
- Branch: `cross-exchange`
- Dispatch base: `e9cb573`

files：
- Modify: `examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- Test: `examples/hyperliquid/test_hyperliquid_tiny_live_m2_event_driven_watcher.py`
- Workflow: this task, business report, QA report, fixed QA document, `task_plan.md`, `progress.md`, `findings.md`

action：
- Add per-event demand/ack between the trading thread and the single public-event pump。
- The pump may call `next(source_iterator)` only after receiving one explicit read request；after publishing one result it must wait for another request。
- The trading thread may request the next read only after it has processed the prior result and confirmed the hold remains active。
- Disconnect、source exhaustion、exception、deadline or observer stop must not authorize another read。
- Wake and terminate an idle pump with an explicit stop request；perform bounded join/ack when no source read is in flight。
- An arbitrary blocking in-flight iterator may remain daemonized after deadline, but after it returns it must observe stop and exit without publishing or initiating another read。
- Preserve the T029 `0.25s` cancel deadline and built-in websocket timeout behavior。
- Preserve all estimator replay code and tests unchanged。

verify：
- A fake first connect failure yields one disconnect；after full manager cycle return the connect call count remains exactly `1`。
- No `manager-hold-public-event-pump` thread from the cycle remains alive after the bounded idle-stop acknowledgement。
- A 3s hold with a 3.6s blocking iterator still begins first cancel no later than deadline plus `0.25s`，cancels twice and leaves no owned open order。
- Public-state mutation remains on the trading thread。
- Source exhaustion、disconnect and observer exception still fail closed while cancel continues。
- T029 replay hostile tests remain unchanged and pass。
- T026 remains accepted with zero confirmed exposure；T016/T022 remain historically blocked。
- Focused and full Hyperliquid tests pass。
- No live/private/network/remote/service action occurs。

stop conditions：
- Pump can call a second `next()` without a second trading-thread demand。
- Disconnect or manager-cycle completion can still trigger a later reconnect/connect。
- Idle pump cannot acknowledge stop inside the cancel-check bound。
- Cancel deadline regresses beyond hold deadline plus `0.25s`。
- Public state is mutated by the pump thread。
- Any estimator replay、strategy、risk or activation behavior changes。
- Any live/private/network/remote/service action occurs。

done：
- The T029 stop-lifecycle P2 is repaired with a built-in disconnect/reconnect hostile test。
- Manager pump shutdown is bounded when idle and non-prefetching when a read is in flight。
- Task 8 observe-only tiny-live may be dispatched only after independent QA accepts this task。

blockers：
- Observe-only live remains locked until T030 independent QA passes。
