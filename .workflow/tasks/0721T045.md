# 0721T045 MANAGER-BATCH-FRESHNESS-VERIFIER-REPAIR

执行线程：
- 总控 auto-loop / 业务执行线程

任务ID：
- 0721T045

标题：
- Repair strict same-window binding for shared manager-batch freshness

简短描述：
- 修复same-window verifier把一次two-sided manager batch freshness refresh误当成每侧独立refresh的P2 false-negative。
- 只在manager身份和唯一batch evidence均被严格证明时允许共享；所有缺失、重复、冲突或跨边界证据继续fail-closed。

状态：
- 执行中

执行顺序：
- 当前唯一任务

前置任务：
- `0721T044`（已通过，但不是same-window accepted baseline）

规则更新提醒：
- 本任务offline-only；不得执行live/private/account/network/remote/service/order/cancel操作。
- 不得修改、补写或重新seal T044 immutable evidence。
- 不得借修复放宽submit-decision、lifecycle或其他逐attempt join。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `.workflow/reports/0721T044-qa.md`
- `docs/superpowers/plans/2026-07-17-principal-alignment-p0-p3.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交。
- 回报必须带`commit id`和提交信息。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

source：
- Dispatch base：`2bc8cd4f7ee27eb12b4c7d0e7e7933c2d2186b3a`
- Immutable target evidence：
  `local_live_analysis/principal_alignment_estimator_observe_only_0721T044/`

files：
- Modify: `examples/hyperliquid/cross_exchange_t024_same_window_acceptance.py`
- Modify: `examples/hyperliquid/test_cross_exchange_t024_same_window_acceptance.py`
- Modify only if needed for strict producer integration coverage:
  `examples/hyperliquid/test_hyperliquid_tiny_live_m2_event_driven_watcher.py`
- Workflow: task、business、QA、fixed QA、`task_plan.md`、`progress.md`、`findings.md`

action：
- 保留`(event_sequence, attempt)` exact freshness join为首选。
- Exact join缺失时，仅对明确的exchange-reconciled two-sided manager batch考虑共享freshness。
- 共享资格必须同时证明：
  - inline manifest明确启用exchange-reconciled manager；
  - 同一event恰好一条freshness row；
  - freshness row属于该batch的首个canonical attempt；
  - 同一event恰好两个submitted manager attempts；
  - attempts为唯一`buy/sell`，attempt id/key/window/task identity完整且一致；
  - 两侧projection字段均非空，并与唯一freshness row逐字段完全相同；
  - freshness状态、时间顺序和既有semantic checks均通过。
- 不允许按event对任意single-side、skipped、non-manager或identity-invalid row回退。
- 对ambiguous duplicate、conflicting projection、cross-event、cross-window/task、blocked/stale freshness、empty projection和malformed identity新增hostile regressions。
- 增加`0721T038+` strict decision-evidence路径的two-sided batch positive regression。
- 使用修复后的verifier只读回放immutable T044到`/tmp`。

verify：
- Focused acceptance tests。
- Strict two-sided manager batch positive和全部新增hostile tests。
- Watcher/acceptance相关combined regression。
- Full `examples/hyperliquid` regression。
- `py_compile`和`git diff --check`。
- Immutable T044 acceptance必须exit `0`、decision `43/0`、lifecycle `78/0`、mechanism/evidence pass、offline-only。
- T044 replay snapshot、live facts、account facts和artifact checksum不得变化。
- Historical artifacts只允许因该明确P2语义修复发生可解释变化；不得覆盖或重封原artifact。

done：
- Verifier能独立证明一次batch-level freshness refresh对exact two-sided manager attempts的投影。
- 所有宽松event fallback和身份/投影伪造继续blocked。
- Immutable T044成为same-window accepted fixed-quote baseline，或新的首个formal blocker被准确记录。
- Dynamic spread、fill feedback、inventory skew和multi-level activation仍保持锁定。

blockers：
- 无。

提交信息：
- `Dispatch T045 manager freshness verifier repair`
