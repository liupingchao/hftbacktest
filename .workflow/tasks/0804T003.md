# 任务派发

执行线程：
- 业务线程-python/tutorial-notebooks

任务ID：
- 0804T003

标题：
- EXAMPLES-TUTORIAL-NOTEBOOK-TARDIS-MODE

简短描述：
- 将 0804T002 已验证的 Tardis 复现能力接入前三个系列的九个原始
  notebook，使其在 Mac sample 和 amdserver 日数据挂载上可直接
  `Run All` 测试。

状态：
- 执行中

执行顺序：
- 与 `0804T001` 可并行；文件范围独立。
- 本任务内部按九个 notebook 的既定顺序串行验收。

前置任务：
- `0804T002` 已通过，提供共享 Tardis runner 和服务器运行证据。

规则更新提醒：
- 无

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `AGENTS.md`

是否需要提交代码：
- 需要

提交要求：
- 仅提交本任务 notebook、共享 notebook helper、生成器、测试和报告。
- 不混入工作区已有的 Hyperliquid 或文档修改。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 核对九个 notebook 均可 Run All、实验映射和顺序正确、无下载和
  Tardis Key 依赖、L3 边界如实，并检查 amdserver 执行产物。

files：
- `examples/Getting Started.ipynb`
- `examples/Working with Market Depth and Trades.ipynb`
- `examples/Data Preparation.ipynb`
- `examples/Fusing Depth Data.ipynb`
- `examples/Order Latency Data.ipynb`
- `examples/Impact of Order Latency.ipynb`
- `examples/Accelerated Backtesting.ipynb`
- `examples/Level-3 Backtesting.ipynb`
- `examples/Integrating Custom Data.ipynb`
- `examples/tutorial_reproduction/notebook_support.py`
- `examples/tutorial_reproduction/refresh_notebooks.py`
- `examples/tutorial_reproduction/test_notebook_support.py`

action：
- 自动检测 amdserver 和 Mac Tardis 根目录及默认日期。
- 支持环境变量覆盖数据根、日期、窗口和输出目录。
- 每个 notebook 只执行自身映射的实验，并共享 prepared-data 缓存。
- 原教程代码保留为 markdown reference，避免 Run All 访问旧下载链接、
  缺失文件或非 Tardis 数据。
- 使用 nbformat 结构化生成 notebook，不做文本级 JSON 拼接。

verify：
- notebook helper focused pytest。
- 九个 notebook `nbformat.validate`、活动代码语法检查和映射检查。
- Git push 后 amdserver fast-forward。
- amdserver 使用 `jupyter nbconvert --execute` 按顺序执行九个 notebook。
- 检查每个 notebook 执行输出和共享 manifest。
- `git diff --check`。

done：
- 九个 notebook 可在 amdserver 直接打开并 Run All。
- 所有活动单元格只使用现有 Tardis 数据。
- L3 notebook 明确展示 `blocked_expected`，不伪造 MBO 结果。
- 大体积输出留在 ignored output root，不进入 Git。

hard boundary：
- 不使用 Tardis Key，不下载新数据。
- 不访问交易所 private/account/order/cancel 接口，不下单。
- 不把 L2 数据声明为真实 L3/MBO。
