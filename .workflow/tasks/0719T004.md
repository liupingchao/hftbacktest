# 0719T004 REDACTION-SAFE-REFERENCE-IDENTITY-STRICT-ATTEMPT-REPAIR

执行线程：
- 总控 auto-loop / 业务实现线程

任务ID：
- 0719T004

标题：
- Redaction-safe persisted reference identity and strict attempt parsing

简短描述：
- Repair the T027 QA P1 findings by introducing stable non-sensitive oid/cloid identity tokens that survive artifact redaction and by rejecting malformed attempt identities without integer truncation.
- This task is offline-only and does not authorize live/private/account/order/cancel/network/remote/service actions.

状态：
- 执行中

执行顺序：
- 当前唯一任务

前置任务：
- `0719T003`（未通过；all-token/raw-proof logic passed, persisted redaction identity and fractional attempt handling failed）

规则更新提醒：
- Live remains the primary strategy data source, but execution-safety and evidence-integrity gates must pass first.
- No new live task may be created or started until this task is independently QA accepted.
- Multi-level, dynamic spread, fill feedback, inventory skew and actual quote behavior changes remain off.

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`

是否需要提交代码：
- 需要

提交要求：
- 离线 focused/full regression 通过后提交。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- Modify: `examples/hyperliquid/hyperliquid_tiny_live_m2_fill_window.py`
- Modify if required: `examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- Modify: `examples/hyperliquid/cross_exchange_t024_same_window_acceptance.py`
- Modify/add focused tests under `examples/hyperliquid/`
- Workflow: this task, business report, QA report, `docs/qa-acceptance-report.md`, `task_plan.md`, `progress.md`, `findings.md`

action：
- Define a versioned non-sensitive identity contract:
  - derive `oid_token` and `cloid_token` as stable SHA-256 tokens before artifact write;
  - token format must be deterministic, validated and unaffected by existing `executor.redact`;
  - persisted raw tracked refs and cancel rows carry these token fields;
  - raw oid/cloid fields remain subject to existing redaction policy;
  - reference keys use attempt plus opaque tokens, never raw oid/cloid.
- Producer reconciliation:
  - derive/validate opaque tokens from raw identity;
  - reject conflicting supplied token versus raw identity;
  - retain all-token unique same-reference matching using opaque tokens;
  - emit the new versioned schema.
- Acceptance independent reconstruction:
  - consume persisted opaque tokens from raw proof;
  - independently validate token format and all-token consistency;
  - independently rebuild the same versioned reference keys after raw oid/cloid redaction;
  - require exact equality with both producer summaries.
- Strict attempt identity:
  - accept only positive integer JSON numbers or canonical positive-integer strings;
  - reject bool, float (including integral float), fractional, zero, negative, NaN/Infinity, scientific notation, whitespace-padded and other noncanonical values;
  - use the same semantic contract independently in producer and acceptance without sharing helper code.
- Add real persisted artifact integration:
  - generate standalone multi-attempt producer artifact through its writer;
  - generate two-sided manager producer artifact through its writer;
  - reload persisted `cancel_shutdown_proof.json`;
  - independently rebuild and require exact equality;
  - place persisted proof into acceptance fixture and require nominal mechanism pass.
- Preserve T026/T027 adversarial regressions.
- Keep quote, strategy, risk, envelope and activation behavior unchanged.

verify：
- T027 QA persisted standalone and manager exact-equality reproductions pass.
- Fractional alias `reference=1.1 / cancel=1.9` fails producer and acceptance.
- Bool, integral float, NaN/Infinity and noncanonical string attempt matrix fails closed.
- Canonical integer attempts continue to pass.
- Redacted persisted raw proof rebuild equals both producer summaries exactly.
- Partial/conflicting target, forged summary, ambiguous raw response and original cross-attempt regressions remain fail closed.
- Modified Python `py_compile`, CLI `--help`, `git diff --check`.
- Focused fill attribution/fill loop/manager/watcher/acceptance regressions.
- Full `python -m pytest examples/hyperliquid -q`.

stop conditions：
- Reference key still contains raw oid/cloid.
- Persisted raw proof cannot independently reproduce the stored summary after redaction.
- Any malformed attempt can alias a valid attempt.
- Acceptance imports/calls producer reconciliation/token helpers.
- Any strategy behavior, live envelope or activation change.

done：
- Persisted raw proof and producer summary share one redaction-safe deterministic identity contract.
- Producer-written standalone and manager artifacts pass independent acceptance reconstruction.
- Strict attempt identity is fail-closed in producer and acceptance.
- Independent QA accepts the offline repair.

blockers：
- No new live task may start before this task is QA accepted.

commit：
- 待填写

提交信息：
- 待填写
