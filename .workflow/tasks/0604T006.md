```md
执行线程：
- 业务线程-research

任务ID：
- 0604T006

标题：
- Canonical signal quality ranking

简短描述：
- Milestone 1 read-only worker：基于 `0604T004` canonical evidence loader foundation 和 `0604T003` canonical event-mode artifacts，对 `0601T004` 接受的四个 Binance lead allowlist features 做 signal quality ranking，并输出 keep/watch/reject 列表；不得做 regime selection 或 maker strategy。

状态：
- 待验收

执行顺序：
- 可并行

前置任务：
- `0604T004` QA 已通过。

规则更新提醒：
- 请先阅读规则文档后再执行本任务。
- 本任务只做 read-only signal quality ranking；不做 regime selection、不做 case-library、不做 shadow decisions、不实现 maker strategy、不接 private/order、不做参数搜索、不 live/default-on/tiny-live、不 promotion。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `docs/thread-playbook.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交任务文件、业务报告、代码/test 修改、task-scoped verification artifacts 和必要 workflow tracking 更新。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0604T006.md`
- `.workflow/reports/0604T006-business.md`
- `examples/hyperliquid/canonical_event_mode_evidence.py` only if a small reusable import/API adjustment is needed.
- `examples/hyperliquid/canonical_signal_quality_ranking.py`
- `examples/hyperliquid/test_canonical_signal_quality_ranking.py`
- `local_live_analysis/canonical_signal_quality_ranking_0604T006/**`
- 必要 workflow tracking 文件仅限本任务状态/结论更新。

read-only inputs：
- Canonical loader foundation:
  - `local_live_analysis/canonical_event_mode_evidence_0604T004/**`
- Formal canonical source:
  - `local_live_analysis/event_mode_canonical_pricing_signal_0604T003/**`
- Primary allowlist source:
  - `docs/binance_led_hyperliquid_maker_data_input_contract.md`
  - `local_live_analysis/binance_led_hyperliquid_data_contract_0601T004/feature_decision_table.csv`

forbidden：
- New data collection.
- Regime selection, case-library construction, shadow decision generation.
- Hyperliquid or Binance private/account/order endpoints.
- Order lifecycle, submit/cancel/fill logic.
- Strategy implementation, production config/default changes, parameter search, default-on, tiny-live, promotion.
- Connector/core API changes, standard npz schema changes, canonical Binance maker audit schema changes.
- Treating `100/250ms` fixed-grid or aliased evidence as sufficient canonical independent evidence.

action：
- Implement a read-only ranking runner that consumes only the canonical event-mode evidence set.
- Rank exactly the accepted Binance lead allowlist features unless the source contract says otherwise:
  - `binance_top5_imbalance`
  - `binance_microprice_minus_mid_ticks`
  - `binance_mid_move_ticks_from_prev`
  - `binance_top5_bid_qty`
- Ranking dimensions must include at least:
  - direction consistency across canonical samples
  - effect size
  - mean absolute correlation
  - usable row/sample count
  - independent future-row-delta count
  - sample concentration / single-sample dominance
  - reliance on `100/250ms` versus `500ms+`, especially `1000ms+`
- Generate task-scoped outputs:
  - `signal_quality_ranking.csv`
  - `signal_quality_reject_watch_list.csv`
  - `signal_quality_ranking_manifest.json`
  - `signal_quality_ranking_report.md`
- Keep the output as research evidence only:
  - allowed final buckets: `keep_for_read_only_research`, `watch_regime_dependent`, `reject_for_canonical_signal_ranking`
  - no maker action, side, quote behavior, default-on, tiny-live, or promotion classification.

acceptance criteria：
- Runner refuses non-canonical or diagnostic-only synthetic inputs through the `0604T004` loader/guard path.
- Ranking output includes all four allowlist features and no non-allowlist primary feature.
- Ranking explicitly distinguishes stable multi-sample / independent-horizon evidence from `100/250ms` weakly independent evidence.
- Report includes a keep/watch/reject list with reasons, including whether `binance_mid_move_ticks_from_prev`, `binance_top5_imbalance`, `binance_top5_bid_qty`, and `binance_microprice_minus_mid_ticks` match or contradict the current controller interpretation.
- Tests cover ranking score/order behavior, reject/watch classification, single-sample concentration penalty, short-horizon reliance penalty, and non-canonical input refusal.
- Boundary flags explicitly remain no collection, no regime selection, no strategy/private/order/live/parameter/default-on/tiny-live/promotion.

verify：
- `python examples/hyperliquid/canonical_signal_quality_ranking.py --help`
- `python -m py_compile examples/hyperliquid/canonical_signal_quality_ranking.py examples/hyperliquid/test_canonical_signal_quality_ranking.py`
- `python -m pytest examples/hyperliquid/test_canonical_signal_quality_ranking.py`
- Run the ranking runner on `local_live_analysis/event_mode_canonical_pricing_signal_0604T003/` and write `local_live_analysis/canonical_signal_quality_ranking_0604T006/`.
- Parse generated JSON manifests with `python -m json.tool`.
- `git diff --check`

done：
- Business report must include ranking method, feature ranking table summary, keep/watch/reject reasons, artifact paths, verification commands, blockers/caveats if any, commit id, and commit message.
- Business report must explicitly state this is only read-only signal quality ranking and does not authorize regime selection, strategy implementation, private/order endpoints, live/default-on/tiny-live, parameter search, or promotion.
- Business report should end in `待验收` if verification passes.
```
