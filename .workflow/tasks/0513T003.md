```md
执行线程：
- 测试线程

任务ID：
- 0513T003

标题：
- 5-13-day-control-15min T002 live-data 验证

简短描述：
- 将 `0513T002` 的 MarketView provenance / top5 audit transparency 最小实现同步到 `awsserver1`，采集 15 分钟 no-rule control live 样本 `5-13-day-control-15min`，拉回并跑 live/replay 对齐、maker acceptance 和 provenance 字段检查，用真实 live 输出验证 T002 的 audit 字段链路。

状态：
- 执行中

执行顺序：
- 当前唯一任务

前置任务：
- 0513T002

规则更新提醒：
- 本任务只验证 T002 的透明化字段链路，不修改交易规则、不做 live promotion、不评估新 alpha 或新风控候选。
- 远端 live 采集已由总控明确授权，采集时长限制为 15 分钟。

按线程规则执行：
- .workflow/workflow-kit/thread-report-template.md
- docs/thread-playbook.md
- .workflow/tasks/0513T002.md
- .workflow/reports/0513T002-business.md

是否需要提交代码：
- 需要，仅用于 git 同步到 `awsserver1`

提交要求：
- 可以创建一个窄范围部署同步 commit/branch，内容限于 T002/T003 相关文件。
- 回报必须写 `commit id` 和远端同步方式。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

Allowed files / outputs：
- `.workflow/tasks/0513T003.md`
- `.workflow/reports/0513T003-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `.workflow/dashboard.html`
- `.workflow/dispatch_suggestions.md`
- `local_live_analysis/5-13-day-control-15min/**`
- `local_live_analysis/archive/5-13-day-control-15min.tar.gz`
- `local_live_analysis/archive/5-13-day-control-15min.tar.gz.sha256`

Allowed remote actions：
- 检查 `awsserver1:/home/admin/hft_live` 当前状态。
- 将 T002 已修改的四个 strategy-layer 文件同步到远端 repo 对应路径。
- 如远端 live 运行依赖的 schema 文件落后于本地当前代码，可同步 `examples/binance_tick_mm/audit_schema.py` 作为远端部署兼容更新；这不是本地 T002 代码扩展。
- 创建 `/home/admin/hft_live/runs/5-13-day-control-15min/` 运行目录和 run-local config。
- 启动 no-rule control live 采集 15 分钟并停止。
- 拉回 run artifacts 到本地 `local_live_analysis/5-13-day-control-15min/`。

Forbidden actions：
- 修改策略规则、fair/target 公式、risk guards、submit/cancel/reduce-side 规则或默认参数。
- 开启 T005/T007 toxic timing guard 或任何新候选规则。
- 修改 core `hbt.depth(0)` API、converter/npz schema、connector book management。
- 修改 live deployment scripts 作为持久变更。
- 将本次采集解释为 live promotion 或交易效果验收。

Fatal Failures：
- 实盘采集不是 no-rule control。
- 采集时长明显超过 15 分钟且没有记录原因。
- audit live 没有输出 T002 新增 provenance 字段。
- replay/audit overlay 后无法区分 `live_depth`、`replay_depth`、`audit_overlay` 或 top5 source。

action：
- 检查远端 repo/运行目录状态。
- 同步 T002 实现文件到 `awsserver1`，并在发现远端 schema 滞后时同步本地当前 `audit_schema.py`。
- 启动 `5-13-day-control-15min` 15 分钟 live control 采集。
- 使用 `align_live_run.py` 拉回、转换、运行 normal replay 和 audit replay，并归档。
- 使用 `maker_acceptance.py` 验证 action-path 对齐。
- 增加只读 provenance 检查：
  - live audit 应包含 T002 新字段，且 `market_view_source=live_depth`、`top5_source=live_depth`。
  - normal replay audit 应包含 T002 新字段，且 `market_view_source=replay_depth`、`top5_source=replay_depth`。
  - audit replay overlay 应能标出 `market_view_source=audit_overlay` / `market_overlay_source=audit`，且 top5 provenance 不应被伪装成 full L2 对齐。

verify：
- 远端启动前后记录 run start/stop marker。
- `python examples/binance_tick_mm/align_live_run.py --run-id 5-13-day-control-15min --local-root local_live_analysis --remote-host admin@awsserver1 --remote-root /home/admin/hft_live`
- `python examples/binance_tick_mm/maker_acceptance.py --alignment-report local_live_analysis/5-13-day-control-15min/alignment_report_audit_replay.json --backtest-result local_live_analysis/5-13-day-control-15min/backtest_audit_replay_result.json --out local_live_analysis/5-13-day-control-15min/maker_acceptance.json`
- provenance 字段只读检查脚本/命令。

done：
- 回报必须说明远端更新方式、采集起止时间、行数、acceptance 结果、provenance 字段事实和 T002 是否通过真实 live 输出验证。
- 回报必须明确本任务不是 live promotion，不证明策略收益改善。
- 回报必须写本次 git 同步使用的 `commit id`。
```
