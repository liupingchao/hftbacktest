# 任务派发

执行线程：
- 业务线程-python/public-data-infra

任务ID：
- 0802T001

标题：
- SKHYNIX-5H-CLOUD-COLLECTION-SOAK

简短描述：
- 在保留的 c6in winner 上启动一次 5 小时 SKHYNIX 双交易所
  information-max public-data 采集。
- 采集按 10 个连续 30 分钟 segment 执行，由 transient systemd 托管。

状态：
- 待验收

执行顺序：
- 当前唯一运行任务；与已完成的本地研究任务不存在写入冲突。

前置任务：
- `0729T009` 已通过。
- `0730T011` 已完成 auxiliary reconnect degraded-interval 修复并部署。
- 用户已明确授权本次 5 小时采集。

规则更新提醒：
- 无。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 不需要

提交要求：
- 本任务只启动正式采集并记录启动证据，不创建代码提交。
- 回报使用 `commit：无`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 采集结束、数据检查和本地拉回完成后再进行正常验收。

files：
- `.workflow/tasks/0802T001.md`
- `.workflow/reports/0802T001-business.md`
- remote output:
  `/home/admin/0802T001_skhynix_5h_10x30m`

action：
- Host: `c6in-winner / i-0a962e47210528526`.
- Profile: `skhynix`.
- Binance: `SKHYNIXUSDT`.
- Hyperliquid: `xyz:SKHX`.
- 保留 research-max fast/standard L2、BBO、trades、asset context、
  main allMids 和 named-dex allMids 轨道。
- Total duration: `18000s`.
- Segment duration: `1800s`.
- Segment count: `10`.
- Start as transient systemd unit:
  `hftbacktest-0802t001-skhynix-5h.service`.
- 启动确认后立即返回，不持续监控、不等待完成、不拉取数据。
- Public market data only；禁止 private/account/order endpoint。

verify：
- 检查磁盘、内存、sudo、SSH、冲突进程、campaign path、lock 和 unit。
- 验证本地与远端 supervisor、collector、registry、timeline source SHA。
- 验证 systemd start request 成功、unit 为 active/running、MainPID 非零。
- 验证 `run_status.json`/`heartbeat.json` 或首个 segment 目录出现。

done：
- 十段 raw collection 已完成，但 campaign 在 strict timeline
  postprocess 的第 1 段失败。
- 失败原因是 Hyperliquid fast L2 source age `2034.006653ms` 超过冻结的
  `2000ms` 上限。
- 本轮不拉回、不进入 R0/R1；任何阈值或可用性修复必须单独派发并验收。

hard boundary：
- 不监控五小时运行进度。
- 不停止或修改 `awsserver1`。
- 不访问私有交易端点，不提交订单。
- 不声称数据已经采集完成。
