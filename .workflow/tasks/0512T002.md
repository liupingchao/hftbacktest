```md
执行线程：
- 业务线程-python

任务ID：
- 0512T002

标题：
- add-side submit/re-add toxic timing rule 设计合同

简短描述：
- 在 `0512T001` 解释 Stage 6J replay 与 live adverse-selection source-path 差异、并由 `0512T004` 补齐观测门禁后，重新设计 adverse-selection timing rule 的合同。新规则目标不是检测已有 working quote 的 target deterioration，而是覆盖 add-side submit/re-add 前的 toxic timing，并定义可观测 audit 字段、验收指标和 no-live 边界。本任务只做设计合同，不实现策略代码。

状态：
- 已通过

执行顺序：
- `0512T001` 和 `0512T004` 完成并通过 QA 后执行

前置任务：
- 0512T001
- 0512T004

规则更新提醒：
- 本任务是设计合同，不改策略代码。
- Stage 6J replay 当前只能作为 replay-model regression gate；不能单独证明 live adverse-selection source-path 改善。
- 本任务必须使用 `0512T004` 给出的观测门禁/替代指标，区分 action-path coverage、replay-model regression、live source-path proof 三类证据。
- 本任务必须沿用 `0512T004` 的样本分层：`5-11-night-active` 是主要开发/诊断样本；`5-10-day-control-1h-06`、`5-9-noon`、`5-9-small` 是 cross-sample sanity check。
- 第一目标是覆盖 add-side submit/re-add path，而不是调旧的 `target_deterioration` 阈值。
- 必须保持 default-off、add-side only block、reduce-side allowed。
- 必须使用 decision-time-visible 输入；future markout 只能作为验收指标。

按线程规则执行：
- .workflow/workflow-kit/thread-report-template.md
- docs/thread-playbook.md
- docs/5-8-future-plan.md
- docs/stage6i-cancel-requested-fill-risk-plan.md

是否需要提交代码：
- 不需要

提交要求：
- 回报必须写 `commit：无`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- .workflow/reports/0511T004-business.md
- .workflow/reports/0512T001-business.md
- .workflow/reports/0512T003-business.md
- .workflow/reports/0512T004-business.md
- local_live_analysis/5-11-night-active/
- examples/binance_tick_mm/strategy_core.py
- examples/binance_tick_mm/live_tick_mm.py
- examples/binance_tick_mm/backtest_tick_mm.py
- examples/binance_tick_mm/stage6j_replay.py
- examples/binance_tick_mm/analyze_cancel_fill_risk.py
- findings.md
- progress.md

Fatal Failures：
- 把本任务变成代码实现。
- 继续沿用旧 trigger，只调整 threshold。
- 把 Stage 6J replay 的 source-path 指标当成 live source-path 充分证明。
- 绕过 `0512T004` 的观测门禁，直接设计晋级标准。
- 只在 `5-11-night-active` 单样本上定义成功条件，缺少 cross-sample sanity check。
- 设计无法证明覆盖 `submit_buy` / `submit_sell`。
- 设计依赖 future markout、future mid、audit overlay 或 live 当时不可见诊断输出。
- 设计没有 no-live 边界或回滚路径。

MUST NOT：
- 不改策略代码。
- 不启动真实 live，不连接交易所，不改 AWS 状态。
- 不默认开启任何新规则。
- 不把 PnL 作为唯一晋级指标。
- 不跳过 `0512T001` 的 replay/source-path 观测结论。
- 不跳过 `0512T004` 对 replay/live adverse-selection 观测门禁的结论。

设计目标：
- 定义一个 default-off 的 add-side submit/re-add toxic timing guard。
- 只阻断 add-side submit/re-add，不阻断 reduce-side submit。
- 覆盖 side 当前没有 working order、策略准备 submit、且近期存在 cancel-request / cancel-fill / target 快速恶化 / latency toxic signal 的场景。
- 第一版设计必须能被 audit 证明：eligible add-side submit count > 0，blocked add-side submit count > 0，blocked rows 与目标风险 path 有重叠。
- 第一版设计必须把验收证据拆成三层：action-path coverage 是必要条件；Stage 6J regression 是辅助门禁；live source-path proof 只能来自 `0512T004` 定义的观测门禁或后续 live micro test。
- 第一版设计必须指定样本使用方式：先用 `5-11-night-active` 做主诊断/开发，再用 `5-10-day-control-1h-06`、`5-9-noon`、`5-9-small` 做 cross-sample sanity check。

允许输入：
- 当前 side 是否有 working order。
- 当前 side 是否准备 `submit_buy` / `submit_sell`。
- position 与 add-side/reduce-side 判定。
- recent same-side cancel request age。
- recent same-side cancel-fill age。
- target tick 自上次 quote / cancel / decision 的变化。
- cancel-requested in-flight exposure。
- feed/order latency signal。
- rolling historical toxicity state，只限当前时点之前。

禁止输入：
- future mid。
- future markout。
- future fill 后价格路径。
- audit overlay state。
- replay-only working overlay。
- replay-only position overlay。
- live 当时不可见的诊断输出。

建议 audit 字段合同：
- `add_side_submit_eligible_buy`
- `add_side_submit_eligible_sell`
- `add_side_submit_blocked_buy`
- `add_side_submit_blocked_sell`
- `add_side_submit_block_reason_buy`
- `add_side_submit_block_reason_sell`
- `target_move_since_last_quote_or_cancel_buy`
- `target_move_since_last_quote_or_cancel_sell`
- `last_cancel_request_age_ms_buy`
- `last_cancel_request_age_ms_sell`
- `last_cancel_fill_age_ms_buy`
- `last_cancel_fill_age_ms_sell`
- `toxic_timing_guard_until_ts_buy`
- `toxic_timing_guard_until_ts_sell`

action：
- 读取 `0511T004` 和 `0512T001` 结论，明确旧 `target_deterioration` 为什么不能直接推广。
- 读取 `0512T004` 结论，明确本任务能使用哪些 replay 指标、哪些 live-derived 指标、哪些指标只能作为诊断不能作为晋级证据。
- 采用样本分层：`5-11-night-active` 为主开发/诊断样本；`5-10-day-control-1h-06`、`5-9-noon`、`5-9-small` 为 cross-sample sanity check。
- 定义新 guard 的输入、状态、side-specific block 语义、default-off 配置和 audit 字段。
- 定义 live/backtest 共享 helper 的合同，禁止复制两套逻辑。
- 定义 Stage 6J 候选矩阵：baseline、add-side guard、new toxic timing 50/100/200ms、add-side guard + toxic timing、broad cooldown control。
- 定义验收指标：action path coverage、blocked submit count、reduce-side allowed、source-path 改善、drop/churn/position/PnL 辅助指标。
- 定义样本级验收表：每个 candidate 必须分别报告主样本结果和 sanity-check 样本结果；主样本改善不能单独授权 live。
- 输出 `.workflow/reports/0512T002-business.md`，作为后续实现任务的唯一合同来源。
- 更新 `findings.md`，写入 durable design constraints。

verify：
- `python3 .workflow/build_dashboard.py`
- 报告必须包含 H/E/F/A：假设、证据、证伪条件、证伪动作。
- 报告必须包含 helper 合同、配置合同、audit 字段合同、Stage 6J replay 合同、QA 验收合同。
- 报告必须明确区分 action-path coverage、Stage 6J replay regression、live adverse-selection source-path proof。
- 报告必须明确样本分层和防 overfit 规则。
- 报告必须明确是否允许创建后续实现任务。默认只允许创建实现任务，不允许 live。

done：
- 回报必须明确：旧 `target_deterioration` 不再作为主 trigger。
- 回报必须明确：新规则如何覆盖 add-side submit/re-add path。
- 回报必须明确：如何证明 reduce-side 没被阻断。
- 回报必须明确：如何使用 `0512T004` 的观测门禁，避免把 Stage 6J replay 单独当作 live source-path proof。
- 回报必须明确：如何在 replay 中观察 action-path coverage / regression，以及哪些 source-path 证据必须留给 live-derived analysis。
- 回报必须明确：`5-11-night-active` 是主开发/诊断样本，`5-10-day-control-1h-06`、`5-9-noon`、`5-9-small` 是 cross-sample sanity check。
- 回报必须明确 no live micro test。
```
