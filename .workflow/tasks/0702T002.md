```md
执行线程：
- 业务线程-collector-fix

任务ID：
- 0702T002

标题：
- Binance depth snapshot 限流失败硬门禁修复

简短描述：
- 修复 synchronized public collector 在 Binance REST depth snapshot 被限流或无效时仍产出可后处理样本的问题。
- 降低 top5 bootstrap 所需 snapshot 默认深度，并增加低频退避重试与 manifest 证据，避免再次生成 Binance top5 全空的样本包。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- `0702T001` 采集结果暴露 Binance `depth_snapshot.json` 三段均为 HTTP 429，导致 Binance top5 context 全空。

规则更新提醒：
- 请先阅读 `.workflow/workflow-kit/workflow-manual.md`、`.workflow/workflow-kit/task-dispatch-template.md`、`.workflow/workflow-kit/thread-report-template.md`、`.workflow/workflow-kit/qa-acceptance-template.md`、`task_plan.md`、`progress.md`、`findings.md` 后再执行本任务。

按线程规则执行：
- `AGENTS.md`
- `docs/thread-playbook.md`

是否需要提交代码：
- 不需要

提交要求：
- 本轮不提交，待总控/QA确认后再决定是否提交。
- 回报必须带 `commit：无` 和 `提交信息：无`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0702T002.md`
- `.workflow/reports/0702T002-business.md`
- `examples/hyperliquid/synchronized_public_collection.py`
- `examples/hyperliquid/test_synchronized_public_collection.py`
- `task_plan.md`
- `progress.md`
- `findings.md`

hard boundary：
- 只修 public raw collector 的 Binance REST depth snapshot 获取、限流处理、manifest 证据与失败语义。
- 不修改 0702T001 已采集 raw 数据，不补造 snapshot，不补造 Binance top5 context。
- 不改 Hyperliquid collector 语义，不改 alignment/join/sample expansion 统计口径。
- No live orders, no credentials, no private/account/order/cancel endpoints, no user stream, no live trading client, no watcher strategy change, no signal threshold tuning, no side mapping, no canary, no promotion.

action：
- 检查 Binance depth snapshot 获取路径和 0702T001 失败原因。
- 将 REST snapshot 获取改为低频 retry/backoff，并记录 429/418 rate-limit 证据。
- 将 top5 bootstrap 需要的 snapshot 变成采集成功的硬前置条件：没有有效 `lastUpdateId/bids/asks` 时采集子进程必须非 0 失败。
- 将默认 `snapshot-limit` 从 `1000` 降到 `100`，因为当前 top5 sidecar 只需要 top5 bootstrap，不需要 1000 档高权重快照。
- 给 `collect` 和 `collect-binance-public` 暴露 snapshot limit/retry 参数。
- 增加针对 429 退避和 snapshot 缺失 fail-fast 的回归测试。

verify：
- `python -m pytest examples/hyperliquid/test_synchronized_public_collection.py -q`
- `python -m py_compile examples/hyperliquid/synchronized_public_collection.py examples/hyperliquid/test_synchronized_public_collection.py`
- `python examples/hyperliquid/synchronized_public_collection.py collect-binance-public --help`
- `python examples/hyperliquid/synchronized_public_collection.py collect --help`
- `git diff --check -- examples/hyperliquid/synchronized_public_collection.py examples/hyperliquid/test_synchronized_public_collection.py`

done：
- 报告 root cause、修复点、验证结果和剩余风险。
- 明确 Binance 报错里的 `2400 requests per minute` 是 IP 限额描述，不是本进程已发出 2400 次 snapshot 请求的直接证据。
```
