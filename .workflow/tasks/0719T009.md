# 0719T009 FILL-LIMIT-TERMINAL-CHECKSUM-REPAIR

执行线程：
- 总控 auto-loop / 业务实现线程

任务ID：
- 0719T009

标题：
- Repair side-aware fill-limit semantics and independent terminal checksum verification

简短描述：
- Close both `0719T008` QA P1 findings before any bounded two-sided live task.
- This task is offline-only.

状态：
- 未通过

执行顺序：
- 当前唯一任务

前置任务：
- `0719T008`（未通过）

规则更新提醒：
- T008 accepted sub-results remain authoritative: raw pullback reconstruction, all-token fill identity, canonical argv and runtime executable/script/run-root/output binding.
- No live/private/account/order/cancel/network/remote/service action is allowed in this task.
- Multi-level, dynamic spread, fill feedback, inventory skew and actual quote behavior changes remain off.

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `.workflow/reports/0719T008-qa.md`

是否需要提交代码：
- 需要

提交要求：
- 离线 focused/full regression 通过后提交。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- Modify: `examples/hyperliquid/hyperliquid_tiny_live_m2_fill_window.py`
- Modify: `examples/hyperliquid/cross_exchange_t024_same_window_acceptance.py`
- Modify: focused fill/acceptance/orchestrator/watcher tests
- Workflow: this task, business report, QA report, `docs/qa-acceptance-report.md`, `task_plan.md`, `progress.md`, `findings.md`

action：
- Enforce producer fill execution semantics for reference-bound fills:
  - fill symbol, when present, must equal the canonical attempt symbol;
  - fill side must equal the canonical attempt side;
  - buy fill price must be at or below the buy limit within one explicit numerical tolerance;
  - sell fill price must be at or above the sell limit within the same tolerance;
  - invalid direct-reference fills remain unattributed/fail-closed and cannot fall back to time/price matching.
- Independently enforce the same side-aware limit semantics during raw pullback reconstruction in Task 12 acceptance.
- Ensure full-fill terminal proof consumes only raw fills that satisfy attempt side and limit-price semantics.
- Add an independent terminal SHA-256 manifest verifier in acceptance:
  - parse `remote_sha256_manifest.txt` without importing orchestrator verification helpers;
  - reject malformed lines, invalid digests, duplicate paths, absolute/traversal paths and path escape;
  - require exact equality between manifest entries and current run-root files, excluding only the manifest and verification summary;
  - recompute every current file digest;
  - require the stored verification summary to equal the independently rebuilt counts/status.
- Add synchronized mutation regressions that update raw pullbacks, ledger, attribution, role, summary and stale verification JSON together.
- Keep quote formulas, thresholds, risk caps, activation flags and non-exact legacy behavior unchanged.

verify：
- Exact buy-at/below-limit and sell-at/above-limit fills pass producer and acceptance.
- Buy-above-limit and sell-below-limit fills fail producer attribution and full acceptance.
- Impossible-price raw fills still fail when all derived CSV/summary evidence is synchronized.
- A post-seal artifact mutation with stale verification JSON fails independent manifest verification.
- Malformed, duplicate, traversal, missing, unexpected and digest-mismatched manifest entries fail closed.
- Fresh real manager-watcher zero-fill producer output still passes complete same-window acceptance.
- T005-T008 cancel/raw-order/terminal/all-token/canonical-command regressions pass.
- Modified Python `py_compile`, CLI `--help`, `git diff --check`.
- Full `python -m pytest examples/hyperliquid -q`.

stop conditions：
- Any impossible fill price can serve as terminal proof.
- Any current run-root byte mismatch can pass through a stale checksum summary.
- Any strategy/risk/activation/live boundary changes.

done：
- Both T008 QA P1 findings are closed.
- Real producer zero-fill output and valid limit-respecting filled fixtures pass the exact acceptance contract.
- Independent QA accepts the offline repair.

blockers：
- No live task may start before this task is QA accepted.

commit：
- `6fd4089310a5dbf9694c977808081494ec63e3ac`

提交信息：
- `Close T009 fill limit checksum gaps`
