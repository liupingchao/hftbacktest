# 任务派发

执行线程：
- 业务线程-integrated-offline-acceptance

任务ID：
- 0717T011

标题：
- LIVE-EVIDENCE-INTEGRATED-OFFLINE-ACCEPTANCE

简短描述：
- 使用 offline fake watcher、synthetic fill fixture 和本地 checksum verification，把 `0717T006` Phase 1-4 的四个 repair contract 放进同一条三窗口验收场景。
- 只增加集成验收测试和必要文档，不改变 live watcher、策略、订单参数或生产执行路径。

状态：
- 执行中

执行顺序：
- 当前唯一任务

前置任务：
- `0717T010 / TERMINAL-ARTIFACT-SEAL-REPAIR` QA 为 `已通过`。

规则更新提醒：
- 无

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- focused integration test 通过后提交。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `examples/hyperliquid/test_cross_exchange_live_integrated_offline_acceptance.py`
- `.workflow/tasks/0717T011.md`
- `.workflow/reports/0717T011-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

action：
- 构造三窗口 offline fake orchestration：window 1 成功并重复 pullback 同一 fill，window 2 有两个同价 attempt、一个唯一时间归因 fill 和一个 ambiguous fill，window 3 timeout。
- 使用真实 `LiveFillLedger` 回放 window 1/2 fixture，检查 window/attempt identity、幂等数量、time-bounded attribution 和 ambiguous quarantine。
- 使用真实 orchestrator 检查 child termination/reap、no-next-window、open-orders proof ordering、终态 manifest verification。
- 在 run root 内执行 `sha256sum -c remote_sha256_manifest.txt`。

verify：
- `python -m pytest examples/hyperliquid/test_cross_exchange_live_integrated_offline_acceptance.py`
- Phase 1-4 focused regression tests
- `python -m py_compile examples/hyperliquid/cross_exchange_live_remote_orchestrator.py examples/hyperliquid/hyperliquid_tiny_live_m2_fill_window.py examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- `python examples/hyperliquid/cross_exchange_live_remote_orchestrator.py --help`
- `git diff --check`
- 全部 offline；不执行 live/private/order/cancel/network/remote/service 动作。

done：
- 三窗口 integrated fixture 通过，window 4 不启动。
- window 1/2 的 fill evidence 与 attribution totals 满足 identity、幂等、time-bound 和 fail-closed ambiguity contract。
- window 3 child 被 timeout、终止并 reaped。
- abort/open-orders evidence 完整，manifest 通过 Python verification 和 `sha256sum -c`。
- 不声称策略 profitability、maker viability、promotion 或 live readiness。

not authorized：
- 不执行 live 或 remote。
- 不修改 strategy、quote-policy、order-size、max-submission、max-loss、threshold。
- 不进入新的 live evidence 或 promotion task。
