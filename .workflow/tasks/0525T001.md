```md
执行线程：
- 业务线程-python

任务ID：
- 0525T001

标题：
- Step 9D keep-for-research candidate fine-bucket refinement

简短描述：
- 基于 `0521T002` 的五个 current-format 样本，对三个 `keep_for_research` candidate 做更细的 scenario bucket 拆分，判断它们是否存在局部稳定有效 regime，并输出后续参数搜索 / 补样本 / reject 的决策依据。

状态：
- 待执行

执行顺序：
- 当前唯一任务

前置任务：
- `0521T002` QA 已通过

规则更新提醒：
- 请先阅读规则文档后再执行本任务。

按线程规则执行：
- `.workflow/workflow-kit/thread-report-template.md`
- `docs/thread-playbook.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0525T001.md`
- `.workflow/reports/0525T001-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `examples/binance_tick_mm/quote_adjustment_replay.py` 或新增的窄 runner
- `examples/binance_tick_mm/test_quote_adjustment_replay.py` 或新增 focused test
- `local_live_analysis/stage9d_candidate_bucket_refinement_0525T001/**`
- 只读输入：
  - `local_live_analysis/stage9c_multi_sample_validation_0521T002/**`
  - `local_live_analysis/5-19-day-control-30min/**`
  - `local_live_analysis/5-19-night-active-30min-a/**`
  - `local_live_analysis/5-19-night-active-30min-b/**`
  - `local_live_analysis/5-19-night-active-30min-c/**`
  - `local_live_analysis/5-21-day-control-60min/**`

action：
- 只分析三个 `keep_for_research` candidate：
  - `min_move_quote_age_churn_guard`
  - `inventory_reservation_shift_band`
  - `size_reduction_or_add_side_suppression_pressure`
- 使用 `baseline_control` 作为每个 sample / bucket 内的比较基准。
- 生成 `sample_id x candidate_id x fine_bucket_family x fine_bucket` 级别的细 bucket 表。
- 至少覆盖以下 fine bucket family：
  - volatility / markout dispersion
  - spread / quote distance
  - trade intensity / fill opportunity
  - latency / stale / quote age / join age / anchor age
  - inventory state：flat / mild skew / large skew / add-side / reduce-side
  - API / churn：min-move、quote-age、cancel-readd、token/throttle/reject/drop
  - post-only safety：clamp / suppress / post-check / missing anchor / stale anchor
  - cancel-fill / adverse markout：fill-after-cancel、5s markout、spread capture
  - market-view quality：strict clean / caveated sample / stale or gap caveat
- 每个 bucket 输出：
  - decision rows
  - submit orders
  - filled orders
  - fill rate and delta vs bucket baseline
  - 5s markout and delta vs bucket baseline
  - spread capture delta
  - fill-after-cancel delta
  - fast-cancel churn delta
  - inventory-increasing / reducing fill rates
  - post-only risk after recheck
  - net PnL proxy delta，仅作为 diagnostic，不作为 promotion 主指标
- 跨样本汇总：
  - median delta
  - worst-sample delta
  - win-rate
  - clean-only sensitivity
  - caveated sample influence
  - coverage / fill sufficiency
- 给每个 candidate-bucket 打 verdict：
  - `stable_promising_bucket`
  - `mixed_keep_for_research`
  - `needs_more_fills`
  - `reject_bucket`
  - `not_decisionable`
- 不做参数搜索。
- 不改 live。
- 不 default-on。
- 不放宽 guard。
- 不使用 future markout / future fill outcome / same-sample PnL feedback 作为决策输入。
- 不把任何结果升级为 `ready_for_tiny_live_design`，除非满足既有 Step 9C hard gates；当前预期只是找 parameter-sweep seed 或 sample-gap direction。

verify：
- `python examples/binance_tick_mm/quote_adjustment_replay.py --help`
- 新增 runner 时运行：
  - `python examples/binance_tick_mm/<new_runner>.py --help`
- 在五个 current-format 样本上生成：
  - `local_live_analysis/stage9d_candidate_bucket_refinement_0525T001/fine_bucket_metrics.csv`
  - `local_live_analysis/stage9d_candidate_bucket_refinement_0525T001/fine_bucket_stability_summary.csv`
  - `local_live_analysis/stage9d_candidate_bucket_refinement_0525T001/fine_bucket_stability_summary.json`
  - `local_live_analysis/stage9d_candidate_bucket_refinement_0525T001/candidate_bucket_recommendations.md`
  - `local_live_analysis/stage9d_candidate_bucket_refinement_0525T001/sample_gap_recommendations.csv`
- `python -m pytest examples/binance_tick_mm/test_quote_adjustment_replay.py`
- 如新增测试文件，运行对应 focused pytest
- `python3 .workflow/build_dashboard.py`
- `git diff --check`

done：
- 明确说明三个 candidate 各自是否存在 `stable_promising_bucket`。
- 明确说明哪些 bucket 是后续参数搜索 seed，哪些 bucket 应 reject，哪些 bucket 只是缺 fills。
- 明确说明是否需要补样本，以及补什么 regime，不允许只写“继续补时长”。
- 明确说明是否仍然不满足 `ready_for_tiny_live_design`。
- 明确说明结果是否支持进入后续 multi-sample parameter sweep design。
- 必须给出以下三个判断：
  - `min_move_quote_age_churn_guard` 是不是只在高 churn / quote-age / min-move 相关 bucket 里有效。
  - `inventory_reservation_shift_band` 是不是只在库存偏斜 / recovery-side bucket 有效，还是 aggregate 正向主要来自样本混合。
  - `size_reduction_or_add_side_suppression_pressure` 的大幅 markout 改善是不是来自减少 toxic fill，而不是简单少成交。

blockers：
- 无

commit：
- 无

提交信息：
- 无
```
