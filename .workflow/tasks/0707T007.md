# 任务派发

执行线程：
- 业务线程-live-awsserver1

任务ID：
- 0707T007

标题：
- T010-REPAIRED-HANDOFF-CONTROLLED-LIVE-EVIDENCE-RERUN

简短描述：
- 使用 `0707T006` handoff contract / instrumentation 修复后的代码路径，执行一个 separately authorized controlled live evidence rerun。
- 本任务沿用 `0707T004` 的保守 live envelope，只验证 repaired schema 下的 live evidence；不改阈值、quote envelope、size、max submissions，不声称 full `0625T010` 或 MVP 通过。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- 0707T006

授权记录：
- 用户针对下一步建议 `新建一个 separately authorized controlled live evidence rerun，用这次修复后的 schema 和同样保守 envelope 跑，不要先改阈值` 回复：`继续`。
- 本任务把该回复记录为对本轮 controlled live evidence rerun 的明确授权。
- 本任务使用与 `0707T004` 相同的低风险 live envelope，并仅在 `0707T006` QA 通过后执行。
- `amdserver` 已检查：repo 存在但缺少本轮 live 所需 `/home/admin/.venvs/hyperliquid-sdk-0618T002/bin/python` 与 `/home/admin/XEMM_rust_latest/.env`；因此 live 执行沿用已验证的 `awsserver1`。

规则更新提醒：
- 请先阅读规则文档后再执行本任务。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 本任务文件先提交为 live authorization/dispatch 节点；执行和 QA 结果后另行提交。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

host / execution location：
- `awsserver1`
- Remote repo: `/home/admin/hftbacktest-cross-exchange`
- Required remote interpreter: `/home/admin/.venvs/hyperliquid-sdk-0618T002/bin/python`
- Env file: `/home/admin/XEMM_rust_latest/.env`
- Remote artifact root: `/home/admin/hftbacktest-cross-exchange-artifacts/cross_exchange_t010_handoff_repaired_controlled_live_evidence_0707T007_<timestamp>/`
- Local artifact root: `local_live_analysis/cross_exchange_t010_handoff_repaired_controlled_live_evidence_0707T007_<timestamp>/`

authorized live envelope：
- Symbol: Hyperliquid `BTC`
- Runner: `examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- Mode: `--event-driven-edge-gate-live`
- Watcher seconds: `1800`
- Max real order submissions: `2`
- Max order size: `0.005 BTC`
- TIF: post-only `Alo`
- Quote hold seconds: `3`
- Wait seconds: `10`
- Requote attempts: `2`
- Edge gate, anti-drift, fresh-touch, current public-state, and post-open-orders public-state guards must remain enabled.
- `0707T006` repaired schema must be active: immediate guard artifacts must preserve trigger candidate fields separately from current reprice fields and must be able to emit `post_open_orders_handoff_latency_exceeded`.
- Tracked cancel required for any submitted order.
- Independent final open-orders proof required after the live run.

hard boundary：
- No taker/crossing orders.
- No inside-spread or one-tick-back placement.
- No threshold change.
- No quote-envelope change.
- No size above `0.005 BTC`.
- No max submissions above `2`.
- No default-on or continuous bot.
- No promotion, stable PnL claim, maker viability claim, full T010 pass, T011, T012, or final MVP pass.
- If no order is submitted, report as repaired fail-closed evidence, not success for full T010.
- If no fill occurs, fee/rebate, inventory, realized PnL, and fill model must remain unsupported/fail-closed.

files：
- `.workflow/tasks/0707T007.md`
- `.workflow/reports/0707T007-business.md`
- `.workflow/reports/0707T007-qa.md`
- `docs/qa-acceptance-report.md`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `local_live_analysis/cross_exchange_t010_handoff_repaired_controlled_live_evidence_0707T007*/`

action：
- Commit this task file before live execution.
- Sync local `cross-exchange` to `awsserver1` with a git-safe fast-forward or bundle workflow.
- Run local focused verification before remote live execution.
- Run remote py_compile and CLI help on the actual `awsserver1` venv.
- Execute the handoff-repaired controlled event-driven edge-gated live evidence window on `awsserver1`.
- Pull back all artifacts.
- Run/verify independent final open-orders proof.
- Generate a business report classifying:
  - whether `0707T006` repaired schema was active
  - whether trigger candidate fields and current reprice fields are separated in guard artifacts
  - whether `post_open_orders_handoff_latency_exceeded` appears if the same blocker recurs
  - whether repaired edge source binding remains active
  - whether post-open-orders public-state resync passed or failed closed
  - whether a trigger occurred
  - whether a real order endpoint was called
  - submit/resting/reject/cancel lifecycle
  - fill/no-fill lifecycle
  - fee/rebate/inventory/PnL support or fail-closed unsupported status
  - final open-orders proof
  - route recommendation for full T010

verify：
- `python -m pytest examples/hyperliquid/test_hyperliquid_tiny_live_m2_event_driven_watcher.py examples/hyperliquid/test_hyperliquid_tiny_live_m2_fill_loop.py -q`
- `python -m py_compile examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py examples/hyperliquid/hyperliquid_tiny_live_m2_fill_window.py`
- `python examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py --help`
- Remote py_compile and CLI help on `awsserver1`.
- Remote live run on `awsserver1` with required venv/env file.
- JSON parse and CSV schema/row checks for pulled-back artifacts.
- Independent final open-orders proof.
- Redaction scan.
- `git diff --check`.

done：
- Report remote/local artifact paths, public stream health, trigger count, source binding status, post-open-orders resync pass/block counts, handoff schema evidence, real order endpoint calls, submitted/resting/reject/cancel/fill counts, final open-orders count, independent final open-orders count, route recommendation, and whether full T010 remains blocked or is ready for a separate same-window replay acceptance task.
