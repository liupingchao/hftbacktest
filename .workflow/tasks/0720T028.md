# 0720T028 MANAGER-RESTING-EXPOSURE-EVENT-TIME-CAPTURE

执行线程：
- 总控 auto-loop / 业务执行线程

任务ID：
- 0720T028

标题：
- Capture confirmed manager resting exposure in event-time buckets

简短描述：
- Close the Task 8 evidence-path gap exposed by T026: the manager currently sleeps during quote hold, so live public events are not joined to confirmed resting orders and the online estimator remains `fallback_fixed`。
- Add a conservative, replayable resting-exposure capture path without activating dynamic spread or changing quote/order/cancel decisions。

状态：
- 未通过

执行顺序：
- 当前唯一任务

前置任务：
- `0720T027`（已通过）
- `0718T019`（Task 8 observe-only implementation 已通过）

规则更新提醒：
- 本任务是 offline implementation/evidence gate。
- 不进行 live、private/account、order/cancel、network、remote 或 service 操作。
- T026 immutable evidence缺少可证明的 resting interval start；本任务不得 retroactively 为 T026 生成 confirmed exposure。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `docs/superpowers/plans/2026-07-17-principal-alignment-p0-p3.md`
- `.workflow/reports/0720T027-qa.md`

是否需要提交代码：
- 需要

提交要求：
- 先提交 implementation。
- 再提交 task/business/tracker report。
- 独立 QA 单独验收。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

source：
- Branch: `cross-exchange`
- Dispatch base: `3d93aa26671ae79039061eeec8268981595880f3`

files：
- Modify: `examples/hyperliquid/cross_exchange_online_estimators.py`
- Modify: `examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- Modify: `examples/hyperliquid/cross_exchange_t024_same_window_acceptance.py`
- Test: `examples/hyperliquid/test_cross_exchange_online_estimators.py`
- Test: `examples/hyperliquid/test_hyperliquid_tiny_live_m2_event_driven_watcher.py`
- Test: `examples/hyperliquid/test_cross_exchange_t024_same_window_acceptance.py`
- Workflow: this task, business report, QA report, fixed QA document, `task_plan.md`, `progress.md`, `findings.md`

action：
- Replace the manager-cycle hold sleep with an injected hold observer that consumes the existing Hyperliquid public event iterator until the exact hold deadline。
- Preserve quote/order/cancel sequencing and submission/risk envelopes；the observer may update only public state、status and evidence buffers。
- For each exact `resting` submit result，define a conservative local-time evidence interval：
  - start strictly after the synchronous resting response completed；
  - end no later than the first cancel request for that reference；
  - if a bound is missing、non-monotonic or crossed by disconnect/reconnect，emit no confirmed exposure and fail closed in the exposure evidence status。
- Convert the conservative interval to fixed 1s event-time bucket overlaps using public events received inside the local-time bounds。
- Emit at most one confirmed exposure row per `(attempt_key, side, bucket_start)`；websocket message count must not add statistical weight。
- Use only exact side/quote/attempt identity，pre-trade L2 side depth，directional at-or-through unique trades and bucket exposure duration。
- Reference mid/distance must come from the event-time book state for that bucket；do not use `abs(trade_px-mid)` as arrival intensity。
- Preserve partial first/last bucket duration；reject zero/negative duration、future/out-of-order events and ambiguous identity。
- Join confirmed exposure rows into `EventTimeOnlineEstimator` before writing `quote_exposure_intervals.csv`、`online_intensity_fit.csv` and snapshots。
- Keep hypothetical public-shadow exposure distinguishable from confirmed private resting exposure。
- Extend deterministic replay to reproduce the same exposure rows、A/k fits and dynamic candidate from event rows plus the confirmed resting interval contract。
- Extend Task 12 acceptance to independently rebuild and compare the confirmed exposure rows；do not call the producer helper。
- Keep `dynamic_spread_activation_enabled=false`、fill feedback off、inventory skew off、multi-level off and fixed Task 7 quote authoritative。
- Preserve v1 behavior when no confirmed interval contract is present。

verify：
- Focused tests prove two exact resting sides with public events during hold produce deterministic per-bucket confirmed exposure rows。
- One rejected side produces no exposure for that side while the resting side remains valid。
- Missing start/end、no event inside bounds、disconnect/reconnect、identity mismatch、non-monotonic interval and reject/resting conflict fail closed。
- Duplicate websocket book states and duplicate trade ids do not increase exposure observations or arrivals。
- Dense and sparse message representations of the same event-time bucket state produce the same exposure rows and fit inputs。
- Arrival evidence uses directional at-or-through trades and pre-trade L2 side depth。
- Replay reproduces exposure rows、intensity fits、candidate and snapshot byte-for-byte。
- Dynamic candidate may become `pass` in fixtures，but authoritative quote remains fixed and activation flags remain false。
- Exact T026 replay remains accepted with zero confirmed estimator exposure；the task does not invent a missing interval start。
- T016/T022 remain blocked。
- Focused and full Hyperliquid tests pass。
- No live/private/network/remote/service action occurs。

stop conditions：
- Public-event collection changes quote、submit、cancel、risk or kill-switch decisions。
- Any exposure starts before exact resting-response completion or ends after cancel request。
- Missing interval bounds are inferred from account-wide absence、cancel ack or wall-clock guesses。
- Websocket message count changes statistical weight for the same event-time state。
- Rejected、unknown or conflicting attempts enter confirmed resting exposure。
- Producer and acceptance cannot independently reproduce the same rows。
- T026 is retroactively given confirmed exposure。
- Any adaptive quote behavior is activated。
- Any live/private/network/remote action occurs。

done：
- Manager bounded-live can preserve public events during resting hold and emit conservative confirmed event-time exposure。
- Same-window replay and acceptance independently reproduce the exposure evidence。
- Existing immutable evidence semantics remain unchanged。
- A later formal Task 8 observe-only live may collect calibration evidence with dynamic spread still disabled。

blockers：
- Dynamic-spread activation remains locked until this implementation passes independent QA and a later observe-only live produces reproducible side-specific evidence。
