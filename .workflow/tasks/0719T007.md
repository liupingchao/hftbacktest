# 0719T007 RAW-LIFECYCLE-TERMINAL-COMMAND-REPAIR

执行线程：
- 总控 auto-loop / 业务实现线程

任务ID：
- 0719T007

标题：
- Repair raw two-sided lifecycle binding, terminal proof, pre-submit side gate and canonical command acceptance

简短描述：
- Close all five `0719T006` QA findings before any bounded two-sided live task.
- This task is offline-only.

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0719T006`（未通过）

规则更新提醒：
- T006 established useful profile/artifact primitives but did not close the evidence chain.
- No live/private/account/order/cancel/network/remote/service action is allowed in this task.
- Multi-level, dynamic spread, fill feedback, inventory skew and actual quote behavior changes remain off.

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `.workflow/reports/0719T006-qa.md`

是否需要提交代码：
- 需要

提交要求：
- 离线 focused/full regression 通过后提交。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- Modify: `examples/hyperliquid/cross_exchange_live_remote_orchestrator.py`
- Modify: `examples/hyperliquid/hyperliquid_maker_order_manager.py`
- Modify: `examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- Modify: `examples/hyperliquid/cross_exchange_t024_same_window_acceptance.py`
- Modify: focused orchestrator/manager/watcher/acceptance tests
- Workflow: this task, business report, QA report, `docs/qa-acceptance-report.md`, `task_plan.md`, `progress.md`, `findings.md`

action：
- Independently parse both raw order responses:
  - require exact outer success and exactly one `resting` status;
  - extract persisted oid/cloid identity tokens;
  - bind each response to one side, canonical attempt, intent and tracked terminal reference;
  - reject empty/duplicate/unrelated/forged response rows.
- Require per-attempt terminal reconciliation for both fill and no-fill lifecycles:
  - target-bound authoritative cancel proof or independently proven filled terminal state;
  - no fill branch may bypass fill identity;
  - fill branch may not bypass cancel/terminal identity.
- Enforce exact side set `{buy,sell}` before the first manager order endpoint call:
  - inventory-based quote pruning must fail closed for the exact two-sided profile;
  - no partial side submission is permitted.
- Canonicalize runner argv:
  - reject duplicate value flags, duplicate boolean flags and multiple mode flags;
  - verify exact mode/profile-related flags, duration, hold, wait, caps and identity;
  - acceptance must model the command actually parsed, not the first duplicate value.
- Replace writer-only nominal integration with a real manager-watcher artifact tree consumed by Task 12 acceptance.
- Add complete adversarial regressions for all T006 findings.
- Keep quote formulas, thresholds, risk caps, activation flags and non-exact legacy behavior unchanged.

verify：
- Empty/duplicate/unrelated raw order responses fail.
- Raw response identity mismatch with intent/attempt/ref fails.
- Filled lifecycle with failed/unrelated terminal proof fails.
- Exact near-cap one-sided candidate fails before any order call.
- Duplicate submission cap, requote, mode, hold, wait, duration or identity flags fail.
- Real manager watcher output passes complete same-window acceptance.
- Existing T005 cancel proof, T006 exact profile, orchestrator provenance/timeout and manager regressions pass.
- Modified Python `py_compile`, CLI `--help`, `git diff --check`.
- Full `python -m pytest examples/hyperliquid -q`.

stop conditions：
- Any forged raw lifecycle can pass.
- Any fill can bypass per-attempt terminal proof.
- Any exact profile can make one order call without both sides validated.
- Any duplicate argv can change runtime behavior while acceptance passes.
- Integration remains hand-built below the manager watcher boundary.
- Any strategy/risk/activation/live boundary changes.

done：
- All five T006 QA findings are closed.
- Real producer output passes independent exact two-sided acceptance.
- Independent QA accepts the offline repair.

blockers：
- No live task may start before this task is QA accepted.

commit：
- `5239af62d67381c5b3584c0873b58e6a0b246cfe`

提交信息：
- `Close T007 lifecycle evidence gaps`
