```md
执行线程：
- 业务线程-live

任务ID：
- 0623T002

标题：
- M2 fresh-touch evidence hardening / real BBO-history reset proof

简短描述：
- 修复 event-driven fresh-touch 候选证据过强的问题：不得再把当前事件 synthetic 标记为 `stayed_touch` 来代表真实 touch stability / queue reset。
- 本任务用真实 `bbo_history`、top size/order-count 变化和稳定时长来证明 fresh-touch；证据不足必须 fail closed。

状态：
- 已通过

执行顺序：
- 当前唯一任务；必须在 `0623T001` 完成并经总控/QA确认后执行。

前置任务：
- `0623T001` 已通过或总控确认其状态新鲜度修复可作为输入。
- `0619T002` QA 为 `已通过`，fresh-touch / dynamic-size / session-gate 策略合同仍然有效，但本任务要收紧其证据口径。

规则更新提醒：
- 请先阅读 `.workflow/workflow-kit/workflow-manual.md`、`.workflow/workflow-kit/task-dispatch-template.md`、`.workflow/workflow-kit/thread-report-template.md`、`.workflow/workflow-kit/qa-acceptance-template.md`、`task_plan.md`、`progress.md`、`findings.md` 后再执行本任务。

按线程规则执行：
- `AGENTS.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- `examples/hyperliquid/hyperliquid_tiny_live_m2_fill_window.py`
- `examples/hyperliquid/test_hyperliquid_tiny_live_m2_event_driven_watcher.py`
- `examples/hyperliquid/test_hyperliquid_tiny_live_m2_fill_loop.py`
- `local_live_analysis/hyperliquid_tiny_live_m2_fresh_touch_evidence_0623T002/**`
- `.workflow/reports/0623T002-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

read-only inputs：
- `.workflow/tasks/0623T001.md`
- `.workflow/reports/0623T001-business.md`
- `local_live_analysis/hyperliquid_tiny_live_m2_state_freshness_0623T001/**`
- `.workflow/tasks/0619T002.md`
- `.workflow/reports/0619T002-qa.md`

hard boundary：
- Keep `Alo`, maker-only, no taker/crossing, no one-tick-back workaround, no cap relaxation, no live submission cap increase, no stable PnL / M3 claim.
- This task should be local/focused verification first. Do not run live orders unless a separate live task is dispatched.
- Do not weaken quality bands: `quality_a <=20x / <=6 orders`, `quality_b 20x-100x / <=12 orders`, hard cap `<=0.005 BTC`.

action：
- Remove or quarantine synthetic `quote_aging_status=stayed_touch` as a sufficient fresh-touch proof in event-driven current-candidate generation.
- Compute fresh-touch evidence from real observed BBO/top-of-book history:
  - current touch persistence duration
  - last BBO change time
  - last same-side top size/order-count reset or meaningful reduction
  - whether top size/order-count evidence supports a fresh queue rather than a refilled crowded queue
- Add explicit fields such as `freshness_source`, `touch_stability_ms`, `last_touch_change_ms`, `top_reset_status`, `top_reset_reason`, and `fresh_touch_evidence_status`.
- If evidence is missing, ambiguous, stale, or synthetic-only, fail closed with a precise reason.
- Add focused tests proving synthetic-only current events do not pass, while real BBO-history stability/reset evidence can pass.

verify：
- `python -m pytest examples/hyperliquid/test_hyperliquid_tiny_live_m2_event_driven_watcher.py -q`
- `python -m pytest examples/hyperliquid/test_hyperliquid_tiny_live_m2_fill_loop.py -q`
- `python -m py_compile examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py examples/hyperliquid/hyperliquid_tiny_live_m2_fill_window.py`
- `python examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py --help`
- `python examples/hyperliquid/hyperliquid_tiny_live_m2_fill_window.py --help`
- `git diff --check`

done：
- 回报 fresh-touch 证据新口径、哪些 synthetic 证据被禁止、真实 BBO-history/reset 场景测试结果、fail-closed 原因矩阵、artifact 路径、blockers、commit id、提交信息。
```
