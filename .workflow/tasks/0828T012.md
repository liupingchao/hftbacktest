# 任务派发

执行线程：
- SKHYNIX Flow Internal Directional Alpha A0 Contract 业务线程

任务ID：
- 0828T012

标题：
- 起草并冻结 FLOW_INTERNAL_DIRECTIONAL_ALPHA_V1 A0 方案

简短描述：
- 注册独立 hypothesis，将 alignment 从完整 flow excursion 结束移动到
  continuous active flow 内 directional dominance 首次被因果确认的
  checkpoint；冻结 zero-target A0、后续 competing-risk directional
  outcomes 和 H0/H1 增量检验。

状态：
- 待验收

执行顺序：
- 当前唯一 formal task

前置任务：
- 0828T011（已通过执行验收，research classification failed）

规则更新提醒：
- 已阅读 AGENTS.md、workflow-kit manual、methodology kernel、
  SAFE_REENTRY_AFTER_FLOW_EXCURSION_V1 plan/execution/QA 和 canonical A0
  evidence。

按线程规则执行：
- AGENTS.md
- .workflow/workflow-kit/workflow-manual.md

是否需要提交代码：
- 需要提交 task、research plan 和 business report

提交要求：
- 自审通过后提交。
- 回报必须带 commit id 和提交信息。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- docs/skhynix_binance_flow_internal_directional_alpha_v1_a0_plan_20260828.md
- .workflow/tasks/0828T012.md
- .workflow/reports/0828T012-business.md

action：
- 注册 `FLOW_INTERNAL_DIRECTIONAL_ALPHA_V1`。
- 冻结 bounded path features，避免依赖 failed quiet-regime z-floor。
- 冻结 active-flow、mixed、dominance candidate、persistence、
  confirmation、release、flip 和 local refractory state semantics。
- 冻结 `directional_dominance_confirmed_at` alignment anchor。
- 冻结 outcome-blind controls、A0 support/dependence gates 和 forbidden
  rescue。
- 冻结 A1 direction-adjusted competing risks、A3 H0/H1 incremental test
  和后续 cost/execution boundary。
- 明确 quote-side recovery 是 execution interaction，不是 V1 primary
  anchor。

verify：
- hypothesis/version boundary 完整。
- Anchor 不使用 future price confirmation 或 return。
- State machine 对 mixed onset、same-direction renewal 和 persistent flip
  无歧义。
- Feature denominator、zero-activity、calibration 和 no-refit 规则完整。
- A0 gates 能同时阻止 sparse、near-continuous、date-concentrated 和
  pseudo-independent anchors。
- Markdown fence parity、constant presence 和 `git diff --check`。

done：
- 新 research plan 已冻结并通过 QA。
- 未实现或执行 detector。
- 未读取 future return、barrier、fill、markout 或 PnL。

明确禁止：
- 不把本方案称为 SAFE_REENTRY V1 robustness。
- 不用 future directional return 选择 dominance threshold、window、
  persistence 或 refractory。
- 不把 raw OBI 或单一 pressure crossing 直接称为 alpha。
- 不在 A0 拟合 H0/H1 或执行成本模型。
