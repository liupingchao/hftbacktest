# 0720T026 SINGLE-LEVEL-TWO-SIDED-HISTORICAL-TERMINAL-PROOF-LIVE

执行线程：
- 总控 auto-loop / 业务执行线程 / live-awsserver1

任务ID：
- 0720T026

标题：
- Run one QA-accepted single-level two-sided lifecycle with bounded terminal history recovery

简短描述：
- Repeat the exact T022 conservative manager profile using the QA-accepted T025 source。
- The only changed implementation surface is the accepted terminal-history evidence chain；strategy behavior and all control variables remain fixed。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0720T025`（已通过）

规则更新提醒：
- Standing authorization permits the exact live/private/read/submit/cancel/remote/service-isolation actions in this task without another prompt。
- Execution safety and evidence integrity remain mandatory before the only live window。
- No second live window is allowed in this task。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `.workflow/reports/0720T025-qa.md`
- `docs/superpowers/plans/2026-07-17-principal-alignment-p0-p3.md`

是否需要提交代码：
- 不需要；live source is the exact committed tree below。

提交要求：
- Source commit is fixed before remote preflight。
- Evidence and workflow report are committed after local pullback/acceptance。
- 回报必须带 source commit id 和业务报告提交信息。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

source：
- Branch: `cross-exchange`
- Exact source commit: `40dc56a3225df4afb0f2185873c91f17b578f550`
- Remote source: `/home/admin/hftbacktest-cross-exchange-0720T026`
- Source marker: `/home/admin/hftbacktest-cross-exchange-0720T026/source_commit.txt`

envelope：
- Host: `awsserver1`
- Account scope: Hyperliquid account configured by `/home/admin/XEMM_rust_latest/.env`
- Venue/symbol: Hyperliquid `BTC`
- Lead source: Binance public book ticker through event-driven edge gate
- Normal order: limit post-only `Alo`
- Exact profile: `two-sided-manager`
- Windows: `1`
- Max duration: `1800s`
- Max order size: `0.005 BTC` per order
- Max aggregate position delta: `0.01 BTC`
- Max loss: `1 USDC`
- Max submissions: `2`
- Requote attempts: `2`
- Quote hold: `3s`
- Wait after cancel/fill pullback: `10s`
- Kill-switch flatten only: reduce-only IOC；normal taker/crossing remains prohibited

control variables：
- Same fixed half-spread、accepted normalization、signal freshness、edge threshold、quality gate、quote size、hold/wait and manager logic as T022。
- Dynamic spread activation: off。
- Fill feedback activation: off。
- Inventory skew activation: off。
- Multi-level activation: off。
- Actual quote behavior change: false。
- Only primary implementation variable: QA-accepted bounded terminal history、strict row classification and redaction evidence source。

files：
- Execute: `examples/hyperliquid/cross_exchange_live_remote_orchestrator.py`
- Execute: `examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- Accept: `examples/hyperliquid/cross_exchange_t024_same_window_acceptance.py`
- Remote evidence: `/home/admin/hftbacktest-cross-exchange-artifacts/principal_alignment_single_level_two_sided_0720T026/`
- Local evidence: `local_live_analysis/principal_alignment_single_level_two_sided_0720T026/`
- Workflow: this task, business report, QA report, fixed QA document, `task_plan.md`, `progress.md`, `findings.md`

action：
- Build an isolated remote source archive from the exact source commit and write the full commit marker。
- Verify local/remote archive SHA-256, runtime imports and exact no-start orchestrator preflight。
- Record current conflicting account service/process state；isolate only if active and restore only after independently proven safe terminal account state。
- Run one read-only account/service preflight for exact source/import/env、kill-switch clear、empty open orders、finite position within cap and no concurrent account trader。
- Start exactly one detached orchestrator after every preflight check passes。
- Require event-driven edge gate、exchange-reconciled two-sided manager、fast Hyperliquid L2、requote `2` and exact external duration `1800s`。
- Persist direct trigger/guard/edge/attempt identities and separate buy/sell intent/status/reference/terminal/history evidence。
- Pull the complete remote artifact root locally，verify terminal and remote/local SHA-256，then run same-window acceptance with exact task/source/root/duration。
- Record final open orders、BTC position、service/process state、kill-switch、writer health and unsupported economics claims。

verify：
- Source provenance is sealed before watcher start and independently reproducible from exact Git blobs。
- Preflight exact-matches task/window/source/profile/duration/caps/activation and does not start watcher or call endpoints。
- Both sides reach actual order endpoint calls unless one formal stop condition blocks before authorization。
- Every submitted attempt has authoritative reference-bound terminal cancel or complete maker-fill proof；bounded historical fallback may be used only under the QA-accepted contract。
- Raw fills、direction、quantity、limit、fee and maker/taker role are independently reconstructed。
- Final and independent owned/account open orders are empty。
- BTC position and realized loss remain within envelope；any residual is reconciled or kill-switch flattened。
- Final manager/operator order and position evidence agree with final account snapshots。
- Child process is reaped；status writer、heartbeat、runtime source verification、terminal checksum and remote/local artifact equality pass。
- Same-window acceptance either passes mechanism/evidence integrity or records the first exact stop condition without optimism。
- No dynamic spread、fill feedback、inventory skew、multi-level or actual quote behavior activation。

stop conditions：
- Any source、task/window、command、duration、envelope or activation mismatch。
- Account ownership、concurrent service、open-order or position state is ambiguous。
- Kill-switch is not clear before live。
- Desired side set is not exactly `{buy,sell}` before first order call。
- Price precision/post-only invariant fails。
- Submission、size、position、loss or duration cap is exceeded。
- Duplicate/ambiguous/unattributed/conflicting fill or terminal-history evidence。
- Unknown order state or any submitted attempt lacks authoritative terminal proof。
- Owned/account open orders cannot be proven empty。
- Position cannot be reconciled or safe flatten cannot be proven。
- Process termination/reap、status writer、source verification or terminal checksum fails。
- Concurrent account activity contaminates the window。

done：
- One and only one repaired single-level two-sided bounded-live window is preserved locally and independently accepted，or the first formal stop condition is recorded。
- Principal Task 7/12 mechanism and evidence conclusions remain separate from unsupported stable economics。
- No second window or strategy-variable expansion occurs inside T026。

blockers：
- Any stop condition ends the task and prevents a second live window。
- Task 8 and adaptive/multi-level remain locked until T026 receives independent QA acceptance as a complete single-level two-sided lifecycle。

commit：
- source `40dc56a3225df4afb0f2185873c91f17b578f550`

提交信息：
- source `Record T025 QA acceptance`
