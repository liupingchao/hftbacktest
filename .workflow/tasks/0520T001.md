```md
执行线程：
- 测试线程

任务ID：
- 0520T001

标题：
- Step 9C read-only multi-sample quote-adjustment validation

简短描述：
- 在总控已接受 `5-19-night-active-30min-a` market-view caveat 的前提下，使用现有 current-format no-rule/default-off 样本运行只读多样本 quote-adjustment validation。复用现有 `quote_adjustment_replay.py`，聚合 candidate 跨样本稳定性、场景覆盖、hard gates、clean-only sensitivity 和下一步分类。

状态：
- 已通过

执行顺序：
- 当前唯一任务；必须在 `0519T011` QA 已通过后执行

前置任务：
- `0519T011` QA 已通过
- `0519T010` QA 已通过
- `0519T009` QA 已通过
- `0519T008` QA 已通过

规则更新提醒：
- 总控已接受 `5-19-night-active-30min-a` 的 market-view caveat，仅限 Step 9C research-comparison 输入；不得把该 caveat 解读为 market-view 已修复或严格干净。
- 本任务必须同时报告：
  - accepted-set：包含 `5-19-night-active-30min-a`
  - clean-only sensitivity：排除 `5-19-night-active-30min-a`
- 本任务只读验证，不允许修改 runner、策略逻辑、生产默认值、live 行为或 promotion 判定。

按线程规则执行：
- `.workflow/workflow-kit/thread-report-template.md`
- `docs/thread-playbook.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 验证、报告、workflow 文档收口后提交。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

Allowed files：
- `.workflow/tasks/0520T001.md`
- `.workflow/reports/0520T001-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `.workflow/dashboard.html`
- `.workflow/dispatch_suggestions.md`
- `local_live_analysis/5-19-day-control-30min/stage9c_quote_adjustment_replay_0520T001/**`
- `local_live_analysis/5-19-night-active-30min-a/stage9c_quote_adjustment_replay_0520T001/**`
- `local_live_analysis/5-19-night-active-30min-b/stage9c_quote_adjustment_replay_0520T001/**`
- `local_live_analysis/5-19-night-active-30min-c/stage9c_quote_adjustment_replay_0520T001/**`
- `local_live_analysis/stage9c_multi_sample_validation_0520T001/**`

Allowed read-only inputs：
- `.workflow/reports/0519T011-qa.md`
- `.workflow/reports/0519T011-business.md`
- `.workflow/reports/0519T010-qa.md`
- `.workflow/reports/0519T010-business.md`
- `.workflow/reports/0519T009-qa.md`
- `.workflow/reports/0519T008-qa.md`
- `examples/binance_tick_mm/quote_adjustment_replay.py`
- `examples/binance_tick_mm/test_quote_adjustment_replay.py`
- `examples/binance_tick_mm/quote_anchor_safety.py`
- `examples/binance_tick_mm/execution_outcome_labels.py`
- `examples/binance_tick_mm/maker_acceptance.py`
- `local_live_analysis/5-19-day-control-30min/**`
- `local_live_analysis/5-19-night-active-30min-a/**`
- `local_live_analysis/5-19-night-active-30min-b/**`
- `local_live_analysis/5-19-night-active-30min-c/**`

Accepted sample set：
- `5-19-day-control-30min`
- `5-19-night-active-30min-a`
- `5-19-night-active-30min-b`
- `5-19-night-active-30min-c`

Clean-only sensitivity set：
- `5-19-day-control-30min`
- `5-19-night-active-30min-b`
- `5-19-night-active-30min-c`

Known caveat：
- `5-19-night-active-30min-a` has `first_valid_update_aligned=false`, `gap_crossed_join_count=28062`, and Step 5C missing anchor rows `28062`.
- This caveat is accepted only to avoid blocking Step 9C research comparison after T011 met aggregate event-mass thresholds.
- The validation report must keep this sample marked as caveated and must not use it to claim strict market-view quality.

Forbidden files / areas：
- Strategy rule logic, fair/target formula, quote behavior, production defaults, live production configs.
- Core/connector APIs, canonical data conversion, standard hftbacktest npz schema.
- `quote_adjustment_replay.py` implementation changes.
- Any candidate enablement, default-on behavior, live run, production behavior change, Step 5C promotion, inventory-control implementation, queue/touch repair, or promotion/live-readiness claim.

Requires separate task：
- Modifying `quote_adjustment_replay.py` or adding a reusable aggregator implementation.
- Collecting replacement or additional live samples.
- Promoting any candidate to tiny live design, live execution, default-on behavior, or production.
- Changing strategy behavior, quote placement, inventory controls, replay lifecycle, market-view sidecar/bootstrap logic, or post-only anchor logic.

Fatal Failures：
- Treating `5-19-night-active-30min-a` as strict-clean instead of caveated.
- Omitting clean-only sensitivity.
- Interpreting a single candidate win or aggregate PnL as live readiness.
- Modifying runner/strategy/core/live code.
- Using future fill/markout labels, audit-overlay outcomes, exact queue claims, `4948`-specific logic, or same-sample PnL feedback as decision inputs.
- Starting live, enabling default-on behavior, or making promotion claims.

action：
- Confirm the accepted sample set and clean-only sensitivity set.
- For each accepted sample, run or refresh the existing Step 9B runner into a task-scoped directory:
  - `python examples/binance_tick_mm/quote_adjustment_replay.py --run-dir local_live_analysis/<run_id> --output-dir local_live_analysis/<run_id>/stage9c_quote_adjustment_replay_0520T001`
- Aggregate outputs into `local_live_analysis/stage9c_multi_sample_validation_0520T001/`.
- Required aggregate artifacts:
  - `sample_manifest.csv` or `.json`
  - `hard_gate_summary.csv` or `.json`
  - `candidate_sample_metrics.csv`
  - `candidate_regime_metrics.csv`
  - `candidate_stability_summary.csv` or `.json`
  - `accepted_set_vs_clean_only_sensitivity.md` or `.json`
  - `validation_report.md`
- Aggregate by `sample_id x candidate_id x scenario_bucket` where available from current artifacts.
- Report per-candidate:
  - per-sample classification
  - accepted-set classification
  - clean-only sensitivity classification
  - decision coverage by intended regime
  - PnL/spread-capture proxy direction
  - fill probability / time-to-fill direction
  - adverse markout direction
  - cancel-fill / fill-after-cancel direction
  - inventory-cycle direction
  - API/churn/reject/throttle direction
  - post-only / bad-price / clamp risk
  - hard-gate breaches
  - worst-sample delta and whether any sample is catastrophic
- Candidate classification must be one of:
  - `reject`
  - `keep_for_research`
  - `ready_for_tiny_live_design`
  - `blocked_by_data_quality`
  - `needs_runner_or_artifact_change`
- If current artifacts are insufficient for a required bucket, report the missing artifact/schema and classify as `needs_runner_or_artifact_change`; do not modify runner in this task.

verify：
- `python examples/binance_tick_mm/quote_adjustment_replay.py --help`
- `python -m pytest examples/binance_tick_mm/test_quote_adjustment_replay.py -q`
- Runner outputs exist for all 4 accepted samples in task-scoped directories.
- Aggregate artifacts exist under `local_live_analysis/stage9c_multi_sample_validation_0520T001/`.
- Report states whether accepted-set meets research-comparison thresholds:
  - at least `4` current-format samples
  - at least `120` minutes aggregate duration
  - at least `10000` submit orders aggregate
  - at least `250` filled orders aggregate
- Report states clean-only sensitivity event mass and whether it is under threshold.
- Report states that `5-19-night-active-30min-a` remains caveated.
- Report states that no live/default-on/promotion/production behavior change was made.
- `python3 .workflow/build_dashboard.py`
- `git diff --check`

done：
- 回报必须说明 accepted-set 是否满足 Step 9C research-comparison thresholds。
- 回报必须说明 clean-only sensitivity 是否改变 candidate 结论。
- 回报必须给出每个 candidate 的 `reject` / `keep_for_research` / `ready_for_tiny_live_design` / `blocked_by_data_quality` / `needs_runner_or_artifact_change` 分类。
- 回报必须明确是否可以继续创建 Step 10 tiny-live-design planning task，或是否需要先补样本 / runner artifact。
- 回报必须明确：本任务不启动 live、不启用 default-on、不修改策略、不修改 runner、不做 promotion。
```
