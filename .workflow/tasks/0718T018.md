# 0718T018 P1-WATCHER-WIRING-FIRST-TINY-LIVE

执行线程：
- 总控 auto-loop / 业务实现线程

任务ID：
- 0718T018

标题：
- P1 watcher 接线、原子 live status 与首次单层双边 tiny-live

简短描述：
- Implement Principal Alignment Task 7: wire the validated cross-exchange decision path into the exchange-reconciled single-level order manager, add the minimum atomic throttled live status artifact, run a public-only shadow, and execute one independently bounded first tiny-live lifecycle window if all task-local gates pass.
- Live is the primary source for lifecycle evidence in this task. Shadow/replay are short controls for obvious-error detection, decision reproduction and control-variable confirmation; they are not a long statistical prerequisite.

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- 0718T017

规则更新提醒：
- User standing authorization from `2026-07-17` covers the recorded symbol/venue/envelope and removes routine permission prompts.
- Task-local execution safety, account/service isolation, reconciliation, artifact integrity and fail-closed gates remain mandatory.

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `docs/superpowers/plans/2026-07-17-principal-alignment-p0-p3.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- `examples/hyperliquid/hyperliquid_tiny_live_m2_fill_window.py`
- `examples/hyperliquid/test_hyperliquid_tiny_live_m2_event_driven_watcher.py`
- `examples/hyperliquid/hyperliquid_maker_order_manager.py` only if a narrowly scoped adapter is required
- `examples/hyperliquid/test_hyperliquid_maker_order_manager.py` only for adapter coverage

action：
- 每个有效 decision 生成 typed `desired_quotes=[bid, ask]`；`signal_side` 只做预测/audit，不把双边 quote 变成单边选择。
- 接入 `MakerOrderManager.reconcile_desired()`、startup/reconnect/account refresh、fill application 和 kill-switch priority；不使用 `run_order_once` 作为持续报价循环。
- 新增状态 writer：原子 temp + `os.replace`、至少 1s 节流，记录 run/window/config hash、freshness、desired/final bid/ask、position/exposure、owned order count、halt state、last action/block/error 和 heartbeat。
- pre-live 运行 deterministic fixture、focused price/manager checks 和一次短 public-only shadow；shadow 必须保持 no private/order/cancel endpoint。
- 通过 task-local live gate 后，使用固定 half-spread、inventory skew off、dynamic spread off、fill feedback off、multi-level off、单层一买一卖、post-only、每单不超过 `0.005 BTC`、累计 position delta 不超过 `0.01 BTC`、最多 `2` 次 submit、最多 `1800s` 的 bounded tiny-live。
- tiny-live 记录真实 submit/resting/reject/cancel/fill/fee/role/latency/inventory lifecycle；窗口结束必须 reconcile、cancel-confirm、sealed artifacts，并为同一 window 运行短 replay/decision comparison。
- 不做收益、fill-rate、maker viability 或参数 promotion 结论；首次 tiny-live 只验证生命周期与证据完整性。

verify：
- watcher/fill-window focused tests，包含双边 desired quote、manager adapter、status atomicity/throttle、halt/risk precedence、no-submit shadow boundary 和 reconnect/fill update。
- `python -m pytest examples/hyperliquid/test_hyperliquid_tiny_live_m2_event_driven_watcher.py examples/hyperliquid/test_hyperliquid_maker_order_manager.py -q`
- `python -m pytest examples/hyperliquid/test_cross_exchange_shared_signal_kernel.py examples/hyperliquid/test_cross_exchange_public_replay_alignment.py examples/hyperliquid/test_cross_exchange_production_shadow.py -q`
- `python examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py --help`
- `python examples/hyperliquid/hyperliquid_tiny_live_m2_fill_window.py --help`
- `python -m py_compile` modified Python files
- `git diff --check`
- pre-live public-only shadow artifact must record `private_endpoint_called=false`, `order_endpoint_called=false`, `cancel_endpoint_called=false`, `credentials_read=false`.
- if live environment/artifacts cannot satisfy the task-local gate, fail closed and report the blocker; do not invent a live result.

done：
- watcher/manager wiring、双边 desired quote、atomic/throttled live status、cancel-confirm evidence 和 exact runtime envelope 已完成。
- 真实 public-only shadow 保持 no credential/private/order/cancel；两个 bounded live watcher 窗口分别运行约 `300s` 和 `900s`，都按固定 gate 自然 fail-closed。
- 两个正式 live 窗口合计 `0` real submit、`0` real cancel、`0` fill；独立 postflight 为 BTC position `0.0`、open orders `0`。
- shadow `15/15`、live-02 `79/79`、live-03 `79/79` terminal checksums 在 remote 和本地均验证通过。
- same-window conservative replay 的 market-view/optimism 通过，但 decision/lifecycle/economics 因无 submit/resting/fill 保持 blocked。
- 本任务结果不支持 profitability、fill-rate、maker viability、dynamic spread activation、multi-level 或 promotion。

blockers：
- 未获得首个真实双边 submit/resting/cancel/fill/fee/role lifecycle 样本；该 evidence gap 不阻塞 Task 8 observe-only，但继续阻塞 Task 10 多层和任何经济性结论。

commit：
- `38f0595`
- `031a198`

提交信息：
- `Wire Task 7 watcher to reconciled manager`
- `Add Task 7 waiting heartbeat`
