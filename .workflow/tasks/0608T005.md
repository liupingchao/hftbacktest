```md
执行线程：
- 业务线程-research

任务ID：
- 0608T005

标题：
- Regime 011 basis-context visibility / lineage diagnosis

简短描述：
- 基于 `0608T004` 发现的 `context_basis_mid_ticks > 0` 强局部 pattern，进一步只读验证该 basis 字段是否是干净、decision-time-visible、可解释且可合同升级的 context。本任务只做 visibility / lineage diagnosis，不实现策略、不生成 case-library 或 shadow decisions、不启动 live。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0604T003` QA 已通过，canonical event-mode artifacts 是正式证据源。
- `0604T004` QA 已通过，canonical event-mode evidence loader / validator foundation 可复用。
- `0604T005` QA 已通过，canonical source-lock guard 可复用。
- `0604T009` QA 已通过，唯一 regime 输入为 `regime_011_1000_spread_10_20_ticks`。
- `0608T002` QA 已通过，maker executability final recommendation 为 `reject_not_maker_executable`。
- `0608T003` QA 已通过，overall directional momentum final recommendation 为 `reject_directional_edge_unstable`。
- `0608T004` 业务执行已完成，`context_basis_mid_ticks > 0` 是最强局部 pattern，但当前因 `0601T004` 合同 caveat 被分类为 `invalid_not_decision_visible`。

规则更新提醒：
- 请先阅读规则文档后再执行本任务。
- 本任务只做 read-only basis visibility / lineage diagnosis；不得实现策略、不得生成 case-library 或 shadow decisions、不得输出交易动作/下单方向/quote/order 行为、不得接 private/order endpoint、不得 live/default-on/tiny-live、不得 promotion。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `docs/thread-playbook.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交任务文件、业务报告、read-only runner/tests、task-scoped artifacts。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0608T005.md`
- `.workflow/reports/0608T005-business.md`
- `examples/hyperliquid/canonical_basis_context_visibility.py`
- `examples/hyperliquid/test_canonical_basis_context_visibility.py`
- `local_live_analysis/canonical_basis_context_visibility_0608T005/**`

read-only inputs：
- Formal canonical source:
  - `local_live_analysis/event_mode_canonical_pricing_signal_0604T003/**`
  - Row-level canonical sample files must be resolved from `multi_sample_manifest.json` via `samples[].pricing_signal_rows`.
- T004 basis validity artifacts:
  - `local_live_analysis/canonical_feature_conditioned_signal_validity_0608T004/**`
- Decision-time-visible input contract:
  - `local_live_analysis/binance_led_hyperliquid_data_contract_0601T004/**`
- Source code lineage:
  - `examples/hyperliquid/cross_exchange_lead_lag_join.py`
  - `examples/hyperliquid/binance_led_pricing_signal_runner.py`

candidate scope：
- Regime id:
  - `regime_011_1000_spread_10_20_ticks`
- Pattern:
  - `context_basis_mid_ticks > 0`
- Base context:
  - `horizon_ms=1000`
  - `joined_row_quality=primary_usable`
  - `hyperliquid_context_quality=primary_usable`
  - `hyperliquid_join_age_bucket=fresh_0_50ms`
  - `hyperliquid_spread_bucket=spread_10_20_ticks`

forbidden：
- New data collection.
- Strategy implementation, directional strategy implementation, order behavior implementation, case-library implementation, shadow decision generation, live/default-on/tiny-live, promotion.
- Hyperliquid or Binance private/account/order endpoints.
- Order lifecycle, submit/cancel/fill logic, production config/default changes, parameter search or optimization sweep.
- Connector/core API changes, standard npz schema changes, canonical Binance maker audit schema changes.
- Treating ordinary synthetic fixed-grid artifacts as formal evidence.
- Treating `100/250ms` evidence as canonical independent support.
- Outputting executable trading instructions, actual order side, quote price, size, leverage, stop rule, take-profit rule, or deployment recommendation.

action：
- Implement a read-only basis-context visibility runner that consumes canonical event-mode artifacts through the accepted loader/source-lock path.
- Validate T004 prerequisite and confirm its `context_basis_mid_ticks > 0` row is `invalid_not_decision_visible` only because of contract caveat, not because of instability, redundancy, cost, or tail.
- Reconstruct the `context_basis_mid_ticks > 0` rows from canonical row-level `samples[].pricing_signal_rows`.
- Diagnose:
  - basis field lineage and formula
  - timestamp/as-of cleanliness from `binance_local_ts`, `hyperliquid_decision_ts`, `binance_source_age_ms`, and future-label policy
  - sample distribution and concentration
  - 1000ms primary edge and 5000/10000ms persistence
  - 100/250ms alias/watch-only context
  - basis caveat source from `0601T004`
  - whether the caveat can be lifted, kept as watch, or must remain diagnostic-only
- Keep all conclusions read-only and proxy-scoped. Do not claim executable strategy PnL, real order fill, private execution feasibility, or live readiness.

output classification values：
- Basis lineage:
  - `lineage_confirmed_decision_time_formula`
  - `lineage_incomplete`
  - `lineage_failed`
- Timestamp cleanliness:
  - `timestamp_clean_asof`
  - `timestamp_watch_join_age`
  - `timestamp_failed_future_join_or_missing`
- Contract decision:
  - `upgrade_to_context_only_supported`
  - `watch_contract_visibility_needs_more_samples`
  - `keep_diagnostic_only_contract_caveat`
  - `reject_basis_context`

strict acceptance line for `upgrade_to_context_only_supported`：
- T004 basis positive pattern is stable across all 3 samples and not sample-concentrated.
- Row inputs are as-of clean: no future input joins, no missing decision-time inputs, primary usable rows only.
- Basis lineage is explicitly formula-derived from decision-time Binance mid and Hyperliquid mid.
- Basis field does not rely on future labels or `100/250ms` aliased evidence.
- 5000/10000ms persistence does not reverse the signal.
- Contract caveat can be narrowed to execution-PnL caveat only, not decision-time visibility caveat.
- Report remains read-only and does not output executable trading instructions, case-library, shadow decisions, or live/promotion claims.

outputs：
- `basis_visibility_manifest.json`
- `basis_lineage_audit.csv`
- `basis_positive_pattern_by_sample.csv`
- `basis_timestamp_join_audit.csv`
- `basis_horizon_persistence.csv`
- `basis_contract_decision.csv`
- `basis_context_visibility_report.md`

acceptance criteria：
- Runner refuses non-canonical or diagnostic-only synthetic inputs through the accepted loader/source-lock path.
- Runner validates T004 prerequisite before running.
- Runner resolves row-level inputs from `multi_sample_manifest.json` `samples[].pricing_signal_rows`.
- Assessment is limited to `regime_011_1000_spread_10_20_ticks` and `context_basis_mid_ticks > 0`.
- Outputs include all required artifact files and final contract decision taxonomy.
- Report explicitly states conclusions are read-only public-data proxies, not executable strategy PnL or private execution proof.
- Report does not output actual order side, quote price, size, leverage, stop rule, take-profit rule, strategy action, shadow decision, private/order endpoint, live/default-on/tiny-live, or promotion claim.
- Tests cover canonical input acceptance, diagnostic input refusal, T004 prerequisite validation, timestamp/as-of failure path, contract decision taxonomy, and output artifact generation.

verify：
- `python examples/hyperliquid/canonical_basis_context_visibility.py --help`
- `python -m py_compile examples/hyperliquid/canonical_basis_context_visibility.py examples/hyperliquid/test_canonical_basis_context_visibility.py`
- `python -m pytest examples/hyperliquid/test_canonical_basis_context_visibility.py`
- `python examples/hyperliquid/canonical_basis_context_visibility.py --input-dir local_live_analysis/event_mode_canonical_pricing_signal_0604T003 --feature-validity-dir local_live_analysis/canonical_feature_conditioned_signal_validity_0608T004 --data-contract-dir local_live_analysis/binance_led_hyperliquid_data_contract_0601T004 --output-dir local_live_analysis/canonical_basis_context_visibility_0608T005`
- `python -m json.tool local_live_analysis/canonical_basis_context_visibility_0608T005/basis_visibility_manifest.json`
- Scripted or manual check that the report contains no executable trading instruction, order side, quote price, size, private/order endpoint, shadow decision, live/default-on/tiny-live, or promotion claim.
- `git diff --check`

done：
- Business report must include method, input source-lock, T004 prerequisite check, basis lineage, timestamp/as-of diagnosis, sample distribution, persistence results, contract decision, artifact paths, verification commands, blockers/caveats if any, commit id, and commit message.
- Business report must explicitly state this is only read-only basis-context visibility / lineage diagnosis and does not authorize strategy implementation, private/order endpoints, order lifecycle, live/default-on/tiny-live, parameter search, case-library implementation, shadow decisions, or promotion.
- Business report should end in `待验收` if verification passes.
```
