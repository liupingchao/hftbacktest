# 0728T074 EC2-HUNT-S3-CLOCK-RECEIPT-FAIL-CLOSED-REPAIR

执行线程：
- 总控 / 业务执行线程

任务ID：
- 0728T074

标题：
- 修复 S3 查询歧义、setup receipt 门禁和真实故障测试

简短描述：
- 保留 T072 accepted full data 和 T073 build/control evidence。
- 关闭 T073 QA 的一个 P1、两个 P2。
- 用最终代码再跑 2 台短 Canary，验证 setup receipt event 与零遗留。

状态：
- 未通过

执行顺序：
- 当前唯一正式任务

前置任务：
- `0728T073`（未通过）

是否需要提交代码：
- 需要实现并加入 Git 跟踪；本任务不要求创建 commit。

是否进行QA验收：
- 是

files：
- `latency-probe/ec2_hunt_orchestrator.py`
- `latency-probe/ec2_hunt_remote.sh`
- `latency-probe/ec2_hunt_build.py`
- `latency-probe/test_ec2_hunt.py`
- `docs/evidence/ec2_latency_hunt_20260728/`
- `.workflow/tasks/0728T074.md`
- `.workflow/reports/0728T074-business.md`
- `.workflow/reports/0728T074-qa.md`
- `docs/qa-acceptance-report.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

action：
- S3 head/tag 查询只有明确 `404/NoSuchBucket/NoSuchTagSet` 才能按缺失
  处理；403、限流、权限、网络错误必须进入 cleanup failure。
- setup command 成功后解析 stdout clock JSON，验证完整字段、Normal、
  gate=pass 和 bound<=limit，再写 verified event。
- remote `cat setup-clock.json` 失败必须非零。
- 增加可执行 remote PUT failure、HeadBucket 403、KeyboardInterrupt、
  setup receipt 缺失/gate fail/failure journal tests。
- 最终代码运行 2 台 60 秒 Canary。

verify：
- focused Python/Rust/fmt/clippy/diff check 全通过。
- 故障注入不会产生 verified cleanup/execution。
- Canary setup receipt event `2/2`，archive checks `2/2`。
- cleanup verified 且 AWS Task `0728T074` 零遗留。

done：
- T073 QA 缺陷全部关闭，Goal 2 可重新进入独立 QA。

blockers：
- Hyperliquid feed-network 继续降级。
- 不证明订单 ACK/fill/PnL。
