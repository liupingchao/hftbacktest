```md
执行线程：
- 业务线程-scheduled-aws-amdserver

任务ID：
- 0702T001

标题：
- 北京时间夜间 Hyperliquid fast 三段 1800s 同步样本采集

简短描述：
- 按 `0627T001` 的 raw-only 约束，定时在 `awsserver1` 采集三段 Binance `BTCUSDT` lead + Hyperliquid `BTC` lag public-only 同步窗口，每段 `1800s`。
- 第一段必须在北京时间 `2026-07-02 19:45:00` 开始；`awsserver1` 位于东京，等价远端本地时间为 `2026-07-02 20:45:00 JST`，等价 UTC 为 `2026-07-02 11:45:00Z`。采集完成后数据拉回本地 amdserver，再执行 alignment 和 downstream package。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0627T001` QA 已通过，且确认 `--hyperliquid-l2book-fast --skip-alignment` raw-only 路径可用。

规则更新提醒：
- 请先阅读 `.workflow/workflow-kit/workflow-manual.md`、`.workflow/workflow-kit/task-dispatch-template.md`、`.workflow/workflow-kit/thread-report-template.md`、`.workflow/workflow-kit/qa-acceptance-template.md`、`task_plan.md`、`progress.md`、`findings.md` 后再执行本任务。

按线程规则执行：
- `AGENTS.md`
- `docs/thread-playbook.md`

是否需要提交代码：
- 不需要

提交要求：
- 不需要提交
- 回报必须带 `commit：无` 和 `提交信息：无`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0702T001.md`
- `.workflow/reports/0702T001-business.md`
- `.workflow/runners/0702T001_aws_collect.sh`
- `.workflow/runners/0702T001_local_postprocess.sh`
- `local_live_analysis/cross_exchange_public_sample_xemm_0702_t001_hlfast_bjt*/**`
- `local_live_analysis/cross_exchange_lead_lag_join_xemm_0702_t001_hlfast_bjt*/**`
- `local_live_analysis/cross_exchange_lead_lag_analysis_xemm_0702_t001_hlfast_bjt*/**`
- `local_live_analysis/binance_led_hyperliquid_pricing_signal_xemm_0702_t001_hlfast_bjt*/**`
- `local_live_analysis/cross_exchange_mvp_hl_fast_sample_expansion_0702T001/**`
- `task_plan.md`
- `progress.md`

hard boundary：
- `awsserver1` 只允许 public raw collection，不允许执行 Binance alignment、Hyperliquid alignment、join、analysis、pricing signal、sample expansion。
- AWS 采集命令必须使用 `--hyperliquid-l2book-fast --skip-alignment`。
- AWS manifests 必须记录 `alignment_status=skipped`、`raw_collection_only=true`、`alignment_execution_host=macmini_or_amdserver`。
- Raw 数据必须拉回本地 amdserver 后再执行 alignment 和 downstream processing。
- No live orders, no credentials, no private/account/order/cancel endpoints, no user stream, no live trading client initialization, no remote final gate, no T008 ledger claim.
- No watcher strategy change, signal threshold tuning, side-mapping freeze, edge-buffer reduction, quote-distance change, cap relaxation, taker/crossing, canary, M3, stable-PnL, default-on or promotion.
- Future Hyperliquid values are labels only and must never be decision inputs.
- Do not reinterpret delayed effective labels as near-target `1000ms` labels.

schedule：
- First AWS collection start:
  - Beijing / Asia-Shanghai: `2026-07-02 19:45:00`
  - Tokyo / awsserver1 local time: `2026-07-02 20:45:00 JST`
  - UTC: `2026-07-02 11:45:00Z`
- Samples:
  - `xemm_0702_t001_hlfast_bjt1945_a`
  - `xemm_0702_t001_hlfast_bjt2015_b`
  - `xemm_0702_t001_hlfast_bjt2045_c`
- Each sample requested duration: `1800s`.
- Samples run sequentially; expected second/third starts are about Beijing `20:15` and `20:45`, plus process startup overhead.
- Local amdserver postprocess should start after all three windows complete plus buffer, target Beijing `2026-07-02 21:25:00`.

sample contract：
- Collect exactly three new accepted candidate windows on `awsserver1`.
- Each accepted window must have synchronized overlap `>=1500s`.
- Window start times must be separated by at least `30` minutes.
- Hyperliquid `l2Book fast=true` cadence must remain materially improved versus ordinary T002 mode; preferred minimum `>=1800` `l2Book` messages per `1800s` window.
- To unlock a later signal acceptance task, valid near-target nominal `1000ms` rows must satisfy `1000ms <= effective_future_age_ms <= 1250ms`, at least `20` per window and at least `100` aggregate.

action：
- Install and verify a timer on `awsserver1` for the Beijing `19:45` first raw-only collection.
- Install and verify a local amdserver postprocess timer for copyback, checksum verification, local alignment, join, lead-lag analysis, pricing signal, and final sample expansion package generation.
- Preserve AWS raw-only evidence and report the exact remote and local timer units.

verify：
- Confirm current time conversion: Beijing `19:45` == Tokyo `20:45` == UTC `11:45`.
- `systemctl list-timers` / timer status for the AWS collection unit.
- `systemctl list-timers` / timer status for the local postprocess unit.
- After execution, verify every copied `raw.gz` against recorded SHA256.
- Parse every JSON manifest with `python -m json.tool`.
- Verify AWS run manifests show `alignment_status=skipped` / `raw_collection_only=true`.
- Verify local alignment metrics exist for all three samples.
- Run sample expansion package and validate required artifacts:
  - `sample_expansion_manifest.json`
  - `sample_quality_matrix.csv`
  - `field_coverage_matrix.csv`
  - `regime_summary.csv`
  - `effective_horizon_coverage.csv`
  - `effective_horizon_validity_matrix.csv`
  - `symmetric_edge_context_coverage.csv`
  - `boundary_manifest.json`
  - `recommendation.md`

done：
- Report timer installation result, exact unit names, next run times, and time-zone conversion.
- After completion, report all three sample ids, requested/actual duration, overlap, stream counts, reconnects, checksum results, alignment locations, package path, near-target `1000ms` counts, recommendation, and whether T003 is unlocked.
- Explicitly state that AWS did not run alignment, no private/order endpoints were used, no orders were placed, and no canary/promotion was authorized.
```
