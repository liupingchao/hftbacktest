# 0722T064 ADAPTIVE-LIVE-EVIDENCE-REACHABILITY-PREFLIGHT

执行线程：
- 总控 / 业务执行线程

任务ID：
- 0722T064

标题：
- Prove adaptive quote activation reachability before the next live order

简短描述：
- 在不下单的前提下，证明当前 single-cycle/2-submission live profile 中
  dynamic spread 和 fill feedback 是否能在 submit 前达到 active candidate，
  并审计 T047/T052 既有 evidence 是否可作为 source-pinned prewarm。

状态：
- 未通过

执行顺序：
- 当前唯一任务

前置任务：
- `0722T063`（已通过）

规则更新提醒：
- 本任务只读本地和 awsserver1 既有 artifacts。
- 不读取 credentials，不调用 private/account/order/cancel endpoint，不启动
  watcher/orchestrator/service，不产生真实订单。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交并进入独立 QA。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- Add reachability analysis under `examples/hyperliquid/`
- Add focused tests
- Add `local_live_analysis/adaptive_live_evidence_reachability_0722T064/`
- workflow/report/tracking files

action：
- Freeze the exact quote-decision timeline:
  candidate snapshot -> manager submit/hold/cancel -> exposure/lifecycle
  observation -> final artifact.
- Compute minimum observations/exposure required by dynamic intensity fit and
  fill-feedback controller.
- Audit accepted T047/T052 remote artifacts by SSM read-only command and pull
  back only redacted estimator/lifecycle files needed for the analysis.
- Determine whether each prior package is sufficient, insufficient or
  ineligible as a prewarm source.
- Produce the only allowed next route:
  implement source-pinned prewarm, collect more no-order evidence, or request
  a changed live envelope.

verify：
- Focused deterministic tests for unreachable current-cycle ordering,
  min-observation gates, seed eligibility and boundary.
- Remote SSM commands must be read-only and limited to existing redacted
  artifacts.
- No private/account/order/cancel/service/live process.
- Repo-relative artifacts, checksum and deterministic rebuild.

done：
- Exact reason current T047/T052 adaptive quote behavior remained unchanged is
  proven from code order and evidence counts.
- A source-pinned, fail-closed next implementation route is selected.
- No live authorization is consumed.

blockers：
- Independent QA requires local structured SSM receipts/source files and
  content-derived counts/recommendation before acceptance.

提交信息：
- `Prove adaptive live evidence reachability`
