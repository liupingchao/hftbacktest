# 0718T013 PERSISTENT-KILL-SWITCH-REPAIR

执行线程：
- 总控 auto-loop / 业务实现线程

任务ID：
- 0718T013

标题：
- Persistent kill-switch state and flatten sequencing

简短描述：
- Implement Principal Alignment Task 2: durable fail-closed halt state, idempotent cancel/flatten sequencing, and the watcher quote-block boundary.
- Keep all verification offline with deterministic mock clients; do not touch live, private, order, cancel, remote, or service endpoints.

状态：
- 执行中

执行顺序：
- 当前唯一任务

前置任务：
- 0718T012

规则更新提醒：
- 无

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `docs/superpowers/plans/2026-07-17-principal-alignment-p0-p3.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files:
- `examples/hyperliquid/hyperliquid_tiny_live_real_order_executor.py`
- `examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- `examples/hyperliquid/test_hyperliquid_kill_switch.py`

action:
- Add `KillSwitchConfig`, `HaltState`, `KillSwitchEvidence`, durable atomic state read/write, explicit reset/expiry semantics, and fail-closed handling for malformed state.
- Add mock/client position and `market_close` interfaces, with reduce-only close evidence based on actual `szi`.
- Enforce ordered cancellation, owned open-order proof, position reread, flatten, residual-position proof, and idempotent repeated invocation.
- Gate watcher quote generation on the independent halt state without enabling a live endpoint.

verify:
- `python -m pytest examples/hyperliquid/test_hyperliquid_kill_switch.py`
- related executor/watcher regression tests
- `python -m py_compile` for modified Python files
- `git diff --check`

done:
- Report changed files, durable-state semantics, sequencing evidence, focused and related test counts, commit id, and explicit confirmation that no live/private/order/cancel/remote/service action occurred.
