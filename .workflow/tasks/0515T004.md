```md
执行线程：
- 业务线程-python

任务ID：
- 0515T004

标题：
- Residual replay fill mismatch diagnosis

简短描述：
- 基于 `0515T003` 修复后的同样本回归结果，做一个很小的 read-only residual diagnosis，只分析剩余 2 个 matched submit residual cases 的触发机制：`live_filled_replay_canceled` 与 `live_canceled_replay_filled`。本任务只读，不做 replay repair，不改策略，不补样本，不启动 live。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0515T003` 业务回报已提交

规则更新提醒：
- 本任务是 residual-case diagnosis，不是 replay repair。
- 只允许读取现有 live audit / replay audit / raw depth-trade / sidecar join 产物，做 case-level 触发机制分析。
- 不允许修改 replay fill model / lifecycle state machine / queue logic / strategy logic。

按线程规则执行：
- .workflow/workflow-kit/thread-report-template.md
- docs/thread-playbook.md
- task_plan.md
- progress.md
- findings.md

是否需要提交代码：
- 需要

提交要求：
- 自测/检查通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

Allowed files：
- `.workflow/tasks/0515T004.md`
- `.workflow/reports/0515T004-business.md`
- `examples/binance_tick_mm/replay_lifecycle_mismatch_diagnosis.py`
- `examples/binance_tick_mm/test_replay_lifecycle_mismatch_diagnosis.py`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `.workflow/dashboard.html`
- `.workflow/dispatch_suggestions.md`
- `docs/qa-acceptance-report.md`

Allowed generated outputs：
- `local_live_analysis/5-13-day-control-30min/stage6e_residual_replay_fill_diagnosis_0515T004/**`

Allowed read-only inputs：
- `.workflow/reports/0515T003-business.md`
- `.workflow/reports/0515T002-business.md`
- `.workflow/reports/0515T001-business.md`
- `local_live_analysis/5-13-day-control-30min/audit_live_5-13-day-control-30min.csv`
- `local_live_analysis/5-13-day-control-30min/out/backtest_audit_replay/audit_bt_audit_replay.csv`
- `local_live_analysis/5-13-day-control-30min/raw_market_data/**`
- `local_live_analysis/5-13-day-control-30min/t009_fixed_sidecar/**`
- `local_live_analysis/5-13-day-control-30min/stage6d_replay_lifecycle_repair_0515T003/**`
- `local_live_analysis/5-13-day-control-30min/stage6_execution_calibration_0514T007/**`
- `local_live_analysis/5-13-day-control-30min/stage6c_replay_lifecycle_mismatch_0515T001/**`

Forbidden files / areas：
- `examples/binance_tick_mm/backtest_tick_mm.py`
- `examples/binance_tick_mm/strategy_core.py`
- strategy behavior, fair/target formulas, configs, risk guards, quote placement behavior
- live scripts, AWS/remote state, live collection
- replay engine behavior / backtest fill model implementation
- core Rust, connector, py `event_dtype`, standard npz schema
- sample expansion tasks

Requires separate task：
- 修改 replay fill/cancel lifecycle 逻辑。
- 修改 queue / touch fill assumption / trade consumption logic。
- 新采集 current-format 样本。
- quote-adjustment promotion / strategy redesign。

Fatal Failures：
- 在 diagnosis 任务里直接改 replay repair 代码。
- 只凭 2 个 residual cases 就下 exact root-cause 结论但没有 case-level 触发证据。
- 将本任务扩展为 sample expansion、live 判断或策略修改。

action：
- 只分析 `0515T003` 修复后残余的 2 个 matched submit residual cases：
  1. `live_filled_replay_canceled`
  2. `live_canceled_replay_filled`
- 对每个 residual case，明确拉出：
  - live submit / cancel / fill / terminal event timeline
  - replay submit / cancel / fill / terminal event timeline
  - submit 后到 terminal 前的 raw depth / trade 片段
  - T009 sidecar join / top5 / age / stale context
- 回答这几个问题：
  - replay fill 是由哪类 market/trade trigger 命中的
  - live 同时段 raw depth / trade 是否支持这笔成交发生
  - 问题更像：
    1. cancel-race fill window 太短没建模
    2. touch fill assumption 太乐观
    3. trade/depth 对 submit 后 queue exposure 的近似有偏差
    4. 或其他 residual-only explanation
- 输出必须明确：
  - 每个 residual case 的最可能触发机制
  - 当前证据足不足以直接开 `0515T005` repair
  - 如果不足，缺的是什么证据

verify：
- `python -m pytest examples/binance_tick_mm/test_replay_lifecycle_mismatch_diagnosis.py`
- `python examples/binance_tick_mm/replay_lifecycle_mismatch_diagnosis.py --help`
- `python3 .workflow/build_dashboard.py`
- `git diff --check`

done：
- 回报必须明确两个 residual cases 的事件链与触发机制归因。
- 回报必须明确更像 short cancel-race miss、touch optimistic fill、还是 queue exposure approximation 偏差。
- 回报必须明确本任务未修改 replay、未改策略、未补样本、未启动 live。
- 回报必须给出是否值得进入 `0515T005` repair 的判断。
- 回报必须写 `commit id` 和 `提交信息`。
```
