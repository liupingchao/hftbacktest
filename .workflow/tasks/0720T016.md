# 0720T016 SINGLE-LEVEL-TWO-SIDED-1800S-BOUNDED-LIVE

执行线程：
- 总控 auto-loop / 业务执行线程 / live-awsserver1

任务ID：
- 0720T016

标题：
- Run one isolated 1800-second single-level two-sided Binance-lead Hyperliquid-maker lifecycle

简短描述：
- Repeat the QA-gated T011 single-level two-sided manager path with one exact change: watcher duration `900s -> 1800s`.
- Collect a new same-window lifecycle/evidence package under the standing conservative envelope; do not activate any adaptive controller or multi-level behavior.

状态：
- 待执行

执行顺序：
- 当前唯一任务

前置任务：
- `0720T015`（已通过）

规则更新提醒：
- Standing authorization permits the exact live/private/read/submit/cancel/remote/service-isolation actions in this task without another prompt。
- Execution safety and evidence integrity remain mandatory before the only live window。
- No second live window is allowed in this task。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `.workflow/reports/0720T015-qa.md`
- `docs/superpowers/plans/2026-07-17-principal-alignment-p0-p3.md`

是否需要提交代码：
- 不需要；live source is the exact committed tree below。

提交要求：
- Source commit is fixed before remote preflight。
- Evidence and workflow report are committed after local pullback/acceptance。
- 回报必须带 source `commit id` 和业务报告提交信息。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

source：
- Branch: `cross-exchange`
- Exact source commit: `657bde21f590829fbe08e2fcba3126cec4010e9f`
- Remote source: `/home/admin/hftbacktest-cross-exchange-0720T016`
- Source marker: `/home/admin/hftbacktest-cross-exchange-0720T016/source_commit.txt`

envelope：
- Host: `awsserver1`
- Account scope: Hyperliquid account configured by `/home/admin/XEMM_rust_latest/.env`
- Venue/symbol: Hyperliquid `BTC`
- Lead source: Binance public book ticker through event-driven edge gate
- Normal order: limit post-only `Alo`
- Exact profile: `two-sided-manager`
- Windows: `1`
- Max duration: `1800s`
- Max order size: `0.005 BTC` per order
- Max aggregate position delta: `0.01 BTC`
- Max loss: `1 USDC`
- Max submissions: `2`
- Requote attempts: `2`
- Quote hold: `3s`
- Wait after cancel/fill pullback: `10s`
- Kill-switch flatten only: reduce-only IOC；normal taker/crossing remains prohibited

control variables：
- Same fixed half-spread、accepted normalization、signal freshness、edge threshold、quality gate、quote size、hold/wait and manager logic as T011。
- Dynamic spread activation: off。
- Fill feedback activation: off。
- Inventory skew activation: off。
- Multi-level activation: off。
- Actual quote behavior change: false。
- Only primary strategy-experiment variable: duration `900s -> 1800s`。

files：
- Execute: `examples/hyperliquid/cross_exchange_live_remote_orchestrator.py`
- Execute: `examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- Accept: `examples/hyperliquid/cross_exchange_t024_same_window_acceptance.py`
- Remote evidence: `/home/admin/hftbacktest-cross-exchange-artifacts/principal_alignment_single_level_two_sided_0720T016/`
- Local evidence: `local_live_analysis/principal_alignment_single_level_two_sided_0720T016/`
- Workflow: this task, business report, QA report, `docs/qa-acceptance-report.md`, `task_plan.md`, `progress.md`, `findings.md`

action：
- Build an isolated remote source archive from the exact source commit and write the full commit marker。
- Verify local/remote archive SHA-256, runtime imports and exact no-start orchestrator preflight。
- Record current `xemm.service`/equivalent process state；stop a conflicting account-trading service only if active，and restore it after the window only when owned open orders are zero、position is reconciled and restoration cannot create unknown risk。
- Run one read-only account/service preflight:
  - exact source/import/Python/env；
  - persistent kill-switch clear；
  - account open orders empty；
  - BTC position within `0.01 BTC` and unambiguous；
  - no conflicting account-trading process；
  - no credential value persisted。
- Start exactly one detached orchestrator only after every preflight check passes。
- Require `event-driven-edge-gate-live`、exchange-reconciled two-sided manager、fast Hyperliquid L2、requote `2` and exact external duration `1800s`。
- Persist direct trigger/guard/edge/attempt identities and separate buy/sell intent/status/reference/terminal evidence。
- Pull the complete remote artifact root locally，verify terminal and remote/local SHA-256，then run same-window acceptance with `--expected-window-seconds 1800` and no legacy bridge。
- Record post-live open orders、BTC position、service/process state、kill-switch、writer health and unsupported economics claims。

verify：
- Source provenance is sealed before watcher start and independently reproducible from exact Git blobs。
- Preflight exact-matches task/window/source/profile/duration/caps/activation and does not start watcher or call endpoints。
- Both sides reach actual order endpoint calls unless one formal stop condition blocks before authorization。
- Every submitted attempt has authoritative reference-bound terminal cancel or complete maker-fill proof。
- Raw fills, direction, quantity, limit, fee and maker/taker role are independently reconstructed。
- Final and independent owned/account open orders are empty。
- BTC position and realized loss remain within envelope；any residual is reconciled or kill-switch flattened。
- Child process is reaped without unknown state；status writer and heartbeat remain healthy。
- Terminal SHA-256 and remote/local artifact equality pass。
- Same-window acceptance uses exact source/root/task and external duration `1800s`；it either passes mechanism/evidence integrity or records the first exact stop condition without optimism。
- No dynamic spread、fill feedback、inventory skew、multi-level or actual quote behavior activation。

stop conditions：
- Any source、task/window、command、duration、envelope or activation mismatch。
- Account ownership、concurrent service、open-order or position state is ambiguous。
- Kill-switch is not clear before live。
- Desired side set is not exactly `{buy,sell}` before first order call。
- Price precision/post-only invariant fails。
- Submission、size、position、loss or duration cap is exceeded。
- Duplicate/ambiguous/unattributed/conflicting fill evidence。
- Unknown order state or any submitted attempt lacks authoritative terminal proof。
- Owned/account open orders cannot be proven empty。
- Position cannot be reconciled or safe flatten cannot be proven。
- Process termination/reap、status writer、source verification or terminal checksum fails。
- Concurrent account activity contaminates the window。

done：
- One and only one `1800s` single-level two-sided bounded live window is preserved locally and independently accepted，or the first formal stop condition is recorded。
- Principal Task 7/12 mechanism and evidence conclusions remain separate from unsupported stable economics。
- No second window or strategy-variable expansion occurs inside T016。

blockers：
- Any stop condition ends the task and prevents a second live window。
- Multi-level remains locked until T016 receives independent QA acceptance as a real single-level two-sided lifecycle。

commit：
- source `657bde21f590829fbe08e2fcba3126cec4010e9f`

提交信息：
- source `Record T015 QA acceptance`
