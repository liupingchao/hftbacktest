```md
执行线程：
- 测试线程

任务ID：
- 0510T002

标题：
- 自动执行跨样本 Stage 6J 验证矩阵

简短描述：
- 使用本地 runner 自动把 `5-10-day-control-1h-06` 纳入 current-format 跨样本 Stage 6J 验证矩阵，比较 baseline、add-side guard、cooldown 等候选在多个样本上的 PnL、max position、drop rate、churn 和 cancel-fill source-path。本轮只跑本地 replay，不启动 live，不连接交易所。

状态：
- 待执行

执行顺序：
- 当前唯一任务

前置任务：
- 0510T001

规则更新提醒：
- 无

按线程规则执行：
- .workflow/workflow-kit/thread-report-template.md
- docs/thread-playbook.md

是否需要提交代码：
- 不需要

提交要求：
- 不需要提交
- 回报必须写 `commit：无` 和 `提交信息：无`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- .workflow/runners/run_task.py
- .workflow/runners/run_0510T002.py
- examples/binance_tick_mm/stage6j_replay.py
- local_live_analysis/5-10-day-control-1h-06/*
- local_live_analysis/5-9-small/*
- local_live_analysis/5-9-noon/*
- local_live_analysis/5-8-stage3-15m-livetest-v4/*

action：
- 通过 `.workflow/runners/run_task.py 0510T002` 调用专用 runner。
- 检查目标样本目录、audit replay config、live raw manifest 是否存在。
- 对样本集合运行 `examples/binance_tick_mm/stage6j_replay.py`。
- 输出到 `local_live_analysis/stage6j_cross_sample_0510T002/`。
- 解析 `stage6j_replay_decision.json`、`stage6j_replay_summary.csv` 和 `STAGE6J_B_REPLAY_SUMMARY.md`。
- 自动写 `.workflow/reports/0510T002-business.md`。
- 自动刷新 `.workflow/dashboard.html` 和 `.workflow/dispatch_suggestions.md`。
- 不启动真实 live，不连接交易所，不改 AWS 状态。

verify：
- `python3 .workflow/runners/run_task.py 0510T002`
- 验证 runner exit code。
- 验证 `.workflow/reports/0510T002-business.md` 存在。
- 验证 `local_live_analysis/stage6j_cross_sample_0510T002/stage6j_replay_decision.json` 存在。
- 验证 `.workflow/dispatch_suggestions.md` 状态进入 `待验收` 或记录明确阻塞。

done：
- 回报必须包含样本列表、候选数、hard failure 数、是否满足 no-promotion/可继续分析条件。
- 回报必须说明是否有候选可以进入规则设计，或者为什么仍然只能 diagnostic-only。
- 回报必须明确下一步是否允许 live micro test。默认不允许，除非跨样本证据充分且 QA 通过。
```
