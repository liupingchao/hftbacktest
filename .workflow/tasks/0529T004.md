```md
执行线程：
- 业务线程-python

任务ID：
- 0529T004

标题：
- Hyperliquid read-only public market-data evidence hardening

简短描述：
- 在 `0529T003` 已验证本地 raw sample / converter / sidecar / synthetic join 链路可行的基础上，补齐 Hyperliquid 只读公共 market-data 采集会话证据：subscription ack、session/connection attempt、reconnect count、startup/reconnect recovery snapshot 和 fresh raw gzip provenance。本任务仍然只做 public market data，不实现 private connector、order lifecycle、策略交易逻辑或 live trading。

状态：
- 待执行

执行顺序：
- `0529T003` QA 通过后执行，或由总控明确允许在 `0529T003` 待验收状态下继续执行。
- 本任务与 `0529T002` 独立；不得修改、依赖或混入 `0529T002`。

前置任务：
- `0529T003` 已完成业务线程并产出 `limited_pricing_research` 分类、runner、测试和本地样本 artifacts。
- 以 `.workflow/reports/0529T003-business.md`、`local_live_analysis/hyperliquid_raw_alignment_0529T003/metrics.json`、`local_live_analysis/hyperliquid_raw_alignment_0529T003/acceptance_report.md` 为直接输入事实。

规则更新提醒：
- 请先阅读规则文档后再执行本任务。
- 本任务是 Hyperliquid read-only public market-data evidence hardening，不是交易 connector、private lifecycle、order submit/cancel、strategy port、parameter search、tiny-live、default-on 或 promotion 任务。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `docs/thread-playbook.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交任务文件、实现代码、测试、只读 public sample artifacts、业务报告和必要 workflow 更新。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

Allowed files：
- `.workflow/tasks/0529T004.md`
- `.workflow/reports/0529T004-business.md`
- `examples/hyperliquid/**`
- `local_live_analysis/hyperliquid_public_sample_0529T004/**`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `.workflow/dashboard.html` and `.workflow/dispatch_suggestions.md` only if they can be regenerated without pulling unrelated parallel task state into this task.

Allowed read-only inputs：
- `.workflow/tasks/0529T003.md`
- `.workflow/reports/0529T003-business.md`
- `examples/hyperliquid/hyperliquid_raw_alignment.py`
- `examples/hyperliquid/test_hyperliquid_raw_alignment.py`
- `local_live_analysis/hyperliquid_raw_alignment_0529T003/**`
- `py-hftbacktest/hftbacktest/data/utils/hyperliquid.py`
- `docs/hyperliquid_live_replay_alignment_design.md`
- Official Hyperliquid docs, rechecked before relying on current schema facts:
  - `https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api`
  - `https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket`
  - `https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket/subscriptions`
  - `https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint`
  - `https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/tick-and-lot-size`
  - `https://github.com/hyperliquid-dex/hyperliquid-python-sdk`

Forbidden files / areas：
- Binance strategy behavior, Binance acceptance-rule changes, Binance live/deploy files, and `0529T002` artifacts.
- Hyperliquid private connector, signing, nonce handling, private account endpoints, `orderUpdates`, user fills, open orders, order status, submit/cancel, fills reconciliation, positions/account state, and order lifecycle code.
- Strategy behavior, quote placement, fair/target formula, inventory logic, risk guards, production defaults, live configs.
- Standard npz schema changes and canonical audit schema changes.
- Any trading live/tiny-live run, remote deploy, parameter search, default-on behavior, guard relaxation, or promotion claim.
- Fabricating fresh-sample evidence when network collection is unavailable.

Implementation scope：
1. Reconfirm current official Hyperliquid public schema facts for WebSocket `l2Book`, WebSocket `trades`, subscription response/ack behavior, public Info `l2Book`, and tick/lot precision before implementation.
2. Add a narrow read-only public sample collector under `examples/hyperliquid/`, tentatively:
   - `python examples/hyperliquid/hyperliquid_public_sample.py --help`
   - default coin `BTC`
   - default duration `60s` or `120s`
   - writes line-oriented raw gzip rows as `<local_ts_ns> <raw_json>`.
3. The collector may use only public market-data endpoints:
   - required WebSocket channels: `l2Book` and `trades`
   - optional public Info `l2Book` request for startup/reconnect recovery snapshot evidence
   - no private keys
   - no account endpoints
   - no order endpoints
   - no strategy process
4. Generate `collection_manifest.json` with:
   - exchange, network, coin, channel set, WebSocket URL, output raw file
   - local start/end timestamps and duration
   - raw sha256
   - session id
   - connection attempts
   - reconnect count
   - subscription ack presence/count by channel
   - first/last local timestamp by channel
   - message counts by channel
   - normal close / disconnect reason
   - explicit private/order endpoint absence flags.
5. Generate `recovery_snapshots.jsonl` with startup and reconnect public Info `l2Book` snapshots:
   - local timestamp
   - reason: `startup` or `reconnect`
   - coin
   - request status/error
   - best bid/ask when available
   - bid/ask level counts
   - raw public snapshot payload or compact payload reference.
6. Feed the fresh raw gzip into the existing `0529T003` alignment runner, writing alignment outputs under:
   - `local_live_analysis/hyperliquid_public_sample_0529T004/alignment/`
7. Extend the alignment report/metrics only as needed to consume the enhanced collection manifest and recovery snapshot counts. Do not change the standard `data.npz` schema.
8. Classification target:
   - `passes_pricing_research_market_view` when parse/conversion/top-N/join metrics pass and subscription/session/recovery evidence is present.
   - `limited_pricing_research` if market view is good but session/recovery evidence remains incomplete.
   - `compressed_action_path_only` or `unusable` when raw/conversion/join coverage is incomplete.
9. If sandbox/network restrictions prevent fresh public collection, report `阻塞` with the exact failed command/error. Do not substitute the old local sample as a passing fresh-sample result.

Required outputs：
- `local_live_analysis/hyperliquid_public_sample_0529T004/raw.gz`
- `local_live_analysis/hyperliquid_public_sample_0529T004/raw.sha256`
- `local_live_analysis/hyperliquid_public_sample_0529T004/collection_manifest.json`
- `local_live_analysis/hyperliquid_public_sample_0529T004/recovery_snapshots.jsonl`
- `local_live_analysis/hyperliquid_public_sample_0529T004/alignment/run_manifest.json`
- `local_live_analysis/hyperliquid_public_sample_0529T004/alignment/converter_manifest.json`
- `local_live_analysis/hyperliquid_public_sample_0529T004/alignment/data.npz`
- `local_live_analysis/hyperliquid_public_sample_0529T004/alignment/raw_provenance.csv`
- `local_live_analysis/hyperliquid_public_sample_0529T004/alignment/raw_to_npz_mapping.csv`
- `local_live_analysis/hyperliquid_public_sample_0529T004/alignment/topn_sidecar.csv`
- `local_live_analysis/hyperliquid_public_sample_0529T004/alignment/synthetic_joined_views.csv`
- `local_live_analysis/hyperliquid_public_sample_0529T004/alignment/metrics.json`
- `local_live_analysis/hyperliquid_public_sample_0529T004/alignment/acceptance_report.md`
- `.workflow/reports/0529T004-business.md`

Acceptance criteria：
- Collector is reproducible from CLI and operates without private credentials.
- Fresh raw gzip contains both `l2Book` and `trades` public messages.
- Subscription ack evidence is present and counted by channel, or a concrete current-schema explanation is documented if Hyperliquid's current response differs.
- Session id, connection attempt count, reconnect count, start/end timestamps, raw sha256, and close/disconnect reason are recorded.
- At least one startup public Info `l2Book` recovery snapshot is recorded, and reconnect snapshots are recorded if reconnect occurs.
- Existing Hyperliquid converter produces non-empty `data.npz`, or the report gives a concrete blocker tied to input/schema/environment.
- Event-order validation is run through the converter path.
- Top-N sidecar and raw provenance are generated without Binance `U/u/pu`, `lastUpdateId`, or `bookTicker` assumptions.
- Market-data-only synthetic as-of join reports join coverage, future joins, missing joins, and join-age distribution.
- Final report clearly states whether the fresh sample reaches `passes_pricing_research_market_view`.
- Report explicitly states no strategy behavior, private connector, order lifecycle, live trading, parameter search, default-on behavior, guard relaxation, tiny-live, or promotion was changed or authorized.

verify：
- `python examples/hyperliquid/hyperliquid_public_sample.py --help`
- `python examples/hyperliquid/hyperliquid_raw_alignment.py --help`
- Focused tests for the collector/manifest and alignment integration.
- `python -m pytest examples/hyperliquid/test_hyperliquid_raw_alignment.py -q`
- `python -m py_compile py-hftbacktest/hftbacktest/data/utils/hyperliquid.py`
- Run a short fresh read-only public sample if network is available.
- Run the alignment runner on the fresh raw gzip.
- `git diff --check`
- `python3 .workflow/build_dashboard.py` only if unrelated parallel task files are not present; otherwise document the isolation-preserving skip.
- No strategy live command.
- No remote deploy.
- No private key / account / order endpoint command.

done：
- Business report ends in `待验收` or `阻塞` if public network collection is unavailable.
- Report lists exact official references checked, files read, code changed, fresh sample path, generated artifacts, key metrics, and final classification.
- Report includes the next recommended task after fresh public market-data evidence is accepted.
- Report includes `commit id` and `提交信息`.
- Report explicitly states:
  - no Binance strategy behavior changed
  - no Hyperliquid private connector was implemented
  - no order submit/cancel/fill lifecycle code was implemented
  - no trading live or remote deploy was run
  - no default-on, guard relaxation, parameter search, tiny-live, or promotion claim was made
```
