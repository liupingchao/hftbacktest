# 0729T001 C6IN-SPREAD-HOST-HUNT-RETAIN-WINNER

执行线程：
- 总控 / 业务执行线程

任务ID：
- 0729T001

标题：
- 同可用区 7 台 c6in spread hunting 并保留真实胜者

简短描述：
- 在 `ap-northeast-1c` 的一个 rack-level spread placement group 中启动
  7 台 `c6in.xlarge`，使用同一已验收 Linux probe 并发采集 Binance
  Futures BTCUSDT 与 Hyperliquid BTC `l2Book fast=true` 900 秒。
- `awsserver1` 全程保持运行。正式采集与本地独立验真通过后，只保留
  综合排名第一的 c6in 实例并开启 API termination protection，终态必须
  恰好为 `awsserver1 + 1 台 hunting 胜者`。

状态：
- 未通过

执行顺序：
- 当前唯一正式任务

前置任务：
- `0728T076`（已通过）

规则更新提醒：
- 请先阅读规则文档后再执行本任务。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `AGENTS.md`

是否需要提交代码：
- 需要实现并加入 Git 跟踪；本任务不要求创建 commit。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `latency-probe/ec2_spread_hunt.py`
- `latency-probe/ec2_hunt.py`
- `latency-probe/test_ec2_hunt.py`
- `docs/ec2_latency_hunt_protocol.md`
- `docs/evidence/ec2_latency_hunt_20260729/`
- `local_live_analysis/ec2_latency_hunt_0729T001-*/`
- `.workflow/tasks/0729T001.md`
- `.workflow/reports/0729T001-business.md`
- `.workflow/reports/0729T001-qa.md`
- `docs/qa-acceptance-report.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

action：
- 启动前独立确认 `awsserver1` 的实例 ID、类型、AZ 和 running 状态，
  并记录为保护资源；本任务不得 stop、terminate、reboot 或修改它。
- 创建一个 `strategy=spread`、`spread-level=rack` 的 placement group，
  在同一子网和 AZ 中分两批启动 2 + 5 台完全相同的 `c6in.xlarge`。
- 两台 canary 先通过 SSM、时钟、双 venue smoke、上传和下载哈希门禁；
  再启动其余五台并执行全体 900 秒正式采集。
- 复用 T074 已验收的确定性 Linux x86_64 probe binary
  `ba2fbaa4...3403`，不得在候选间改变 AMI、user-data、probe、duration、
  warmup、venue 或 symbol。
- 所有候选必须通过 before/after Chrony 完整 error-bound `<=750us`、
  双 venue trace 完整性、零 drop、remote rebuild、archive SHA/size 和
  本地独立 raw-to-summary rebuild。
- 启动前冻结胜者综合分：
  `4*Binance feed P99 rank + 2*Binance feed P50 rank +
  2*Binance tick-to-wire P99 rank +
  1*Hyperliquid tick-to-wire P99 rank +
  1*benchmark P99 rank`；低分胜出，依次用最差单项 rank、
  clock max bound、instance ID 打破平局。
- 仅在 7/7 候选全部通过门禁且胜者可唯一确定后，给胜者写入
  `HuntDisposition=winner`、`RetainedByTask=0729T001` 和明确 Name tag，
  开启 API termination protection；随后终止其余 6 台。
- 胜者继续留在 spread placement group 中。删除临时 S3 bucket；确认
  六台落选者均 terminated，胜者 running 且 termination protection
  enabled，`awsserver1` 仍 running。
- 任一执行、验证、评分或最终化步骤失败时，默认清理全部 7 台 hunting
  候选和临时资源，但绝不触碰 `awsserver1`。

verify：
- Python focused tests 覆盖单 rack spread group、7 台上限、冻结评分、失败全清、
  成功保留唯一胜者和 protected-instance 排除。
- `bash -n latency-probe/ec2_hunt_remote.sh`。
- Rust fmt/test/clippy。
- canary `2/2` 通过；full run `7/7`、每台 900 秒。
- 双 venue remote rebuild `14/14`，本地独立 rebuild `14/14`。
- AWS 最终独立查询：
  - `awsserver1` 原实例仍为 running；
  - 恰好 1 台 `0729T001` c6in 为 running；
  - 其余 6 台为 terminated；
  - 胜者 termination protection 为 enabled；
  - 临时 bucket 不存在；
  - spread placement group 仅承载保留胜者。
- `git diff --check` 和 staged scope 检查。

done：
- 保留的是本轮实际测量的胜出 instance，而不是同机型重建替代品。
- AWS 终态只有既有 `awsserver1` 与一台受保护的 c6in hunting 胜者运行。
- 原始证据保留在本机，紧凑 manifest、排名、终态与 QA 文件加入 Git 跟踪。

blockers：
- 本任务只证明 public feed 与本地 probe pipeline 的相对延迟，不证明真实
  order ACK、fill、PnL 或迁移后的生产稳定性。
- 胜者在后续 8 小时 burn-in 与服务迁移验收前不得替代或释放
  `awsserver1`。
