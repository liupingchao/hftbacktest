# 任务派发

执行线程：
- SKHYNIX Safe Reentry After Flow Excursion A0 Execution 业务线程

任务ID：
- 0828T011

标题：
- 实现并执行 SAFE_REENTRY_AFTER_FLOW_EXCURSION_V1 A0

简短描述：
- 严格按 0828T010 已冻结并通过 QA 的 zero-target contract，实现
  novelty、persistence、excursion、bilateral recovery、refractory 与
  safe-reentry 状态机，在 29 个 admitted Binance captures 上执行 A0
  support audit 并输出唯一 classification。

状态：
- 待验收

执行顺序：
- 当前唯一 formal task

前置任务：
- 0828T010（已通过）

规则更新提醒：
- 已阅读 AGENTS.md、workflow-kit manual、methodology kernel、
  SAFE_REENTRY_AFTER_FLOW_EXCURSION_V1 A0 plan/QA，以及 predecessor
  LIQUIDITY_BREAK_ONSET_V1 implementation/execution/QA。

按线程规则执行：
- AGENTS.md
- .workflow/workflow-kit/workflow-manual.md

是否需要提交代码：
- 需要

提交要求：
- 自测和 deterministic double-build 通过后提交。
- 回报必须带 commit id 和提交信息。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- examples/hyperliquid/skhynix_safe_reentry_after_flow_excursion_a0.py
- examples/hyperliquid/test_skhynix_safe_reentry_after_flow_excursion_a0.py
- local_live_analysis/skhynix_safe_reentry_after_flow_excursion_a0_0828T011/
- .workflow/tasks/0828T011.md
- .workflow/reports/0828T011-business.md

action：
- 绑定冻结 plan SHA、predecessor accepted checkpoint caches 与 raw source
  closure。
- 使用 20ms checkpoint 实现 1000ms bilateral novelty pre-quiet。
- 使用 250ms window 内累计 100ms qualifying exposure 实现 causal
  persistence，confirmation 不 backdate。
- 将 active/recovery/refractory 内的方向翻转合并到同一 episode。
- 使用 causal pre-excursion trailing median 冻结 bid/ask depth baseline。
- 实现 bilateral 80% recovery 和连续 1000ms refractory。
- 仅在 refractory completion 当前时点检查 spread、OBI、depth 并决定
  是否发出 safe-reentry anchor。
- 构造 outcome-blind controls、匹配、follow-up geometry 和 A0 gates。
- 执行双构建并核对非 cache artifacts SHA。

verify：
- focused pytest。
- Python compile。
- ruff。
- raw size/SHA closure 与 upstream cache size/SHA closure。
- zero-outcome ledger。
- deterministic double-build identity。
- artifact size/SHA closure。
- git diff --check。

done：
- 输出 canonical `classification.json` 和 `reports/A0_summary.json`。
- 明确 A0 是否授权 A1。
- 报告 novelty、persistence、compression、recovery、safe-reentry 和
  control support 的关键结果。

明确禁止：
- 不提高 z threshold。
- 不缩短 novelty、persistence 或 refractory。
- 不等待 future wider spread。
- 不查看 future midpoint、best quote、contact、queue fill、markout、
  PnL 或 private data。
- 不拟合 H0/H1。
- 不因结果稀疏而修改 V1 tuple。
