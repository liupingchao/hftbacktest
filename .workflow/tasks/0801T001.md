# 任务派发

执行线程：
- 业务线程-python/cross-exchange-research

任务ID：
- 0801T001

标题：
- T017-MOTIF-FAIL-CLOSED-AND-ATOMIC-PUBLICATION-REPAIR

简短描述：
- 修复 T017 首轮 QA 的三项 P1 和一项 P2，重建
  `Hyperliquid liquidity-response motif family` M1 数据集并重新独立验收。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- `0730T017` QA `未通过`。

规则更新提醒：
- 无

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/reports/0730T017-qa.md`
- `docs/skhynix_liquidity_response_motif.md`

是否需要提交代码：
- 不需要

提交要求：
- 不需要提交
- 回报写 `commit：无`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `examples/hyperliquid/cross_exchange_liquidity_response_episodes.py`
- `examples/hyperliquid/test_cross_exchange_liquidity_response_episodes.py`
- `docs/skhynix_liquidity_response_motif.md`
- `local_live_analysis/skhynix_liquidity_response_0730T017/`
- `.workflow/tasks/0801T001.md`
- `.workflow/reports/0801T001-business.md`
- `.workflow/reports/0801T001-qa.md`
- `docs/qa-acceptance-report.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

action：
- 对 segment manifest 与 timeline 行执行 exact campaign/segment/profile
  identity gate，并校验 Binance symbol / Hyperliquid coin。
- 将八个 segment manifest 纳入 initial provenance、构建后 rehash 和最终
  manifest。
- exact 校验 R1 primary horizons、horizon tolerance、diagnostic subset、
  provenance/mask/labels/reconciliation gates 及零 timestamp/future/cross
  segment 错误。
- 已有输出目录使用操作系统级原子目录交换发布，消除 output path
  visibility gap；交换后旧产物再从 temporary path 清理。
- 增加对应 failure-injection tests，重建真实八段数据并重新独立 QA。

verify：
- `python -m pytest examples/hyperliquid/test_cross_exchange_liquidity_response_episodes.py -q`
- `python -m pytest examples/hyperliquid/test_cross_exchange_alignment_acceptance.py examples/hyperliquid/test_cross_exchange_liquidity_response_episodes.py -q`
- 真实八段 builder 重建。
- 独立复核 identity/provenance/tolerance/atomic exchange 反例、所有输出
  SHA/row 和 episode 时间轴语义。

done：
- T017 QA 的 P1 x3 / P2 x1 全部由代码门禁和反例关闭。
- 修复后的真实 manifest 使用任务 ID `0801T001`，全部 acceptance gates
  通过。
- 独立 QA 为 `已通过` 后，M1 episode-builder 阶段完成。

hard boundary：
- 仅使用现有本地数据。
- 不访问网络、AWS 或 SSH。
- 不新增采集。
- 不做聚类、信号拟合、策略回测、maker 身份或 PnL 推断。
