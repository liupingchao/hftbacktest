# 0721T038 DUAL-GUARD-PRIMARY-CAUSE-HOLD-PUMP-SHUTDOWN-REPAIR

执行线程：
- 总控 auto-loop / 业务执行线程

任务ID：
- 0721T038

标题：
- Unify simultaneous guard primary cause and prove bounded hold-pump shutdown

简短描述：
- 修复 T037 QA 的 producer primary-cause 不一致：所有 trigger、attempt 和 submit-decision artifacts 共用同一个 canonical outcome。
- 收紧 manager hold observer 与 independent acceptance，使 T038+ pass 必须证明 bounded pump stop acknowledgement 和适用时的 source closure。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0721T037`（未通过）

规则更新提醒：
- 本任务 offline-only。
- T037 live evidence immutable；不得修改、重封或追溯升级。
- Acceptance 必须保持 strict/fail-closed；不得通过放宽 join、一致性或 terminal-proof 检查来消除 T037 failures。
- 独立 QA 通过前不得执行 private read、新 live 或任何 adaptive/multi-level activation。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `.workflow/reports/0721T037-qa.md`
- `docs/superpowers/plans/2026-07-17-principal-alignment-p0-p3.md`

是否需要提交代码：
- 需要

提交要求：
- 业务实现和业务报告分开提交。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- Modify: `examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- Modify: `examples/hyperliquid/cross_exchange_t024_same_window_acceptance.py`
- Test: `examples/hyperliquid/test_hyperliquid_tiny_live_m2_event_driven_watcher.py`
- Test: `examples/hyperliquid/test_cross_exchange_t024_same_window_acceptance.py`
- Workflow: task、business、QA、fixed QA、`task_plan.md`、`progress.md`、`findings.md`

action：
- 定义一个 producer canonical submit-authorization outcome，固定 precedence 为 `immediate fail_closed > anti-drift block > edge block > pass`。
- Trigger `guard_status/guard_reason/live_window_called`、attempt `guard_status/guard_reason/skip_reason` 和 anti-drift submit `skip_reason` 必须复用该 outcome。
- 各 stage 原始 evidence 保持独立：immediate guard、anti-drift 和 edge 的 status/reason 不得被覆盖或丢失。
- Hold observer 在停止时等待最多 `INLINE_REPRICE_CANCEL_CHECK_SECONDS`，记录 stop-request 时是否有 read in flight、等待后是否仍 in flight、stop acknowledgement、source close applicability/result/error。
- T038+ hold pass 必须满足 strict boolean schema、pump 已停止、等待后无 in-flight read、可关闭 source 已关闭、无 close error、deadline overrun 不超过既有上限且 continuity 未变化。
- Independent acceptance 必须从 raw hold observation 重建相同结论；不能只信 producer `status=pass`。
- 新 hold 字段使用 task rollout 边界；T026/T031/T037 历史 evidence 保持 immutable，既有 acceptance 边界不被追溯改变。

verify：
- Simultaneous immediate fail + anti-drift block 时，trigger、attempt、anti-drift-submit 均选择 immediate reason，且 order endpoint 不被调用。
- Anti-drift-only、edge-only、immediate-only 和 all-pass precedence 保持原有含义。
- Independent decision reconstruction 对合法 simultaneous failure 与 producer exact-match；同步伪造 primary/skip reason 必须 fail closed。
- Built-in closable source 在 bounded wait 内 stop-acknowledged、after-wait no-inflight、source closed 后才可 pass。
- 在 wait bound 内释放的 hostile source 可完成 shutdown；超过 bound 的 source 必须 fail closed，但 manager cancel 仍受既有 deadline bound 控制。
- T038+ missing、wrong-type、false acknowledgement、after-wait inflight、required-but-not-closed、close error 均被 acceptance 独立拒绝。
- Focused and full Hyperliquid regression pass。
- T037 immutable acceptance 继续 blocked；T031 blocked、T026 pass、T016/T022 blocked 的历史边界不变。
- No live/private/account/order/cancel/network/remote/service operation occurs。

stop conditions：
- Producer 与 independent acceptance 的 canonical precedence 不一致。
- Secondary stage evidence 被 primary normalization 覆盖或删除。
- Pump shutdown 可以在未 acknowledgement、after-wait in-flight 或 required source 未关闭时报告 pass。
- Cancel path 被任意 source read/close 无限阻塞，或既有 deadline overrun 上限被放宽。
- Historical evidence、live endpoint、quote、risk、submission、terminal-query timing 或 activation behavior 被修改。
- Any live/private/network/remote/service action。

done：
- Producer 的 simultaneous guard failure 只有一个可复现 primary cause，同时保留所有 subordinate stage evidence。
- T038+ manager hold pass 具有 independently verifiable bounded shutdown proof。
- Source 通过业务回归并进入独立 QA；只有 QA 通过后才可派发新的 fixed-quote live。

blockers：
- 独立 QA 验收。

commit：
- implementation `c436ce00cee50a03c3accadc01517691880a9927`

提交信息：
- `Unify guard and hold shutdown evidence`
