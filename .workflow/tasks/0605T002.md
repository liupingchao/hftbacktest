```md
执行线程：
- 业务线程-python

任务ID：
- 0605T002

标题：
- Diagnose shutdown local-absent proof and missing exchange open-orders reconciliation

简短描述：
- 针对 `0604T016` / `0605T001` 后剩余的 shutdown proof 风险做复现和定位：当前 `local_absent_from_local_orders` 只能证明本地订单 snapshot 中找不到订单，不能证明交易所 open orders 已清空；同时当前 shutdown 没有 REST/open-orders reconciliation 或 audit tail final proof。本任务只做诊断、复现、根因定位和后续修复边界建议，不做修复实现。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- `0604T016` 已完成 shutdown wait-result 分类与 `order_response_received` / `terminal_confirmed` 拆分。
- `0605T001` 已通过 QA，修复了 `PARTIALLY_FILLED` 本地 active 订单被误判为 terminal 的漏洞。

规则更新提醒：
- 无

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `docs/thread-playbook.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 提交任务文件、业务诊断报告；如为稳定复现新增 focused 诊断测试或只读复现脚本，也应提交。
- 不得提交 production 修复代码。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0605T002.md`
- `.workflow/reports/0605T002-business.md`
- `examples/binance_tick_mm/live_tick_mm.py` 只读检查；禁止修复。
- `examples/binance_tick_mm/test_backtest_tick_mm.py` 或更合适的 focused test 文件，仅允许新增诊断/复现测试，不允许修复 production 行为。
- 如需要只读检查 REST/open-orders 能力边界，可检查 Binance connector / live HBT 暴露接口，但不得实现调用路径。
- 必要 workflow tracking 文件仅限本任务状态/结论更新。

forbidden：
- 不修复 `examples/binance_tick_mm/live_tick_mm.py` shutdown helper。
- 不接入 REST private/order endpoint。
- 不修改 py binding、Rust live bot/backtest、connector、production config、normal trading loop cancel 语义、audit schema、strategy quote/submit/cancel 行为。
- 不启动 live / tiny-live / default-on。
- 不做参数搜索、回放实验扩展、promotion。
- 不把 open-orders reconciliation、cancel retry、REST 状态复查或 audit tail record 写入生产逻辑。
- 不静默扩大到无关 shutdown / live safety 重构。

review under test：
- 当前 `0605T001` 后，`_confirm_shutdown_terminal_state_from_local_orders(...)` 对同一 `order_id`：
  - `NEW` / `PARTIALLY_FILLED`：active，`terminal_confirmed=False`。
  - terminal status：`terminal_confirmed=True`。
  - 本地 snapshot 找不到该 `order_id`：`terminal_confirmed=True`，`terminal_confirmation_source=local_orders`，`final_order_status=local_absent_from_local_orders`。
- 风险点 1：`local_absent_from_local_orders` 是本地内存 / 本地订单 snapshot 证明，不等价于交易所 open orders 已清空。
- 风险点 3：当前 shutdown summary / result 中没有独立 exchange-side final proof，例如 REST open orders reconciliation 或 audit tail final no-open-order record。

action：
- 只读确认当前 shutdown helper 行为：
  - `local_absent_from_local_orders` 是否会设置 `terminal_confirmed=True`。
  - 该 proof 是否只来自 `hbt.orders(asset_no)` 本地 snapshot。
  - shutdown summary 是否没有输出 REST/open-orders reconciliation proof。
  - `ShutdownCancelResult` 是否没有记录 exchange-side final proof 字段或 audit tail final proof。
- 稳定复现以下 case matrix；可用 focused diagnostic tests、small fake/stub objects、或只读 semantic checks，但不得修复 production 行为：
  - `local_absent_sets_terminal_confirmed_without_exchange_check`：本地 snapshot 找不到订单时，当前 helper 会确认 terminal。
  - `local_absent_can_disagree_with_exchange_open_orders_stub`：fake/stub 中本地 orders 不含订单，但 exchange/open-orders stub 仍含该 order id；当前 helper 不查询该 exchange stub，仍确认 terminal。
  - `shutdown_result_has_no_exchange_final_proof_field`：结果对象只记录 local proof，不记录 exchange reconciliation 状态。
  - `shutdown_summary_has_no_exchange_reconciliation_counter`：summary 只统计 wait/order_response/terminal/unknown/failed，没有 exchange-open-orders proof 计数。
  - `audit_tail_final_state_not_written`：shutdown helper / summary 没有写入可审计的 final no-open-order / reconciliation tail record。
  - `rest_reconciliation_boundary_not_implemented`：只读定位当前代码中没有安全的 REST open-orders reconciliation 调用路径；如存在 connector 能力，也未接入 shutdown proof。
- 对每个 case 输出：
  - 是否可稳定复现。
  - 复现方式和命令。
  - 实际 helper result / log counter / fake stub 调用记录。
  - 根因代码位置。
  - 影响边界：live shutdown safety、日志证明、audit/alignment tail、测试覆盖缺口。
  - 是否属于当前任务发现的问题，或只是后续修复候选。
- 输出根因分层：
  - local proof 层：`local_absent_from_local_orders` 只说明本地 snapshot 没有同一 `order_id`。
  - exchange proof 层：当前无独立 REST/open-orders reconciliation。
  - logging/reporting 层：summary 无 exchange final proof counter。
  - audit 层：没有 final reconciliation tail record。
  - tests 层：需要 focused fake 覆盖 local absent 与 exchange still-open 不一致场景。
- 给出后续修复任务建议边界，但不得在本任务内实施：
  - 是否需要新增独立字段，例如 `exchange_reconciliation_checked`、`exchange_open_order_absent`、`exchange_confirmation_source`、`final_proof_level`。
  - 是否需要把 `local_absent` 从 terminal proof 降级为 local-only proof，或保留 terminal but 标注 proof level。
  - 是否需要安全接入 REST open-orders reconciliation，并明确超时、失败、权限缺失时的状态。
  - 是否需要 audit tail 写入 final reconciliation record。

acceptance criteria：
- 必须明确判断风险 1 / 3 是否为事实，并指出哪些表述需要更精确。
- 必须稳定复现或只读证明 `local_absent_from_local_orders` 当前只来自本地 snapshot。
- 必须稳定复现 local absent 与 fake exchange open-orders 仍含订单时，当前 helper 不会发现该不一致。
- 必须证明当前 shutdown result / summary / audit tail 没有 exchange-side final proof。
- 必须说明该风险是否构成当前已修 bug 的回归；预期结论应区分“已修 bug”与“更高 proof level 缺口”。
- 不提交 production 修复。
- 业务报告写入 `.workflow/reports/0605T002-business.md`，状态为 `待验收`。

verify：
- `/home/molly/anaconda3/envs/hftbacktest/bin/python -m pytest examples/binance_tick_mm/test_backtest_tick_mm.py -k "shutdown_cancel or terminal_confirmed or exchange_reconciliation or local_absent or open_orders"`
- 如新增单独诊断 test 文件，运行对应 pytest 文件。
- `/home/molly/anaconda3/envs/hftbacktest/bin/python -m pytest examples/binance_tick_mm/test_*.py`，如 scope/耗时允许。
- `git diff --check`

done：
- 回报必须说明改动点、稳定复现矩阵、根因分层、所有发现的问题位置、不可复现/未覆盖项、推荐后续修复范围、验证结果、commit id 和 commit message。
- 回报必须明确本任务未修复 shutdown helper、未接入 REST/open-orders reconciliation、未修改 binding/Rust live bot/connector、未启动 live、未做 promotion。
```
