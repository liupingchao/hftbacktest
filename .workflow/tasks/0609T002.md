```md
执行线程：
- 业务线程-research

任务ID：
- 0609T002

标题：
- Basis-positive targeted public collection on awsserver1 and local validation

简短描述：
- 基于 `0609T001` 的 `targeted_collection_ready` 结果，在 `awsserver1` 采集 public-only Binance lead / Hyperliquid lag synchronized raw samples，并拉回 local machine 完成本地 alignment / join / pricing-signal / canonical aggregate / T001-style wrong-way decomposition 验证。本任务只做 public market-data collection 和本地 read-only testing，不实现策略、不接 private/order、不生成 case-library 或 shadow decisions、不启动 live/default-on/tiny-live、不 promotion。

状态：
- 待验收

执行顺序：
- 当前下一任务；必须在 `0609T001` QA 通过后执行。

前置任务：
- `0609T001` 需 QA 已通过，且 final recommendation 必须为 `targeted_collection_ready`。
- `0608T006` QA 已通过，final recommendation 为 `needs_more_samples`。
- `0604T003` canonical event-mode artifacts / `0604T004` loader / `0604T005` source-lock guard 仍是 formal evidence foundation。

规则更新提醒：
- 本任务允许 public-only raw market-data collection，但只允许在 `awsserver1` 采集 raw public data；所有 accepted alignment、join、pricing-signal、canonical aggregate 和 decomposition/testing 必须在 local machine 用拉回的 raw artifacts 完成。
- 不得采集 private/account/order 数据，不得启动策略 live，不得输出交易动作或下单/quote 行为。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `docs/thread-playbook.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 自测和本地验证通过后提交任务文件、业务报告、task-scoped collection/processing/test artifacts、必要 runner/test 变更（如有）和必要 workflow tracking 更新。
- 回报必须带 `commit id` 和 `提交信息`。
- 如果执行中未改代码，只提交任务报告、artifacts 和 tracking；不得混入无关 dirty 文件。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0609T002.md`
- `.workflow/reports/0609T002-business.md`
- Existing public-only collection / local processing scripts as command targets:
  - `examples/hyperliquid/synchronized_public_collection.py`
  - `examples/hyperliquid/hyperliquid_public_sample.py`
  - `examples/hyperliquid/hyperliquid_raw_alignment.py`
  - `examples/hyperliquid/cross_exchange_lead_lag_join.py`
  - `examples/hyperliquid/cross_exchange_lead_lag_analysis.py`
  - `examples/hyperliquid/binance_led_pricing_signal_runner.py`
  - `examples/hyperliquid/binance_led_multi_sample_robustness.py`
  - `examples/hyperliquid/canonical_basis_positive_wrong_way_decomposition.py`
  - Existing accepted canonical/event-mode helper scripts if required by the local chain.
- New/updated tests only if runner glue changes are needed.
- Remote collection scope:
  - `awsserver1:/home/admin/hft_live/worktrees/0609T002-basis-positive-targeted-public/**`
  - `awsserver1:/home/admin/hft_live/runs/0609T002-basis-positive-targeted-public/**`
- Local task artifacts:
  - `local_live_analysis/basis_positive_targeted_public_collection_0609T002/**`
  - `local_live_analysis/canonical_basis_positive_wrong_way_decomposition_0609T002/**`
- 必要 workflow tracking 文件仅限本任务状态/结论更新。

read-only inputs：
- `local_live_analysis/canonical_basis_positive_wrong_way_decomposition_0609T001/**`
- `local_live_analysis/canonical_basis_positive_robustness_0608T006/**`
- `local_live_analysis/event_mode_canonical_pricing_signal_0604T003/**`
- Existing accepted public collection / processing task artifacts as reference only:
  - `local_live_analysis/cross_exchange_public_sample_0602T001/**`
  - `local_live_analysis/event_horizon_comparison_0604T002/**`

sample plan：
- Collect synchronized public-only BTC samples on `awsserver1`; local workstation collection does not count.
- Target new samples:
  - `2` active/high-vol windows.
  - `2` normal-liquidity windows.
  - Optional `1` quiet retest if active/normal windows are not available or if wrong-way concentration needs same-regime retest.
- Required requested duration per sample: `1800s`; minimum acceptable duration per sample: `1200s` if collection window closes and report explains the caveat.
- Naming convention:
  - `xemm_0609_active_a`
  - `xemm_0609_active_b`
  - `xemm_0609_normal_a`
  - `xemm_0609_normal_b`
  - optional `xemm_0609_quiet_retest_a`
- Regime labels are planning labels only. Final regime classification must be computed from local public artifacts after pullback.

remote collection requirements：
- Before collection, push/sync the approved commit to `awsserver1` and create a clean task-scoped worktree. Do not overwrite, reset, or reuse the existing dirty live worktree.
- Run only public market-data collection commands on `awsserver1`; do not run Binance connector, Hyperliquid private connector, strategy bot, `run_live.sh`, or any private/order endpoint process.
- Use the accepted synchronized public collection wrapper or equivalent accepted public-only command target.
- Record, per sample:
  - remote host and worktree path
  - git commit
  - command line
  - planned regime
  - planned duration
  - process start/end timestamps
  - Binance and Hyperliquid public channel counts
  - raw artifact paths
  - raw sha256
  - collection logs
  - no-private/no-order/no-strategy boundary flags
- Copy back only public raw artifacts, collection manifests, sha256 files, public depth snapshots, recovery snapshots, and logs needed for local processing.

local processing / testing requirements：
- Accepted processing must run on the local machine from pulled-back raw artifacts.
- For each pulled sample, locally run or reproduce the accepted chain:
  - Binance public raw alignment / sidecar/provenance sufficient for lead BBO/mid/spread/top-N where supported.
  - Hyperliquid public raw alignment with quality classification.
  - Cross-exchange as-of join with local-observation-time policy.
  - Lead-lag analysis / pricing-signal runner preserving no-future-leakage policy.
  - Event-mode / canonical aggregate generation or equivalent accepted canonical event-mode formal-evidence path.
  - `0609T001` wrong-way decomposition rerun against the refreshed canonical aggregate.
- Local outputs must explicitly distinguish:
  - raw remote collection evidence
  - local processing artifacts
  - formal canonical evidence artifacts
  - diagnostic-only artifacts

required outputs：
- `local_live_analysis/basis_positive_targeted_public_collection_0609T002/remote_collection_manifest.json`
- `local_live_analysis/basis_positive_targeted_public_collection_0609T002/pulled_artifact_manifest.json`
- `local_live_analysis/basis_positive_targeted_public_collection_0609T002/sample_quality_matrix.csv`
- `local_live_analysis/basis_positive_targeted_public_collection_0609T002/local_processing_manifest.json`
- `local_live_analysis/basis_positive_targeted_public_collection_0609T002/basis_positive_targeted_sample_design_result.md`
- Per-sample pulled public raw artifacts:
  - Binance `raw.gz`, `raw.sha256`, `collection_manifest.json`, `depth_snapshot.json`, logs.
  - Hyperliquid `raw.gz`, `raw.sha256`, `collection_manifest.json`, `recovery_snapshots.jsonl`, logs.
- Per-sample local processed artifacts:
  - synchronized sample manifest / run manifest / synchronization quality summary
  - local Binance alignment/provenance artifacts
  - local Hyperliquid alignment metrics
  - local as-of join artifacts
  - local pricing-signal artifacts
- Refreshed task-scoped decomposition outputs:
  - `local_live_analysis/canonical_basis_positive_wrong_way_decomposition_0609T002/basis_positive_wrong_way_manifest.json`
  - `basis_positive_vs_nonpositive_baseline.csv`
  - `basis_positive_magnitude_bins.csv`
  - `basis_positive_wrong_way_rows.csv`
  - `basis_positive_tail_state_decomposition.csv`
  - `basis_positive_controlled_effects.csv`
  - `basis_positive_tail_filter_feasibility.csv`
  - `basis_positive_targeted_collection_plan.md`
  - `basis_positive_next_step_recommendation.md`
- `.workflow/reports/0609T002-business.md`

acceptance criteria：
- `0609T001` QA has passed before remote collection starts.
- Remote raw collection provenance proves the new samples were collected on `awsserver1`.
- Local accepted processing proves alignment/join/pricing-signal/decomposition were run on the local machine from copied raw artifacts.
- At least `4` new public synchronized samples are attempted; at least `3` must be usable or the report must end `阻塞` / `未通过` with exact collection or market-window blockers.
- Target evidence gates:
  - combined basis-positive sample count reaches at least `5`.
  - max basis-positive sample row share is below `0.40`, or the report explains why the sample design failed to reduce concentration.
  - active/high-vol and normal-liquidity samples are both represented if market conditions allow.
  - candidate tail hypotheses from T001 are rechecked: `basis_positive_small`, negative HL top5 imbalance, and negative HL microprice-minus-mid.
  - proposed filter concentration must repeat across at least `3` samples before it can be called stable.
- Binance momentum control must use `input_binance_mid_move_ticks_from_prev`; if unavailable, emit an unavailable bucket and do not substitute a future label.
- Missing/non-numeric basis rows must be excluded from `basis > 0` vs `basis <= 0` comparison.
- All manifests explicitly state no private keys, no private/account/order endpoints, no order lifecycle, no strategy implementation, no live/default-on/tiny-live, no parameter search, no case-library/shadow decision, and no promotion.
- Final recommendation must be exactly one of:
  - `tail_filter_hypothesis_validated_for_read_only_research`
  - `needs_more_targeted_samples`
  - `reject_tail_filter_hypotheses`
  - `blocked_collection_or_processing`
- Any recommendation remains read-only research only and does not authorize strategy/live/private/order behavior.

verify：
- Pre-collection local checks:
  - `python examples/hyperliquid/synchronized_public_collection.py --help`
  - `python examples/hyperliquid/synchronized_public_collection.py collect --help`
  - `python examples/hyperliquid/canonical_basis_positive_wrong_way_decomposition.py --help`
  - `python -m pytest examples/hyperliquid/test_synchronized_public_collection.py examples/hyperliquid/test_canonical_basis_positive_wrong_way_decomposition.py -q`
- Remote collection evidence:
  - record exact `ssh admin@awsserver1 ...` commands used
  - record exact sample ids, start/end timestamps, exit codes, and raw artifact paths
  - verify remote git commit and dirty state in the task-scoped worktree
- Pullback verification:
  - verify pulled raw sha256 against remote manifest
  - parse every pulled collection manifest
  - parse `remote_collection_manifest.json` and `pulled_artifact_manifest.json`
- Local processing verification:
  - parse every local sample/run/synchronization manifest with `python -m json.tool`
  - run local alignment/join/pricing-signal/decomposition commands and record exact command lines
  - parse refreshed `basis_positive_wrong_way_manifest.json`
  - check required CSV/markdown outputs exist
  - boundary text check for no private/order/strategy/live/case-library/shadow/promotion authorization
  - `git diff --check`

forbidden：
- Local workstation public raw collection counted as accepted sample.
- Private/account/order endpoint use on Binance or Hyperliquid.
- Order lifecycle, submit/cancel/fill logic, user stream, account state, positions, signing, nonce handling.
- Strategy implementation, strategy live, production config/default changes, parameter search, default-on, tiny-live, promotion.
- Case-library implementation, shadow decision generation, executable trading instruction, actual order side, quote price/size, leverage, stop rule, take-profit rule, deployment recommendation.
- Connector/core API changes, standard npz schema changes, canonical Binance maker audit schema changes unless a separate task explicitly authorizes them.

done：
- Business report must include remote collection commands, sample ids, planned vs actual regimes, start/end times, duration, remote commit/dirty state, pullback commands, sha256 reconciliation, local processing commands, local artifact paths, quality classifications, refreshed decomposition metrics, final recommendation, verification commands, blockers/caveats if any, commit id, and commit message.
- Business report must explicitly state that all accepted collection happened on `awsserver1` and all accepted analysis/testing happened on the local machine from pulled raw artifacts.
- Business report must end in `待验收` if collection and local validation complete, or `阻塞` / `未通过` if the criteria cannot be met.
```
