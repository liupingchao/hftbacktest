# 任务派发

执行线程：
- SKHYNIX OBI Reversal Track A3 业务线程

任务ID：
- 0828T006

标题：
- 执行 OBI_REVERSAL_V1 A3 H0/H1 Competing-Risk Increment Test

简短描述：
- 在 0828T005 已冻结的 7,164 个 primary entries 和 one-tick
  first-passage targets 上，拟合 discrete-time multinomial competing-risk
  H0 baseline 与仅增加 binary reversal indicator `R` 的 H1，检验 arrival
  path 是否提供 current OBI 和 ordinary context 之外的增量信息。

状态：
- 已通过

执行顺序：
- 当前唯一正式任务

前置任务：
- 0828T005

规则更新提醒：
- 已阅读 AGENTS.md、workflow-kit manual、task/report/QA templates、
  Conditional-Risk Research Methodology Kernel v1、canonical OBI reversal
  Track A 方案和 0828T004/0828T005 contracts、ledgers、warnings。

按线程规则执行：
- AGENTS.md
- .workflow/workflow-kit/workflow-manual.md

是否需要提交代码：
- 需要

提交要求：
- 聚焦测试、双构建和 artifact closure 后提交
- 回报必须带 `commit id` 和提交信息

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- examples/hyperliquid/skhynix_obi_reversal_track_a3.py
- examples/hyperliquid/test_skhynix_obi_reversal_track_a3.py
- docs/skhynix_binance_continuous_background_interpretable_m_state_competing_risk_track_a_plan_20260828.md
- local_live_analysis/skhynix_obi_reversal_track_a3_0828T006/
- .workflow/tasks/0828T006.md
- .workflow/reports/0828T006-business.md

action：
- 验证 0828T005 run manifest、classification、state/target ledger 和
  outcome-access closure。
- 冻结 role split：
  - train/preprocess：2026-07-29、07-30、08-03、08-04；
  - blocked validation：2026-08-07、08-24、08-25；
  - no-refit replay：2026-08-26、08-27。
- 冻结 6 段 elapsed-time basis：
  - 100ms；
  - 200-500ms；
  - 600-1,000ms；
  - 1,100-2,000ms；
  - 2,100-5,000ms；
  - 5,100-120,000ms。
- H0 使用 19 个 decision-time、side-oriented、train-standardized
  snapshot/context features；不包含 reversal state-machine 的确定性重构。
- H1 与 H0 使用相同 risk rows、targets、time basis、scaler 和 ridge，
  唯一新增字段为 binary `reversal_indicator_R`。
- 每个 cause 的 baseline-category multinomial logit 只增加一个
  `beta_k * R`；冻结方向为 `beta_follow > 0`、`beta_fail < 0`。
- Ridge grid 冻结为 `[1e-4, 1e-3, 1e-2, 1e-1]`，仅根据开发日期
  leave-one-date-out 的 H0 entry-level NLL 选择；H1 不参与选择。
- Final H0/H1 只在 train roles 拟合；blocked validation 和 replay 均
  no-refit。
- Primary validation metric 为 date-equal mean entry negative log loss：
  `Delta_NLL = NLL_H0 - NLL_H1`。
- Materiality threshold 冻结为 `0.002 nats/entry`；使用 5,000 次
  date-block bootstrap，要求 95% CI lower bound 严格大于 0.002。
- 同时要求：
  - validation integrated Brier delta 非负；
  - follow/fail cause-specific integrated Brier delta 均不低于 `-0.001`；
  - `beta_follow > 0` 且 `beta_fail < 0`；
  - 至少 2/3 validation dates 的 NLL delta 为正；
  - 最大正向单日贡献不超过 0.70；
  - 至少 3 个 elapsed-time bins 的 NLL delta 为正；
  - 最大正向 elapsed-bin contribution 不超过 0.70；
  - 排除 first-100ms entries 后 NLL delta 仍为正。
- 强制报告：
  - pooled 与 date-equal scores；
  - first-100ms/500ms influence；
  - per-date、per-role、per-time-bin increments；
  - calibration、OOD 和 coefficients；
  - 92.43% pre-detection warning。
- A3 classification 仅由 blocked validation 决定；replay 结果留给 A4
  transport，不可救回 A3 failure。

verify：
- focused unit tests覆盖 multinomial gradient、risk-row expansion、
  no-refit design identity、CIF conservation、date-block bootstrap 和
  H1-only-R contract。
- finite-difference gradient check。
- synthetic positive/negative/null fixtures。
- full historical execution。
- 两次 deterministic build artifact hashes 一致。
- formal artifact size/SHA closure。
- `git diff --check`。

done：
- 回报 selected ridge、parameter count、convergence、validation/replay
  Delta_NLL/IBS、bootstrap CI、beta directions、date/bin influence、
  calibration/OOD、A3 gates/classification 和下一 A4 边界。

明确禁止：
- 不修改 A0/A1/A2 的 OBI、threshold、dwell、alignment、controls、
  barrier、grid、target、censoring 或 `tau_max`。
- H1 不增加 reversal strength、delay、slope、interaction 或其他 path
  descriptor。
- 不用 validation/replay 选择 ridge、features、time basis 或 gate。
- 不用 replay 救回 blocked-validation failure。
- 不执行 dependence nulls、stronger-H0 或 prospective confirmation；
  它们属于 A4/A5。
- 不访问 private API，不下单，不采集新数据。
- 不生成 maker PnL、fill 或 actionability 正结论。
