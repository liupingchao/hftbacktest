# 0722T051 OFFLINE-CANCEL-IDENTITY-RETRY-INSTRUMENTATION

执行线程：
- 总控 auto-loop / 业务执行线程

任务ID：
- 0722T051

标题：
- Add bounded exact-identity cancel retry and terminal evidence instrumentation

简短描述：
- 针对 T049 attempt 1 的 ambiguous cancel 证据，增加同一 owned reference
  的 bounded exact-cloid cancel retry，并持久化每次 cancel request 的 identity
  和结果；失败时仍保持 unknown/fail-closed。

状态：
- 执行中

执行顺序：
- 当前唯一任务

前置任务：
- `0722T050`（已通过）
- `0722T049`（阻塞；sealed evidence 只读）

规则更新提醒：
- offline-only；禁止 live、private account、order、cancel、remote、
  service 和 immutable artifact 修改。
- retry 只能针对同一 owned reference、从 oid fallback 到 exact cloid 一次；
  不得把 generic error、empty open-orders 或 unknown query 解释成 terminal
  success。

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交。
- 回报必须带 commit id 和提交信息。

是否进行QA验收：
- 是

files：
- `examples/hyperliquid/hyperliquid_maker_order_manager.py`
- `examples/hyperliquid/test_hyperliquid_maker_order_manager.py`
- workflow task/business/QA/fixed QA records

action：
- 首次按 oid cancel 失败且有 exact cloid 时，最多追加一次 cloid cancel。
- 成功时将第二次 exact response 作为 authoritative result，并记录 retry
  identity；两次都失败时返回 `cancel_unknown` 并保留两次 redacted evidence。
- 保持 acceptance 对 ambiguous generic/unknown terminal 的 fail-closed 语义。

verify：
- focused maker-order-manager tests。
- `python -m pytest examples/hyperliquid -q`。
- `py_compile`、`git diff --check`。
- 不执行 live/private/remote/service。

done：
- exact identity retry 有正例和失败保持 fail-closed 的测试覆盖。
- T049 sealed artifacts 不被修改或重解释。

blockers：
- 任何 live/private/remote 操作请求均超出本任务范围。

提交信息：
- `Add bounded exact-identity cancel retry`
