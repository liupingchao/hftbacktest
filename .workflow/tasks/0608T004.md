```md
执行线程：
- 业务线程-research

任务ID：
- 0608T004

标题：
- Regime 011 feature-conditioned directional signal validity diagnosis

简短描述：
- 基于 `0608T003` 对整体 `regime_011_1000_spread_10_20_ticks` directional momentum viability 的 rejection，进一步只读诊断 T003 中局部 feature-conditioned pattern 是否是真实、稳定、decision-time-visible、非共线/非泄漏、且在 cost/tail proxy 后仍有研究价值。本任务只做 validity diagnosis，不实现策略、不生成 shadow decisions、不启动 live。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- `0604T003` QA 已通过，canonical event-mode artifacts 是正式证据源。
- `0604T004` QA 已通过，canonical event-mode evidence loader / validator foundation 可复用。
- `0604T005` QA 已通过，canonical source-lock guard 可复用。
- `0604T009` QA 已通过，唯一 regime 输入为 `regime_011_1000_spread_10_20_ticks`。
- `0608T002` QA 已通过，maker executability final recommendation 为 `reject_not_maker_executable`。
- `0608T003` QA 已通过，整体 directional momentum final recommendation 为 `reject_directional_edge_unstable`；T003 不支持 directional case-library / strategy / live / promotion，但暴露出若干局部 feature-conditioned signals 需要只读诊断。

规则更新提醒：
- 请先阅读规则文档后再执行本任务。
- 本任务只做 read-only feature-conditioned signal validity diagnosis；不得实现策略、不得生成 shadow decisions、不得输出交易动作/下单方向/quote/order 行为、不得接 private/order endpoint、不得 live/default-on/tiny-live、不得 promotion。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `docs/thread-playbook.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交任务文件、业务报告、read-only runner/tests、task-scoped artifacts 和必要 workflow tracking 更新。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0608T004.md`
- `.workflow/reports/0608T004-business.md`
- `examples/hyperliquid/canonical_feature_conditioned_signal_validity.py`
- `examples/hyperliquid/test_canonical_feature_conditioned_signal_validity.py`
- `local_live_analysis/canonical_feature_conditioned_signal_validity_0608T004/**`
- 必要 workflow tracking 文件仅限本任务状态/结论更新。

read-only inputs：
- Formal canonical source:
  - `local_live_analysis/event_mode_canonical_pricing_signal_0604T003/**`
  - Row-level canonical sample files must be resolved from `multi_sample_manifest.json` via `samples[].pricing_signal_rows`.
- Candidate / regime context:
  - `local_live_analysis/canonical_regime_synthesis_0604T009/**`
- T002 maker executability artifacts:
  - `local_live_analysis/canonical_maker_executability_0608T002/**`
- T003 directional momentum artifacts:
  - `local_live_analysis/canonical_directional_momentum_viability_0608T003/**`
- Loader / source-lock foundation:
  - `examples/hyperliquid/canonical_event_mode_evidence.py`
  - `local_live_analysis/canonical_event_mode_evidence_0604T004/**`
  - `local_live_analysis/canonical_evidence_source_lock_0604T005/**`
- Signal and horizon context:
  - `local_live_analysis/canonical_signal_quality_ranking_0604T006/**`
  - `local_live_analysis/canonical_horizon_regime_diagnostics_0604T007/**`
- Decision-time-visible input contract:
  - `local_live_analysis/binance_led_hyperliquid_data_contract_0601T004/**`

candidate scope：
- Regime id:
  - `regime_011_1000_spread_10_20_ticks`
- Base context:
  - `horizon_ms=1000`
  - `hyperliquid_context_quality=primary_usable`
  - `hyperliquid_join_age_bucket=fresh_0_50ms`
  - `hyperliquid_spread_bucket=spread_10_20_ticks`
- T003 fact:
  - Overall final recommendation is `reject_directional_edge_unstable`.
  - Overall direction hit rate is near random and sample stability fails.
  - T003 feature summary shows local sign-conditioned signals that require validity diagnosis before any further research use.

fixed feature scope：
- Diagnose only these T003-observed fields:
  - `context_basis_mid_ticks`
  - `context_hyperliquid_top5_imbalance`
  - `context_hyperliquid_microprice_minus_mid_ticks`
  - `input_binance_top5_imbalance`
  - `input_binance_microprice_minus_mid_ticks`
- Optional diagnostic-only comparison:
  - `input_binance_mid_move_ticks_from_prev`
  - `input_binance_top5_bid_qty`
- Do not add a broad feature search.

forbidden：
- New data collection.
- Strategy implementation, directional strategy implementation, order behavior implementation, case-library implementation, shadow decision generation, live/default-on/tiny-live, promotion.
- Hyperliquid or Binance private/account/order endpoints.
- Order lifecycle, submit/cancel/fill logic, production config/default changes, parameter search or optimization sweep.
- Connector/core API changes, standard npz schema changes, canonical Binance maker audit schema changes.
- Treating ordinary synthetic fixed-grid artifacts as formal evidence.
- Treating `100/250ms` evidence as canonical independent support.
- Outputting executable trading instructions, actual order side, quote price, size, leverage, stop rule, take-profit rule, or deployment recommendation.

action：
- Implement a read-only validity diagnosis runner that consumes canonical event-mode artifacts through the accepted loader/source-lock path.
- Validate T002 prerequisite `reject_not_maker_executable` and T003 prerequisite `reject_directional_edge_unstable`.
- Recompute the fixed feature-conditioned signals from row-level canonical `samples[].pricing_signal_rows` using the same regime context as T003.
- For each fixed feature/sign/strength bucket, diagnose:
  - per-sample stability
  - row and sample mass
  - direction consistency at `1000ms`
  - diagnostic `5000ms` / `10000ms` persistence
  - `100/250ms` watch-only alias context
  - cost-adjusted net edge proxy
  - wrong-way/tail risk proxy
  - decision-time visibility / contract caveat status
  - feature redundancy / collinearity with other fixed features
  - possible label leakage or future-field contamination risk
- Keep all conclusions read-only and proxy-scoped. Do not claim executable strategy PnL, real order fill, private execution feasibility, or live readiness.

required diagnostic questions：
- Are the strong T003 local feature-conditioned hit rates stable across all 3 canonical samples?
- Are they driven by one sample, one tail event, or one redundant feature family?
- Are `context_basis_mid_ticks`, Hyperliquid top5 imbalance, and Hyperliquid microprice fields decision-time-visible under the accepted `0601T004` contract, or contract-caveated diagnostic context only?
- Does any fixed pattern survive conservative cost/tail proxy without relying on `100/250ms` aliased evidence?
- Is any pattern independent enough to justify a later read-only case-library design discussion, or should Regime 011 be closed?

output classification values：
- Signal validity:
  - `valid_signal_supported`
  - `valid_signal_watch`
  - `invalid_unstable`
  - `invalid_not_decision_visible`
  - `invalid_redundant_or_leakage_risk`
  - `invalid_tail_or_cost_reject`
- Next-step recommendation:
  - `candidate_for_read_only_case_design_discussion`
  - `watch_needs_more_canonical_public_samples`
  - `watch_needs_contract_visibility_clarification`
  - `reject_feature_conditioned_pattern`
  - `close_regime_011_line`

strict acceptance line for `candidate_for_read_only_case_design_discussion`：
- Pattern is decision-time-visible or explicitly accepted as contract-visible context.
- Pattern is stable across all 3 canonical samples and not single-sample dominated.
- Pattern does not rely on `100/250ms` aliased evidence.
- Conservative cost proxy does not turn net edge negative.
- Tail risk is not clearly unacceptable.
- Redundancy/leakage diagnosis does not classify the pattern as invalid.
- Report remains read-only and does not output executable trading instructions, shadow decisions, or live/promotion claims.

outputs：
- `feature_validity_manifest.json`
- `feature_pattern_validity_summary.csv`
- `per_sample_pattern_stability.csv`
- `feature_redundancy_collinearity.csv`
- `decision_visibility_caveat_audit.csv`
- `cost_tail_validity.csv`
- `feature_conditioned_validity_report.md`

acceptance criteria：
- Runner refuses non-canonical or diagnostic-only synthetic inputs through the accepted loader/source-lock path.
- Runner validates T002 `reject_not_maker_executable` and T003 `reject_directional_edge_unstable` before running.
- Runner resolves row-level inputs from `multi_sample_manifest.json` `samples[].pricing_signal_rows`.
- Assessment is limited to `regime_011_1000_spread_10_20_ticks` and the fixed feature list.
- Outputs include all required artifact files and final recommendation taxonomy.
- Report explicitly states conclusions are read-only public-data proxies, not executable strategy PnL or private execution proof.
- Report does not output actual order side, quote price, size, leverage, stop rule, take-profit rule, strategy action, shadow decision, private/order endpoint, live/default-on/tiny-live, or promotion claim.
- Tests cover canonical input acceptance, diagnostic input refusal, T002/T003 prerequisite validation, decision-visibility caveat classification, redundancy/collinearity classification, cost/tail reject path, and final taxonomy generation.

verify：
- `python examples/hyperliquid/canonical_feature_conditioned_signal_validity.py --help`
- `python -m py_compile examples/hyperliquid/canonical_feature_conditioned_signal_validity.py examples/hyperliquid/test_canonical_feature_conditioned_signal_validity.py`
- `python -m pytest examples/hyperliquid/test_canonical_feature_conditioned_signal_validity.py`
- `python examples/hyperliquid/canonical_feature_conditioned_signal_validity.py --input-dir local_live_analysis/event_mode_canonical_pricing_signal_0604T003 --candidate-dir local_live_analysis/canonical_regime_synthesis_0604T009 --maker-executability-dir local_live_analysis/canonical_maker_executability_0608T002 --directional-momentum-dir local_live_analysis/canonical_directional_momentum_viability_0608T003 --signal-ranking-dir local_live_analysis/canonical_signal_quality_ranking_0604T006 --horizon-regime-dir local_live_analysis/canonical_horizon_regime_diagnostics_0604T007 --data-contract-dir local_live_analysis/binance_led_hyperliquid_data_contract_0601T004 --output-dir local_live_analysis/canonical_feature_conditioned_signal_validity_0608T004`
- `python -m json.tool local_live_analysis/canonical_feature_conditioned_signal_validity_0608T004/feature_validity_manifest.json`
- Scripted or manual check that the report contains no executable trading instruction, order side, quote price, size, private/order endpoint, shadow decision, live/default-on/tiny-live, or promotion claim.
- `git diff --check`

done：
- Business report must include method, input source-lock, T002/T003 prerequisite checks, fixed feature validity results, decision-visibility caveats, redundancy/leakage diagnosis, cost/tail results, final recommendation, artifact paths, verification commands, blockers/caveats if any, commit id, and commit message.
- Business report must explicitly state this is only read-only feature-conditioned signal validity diagnosis and does not authorize strategy implementation, private/order endpoints, order lifecycle, live/default-on/tiny-live, parameter search, case-library implementation, shadow decisions, or promotion.
- Business report should end in `待验收` if verification passes.
```
