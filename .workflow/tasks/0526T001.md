```md
执行线程：
- 测试线程

任务ID：
- 0526T001

标题：
- Step 9E targeted active control sample collection for min-move sweep seeds

简短描述：
- 基于 `0525T001` 的 fine-bucket 发现，采集 current-format no-rule / default-off active control 样本，优先补 `min_move_quote_age_churn_guard` 参数搜索 seed 相关场景和自然 fills 缺口。任务只做采集、归档、audit replay 和可重跑诊断，不放宽 guard、不启用 candidate、不做 tiny live 或 promotion。

状态：
- 已通过

执行顺序：
- 当前下一任务；必须在 `0525T001` QA 通过后执行。执行中用户要求第二次、第三次采集暂缓，因此本轮只完成 `5-26-active-minmove-control-30min-a`。

前置任务：
- `0525T001` QA 通过
- `0521T002` QA 已通过
- `0519T006` QA 已通过，T006 quote-update audit fields 已落地

规则更新提醒：
- 请先阅读规则文档后再执行本任务。
- 本任务是采集 / 归档 / replay / 诊断任务，不是策略实现任务。
- 不允许为了增加 fills 放宽 guard、启用 quote-adjustment candidate、修改策略行为、修改生产默认值、执行 tiny live、或声明 `ready_for_tiny_live_design`。
- 新样本必须保持 current-format、no-rule、default-off，并尽量避开 `5-19-night-active-30min-a` 这类 caveated sample 质量问题。

按线程规则执行：
- `.workflow/workflow-kit/thread-report-template.md`
- `docs/thread-playbook.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 采集前提交/推送可部署 commit；采集、拉回、重跑、报告完成后提交 workflow/report/artifacts。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

Allowed files：
- `.workflow/tasks/0526T001.md`
- `.workflow/reports/0526T001-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `.workflow/dashboard.html`
- `.workflow/dispatch_suggestions.md`
- `docs/qa-acceptance-report.md`
- `local_live_analysis/5-26-active-minmove-control-30min-a/**`
- `local_live_analysis/5-26-active-minmove-control-30min-b/**`
- `local_live_analysis/5-26-active-minmove-control-30min-c/**`
- `local_live_analysis/archive/5-26-active-minmove-control-30min-a.tar.gz`
- `local_live_analysis/archive/5-26-active-minmove-control-30min-a.tar.gz.sha256`
- `local_live_analysis/archive/5-26-active-minmove-control-30min-b.tar.gz`
- `local_live_analysis/archive/5-26-active-minmove-control-30min-b.tar.gz.sha256`
- `local_live_analysis/archive/5-26-active-minmove-control-30min-c.tar.gz`
- `local_live_analysis/archive/5-26-active-minmove-control-30min-c.tar.gz.sha256`

Allowed read-only inputs：
- `.workflow/reports/0525T001-business.md`
- `.workflow/reports/0525T001-qa.md`
- `local_live_analysis/stage9d_candidate_bucket_refinement_0525T001/**`
- `local_live_analysis/stage9c_multi_sample_validation_0521T002/**`
- `local_live_analysis/5-19-day-control-30min/**`
- `local_live_analysis/5-19-night-active-30min-a/**`
- `local_live_analysis/5-19-night-active-30min-b/**`
- `local_live_analysis/5-19-night-active-30min-c/**`
- `local_live_analysis/5-21-day-control-60min/**`
- `examples/binance_tick_mm/deploy/run_live.sh`
- `examples/binance_tick_mm/deploy/preflight_live_run.py`
- `examples/binance_tick_mm/audit_schema.py`
- `examples/binance_tick_mm/strategy_core.py`
- `examples/binance_tick_mm/live_tick_mm.py`
- `examples/binance_tick_mm/backtest_tick_mm.py`
- `examples/binance_tick_mm/align_live_run.py`
- `examples/binance_tick_mm/maker_acceptance.py`
- `examples/binance_tick_mm/binance_top5_provenance.py`
- `examples/binance_tick_mm/execution_outcome_labels.py`
- `examples/binance_tick_mm/quote_anchor_safety.py`
- `examples/binance_tick_mm/execution_outcome_calibration.py`
- `examples/binance_tick_mm/quote_adjustment_replay.py`
- `examples/binance_tick_mm/candidate_bucket_refinement.py`

Allowed remote paths：
- `awsserver1:/home/admin/hft_live/**`

Forbidden files / areas：
- Strategy rule logic, fair/target formula, quote behavior, production defaults, live production configs beyond task-scoped run dirs.
- Core/connector APIs, canonical data conversion, standard hftbacktest npz schema.
- Any candidate enablement, default-on behavior, guard relaxation, Step 5C promotion, inventory-control implementation, queue/touch repair, runner modification, or promotion claim.

Requires separate task：
- Multi-sample parameter sweep implementation.
- Modifying `quote_adjustment_replay.py` or `candidate_bucket_refinement.py`.
- Promoting any candidate to live or production.
- Running a tiny live candidate test.
- Changing strategy behavior or production defaults.

Fatal Failures：
- Starting before `0525T001` QA passes.
- Enabling any non-baseline quote-adjustment candidate.
- Relaxing guard / throttle / post-only / stale protection to manufacture fills.
- Continuing a sample after early audit header check shows missing T006 fields.
- Accepting raw gzip corruption without documented repair and preserved original.
- Accepting sidecar/join quality with `future_join_count > 0` or `gap_crossed_join_count > 0` as clean input.
- Interpreting collected samples as promotion proof or `ready_for_tiny_live_design`.

target sampling：
- Collect up to 3 separated `30min` current-format no-rule / default-off control samples during active / non-calm market windows.
- Run ids:
  - `5-26-active-minmove-control-30min-a`
  - `5-26-active-minmove-control-30min-b`
  - `5-26-active-minmove-control-30min-c`
- Prefer separated windows over one continuous long run. Target at least `30min` between accepted windows unless market conditions clearly changed and the report documents why shorter spacing is acceptable.
- Target aggregate additional filled orders: `>=160` natural fills if market conditions allow. If not reached, report exact residual fill gap and the missing regimes; do not compensate by loosening guards.

scenario focus：
- Primary target: `min_move_quote_age_churn_guard` seed regimes from `0525T001`.
- Prefer windows likely to cover:
  - young quote churn
  - API/churn normal and churn pressure
  - stale latency medium/high
  - one-tick tight spread
  - medium volatility / markout dispersion
  - large inventory skew / low inventory score
- Do not target `inventory_reservation_shift_band`; it has no current parameter-sweep seed and many reject buckets.
- Treat `size_reduction_or_add_side_suppression_pressure` as a narrow secondary objective only for:
  - `spread_quote_distance=one_tick_tight`
  - `volatility_markout_dispersion=volatility_high`

action：
- Confirm local deployable commit contains T006 quote-update audit fields and the accepted Step 9B / 9C / 9D runners.
- Run focused preflight checks before deploy:
  - `python -m pytest examples/binance_tick_mm/test_deploy_preflight.py`
  - `python -m pytest examples/binance_tick_mm/test_quote_adjustment_replay.py`
  - `python examples/binance_tick_mm/deploy/preflight_live_run.py --help`
  - `python examples/binance_tick_mm/align_live_run.py --help`
  - `python examples/binance_tick_mm/maker_acceptance.py --help`
  - `python examples/binance_tick_mm/quote_adjustment_replay.py --help`
  - `python examples/binance_tick_mm/candidate_bucket_refinement.py --help`
  - `bash -n examples/binance_tick_mm/deploy/run_live.sh`
- Push/deploy a clean commit to `awsserver1` using a task-scoped clean worktree.
- For each run id:
  - prepare task-scoped remote run dir
  - run no-rule / default-off control collection for about `30min`
  - early-check live audit header for all 15 T006 fields
  - stop cleanly and preserve start/stop markers, deployment manifest, audit, raw gzip, connector logs, and bot logs
  - pull artifacts to local `local_live_analysis/<run_id>/`
  - verify or repair raw gzip only if needed, preserving the original corrupted file
  - create archive and sha256
  - run audit replay / alignment:
    - `python examples/binance_tick_mm/align_live_run.py --run-id <run_id> --local-root local_live_analysis --remote-host admin@awsserver1 --remote-root /home/admin/hft_live`
  - run maker acceptance:
    - `python examples/binance_tick_mm/maker_acceptance.py --alignment-report local_live_analysis/<run_id>/alignment_report_audit_replay.json --backtest-result local_live_analysis/<run_id>/backtest_audit_replay_result.json --out local_live_analysis/<run_id>/maker_acceptance.json`
  - run top5 sidecar / join:
    - `python examples/binance_tick_mm/binance_top5_provenance.py build-sidecars --input-gz local_live_analysis/<run_id>/raw_market_data/<raw_gzip> --out-dir local_live_analysis/<run_id>/t009_fixed_sidecar --sample-id <run_id>-t009 --symbol BTCUSDT --tick-size 0.1 --buffer-size 8000000`
    - `python examples/binance_tick_mm/binance_top5_provenance.py join-decisions --audit-csv local_live_analysis/<run_id>/audit_live_<run_id>.csv --top5-csv local_live_analysis/<run_id>/t009_fixed_sidecar/top5_sidecar.csv --out-csv local_live_analysis/<run_id>/t009_fixed_sidecar/joined_decisions.csv --max-age-ms 250`
  - run Stage 5 labels:
    - `python examples/binance_tick_mm/execution_outcome_labels.py --run-dir local_live_analysis/<run_id>`
  - run Step 5C diagnostics:
    - `python examples/binance_tick_mm/quote_anchor_safety.py --run-dir local_live_analysis/<run_id> --output-dir local_live_analysis/<run_id>/stage5c_quote_anchor_safety_0526T001`
  - run Stage 6 calibration:
    - `python examples/binance_tick_mm/execution_outcome_calibration.py --run-dir local_live_analysis/<run_id> --output-dir local_live_analysis/<run_id>/stage6_execution_calibration_0526T001`
  - run Step 9B runner:
    - `python examples/binance_tick_mm/quote_adjustment_replay.py --run-dir local_live_analysis/<run_id> --output-dir local_live_analysis/<run_id>/stage9b_quote_adjustment_replay_0526T001`
  - run Step 9D fine-bucket classification for the new sample, or rerun the accepted Step 9D aggregate with the new sample included if the runner supports it:
    - `python examples/binance_tick_mm/candidate_bucket_refinement.py --help`
- Record actual wall-clock intervals between sample windows and the market-regime reason for accepting each window.

verify：
- For each accepted sample:
  - preflight passed with `git.dirty=false`, `compatibility.passed=true`, and audit field count including all 15 T006 fields
  - early audit header contains all 15 T006 fields
  - start/stop markers exist and duration is about `30min`
  - raw gzip exists and passes `gzip -t`, or recovery is documented and original preserved
  - `align_live_run.py` exits 0
  - `maker_acceptance.py` exits 0 with hard failures `[]`
  - sidecar/join output exists and reports `future_join_count=0` and `gap_crossed_join_count=0`
  - Stage 5 labels output exists
  - Step 5C diagnostics output exists
  - Stage 6 calibration output exists
  - Step 9B output exists and has `missing_t006_field_count=0`
  - Step 9D / fine-bucket classification artifacts exist for the new sample or an aggregate rerun
- Aggregate report must state:
  - total new duration
  - total new submit orders
  - total new filled orders
  - additional fills versus the `160` fill gap
  - whether primary min-move seed buckets gained enough fill evidence
  - whether any sample is caveated and why
  - whether additional collection is still needed, and which regimes to target next
- `python3 .workflow/build_dashboard.py`
- `git diff --check`

done：
- 回报必须列出 run id、start/stop time、inter-sample gaps、deployed commit、preflight results、T006 header coverage、acceptance results、sidecar/join metrics、Stage 5 counts、Step 5C key metrics、Stage 6 summary、Step 9B classification、Step 9D/fine-bucket coverage、archive sha256。
- 回报必须明确：本任务只补 current-format no-rule/default-off active control samples，不放宽 guard，不启用 candidate，不做 multi-sample parameter sweep，不授权 live/default-on/promotion。
- 回报必须说明新增样本对 `min_move_quote_age_churn_guard` 参数搜索 seed 的影响，以及是否支持进入后续 multi-sample parameter sweep design / implementation task。
- 回报必须说明是否仍不满足 `ready_for_tiny_live_design`。
```
