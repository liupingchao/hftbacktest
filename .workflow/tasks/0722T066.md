# 0722T066 PUBLIC-MULTI-DISTANCE-DYNAMIC-SEED

执行线程：
- 总控 / 业务执行线程

任务ID：
- 0722T066

标题：
- Build a source-pinned public multi-distance intensity seed

简短描述：
- 使用 Hyperliquid BTC 的真实公共 L2/trades，在多个固定距离构造明确标注为
  counterfactual/non-resting 的双边 quote exposure，拟合 arrival intensity，
  并输出可从 committed public event rows 确定性重建的 dynamic-spread seed。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- `0722T065`（已通过）

规则更新提醒：
- 只允许公共市场数据和本地离线处理。
- 不读取 credentials，不初始化 live client，不调用 private/account/order/
  cancel endpoint，不启动 service/orchestrator，不产生订单。
- Counterfactual exposure 不得宣称为 confirmed resting/fill evidence。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`

是否需要提交代码：
- 需要

提交要求：
- 先提交 runner/tests，再以该 implementation commit 运行正式 public
  collection；证据和业务回报另行提交并进入独立 QA。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- New public multi-distance seed runner and focused tests under
  `examples/hyperliquid/`
- Task-scoped evidence under
  `local_live_analysis/public_multi_distance_dynamic_seed_0722T066/`
- Workflow/report/tracking files

action：
- Collect task-scoped Hyperliquid BTC public L2/trades only.
- Persist raw normalized estimator event rows and exact collection config.
- For each complete event-time interval, create buy/sell counterfactual quote
  exposures at the fixed configured distance grid.
- Derive directional arrivals using existing estimator semantics.
- Fit both side intensity curves with the existing estimator.
- Freeze source commit, runner hash, event hash, exposure hash, config,
  thresholds and fit results into a strict seed contract.
- Add an offline fail-closed loader/validator for later explicit live wiring;
  do not wire it into the watcher live path in this task.
- Add deterministic rebuild and hostile contract/hash/schema tests.

verify：
- Focused tests and `py_compile`.
- Formal public collection has both sides:
  `>=3 observations`, `>=2 distinct distances`, intensity fit `pass`.
- Clean offline rebuild reproduces all normalized source/exposure rows, fits,
  seed hash and recommendation.
- Full Hyperliquid regression and `git diff --check`.
- Boundary proves no credentials/private/account/order/cancel/service/live.

done：
- A committed source-pinned public seed is eligible for a later explicit
  dynamic-spread live task.
- No real quote behavior changes in T066.

blockers：
- Public stream failure, insufficient distance variation, non-decaying fit,
  hash/rebuild mismatch or any forbidden endpoint boundary blocks the task.

执行结果：
- Source implementation:
  `903a3e68284942852cf30997c6fd19c960995afc`.
- awsserver1 public collection:
  `180.02364s`, `334` L2 events, `550` trades, `0` disconnects.
- Counterfactual exposure:
  `140` rows per side across `4` distances.
- Buy fit:
  `A=0.3606537`, `k=0.14777385`, status `pass`.
- Sell fit:
  `A=0.58759448`, `k=0.20059328`, status `pass`.
- Seed contract:
  `e35c7fd8f3ec8268e5d50c7963889b73409470c9f01f92ca3a0d598a96562be9`.
- Existing estimator replay:
  `snapshot_match=true`, `884` event rows, `280` exposure rows.
- Focused: `30 passed`; full Hyperliquid:
  `1297 passed, 2 skipped`.
- The terminal snapshot's complete dynamic candidate remains fallback because
  the final bucket has no toxicity observation. This does not invalidate the
  intensity seed, but a later live task must gate on a current `pass`
  candidate before submit.

提交信息：
- source implementation:
  `903a3e68284942852cf30997c6fd19c960995afc`
- evidence/workflow:
  `0705aac9991e568ce9d4c718c25db1fc8d51f63d`
- `Build public multi-distance dynamic seed`

QA验收结果：
- 状态：`已通过`。
- Source runner blob/runner SHA-256、accepted receipt identity chain 和
  remote manifest `12/12` hashes 通过。
- 从 `884` committed event rows 独立重建 `280` exposures；buy/sell 各
  `140 observations / 4 distances`。
- 独立 OLS 精确复现双侧 A/k/RMSE/confidence 与 confidence bounds。
- Seed exact `17` fields、自哈希、expected hash、strict loader 和 hostile
  tamper 通过。
- 两份 clean checkout 的 9 个 rebuild files 与 committed offline rebuild
  byte-identical；existing estimator replay `snapshot_match=true`。
- Terminal candidate 仍为
  `fallback_fixed / missing_latest_market_estimator`；T066 不证明 live
  candidate、fill/economics 或 live authorization。
- Focused：`30 passed in 0.23s`。
- Full Hyperliquid：`1297 passed, 2 skipped in 56.17s`。
- QA report：`.workflow/reports/0722T066-qa.md`。
