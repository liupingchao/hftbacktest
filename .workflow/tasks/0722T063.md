# 0722T063 T062-BASIS-CONTRACT-TRUTHFULNESS-REPAIR

执行线程：
- 总控 / 业务执行线程

任务ID：
- 0722T063

标题：
- Repair T062 frozen-contract, legacy parity and inherited-boundary truthfulness

简短描述：
- 仅修复 T062 QA 的三个 P2：严格冻结 basis contract、恢复 legacy
  side-mapping 行为、消除 inherited T061 boundary 对 T062 kernel change 的
  语义歧义。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0722T062`（未通过）

规则更新提醒：
- Offline/public/no-submit only。
- 不修改 watcher/live 默认配置，不读取 credentials，不调用 private/order/
  cancel endpoint。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交并进入独立 QA。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `examples/hyperliquid/cross_exchange_shared_signal_kernel.py`
- `examples/hyperliquid/test_cross_exchange_shared_signal_kernel.py`
- `examples/hyperliquid/cross_exchange_basis_regression_production_shadow.py`
- `examples/hyperliquid/test_cross_exchange_basis_regression_production_shadow.py`
- `local_live_analysis/cross_exchange_basis_regression_production_shadow_0722T062/`
- workflow/report/tracking files

action：
- Basis contract validator 拒绝额外/缺失字段、错误 task/source identity、
  normalization/training metadata 和不一致 row count。
- Basis-enabled kernel call 必须携带并核对 expected canonical contract hash。
- Legacy/default-off path 恢复使用 legacy contract 的 `side_mapping`，并增加
  invalid/missing mapping parity tests。
- T062 boundary 顶层明确记录 shared kernel changed；T061 boundary 仅作为
  命名清晰、task-scoped 的 source snapshot。
- 重建 official T062 artifacts，不改变数值 recommendation、decision rows
  或 warning set。

verify：
- Focused strict-contract/hash/extra-field/identity/training-row tests。
- Legacy invalid/missing side-mapping parity tests。
- Boundary truthfulness positive/negative tests。
- Official deterministic rebuild、repo-relative/no-submit gates。
- Full Hyperliquid、py_compile、diff checks。

done：
- 三项 T062 P2 均有 fail-closed regression coverage。
- Official shadow 数值结果与 warning set 不变。
- QA-ready implementation commit 和 business report。

blockers：
- 任何 forecast 数值变化、legacy valid-contract drift、live boundary 扩大或
  artifact nondeterminism 都阻塞验收。

提交信息：
- `Repair T062 basis contract truthfulness`
