```md
执行线程：
- 测试线程

任务ID：
- 0526T002

标题：
- Step 9E targeted active 1H control live test collection

简短描述：
- 基于 `0526T001` 的 clean 30min active 样本结果，采集一个 `1H` current-format no-rule / default-off active control live test 样本，用自然市场条件继续补 `min_move_quote_age_churn_guard` 相关 seed regimes 和 filled-order mass。任务只做采集、同步、归档、audit replay 和可重跑诊断，不放宽 guard、不启用 candidate、不做 tiny live 或 promotion。

状态：
- 已通过

执行顺序：
- 当前下一任务；必须在 `0526T001` QA 通过后执行

前置任务：
- `0526T001` QA 通过
- `0525T001` QA 已通过
- `0519T006` QA 已通过，T006 quote-update audit fields 已落地

规则更新提醒：
- 请先阅读规则文档后再执行本任务。
- 本任务是 1H live control sample collection，不是策略实现、参数搜索、tiny live、promotion 或 default-on 任务。
- 不允许为了增加 fills 放宽 guard、启用 quote-adjustment candidate、修改 fair/target/risk/quote 逻辑、修改生产默认值、或声明 `ready_for_tiny_live_design`。
- 采集窗口应选择 active / non-calm 时段；如果市场状态 calm，应等待更合适窗口，而不是通过参数放宽制造 fills。

按线程规则执行：
- `.workflow/workflow-kit/thread-report-template.md`
- `docs/thread-playbook.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 采集前提交/推送可部署 commit，并同步到 `awsserver1` 的 task-scoped clean worktree。
- 采集、拉回、重跑、报告完成后提交 workflow/report/artifacts。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

Allowed files：
- `.workflow/tasks/0526T002.md`
- `.workflow/reports/0526T002-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `.workflow/dashboard.html`
- `.workflow/dispatch_suggestions.md`
- `docs/qa-acceptance-report.md`
- `local_live_analysis/5-26-active-minmove-control-60min-a/**`
- `local_live_analysis/archive/5-26-active-minmove-control-60min-a.tar.gz`
- `local_live_analysis/archive/5-26-active-minmove-control-60min-a.tar.gz.sha256`
- `local_live_analysis/stage9d_candidate_bucket_refinement_0526T002_aggregate/**`

Allowed read-only inputs：
- `.workflow/reports/0526T001-business.md`
- `.workflow/reports/0526T001-qa.md`
- `local_live_analysis/5-26-active-minmove-control-30min-a/**`
- `local_live_analysis/stage9d_candidate_bucket_refinement_0526T001_aggregate/**`
- `local_live_analysis/stage9d_candidate_bucket_refinement_0525T001/**`
- `local_live_analysis/stage9c_multi_sample_validation_0521T002/**`
- `local_live_analysis/5-19-day-control-30min/**`
- `local_live_analysis/5-19-night-active-30min-a/**`
- `local_live_analysis/5-19-night-active-30min-b/**`
- `local_live_analysis/5-19-night-active-30min-c/**`
- `local_live_analysis/5-21-day-control-60min/**`
- `examples/binance_tick_mm/deploy/run_live.sh`
- `examples/binance_tick_mm/deploy/preflight_live_run.py`
- `examples/binance_tick_mm/audit_schema.py`
- `examples/binance_tick_mm/strategy_core.py`
- `examples/binance_tick_mm/live_tick_mm.py`
- `examples/binance_tick_mm/backtest_tick_mm.py`
- `examples/binance_tick_mm/align_live_run.py`
- `examples/binance_tick_mm/maker_acceptance.py`
- `examples/binance_tick_mm/binance_top5_provenance.py`
- `examples/binance_tick_mm/execution_outcome_labels.py`
- `examples/binance_tick_mm/quote_anchor_safety.py`
- `examples/binance_tick_mm/execution_outcome_calibration.py`
- `examples/binance_tick_mm/quote_adjustment_replay.py`
- `examples/binance_tick_mm/candidate_bucket_refinement.py`

Allowed remote paths：
- `awsserver1:/home/admin/hft_live/worktrees/0526T002-active-minmove-60min/**`
- `awsserver1:/home/admin/hft_live/runs/5-26-active-minmove-control-60min-a/**`
- `awsserver1:/home/admin/hft_live/config/config_live.toml`
- `awsserver1:/home/admin/hft_live/config/binancefutures.toml`

Forbidden files / areas：
- Strategy rule logic, fair/target formula, quote behavior, production defaults, live production configs beyond task-scoped run dirs.
- Core/connector APIs, canonical data conversion, standard hftbacktest npz schema.
- Any candidate enablement, default-on behavior, guard relaxation, Step 5C promotion, inventory-control implementation, queue/touch repair, runner modification, parameter sweep implementation, tiny live, or promotion claim.

Requires separate task：
- Multi-sample parameter sweep implementation.
- Promoting any candidate to live or production.
- Running a tiny live candidate test.
- Changing strategy behavior or production defaults.
- Generalizing or fixing `align_live_run.py` remote raw path behavior; this task may use a task-scoped remote symlink if needed.

Fatal Failures：
- Starting before `0526T001` QA passes.
- Enabling any non-baseline quote-adjustment candidate.
- Relaxing quote throttle, min-move, latency, post-only, stale, inventory, or risk guards to manufacture fills.
- Continuing the sample after early audit header check shows missing T006 fields.
- Accepting raw gzip corruption without documented repair and preserved original.
- Accepting sidecar/join quality with `future_join_count > 0` or `gap_crossed_join_count > 0` as clean input.
- Interpreting the 1H control sample as live strategy promotion proof or `ready_for_tiny_live_design`.

dataset：
- Run id: `5-26-active-minmove-control-60min-a`
- Duration target: about `60min`
- Target window: active / non-calm market conditions
- Fill target: natural fills, ideally `>=80`; if not reached, report exact residual gap and missing regimes.

live-test parameter policy：
- Allowed live-test parameter changes:
  - `run_id_prefix = "5-26-active-minmove-control-60min-a"`
  - `audit_csv = "/home/admin/hft_live/runs/5-26-active-minmove-control-60min-a/output/audit_live_5-26-active-minmove-control-60min-a.csv"`
  - `[paths].output_root = "/home/admin/hft_live/runs/5-26-active-minmove-control-60min-a/output"`
  - `[live_safety].connector_config = "/home/admin/hft_live/runs/5-26-active-minmove-control-60min-a/binancefutures.toml"`
  - remote stopper sleep seconds from `1800` to `3600`
  - task-scoped run dir and log/raw/output paths
- Disallowed live-test parameter changes:
  - `quote_throttle_enabled`
  - `min_quote_update_interval_ms`
  - `min_quote_move_ticks`
  - `two_phase_replace_enabled`
  - `latency_guard_ms`
  - `api_limit.*`
  - `risk.*`
  - `fair.*`
  - `impact.*`
  - `order_notional`
  - `max_position_qty`
  - any candidate or default-on control
- Baseline config source should be the accepted current-format no-rule/default-off config from `5-26-active-minmove-control-30min-a` or, if unavailable, `5-21-day-control-60min`; the report must state which source was copied and show the diff only contains allowed run-path / duration changes.

AWS sync / deploy action：
- Push the local commit containing this task to origin.
- On `awsserver1`, create or update clean detached worktree:
  - `/home/admin/hft_live/worktrees/0526T002-active-minmove-60min`
- Checkout the pushed commit in that worktree.
- Ensure the shared release binaries are available, using the existing accepted pattern:
  - `target -> /home/admin/hft_live/hftbacktest/target`
- Confirm no existing `tmux` live session or live collector/connector/bot process is running before start.
- Prepare remote run dir:
  - `/home/admin/hft_live/runs/5-26-active-minmove-control-60min-a/`
- Copy baseline config into remote run dir and apply only the allowed path/run-id changes above.
- If `align_live_run.py` requires remote `/data/*.gz`, create task-scoped symlink:
  - `data -> raw_market_data`
  after collection or before local pull; do not move or delete raw files.

action：
- Run focused local preflight checks before deploy:
  - `python -m pytest examples/binance_tick_mm/test_deploy_preflight.py`
  - `python -m pytest examples/binance_tick_mm/test_quote_adjustment_replay.py`
  - `python examples/binance_tick_mm/deploy/preflight_live_run.py --help`
  - `python examples/binance_tick_mm/align_live_run.py --help`
  - `python examples/binance_tick_mm/maker_acceptance.py --help`
  - `python examples/binance_tick_mm/quote_adjustment_replay.py --help`
  - `python examples/binance_tick_mm/candidate_bucket_refinement.py --help`
  - `bash -n examples/binance_tick_mm/deploy/run_live.sh`
- Start one no-rule / default-off control live run for about `60min`.
- Early-check live audit header for all 15 T006 fields.
- Stop cleanly through a `3600s` stopper and preserve deployment manifest, start/stop markers, audit, raw gzip, connector logs, collector logs, and bot logs.
- Pull artifacts locally and run:
  - `align_live_run.py`
  - `maker_acceptance.py` with market-view sidecar inputs
  - T009 sidecar build and decision join
  - Stage 5 labels
  - canonical Step 5C diagnostics directory `stage5c_quote_anchor_safety_0518T004`
  - task-scoped Step 5C diagnostics directory `stage5c_quote_anchor_safety_0526T002`
  - Stage 6 calibration
  - Step 9B runner
  - Step 9D single-sample classification
  - 5+1+1 aggregate Step 9D rerun including all accepted current-format samples and `5-26-active-minmove-control-60min-a`
- Archive local run dir and write sha256.

scenario focus：
- Primary target: natural fills in `min_move_quote_age_churn_guard` seed regimes.
- Prefer windows likely to cover:
  - young quote churn
  - API/churn normal and churn pressure
  - stale latency medium/high
  - one-tick tight spread
  - medium/high volatility / markout dispersion
  - large inventory skew / low inventory score
- Do not target `inventory_reservation_shift_band`.
- Treat `size_reduction_or_add_side_suppression_pressure` only as secondary observation for `volatility_high`.

verify：
- Preflight passed with `git.dirty=false`, `compatibility.passed=true`, and audit field count including all 15 T006 fields.
- Early audit header contains all 15 T006 fields.
- Start/stop markers exist and duration is about `60min`; stop marker exit code is `0`.
- Raw gzip exists and passes `gzip -t`, or recovery is documented and original preserved.
- `align_live_run.py` exits 0.
- `maker_acceptance.py` exits 0 with hard failures `[]`.
- T009 sidecar/join output exists and reports:
  - `future_join_count=0`
  - `gap_crossed_join_count=0`
  - `join_missing_count=0`
  - top5 tick/qty match above accepted thresholds
- Stage 5 labels output exists and reports submit/fill/fill-after-cancel counts.
- Step 5C diagnostics output exists and reports `post_only_risk_after_recheck_rows=0`.
- Stage 6 calibration output exists.
- Step 9B output exists and has `missing_t006_field_count=0`.
- Step 9D single-sample and aggregate artifacts exist.
- Aggregate report must state:
  - new filled orders
  - residual fill gap versus `500`
  - whether `min_move_quote_age_churn_guard` seed buckets remain stable
  - whether any sample is caveated and why
  - whether more collection is still needed, and which regimes to target next
- `python3 .workflow/build_dashboard.py`
- `git diff --check`

done：
- 回报必须列出 run id、start/stop time、deployed commit、remote worktree path、baseline config source、allowed config diff、preflight results、T006 header coverage、acceptance results、sidecar/join metrics、Stage 5 counts、Step 5C key metrics、Stage 6 summary、Step 9B classification、Step 9D/fine-bucket coverage、archive sha256。
- 回报必须明确：本任务只补 current-format no-rule/default-off active control 1H sample，不放宽 guard，不启用 candidate，不做 parameter sweep，不授权 live/default-on/promotion。
- 回报必须说明新增 1H 样本是否补足或显著缩小 `114` residual fill gap。
- 回报必须说明是否仍不满足 `ready_for_tiny_live_design`。
```
