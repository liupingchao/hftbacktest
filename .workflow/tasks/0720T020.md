# 0720T020 FINAL-POSITION-SNAPSHOT-VALIDITY-PROPAGATION

执行线程：
- 总控 auto-loop / 业务执行线程

任务ID：
- 0720T020

标题：
- Propagate semantic final position-snapshot validity into blocker and operator status

简短描述：
- Close the single T019 QA P1 without changing order reconciliation or trading behavior。
- The manager reconciliation result, not structural `dict(...)` conversion, is authoritative for final position evidence。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0720T019`（未通过；order/working reconciliation accepted, position validity propagation remains open）

规则更新提醒：
- 本任务 offline-only。
- No live/private/account/order/cancel/network/remote/service action is authorized。
- New bounded live、Task 8 and adaptive/multi-level activation remain locked。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `.workflow/reports/0720T019-qa.md`
- `docs/superpowers/plans/2026-07-17-principal-alignment-p0-p3.md`

是否需要提交代码：
- 是

提交要求：
- 实现和测试提交。
- 业务报告单独提交。
- 回报必须带 implementation commit id 和提交信息。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

source：
- Branch: `cross-exchange`
- Dispatch base: `0822631`

files：
- `examples/hyperliquid/hyperliquid_maker_order_manager.py`
- `examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- Focused watcher/manager tests
- `.workflow/tasks/0720T020.md`
- `.workflow/reports/0720T020-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

action：
- Capture the dict returned by `reconcile_supplied_snapshot()` and treat `position_snapshot_status` as the authoritative semantic result。
- Add `final_position_snapshot_unavailable` whenever that result is not exactly `pass`, including `{}`、`assetPositions=None/object` and other invalid shapes。
- Preserve valid final open-order reconciliation and working exposure even when position evidence fails。
- Make final operator status explicitly expose position snapshot status/reason and prevent an unverified default/stale numeric position from appearing as verified。
- Preserve valid empty `assetPositions=[]` as a verified zero-position snapshot。
- Add adversarial watcher tests for invalid shapes, valid empty positions and valid empty/open-order combinations。

verify：
- Focused watcher/manager/fill/acceptance tests pass。
- Full `python -m pytest -q -p no:cacheprovider examples/hyperliquid` passes。
- Modified modules/tests pass `py_compile`。
- Acceptance CLI `--help` passes。
- Invalid semantic user-state shapes retain known orders/working exposure, emit a position blocker, expose fail-closed status/reason and do not expose an unverified numeric final position。
- Valid `assetPositions=[]` exposes verified zero without a position blocker。
- T019 accepted open-order/cancel semantics and T017/T018/T016 regressions remain green。
- Exact T016 replay remains byte-preserving, decision `43/43` pass and lifecycle blocked for historical attempt 2。
- `git diff --check` and implementation `git show --check` pass。
- No live/private/account/order/cancel/network/remote/service action occurs。

done：
- Final operator status distinguishes verified position from unavailable/invalid position evidence。
- Semantic manager reconciliation result controls final position blocker/status。
- Known final open-order facts remain independent and visible。
- Independent QA accepts T020 before any new bounded live task。

stop conditions：
- Structural conversion can still override semantic position failure。
- Invalid final user-state can omit the position blocker or expose an unverified numeric position as verified。
- Valid empty `assetPositions=[]` is incorrectly rejected。
- Known final open orders or working exposure are discarded。
- T017-T019 accepted behavior regresses, historical T016 is upgraded, or strategy/risk/activation changes。
- Any verification requires live/private/account/order/cancel/network/remote/service。

blockers：
- Independent QA failure keeps new bounded live、Task 8 and adaptive/multi-level locked。

commit：
- implementation `9a332c13e3a00d496e50c9ff73bc77b1962cec96`

提交信息：
- `Propagate final position snapshot validity`
