# 任务派发

执行线程：
- SKHYNIX Fixed Causal Epoch M-State V2 A-1 业务线程

任务ID：
- 0829T003

标题：
- 注册并执行 fixed causal epoch deterministic thinning A-1

简短描述：
- 保留已验收的 fresh-channel M-state，不放宽方向阈值；以绝对 60s
  causal epoch、中央 30s eligible core、每方向最早 onset 和固定 epoch
  cluster 替换路径依赖的 renewal refractory/cluster。

状态：
- 执行中

执行顺序：
- 当前唯一 formal task

前置任务：
- 0829T002（QA 已通过；科学分类
  `Aminus1_mstate_integrity_failed`）

规则更新提醒：
- 已阅读 AGENTS.md、workflow-kit、task_plan.md、progress.md、findings.md
  和 0829T002 plan/execution/QA。

按线程规则执行：
- AGENTS.md
- .workflow/workflow-kit/workflow-manual.md

是否需要提交代码：
- 需要

提交要求：
- 先提交 candidate plan。
- 独立 plan review 达到 `P0/P1/P2/P3=0/0/0/0` 前禁止运行 29-cache。
- 实现、执行、remediation 和 QA 分别形成 Git 历史。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- docs/skhynix_binance_precision_first_fixed_causal_epoch_mstate_v2_a_minus1_audit_plan_20260829.md
- examples/hyperliquid/skhynix_fixed_causal_epoch_mstate_a_minus1.py
- examples/hyperliquid/test_skhynix_fixed_causal_epoch_mstate_a_minus1.py
- local_live_analysis/skhynix_fixed_causal_epoch_mstate_a_minus1_0829T003/
- .workflow/tasks/0829T003.md
- .workflow/reports/0829T003-*.md

action：
- 冻结绝对 epoch、eligible core、deterministic thinning、固定 cluster、
  null、exposure、selection、gates 和 outcome lock。
- 独立审查合同；通过后执行完整 outcome-blind 29-cache A-1。

Plan review history：
- Round 1：`P0/P1/P2/P3=0/6/4/0`，未通过。
- Revision 2 将任何非完整、非单 segment 的 60s epoch 整体设为
  outcome-blind ineligible；cluster 固定为 `(capture_id, epoch_id)`。
- Revision 2 同时冻结 artificial-slice comparison epoch、直接 null
  authority、conditional H0、A-1-2/A-1-3/A-1-4 条件、25 项输出闭包、
  epoch/candidate/slice schemas 和 historical-only claim limit。
- Round 2：`P0/P1/P2/P3=0/6/4/0`，未通过。
- Revision 3 将 epoch universe/disposition 写为唯一 `if/elif` 谓词；
  slice 必须从 sliced raw cache direct-call `build_features`；冻结 exact
  M-state support tuple/hash、非真空 coverage、逐指标 null semantics、
  support-conditioned occupancy、sequential `NOT_EVALUATED` 和 schema
  sentinel/canonicalization。
- 29-cache execution lock 继续保持，直至独立复审为 `0/0/0/0`。

verify：
- Plan review。
- Hostile tests、静态检查。
- Canonical Build A、fresh Build B、三阶段 determinism。
- 独立最终 QA。

done：
- 给出唯一科学分类和下一权限边界。

明确禁止：
- 不放宽 `0.50/0.25` directional thresholds。
- 不使用 2-of-3 quorum。
- 不把 UNKNOWN 当 BACKGROUND。
- 不根据 observed count 选择 epoch geometry 或 filter。
- 不读取 future price、target、fill、fee 或 PnL。
- 不运行 live/private/order API。
