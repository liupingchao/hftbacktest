```md
执行线程：
- 业务线程-research

任务ID：
- 0617T002

标题：
- awsserver1 cross-exchange python3 preflight repeat / artifact pullback

简短描述：
- 在 `0617T001` QA 通过后，复跑一次基于 `/home/admin/hftbacktest-cross-exchange` 的 dry-run preflight，确认新路径、系统 `python3`、artifact pullback 和 no-order boundary 可重复稳定执行。本任务不读取凭证、不调用私有接口、不查询账号、不下单、不撤单、不启动 live bot。

状态：
- 待执行

执行顺序：
- 当前唯一任务

前置任务：
- `0617T001` QA 必须为 `已通过`。

规则更新提醒：
- 请先阅读 `.workflow/workflow-kit/workflow-manual.md`、`.workflow/workflow-kit/task-dispatch-template.md`、`.workflow/workflow-kit/thread-report-template.md`、`.workflow/workflow-kit/qa-acceptance-template.md`、`task_plan.md`、`progress.md`、`findings.md`、`docs/hyperliquid_tiny_live_0616T006_T008_auto_loop.md` 后再执行本任务。
- 本任务只做 cross-exchange preflight repeat，不是 live execution。

按线程规则执行：
- `AGENTS.md`
- `docs/hyperliquid_tiny_live_0616T006_T008_auto_loop.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0617T002.md`
- `.workflow/reports/0617T002-business.md`
- `docs/hyperliquid_tiny_live_0616T006_T008_auto_loop.md`
- `local_live_analysis/hyperliquid_awsserver1_cross_exchange_python3_preflight_0617T002/**`
- `task_plan.md`
- `progress.md`
- `findings.md`

action：
- Reuse the separate remote checkout at `/home/admin/hftbacktest-cross-exchange`.
- Reconfirm branch, commit, and clean status.
- Reconfirm remote system `python3` path and version.
- Create fresh dry-run evidence files with no secrets and no private/account/order data.
- Pull artifacts back locally and validate them with `/home/liushuai/workspace/hftbacktest/.conda-envs/hft-py38/bin/python`.
- Fail closed on host mismatch, branch mismatch, commit mismatch, dirty worktree, missing Python, missing dry-run marker, any private/order/account/live claim, or missing no-order boundary.

acceptance criteria：
- The remote cross-exchange checkout is still present and clean.
- The remote selected Python is still `/usr/bin/python3` and the version is recorded.
- Artifact pullback and local validation pass again.
- All artifacts preserve no-private/no-credential/no-account/no-order/no-cancel/no-live-bot boundary.
- Final recommendation must be exactly one of:
  - `hyperliquid_awsserver1_cross_exchange_python3_preflight_repeat_ready_for_qa`
  - `hyperliquid_awsserver1_cross_exchange_python3_preflight_repeat_needs_revision`
  - `hyperliquid_awsserver1_cross_exchange_python3_preflight_repeat_blocked`

verify：
- Remote branch / commit / dirty count check.
- Remote `python3 --version` and selected path check.
- Pullback checksum verification.
- Local JSON validation with `/home/liushuai/workspace/hftbacktest/.conda-envs/hft-py38/bin/python -m json.tool <artifact>.json`.
- `git diff --check`

done：
- 回报复跑后的 branch/commit/dirty 状态、python3 路径/版本、pullback method、本地验证结果。
- 明确说明没有读取凭证、没有调用私有接口、没有账号查询、没有下单/撤单/改单、没有 live bot。
```
