# 任务派发

执行线程：
- 业务线程-python/cross-exchange-research

任务ID：
- 0803T002

标题：
- AUG03-LIQUIDITY-RESPONSE-CASE-HIERARCHY-DIAGNOSTIC-REPLAY

简短描述：
- 按 QA 已通过的 7 月 30 日 v2 框架，在 8 月 3 日 SKHYNIX 十段数据上
  重放 ShockAtom、ShockCluster / ContinuousFlowEpisode、Residual Motif /
  Prototype 和 TemporaryRegime。
- 由于 8 月 3 日 campaign R1 未通过，所有新产物必须明确标记为
  diagnostic、`formal_eligible=false`，不得升级为正式 signal 或 arbitrage
  证据。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- `0801T006-0801T009` QA `已通过`，提供冻结 v2 方法。
- `0803T001` 已完成 R0 和 R1 诊断包；R1
  `reconciliation_pass=false`、`passes=false`。

规则更新提醒：
- 无

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `docs/hyperliquid_liquidity_response_case_hierarchy_plan.md`

是否需要提交代码：
- 不需要

提交要求：
- 当前工作区已有未提交前置工作，不创建混合提交。
- 回报写 `commit：无`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 独立复核方法冻结、输入 provenance、diagnostic 边界传播、确定性和统计结果。

files：
- `examples/hyperliquid/cross_exchange_liquidity_response_episodes.py`
- `examples/hyperliquid/cross_exchange_liquidity_response_case_hierarchy.py`
- `examples/hyperliquid/cross_exchange_liquidity_response_baseline_v2.py`
- `examples/hyperliquid/cross_exchange_liquidity_response_motif_v2.py`
- `examples/hyperliquid/cross_exchange_liquidity_response_regime_v2.py`
- 新增 Aug03 diagnostic replay runner 和 focused tests。
- `local_live_analysis/skhynix_liquidity_response_0803T002/`
- `local_live_analysis/skhynix_liquidity_response_case_hierarchy_0803T002/`

action：
- 使用 8 月 3 日 R0/R1 文件生成独立 diagnostic M1；保留 exact masks、
  missing outcomes 和所有 source SHA，不伪造 R1 accepted 状态。
- 使用与 7 月 30 日一致的 primary boundary、phase、baseline、motif、
  surrogate 和 regime 方法。
- discovery 固定为 `segment_0001-0003`；evaluation 固定为
  `segment_0004-0010`。
- 每层 manifest 传播 source alignment failure 和
  `formal_eligible=false`。
- 不修改 7 月 30 日任何历史产物。

verify：
- focused pytest、py_compile、CLI help 和 `git diff --check`。
- 真实十段 M1、atom、episode-v2、baseline-v2、motif-v2、regime-v2 build。
- 独立复核 row count/SHA、segment conservation、无跨段、feature leakage、
  motif/prototype 分类和 regime surrogate。

done：
- 完整发布 8 月 3 日 diagnostic hierarchy。
- 明确区分结构复现结果与正式 signal/arbitrage 资格。

hard boundary：
- 不访问网络、AWS、SSH，不新增采集。
- 不放宽或改写 8 月 3 日 R1 结果。
- 不覆盖 7 月 30 日 hierarchy 和 M1 package。
- 不声明 tradable signal、exact fill、maker identity 或 maker PnL。
