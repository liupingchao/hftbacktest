```md
执行线程：
- 业务线程-python

任务ID：
- 0514T006

标题：
- Stage 6A fill/cancel lifecycle proxy calibration plan

简短描述：
- 基于 `0514T005` 的 read-only execution outcome findings，制定 Stage 6 的 planning-only 合同：明确为什么把 Stage 6 从宽泛的 queue-model 语言微调为 replay/live fill-cancel lifecycle proxy calibration，并定义 labels、strata、acceptance metrics、样本要求、产物格式和后续实现任务边界。本任务只做计划，不实现 runner、不跑新 replay、不启动 live、不改策略。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- 0514T005 QA 已通过

规则更新提醒：
- Stage 6 现在允许进入 planning-only 细化，但不得把本任务扩展为 calibration runner 实现、策略改动、live 采集或 replay 实验。

按线程规则执行：
- .workflow/workflow-kit/thread-report-template.md
- docs/thread-playbook.md
- task_plan.md
- progress.md
- findings.md

是否需要提交代码：
- 需要

提交要求：
- 自测/检查通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

Allowed files：
- `.workflow/tasks/0514T006.md`
- `.workflow/reports/0514T006-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `.workflow/dashboard.html`
- `.workflow/dispatch_suggestions.md`
- `docs/qa-acceptance-report.md`

Allowed read-only inputs：
- `.workflow/tasks/0514T005.md`
- `.workflow/reports/0514T005-business.md`
- `.workflow/reports/0514T005-qa.md`
- `local_live_analysis/5-13-day-control-30min/stage5_execution_outcome_labels_0514T005/**`
- `local_live_analysis/5-13-day-control-30min/t009_fixed_sidecar/**`
- `local_live_analysis/5-13-day-control-30min/maker_acceptance_stage3.json`
- `docs/stage6i-cancel-requested-fill-risk-plan.md`

Forbidden files / areas：
- `examples/binance_tick_mm/*.py`
- `local_live_analysis/**` generated outputs beyond reading existing artifacts
- strategy behavior, fair/target formulas, configs, risk guards, quote placement behavior
- live scripts, AWS/remote state, live collection
- core Rust, connector, py `event_dtype`, standard npz schema

Requires separate task：
- 实现 Stage 6 calibration runner 或测试。
- 运行新的 replay sweep、Stage 6J matrix 或 live 采集。
- 把任何 calibration 结论接入策略或默认参数。
- 做 exact queue / MBO queue-position 建模。
- 启动 live 或 live micro test。

Fatal Failures：
- 把 planning-only 任务扩展为代码实现或实验执行。
- 把 T005 observed-only labels 解释成 exact queue proof、counterfactual fill proof 或策略 PnL 证明。
- 仅凭 `5-13-day-control-30min` 单样本就授权 quote-adjustment promotion 或 live。
- 忽略 T005 已经确认的 observed-only proxy / low-sample 边界。

action：
- 明确 Stage 6 的 refined objective：
  - 不是 exact queue model calibration。
  - 而是 replay/live fill-cancel lifecycle proxy calibration，使 quote-adjustment PnL 研究的 fill-side假设更可信。
- 明确为什么要微调 Stage 6：
  - T005 显示当前样本以 high-cancel / low-fill / fill-after-cancel-race / fast-cancel-churn 为主。
  - `queue_priority_proxy`、`missed_fill_opportunity_cost`、`realized_pnl_decomposition` 仍是 observed-only proxy。
  - `tail_risk` 受 `53` 个 filled orders 限制，partial-fill 在当前样本中为 `0`。
- 定义 Stage 6 应复用的 common labels：
  - fill by `100ms / 500ms / 1s / 5s`
  - time-to-fill
  - final order state / lifecycle
  - fill-after-cancel-request
  - cancel-to-fill delay
  - fast-cancel-churn
  - fill markout / spread-capture retention
  - inventory transition / time-to-flat
  - coverage / censoring flags
- 定义必须分层的 calibration strata：
  - `placement_bucket`
  - `distance_to_bbo_ticks`
  - `edge_vs_fair_ticks`
  - `inventory_score`
  - top-of-book / top5 size proxy
  - join-age / stale-age proxy
  - latency regime
- 定义 acceptance metrics：
  - live vs replay 的 fill-horizon absolute gap / relative gap
  - time-to-fill 分布差异
  - final-state 分布差异
  - cancel-race 指标差异
  - strata-level calibration gap
  - coverage / censoring completeness
- 明确样本策略：
  - `5-13-day-control-30min` 可用于单样本方法论验证。
  - 是否足以给 quote-adjustment PnL experiment 放行，必须在计划中单独判断。
  - 多样本 acceptance 与 promotion 结论需要后续 current-format 样本支持。
- 定义后续 `0514T007` implementation 任务边界、建议输出、验证口径和 non-goals。

verify：
- 人工检查 `.workflow/tasks/0514T006.md` 和 `.workflow/reports/0514T006-business.md` 是否完整。
- `python3 .workflow/build_dashboard.py`
- `git diff --check`

done：
- 回报必须说明为什么 Stage 6 要从宽泛 queue-model 语言微调为 fill/cancel lifecycle proxy calibration。
- 回报必须明确 Stage 6 的 common labels、分层维度、acceptance metrics、样本要求、输出文件和 `0514T007` 边界。
- 回报必须明确本任务未实现代码、未运行 replay、未启动 live、未改策略、未改 core/connector/schema。
- 回报必须写 `commit id` 和提交信息。
```
