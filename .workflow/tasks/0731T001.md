# 任务派发

执行线程：
- 业务线程-repository-alignment

任务ID：
- 0731T001

标题：
- MACMINI-REPOSITORY-WORKTREE-ALIGNMENT

简短描述：
- 以 `macmini-tunnel:/Users/liu/Documents/hftbacktest` 为来源，将其
  `cross-exchange` 提交、暂存改动、未暂存改动和未跟踪项目文件同步到
  当前仓库。
- 保留双端可恢复快照，不传输或删除 `local_live_analysis` 等 ignored
  数据和构建产物。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- 无

规则更新提醒：
- 无

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`

是否需要提交代码：
- 不需要

提交要求：
- 不需要提交
- 回报写 `commit：无`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- 当前仓库 Git 工作树
- `macmini-tunnel:/Users/liu/Documents/hftbacktest`
- `/tmp/hftbacktest-sync-20260731-*` 恢复快照
- `.workflow/tasks/0731T001.md`
- `.workflow/reports/0731T001-business.md`
- `.workflow/reports/0731T001-qa.md`

action：
- 固定两边 HEAD、分支和 Git 状态。
- 为当前干净基线创建备份分支。
- 从 Mac 导出其额外提交、staged patch、unstaged patch 和 untracked
  archive。
- 在当前仓库按提交、索引、工作树、未跟踪文件的顺序恢复 Mac 状态。
- 按用户收缩后的范围，只同步 Git 可见代码、关键文档及工作流文件；
  不同步 `local_live_analysis`、`target`、缓存、秘密文件或其他 ignored
  数据。

verify：
- 两边 `HEAD` 一致。
- 两边 `git status --porcelain=v1 -z` 一致。
- 两边 Git-visible 文件内容、模式和 staged/unstaged 边界一致。
- Mac 的 Git-visible 未跟踪文件逐文件 SHA-256 与本地一致。

done：
- 当前仓库可继续 Mac 上的未提交工作。
- 恢复路径和未同步边界有明确记录。
- `local_live_analysis` 等 ignored 数据明确留在 Mac，不复制到当前机器。
