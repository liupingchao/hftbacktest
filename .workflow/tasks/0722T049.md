# 0722T049 BOUNDED-FILL-FEEDBACK-ACTIVATION

执行线程：
- 总控 auto-loop / 业务执行线程

任务ID：
- 0722T049

标题：
- Enable bounded exposure-weighted fill feedback as the only adaptive quote input

简短描述：
- 在 T047/T048 accepted evidence contract 上增加独立 fill-feedback activation path。
- fill feedback 只允许通过真实 eligible lifecycle、外部 target 配置和 bounded candidate 改变 half-spread；否则 neutral fixed fallback。

状态：
- 阻塞

执行顺序：
- 当前唯一任务

前置任务：
- `0721T047`、`0722T048`（均已通过）

规则更新提醒：
- 本任务允许一次 exact-envelope single-window tiny-live；必须先完成 source、execution-safety、evidence-integrity 和 same-window replay gate。
- live 是主要数据来源；target fill ratio 不得硬编码，未有合法 target 或 eligible exposure 时必须保持 neutral。
- dynamic spread、inventory skew、multi-level 和其它报价变量在本任务关闭；不得与 fill feedback 同时改变报价。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `AGENTS.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交。
- 回报必须带 `commit id` 和提交信息。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `examples/hyperliquid/cross_exchange_online_estimators.py`
- `examples/hyperliquid/cross_exchange_shared_signal_kernel.py`
- `examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- `examples/hyperliquid/cross_exchange_live_remote_orchestrator.py`
- `examples/hyperliquid/cross_exchange_t024_same_window_acceptance.py`
- focused fill-feedback/orchestrator/watcher/acceptance tests
- workflow task/business/QA/fixed QA records

action：
- 为 fill-feedback controller 增加显式 activation boundary；observe-only 默认行为保持。
- 增加 bounded fill-feedback pricing overlay：仅 candidate `pass`、activation enabled、finite、bounded 时应用 offset；否则 fixed base 并记录 reason。
- 增加 exact profile `two-sided-fill-feedback-manager` 和显式 `--enable-fill-feedback`。
- target fill ratio 只从 task config/live evidence 传入；默认 absent 时 fail closed/neutral，不制造 target。
- 保留 kill-switch、risk、post-only、two-sided manager、single-level 和 submission caps 优先级。
- 只执行一个 exact-envelope live window；dynamic spread、inventory skew、multi-level 全部关闭。
- 完成 same-window acceptance、estimator/fill-feedback replay、checksum 和独立 post-live account proof。

verify：
- focused controller tests：activation/fallback/bounds/rate-limit/hysteresis/anti-windup/restart checksum。
- focused quote overlay、orchestrator profile/flag 和 acceptance tests。
- full `python -m pytest examples/hyperliquid -q`、`py_compile`、`git diff --check`。
- one live window: `1800s` cap, `0.005 BTC/order`, `0.01 BTC position`, `1 USDC loss`, `2 submissions`, `2` manager attempts, `3s hold`, `10s wait`, `Alo`, fast L2。
- live artifact must prove fill feedback requested, target provenance, candidate status/fallback, dynamic/inventory/levels off, and no unsupported economics claim.

done：
- QA-ready implementation commit and business report。
- Live evidence distinguishes activation request from actual quote behavior change。
- No target is inferred from zero fills; no stable PnL/fill-rate/maker viability/promotion/multi-level claim。

blockers：
- Any failed source, execution, evidence, account-state, checksum or same-window gate stops the task without a second window。

提交信息：
- `Enable bounded fill-feedback activation`
