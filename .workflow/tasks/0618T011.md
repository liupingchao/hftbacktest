```md
执行线程：
- 业务线程-research

任务ID：
- 0618T011

标题：
- M2 no-fill diagnosis and fill-acquisition design decision

简短描述：
- 对 `0618T009` / `0618T010` 的 no-fill 结果做离线诊断，量化队列位置 proxy、盘口深度、size、side、时段、quote 位置和成交概率证据缺口。
- 本任务不继续盲跑 live retry；输出下一步设计决策，决定是否需要只读采集、策略参数调整、或停止当前 M2 fill-acquisition 路线。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0618T010` QA 为 `阻塞`。

规则更新提醒：
- 请先阅读 `.workflow/workflow-kit/workflow-manual.md`、`.workflow/workflow-kit/task-dispatch-template.md`、`.workflow/workflow-kit/thread-report-template.md`、`.workflow/workflow-kit/qa-acceptance-template.md`、`task_plan.md`、`progress.md`、`findings.md` 后再执行本任务。

按线程规则执行：
- `AGENTS.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0618T011.md`
- `.workflow/reports/0618T011-business.md`
- `.workflow/reports/0618T011-qa.md`
- `docs/qa-acceptance-report.md`
- `examples/hyperliquid/hyperliquid_tiny_live_m2_no_fill_diagnosis.py`
- `examples/hyperliquid/test_hyperliquid_tiny_live_m2_no_fill_diagnosis.py`
- `local_live_analysis/hyperliquid_tiny_live_m2_no_fill_diagnosis_0618T011/**`
- `findings.md`
- `progress.md`
- `task_plan.md`

hard boundary：
- No live order.
- No remote checkout refresh.
- No final gate rerun.
- No credential read.
- No private/account/order endpoint call.
- No taker/crossing recommendation.
- No cap relaxation recommendation.
- No stable PnL or maker viability claim.

action：
- Implement a no-network diagnostic runner that consumes local T009/T010 pulled-back artifacts only.
- Extract and summarize:
  - per-window / per-attempt quote side, size, quote price, BBO, spread, quote position, same-side top depth, top-level order count, top5 depth, notional, and pre/post mid movement where available;
  - no-fill facts: `resting` status, `fill_count=0`, final open orders 0, T008 ledger fail-closed;
  - evidence gaps: exact queue priority unavailable, trades-through unavailable, per-attempt post-L2 unavailable for T010, fill probability not identifiable from the current artifacts.
- Produce artifacts:
  - `no_fill_diagnosis_manifest.json`
  - `quote_attempt_diagnostics.csv`
  - `window_depth_summary.csv`
  - `evidence_gap_matrix.csv`
  - `design_decision_matrix.csv`
  - `README.md`
- Make a controller-facing design decision:
  - either allow a later separately scoped read-only public collector task;
  - or allow a later separately scoped maker-only retry only after better evidence;
  - or stop/rework M2 fill acquisition.

acceptance criteria：
- The task must be fully no-network and no-live.
- It must not treat public L2 as exact queue position proof.
- It must clearly separate observed facts, proxy inference, and unproven areas.
- It must explain why another blind same-caps retry is or is not justified.
- QA must verify artifacts, focused tests, JSON validity, redaction/boundary scan, and `git diff --check`.

verify：
- `python -m pytest examples/hyperliquid/test_hyperliquid_tiny_live_m2_no_fill_diagnosis.py examples/hyperliquid/test_hyperliquid_tiny_live_m2_fill_loop.py examples/hyperliquid/test_hyperliquid_tiny_live_m2_pnl_ledger.py -q`
- `python -m py_compile examples/hyperliquid/hyperliquid_tiny_live_m2_no_fill_diagnosis.py`
- `python examples/hyperliquid/hyperliquid_tiny_live_m2_no_fill_diagnosis.py --help`
- `python examples/hyperliquid/hyperliquid_tiny_live_m2_no_fill_diagnosis.py --output-dir local_live_analysis/hyperliquid_tiny_live_m2_no_fill_diagnosis_0618T011`
- JSON validation for the manifest.
- Artifact non-empty check.
- Boundary/redaction scan.
- `git diff --check`

done：
- 回报诊断结论、artifact path、设计决策、验证命令、blockers、commit id、提交信息。
- 明确说明 M2 是否仍 blocked，以及下一步是否应先做 read-only public collector。
```
