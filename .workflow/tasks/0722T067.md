# 0722T067 EXACT-SEEDED-DYNAMIC-WIRING-AND-STRICT-SHADOW

执行线程：
- 总控 / 业务执行线程

任务ID：
- 0722T067

标题：
- Wire the exact public dynamic seed into the production quote path

简短描述：
- 将 `0722T066` 已验收的 exact dynamic-spread seed 显式接入
  event-driven manager 路径，并增加 submit 前 fail-closed gate。
- 使用 committed public event rows 运行 no-submit production-equivalent
  shadow，证明 current candidate 为 `pass` 且最终 tick-rounded bid/ask
  相对固定 spread 确实变化。

状态：
- 执行中

执行顺序：
- 当前唯一任务

前置任务：
- `0722T066`（已通过）

规则更新提醒：
- 本任务不读取 credentials，不初始化 live client，不调用 private/account/
  order/cancel endpoint，不启动 service/orchestrator，不产生真实订单。
- T066 seed 只预载 counterfactual exposure rows；当前 public events 必须继续
  独立生成 volatility、liquidity 和 toxicity。
- Same-sample public shadow 只证明生产机制可达，不证明 OOS 稳定性、fill 或
  economics。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交 implementation 与 deterministic shadow evidence。
- 业务回报必须带 commit id 和提交信息。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- `examples/hyperliquid/cross_exchange_live_remote_orchestrator.py`
- Focused tests under `examples/hyperliquid/`
- Task-scoped evidence under
  `local_live_analysis/exact_seeded_dynamic_shadow_0722T067/`
- Workflow/report/tracking files

action：
- Add explicit all-or-none seed contract, exposure file and expected SHA-256
  inputs to the watcher.
- Load only the accepted seed exposure rows into the online estimator before
  current public event ingestion.
- Add a distinct exact seeded dynamic manager profile without weakening the
  existing fixed or legacy dynamic profiles.
- Add a strict submit gate requiring:
  exact expected seed hash loaded, current candidate `status=pass`,
  `bounded=true`, no dynamic fallback, and actual tick-rounded quote behavior
  changed.
- Build a deterministic no-submit shadow from committed T066 event rows using
  `build_task7_desired_quotes`, the production quote builder.
- Persist per-event candidate/gate/quote rows and a strict acceptance summary.
- Add hostile missing/partial/hash/schema/tamper/fallback/no-change tests.

verify：
- Focused watcher/orchestrator/seeded-shadow tests and `py_compile`.
- Official shadow contains at least one strict eligible event with current
  candidate `pass`, bounded dynamic spread and changed final bid or ask.
- Every fallback, incomplete candidate, wrong seed hash or no-final-quote-change
  case is submit-ineligible.
- Seed rows do not populate current market buckets.
- Existing fixed and legacy dynamic profile tests remain unchanged.
- Clean deterministic rebuild reproduces official shadow artifacts.
- Full Hyperliquid regression and `git diff --check`.
- Boundary proves no credentials/private/account/order/cancel/service/live.

done：
- Exact seeded dynamic pricing is production-path reachable and fail-closed
  before submit.
- No live fill or economics claim is made by T067.

blockers：
- Circular import, seed/current-market contamination, fallback acceptance,
  final quote no-change, nondeterministic rebuild or forbidden endpoint use
  blocks the task.
- Any real-order task remains a separate fresh-authorized formal task.
