```md
执行线程：
- 业务线程-python

任务ID：
- 0516T002

标题：
- Queue-ahead proxy repeatability diagnosis

简短描述：
- 基于 `0516T001` 对 `4948` 的 queue-ahead evidence，继续在现有 `5-13-day-control-30min` 样本内做只读 repeatability diagnosis：寻找类似 `4948` 的 touch no-fill / replay-fill / fill-candidate cases，计算 queue-ahead proxy，并判断这种 mismatch 是否重复出现。本任务不做 replay repair。

状态：
- 待验收

执行顺序：
- 当前唯一后续任务

前置任务：
- `0516T001` QA 已通过，或总控明确接受 `0516T001` 业务结论用于后续只读诊断

规则更新提醒：
- 本任务是 read-only diagnosis，不是 repair。
- 不允许修改 replay fill model、queue logic、strategy logic。
- 不允许把 top5 / trade proxy 解释成 exact queue position proof。

按线程规则执行：
- .workflow/workflow-kit/thread-report-template.md
- docs/thread-playbook.md
- task_plan.md
- progress.md
- findings.md

是否需要提交代码：
- 需要

提交要求：
- 自测/检查通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

Allowed files：
- `.workflow/tasks/0516T002.md`
- `.workflow/reports/0516T002-business.md`
- `examples/binance_tick_mm/replay_lifecycle_mismatch_diagnosis.py`
- `examples/binance_tick_mm/test_replay_lifecycle_mismatch_diagnosis.py`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `.workflow/dashboard.html`
- `.workflow/dispatch_suggestions.md`

Allowed generated outputs：
- `local_live_analysis/5-13-day-control-30min/stage6i_queue_ahead_repeatability_0516T002/**`

Allowed read-only inputs：
- `.workflow/reports/0516T001-business.md`
- `local_live_analysis/5-13-day-control-30min/stage6h_queue_priority_evidence_0516T001/**`
- `local_live_analysis/5-13-day-control-30min/stage6g_single_residual_fill_diagnosis_0515T006/**`
- `local_live_analysis/5-13-day-control-30min/stage6f_cancel_race_residual_repair_0515T005/**`
- `local_live_analysis/5-13-day-control-30min/audit_live_5-13-day-control-30min.csv`
- `local_live_analysis/5-13-day-control-30min/out/backtest_audit_replay/audit_bt_audit_replay.csv`
- `local_live_analysis/5-13-day-control-30min/raw_market_data/**`
- `local_live_analysis/5-13-day-control-30min/t009_fixed_sidecar/**`

Forbidden files / areas：
- `examples/binance_tick_mm/backtest_tick_mm.py`
- `examples/binance_tick_mm/strategy_core.py`
- replay engine behavior / backtest fill model implementation
- strategy behavior, fair/target formulas, configs, risk guards, quote placement behavior
- live scripts, AWS/remote state, live collection
- sample expansion tasks
- core Rust, connector, py `event_dtype`, standard npz schema

Requires separate task：
- 任何 replay repair implementation。
- generalized touch fill assumption change。
- generalized queue exposure / priority model change。
- 新采集 current-format 样本。
- quote-adjustment promotion 或 live micro test。

Fatal Failures：
- 在 diagnosis 任务里直接修改 replay repair 代码。
- 将 queue-ahead proxy 解释成 exact queue-position proof。
- 只凭 `4948` 或单样本 proxy 结果直接授权 generalized queue/touch repair。
- 把本任务扩展成 sample collection、strategy change 或 live 判断。

action：
- 在现有 `5-13-day-control-30min` 内寻找类似 `4948` 的候选 cases：
  - live no-fill / later canceled
  - order 曾经或持续在 touch
  - replay fill 或 replay/proxy fill-candidate
  - submit -> cancel 或 submit -> replay fill 期间存在 same-price aggressive trades
  - cumulative same-price trade qty 小于 visible queue proxy
- 为每个候选计算 queue-ahead proxy：
  - same-price trade qty / visible qty
  - top1 visible qty decay
  - unexplained depth shrink
  - order-at-touch duration / touch share
  - quote age / join age / latency
- 输出至少这些 artifacts：
  - `queue_ahead_repeatability_summary.md`
  - `queue_ahead_candidate_cases.csv`
  - `queue_ahead_proxy_metrics.csv`
  - `queue_ahead_depth_trade_windows.csv`
  - `queue_ahead_bucket_summary.csv`
  - `run_manifest.json`
- 判断：
  - `4948` 是孤例，还是同类 queue-ahead mismatch 有重复性
  - 当前是否足够支持一个 repair-design task
  - 如果不够，明确缺什么证据

verify：
- `python -m pytest examples/binance_tick_mm/test_replay_lifecycle_mismatch_diagnosis.py`
- `python examples/binance_tick_mm/replay_lifecycle_mismatch_diagnosis.py --help`
- `python3 .workflow/build_dashboard.py`
- `git diff --check`

done：
- 回报必须明确 queue-ahead mismatch 是否重复出现。
- 回报必须明确候选 case 数、强候选数、`4948` 是否仍是孤例。
- 回报必须明确是否足以进入新的 repair-design task。
- 回报必须明确本任务未修改 replay、未改策略、未补样本、未启动 live。
- 回报必须写 `commit id` 和提交信息。
```
