# 任务派发

执行线程：
- SKHYNIX OBI Reversal Track A 方案更新业务线程

任务ID：
- 0828T003

标题：
- 将 OBI_REVERSAL_V1 加入 Interpretable Competing-Risk Track A 计划

简短描述：
- 将明确的 OBI reversal alignment 经验冻结为第一个 primary hypothesis，
  使用 current-OBI-matched non-reversal controls、first-passage price
  competing risks 和 H0/H1 path-dependence increment 检验。

状态：
- 已通过

执行顺序：
- 当前唯一正式任务

前置任务：
- 0828T002

规则更新提醒：
- 已阅读 AGENTS.md、workflow-kit manual、task/report/QA templates、
  Conditional-Risk Research Methodology Kernel v1、0828T002 正式方案和
  OBI reversal 讨论。

按线程规则执行：
- AGENTS.md
- .workflow/workflow-kit/workflow-manual.md

是否需要提交代码：
- 需要

提交要求：
- 文档验证通过后提交
- 回报必须带 `commit id` 和提交信息

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- docs/skhynix_binance_continuous_background_interpretable_m_state_competing_risk_track_a_plan_20260828.md
- .workflow/tasks/0828T003.md
- .workflow/reports/0828T003-business.md

action：
- 冻结 `OBI_REVERSAL_V1` 的经验来源、claim boundary 和 primary status。
- 定义标准 L1-L5 OBI、pre-state、crossing、confirmation、cross/detect
  timestamps 和 causal alignment。
- 定义同 current OBI 水平的 reversal/non-reversal common risk set。
- 定义 pressure-oriented first-passage follow/fail competing-risk targets。
- 定义 H0 snapshot model 与 H1 reversal-path increment。
- 更新 null、proper score、online actionability、failure classifications
  和 A0 first task。
- 将原 withdrawal/flow/refill composite 降为独立 successor hypothesis。

verify：
- `git diff --check`。
- Markdown fenced-code block 数量为偶数。
- 人工核对 `reversal_detected_at` 为唯一 primary decision/alignment time。
- 人工核对 H0 控制 current OBI，H1 只增加 reversal history。
- 人工核对 confirmation 之前的价格变化不计入可捕捉结果。
- 人工核对 generic composite M 不得救回 OBI primary failure。

done：
- 回报 OBI formula、reversal state machine、controls、targets、hazard、
  H0/H1、gates、failure classes、prospective 缺口和下一 A0 task。
