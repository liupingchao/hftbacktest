# 0721T043 REPRESENTATION-BEFORE-TOKEN-SEMANTICS-REPAIR

执行线程：
- 总控 auto-loop / 业务执行线程

任务ID：
- 0721T043

标题：
- Gate persisted representation before token semantics

简短描述：
- 修复 T042 QA 的执行顺序P2。
- Representation失败时不得调用generic historical token parser，也不得从raw identity产生任何independent token reconstruction。

状态：
- 执行中

执行顺序：
- 当前唯一任务

前置任务：
- `0721T042`（未通过）

规则更新提醒：
- 本任务offline-only；不得执行live、private、account、order、cancel、network、remote或service操作。
- Immutable T040 evidence不得修改。
- Fixed-quote rerun及adaptive/multi-level activation继续锁定。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `.workflow/reports/0721T042-qa.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交；业务报告和QA分别提交。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- Modify: `examples/hyperliquid/cross_exchange_delayed_history_probe_acceptance.py`
- Modify/Test: `examples/hyperliquid/test_cross_exchange_delayed_history_probe_acceptance.py`
- Workflow: task、business、QA、fixed QA、`task_plan.md`、`progress.md`、`findings.md`

source：
- Branch: `cross-exchange`
- Dispatch base: `90b2327`
- Immutable live artifact: `local_live_analysis/principal_alignment_delayed_history_probe_0721T040/`

action：
- 先调用`persisted_history_order_evidence_issues()`。
- 仅当representation issues为空时调用`historical_reference_tokens()`。
- Representation失败时independent `tokens={}`，classification为`malformed`，不得派生raw identity token。
- 增加raw identity hostile对`independent_rebuild.history_envelope.orders[].tokens == {}`的断言。
- 保留27 hostile、9 positive、T041 raw nested、immutable T040和原六hostile边界。

verify：
- Focused acceptance通过。
- Raw nested representation失败时tokens为空且blocked。
- Producer-redacted positive rows仍pass并重建非空tokens。
- Full Hyperliquid regression完整结束并有最终数字。
- 五份historical exact boundaries保持且offline-only。

done：
- Representation gate在代码路径和persisted independent rebuild中均严格先于token semantics。
- T042 QA唯一P2关闭。
- 独立QA通过前不得启动新live。

stop conditions：
- Invalid representation仍产生derived tokens。
- Positive producer-redacted alias被误拒绝。
- Full regression无法完成。
- 需要任何live/private/network/remote/service操作。

blockers：
- 无。

提交信息：
- `Gate history representation before token semantics`
