```md
执行线程：
- 业务线程-python

任务ID：
- 0529T002

标题：
- Read-only fill-quality bucket synthesis runner implementation

简短描述：
- 按 `0529T001` 的 fill-quality-first 设计结论，实现一个只读 runner，把现有 Stage 5/5C/6/9 artifacts 合成为 bucket-level fill-quality 证据表，判断哪些 bucket 可进入下一步 policy design，哪些应被拒绝或需要更多 clean fills。本任务不实现策略、不跑 live、不做参数搜索、不 default-on、不 promotion。

状态：
- 待验收

执行顺序：
- 当前唯一任务；正式派发后执行。

前置任务：
- `0529T001` QA 已通过。

规则更新提醒：
- 请先阅读规则文档后再执行本任务。
- 本任务是 read-only/default-off evidence synthesis runner，不是策略实现、参数搜索、live、tiny-live 或 promotion 任务。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `docs/thread-playbook.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交 runner、tests、artifacts、workflow/task/report/dashboard 更新。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

Allowed files：
- `.workflow/tasks/0529T002.md`
- `.workflow/reports/0529T002-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `.workflow/dashboard.html`
- `.workflow/dispatch_suggestions.md`
- `examples/binance_tick_mm/fill_quality_bucket_synthesis.py`
- `examples/binance_tick_mm/test_fill_quality_bucket_synthesis.py`
- `docs/binance_tick_mm_next_policy_design.md` only if documenting small clarifications from implementation
- `local_live_analysis/stage9k_fill_quality_bucket_synthesis_0529T002/**`

Allowed read-only inputs：
- `docs/binance_tick_mm_next_policy_design.md`
- `.workflow/reports/0529T001-business.md`
- `.workflow/reports/0529T001-qa.md` if available
- Existing Stage 5 execution labels under `local_live_analysis/**/stage5_execution_outcome_labels_0514T005/**`
- Existing Stage 5C quote-anchor safety artifacts under `local_live_analysis/**/stage5c_quote_anchor_safety_*/**`
- Existing Stage 6 calibration artifacts under `local_live_analysis/**/stage6_execution_calibration_*/**`
- Existing Stage 9B/9D/9H/9I artifacts under `local_live_analysis/**`
- Existing current-format sample directories under `local_live_analysis/**`
- Existing source files under `examples/binance_tick_mm/**` for read-only reference.

Forbidden files / areas：
- Strategy behavior, quote placement implementation, fair/target formula, inventory logic, risk guards, live configs, production defaults.
- Any live/tiny-live run, remote deploy, parameter search, default-on behavior, guard relaxation, or promotion claim.
- Reopening `0528T002` compact audit work unless a concrete regression is found.
- Continuing to tune the rejected `0528T001` fixed inventory-aware quote placement skeleton as if it only needed parameters.
- Continuing the current `0526T004` min-move projected-suppression grid as the main route.
- Queue/touch residual repair, exact queue position, full L2 proof, connector/core API changes.

Implementation scope：
1. Add a CLI runner, tentatively:
   - `python examples/binance_tick_mm/fill_quality_bucket_synthesis.py --help`
   - accepts one or more `--run-dir` inputs or a manifest/current-format sample list, matching existing Stage 9 runner style where practical.
   - writes outputs to `local_live_analysis/stage9k_fill_quality_bucket_synthesis_0529T002/` by default or via `--output-dir`.
2. Use existing artifacts only. Do not generate new replay, do not run backtests, do not infer counterfactual fills.
3. Aggregate bucket-level fill-quality evidence across clean samples and caveated sensitivity samples.
4. Required bucket dimensions:
   - `inventory_bucket`
   - `side_class` / add-reduce-flat side
   - `quote_distance_bucket`
   - `fair_or_reservation_edge_bucket`
   - `latency_stale_bucket`
   - `post_only_safety_bucket`
   - `reject_throttle_churn_bucket`
   - `fill_after_cancel_bucket`
5. Required metrics:
   - rows / fills / fill rate
   - clean-only fill mass
   - side-adjusted 5s markout
   - spread capture
   - fill-after-cancel rate
   - inventory increasing / reducing fill split
   - post-only risk / reject / throttle / churn safety
   - clean-only vs caveated sensitivity stability
6. Produce candidate trigger tables for the two design shapes from `0529T001`:
   - passive quality gate with inventory sizing
   - reduce-side participation gate with spread-capture floor
7. Produce rejected bucket tables for:
   - high-fill but poor-quality buckets
   - high-spread but too-low-fill / unstable buckets
   - unsafe stale/post-only/reject/throttle/churn buckets
8. Assign verdicts:
   - `ready_for_policy_design`
   - `needs_more_clean_fills`
   - `reject_quality_negative`
   - `not_decisionable`

Required outputs：
- `run_manifest.json`
- `bucket_fill_quality_metrics.csv`
- `clean_only_stability_summary.csv`
- `caveated_sensitivity_summary.csv`
- `shape_a_passive_quality_gate_candidates.csv`
- `shape_b_reduce_side_participation_candidates.csv`
- `rejected_bucket_reasons.csv`
- `fill_quality_bucket_recommendation.md`
- `.workflow/reports/0529T002-business.md`

Acceptance criteria：
- Runner is read-only and reproducible from CLI.
- Focused tests cover bucket verdict assignment, clean/caveated split, and rejection taxonomy.
- Outputs show whether either Shape A or Shape B is ready for a later design-only policy contract.
- If neither is ready, outputs specify missing fills / unstable strata / negative-quality reasons.
- Report explicitly states no strategy behavior, live behavior, fill/cancel replay semantics, parameter search, default-on, guard relaxation, tiny-live, or promotion was changed.

verify：
- `python examples/binance_tick_mm/fill_quality_bucket_synthesis.py --help`
- Focused pytest:
  - `python -m pytest examples/binance_tick_mm/test_fill_quality_bucket_synthesis.py -q`
- Run the runner on accepted current-format samples used by `0528T001` or an explicitly documented subset if runtime is too high.
- `python3 .workflow/build_dashboard.py`
- `git diff --check`

done：
- Business report ends in `待验收`.
- Report lists exact input samples/artifacts, generated files, key verdict counts, and next recommended task.
- Report includes whether Shape A or Shape B is ready for policy-design follow-up or should be rejected / require more clean fills.
- Report includes `commit id` and `提交信息`.
```
