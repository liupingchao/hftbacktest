# 任务派发

执行线程：
- 业务线程-price-normalization

任务ID：
- 0718T012

标题：
- PRICE-NORMALIZATION-REPAIR

简短描述：
- 实现 Principal Alignment Task 1 / C3：建立唯一权威 Hyperliquid perp price normalization 与 post-only helper，并接入 shared kernel、executor 和 fill-window。
- 只做离线价格合法化与回归测试，不改变 quote policy、订单边界、风险 envelope 或 live 行为。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0717T011 / LIVE-EVIDENCE-INTEGRATED-OFFLINE-ACCEPTANCE` QA 为 `已通过`。

规则更新提醒：
- 无

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- focused tests 通过后提交。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `examples/hyperliquid/cross_exchange_price_math.py`
- `examples/hyperliquid/cross_exchange_shared_signal_kernel.py`
- `examples/hyperliquid/hyperliquid_tiny_live_real_order_executor.py`
- `examples/hyperliquid/hyperliquid_tiny_live_m2_fill_window.py`
- `examples/hyperliquid/test_cross_exchange_price_math.py`
- `examples/hyperliquid/test_cross_exchange_shared_signal_kernel.py`
- `examples/hyperliquid/test_hyperliquid_tiny_live_real_order_executor.py`
- `examples/hyperliquid/test_hyperliquid_tiny_live_m2_fill_loop.py`
- `docs/cross_exchange_price_taxonomy_contract.md`
- `.workflow/tasks/0718T012.md`
- `.workflow/reports/0718T012-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

action：
- 使用 `Decimal` 建立 `normalize_hl_perp_price`、`assert_hl_perp_price_valid`、`post_only_price`。
- 支持 buy floor、sell ceil、nearest，满足最多 5 位有效数字和 `6 - szDecimals` 小数位限制。
- 统一 kernel/executor/fill-window 的价格输出和 post-only 非穿价检查。
- 保留 executor 旧函数名作为兼容 wrapper，但实现只能委托共享 helper。
- 添加整数价格、低价非整数、二次 normalization 幂等、极端价格、NaN/inf/非正值和 crossed/invalid BBO fail-closed 测试。

verify：
- `python -m pytest examples/hyperliquid/test_cross_exchange_price_math.py examples/hyperliquid/test_cross_exchange_shared_signal_kernel.py examples/hyperliquid/test_hyperliquid_tiny_live_real_order_executor.py examples/hyperliquid/test_hyperliquid_tiny_live_m2_fill_loop.py`
- Phase 1-4 repair regression tests
- `python -m py_compile` 相关生产/测试文件
- `python examples/hyperliquid/cross_exchange_live_remote_orchestrator.py --help`
- `git diff --check`
- 全部 offline；不执行 live/private/order/cancel/network/remote/service 动作。

done：
- 共享价格 helper 成为 kernel、executor、fill-window 的唯一实现来源。
- 任何输出满足 precision invariant，normalize 幂等。
- buy/sell post-only 输出严格不穿过对手价。
- 非法输入 fail-closed，既有 live 参数、订单边界和 endpoint gate 不改变。

not authorized：
- 不执行 live 或 remote。
- 不修改 quote policy、order size、max submissions、max loss、runtime envelope、threshold 或 strategy semantics。
- 不进入 Principal Alignment Task 2 kill-switch 实现。
