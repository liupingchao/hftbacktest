# 任务派发

执行线程：
- 业务线程-python/research-infra

任务ID：
- 0820T001

标题：
- RESEARCH-PACKAGE-TRUST-KERNEL-LAYERED-IDENTITY-AND-STAGE4-PARITY

简短描述：
- 按 `docs/research_package_trust_kernel_execution_plan.md` 实现
  Research Package Trust Kernel v1、R/C/E 分层身份、Stage 4
  compatibility adapter、workflow Gate 0、durable archive 与 exact
  empty-dir cleanup。
- 本任务是 continuous conditional-risk v2 Stage H0-A 之前的唯一正式
  前置。它是纯 research infrastructure，不生成新的研究结论。

状态：
- 执行中

第一轮 QA：
- 状态：`未通过`。
- commit：`8b1caf6c12d911f40dda4802b170041338e45010`。
- report/mirror SHA256：
  `754a55fc0dbbdd854bb139401df32428b0666b3b4af8482c027e0159a613f275`。
- `P0/P1/P2/P3=0/4/1/0`。
- 本轮只允许修复：
  - current/frozen 真实 negative topology；
  - accepted registry future pin 与 acceptance-package exact binding；
  - canonical relocation 与 QA-specific full-admission outputs；
  - matrix-declared stable error code execution；
  - final archive envelope 与 cleanup identity/time binding。
- 禁止借返修修改 research bytes、Stage 4 package、研究语义或 accepted
  registry；Stage H0-A 继续锁定。

第二轮业务交接：
- code repair commit：
  `268b3795874c6487d3fadbd1a5ea812b803ded46`。
- source/frozen SHA256：
  `cee2395afad9420c38235ba195bf030e92330015e1a15937ebc22fa707c80203`。
- hostile topology：
  `98/36/12/4/10`，fail-open=`0`。
- final archive / cleanup binding receipt：
  `6e1d42f197d916fab5bcc8b610958b81d0c8487e810d05470fdafcb3cc5fbdb4`
  /
  `c4dd06d2efa502163f08010d996a2e484aae67662c067a87513924fe11dc6413`。
- 第一轮 QA 报告继续是历史失败事实源；第二轮 QA 必须写新的最终结论，
  business 不修改 `docs/qa-acceptance-report.md`。

第二轮 QA：
- 状态：`未通过`。
- commit：`3cccaa47070cd5c80129075f4fa8385292847a3c`。
- report/mirror SHA256：
  `34425dc7b7a3507cabc3580239c86809fba583d94f5d120b733dce7988193d51`。
- `P0/P1/P2/P3=0/1/1/0`。
- 本轮只允许修复：
  - archive/envelope operation 的真实 start/completion 采集与
    `parity < archive start < archive completion` 结构化断言；
  - Mac full source-semantic admission 与 amdserver kernel-only
    admission 的 host/package/runbook 分离。
- 禁止借返修改 kernel/research semantics、research bytes、Stage 4
  package 或 accepted registry；Stage H0-A 继续锁定。

执行顺序：
- 当前唯一任务

前置任务：
- `0815T003` 已通过；accepted Stage 4 core/full：
  - `78be6559c7ac5aaec4d5411b042f7ddcce522473f60f987237bcaa9d2dd5e157`
  - `669fb7d12f25cfa7828aec0fb1546398b1def754952de2290cd19784a477a433`
- `0815T003` QA Round 6 report SHA256：
  `0b85ae527f7e91f40f3e4401969596ff6462964a7e05cb8d0de73e763ad3e592`
- Trust Kernel review remediation commit：
  `0efd46a3`。
- v1 Ordered Research Queue items 5-14 已由用户批准的 v2 取代。

规则更新提醒：
- 请先阅读 workflow、Trust Kernel 执行方案、Stage 4 Round 6 QA 和本任务
  surface matrix 后再执行。

按线程规则执行：
- `AGENTS.md`
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `docs/research_package_trust_kernel_execution_plan.md`
- `.workflow/reports/0815T003-qa-round6.md`
- `.workflow/contracts/0820T001-surface-matrix.json`

task_type：
- `research_package_infrastructure`

produces_research_package：
- `true`

kernel pin：
- `mode=bootstrap_candidate`
- `kernel_name=research_package_trust_kernel`
- `candidate_version=v1`
- `registry_path=baselines/research_package_trust_kernel/accepted_versions.json`
- `registry_schema_path=.workflow/workflow-kit/research-package-kernel-registry.schema.json`
- 空 registry 只表示尚无 accepted kernel；业务线程不得写 accepted entry。

是否需要提交代码：
- 需要

提交要求：
- 完成聚焦自测后提交。
- 回报必须带 `commit id` 和提交信息。
- accepted registry promotion 不属于业务提交；只有独立 QA 通过后由总控
  closure commit 执行。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 按执行方案 Gate 0-7 独立验收。
- 在 macmini accepted formal package 和完整 Stage 1/2/3、Jul30
  dependencies 上，使用 Python 3.10+ 与 package 内 frozen runtime source
  独立运行一次 Stage 4 完整只读 source-semantic admission。
- QA full source-semantic admission 必须使用独立输出：
  - `--first-full-start-output
    .workflow/reports/0820T001-qa3-first-full-admission-start.json`
  - `--layer-assignment-output
    .workflow/reports/0820T001-qa3-stage4-layer-assignment.json`
  - `--parity-report-output
    .workflow/reports/0820T001-qa3-stage4-parity.json`
- amdserver archive 明确禁止执行 `--full-admission`；必须只运行
  `.workflow/runners/0820T001_archive_stage4_to_amdserver.sh
  --verify-kernel-only
  .workflow/reports/0820T001-qa3-stage4-kernel-only.json`，独立验证
  byte-exact package、kernel admission、R/C/E/composite、portability
  contract、cleanup receipt 和 zero-write。
- exact 验证 `hostile < first-full < parity < final archive <
  cleanup final-envelope attestation`，并核对 archive/cleanup 的
  R/C/E/composite binding。
- QA 不写 accepted registry，不把 candidate kernel 自行标记为 accepted。

Surface Matrix：

| Surface | Authoritative source | Decision/as-of time | Exact fields/keys | Rebuild oracle | Negative mutation | Durable evidence | Identity layer |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `stage4_research_data_anchor` | accepted `0815T003` Stage 4 package | accepted final publication | `path/bytes/sha256` over 99 research files | exact durable inventory comparison | mutate one research byte; old C/E must reject | `0815T003-round4-pre-repair-research-inventory.csv` | `R` |
| `kernel_runtime_contract` | frozen Trust Kernel execution plan | not applicable; deterministic infrastructure | source tree/API/error codes/tests | seconds-level kernel suite | domain constant injection and same-version source drift | new v1 candidate, no prior accepted surface | `C` |
| `workflow_governance_contract` | frozen surface/registry schemas | not applicable; explicit task fields only | task type, matrix, pin, surfaces, EC1-EC7 | workflow validator fixtures | missing surface and `TBD` | new v1 candidate, no prior accepted surface | `C` |
| `registry_bootstrap_governance` | frozen empty registry | business/QA final publication state | schema version, revision 0, empty versions | exact raw-byte registry validation | premature accepted entry | empty registry raw SHA | `E` |
| `hostile_preflight_and_candidate_evidence` | candidate kernel C surface | sealed before first full admission | source/schema/fixture/matrix hashes and negative counts | hostile-preflight CLI | missing/drifted receipt | new v1 candidate, no prior accepted surface | `E` |
| `stage4_parity_envelope` | accepted Stage 4 package and legacy admission | accepted final publication | legacy/new verdicts, R/C/E/composite, pre/post inventory | one read-only full admission | production-shaped coherent rehash | `0815T003` QA Round 6 | `E` |
| `archive_and_cleanup_envelope` | accepted Stage 4 package | archive publish before cleanup | source/destination inventory, foreground wait, portability, exact allowlist | remote exact-tree verification and two-phase cleanup | archive drift or cleanup symlink/inode drift | `0815T003` QA Round 6 | `E` |

Canonical matrix：
- `.workflow/contracts/0820T001-surface-matrix.json`
- JSON 是机器事实源；本表的 surface ID 必须与 JSON exact。

七条 exit criteria：
- `EC1` 每个 load-bearing surface 有 authoritative source。
- `EC2` 每个字段 exact source-derived 或显式 unavailable/not-applicable。
- `EC3` evidence/manifest exact key/type/value universe。
- `EC4` 每个 filesystem entry 进入 exact identity universe。
- `EC5` unchanged claim 有 durable prior evidence。
- `EC6` 每个 surface 至少一个 stable-code negative mutation。
- `EC7` hostile preflight receipt 通过后才允许第一次 full admission。

files：
- `examples/hyperliquid/research_package_trust/**`
- `examples/hyperliquid/research_package_trust_cli.py`
- `examples/hyperliquid/research_package_trust_stage4_adapter.py`
- `examples/hyperliquid/test_research_package_trust_*.py`
- `baselines/research_package_trust_kernel/**`
- `.workflow/workflow-kit/**`
- `.workflow/contracts/0820T001-surface-matrix.json`
- `.workflow/runners/0820T001_archive_stage4_to_amdserver.sh`
- `.workflow/tasks/0820T001.md`
- `.workflow/reports/0820T001-*`
- `AGENTS.md`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `docs/research_package_trust_kernel_execution_plan.md`

明确禁止修改：
- `local_live_analysis/skhynix_trigger_aligned_episode_research_v1_stage04_jul30_episode_v3/**`
- 99 个 accepted research CSV/GZ。
- Stage 1/2/3 accepted packages。
- Candidate、Confirmed、Family A/B、feature、path、outcome、censor 或
  detector semantics。
- v2 Stage H0 outputs。
- accepted registry entries；本任务业务/QA 阶段 registry 必须保持空。

例外 filesystem action：
- 仅在 archive 已完成且 receipt 通过后，对冻结的七个 exact-name 空目录
  执行两阶段全体 precheck/recheck 和非递归 `os.rmdir`。
- 仅在 frozen amdserver worktree 外 durable root 创建 temp/final archive。
- 本机 accepted Stage 4 package 必须保留。

action：
- Phase 0：冻结 Stage 4 anchors、任务 surface matrix、registry bootstrap、
  amdserver facts 和七目录 allowlist。
- Phase 1：实现 pure kernel、stable errors、canonical/evidence/tree/
  publication/admission API 与秒级 fixtures。
- Phase 2：实现 R/C/E/composite、reverse binding、legacy bridge 和
  mutation isolation。
- Phase 3：hostile receipt 通过后，执行一次 Stage 4 完整只读 parity
  admission；不得 full rebuild。
- Phase 4：实现 research-package task template、validator、fixtures 和
  historical compatibility。
- Phase 5：前台同步归档到 frozen amdserver path，远端 exact verify 后
  atomic publish，再执行七目录 fail-closed cleanup。
- Phase 6：生成 candidate acceptance package 和业务回报，状态进入
  `待验收`。

verify：
- `python3 -m pytest examples/hyperliquid/test_research_package_trust_evidence.py`
- `python3 -m pytest examples/hyperliquid/test_research_package_trust_tree.py`
- `python3 -m pytest examples/hyperliquid/test_research_package_trust_identity.py`
- `python3 -m pytest examples/hyperliquid/test_research_package_trust_publication.py`
- `python3 -m pytest examples/hyperliquid/test_research_package_trust_stage4_parity.py`
- `python3 .workflow/workflow-kit/validate_research_package_task.py --task
  .workflow/tasks/0820T001.md --matrix
  .workflow/contracts/0820T001-surface-matrix.json --negative-evidence
  .workflow/reports/0820T001-kernel-candidate/negative_matrix.json`
- `python3 -m pytest .workflow/workflow-kit/test_validate_research_package_task.py`
- hostile aggregate/direct-tree/production-shape/metamorphic/surface matrix：
  `98/36/12/4/10` fail closed，fail-open=`0`。
- hostile receipt 与 first-full-admission receipt 的 hash/time binding。
- business Stage 4 full read-only legacy/new parity。
- QA macmini Stage 4 full source-semantic admission 使用 formal package
  frozen runtime source 和独立 qa3 outputs。
- amdserver archive kernel-only admission；不得打开 Mac external
  dependency paths，不得调用 full source-semantic replay。
- accepted package pre/post exact inventory、type、mode、mtime/ctime。
- remote source/destination exact tree、R/C/E/composite、legacy identities。
- cleanup two-phase fixtures、formal seven-path receipt 和
  post-final-envelope identity/time binding。
- purity/boundary scan、`git diff --check` 和 focused compile/lint。

done：
- Kernel v1 candidate API、source snapshot、negative matrix 和 acceptance
  candidate package完整。
- Stage 4 legacy/new admission exact parity，99 research files 与 accepted
  R identity不变。
- Workflow Gate 0 对不完整 research tasks fail closed，对历史/general
  tasks 不误伤。
- amdserver archive byte-exact、前台闭合、source replay portability
  固定为 `false`。
- 七个 exact empty dirs 安全删除，正式 package 保留且 identity 不变。
- 业务报告状态为 `待验收`，记录 commit、全部证据和 blockers。
