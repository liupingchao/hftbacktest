```md
执行线程：
- 业务线程-python

任务ID：
- 0519T006

标题：
- Step 8C default-off quote-update helper / instrumentation implementation

简短描述：
- 基于 `0519T005` Step 8B 的 `default_off_helper_candidate` 结论，实施一个默认关闭的 quote-update helper / instrumentation 层：统一记录 quote-update intent/action/reason、throttle/token/cancel-readd/post-only/inventory-request 字段，并保持现有策略行为默认不变。本任务不进入 Step 9，不跑 replay sweep，不启动 live。

状态：
- 阻塞

执行顺序：
- 当前唯一任务；当前阻塞于 `0519T005` QA，QA 通过后才能派发执行

前置任务：
- `0519T005` QA 已通过
- `0519T004` QA 已通过
- `0518T004` QA 已通过
- `0519T003` QA 已通过

规则更新提醒：
- 本任务是 Step 8C default-off helper / instrumentation implementation。默认行为必须保持不变；不得把 quote-update helper、Step 5C safety、Step 7 inventory request 或 Step 9 replay candidate 升级为 default-on。

按线程规则执行：
- .workflow/workflow-kit/thread-report-template.md
- docs/thread-playbook.md
- task_plan.md
- progress.md
- findings.md

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

Allowed files：
- `.workflow/tasks/0519T006.md`
- `.workflow/reports/0519T006-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `.workflow/dashboard.html`
- `.workflow/dispatch_suggestions.md`
- `examples/binance_tick_mm/audit_schema.py`
- `examples/binance_tick_mm/strategy_core.py`
- `examples/binance_tick_mm/backtest_tick_mm.py`
- `examples/binance_tick_mm/live_tick_mm.py`
- `examples/binance_tick_mm/test_backtest_tick_mm.py`
- `examples/binance_tick_mm/test_validate_audit.py`
- `examples/binance_tick_mm/test_deploy_preflight.py`
- `examples/binance_tick_mm/test_quote_anchor_safety.py`

Allowed read-only inputs：
- `.workflow/reports/0519T005-business.md`
- `.workflow/reports/0519T005-qa.md` after it exists
- `.workflow/reports/0519T004-qa.md`
- `.workflow/reports/0518T004-qa.md`
- `local_live_analysis/5-13-day-control-30min/stage8b_quote_update_diagnostic_0519T005/**`
- `local_live_analysis/5-13-day-control-30min/stage5c_quote_anchor_safety_0518T004/**`
- `local_live_analysis/5-13-day-control-30min/stage5b_quote_anchor_diagnostic_0518T003/**`

Forbidden files / areas：
- Any default-on quote-update behavior.
- Any Step 9 replay candidate, replay sweep, strategy PnL experiment, live run, live collection, AWS/remote state, or deployment change.
- Any Step 5C default-on promotion or hard-anchor source-level drift repair.
- Any Step 7 inventory-control implementation beyond a passive `inventory_request_id` / request placeholder field.
- Any broad refactor of strategy logic, replay lifecycle, connector behavior, core engine behavior, or canonical data conversion.

Requires separate task：
- Step 9 default-off quote-adjustment replay experiment.
- Enabling helper behavior as a strategy control rather than instrumentation.
- Inventory skew / AS-style execution implementation.
- Live validation or live micro test.
- Full market-view source alignment, top5 hard-anchor promotion, queue/touch repair, or raw data/core schema redesign.

Fatal Failures：
- 改变默认 quote placement / cancel / submit 行为。
- 改变现有 throttle/API/latency suppression semantics。
- 将 GTX/post-only reject 当作正常 quote-update 控制流。
- 将 helper 字段的存在解释为 live readiness、promotion、PnL proof 或 Step 9 已通过。

scope：
- 实施一个默认关闭、诊断优先的 quote-update helper / instrumentation 层。
- 目标是补齐 Step 8B 发现的字段和统一入口，让后续 Step 9 replay 能解释行为变化来源。
- 本任务的验收重点是 audit/instrumentation 和 no-behavior-change guard，不是 PnL 或 replay 改善。

action：
- 在共享策略层建立 quote-update instrumentation helper，至少覆盖：
  - quote update intent
  - quote update action
  - quote update reason
  - min move pass / fail
  - quote age / join age / anchor age / latency bucket
  - throttle state / token bucket state
  - cancel-readd bucket
  - reject/throttle/drop cause
  - post-only pre-check / post-only post-check
  - inventory request id placeholder
- 扩展 audit schema，使 live/backtest audit rows 都能写出这些字段，且缺省值稳定、兼容 preflight/schema 校验。
- 接入 backtest 和 live audit row 构建路径，但默认不改变 action、planned_action、reject_reason、throttle_reason 或实际 submit/cancel 行为。
- 保留现有 throttle/API/latency suppression semantics；helper 只能记录，不得绕过或替代既有 guard。
- 增加 focused tests，至少证明：
  - audit schema 包含新增 Step 8C 字段
  - 默认配置或默认调用下策略 action path 不变
  - helper 能输出稳定的 intent/action/reason/default fields
  - live/backtest 都使用同一套 helper / field names
  - preflight/schema compatibility 不因新增字段失效

verify：
- `python -m pytest examples/binance_tick_mm/test_backtest_tick_mm.py`
- `python -m pytest examples/binance_tick_mm/test_validate_audit.py examples/binance_tick_mm/test_deploy_preflight.py`
- `python -m pytest examples/binance_tick_mm/test_quote_anchor_safety.py`
- `python examples/binance_tick_mm/maker_acceptance.py --help`
- `python3 .workflow/build_dashboard.py`
- `git diff --check`

done：
- 回报必须列出新增 helper / audit 字段。
- 回报必须说明默认行为是否保持不变，以及用哪些测试证明。
- 回报必须说明哪些字段仍是 placeholder / diagnostic-only。
- 回报必须明确：no Step 9, no replay sweep, no live, no default-on, no promotion。
- 回报必须写 `commit id` 和提交信息。
```
