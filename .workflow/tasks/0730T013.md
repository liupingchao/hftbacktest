# 任务派发

执行线程：
- 业务线程-python/cross-exchange-research

任务ID：
- 0730T013

标题：
- SKHYNIX-RESEARCH-EVENT-STORE-BUILDER

简短描述：
- 执行 T012 研究方案的 R0，在不重新采集的前提下，把本地八段四小时
  SKHYNIX raw package 整理为可审计的 research event store。
- 保留 common L2 timeline 作为 replayed depth state，新增标准化 Binance
  hot events、Hyperliquid hot events、auxiliary events、segment masks 和
  完整 source/output provenance。

状态：
- 未通过

执行顺序：
- 当前唯一研究实现任务

前置任务：
- `0730T012` 已通过。
- `0730T011` 业务数据与本地完整性检查已完成，独立 QA 状态仍保留在
  input manifest 中。

规则更新提醒：
- 无

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 不需要

提交要求：
- 当前工作区包含未提交前置任务，不创建混合提交。
- 回报使用 `commit：无`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 独立检查 builder fail-closed contract、source/output hashes、row-count
  reconciliation、degraded masks 和真实四小时构建产物。

files：
- `.workflow/tasks/0730T013.md`
- `.workflow/reports/0730T013-business.md`
- `examples/hyperliquid/cross_exchange_research_dataset.py`
- `examples/hyperliquid/test_cross_exchange_research_dataset.py`
- `docs/skhynix_cross_exchange_research_dataset.md`
- input:
  `local_live_analysis/cross_exchange_collection_campaign_0730T011_skhynix_4h_8x30m/`
- output:
  `local_live_analysis/skhynix_cross_exchange_research_0730T013/`
- `task_plan.md`
- `progress.md`
- `findings.md`

action：
- Validate campaign terminal pass, timeline index, segment IDs, profile,
  timeline hashes and all source raw hashes.
- Preserve each segment common L2 timeline as the replayed L2 state source
  instead of duplicating its 337-column rows.
- Normalize Binance `bookTicker` and `trade` events with local/exchange
  timestamps, BBO/trade fields, update IDs and source raw sequence.
- Normalize Hyperliquid `bbo` and individual trade items while retaining raw
  side, trade ID, hash and user identities.
- Index auxiliary `activeAssetCtx`, candle and allMids events with full compact
  payload JSON and explicit degraded flags.
- Persist the exact two segment-2 degraded intervals and segment restart gaps.
- Write per-segment manifests plus campaign-level input/output manifests with
  SHA-256 and row-count reconciliation.

hard boundary：
- Existing local raw and common L2 files are read-only.
- No network, AWS, SSH, public-data collection, private/account/order endpoint
  or strategy process.
- No feature fitting, parameter search, signal conclusion or arbitrage claim.
- No exact queue/fill/PnL claim.
- Any future new collection remains behind the T012 explicit authorization
  and active-trading-window gate.

verify：
- Focused synthetic tests for valid build, hash mismatch, count mismatch,
  timestamp regression, wrong symbol/coin, degraded mask and nonempty output.
- Local Conda `py_compile`, CLI `--help` and focused pytest.
- Build all eight real segments.
- Independently recount generated gzip CSV rows and verify every output SHA.
- Verify raw and timeline inputs remain byte-identical before/after.
- `git diff --check`.

done：
- Eight-segment research event store is present locally.
- Every source and output hash/count reconciles.
- Segment gaps and auxiliary degraded masks are explicit.
- R0 enters `待验收`; R1 cannot start until independent QA passes.
