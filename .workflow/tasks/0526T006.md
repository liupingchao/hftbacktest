```md
执行线程：
- 业务线程-python

任务ID：
- 0526T006

标题：
- Focused maker-edge design: inventory-aware quote placement

简短描述：
- 基于 `0526T005` 的 read-only triage 结果，制定一个 focused maker-edge design contract：以 inventory-aware quote placement 为主线，把 fair/reservation 作为 pricing context，把 quote-distance 和 size/side 作为执行控制维度。任务只做设计，不实现策略、不跑参数搜索、不启动 live、不 default-on、不 promotion。

状态：
- 待验收

执行顺序：
- 当前唯一任务；已正式派发

前置任务：
- `0526T005` QA 通过后执行
- `0526T004` QA 已通过
- `0519T006` QA 已通过，quote-update audit fields 已落地

规则更新提醒：
- 请先阅读规则文档后再执行本任务。
- 本任务是 design-only，不是 runner implementation、parameter search、strategy implementation、live、tiny-live、default-on 或 promotion 任务。
- 不允许继续沿着 `min_move_quote_age_churn_guard` projected-suppression grid 加码；该方向已降级为 execution hygiene / instrumentation，除非另立新假设。

按线程规则执行：
- `.workflow/workflow-kit/thread-report-template.md`
- `docs/thread-playbook.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交 workflow/task/report/doc updates。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

Allowed files：
- `.workflow/tasks/0526T006.md`
- `.workflow/reports/0526T006-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `.workflow/dashboard.html`
- `.workflow/dispatch_suggestions.md`

Allowed read-only inputs：
- `.workflow/tasks/0526T005.md`
- `.workflow/reports/0526T005-business.md`
- `local_live_analysis/stage9h_maker_edge_triage_0526T005/**`
- `local_live_analysis/stage9g_min_move_parameter_sweep_0526T004/**`
- `local_live_analysis/stage9d_candidate_bucket_refinement_0526T002_aggregate/**`
- `local_live_analysis/stage9c_multi_sample_validation_0521T002/**`
- current-format sample artifacts under:
  - `local_live_analysis/5-19-day-control-30min/**`
  - `local_live_analysis/5-19-night-active-30min-a/**`
  - `local_live_analysis/5-19-night-active-30min-b/**`
  - `local_live_analysis/5-19-night-active-30min-c/**`
  - `local_live_analysis/5-21-day-control-60min/**`
  - `local_live_analysis/5-26-active-minmove-control-30min-a/**`
  - `local_live_analysis/5-26-active-minmove-control-60min-a/**`
- `examples/binance_tick_mm/strategy_core.py`
- `examples/binance_tick_mm/quote_adjustment_replay.py`
- `examples/binance_tick_mm/maker_edge_triage.py`
- `examples/binance_tick_mm/execution_outcome_labels.py`
- `examples/binance_tick_mm/quote_anchor_safety.py`

Forbidden files / areas：
- Strategy behavior, production defaults, live configs, fair/target formula, risk/position logic, quote placement logic.
- Core/connector APIs, standard npz schema, converter behavior, replay lifecycle repair, queue/touch repair.
- Any code implementation of the proposed design, parameter search, replay sweep execution, live/tiny-live run, default-on behavior, or promotion claim.

Requires separate task：
- Implementing any strategy helper / runner.
- Running parameter search.
- Running offline replay experiment.
- Collecting more samples.
- Promoting any candidate into tiny-live design or production.
- Changing live or backtest quote behavior.

Fatal Failures：
- Treating this task as implementation rather than design.
- Splitting into five independent tracks instead of producing one focused inventory-aware quote-placement design.
- Using future fill, future markout, same-sample PnL feedback, or fill-after-cancel outcome as live decision inputs.
- Treating caveated samples as strict-clean promotion proof.
- Emitting `ready_for_tiny_live_design`, default-on, or live-promotion recommendations.

design goal：
- Produce a concrete design contract for a later read-only implementation task that can test an inventory-aware quote-placement candidate.
- The design should answer:
  - How inventory state controls quote side preference, skew, and size.
  - How fair/reservation context informs whether the quote is favorable enough to place or keep.
  - How quote-distance buckets define placement frontier and participation / adverse-selection tradeoff.
  - How size/side logic reduces toxic add-side fills while preserving recovery-side fills.
  - Which metrics must prove that improvement is not simply less trading.
- The design must be specific enough for the next task to implement a read-only runner without rediscovering the policy shape.

required candidate policy skeleton：
- The report must define one focused candidate shape, not only abstract principles.
- It must specify:
  - inventory bucket -> side preference mapping:
    - flat / mild skew / large skew or low score
    - add-side / reduce-side / flat-side handling
  - fair/reservation edge -> keep/place threshold mapping:
    - when a side is favorable enough to keep/place
    - when edge is too weak and should fall back to no-change
  - quote-distance -> placement frontier mapping:
    - one-tick tight / wider / outside-placement behavior
    - how quote-distance interacts with inventory pressure
  - size-side -> execution-control mapping:
    - when to reduce or suppress add-side size
    - when to preserve or prioritize recovery-side size
  - no-change fallback:
    - exact conditions where the design must produce no request rather than forcing a quote update
  - safety context:
    - stale / latency / anchor-age / post-only state are guard context and cannot become the main alpha.

required design components：
- State variables:
  - inventory bucket: flat / mild skew / large skew or low score
  - side class: add-side / reduce-side / flat-side
  - fair/reservation edge bucket
  - quote-distance bucket
  - spread / post-only safety state
  - latency / stale / anchor age state as guard context, not main alpha
- Proposed action outputs:
  - side preference request
  - quote distance adjustment request
  - size multiplier or suppression request
  - reduce-side priority request
  - no-change fallback
- Explicit non-actions:
  - no new live behavior in this task
  - no direct order placement change
  - no default-on config
  - no PnL optimization loop

required evidence policy：
- Decision inputs may use only live-decision-visible fields.
- Evaluation labels may use future fill / markout / spread capture only after the fact.
- Net PnL proxy remains diagnostic, not promotion metric.
- Caveated samples are sensitivity only.
- Clean-only evidence is primary.

required evaluation plan：
- For a later implementation task, define metrics for:
  - submit reduction / participation
  - fill rate and fill count
  - 5s side-adjusted markout
  - spread capture
  - fill-after-cancel
  - inventory-increasing vs inventory-reducing fills
  - recovery-side fill quality
  - quote-distance x inventory bucket frontier
  - post-only risk after recheck
  - reject/throttle/drop exposure
  - clean-only stability
  - caveated-sample sensitivity
- Require the design to distinguish:
  - true toxic-fill reduction
  - improved recovery-side participation
  - simple fill suppression
  - caveated-only effect
  - insufficient-fill uncertainty

required later read-only runner contract：
- The report must include a concrete implementation contract for the next task, without implementing it in this task.
- The contract must name expected read-only inputs, at minimum:
  - Stage 9H maker-edge triage artifacts from `0526T005`
  - Stage 5 execution labels
  - Stage 5C anchor / post-only diagnostics
  - Step 9B / 9C / 9D per-sample quote-adjustment artifacts where needed
  - the seven current-format sample directories listed above
- The contract must name expected outputs, at minimum:
  - `run_manifest.json`
  - per-sample candidate decision rows
  - bucket-level metrics by inventory bucket, side class, fair/reservation edge, and quote-distance
  - clean-only stability summary
  - caveated-sample sensitivity summary
  - candidate recommendation markdown
- The contract must define the minimum verdict inputs:
  - coverage / submit count / fill count
  - inventory-increasing vs inventory-reducing fill quality
  - 5s markout and spread capture
  - fill-after-cancel and post-only risk
  - reject / throttle / drop and churn exposure
  - clean-only vs caveated sensitivity
- The contract must explicitly state that the later runner is read-only and default-off.

required verdict taxonomy for later implementation：
- `focused_design_promising`
- `promising_but_needs_parameter_sweep`
- `needs_more_clean_fills`
- `too_conservative_fill_loss`
- `caveated_only`
- `reject`
- `not_decisionable`

required outputs：
- `.workflow/reports/0526T006-business.md`
- A design section in `findings.md` or `task_plan.md` summarizing the accepted focused design contract.
- The report must include:
  - final design objective
  - state variables
  - action outputs
  - decision-input policy
  - evaluation metrics
  - candidate policy skeleton
  - later read-only runner input / output contract
  - verdict rules
  - explicit non-goals
  - whether the design supports a later read-only implementation task

verify：
- `python3 .workflow/build_dashboard.py`
- `git diff --check`
- Manual consistency check that:
  - task status is `执行中` after formal dispatch
  - no code / runner / live / parameter-search / sample collection changes were made
  - design is one focused track, not five independent tracks

done：
- State whether the design supports a later read-only implementation task.
- State that this task does not authorize tiny-live, default-on, live promotion, guard relaxation, or `ready_for_tiny_live_design`.
```
