# 0721T033 DELAYED-HISTORY-QUARANTINE-FAIL-CLOSED-REPAIR

执行线程：
- 总控 auto-loop / 业务执行线程

任务ID：
- 0721T033

标题：
- Bind delayed-history evidence to production timing and require the complete quarantine artifact

简短描述：
- 修复 T032 QA 的两个 P1：
  - `delayed_one_call_history_v1` 必须证明 exact production timing，不能接受 zero-delay relabel；
  - Task 12 必须要求 quarantine artifact 即使零行也存在、header 精确，并 exact-compare 完整 canonical rows。
- 本任务只收紧 evidence verifier，不修改真实查询流程、quote、order、risk、submission envelope 或任何 adaptive activation。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0720T032`（未通过）

规则更新提醒：
- 本任务 offline-only。
- T031 live artifacts immutable，不得修改、重写或离线升级为通过。
- T032 已接受的 censor、history call ordering、budget、historical boundaries 和 strategy invariants 不得回退。
- 独立 QA 通过前不得进行 private read、新 live 或 dynamic/fill/inventory/multi-level activation。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `.workflow/reports/0720T032-qa.md`
- `docs/superpowers/plans/2026-07-17-principal-alignment-p0-p3.md`

是否需要提交代码：
- 需要

提交要求：
- 业务实现和业务报告分开提交。
- 回报必须包含 implementation commit、验证命令和结果。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- Modify: `examples/hyperliquid/hyperliquid_tiny_live_m2_fill_window.py`
- Modify: `examples/hyperliquid/cross_exchange_t024_same_window_acceptance.py`
- Test: `examples/hyperliquid/test_hyperliquid_tiny_live_m2_fill_attribution.py`
- Test: `examples/hyperliquid/test_cross_exchange_t024_same_window_acceptance.py`
- Test: `examples/hyperliquid/test_hyperliquid_tiny_live_m2_event_driven_watcher.py` for virtual-clock regression of the unchanged exact `4.0s` production delay
- Workflow: this task, business report, QA report, fixed QA document, `task_plan.md`, `progress.md`, `findings.md`

action：
- Define one shared exact delayed-history production contract for new tasks:
  - protocol `delayed_one_call_history_v1`;
  - propagation delay `4.0s`;
  - final snapshot reserve `0.5s`;
  - direct rounds `5`;
  - total budget `5.0s`;
  - maximum historical calls per reference `1`.
- Producer audit and Task 12 must reject zero、negative、shorter、longer、missing、non-finite or type-confused delay/reserve values。
- Bind not-before、query deadline、planned/actual wait、call timing and snapshot timing to the exact contract without changing the live executor timing。
- Add the required quarantine path、presence and exact fieldname checks for manager-resting contracts, including a valid header-only zero-row artifact。
- Canonicalize every persisted and independently rebuilt quarantine row using the complete schema and compare the full sorted rows, not only `reason` projections。
- Reject missing、duplicate、malformed、extra-field、reordered/incorrect header、forged bounds、attempt identity、side、row kind、reason or inference scope。
- Keep any persisted or rebuilt quarantine row as a mechanism/evidence failure even when both sides match。
- Preserve the accepted censor artifact contract and estimator replay behavior。

verify：
- A zero-delay hostile terminal budget fails producer audit and Task 12 with a timing-config reason。
- Delay values other than exact `4.0` and reserve values other than exact `0.5` fail for T033+。
- The normal production `4.0/0.5/5/5.0/1` contract remains pass with strict wait/call/snapshot ordering。
- Deleting the empty quarantine artifact from a valid fixture and resealing still fails Task 12。
- Header-only quarantine artifact with exact schema passes when independent rebuild is empty。
- Missing、malformed-header、forged-row and reason-equal-but-fields-different quarantine artifacts fail。
- T031 immutable replay remains blocked；T026 exact acceptance remains pass；T016/T022 remain blocked。
- Dynamic spread、fill feedback、inventory skew、multi-level and actual quote behavior activation remain false。
- Focused and full Hyperliquid regression pass。
- No live、private/account、order、cancel、network、remote or service operation occurs。

stop conditions：
- Any T031 raw artifact modification。
- Any relaxation of censor、quarantine、terminal reference or strict history ordering contracts。
- Any live executor timing or endpoint behavior change。
- Any quote、risk、size、submission、side-set or activation behavior change。
- Any T026/T016/T022 historical boundary regression。
- Any live/private/network/remote/service action。

done：
- Both T032 QA fail-open probes are rejected end to end with focused hostile tests。
- The repaired source is ready for independent QA and only then a separately dispatched fixed-quote observe-only single window。

blockers：
- Independent QA acceptance is required before any new private read/live or Task 8 activation。

commit：
- implementation `7369e16a9b2a7c47a99e7ac6c3e157cc85d192de`

提交信息：
- implementation `Repair exact history and quarantine gates`
