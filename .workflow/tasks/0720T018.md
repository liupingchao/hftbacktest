# 0720T018 ORDER-STATUS-EXCEPTION-FINAL-SNAPSHOT-REPAIR

执行线程：
- 总控 auto-loop / 业务执行线程

任务ID：
- 0720T018

标题：
- Harden malformed order-status evidence and rebuild final operator state from the final account snapshot

简短描述：
- Close the three offline defects identified by T017 QA。
- Make non-string query status exception-safe, reconcile the manager from the exact final open-order/account snapshot, and keep query-filled references unresolved until complete raw fill proof exists。

状态：
- 未通过

执行顺序：
- 当前唯一任务

前置任务：
- `0720T017`（未通过；核心 v3 reconciliation accepted in part, exception/final-status semantics not accepted）

规则更新提醒：
- 本任务 offline-only。
- No live/private/account/order/cancel/network/remote/service action is authorized。
- New bounded live、Task 8 and adaptive/multi-level activation remain locked。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `.workflow/reports/0720T017-qa.md`
- `docs/superpowers/plans/2026-07-17-principal-alignment-p0-p3.md`

是否需要提交代码：
- 是

提交要求：
- 实现和测试提交。
- 业务报告单独提交。
- 回报必须带 implementation commit id 和提交信息。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

source：
- Branch: `cross-exchange`
- Dispatch base: `23f4224b44357de6c8f79b46a14b42be84a6510b`

files：
- `examples/hyperliquid/hyperliquid_maker_order_manager.py`
- `examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- `examples/hyperliquid/hyperliquid_tiny_live_m2_fill_window.py`
- `examples/hyperliquid/cross_exchange_t024_same_window_acceptance.py`
- Focused tests for the modules above
- `.workflow/tasks/0720T018.md`
- `.workflow/reports/0720T018-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

action：
- Require `orderStatus.status` to be a string before exact enum membership in manager、producer and independent acceptance classifiers。
- Contain retrieval and classification exceptions in the manager query evidence path；persist redacted error and return `unknown` instead of raising。
- Add a manager reconciliation API that consumes already-fetched final open orders and user state without making new endpoint calls。
- Rebuild the final manager/operator snapshot from the exact `final_open_orders` and `post_state` passed into shutdown proof：
  - a tracked reference that reappears becomes active/resting or partial according to the final row；
  - an absent cancel-confirmed reference remains terminal；
  - an absent unresolved/filled-query-only reference remains unknown；
  - working exposure and owned count derive from that same snapshot。
- Do not promote query `filled` to manager terminal state without complete raw fill evidence；record query status but keep the reference unknown and blocking。
- Keep valid canceled-query v3 behavior、v2 compatibility、submitted-row cardinality and T016 monotonic replay unchanged。
- Add adversarial tests for list/dict/bool/numeric status values、classifier exceptions、tracked-order reappearance and query-filled/no-raw-fill operator status。

verify：
- Focused manager/watcher/fill/acceptance tests pass。
- Full `python -m pytest -q -p no:cacheprovider examples/hyperliquid` passes。
- Modified modules/tests pass `py_compile`。
- Acceptance CLI `--help` passes。
- Malformed synchronized fixture returns a blocked manifest and never raises。
- Finalizer reappearance fixture shows nonzero owned/working exposure and a blocker in final `live_status`。
- Query-filled/no-raw-fill fixture shows unknown/unresolved order state、zero proven fill and fail-closed lifecycle。
- Valid canceled-query writer path remains pass；v2 producer/acceptance outputs remain exact。
- Exact T016 replay remains byte-preserving、decision `43/43` pass and lifecycle blocked for historical attempt 2。
- `git diff --check` and implementation `git show --check` pass。
- No live/private/account/order/cancel/network/remote/service action occurs。

done：
- Malformed exchange evidence is exception-safe and fail-closed。
- Final operator state and shutdown proof use the same final account facts。
- Query-filled cannot create a proved fill or terminal operator state without complete raw fill evidence。
- T017 accepted sub-results remain intact。
- Independent QA accepts T018 before any new bounded live task。

stop conditions：
- Any malformed status can escape as an exception。
- Final `live_status` can disagree with final tracked open orders or working exposure。
- Query-filled without raw fill proof becomes terminal or nonblocking。
- Valid canceled-query or v2 compatibility regresses。
- T016 historical attempt 2 becomes synthetically proven。
- Strategy formula、signal freshness、edge threshold、risk cap、activation or quote behavior changes。
- Any verification requires live/private/account/order/cancel/network/remote/service。

blockers：
- Independent QA failure keeps new bounded live、Task 8 and adaptive/multi-level locked。
- Final open-orders success with final user-state failure still leaves stale operator exposure。
- Supplied final open row does not override `cancel_requested` to exchange-authoritative resting/partial。

commit：
- implementation `d475fbb261fba912e61a3c39fa2bf95df998ea84`

提交信息：
- `Harden final order status reconciliation`
