```md
执行线程：
- 业务线程-python

任务ID：
- 0514T001

标题：
- Stage 3 market-view acceptance gate 实现

简短描述：
- 在 `maker_acceptance.py` 中新增可选 market-view quality gate，把 T009 fixed sidecar / decision join 的 top5 provenance、join-age、sync-gap、BBO consistency 和 source fields 纳入正式 acceptance 输出。使用既有 `5-13-day-control-30min` 全量样本验证；不重新 live 采集。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- 0513T009 QA 已通过

规则更新提醒：
- 本任务直接实现 Stage 3 gate，不再新增单独 planning-only 任务。`5-13-day-control-30min` 已满足验收输入：T004 manifest/action acceptance、T009 fixed sidecar、joined decisions、source/provenance 字段、明确的 top5 非完全对齐指标。

按线程规则执行：
- .workflow/workflow-kit/thread-report-template.md
- docs/thread-playbook.md
- task_plan.md
- progress.md
- findings.md

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

Allowed files：
- `.workflow/tasks/0514T001.md`
- `.workflow/reports/0514T001-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `.workflow/dashboard.html`
- `.workflow/dispatch_suggestions.md`
- `docs/qa-acceptance-report.md`
- `examples/binance_tick_mm/maker_acceptance.py`
- `examples/binance_tick_mm/test_maker_acceptance.py`
- `local_live_analysis/5-13-day-control-30min/maker_acceptance_stage3.json`

Allowed read-only inputs：
- `local_live_analysis/5-13-day-control-30min/alignment_report_audit_replay.json`
- `local_live_analysis/5-13-day-control-30min/backtest_audit_replay_result.json`
- `local_live_analysis/5-13-day-control-30min/t009_fixed_sidecar/metrics.json`
- `local_live_analysis/5-13-day-control-30min/t009_fixed_sidecar/joined_decisions.metrics.json`
- `local_live_analysis/5-13-day-control-30min/t009_fixed_sidecar/joined_decisions.csv`
- `local_live_analysis/5-13-day-control-30min/t009_fixed_sidecar/top5_sidecar.csv`

Forbidden files / areas：
- strategy rule logic, fair/target formula, risk guards, quote placement behavior
- live scripts, AWS/remote state, new live collection
- core Rust `Event`, `hbt.depth(0)` API, connector local book management
- standard hftbacktest npz `data` schema or py `event_dtype`
- T009 sidecar/bootstrap logic unless a focused test exposes a blocker

Requires separate task：
- 修改 formal pricing/fill research scripts。
- 将 sidecar/join 集成进 `align_live_run.py` 标准流程。
- 改 connector/core API 或 canonical live audit schema。
- 重新采集 live 或 live micro test。
- 策略规则、pricing model 或 queue/fill model 改动。

Fatal Failures：
- 启动 live、修改远端或重新采集数据。
- 把 top5 sidecar 解释为 full L2 equivalence 或 exact queue position proof。
- 让 live audit top5 与 sidecar reconstructed top5 必须逐行 100% 一致才算通过。
- 隐藏 `future_join_count`、`gap_crossed_join_count`、`join_stale` 或 source/provenance 字段。
- 改变原有 action-path hard gate 的语义。

action：
- 判断 `5-13-day-control-30min` 是否满足 Stage 3 验收输入，并在业务回报中说明。
- 扩展 `maker_acceptance.py`：
  - 保留原 action/planned/reject/throttle/working-order/replay-lag hard gates。
  - 新增可选 CLI 参数读取 sidecar metrics 与 joined-decision metrics。
  - 新增 market-view checks：`first_valid_update_aligned`、`depth_pu_mismatch_count`、`final_data_row_mapping_coverage`、`decision_join_coverage`、`future_join_count`、`join_missing_count`、`gap_crossed_join_count`、BBO mismatch、stale join count / stale rate、top5 join age p99、source/provenance presence。
  - 新增 alignment top5 diagnostics 输出：best bid/ask top5 match rate、top5 tick/qty match rate、top5 qty diff percentiles。
  - 输出 market-view classification：`passes_pricing_research_market_view`、`limited_pricing_research`、`compressed_action_path_only` 或 `unusable`。
  - 分类必须允许 top5 tick/qty 非 100% 完全对齐，但必须显式报告。
- 增加 focused tests 覆盖 pass、limited/fail、缺失 metrics、stale/age 诊断和原 hard gate 不回退。
- 用 `5-13-day-control-30min` 全量跑 `maker_acceptance.py` 生成 `maker_acceptance_stage3.json`。
- 刷新 dashboard。

verify：
- `python -m pytest examples/binance_tick_mm/test_maker_acceptance.py`
- `python examples/binance_tick_mm/maker_acceptance.py --alignment-report local_live_analysis/5-13-day-control-30min/alignment_report_audit_replay.json --backtest-result local_live_analysis/5-13-day-control-30min/backtest_audit_replay_result.json --sidecar-metrics local_live_analysis/5-13-day-control-30min/t009_fixed_sidecar/metrics.json --joined-decision-metrics local_live_analysis/5-13-day-control-30min/t009_fixed_sidecar/joined_decisions.metrics.json --out local_live_analysis/5-13-day-control-30min/maker_acceptance_stage3.json`
- `python3 .workflow/build_dashboard.py`

Acceptance metrics：
- 原 maker hard gates 仍通过。
- Stage 3 输出包含 `market_view` section。
- `first_valid_update_aligned=true`
- `depth_pu_mismatch_count=0`
- `final_data_row_mapping_coverage=1.0`
- `decision_join_coverage=1.0`
- `future_join_count=0`
- `join_missing_count=0`
- `gap_crossed_join_count=0`
- top5 tick/qty 非完全一致时仍被报告为 diagnostics，不作为本任务 fatal failure。
- `5-13-day-control-30min` 应分类为 pricing research usable 的 top5 market-view candidate，且明确不是 full L2 / exact queue proof。

done：
- 回报必须说明为什么 `5-13-day-control-30min` 足够支撑 Stage 3 验收。
- 回报必须列出 Stage 3 新增输出字段、阈值/分类结果、全量样本结果和剩余边界。
- 回报必须明确没有 live、没有策略变更、没有 core/connector/API/npz schema 变更。
- 回报必须写 `commit id` 和提交信息。
```
