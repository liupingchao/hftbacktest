```md
执行线程：
- 业务线程-research

任务ID：
- 0616T007

标题：
- Hyperliquid awsserver1 live-capable preflight dry-run / artifact pullback

简短描述：
- 在 `0616T006` QA 通过后，对 `awsserver1` 执行 Hyperliquid tiny-live 前置 dry-run：检查主机、分支、Python/conda、时间、磁盘、公共网络、远端 artifact 目录、checksum 和 pullback。本任务不读取凭证、不调用私有接口、不查询账号、不下单、不撤单、不启动 live bot。

状态：
- 待执行

执行顺序：
- 当前唯一任务

前置任务：
- `0616T006` QA 必须为 `已通过`。

规则更新提醒：
- 请先阅读 `.workflow/workflow-kit/workflow-manual.md`、`.workflow/workflow-kit/task-dispatch-template.md`、`.workflow/workflow-kit/thread-report-template.md`、`.workflow/workflow-kit/qa-acceptance-template.md`、`task_plan.md`、`progress.md`、`findings.md`、`docs/hyperliquid_tiny_live_0616T006_T008_auto_loop.md` 后再执行本任务。
- 本任务是 `awsserver1` dry-run / preflight，不是 live execution。

按线程规则执行：
- `AGENTS.md`
- `docs/hyperliquid_tiny_live_0616T006_T008_auto_loop.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0616T007.md`
- `.workflow/reports/0616T007-business.md`
- `docs/hyperliquid_tiny_live_0616T006_T008_auto_loop.md`
- `examples/hyperliquid/**`
- `local_live_analysis/hyperliquid_awsserver1_preflight_dry_run_0616T007/**`
- `task_plan.md`
- `progress.md`
- `findings.md`

action：
- Consume accepted `0616T006` operator packet and the auto-loop boundary.
- Use SSH alias `awsserver1` to run host preflight checks only.
- Record hostname, user, working directory candidates, git branch if repo is present, Python/conda availability, UTC/local time, timezone, disk space, process owner, and artifact path.
- Check public/read-only network reachability only; private Hyperliquid endpoints, account endpoints, credentials, signing, nonce, user-stream, order placement, cancellation, amendment, and live bot startup are forbidden.
- Create a remote dry-run artifact directory for `0616T007`.
- Write remote dry-run evidence files that contain no secrets and no private/account/order data.
- Generate remote checksums and pull artifacts back to `local_live_analysis/hyperliquid_awsserver1_preflight_dry_run_0616T007/`.
- Locally validate pulled-back artifacts and fail closed on host mismatch, branch mismatch, missing checksum, missing dry-run marker, any private/order/account/live claim, or missing no-order boundary.

acceptance criteria：
- SSH to `awsserver1` succeeds and host identity is recorded.
- The task proves dry-run artifact generation on `awsserver1` and local pullback.
- Public/read-only connectivity is checked or a clear blocker is reported.
- The report records repo path/branch and Python/conda state; branch must be `cross-exchange` if the repo is present.
- All artifacts preserve no-private/no-credential/no-account/no-order/no-cancel/no-live-bot boundary.
- Final recommendation must be exactly one of:
  - `hyperliquid_awsserver1_preflight_dry_run_ready_for_qa`
  - `hyperliquid_awsserver1_preflight_dry_run_needs_revision`
  - `hyperliquid_awsserver1_preflight_dry_run_blocked`

verify：
- Use `/home/liushuai/workspace/hftbacktest/.conda-envs/hft-py38/bin/python`, not default `python`, for local validation code.
- Run focused local validator/help commands if code is added.
- Validate JSON artifacts with `/home/liushuai/workspace/hftbacktest/.conda-envs/hft-py38/bin/python -m json.tool <artifact>.json`.
- Recalculate or verify checksums for pulled-back artifacts.
- Boundary text check for no live/order/private authorization.
- `git diff --check`

done：
- 回报 SSH、host、repo/branch、Python/conda、public connectivity、remote artifact dir、pullback path、本地验证结果。
- 明确说明没有读取凭证、没有调用私有接口、没有账号查询、没有下单/撤单/改单、没有 live bot。
- 明确说明 `0616T008` 是否可作为下一步创建；只有本任务 QA 通过后才可创建。
```
