```md
执行线程：
- 业务线程-research

任务ID：
- 0626T001

标题：
- Effective-horizon label repair and strict 1000ms signal retry

简短描述：
- 诊断并修复 `0625T003` 把 nominal `1000ms` 标签实际当成约 `5000ms` 标签验收的问题。
- 本任务只允许方案 A：保持 `1000ms` horizon，新增 explicit effective-age tolerance gate，并用 `1000ms +/- 250ms` 标签重跑离线 signal acceptance；不得直接把 contract 改成 `5000ms`。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0625T003` 业务执行已产出 `signal_contract_needs_repair`，用户/总控指定进入方案 A 修复；`0625T004` 仍未解锁。

规则更新提醒：
- 请先阅读 `.workflow/workflow-kit/workflow-manual.md`、`.workflow/workflow-kit/task-dispatch-template.md`、`.workflow/workflow-kit/thread-report-template.md`、`.workflow/workflow-kit/qa-acceptance-template.md`、`task_plan.md`、`progress.md`、`findings.md` 后再执行本任务。

按线程规则执行：
- `AGENTS.md`
- `docs/thread-playbook.md`

是否需要提交代码：
- 需要

提交要求：
- 自测和离线 artifact 生成通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0626T001.md`
- `.workflow/reports/0626T001-business.md`
- `examples/hyperliquid/binance_led_pricing_signal_runner.py`
- `examples/hyperliquid/test_binance_led_pricing_signal_runner.py`
- `local_live_analysis/cross_exchange_mvp_effective_horizon_repair_0626T001/**`
- `task_plan.md`
- `progress.md`
- `findings.md`

read-only inputs：
- `local_live_analysis/cross_exchange_mvp_sample_expansion_0625T002/**`
- `local_live_analysis/cross_exchange_mvp_signal_acceptance_0625T003/**`

hard boundary：
- Offline public-only no-submit.
- Keep target horizon at `1000ms`; do not redefine the signal contract to `5000ms`.
- Future Hyperliquid values are labels only; they must not be used as decision inputs.
- No live orders, no credentials, no private/account/order/cancel endpoints, no live client initialization, no remote refresh, no remote final gate, no T008 ledger claim.
- No strategy/live behavior change, no quote-distance/cap relaxation, no taker/crossing, no canary, no M3, no stable-PnL, no default-on, no promotion.
- Do not silently expand into production shadow, data recollection, or live calibration.

action：
- Add an explicit strict effective-age label policy for `1000ms +/- 250ms`.
- Diagnose per sample how many T002 complete contexts have near-target labels and how many are excluded as too far from target.
- Re-run the same fixed train/evaluation signal acceptance only on rows passing the strict effective-age gate.
- Report whether the current T002 package supports a true `1000ms` signal contract.
- If strict near-target row coverage is insufficient, fail closed as `signal_contract_needs_repair` and state the required next evidence.

acceptance criteria：
- Runner exposes a deterministic task-scoped strict `1000ms +/- 250ms` repair mode.
- The mode emits label-policy diagnostics, strict score rows, train/evaluation summaries, and a manifest.
- Rows outside effective-age tolerance are excluded from signal acceptance and are not used to claim a `1000ms` signal.
- Final recommendation is exactly one of:
  - `signal_contract_accepted_for_shadow`
  - `signal_contract_needs_repair`
  - `signal_contract_rejected`
- `0625T004` remains locked unless strict `1000ms` evidence returns `signal_contract_accepted_for_shadow`.

verify：
- `python -m pytest examples/hyperliquid/test_binance_led_pricing_signal_runner.py examples/hyperliquid/test_cross_exchange_sample_expansion.py -q`
- `python examples/hyperliquid/binance_led_pricing_signal_runner.py --help`
- Run the strict effective-horizon repair mode over the accepted T002 package.
- Parse JSON manifests with `python -m json.tool`.
- Validate CSV schemas, non-empty files, row counts, boundary flags, effective-age tolerance flags, and recommendation enum.
- `python -m py_compile examples/hyperliquid/binance_led_pricing_signal_runner.py`
- `git diff --check`

done：
- Report target horizon, tolerance, strict near-target row counts per sample, excluded off-target row counts, train/evaluation boundary, and final recommendation.
- State whether T004 may be created.
- Explicitly state that 0626T001 did not redefine horizon to 5000ms, did not modify live behavior, did not place orders, did not read credentials, and did not authorize shadow/live promotion.
- End in `待验收` if execution and local verification complete; use `阻塞` if required T002/T003 local inputs are unavailable; use `未通过` for invalid evidence or boundary violation.
- Include commit id and commit message.
```
