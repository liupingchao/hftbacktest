```md
执行线程：
- 业务线程-research

任务ID：
- 0616T005

标题：
- Hyperliquid tiny-live protocol design and human approval gate

简短描述：
- 基于 `0616T002-T004` 的 readiness / validator / shutdown dry-run 结果，定义 Hyperliquid tiny-live protocol design 和人工批准 gate。本任务只做协议设计，不调用 private endpoint、不使用 credentials、不下单、不撤单、不启动 live。

状态：
- 待执行

执行顺序：
- 当前唯一任务；auto loop step 5；本任务完成后必须停止在 human approval gate。

前置任务：
- `0616T004` QA 必须为 `已通过`。

规则更新提醒：
- 本任务不能开启真实 live window。
- 真实订单任务必须由总控另行批准 symbol、caps、account、host 和 real orders allowed。

是否需要提交代码：
- 需要

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

files：
- `.workflow/tasks/0616T005.md`
- `.workflow/reports/0616T005-business.md`
- `docs/hyperliquid_tiny_live_protocol_design.md`
- `local_live_analysis/hyperliquid_tiny_live_protocol_design_0616T005/**`

action：
- Define the tiny-live protocol structure and evidence requirements.
- Define human approval fields as pending.
- Define preflight checklist, risk cap schema, stop conditions, artifact capture, shutdown evidence, and post-run analysis requirements.
- Explicitly block live execution until a later separate task is approved.

acceptance criteria：
- Protocol design references `0616T002-T004` gates.
- Required approval fields include symbol, max notional, max order size, max position, max loss, duration, host, account scope, and whether real orders are allowed.
- All approval fields remain `pending_controller_approval`.
- Final recommendation must be exactly one of:
  - `hyperliquid_tiny_live_protocol_design_ready_for_qa`
  - `hyperliquid_tiny_live_protocol_design_needs_revision`
  - `hyperliquid_tiny_live_protocol_design_blocked`

verify：
- `python -m json.tool local_live_analysis/hyperliquid_tiny_live_protocol_design_0616T005/tiny_live_protocol_manifest.json`
- Check required CSV/markdown artifacts are non-empty.
- Boundary text check for no live/order authorization.
- `git diff --check`
```
