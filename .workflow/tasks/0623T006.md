```md
执行线程：
- 业务线程-live

任务ID：
- 0623T006

标题：
- M2 live-compatible fair-mid source acceptance gate

简短描述：
- 在 `0623T005` 决定继续 `0 tick` touch-only repaired gates 后，补齐 `0623T004` 留下的 blocker：缺少可在 watcher submit 决策时调用的 live-compatible fair-mid / edge signal source。
- 本任务允许实现或正式验收一个 `edge_signal_provider`，但只用本地 mock / replay / public-source-compatible 验证；不跑 live、不读凭据、不调用私有或订单接口、不改变 quote distance。

状态：
- 待验收

执行顺序：
- 当前唯一任务；必须在 `0623T005` QA 已通过后执行。

前置任务：
- `0623T005` 已通过，并明确推荐本任务作为下一单任务。
- `0623T004` fair-value edge gate 已通过，且其 `edge_signal_provider` 接口和 fail-closed evaluator 可作为接入点。

规则更新提醒：
- 请先阅读 `.workflow/workflow-kit/workflow-manual.md`、`.workflow/workflow-kit/task-dispatch-template.md`、`.workflow/workflow-kit/thread-report-template.md`、`.workflow/workflow-kit/qa-acceptance-template.md`、`task_plan.md`、`progress.md`、`findings.md` 后再执行本任务。

按线程规则执行：
- `AGENTS.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- `examples/hyperliquid/test_hyperliquid_tiny_live_m2_event_driven_watcher.py`
- 可新增 task-scoped fair-mid / edge-source focused tests
- `local_live_analysis/hyperliquid_tiny_live_m2_fair_mid_source_0623T006/**`
- `.workflow/reports/0623T006-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

read-only inputs：
- `.workflow/tasks/0623T004.md`
- `.workflow/reports/0623T004-business.md`
- `.workflow/reports/0623T004-qa.md`
- `.workflow/tasks/0623T005.md`
- `.workflow/reports/0623T005-business.md`
- `.workflow/reports/0623T005-qa.md`
- `docs/qa-acceptance-report.md`
- Accepted read-only pricing / signal evidence from `0617T005`, `0617T006`, and canonical pricing-signal artifacts, only as schema / threshold / calibration context.

hard boundary：
- No live orders, no credential reads, no private/account/order endpoint calls, no remote refresh, no final gate rerun, no live data collection, no taker/crossing, no one-tick-back implementation, no inside-spread implementation, no quote-distance change, no cap relaxation, no default-on behavior, no promotion, no stable PnL / M3 claim.
- Do not treat offline `pricing_signal_rows.csv`, optimistic proxy output, future markout, realized PnL, or oracle horizon as a live decision-time fair-mid source.
- If a fresh decision-time fair-mid cannot be produced from allowed public / in-process state, fail closed and report the blocker instead of fabricating edge.

action：
- Define the accepted live-compatible fair-mid source contract for T004 edge gate:
  - `symbol` / `target_symbol`
  - `horizon_ms=1000`
  - `signal_ts_ms`
  - `fair_mid_px`
  - `source`
  - optional diagnostics such as `hl_mid_px`, `binance_mid_px`, `basis_mid_ticks`, `lead_move_ticks`, `source_age_ms`, and `source_status`
- Implement or formally accept a task-scoped `edge_signal_provider` that can be called by the watcher-local edge gate at pre-submit decision time.
- The provider should use decision-time-visible public / in-process state only. A minimal acceptable v1 may combine current Hyperliquid mid, current Binance lead mid, current basis, and an accepted conservative lead-move projection; missing or stale inputs must return no signal / block.
- Add strict fail-closed validation for missing source, stale source, wrong symbol, wrong horizon, missing fair mid, future timestamp, invalid quote/tick, insufficient edge, and provider exception.
- Generate local non-live artifacts under `local_live_analysis/hyperliquid_tiny_live_m2_fair_mid_source_0623T006/` covering:
  - positive fresh fair-mid pass through the T004 edge gate
  - missing source block
  - stale source block
  - wrong symbol / wrong horizon block
  - insufficient edge block
  - missing Binance / Hyperliquid public state block if the provider depends on both
- Update `task_plan.md`, `progress.md`, and `findings.md` with the provider contract, accepted / blocked cases, and remaining live blockers.

verify：
- `python -m pytest examples/hyperliquid/test_hyperliquid_tiny_live_m2_event_driven_watcher.py -q`
- Run any newly added fair-mid / edge-source focused tests.
- `python -m py_compile examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- `python examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py --help`
- Validate generated local JSON/CSV artifacts for the T006 scenarios.
- `git diff --check`

done：
- 回报 provider contract、source formula / accepted inputs、freshness rules、pass/block matrix、artifact 路径、remaining blockers、whether live edge source is accepted, whether M2 remains blocked、commit id、提交信息。
- 明确说明本任务不授权 live execution、quote-distance change、one-tick-back、inside-spread、cap relaxation、M3 readiness、stable PnL、default-on behavior 或 promotion。
```
