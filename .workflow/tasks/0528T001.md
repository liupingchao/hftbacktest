```md
执行线程：
- 业务线程-python

任务ID：
- 0528T001

标题：
- Read-only inventory-aware quote placement runner implementation

简短描述：
- 按 `0526T006` 已通过的 `inventory_aware_quote_placement_request` 设计合同，实现一个离线只读、default-off runner，用现有 current-format 样本生成 candidate decision rows、bucket metrics、clean-only stability、caveated sensitivity、participation/fill-loss、inventory recovery quality、quote mechanics safety 和 recommendation。不得改策略行为、不得 live、不得 parameter search、不得 default-on 或 promotion。

状态：
- 待验收

执行顺序：
- 当前下一任务；与 `0527T001` replay audit bloat 占位任务独立，不互相阻塞。

前置任务：
- `0526T006` QA 已通过。
- `0526T005` QA 已通过。
- `0526T004` QA 已通过。
- `0519T006` QA 已通过，quote-update audit fields 已落地。

规则更新提醒：
- 请先阅读规则文档后再执行本任务。
- 本任务是 read-only/default-off offline runner implementation，不是策略实现、参数搜索、live、tiny-live、default-on 或 promotion 任务。
- `0526T006` 是本任务唯一的 candidate policy contract；不得临场改成新的策略假设。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `docs/thread-playbook.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交代码、workflow/task/report/dashboard 更新。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

Allowed files：
- `.workflow/tasks/0528T001.md`
- `.workflow/reports/0528T001-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `.workflow/dashboard.html`
- `.workflow/dispatch_suggestions.md`
- `examples/binance_tick_mm/inventory_aware_quote_placement.py`
- `examples/binance_tick_mm/test_inventory_aware_quote_placement.py`
- `examples/binance_tick_mm/test_quote_adjustment_replay.py` only if sharing existing fixtures is simpler and edits stay focused
- `local_live_analysis/stage9i_inventory_aware_quote_placement_0528T001/**`

Allowed read-only inputs：
- `.workflow/tasks/0526T006.md`
- `.workflow/reports/0526T006-business.md`
- `.workflow/reports/0526T006-qa.md`
- `local_live_analysis/stage9h_maker_edge_triage_0526T005/**`
- `local_live_analysis/stage9g_min_move_parameter_sweep_0526T004/**`
- `local_live_analysis/stage9d_candidate_bucket_refinement_0526T002_aggregate/**`
- `local_live_analysis/stage9c_multi_sample_validation_0521T002/**`
- Current-format sample directories:
  - `local_live_analysis/5-19-day-control-30min/**`
  - `local_live_analysis/5-19-night-active-30min-a/**`
  - `local_live_analysis/5-19-night-active-30min-b/**`
  - `local_live_analysis/5-19-night-active-30min-c/**`
  - `local_live_analysis/5-21-day-control-60min/**`
  - `local_live_analysis/5-26-active-minmove-control-30min-a/**`
  - `local_live_analysis/5-26-active-minmove-control-60min-a/**`
  - `local_live_analysis/5-26-active-makeredge-control-180min-a/**`
  - `local_live_analysis/5-26-active-minmove-control-30min-b/**`
- Existing helper/runner references:
  - `examples/binance_tick_mm/maker_edge_triage.py`
  - `examples/binance_tick_mm/quote_adjustment_replay.py`
  - `examples/binance_tick_mm/execution_outcome_labels.py`
  - `examples/binance_tick_mm/quote_anchor_safety.py`

Forbidden files / areas：
- Strategy behavior, live/backtest quote placement behavior, production defaults, live configs, fair/target formula, risk/position logic.
- Core/connector APIs, standard npz schema, converter behavior, replay lifecycle repair, queue/touch repair.
- `0527T001` replay audit bloat repair implementation.
- Any live/tiny-live run, default-on behavior, parameter search, production strategy behavior change, guard relaxation, or promotion claim.

Requires separate task：
- Enabling any candidate in strategy code.
- Running parameter search after this runner's first verdict.
- Running live/tiny-live.
- Changing quote placement, risk, fair/reservation, inventory, order sizing, or production defaults.
- Repairing replay audit repeated `cancel_ack` bloat or Stage 6 scalable input.

Fatal Failures：
- Modifying live/backtest strategy behavior.
- Using future fill, future markout, future spread capture, fill-after-cancel outcome, same-sample PnL feedback, exact queue position, or hidden queue assumptions as decision inputs.
- Treating caveated samples as strict-clean proof.
- Producing `ready_for_tiny_live_design`, live/default-on, guard-relaxation, or promotion claims.
- Turning this into a parameter search rather than implementing the fixed `0526T006` policy skeleton.

policy contract：
- Implement `inventory_aware_quote_placement_request` exactly as a read-only classifier/request generator:
  - `inventory_bucket`: `flat`, `mild_skew`, `large_skew_or_low_score`
  - `side_class`: `add_side`, `reduce_side`, `flat_side`
  - `edge_bucket`: `edge_strong_favorable`, `edge_weak_or_neutral`, `edge_adverse`
  - `quote_distance_bucket`: `touch`, `one_tick_tight`, `step_back_gt1`, `outside_or_no_quote`
  - `safety_context`: post-only / anchor / latency / stale context as gates only, not alpha
- Outputs per decision row:
  - `side_preference_request`: `prefer_reduce_side`, `allow_both_sides`, `discourage_add_side`
  - `quote_distance_request`: `keep_baseline`, `allow_touch`, `prefer_one_tick_tight`, `prefer_step_back_gt1`, `outside_or_no_quote`
  - `size_request`: `baseline_size`, `reduce_add_side_size`, `suppress_add_side`, `preserve_reduce_side_size`
  - `candidate_action`: `no_change`, `request_quote_adjustment`, `request_size_adjustment`, `request_side_priority`
- Must preserve no-change fallback when required fields are missing, side is reduce-side with weak/neutral non-adverse edge, safety context is unsafe, or the only support would be caveated-only.

implementation action：
- Add a new CLI runner:
  - `examples/binance_tick_mm/inventory_aware_quote_placement.py`
- CLI requirements:
  - `--output-dir`
  - optional `--sample` repeatable or default current-format sample set from this task
  - `--include-caveated` default true for sensitivity output, but clean-only summary must remain primary
  - `--help`
- The runner should load existing Stage 5 execution labels and Stage 5C safety diagnostics where available, use decision-time-visible audit fields only for request generation, then join evaluation labels after the fact.
- If a sample lacks required artifacts, classify it explicitly in manifest / coverage rather than silently dropping it.
- It may use proxy buckets already present in existing labels/audit fields, but must name any proxy limitation in the manifest and recommendation.

required output directory：
- `local_live_analysis/stage9i_inventory_aware_quote_placement_0528T001/`

required artifacts：
- `run_manifest.json`
- `candidate_decision_rows.csv`
- `bucket_metrics_by_inventory_side_edge_distance.csv`
- `clean_only_stability_summary.csv`
- `caveated_sample_sensitivity.csv`
- `participation_and_fill_loss.csv`
- `inventory_recovery_quality.csv`
- `quote_mechanics_safety.csv`
- `candidate_recommendation.md`

required verdict taxonomy：
- `focused_design_promising`
- `promising_but_needs_parameter_sweep`
- `needs_more_clean_fills`
- `too_conservative_fill_loss`
- `caveated_only`
- `reject`
- `not_decisionable`

required evaluation metrics：
- Coverage / participation:
  - decision rows covered
  - submit count
  - fill count
  - fill rate
  - candidate action coverage
  - participation/fill loss vs baseline buckets
- Fill quality:
  - 5s side-adjusted markout
  - spread capture
  - fill-after-cancel rate
  - time-to-fill buckets when available
- Inventory:
  - inventory-increasing fills
  - inventory-reducing fills
  - recovery-side fill quality
  - inventory cycle exposure where available
- Mechanics / safety:
  - quote-distance x inventory frontier
  - post-only risk after recheck
  - reject/throttle/drop exposure
  - churn and cancel/readd exposure where available
- Stability:
  - clean-only primary verdict
  - caveated-sample sensitivity
  - per-sample direction consistency
  - fill-mass sufficiency

verify：
- `python examples/binance_tick_mm/inventory_aware_quote_placement.py --help`
- Focused pytest for the new policy/runner:
  - `python -m pytest examples/binance_tick_mm/test_inventory_aware_quote_placement.py -q`
- Run the new runner on the accepted sample set and generate the required output directory.
- `python3 .workflow/build_dashboard.py`
- `git diff --check`

done：
- Business report must state:
  - runner path
  - output directory
  - generated artifacts
  - sample set and caveated samples
  - clean-only verdict and caveated sensitivity verdict
  - whether the result supports a later parameter-design task, needs more clean fills, rejects the design, or is not decisionable
  - explicit no-live / no-default-on / no-strategy-change / no-parameter-search / no-promotion boundary
```
