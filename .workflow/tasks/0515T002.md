```md
执行线程：
- 业务线程-python

任务ID：
- 0515T002

标题：
- Replay lifecycle repair design / implementation plan

简短描述：
- 基于 `0515T001` 的只读 mismatch 诊断结果，制定一个 replay lifecycle repair 的 planning-only 合同，目标是把 cancel-request / cancel-ack / terminal-state / long-horizon persistence 这些候选 repair 方向收敛成最小可实施边界，并明确后续真正的 repair implementation task 该怎么切分。本任务只做设计，不修改 replay、不新跑 replay sweep、不补采样本、不改策略。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0515T001` QA 已通过

规则更新提醒：
- 本任务是 planning-only repair design。
- 不允许实现 replay repair、不允许修改 replay fill model / lifecycle state machine / cancel-ack handling。
- 不允许新跑 replay sweep、不允许 live 采集、不允许策略变更。
- 目标是先把 repair hypotheses、touchpoints、validation plan 和实施边界写成合同，再决定是否需要单独的 repair implementation task。

按线程规则执行：
- .workflow/workflow-kit/thread-report-template.md
- docs/thread-playbook.md
- task_plan.md
- progress.md
- findings.md

是否需要提交代码：
- 需要

提交要求：
- 自测/检查通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

Allowed files：
- `.workflow/tasks/0515T002.md`
- `.workflow/reports/0515T002-business.md`
- task_plan.md
- progress.md
- findings.md
- `.workflow/dashboard.html`
- `.workflow/dispatch_suggestions.md`
- docs/qa-acceptance-report.md

Allowed read-only inputs：
- `.workflow/reports/0515T001-business.md`
- `.workflow/reports/0515T001-qa.md`
- `.workflow/reports/0514T008-business.md`
- `.workflow/reports/0514T008-qa.md`
- `.workflow/reports/0514T007-business.md`
- `.workflow/reports/0514T007-qa.md`
- `local_live_analysis/5-13-day-control-30min/stage6c_replay_lifecycle_mismatch_0515T001/**`
- `local_live_analysis/5-13-day-control-30min/stage6_execution_calibration_0514T007/**`
- `local_live_analysis/5-13-day-control-30min/stage5_execution_outcome_labels_0514T005/**`
- `local_live_analysis/5-13-day-control-30min/audit_live_5-13-day-control-30min.csv`
- `local_live_analysis/5-13-day-control-30min/out/backtest_audit_replay/audit_bt_audit_replay.csv`
- `local_live_analysis/5-13-day-control-30min/t009_fixed_sidecar/**`

Forbidden files / areas：
- `examples/binance_tick_mm/*.py`
- `local_live_analysis/**` generated outputs beyond reading existing artifacts
- strategy behavior, fair/target formulas, configs, risk guards, quote placement behavior
- live scripts, AWS/remote state, live collection
- replay engine behavior / backtest fill model implementation
- core Rust, connector, py `event_dtype`, standard npz schema

Requires separate task：
- 实现 replay fill/cancel lifecycle repair code。
- 修改 replay fill model / lifecycle state machine / cancel-ack handling。
- 新跑 replay sweep 或更多 replay experiments。
- 新采集 current-format 样本。
- 策略实现、quote-adjustment promotion、inventory redesign。

Fatal Failures：
- 在 planning-only 任务里直接修改 replay / fill model / strategy。
- 把 repair design 直接写成已验证修复结论。
- 跳过 `0515T001` 已经识别出的 mismatch hot spots 与 lifecycle 偏差。
- 把本任务扩展为 sample-collection 或 live 判断。

action：
- 基于 `0515T001` 的只读 diagnosis 结果，产出 replay lifecycle repair design contract：
  - 归纳并排序 repair hypotheses：
    - cancel-ack timing
    - terminal-state cutoff
    - long-horizon persistence
    - cancel-requested fill eligibility
    - remaining qty / partial lifecycle semantics
  - 定义最小可实施 repair scope：
    - 哪些问题先不修
    - 哪些问题必须先修
    - 哪些问题可留到后续 task
  - 给出 repair implementation 前需要的验证表 / 成功标准 / 失败标准
  - 明确后续 implementation task 的输入、输出和 non-goals
- 输出至少这些产物：
  - `REPLAY_LIFECYCLE_REPAIR_DESIGN.md`
  - `repair_hypothesis_matrix.csv`
  - `repair_touchpoint_candidates.csv`
  - `repair_validation_plan.md`
  - `repair_task_boundary.md`
  - `run_manifest.json`
- 产物必须明确：
  - 为什么当前不能直接进入 repair implementation
  - 为什么不能先做 sample expansion
  - 哪些修复方向最可能缩小 replay-only fills / fill-after-cancel / terminal-state mismatch

verify：
- 人工检查 `.workflow/tasks/0515T002.md` 和 `.workflow/reports/0515T002-business.md` 是否完整。
- `python3 .workflow/build_dashboard.py`
- `git diff --check`

done：
- 回报必须明确 repair hypotheses、优先级、最小 repair scope、验证计划和后续 implementation 边界。
- 回报必须明确本任务未实现 replay 修复、未运行新 replay、未补采样本、未改策略、未启动 live。
- 回报必须写 `commit id` 和提交信息。
```
