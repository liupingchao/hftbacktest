# 0720T024 STRICT-HISTORICAL-REFERENCE-ROW-CLASSIFICATION

执行线程：
- 总控 auto-loop / 业务执行线程

任务ID：
- 0720T024

标题：
- Reject conflicting or malformed Hyperliquid historical rows before terminal selection

简短描述：
- Close the single T023 QA P1 in manager、producer and independent acceptance。
- Classify every historical row relative to the expected reference so a shared oid/cloid conflict or malformed identity cannot be skipped as foreign。

状态：
- 未通过

执行顺序：
- 当前唯一任务

前置任务：
- `0720T023`（未通过；bounded recovery accepted，strict historical row classification remains open）

规则更新提醒：
- 本任务 offline-only。
- No live/private/account/order/cancel/network/remote/service action is authorized。
- Private historical read、new bounded live、Task 8 and adaptive/multi-level activation remain locked。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `.workflow/reports/0720T023-qa.md`
- `docs/superpowers/plans/2026-07-17-principal-alignment-p0-p3.md`

是否需要提交代码：
- 是

提交要求：
- 实现和测试提交。
- 业务报告单独提交。
- 回报必须带 implementation commit id 和提交信息。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

source：
- Branch: `cross-exchange`
- Dispatch base: `bf1a7dfa98594cf14fdd236ced2bc4393133acef`

files：
- `examples/hyperliquid/hyperliquid_tiny_live_real_order_executor.py`
- `examples/hyperliquid/hyperliquid_maker_order_manager.py`
- `examples/hyperliquid/hyperliquid_tiny_live_m2_fill_window.py`
- `examples/hyperliquid/cross_exchange_t024_same_window_acceptance.py`
- `examples/hyperliquid/test_hyperliquid_tiny_live_real_order_executor.py`
- `examples/hyperliquid/test_hyperliquid_maker_order_manager.py`
- `examples/hyperliquid/test_hyperliquid_tiny_live_m2_fill_attribution.py`
- `examples/hyperliquid/test_cross_exchange_t024_same_window_acceptance.py`
- `.workflow/tasks/0720T024.md`
- `.workflow/reports/0720T024-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

action：
- Classify every historical row for one expected reference as exactly one of：
  - exact；
  - fully-disjoint well-formed foreign；
  - reference-conflicting；
  - malformed or indeterminate reference。
- Ignore only fully-disjoint well-formed foreign rows。
- If any row shares the expected oid or cloid but another supplied identity differs，fail closed even if a second clean exact row exists。
- If oid/cloid aliases conflict、identity types are malformed、the order envelope is malformed or the row cannot be proven fully disjoint，fail closed。
- Preserve per-alias reference tokens during redaction so producer and acceptance can independently detect alias conflict without persisting raw oid/cloid values。
- Require one and only one clean exact row after all relevant rows pass classification。
- Apply the same classification independently in manager、producer and acceptance。
- Preserve official nested status parsing、five-second/five-round budget、one-history-call limit、canonical result、post-history snapshot and filled-without-raw-fill semantics。
- Add adversarial tests for：
  - same oid / different cloid plus clean canceled row；
  - same cloid / different oid plus clean canceled row；
  - oid and cloid alias conflicts；
  - malformed identity/envelope；
  - clean exact plus fully disjoint foreign rows；
  - conflicting filled plus canceled rows。

verify：
- Focused manager/fill/acceptance tests pass。
- Full `python -m pytest -q -p no:cacheprovider examples/hyperliquid` passes。
- Modified modules/tests pass `py_compile`。
- Acceptance CLI `--help` passes。
- Manager、producer and acceptance reject every shared-identity conflict and malformed/indeterminate row。
- Clean exact plus well-formed fully disjoint foreign rows pass。
- T023 accepted sub-results and SDK readiness remain green。
- T016/T022 exact replay remains byte-preserving、offline and blocked with decision `43/43` and lifecycle `49 pass / 12 fail`。
- `git diff --check` and implementation `git show --check` pass。
- No live/private/account/order/cancel/network/remote/service action occurs。

done：
- Historical fallback cannot cherry-pick a clean terminal row while ignoring a conflicting fact for the same reference。
- Manager、producer and acceptance independently enforce the same strict row classification。
- Independent QA accepts T024 before any private historical read or new bounded live。

stop conditions：
- Same oid/different cloid or same cloid/different oid can still be ignored。
- Alias conflict or malformed row can coexist with an accepted exact row。
- A truly disjoint well-formed foreign row blocks a valid exact result。
- T023 bounded audit、SDK、title、T016/T022 replay or strategy/risk/activation behavior regresses。
- Any verification requires live/private/account/order/cancel/network/remote/service。

blockers：
- Independent QA failure keeps private historical recovery、new bounded live、Task 8 and adaptive/multi-level activation locked。

commit：
- `4b9d98452168f082599c033e41e461a8538ab467`

提交信息：
- `Harden historical reference row classification`
