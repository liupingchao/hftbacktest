# 任务派发

执行线程：
- 业务线程-python/cross-exchange-research

任务ID：
- 0730T015

标题：
- SKHYNIX-R1-ALIGNMENT-ACCEPTANCE

简短描述：
- 在已验收 R0 event store 上完成 alignment acceptance，冻结可用 horizon、
  freshness tier 和 top-of-book reconciliation 证据。

状态：
- 未通过

执行顺序：
- R1，位于 R2 basis 和 R3 lead-lag 之前。

前置任务：
- `0730T012` 已通过。
- `0730T014` 已通过。

是否需要提交代码：
- 不需要

是否进行QA验收：
- 是

files：
- `.workflow/tasks/0730T015.md`
- `.workflow/reports/0730T015-business.md`
- `examples/hyperliquid/cross_exchange_alignment_acceptance.py`
- `examples/hyperliquid/test_cross_exchange_alignment_acceptance.py`
- `local_live_analysis/skhynix_cross_exchange_research_0730T013/alignment/`
- `task_plan.md`
- `progress.md`
- `findings.md`

action：
- 以 Binance bookTicker 状态变化为 decision events，禁止跨 segment。
- 使用同机 local receipt timestamp 做所有 as-of 和 response join。
- 计算 `10,25,50,100,250,500,1000,2000ms` primary response-label
  coverage，并保留 effective horizon。
- 报告 Hyperliquid BBO、fast L2、standard L2 freshness tier。
- 对账 Binance depth/bookTicker、Hyperliquid fast/BBO、fast/standard top。
- 输出 alignment manifest、segment quality、source age、top reconciliation、
  horizon coverage 和 acceptance 文档。

hard boundary：
- 仅使用现有本地数据。
- 不访问 network、AWS、SSH 或交易端点。
- 不新增采集、不拟合 basis/lead-lag/maker 信号。
- R6 新增采集仍需独立任务、用户明确授权和活跃交易时段确认。

done：
- 所有 acceptance outputs 可复算。
- future joins、timestamp regressions、cross-segment labels 均为零。
- 只有每段 coverage 均达到 `95%` 的 frozen horizon 才可进入主研究。
