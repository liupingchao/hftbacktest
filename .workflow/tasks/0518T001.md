```md
执行线程：
- 业务线程-python

任务ID：
- 0518T001

标题：
- Conservative queue proxy gate repair design

简短描述：
- 基于 `0516T001` / `0516T002` 的 queue-ahead proxy evidence，制定一个 repair-design-only 合同：把 `4948` case 描述清楚，设计未来可能的保守 queue proxy gate，明确为什么当前不实施修复，以及后续需要等更多 current-format samples / replay false-positive cases 后再进入 repair。

状态：
- 已通过

执行顺序：
- 当前唯一后续任务

前置任务：
- `0516T001` / `0516T002` 业务结论已形成；若要进入实现，必须另开任务并等更多 case。

规则更新提醒：
- 本任务是 repair-design-only，不是 repair implementation。
- 不允许修改 replay fill model、queue logic、strategy logic。
- 不允许运行新 replay sweep、补采样本或启动 live。
- 不允许把 top5 / trade proxy 解释成 exact queue position proof。

按线程规则执行：
- .workflow/workflow-kit/thread-report-template.md
- docs/thread-playbook.md
- task_plan.md
- progress.md
- findings.md

是否需要提交代码：
- 需要

提交要求：
- 自测/检查通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

Allowed files：
- `.workflow/tasks/0518T001.md`
- `.workflow/reports/0518T001-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `.workflow/dashboard.html`
- `.workflow/dispatch_suggestions.md`

Allowed read-only inputs：
- `.workflow/reports/0516T001-business.md`
- `.workflow/reports/0516T002-business.md`
- `local_live_analysis/5-13-day-control-30min/stage6h_queue_priority_evidence_0516T001/**`
- `local_live_analysis/5-13-day-control-30min/stage6i_queue_ahead_repeatability_0516T002/**`
- `local_live_analysis/5-13-day-control-30min/stage6g_single_residual_fill_diagnosis_0515T006/**`

Forbidden files / areas：
- `examples/binance_tick_mm/*.py`
- `local_live_analysis/**` generated outputs beyond reading existing artifacts
- replay engine behavior / backtest fill model implementation
- strategy behavior, fair/target formulas, configs, risk guards, quote placement behavior
- live scripts, AWS/remote state, live collection
- sample expansion tasks
- core Rust, connector, py `event_dtype`, standard npz schema

Requires separate task：
- 任何 replay repair implementation。
- generalized touch fill assumption change。
- generalized queue exposure / priority model change。
- 新采集 current-format 样本。
- quote-adjustment promotion 或 live micro test。

Fatal Failures：
- 在 design-only 任务里直接修改 replay repair 代码。
- 将 queue-ahead proxy 写成 exact queue-position proof。
- 只凭 `4948` 或单样本 proxy 结果直接授权 generalized queue/touch repair。
- 把本任务扩展成 sample collection、strategy change 或 live 判断。

action：
- 产出 conservative queue proxy gate repair design contract。
- 必须清楚描述 `4948` case：
  - submit_key `28940|sell`
  - order_id `4948`
  - live canceled / replay filled
  - replay fill 前同价成交量 `8.884`
  - submit visible qty `21.143`
  - replay-fill visible qty `15.633`
  - touch share `1.0`
  - 为什么 live no-fill 合理、replay fill 可疑
- 设计未来可能的 conservative queue proxy gate：
  - gate 输入字段
  - same-price trade qty / visible qty threshold
  - touch duration / touch share threshold
  - top1 visible qty decay / unexplained shrink handling
  - latency / join-age / stale-data guards
  - replay-fill candidate gating semantics
  - failure modes and false-negative risk
- 明确 implementation 前置条件：
  - 更多 current-format samples
  - 更多 replay-fill false-positive cases
  - 或跨样本 repeatability evidence
  - QA 再次确认不是 overfit `4948`
- 明确本任务的 non-goals：
  - 不实现 gate
  - 不改 replay
  - 不改策略
  - 不补样本
  - 不授权 promotion

verify：
- 人工检查 `.workflow/tasks/0518T001.md` 和 `.workflow/reports/0518T001-business.md`
- `python3 .workflow/build_dashboard.py`
- `git diff --check`

done：
- 回报必须明确 `4948` case 的 evidence summary。
- 回报必须明确 conservative queue proxy gate 的候选设计。
- 回报必须明确当前为什么不实施修复。
- 回报必须明确后续等更多数据 / 更多 case 后再修复。
- 回报必须写 `commit id` 和提交信息。
```
