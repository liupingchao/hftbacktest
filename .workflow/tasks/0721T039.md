# 0721T039 RAW-ANTI-DRIFT-SEPARATE-LATE-HALT-IMMEDIATE-PARITY-REPAIR

执行线程：
- 总控 auto-loop / 业务执行线程

任务ID：
- 0721T039

标题：
- Rebuild anti-drift from raw facts and preserve late halt as a separate stage

简短描述：
- 修复 T038 QA 的 anti-drift derived-label bypass、late-halt stage overwrite 和 immediate reason branch mismatch。
- 保持 T038 已通过的 canonical primary、public-state freshness 和 hold shutdown v2 合同不变。

状态：
- 执行中

执行顺序：
- 当前唯一任务

前置任务：
- `0721T038`（未通过）

规则更新提醒：
- 本任务 offline-only。
- T037 live evidence immutable；不得修改、重封或追溯升级。
- Acceptance 必须从 lower-level facts fail closed；不得通过信任 producer status/derived labels 消除 QA findings。
- 独立 QA 通过前不得执行 private read、新 live 或任何 adaptive/multi-level activation。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `.workflow/reports/0721T038-qa.md`
- `docs/superpowers/plans/2026-07-17-principal-alignment-p0-p3.md`

是否需要提交代码：
- 需要

提交要求：
- 业务实现和业务报告分开提交。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- Modify: `examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- Modify: `examples/hyperliquid/cross_exchange_t024_same_window_acceptance.py`
- Test: `examples/hyperliquid/test_hyperliquid_tiny_live_m2_event_driven_watcher.py`
- Test: `examples/hyperliquid/test_cross_exchange_t024_same_window_acceptance.py`
- Workflow: task、business、QA、fixed QA、`task_plan.md`、`progress.md`、`findings.md`

action：
- Anti-drift gate row持久化重建所需 threshold/fact字段；independent acceptance从 `side/limit_px/current_bid/current_ask` 重建 cross risk。
- 从 adverse/favorable quantities、strict-through/depletion quantities、ratio、minimum quantity、ratio threshold和 adverse-BBO fact重建 flow status及 pressure block。
- Derived `current_cross_risk`、`adverse_flow_ratio`、`adverse_flow_status` 必须与重建结果 exact-match；同步改写派生标签不得通过。
- 如 lower-level quantity、ratio、threshold、boolean、side、price 或 BBO shape非法，显式 fail closed而不是跳过 semantic validation。
- Late persistent kill-switch 使用独立 versioned stage row；不得修改已append的 immediate guard dict。
- Canonical submit outcome接收独立 late-halt stage并将其作为最高优先级 safety stop，同时保留 immediate、anti-drift和edge原始行。
- Trigger、attempt、submit-decision和independent reconstruction exact绑定 late-halt row；producer不可能的 stage组合继续被拒绝。
- Immediate independent reconstruction与producer逐分支一致，特别是 selected quote不等于 touch时使用相同 `if/elif` reason atoms和顺序。
- 新 schema使用 T039 rollout；T038及更早历史 artifacts保持原有边界。

verify：
- QA hostile anti-drift fixture：crossing buy、adverse `0.04`、favorable `0`、ratio `inf`、adverse BBO true，派生标签改pass后必须 fail closed。
- Quantity/ratio/status、cross-risk、side/limit/BBO、threshold和boolean逐字段篡改均被独立重建拒绝。
- Late halt edge pass/block时 immediate guard原始pass row保持不变，并新增唯一 late-halt row；endpoint不被调用。
- Missing/duplicate/forged late-halt row、anti block + edge + late halt和submit late-halt drift均 fail closed。
- Immediate quote既非touch又cross ask时，producer和independent都只输出producer定义的同一 reason atom集合。
- T038 canonical/public-state/hold tests保持通过。
- Focused and full Hyperliquid regression pass。
- T037 immutable acceptance继续blocked；T031 blocked、T026 pass、T016/T022 blocked的历史边界不变。
- No live/private/account/order/cancel/network/remote/service operation occurs。

stop conditions：
- Independent anti-drift仍信任可同步篡改的 derived status/boolean。
- Late halt仍覆盖 immediate、anti-drift或edge原始 stage evidence。
- Canonical primary与raw stage rows不一致。
- T038 hold/public-state contracts被放宽。
- Historical evidence、quote、risk、submission、terminal-query timing或activation behavior被修改。
- Any live/private/network/remote/service action。

done：
- Anti-drift由 lower-level facts可复现，派生标签不再是信任根。
- Late halt是独立且不可覆盖其他stage的证据。
- Immediate producer/independent reason语义完全一致。
- Source通过业务回归并进入独立QA；只有QA通过后才可派发 delayed-history observe-only probe。

blockers：
- 无。

commit：
- dispatch base `e9b5bb1c4aae33af6c80293c9af60d2a11537bdb`

提交信息：
- 待业务实现提交。
