```md
执行线程：
- 业务线程-live-awsserver1

任务ID：
- 0624T003

标题：
- M2 AWS repaired public-shadow funnel live validation

简短描述：
- 在 `awsserver1` 上用 T002 修复后的 public-shadow 路径跑 fresh public-only no-submit live validation，确认 repaired BBO history/cache 字段在真实 public stream 中稳定填充，而不是只在 T010 replay 中成立。
- 本任务只做 public live source / no-submit 验证，不下单、不读凭据、不碰 private/order/cancel/final gate，不放松 quote distance、cap、post-only 或 accepted fresh-touch / queue-reset 条件。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- `0624T002` QA 已通过。

规则更新提醒：
- 请先阅读 `.workflow/workflow-kit/workflow-manual.md`、`.workflow/workflow-kit/task-dispatch-template.md`、`.workflow/workflow-kit/thread-report-template.md`、`.workflow/workflow-kit/qa-acceptance-template.md`、`task_plan.md`、`progress.md`、`findings.md` 后再执行本任务。

按线程规则执行：
- `AGENTS.md`

是否需要提交代码：
- 需要

提交要求：
- 自测 / 远端 public-only no-submit 验证证据生成后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

host / execution location：
- `awsserver1`
- Remote repo: `/home/admin/hftbacktest-cross-exchange`
- Remote artifact root: `/home/admin/hftbacktest-cross-exchange-artifacts/hyperliquid_tiny_live_m2_aws_repaired_public_shadow_funnel_0624T003_<timestamp>/`
- Required remote interpreter: `/home/admin/.venvs/hyperliquid-sdk-0618T002/bin/python`
- Local machine may create the task/report, perform git-safe remote sync, pull back artifacts, validate outputs, and record results.

files：
- `.workflow/tasks/0624T003.md`
- `.workflow/reports/0624T003-business.md`
- `examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- `examples/hyperliquid/test_hyperliquid_tiny_live_m2_event_driven_watcher.py`
- `local_live_analysis/hyperliquid_tiny_live_m2_aws_repaired_public_shadow_funnel_0624T003*/**`
- `task_plan.md`
- `progress.md`
- `findings.md`

read-only inputs：
- `.workflow/tasks/0624T002.md`
- `.workflow/reports/0624T002-business.md`
- `.workflow/reports/0624T002-qa.md`
- `local_live_analysis/hyperliquid_tiny_live_m2_bbo_evidence_chain_repair_0624T002/**`
- `.workflow/tasks/0623T010.md`
- `.workflow/reports/0623T010-business.md`
- `local_live_analysis/hyperliquid_tiny_live_m2_aws_candidate_funnel_0623T010_20260623T064432Z/**`

hard boundary：
- No live orders, no credential reads, no private/account/order endpoints, no real order endpoint, no cancel endpoint, no live client initialization, no remote final gate rerun, no T008 live ledger claim, no quote-distance change, no one-tick-back, no inside-spread, no cap relaxation, no taker/crossing, no default-on behavior, no M3/stable PnL/promotion.
- Public live source use is allowed only for Hyperliquid public L2/trades and Binance public market data.
- The task must use `/home/admin/.venvs/hyperliquid-sdk-0618T002/bin/python` on `awsserver1`; do not fall back to system `/usr/bin/python3` for the live public stream.
- `synthetic_current_event_only` must remain fail-closed. Do not convert synthetic-only evidence into a pass.
- Do not weaken accepted fresh-touch / queue-reset pass criteria. This task validates the repaired evidence fields and funnel, not strategy loosening.

action：
- Sync the accepted local code to `awsserver1` using the existing git-safe remote workflow, without running remote final gate and without touching credentials.
- Run a fresh public-only no-submit shadow window with the repaired T002 candidate audit fields.
- Use a 600s live public shadow window as the default. A bounded longer rerun up to 900s is allowed only if the first run has healthy public streams but too few candidate rows to judge field coverage.
- Generate repaired BBO evidence-chain validation from the fresh live shadow output.
- Produce funnel diagnostics covering:
  - public stream health: l2Book/trades/subscription/reconnect counts
  - repaired field coverage in `current_candidate_audit.csv`
  - `synthetic_current_event_only` count/share
  - `fresh_touch_evidence_pass_count`
  - `fresh_touch_allowed_count`
  - first downstream blocking stage if fresh-touch passes: strict-through / throughput / anti-drift / Binance public freshness / fair-mid source / edge gate
  - fair-mid source and edge gate pass/block counts if reached
  - no-submit/no-private/no-credential boundary manifest
- If the live stream produces no usable public data or no candidate rows, report the condition as a blocker with raw stream counts; do not fabricate candidate or would-submit evidence.

verify：
- `python -m pytest examples/hyperliquid/test_hyperliquid_tiny_live_m2_event_driven_watcher.py -q`
- `python -m py_compile examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- `python examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py --help`
- Remote public-only no-submit run on `awsserver1` with `/home/admin/.venvs/hyperliquid-sdk-0618T002/bin/python`
- Fresh-output repair validation with `--generate-bbo-evidence-chain-repair-validation`
- Validate JSON/CSV outputs, required repaired fields, reason histograms, non-empty artifacts, and boundary manifests
- `git diff --check`

done：
- 回报 live window duration、remote/local artifact path、public stream counts、candidate count、required repaired field coverage、`synthetic_current_event_only` count/share、`fresh_touch_evidence_pass_count`、`fresh_touch_allowed_count`、fresh-touch pass 后下一层阻塞分布、Binance freshness / fair-mid / edge gate pass/block counts、dominant live blocker after repair、是否观察到真实 public source、是否任何 private/order endpoint 被触碰、是否 next real canary authorized。
- 明确说明本任务不授权真实下单、读凭据、private/account/order/cancel endpoint、remote final gate、T008 live ledger claim、quote-distance change、one-tick-back、inside-spread、cap relaxation、M3/stable PnL/default-on/promotion。
- 回报 commit id 和提交信息。
```
