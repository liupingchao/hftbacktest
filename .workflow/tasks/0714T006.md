# 任务派发

执行线程：
- 业务线程-python/live-awsserver1

任务ID：
- 0714T006

标题：
- T011-SCHEDULED-US-OPEN-CONTROLLED-LIVE-EVIDENCE-WITH-INTERVAL-COVERAGE-REPAIR

简短描述：
- 在 `0714T005` QA 已通过后，使用 interval coverage repair 后的代码路径，按美股开盘前 15 分钟启动 controlled live evidence。
- 本任务运行三段连续 30 分钟 live windows，目标是采集可解释的 resting interval evidence，不是提高成交率，不是改 quote policy，不是做 fee/PnL calibration。

状态：
- 待执行

执行顺序：
- 当前唯一任务，定时执行

前置任务：
- `0714T005` QA 已通过。
- 本地 `cross-exchange` 分支必须 clean，且待执行代码必须同步到 `awsserver1:/home/admin/hftbacktest-cross-exchange`。

授权记录：
- 用户在 2026-07-14 授权：repair 完成后继续做 live test，三段 30 分钟，开始时间为 2026-07-14 美股开盘前 15 分钟。
- 已核对常规美股开盘时间为 09:30 ET；本任务计划启动时间为：
  - 2026-07-14 09:15 EDT
  - 2026-07-14 21:15 CST / Asia-Shanghai
- 定时唤醒 automation id:
  - `0714t006-live-test-at-us-open-preflight`

规则更新提醒：
- 请先阅读规则文档后再执行本任务。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- live evidence、pullback artifact、business report 和 tracking 更新必须提交。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0714T006.md`
- `.workflow/reports/0714T006-business.md`
- `.workflow/reports/0714T006-qa.md`
- `docs/qa-acceptance-report.md`
- `local_live_analysis/cross_exchange_interval_coverage_repaired_live_evidence_0714T006_<UTC>/`
- `task_plan.md`
- `progress.md`
- `findings.md`

action：
- At the scheduled gate, first verify:
  - `0714T005` QA is `已通过`.
  - local repo is clean.
  - `origin/cross-exchange` contains the accepted `0714T005` repair.
  - `awsserver1:/home/admin/hftbacktest-cross-exchange` is synced to that accepted code. If remote `origin` still points to an old bundle, sync by a git bundle from local.
- Run three sequential controlled live windows on `awsserver1`, not in parallel.
- Each live window duration is `1800s`.
- Pull back the complete remote artifact package with `scp` after completion.
- Generate local validation, sha256 reconciliation, boundary manifest, per-window classification, and business report.

authorized live envelope：
- Host: `awsserver1`
- Remote repo: `/home/admin/hftbacktest-cross-exchange`
- Remote artifact parent: `/home/admin/hftbacktest-cross-exchange-artifacts/`
- Local artifact parent after pullback: `local_live_analysis/`
- Pullback method: `scp`
- Interpreter: `/home/admin/.venvs/hyperliquid-sdk-0618T002/bin/python`
- Env file: `/home/admin/XEMM_rust_latest/.env`
- Runner: `examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- Required mode: `--event-driven-edge-gate-live`
- Required feed option: `--hyperliquid-l2book-fast`
- Symbol: Hyperliquid `BTC`
- Order type: limit
- TIF: post-only `Alo`
- Side policy: `fresh_touch`
- Quote offset: `0` tick touch-only
- Window count: exactly `3` sequential windows unless a safety/boundary failure requires stopping early.
- Max duration per window: `1800s`
- Max submissions per window: `2`
- Max order size: `0.005 BTC`
- Quote hold seconds: `3`
- Wait seconds: `10`

hard boundary：
- No default-on or continuous bot operation.
- No parallel live windows.
- No threshold change.
- No quote-envelope change.
- No order size above `0.005 BTC`.
- No max submissions above `2` per live window.
- No taker, crossing, inside-spread, one-tick-back, or fill-seeking placement.
- Live orders, if any, must be post-only `Alo`.
- Fast Hyperliquid `l2Book` must be enabled.
- Every submitted order must have tracked cancel/shutdown evidence and independent final open-orders proof.
- If any window ends with nonzero open orders, missing cancel proof, malformed lifecycle evidence, malformed interval coverage artifacts, or unexpected endpoint usage, stop the auto-loop and do not run later windows.
- Do not claim fill probability, queue priority, fee/rebate, realized PnL, profitability, stable PnL, maker viability, `T012`, promotion, final MVP pass, or parameter expansion.

required evidence：
- Per-window lifecycle classification.
- Repaired `public_stream_coverage.csv` with coverage proof/diagnostic fields.
- `resting_interval_capture_manifest.json` with `coverage_repair_version`.
- Public trades during the actual/proxy-statused resting interval, keyed by attempt.
- Resting-start L2/depth snapshot or explicit not-available status.
- Depletion/trade-through matrix over the interval.
- Feed health and reconnect/timeout summaries.
- Runner final open-orders `0` proof and independent final open-orders `0` proof.
- Boundary manifest / endpoint boundary evidence.

verify：
- Preflight local and remote:
  - `python -m py_compile examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
  - `python examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py --help`
  - `git diff --check`
- Remote controlled live execution:
  - exactly three sequential 1800s windows unless safety stop.
- Pullback validation:
  - JSON parse
  - CSV parse
  - sha256 reconciliation
  - final open-orders proof per window
  - endpoint boundary checks
  - repaired coverage fields present and parsed
- Local focused regression:
  - `python -m pytest examples/hyperliquid/test_hyperliquid_tiny_live_m2_event_driven_watcher.py -q`

done：
- Report dispatch commit, live evidence commit, remote commit, remote/local artifact roots, window start/end times, per-window classification, submissions/fills/rejects, final open-orders proof, repaired coverage statuses, zero-row interpretations, endpoint boundary status, blockers, and recommended route.
