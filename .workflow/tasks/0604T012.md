```md
执行线程：
- 业务线程-research

任务ID：
- 0604T012

标题：
- Live shutdown cancel acknowledgement case matrix and root-cause diagnosis

简短描述：
- 系统性复现并诊断 live shutdown 不等待 cancel acknowledgement 的问题。目标是枚举所有主要 shutdown cancel case，证明当前退出路径只发送 cancel request、不等待 cancel response，定位到底层 `close()` 是否兜底，并区分 live 安全风险与 audit/alignment 尾部污染风险。本任务只做复现/诊断/根因定位，不做修复。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- 无

规则更新提醒：
- 请先阅读规则文档后再执行本任务。
- 本任务只做诊断/复现/根因定位；不得修改 shutdown 逻辑、不得改 connector/live bot、不得启动 live、不得做默认配置变更。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 不需要

提交要求：
- 不需要提交
- 回报必须带 `commit：无` 和 `提交信息：无`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0604T012.md`
- `.workflow/reports/0604T012-business.md`
- `examples/binance_tick_mm/live_tick_mm.py`
- `examples/binance_tick_mm/strategy_core.py`
- `py-hftbacktest/hftbacktest/binding.py`
- `py-hftbacktest/src/live.rs`
- `hftbacktest/src/live/bot.rs`
- 必要时只读检查 connector cancel 实现：`connector/src/binancefutures/mod.rs`、`connector/src/binancespot/mod.rs`、`connector/src/connector.rs`。
- 可在报告中附只读复现矩阵和一次性 Python/Rust-reading evidence；不得把复现脚本落盘为新代码文件。

forbidden：
- 不修复 `live_tick_mm.py` shutdown cancel 逻辑。
- 不新增/修改测试。
- 不修改 py binding、Rust live bot、connector 或生产配置。
- 不启动 live / tiny-live / default-on。
- 不做参数搜索、回放实验扩展、promotion。
- 不静默扩大到其他 shutdown 风险项。

action：
- 只读检查当前 review 是否属实：
  - `live_tick_mm.py` shutdown `finally` 是否对 buy/sell/extra working orders 调用 `hbt.cancel(..., False)`。
  - Python binding 是否定义 `wait=True` 才等待 cancel response。
  - Rust live bot `cancel(..., wait)` 是否只有 `wait=true` 才调用 `wait_order_response(...)`。
  - Rust live bot `close()` 是否为空实现或不承担等待所有撤单落地职责。
  - normal loop cancel 使用 `False` 是否与 shutdown 风险不同：normal loop 后续仍继续处理响应，shutdown 则发完 cancel 后 close/return。
- 枚举并逐类复现主要 shutdown cancel case，至少覆盖：
  - 仅 buy working order，cancellable=true。
  - 仅 sell working order，cancellable=true。
  - buy + sell 同时存在。
  - extra working orders 存在。
  - buy/sell/extra 混合多订单。
  - working order 存在但 `cancellable=false`，确认不会发 cancel，也谈不上等待 ack。
  - `hbt.cancel(..., False)` 成功返回但未等待 order response。
  - `hbt.cancel(..., False)` 抛异常，shutdown 捕获后是否仍继续 read position / close。
  - `hbt.close()` 调用后是否不等待 pending cancel ack。
  - 如果能用 fake/stub hbt 做一次性复现，确认 shutdown cancel 调用参数均为 `wait=False`，且没有调用 `wait_order_response()` / `wait_next_feed(include_order_resp=True)`。
- 对每个 case 输出复现矩阵，至少包含：
  - case 名称。
  - 初始 working order 形态。
  - 是否发出 cancel。
  - cancel 调用参数 `wait`。
  - 是否调用等待 cancel response 的 API。
  - 是否调用 `close()`。
  - `close()` 是否有兜底等待语义。
  - 判定：漏等 ack / 非漏等 / 不适用。
- 定位根本原因：
  - shutdown 路径是否把 cancel 当成 fire-and-forget request。
  - `close()` 是否没有 lifecycle drain / pending cancel ack 等待职责。
  - 当前 audit 是否可能缺少 shutdown 尾部 cancel_ack / REST open order reconciliation。
  - 当前代码是否没有 shutdown cancel timeout / retry / REST final open-orders check。
- 输出诊断报告，说明：
  - review 结论是否成立。
  - 完整 case matrix 与每类复现结果。
  - live 安全影响路径。
  - audit/alignment 尾部污染路径。
  - 共同根因和分支根因。
  - 建议后续修复任务边界，但不得在本任务内实施修复。

acceptance criteria：
- 报告必须给出明确结论：review 属实 / 不属实 / 部分属实。
- 如属实，报告必须引用具体代码位置说明 shutdown 如何发送 cancel request 但不等待 ack。
- 报告必须覆盖底层 binding / Rust live bot 的 `wait` 与 `close()` 语义。
- 报告必须包含 shutdown cancel case matrix，且至少覆盖 action 中列出的 case。
- 报告必须区分：
  - 已发 cancel request 但未等待 ack 的 live safety 风险。
  - 未发 cancel 的不可撤订单 / 异常场景。
  - `close()` 不兜底导致 pending cancel 不会被 drain 的根因。
- 报告必须说明现有 audit/alignment 尾部可能被该 shutdown 语义污染的路径。
- 报告必须给出后续修复任务建议边界，但不包含任何代码修改。

verify：
- 只读代码检查。
- 可运行一次性 fake/stub Python 复现片段，模拟 shutdown cancellation 片段或抽取最小等价逻辑；如运行，报告必须记录命令与结果。
- 可运行 `python -m pytest examples/binance_tick_mm/test_backtest_tick_mm.py -k "shutdown or cancel"` 之类 focused discovery 命令；如无相关测试，报告说明覆盖缺口。
- 不允许新增测试或修改代码。

done：
- 业务报告写入 `.workflow/reports/0604T012-business.md`。
- 回报必须说明结论、case matrix 覆盖情况、逐类复现结果、共同根本原因、分支根因、live safety 影响、audit/alignment 影响、测试缺口、建议后续修复范围。
- 状态应为 `待验收` 或 `阻塞`，但本任务当前不进入 QA。
- 本任务按用户要求已派发执行；本轮不进入 QA。
```
