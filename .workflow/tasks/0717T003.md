# 任务派发

执行线程：
- 业务线程-live-safety-audit

任务ID：
- 0717T003

标题：
- LIVE-SYMBOL-MISMATCH-WTIOIL-AUDIT

简短描述：
- 排查用户发现的交易历史异常：BTC long 同时出现 WTIOIL short，确认是否为本 repo live runner 造成的标的错配、账户历史混入、UI/export 映射问题，或外部策略/手工交易。
- 本任务只做只读审计和代码/日志检查；不运行 live、不提交订单、不撤单、不改策略参数。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0717T002` QA 为 `阻塞`，但 SSM-first run 完整收口，final open orders 为 `0`。
- 用户补充 `trade_logs/` 中有交易所导出的成交记录，并指出 BTC long / WTIOIL short 疑似 short 标的错误。

规则更新提醒：
- 请先阅读规则文档后再执行本任务。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 自测/审计完成后提交报告。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0717T003.md`
- `.workflow/reports/0717T003-business.md`
- `.workflow/reports/0717T003-qa.md`
- `docs/qa-acceptance-report.md`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `examples/hyperliquid/`
- `trade_logs/` read-only
- `local_live_analysis/` read-only

action：
- Parse `trade_logs/trade_history.csv` and summarize BTC/WTIOIL fills by timestamp.
- Compare trade-history timestamps against 0715T001, 0716T006, and 0717T002 authorized run windows.
- Inspect live artifacts for order-intent symbol, side, private response, fill ledger, and user fills pullback.
- Audit code paths for hardcoded symbol, side policy, SDK order/cancel symbol, fill attribution symbol guards, and Hyperliquid asset/index mapping.
- Query awsserver1 read-only account state/meta/open-orders if needed; do not print secrets.
- Produce a stop/go recommendation for future live runs.

verify：
- JSON/CSV parse where relevant.
- Focused grep/code inspection.
- Read-only Hyperliquid meta/account/open-orders check only if needed.
- `git diff --check`.

done：
- Report whether WTIOIL records are attributable to this repo's live runner.
- Report whether code has a plausible symbol-mismatch defect.
- Report exact evidence and residual uncertainty.
- Recommend whether future live tests must stay paused until additional symbol guard is implemented.

not authorized：
- No live run.
- No order submit.
- No cancel.
- No quote-policy change.
- No threshold/order-size/max-submission/max-loss change.
- No fee/PnL calibration.
- No maker viability or promotion claim.
