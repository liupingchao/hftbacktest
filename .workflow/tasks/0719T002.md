# 0719T002 PER-ATTEMPT-REFERENCE-CANCEL-PROOF-REPAIR

执行线程：
- 总控 auto-loop / 业务实现线程

任务ID：
- 0719T002

标题：
- Per-attempt/per-reference authoritative cancel proof repair

简短描述：
- Repair the T025 QA P1 by preserving every submitted order reference and requiring authoritative terminal cancel evidence for each submitted attempt/reference before classifying zero fills as reconciled.
- This is an offline-only repair and QA task. It does not authorize a new live window.

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- 0719T001（未通过；runtime provenance accepted, cancel proof still fail-open across attempts/references）

规则更新提醒：
- Live remains the primary data source, but execution-safety and evidence-integrity gates must pass first.
- No live/private/account/order/cancel/remote/service action is allowed in this task.
- Multi-level, dynamic spread, fill feedback, inventory skew and actual quote behavior changes remain off.

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`

是否需要提交代码：
- 需要

提交要求：
- 离线自测和 full Hyperliquid regression 通过后提交。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- Modify: `examples/hyperliquid/hyperliquid_tiny_live_m2_fill_window.py`
- Modify if required: `examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- Modify if required: `examples/hyperliquid/cross_exchange_t024_same_window_acceptance.py`
- Modify/add focused tests under `examples/hyperliquid/`
- Workflow: this task, business report, QA report, `docs/qa-acceptance-report.md`, `task_plan.md`, `progress.md`, `findings.md`

action：
- Preserve every submitted attempt's tracked oid/cloid references instead of overwriting the aggregate reference list.
- Add attempt and target-reference identity to every cancel result row emitted by standalone and manager execution paths.
- Define a deterministic submitted-reference key using attempt plus oid/cloid identity.
- For zero-fill reconciliation, require each submitted reference to have at least one matching authoritative successful cancel result.
- Treat a successful cancel for a different attempt/reference as unrelated evidence.
- Permit a later generic `already canceled, or filled` response only for a reference that already has its own authoritative success.
- Fail closed when:
  - any submitted attempt/reference has no matching successful cancel;
  - cancel evidence has no target identity;
  - target identity is ambiguous or maps to multiple submitted references;
  - a reference has only generic ambiguous responses;
  - fill/account/open-order evidence conflicts.
- Emit per-reference reconciliation rows in producer artifacts so QA can identify exactly which reference passed or failed.
- Keep acceptance fail-closed on any producer mechanism/evidence blocker and require the new reconciliation schema/status for zero-fill acceptance.

verify：
- Reproduce the QA counterexample: attempt 1 success plus attempt 2 ambiguous-only must return `no_fill_unproven`.
- Two submitted references with one success each must return `no_fill_reconciled`.
- Success by oid and success by cloid both map only to the correct reference.
- Unrelated success, missing target identity, duplicate/ambiguous mapping and unknown target fail closed.
- Same-reference success followed by redundant generic cancel remains reconciled.
- Standalone multi-attempt artifacts preserve all tracked refs and target identities.
- Existing two-sided manager cycle emits target-bound cancel evidence for both sides.
- Focused fill attribution, fill loop, manager, event-driven watcher, orchestrator and acceptance regressions.
- Modified Python `py_compile`, CLI `--help`, `git diff --check`.
- Full `python -m pytest examples/hyperliquid -q`.

stop conditions：
- Any implementation that still uses an aggregate “any cancel succeeded” boolean.
- Any submitted reference can be dropped or overwritten before final artifacts.
- Any ambiguous cancel is accepted without a same-reference authoritative success.
- Any change to quote selection, strategy thresholds, live envelope or activation state.

done：
- Every submitted attempt/reference has explicit terminal cancel proof status.
- QA counterexample and multi-reference success/failure matrix are covered.
- Producer and acceptance fail closed on any unproven reference.
- Independent QA accepts the offline repair before any new live task is created.

blockers：
- No new live window may start until this task is QA accepted.

commit：
- `7235372bb2a2d6bde043a1fee3d4abc16bb5067d`

提交信息：
- `Bind cancel proof to every submitted reference`
