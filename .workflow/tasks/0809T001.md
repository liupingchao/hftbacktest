# 任务派发

执行线程：
- 业务线程-python/research

任务ID：
- 0809T001

标题：
- SKHYNIX-MAKER-DIAGNOSTICS-ARRIVAL-AND-NATIVE-MATRIX

简短描述：
- 按顺序完成 `0807T001` 4H paired live-shadow 的精确策略重放、
  side/regime 漏斗与 markout、Level guard 归因、arrival-intensity
  校准，以及三 session hftbacktest/Nautilus 原生成交矩阵。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0807T002` 已通过，提供同一 4H 采集的正式后处理数据。
- 4H paired live-shadow 已完成并保留 full/control 决策、理论成交和 markout。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `AGENTS.md`

是否需要提交代码：
- 不需要

提交要求：
- 本任务不提交代码；保留当前 working tree 供 QA 检查。
- 回报使用 `commit：无`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 检查配置 hash 闭合、full/control 同时钟、策略输出一致性、guard
  触发与续期语义、bid floor 约束和 source immutability。
- 检查 Level attribution 的 episode/fill/PnL 归因闭合。
- 检查 arrival 训练/OOS 边界、candidate 未写入 active/live config。
- 检查 hftbacktest/Nautilus scenario/session 完整性、unsupported
  dimensions 与报告结论边界。

files：
- `/Users/liu/Documents/glft/src/skhynix_glft/`
- `/Users/liu/Documents/glft/tests/`
- `/Users/liu/Documents/glft/docs/skhynix-maker-diagnostics-native-matrix-0809T001.md`
- `/Users/liu/Documents/glft/runtime/normal-v3-four-state-bid/diagnostics/`
- `/home/molly/projects/glft/runtime/0809T001-maker-slice/output/`
- immutable source：
  `/Users/liu/Documents/glft/runtime/normal-v3-four-state-bid/live-shadow/c6in-paired-4h-0807T001-20260807T0101Z`

action：
- 新增流式 maker policy replay audit。
- 从 run manifest 恢复实际 effective full/control config 并严格核对 hash。
- 逐行重放策略，对 intent/hash/quote/guard 结果做 exact 或数值容差比较。
- 输出 guard trigger/retrigger/hold-only、duty 和 4.5bp floor binding 统计。
- 仅保存有限 mismatch evidence，审计失败时 fail closed。
- 新增 side/regime 决策漏斗、理论成交 proxy 和
  `100/250/500/1000/2000ms` markout 汇总。
- 重建 Level guard episode，归因 ask/bid 差异成交、库存和 paired PnL。
- 使用 Aug03/Aug04 训练、Aug07 等 session 验证 side/regime
  arrival-intensity；候选配置只使用 global side 参数，不启用动态 regime。
- 在 amdserver 完成 hftbacktest `12 x 3` 与 Nautilus `6 x 3` 场景矩阵。
- 明确 Nautilus 不支持 queue/partial-fill 本轮合同；不静默使用默认模型。

verify：
- 本机完整测试：`70 passed, 2 skipped, 2 subtests passed`。
- AMD `nt-backtest` 完整测试：`61 passed, 2 skipped, 2 subtests passed`。
- AMD hftbacktest 定向测试：`13 passed, 2 skipped`。
- Ruff：通过；compileall：通过；`git diff --check`：通过。
- 4H replay：`144001` 行对齐，`0` timestamp/策略字段 mismatch。
- AMD manifest closure：hftbacktest `36` 行、Nautilus `18` 行，
  全部 `stream_complete=True`，两框架 candidate config hash 相同。
- local/AMD 正式报告 SHA256：
  `c55ea387ba52c1a0a24a18190ff3fb8c1b7b1751b401d0412d4198cff2f0bb89`。

done：
- 五步顺序全部完成，业务执行结果进入 QA。
- 4.5bp floor 与 change guard 保留；Level guard 保持 research freeze。
- global side-arrival candidate 仅用于研究回放，没有写入 active/live config。
- 当前证据不解锁 live order：markout 仍为负，arrival 存在跨 session
  shift，candidate PnL 部分 in-sample，250ms 压力 PnL 为负，
  queue/partial-fill 辨识力不足。

hard boundary：
- 不启动采集。
- 不访问 private/account/order/cancel endpoint。
- 不修改 4H live-shadow 原始目录。
- 不修改 active/live config，不做 live promotion。
