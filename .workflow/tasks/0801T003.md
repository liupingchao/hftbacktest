# 任务派发

执行线程：
- 业务线程-python/cross-exchange-research

任务ID：
- 0801T003

标题：
- HYPERLIQUID-SHOCK-CLUSTER-CONTINUOUS-FLOW-EPISODE

简短描述：
- 按 `docs/hyperliquid_liquidity_response_case_hierarchy_plan.md` 执行
  Goal 2，在已验收 ShockAtom catalog 上构建 primary ShockCluster 和
  ContinuousFlowEpisode。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- `0801T002` QA `已通过`。

规则更新提醒：
- 无

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `docs/hyperliquid_liquidity_response_case_hierarchy_plan.md`

是否需要提交代码：
- 不需要

提交要求：
- 不需要提交
- 回报写 `commit：无`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `examples/hyperliquid/cross_exchange_liquidity_response_case_hierarchy.py`
- `examples/hyperliquid/test_cross_exchange_liquidity_response_case_hierarchy.py`
- `local_live_analysis/skhynix_liquidity_response_case_hierarchy/`
- `.workflow/tasks/0801T003.md`
- `.workflow/reports/0801T003-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

action：
- 验证 T002 atom manifest、catalog SHA 和 row count。
- 按 segment 内 `shock_ts` 排序，使用 primary `100ms` gap 构造
  ShockCluster。
- 使用 primary `250ms` bridge gap 和 fail-closed recovery checkpoint 规则
  构造 ContinuousFlowEpisode。
- 输出每个 atom 的 cluster/episode membership，确保无重复、无遗漏、无跨段。
- 输出 cluster catalog、episode catalog、phase rows、boundary audit、boundary
  sensitivity 和 `episode_manifest.json`。
- 冻结并记录 `episode_boundary_v1` 参数、provenance、输出 SHA 和本地边界。

verify：
- `python -m pytest examples/hyperliquid/test_cross_exchange_liquidity_response_case_hierarchy.py -q`
- `python examples/hyperliquid/cross_exchange_liquidity_response_case_hierarchy.py --help`
- 真实 T002 atom -> Goal 2 builder。
- 独立脚本复核 membership conservation、cluster/episode segment boundary、
  output SHA、boundary audit 和 long-flow reporting。

done：
- 每个 atom 精确属于一个 primary cluster 和一个 primary episode。
- Primary 参数只发布为 `episode_boundary_v1`，诊断网格不进入后续主线。
- Long-flow cases 单独标记，不静默丢弃。
- 本轮不做 baseline、motif、regime、信号拟合、策略回测、maker identity、
  exact fill 或 maker PnL 推断。
