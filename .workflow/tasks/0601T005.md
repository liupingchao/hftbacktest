```md
执行线程：
- 业务线程-research

任务ID：
- 0601T005

标题：
- Binance-led Hyperliquid read-only pricing-signal runner implementation

简短描述：
- 基于 `0601T004` 已通过 QA 的 data input / next-runner contract，实现一个只读 pricing-signal runner。该 runner 只消费本地已验收 `0601T002/0601T003/0601T004` 产物，生成 decision-time feature rows、future label summaries、feature/regime/venue-state quality summaries 和只读研究建议；不实现策略、不接私有/订单端点、不采集新数据、不参数搜索、不 live。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- `0601T004` QA 已通过。
- `0601T003` QA 已通过。
- `0601T002` QA 已通过。

规则更新提醒：
- 请先阅读规则文档后再执行本任务。
- 本任务是 read-only pricing-signal runner implementation，不是 strategy implementation、live authorization、parameter search 或 promotion。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `docs/thread-playbook.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交任务文件、runner/test、业务报告、task-scoped artifacts 和 workflow tracking 更新。
- 回报必须带 `commit id` 和 `提交信息`。
- 不得触碰 private/order/strategy/live/parameter/default-on/tiny-live/promotion 边界。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0601T005.md`
- `.workflow/reports/0601T005-business.md`
- `examples/hyperliquid/binance_led_pricing_signal_runner.py`
- `examples/hyperliquid/test_binance_led_pricing_signal_runner.py`
- `local_live_analysis/binance_led_hyperliquid_pricing_signal_0601T005/**`
- 必要 workflow tracking 文件仅限本任务状态/结论更新。

read-only inputs：
- `local_live_analysis/cross_exchange_lead_lag_join_0601T002/**`
- `local_live_analysis/cross_exchange_lead_lag_analysis_0601T003/**`
- `local_live_analysis/binance_led_hyperliquid_data_contract_0601T004/**`
- `docs/binance_led_hyperliquid_maker_data_input_contract.md`

forbidden files / areas：
- New public or private network collection.
- Hyperliquid private connector, account/order endpoints, order lifecycle, submit/cancel/fill logic.
- Binance or Hyperliquid strategy implementation.
- Strategy live, parameter search, default-on, tiny-live, promotion.
- Standard npz schema, canonical audit schema, connector/core API changes.

action：
- Implement a read-only runner that consumes only the accepted local artifacts from `0601T002/0601T003/0601T004`.
- Enforce the `0601T004` primary allowlist:
  - `binance_top5_imbalance`
  - `binance_microprice_minus_mid_ticks`
  - `binance_mid_move_ticks_from_prev`
  - `binance_top5_bid_qty`
- Keep Binance and Hyperliquid trade pressure disabled.
- Preserve the timestamp/no-future policy:
  - decision-time inputs use current joined rows only.
  - future labels use first Hyperliquid row at or after `decision_ts + horizon_ms`.
  - output reports both `horizon_ms` and `effective_future_age_ms`.
- Generate task-scoped outputs:
  - `run_manifest.json`
  - `pricing_signal_rows.csv`
  - `pricing_signal_feature_quality.csv`
  - `horizon_label_summary.csv`
  - `feature_stability_by_regime.csv`
  - `venue_state_conditioning_summary.csv`
  - `pricing_signal_recommendation.md`
- Recommendation may only be one of:
  - `keep_for_read_only_research`
  - `needs_more_public_samples`
  - `reject_for_runner_design`

acceptance criteria：
- Runner is deterministic and reads only local accepted artifacts.
- Primary rows are limited to `joined_row_quality=primary_usable`, no missing Binance join, no future Binance join.
- `pricing_signal_rows.csv` contains decision-time features and future labels separated by horizon, with no future-derived input fields.
- Feature quality summary proves only the four `0601T004` primary allowlist features drive candidate signal rows.
- Horizon label summary reports nominal horizon and effective future-age min/mean/max.
- Regime and venue-state summaries are generated.
- Recommendation stays within allowed read-only conclusion taxonomy.
- Boundary flags explicitly prohibit private/order/live/strategy/parameter/default-on/tiny-live/promotion.

verify：
- `python examples/hyperliquid/binance_led_pricing_signal_runner.py --help`
- `python -m py_compile examples/hyperliquid/binance_led_pricing_signal_runner.py examples/hyperliquid/test_binance_led_pricing_signal_runner.py`
- `python -m pytest examples/hyperliquid/test_binance_led_pricing_signal_runner.py`
- `python examples/hyperliquid/binance_led_pricing_signal_runner.py --output-dir local_live_analysis/binance_led_hyperliquid_pricing_signal_0601T005`
- `python -m json.tool local_live_analysis/binance_led_hyperliquid_pricing_signal_0601T005/run_manifest.json`
- Required artifact existence checks for all seven outputs.
- `git diff --check`

done：
- Business report must include generated artifacts, primary/excluded row counts, feature allowlist enforcement, label/effective-age policy, recommendation, verification commands, blockers if any, commit id, and commit message.
- Business report must explicitly state that the output is read-only pricing-signal research evidence only and does not authorize strategy implementation, private/order endpoints, live/default-on/tiny-live, parameter search, or promotion.
- Business report must end in `待验收` if local verification completes, or `阻塞` / `未通过` if criteria above cannot be met.
```
