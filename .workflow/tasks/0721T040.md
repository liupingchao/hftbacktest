# 0721T040 DELAYED-HISTORY-OBSERVE-ONLY-EXACT-PROBE

执行线程：
- 总控 auto-loop / 业务执行线程 / live-awsserver1

任务ID：
- 0721T040

标题：
- Add and run one read-only delayed-history synthetic-reference probe

简短描述：
- 新增一个与真实订单 terminal 判定完全隔离的 delayed-history observe-only probe。
- 使用 synthetic non-owned cloid 确定性执行五轮 direct private query、exact `4.0s` propagation delay、一次 `historical_orders` 和保留 `0.5s` 的 final open-orders snapshot。
- 本任务不提交、撤单、flatten、报价、连接公共行情或改变任何策略行为。

状态：
- 未通过

执行顺序：
- 当前唯一任务

前置任务：
- `0721T039`（已通过）

规则更新提醒：
- Standing authorization只解锁本任务一个 exact bounded private/live probe，不解锁任何其他窗口。
- Probe evidence不得写入现有 `cancel_shutdown_proof.json`、Task 12 `terminal_query_*` artifact或 same-window acceptance主合同。
- Synthetic reference不得使用当前 task/run managed ownership prefix，也不得被解释为真实订单或 terminal proof。
- T037/T031/T026/T022/T016 immutable evidence和exact acceptance boundary不得改变。
- Dynamic spread、fill feedback、inventory skew、multi-level及任何报价行为全部关闭。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `.workflow/reports/0721T039-qa.md`
- `docs/superpowers/plans/2026-07-17-principal-alignment-p0-p3.md`

是否需要提交代码：
- 需要

提交要求：
- 先提交 probe实现和离线验证，固定 exact source后才能构建远端隔离archive。
- Live evidence和业务报告另行提交。
- QA报告和固定QA文档再次单独提交。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

files：
- Modify: `examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- Modify: `examples/hyperliquid/cross_exchange_live_remote_orchestrator.py`
- Create: `examples/hyperliquid/cross_exchange_delayed_history_probe_acceptance.py`
- Test: `examples/hyperliquid/test_hyperliquid_tiny_live_m2_event_driven_watcher.py`
- Test: `examples/hyperliquid/test_cross_exchange_live_remote_orchestrator.py`
- Create/Test: `examples/hyperliquid/test_cross_exchange_delayed_history_probe_acceptance.py`
- Workflow: task、business、QA、fixed QA、`task_plan.md`、`progress.md`、`findings.md`

source：
- Branch: `cross-exchange`
- Dispatch base: `a7ab350`
- Exact source commit: `76bea45046c21d99299b4801c82a262dec6b6e7d`
- Remote source: `/home/admin/hftbacktest-cross-exchange-0721T040`
- Source marker: `/home/admin/hftbacktest-cross-exchange-0721T040/source_commit.txt`

exact envelope：
- Host: `awsserver1`
- Account scope: Hyperliquid account configured by `/home/admin/XEMM_rust_latest/.env`
- Venue/symbol: Hyperliquid `BTC`
- Profile/mode: `delayed-history-observe-only` / `delayed-history-observe-only-probe`
- Windows: `1`
- Child duration cap: `30s`
- Direct query rounds: `5`
- Direct method: `query_order_by_cloid`
- Historical calls: `1`
- Propagation delay: exact `4.0s`
- Final snapshot reserve: exact `0.5s`
- Total query budget: exact `5.0s`
- Normal order size: `0 BTC`
- Max submissions: `0`
- Max cancel calls: `0`
- Max aggregate position delta: `0 BTC`
- Max loss: `0 USDC` expected；standing emergency cap remains `1 USDC`
- Public market-data connection: none
- Terminal participation: false
- Flatten: prohibited；preflight若发现非零仓位、open order或未知账户状态则在probe前停止

implementation action：
- Watcher新增独立 `--delayed-history-observe-only-probe` mode；不得进入任何 event-driven、quote、manager reconcile、order、cancel或fill路径。
- Probe构造合法的 `0x` + 32 hex synthetic cloid，并显式证明不匹配当前 task/run managed ownership prefix。
- Pre-probe读取open orders和BTC position；必须为empty/flat，且持久kill-switch状态可读并clear。
- 对synthetic cloid执行恰好五轮 bounded `query_order_by_cloid`；每轮必须独立重建为unknown，否则fail closed。
- 从query budget start exact计算history not-before和history deadline；等待到`start+4.0s`后执行恰好一次bounded `historical_orders`。
- Historical response必须为list；empty/foreign-only保持unknown。若出现synthetic exact match、malformed envelope或任何非unknown terminal解释，probe fail closed。
- History后在总预算内完成独立open-orders snapshot；必须empty且不含synthetic reference。
- 持久化独立 `delayed_history_observe_only_probe.json`，包含raw lower-level timing、method/result envelope、redacted reference tokens、pre/post account facts和显式execution boundary。
- 新独立acceptance从raw rows重建query sequence、五轮unknown、history timing、result envelope、final snapshot和zero-submit/zero-cancel边界；不得信任producer summary。
- Orchestrator新增profile-scoped exact envelope；仅该profile允许`max_order_size=0`和`max_submissions=0`，不得全局放宽其他live profile。
- No-start preflight写明synthetic probe、`5/1/4.0/0.5/5.0`、terminal participation false、无public feed、无manager和zero order/cancel。

live action：
- 从exact implementation commit构建isolated Git archive并核对local/remote SHA-256。
- 执行no-start exact orchestrator preflight；不得读取credential或调用endpoint。
- 读取账户、kill-switch、service和冲突进程状态；任何open order、非零BTC position、halt或并发交易进程均在probe前fail closed。
- 启动唯一一个detached orchestrator window；不得启动第二窗口。
- Pullback完整artifact root，验证remote terminal manifest、source start/postrun verification和local/remote transfer digest。
- 本地运行独立probe acceptance，并复跑五个历史same-window exact boundaries。
- Post-probe独立证明open orders `0`、BTC position `0.0`、service/process状态安全。

verify：
- Probe producer/acceptance valid fixture通过。
- Exactly 5 direct rows、one history row、contiguous sequence、exact timing和complete final snapshot通过。
- Direct非unknown、少/多轮、sequence gap、history提前、reserve耗尽、duplicate history、history后direct、malformed result和synthetic exact match均fail closed。
- `order()`、`cancel_tracked()`、`market_close()`、public feed和quote manager调用计数始终为`0`。
- No-start orchestrator profile精确拒绝非zero submissions/order size、wrong mode、manager、fast L2、wrong timing或第二window。
- Focused and full Hyperliquid regression pass。
- T037/T031/T026/T022/T016 exact replay边界与T039 QA完全一致。
- Remote live evidence独立acceptance通过，且source、task/run/window、timing、method、account和zero-side-effect facts exact。

stop conditions：
- Synthetic cloid匹配managed ownership prefix或真实historical row。
- 任一direct query不是unknown。
- History call未实际执行、超过一次、早于not-before或侵占final snapshot reserve。
- Final open-orders snapshot非empty、无法完成或发现synthetic reference。
- 任一order、cancel、flatten、public feed、quote generation或adaptive activation。
- Pre/post account、kill-switch、service/process、source或checksum不明确。
- Existing Task 12 acceptance、T039 rollout或历史evidence boundary被修改。
- Probe/acceptance需要通过信任producer summary而非raw evidence才能通过。

done：
- 一个且仅一个真实private delayed-history observe-only probe完成并由独立verifier接受。
- Exact `5 direct unknown -> 4.0s delay -> 1 history -> 0.5s reserved final snapshot` live path被真实endpoint evidence覆盖。
- Order、cancel、submit、flatten、position delta和public market-data连接均为零。
- 本任务通过独立QA后，才可重新判断Principal Task 8 fixed-quote observe-only或后续activation。

blockers：
- Direct `unknownOid` result允许额外 `orders/error/unknown`字段，producer和verifier均可能将malformed response解释为exact unknown。
- History row的target token、alias、`attempt`和`historical_row_classifications`未与deterministic synthetic reference及raw history独立绑定。
- 修复必须offline-only；不得重跑T040 live或解锁dynamic spread、fill feedback、inventory skew、multi-level。

commit：
- implementation `76bea45046c21d99299b4801c82a262dec6b6e7d`

提交信息：
- `Add delayed history observe-only probe`
