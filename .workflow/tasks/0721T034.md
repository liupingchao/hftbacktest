# 0721T034 DELAYED-PROTOCOL-MARKER-TRIGGER-REPAIR

执行线程：
- 总控 auto-loop / 业务执行线程

任务ID：
- 0721T034

标题：
- Make delayed-history validation independent of the protocol marker value

简短描述：
- 修复 T033 QA 的唯一 P1：producer 和 independent audit helper 不能用 exact marker 自身决定是否执行 delayed validation。
- Historical call、delayed timing evidence 或 Task 12 rollout requirement 任一成立时，都必须要求 exact marker 和完整 T033 production contract。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0721T033`（未通过）

规则更新提醒：
- 本任务 offline-only。
- T031 live artifacts immutable。
- T033 已接受的 exact timing/caps、manager contract、quarantine 和 historical boundaries 不得回退。
- 独立 QA 通过前不得进行 private read、新 live 或任何 adaptive/multi-level activation。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `.workflow/reports/0721T033-qa.md`

是否需要提交代码：
- 需要

提交要求：
- 业务实现和业务报告分开提交。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

files：
- Modify: `examples/hyperliquid/hyperliquid_tiny_live_m2_fill_window.py`
- Modify: `examples/hyperliquid/cross_exchange_t024_same_window_acceptance.py`
- Test: `examples/hyperliquid/test_hyperliquid_tiny_live_m2_fill_attribution.py`
- Test: `examples/hyperliquid/test_cross_exchange_t024_same_window_acceptance.py`
- Workflow: task、business、QA、fixed QA、`task_plan.md`、`progress.md`、`findings.md`

action：
- Define delayed-validation-required independently from marker equality。
- Require delayed validation when any historical query exists or any delayed timing field is present。
- Let Task 12 rollout continue to require the protocol even when no historical call completed。
- Missing、wrong、empty、non-string or legacy marker must fail with an explicit protocol reason whenever delayed validation is required。
- Exact marker with exact T033 timing remains pass。
- Legacy v4 evidence with no historical call、no delayed fields and pre-rollout task remains unchanged。

verify：
- Missing/wrong/empty/bool marker plus historical call fails producer and independent audit identically。
- Missing/wrong marker plus delayed timing fields but zero completed history calls fails。
- Task 12 T034+ missing/wrong marker fails end to end。
- Legacy marker-absent direct-only v4 tests remain pass where previously accepted。
- Focused/full Hyperliquid regression passes。
- T031 blocked、T026 pass、T016/T022 blocked。
- No live/private/network/remote/service operation occurs。

stop conditions：
- Any weakening of T033 exact timing、manager or quarantine contracts。
- Any live executor timing、endpoint or strategy behavior change。
- Any historical evidence modification。
- Any live/private/network/remote/service action。

done：
- Marker missing/wrong hostile probes fail consistently across producer、independent helper and Task 12。
- Source is ready for independent QA and only then a fixed-quote observe-only live task。

blockers：
- Independent QA acceptance。

commit：
- implementation `fe8d725304ca8edf27b32de8f95322bce458154d`

提交信息：
- implementation `Repair delayed history marker trigger`
