```md
执行线程：
- 业务线程-python

任务ID：
- 0518T003

标题：
- Step 5B quote-anchor / post-only read-only diagnostic

简短描述：
- 基于 `0518T002` 的 Step 5A design contract，实现并运行只读诊断，量化 BBO/bookTicker、depth/top5、quote distance、stale quote、post-only/reject/throttle/churn、latency regime 与 fill/markout 的关系。本任务只做诊断，不改策略。

状态：
- 待执行

执行顺序：
- `0518T002` QA 后执行

前置任务：
- `0518T002` QA 已通过

规则更新提醒：
- 本任务是 read-only diagnostic，不是 quote-control implementation。
- 不允许修改 strategy behavior、fair/reservation、quote placement、risk guards 或 live scripts。
- 不允许启动 live、采集新样本、默认开启任何规则或做 promotion 判断。

按线程规则执行：
- .workflow/workflow-kit/thread-report-template.md
- docs/thread-playbook.md
- task_plan.md
- progress.md
- findings.md

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

Allowed files：
- `.workflow/tasks/0518T003.md`
- `.workflow/reports/0518T003-business.md`
- `examples/binance_tick_mm/quote_anchor_diagnostic.py`
- `examples/binance_tick_mm/test_quote_anchor_diagnostic.py`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `.workflow/dashboard.html`
- `.workflow/dispatch_suggestions.md`

Allowed generated outputs：
- `local_live_analysis/5-13-day-control-30min/stage5b_quote_anchor_diagnostic_0518T003/**`

Allowed read-only inputs：
- `.workflow/reports/0518T002-business.md`
- `.workflow/reports/0518T002-qa.md`
- `.workflow/reports/0514T001-business.md`
- `.workflow/reports/0514T003-business.md`
- `.workflow/reports/0514T005-business.md`
- `local_live_analysis/5-13-day-control-30min/audit_live_5-13-day-control-30min.csv`
- `local_live_analysis/5-13-day-control-30min/t009_fixed_sidecar/**`
- `local_live_analysis/5-13-day-control-30min/stage4_pricing_research_0514T003/**`
- `local_live_analysis/5-13-day-control-30min/stage5_execution_outcome_labels_0514T005/**`

Forbidden files / areas：
- strategy behavior, fair/target formulas, configs, risk guards, quote placement behavior
- live scripts, AWS/remote state, live collection
- replay fill model or replay lifecycle repair
- core Rust, connector, py `event_dtype`, standard npz schema
- default-on or default-off strategy implementation

Requires separate task：
- Any quote-anchor / post-only implementation.
- Any default-off strategy experiment.
- Any live micro test or sample collection.
- Any promotion decision.

Fatal Failures：
- 把 diagnostic runner 写成策略实现。
- 修改 quote placement 或 post-only behavior。
- 用单样本 diagnostic 直接授权 strategy implementation 或 live。

action：
- 实现 Step 5B read-only quote-anchor / post-only diagnostic runner。
- 在 `5-13-day-control-30min` 上运行。
- 至少输出：
  - `quote_anchor_diagnostic_summary.md`
  - `bbo_source_drift.csv`
  - `quote_distance_bucket_summary.csv`
  - `post_only_reject_throttle_churn_summary.csv`
  - `stale_latency_guard_summary.csv`
  - `anchor_source_x_fill_markout.csv`
  - `run_manifest.json`
- 必须量化：
  - bookTicker/BBO vs depth/top5 BBO drift
  - quote distance / touch / step-back bucket
  - crossed / post-only-risk candidates
  - reject / throttle / drop / churn bucket
  - stale quote / join age / latency bucket
  - fill rate、time-to-fill、fill markout、missed opportunity 与 quote-anchor state 的关系
- 必须保留 observed-only 边界，不得声明 counterfactual fill proof。

verify：
- `python -m pytest examples/binance_tick_mm/test_quote_anchor_diagnostic.py`
- `python examples/binance_tick_mm/quote_anchor_diagnostic.py --help`
- `python examples/binance_tick_mm/quote_anchor_diagnostic.py --run-dir local_live_analysis/5-13-day-control-30min --output-dir local_live_analysis/5-13-day-control-30min/stage5b_quote_anchor_diagnostic_0518T003`
- `python3 .workflow/build_dashboard.py`
- `git diff --check`

done：
- 回报必须说明 quote-anchor / post-only diagnostic 的主要发现。
- 回报必须说明哪些 5A 设计假设被支持、哪些需要修改。
- 回报必须说明是否足以创建后续 default-off implementation task。
- 回报必须明确未改策略、未启动 live、未补样本。
- 回报必须写 `commit id` 和提交信息。
```
