# 任务派发

执行线程：
- SKHYNIX Flow Internal Directional Alpha A0 Execution 业务线程

任务ID：
- 0828T013

标题：
- 实现并执行 FLOW_INTERNAL_DIRECTIONAL_ALPHA_V1 A0

简短描述：
- 严格按 0828T012 已冻结并通过 QA 的 zero-target contract，在独立干净
  worktree 中实现 continuous active flow directional-dominance detector，
  对 29 个 admitted Binance captures 执行 A0 support audit，并输出唯一
  classification。

状态：
- 待验收

执行顺序：
- 当前唯一 formal task

前置任务：
- 0828T012（已通过）

规则更新提醒：
- 已阅读 AGENTS.md、workflow-kit manual、task_plan.md、progress.md、
  findings.md、FLOW_INTERNAL_DIRECTIONAL_ALPHA_V1 A0 plan/QA 和
  predecessor 0828T011 implementation/evidence。

按线程规则执行：
- AGENTS.md
- .workflow/workflow-kit/workflow-manual.md

是否需要提交代码：
- 需要

提交要求：
- 自测、29-capture canonical build 和 deterministic rebuild 通过后提交。
- 回报必须带 commit id 和提交信息。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- examples/hyperliquid/skhynix_flow_internal_directional_alpha_a0.py
- examples/hyperliquid/test_skhynix_flow_internal_directional_alpha_a0.py
- local_live_analysis/skhynix_flow_internal_directional_alpha_a0_0828T013/
- .workflow/tasks/0828T013.md
- .workflow/reports/0828T013-business.md

action：
- 绑定 frozen plan SHA
  `6278d6b5f0d0dfd855b2d2ee121fa4fbebf6b3a864acecc4a0f5368e558aeeed`。
- 从 commit `91cc0770` 验证四个 source-authority blobs 和 exact 29-row
  inventory。
- 实现 exact 20ms right-open bins、atomic depth/trade/OFI/activity
  contributions、100/500/2000ms bounded ratios 和 calibration-only
  `Q_activity_60`。
- 实现 total mixed/candidate/dominant/flip/release/refractory state machine。
- 构造 deterministic dual-direction controls、chronological no-reuse
  matching、dependence/overlap geometry 和 A0 gates。
- 仅使用 timestamp/quality geometry 选择并冻结 `primary_tau`。
- 生成 contracts、support ledgers、summary、classification 和 run
  manifest。
- 执行 canonical build 与 fresh-output rebuild，核对所有非 cache artifact
  bytes。

verify：
- focused pytest。
- Python compile。
- ruff。
- source size/SHA closure。
- exact bin/ratio/state/matching/gate focused invariants。
- zero-outcome ledger。
- deterministic double-build identity。
- artifact size/SHA closure。
- git diff --check。

done：
- 输出 canonical `classification.json` 和 `reports/A0_summary.json`。
- 明确 `A1_authorized` 与 `primary_tau`。
- 报告 feature availability、anchor support、state compression、
  dependence、control common support 和 geometry gates。

明确禁止：
- 不读取 future midpoint/BBO barrier outcome、markout、fill、fee 或 PnL。
- 不拟合 H0/H1/H2。
- 不修改 frozen ratio/window/persistence/refractory/control/gate tuple。
- 不因 anchor 稀疏、密集或日期集中而做参数 rescue。
- 不收集新数据，不访问 private/order API。
