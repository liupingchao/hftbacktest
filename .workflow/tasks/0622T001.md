```md
执行线程：
- 业务线程-live

任务ID：
- 0622T001

标题：
- M2 fresh-touch dynamic-size session-gate implementation and controlled live micro-test

简短描述：
- 基于已通过 QA 的 `0619T002` redesign contract，把 `fresh_touch_size_by_throughput_session_gate` 真正落到 Hyperliquid M2 `fill_window` / `fill_loop`，并在本任务内完成 focused verification、remote checkout refresh、final gate、public precheck 和一个受控 live micro-test。
- 本任务的目标是快速进入 live data 校准闭环：若 gate 和 session-gate 通过，最多提交 `2` 个真实 post-only `Alo` maker orders；若无 eligible micro-window、无 fill 或 T008 ledger 不能证明 fee/inventory/realized PnL，则 fail closed，M2 仍保持 blocked。

状态：
- 待执行

执行顺序：
- 当前唯一任务

前置任务：
- `0619T002` QA 为 `已通过`。
- `0618T008` QA 为 `已通过`，T008 ledger/reconciler 必须继续作为 M2 PnL 事实源。
- `0619T001` QA 为 `阻塞`，其 no-fill / crowded-touch / quote-aging 事实是本任务的实现输入。

规则更新提醒：
- 请先阅读 `.workflow/workflow-kit/workflow-manual.md`、`.workflow/workflow-kit/task-dispatch-template.md`、`.workflow/workflow-kit/thread-report-template.md`、`.workflow/workflow-kit/qa-acceptance-template.md`、`task_plan.md`、`progress.md`、`findings.md` 后再执行本任务。

按线程规则执行：
- `AGENTS.md`
- `docs/hyperliquid_tiny_live_real_order_executor.md`
- `docs/hyperliquid_tiny_live_final_go_no_go_gate.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `examples/hyperliquid/hyperliquid_tiny_live_m2_fill_window.py`
- `examples/hyperliquid/hyperliquid_tiny_live_m2_fill_loop.py`
- `examples/hyperliquid/test_hyperliquid_tiny_live_m2_fill_loop.py`
- `local_live_analysis/hyperliquid_tiny_live_m2_fresh_touch_live_0622T001/**`
- `.workflow/reports/0622T001-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

read-only inputs：
- `.workflow/tasks/0619T002.md`
- `.workflow/reports/0619T002-business.md`
- `.workflow/reports/0619T002-qa.md`
- `.workflow/reports/0619T001-qa.md`
- `local_live_analysis/hyperliquid_tiny_live_m2_flow_aware_retry_0619T001/**`
- `local_live_analysis/hyperliquid_tiny_live_m2_public_flow_diagnosis_0618T012/**`
- `local_live_analysis/hyperliquid_tiny_live_m2_pnl_ledger_0618T008/**`

hard boundary：
- Keep Hyperliquid `Alo` post-only. No taker/crossing, no `Ioc`, no one-tick-back workaround, no cap relaxation, no default-on behavior, no promotion, no stable PnL claim, and no M3 readiness claim.
- Do not submit any live order until local focused verification passes, remote checkout refresh succeeds, final gate returns go, public precheck succeeds, and the new session-gate finds an eligible `quality_a` or `quality_b` candidate.
- Live execution is limited to one controlled micro-window and at most `2` real order submissions. Any extra live order, any relaxed cap, or any non-`Alo` TIF is a task failure.
- Preserve tracked cancel, final open-orders proof, independent remote open-orders check, redaction, and T008 ledger fail-closed.
- Do not print or persist credential values, private keys, raw signatures, nonces, or wallet secrets.

implementation action：
- Implement a new policy version named `m2_fresh_touch_size_by_throughput_session_gate_v1` in `fill_window`.
- Keep quote placement at touch with `quote_offset_ticks=0` when a quote is allowed.
- Add fresh-touch / queue-reset gating:
  - require defensible public evidence that the selected same-side touch is fresh or reset recently;
  - if touch freshness cannot be computed, skip rather than submit.
- Add candidate quality buckets:
  - `quality_a`:
    - same-side top depth multiple `<=20x`
    - same-side top order count `<=6`
    - same-side strict-through support present
    - touch freshness / queue-reset evidence present
    - max hold `<=3s`
  - `quality_b`:
    - same-side top depth multiple `>20x` and `<=100x`
    - same-side top order count `<=12`
    - same-side strict-through support present
    - recent same-side at-or-through trade throughput supports a small passive order
    - max hold `<=1s`
  - anything outside these buckets must skip instead of submit.
- Add dynamic size:
  - hard cap `<=0.005 BTC`
  - computed size `min(bucket_cap, 0.25 * recent_same_side_at_or_through_trade_qty_btc_last_3s, 0.005 BTC)` floored to lot size
  - `quality_a` bucket cap `0.005 BTC`
  - `quality_b` bucket cap `0.002 BTC`
  - if computed size floors to zero or throughput evidence is missing, skip rather than force an order.
- Add side/session gating:
  - default live side is `buy_only`;
  - sell must remain disabled in this task unless both same-window precheck and an explicit later scorecard artifact materially favor sell;
  - no fixed UTC-hour allowlist is authorized in this task;
  - live can execute only inside a current public-precheck-confirmed micro-window.
- Update `fill_loop` defaults for this task:
  - task/output path uses `0622T001`;
  - default policy uses fresh-touch dynamic-size session-gate;
  - max live submissions per run is `2`;
  - abort quickly if no eligible `quality_a` / `quality_b` candidate appears.
- Preserve existing remote refresh, final gate, artifact pullback, T008 ledger, and independent open-orders verification flow.

required artifacts：
- `touch_freshness_matrix.csv`
- `dynamic_size_decision_matrix.csv`
- `session_side_eligibility.csv`
- `time_gate_decision_matrix.csv`
- updated `quote_attempt_matrix.csv`
- `window_result_matrix.csv`
- final gate manifest and remote facts
- aggregate live fill ledger
- T008 ledger manifest/output
- independent remote open-orders proof

live execution rules：
- Run focused tests and local CLI checks before any remote/live step.
- Refresh `/home/admin/hftbacktest-cross-exchange` by git-safe incremental bundle / `merge --ff-only`; fail if branch is not `cross-exchange` or dirty count is nonzero.
- Rerun final gate; stop before live if `allow_create_0617T008 != true`.
- Run public precheck/session-gate; stop before live if no eligible micro-window exists.
- If eligible, submit at most `2` real post-only `Alo` orders under the new policy.
- Always tracked-cancel live orders and prove final open orders are empty.
- Always run T008 ledger after pullback; no fill or incomplete economics/inventory evidence must return fail-closed.

expected outcomes：
- If at least one live maker fill occurs and T008 ledger proves fee/inventory/mark/realized PnL evidence, report the exact PnL facts without claiming stability.
- If no fill occurs, report the new gated no-fill artifacts as calibration input and end `阻塞`, not `已通过`.
- If no eligible micro-window appears, do not force an order; end `阻塞` with precheck/session-gate artifacts.

verify：
- `python -m pytest examples/hyperliquid/test_hyperliquid_tiny_live_m2_fill_loop.py -q`
- `python -m pytest examples/hyperliquid/test_hyperliquid_tiny_live_m2_pnl_ledger.py -q`
- `python -m py_compile examples/hyperliquid/hyperliquid_tiny_live_m2_fill_window.py examples/hyperliquid/hyperliquid_tiny_live_m2_fill_loop.py`
- `python examples/hyperliquid/hyperliquid_tiny_live_m2_fill_window.py --help`
- `python examples/hyperliquid/hyperliquid_tiny_live_m2_fill_loop.py --help`
- `git diff --check`
- If live reaches the execution gate: remote refresh evidence, final gate go/no-go, pulled-back live artifacts, independent remote open-orders check, and T008 ledger output.

done：
- 回报实现内容、policy/version、测试命令、remote refresh/final gate 结果、是否进入 live、live submissions 数、fill_count / maker_fill_count、T008 ledger 结论、artifact 路径、blockers、commit id、提交信息。
- 明确说明 M2 是否仍 blocked；除非 T008 ledger 证明 live fill / fee / inventory / realized PnL，否则不得声明 M2 完成、稳定 PnL、或 M3 ready。
```
