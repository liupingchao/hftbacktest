# 任务派发

执行线程：
- 业务线程-python/public-data-infra

任务ID：
- 0806T001

标题：
- SKHYNIX-2H-CONTINUOUS-PUBLIC-COLLECTION-AND-AMD-TRANSFER

简短描述：
- 在保留的 `c6in-winner` 上启动一次连续 `7200s` 的 SKHYNIX
  Binance/Hyperliquid public market-data 采集。
- 采集成功后由本机独立 watcher 自动传输完整 raw campaign 到
  `amdserver`，并执行逐文件 SHA256/大小对账。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0805T001` 已通过，提供 bounded recovered core-L2 reconnect 门禁。
- `0805T003` 已通过，不与本任务并行写入。

规则更新提醒：
- 无

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `AGENTS.md`

是否需要提交代码：
- 不需要

提交要求：
- 当前工作区包含已验收但未提交的前置工作，不创建混合提交。
- 回报使用 `commit：无`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 采集和传输完成后，复核 systemd 终态、campaign manifest、runtime
  source SHA、两端逐文件 SHA/大小清单及 AMD 目标目录原子发布结果。

files：
- c6in runtime：`/home/admin/0806T001-hftbacktest`
- c6in raw campaign：`/home/admin/0806T001_skhynix_2h_continuous`
- AMD raw campaign：
  `/home/molly/project/hftbacktest/local_live_analysis/0806T001_skhynix_2h_continuous`
- `.workflow/runners/0806T001_transfer_to_amdserver.sh`
- `.workflow/runners/com.hftbacktest.0806T001.transfer.plist`
- workflow task/progress/findings/report files

action：
- 从当前本地源码冻结本次 collection runtime，并在远端运行 focused
  tests/CLI preflight。
- 使用 `--hyperliquid-research-max` 隐含的完整 Hyperliquid 多轨道采集：
  fast market、standard L2、asset context、main allMids 和 target-dex
  allMids；Binance 保留 trade、depth@0ms、bookTicker 和 depth snapshot。
- 使用单一连续 segment，避免人为的半小时 segment 重连边界。
- 启用 opt-in recovered core-L2 reconnect 合同；只有 transport marker、
  re-ACK、恢复 channel、recovery snapshot 和精确时间边界全部闭合，
  且单次不超过 `15s`、累计不超过 `30s` 时才允许 raw campaign 通过。
- 云端仅采集，不运行 timeline、R0、R1、basis 或其他研究计算。
- 本机 watcher 只在 systemd 成功退出且 collection-only manifest
  `passes=true` 后传输；使用临时目录、逐文件 SHA/大小对账和原子 rename
  发布到 AMD。

verify：
- focused pytest、py_compile、CLI help。
- systemd unit active/running、MainPID、heartbeat、run status 和双 venue
  child process 启动证据。
- 终态由 watcher 检查 `Result=success`、`ExecMainStatus=0`、
  `network_collection_complete=true`、`postprocess_pending=true`。
- c6in 与 AMD 完整文件 inventory 的 relative path、size、SHA256
  必须完全一致。

done：
- 2H public collection 已启动且不依赖当前交互 shell。
- 自动传输 watcher 已启动且不依赖当前 Codex 会话。
- 最终完成条件是 raw campaign 已原子发布到 AMD，且两端 inventory
  完全一致。

hard boundary：
- 仅 public market data；不访问 credential/private/account/order/cancel
  endpoint，不下单。
- 用户已在 2026-08-06 明确授权立即开始本次 2H 采集。
- 不覆盖任何历史 campaign 或 AMD 数据目录。
- 本任务不形成 signal、arbitrage、exact fill、maker PnL 或 live
  promotion 结论。
