```md
执行线程：
- 业务线程-research

任务ID：
- 0609T001

标题：
- Basis-positive wrong-way decomposition and targeted sample design

简短描述：
- 基于 `0608T006` 对 `context_basis_mid_ticks > 0` 的 independent robustness 结果，进一步只读拆解 wrong-way tail 是否可由 decision-time 字段解释，并输出后续 targeted sample collection 的样本设计。本任务不采集新数据、不实现策略、不生成 case-library 或 shadow decisions、不启动 live。

状态：
- 待执行

执行顺序：
- 当前唯一任务，必须在 `0608T006` QA 通过后执行

前置任务：
- `0608T006` QA 已通过。

规则更新提醒：
- 本任务只做 read-only decomposition / collection-design；不得采集新数据、不得实现策略、不得生成 case-library 或 shadow decisions、不得输出交易动作/下单方向/quote/order 行为、不得接 private/order endpoint、不得 live/default-on/tiny-live、不得 promotion。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `docs/thread-playbook.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交任务文件、业务报告、read-only runner/tests、task-scoped artifacts 和必要 workflow tracking 更新。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0609T001.md`
- `.workflow/reports/0609T001-business.md`
- `examples/hyperliquid/canonical_basis_positive_wrong_way_decomposition.py`
- `examples/hyperliquid/test_canonical_basis_positive_wrong_way_decomposition.py`
- `local_live_analysis/canonical_basis_positive_wrong_way_decomposition_0609T001/**`
- 必要 workflow tracking 文件仅限本任务状态/结论更新。

read-only inputs：
- `local_live_analysis/canonical_basis_positive_robustness_0608T006/**`
- `local_live_analysis/event_mode_canonical_pricing_signal_0604T003/**`
- Row-level canonical sample files resolved from `multi_sample_manifest.json` `samples[].pricing_signal_rows`.
- T006 prerequisite manifest must be validated before analysis:
  - `task_id=0608T006`
  - `schema_version=canonical_basis_positive_robustness_v1`
  - `final_recommendation=needs_more_samples`
  - `scope_policy=not_limited_to_regime_011`
  - `t005_final_contract_decision=upgrade_to_context_only_supported`
- Accepted loader/source-lock foundation:
  - `examples/hyperliquid/canonical_event_mode_evidence.py`
  - `local_live_analysis/canonical_event_mode_evidence_0604T004/**`
  - `local_live_analysis/canonical_evidence_source_lock_0604T005/**`

analysis scope：
- Pattern under study:
  - `context_basis_mid_ticks > 0`
- Required comparisons:
  - `basis > 0` versus `basis <= 0`, excluding rows where `context_basis_mid_ticks` is missing or non-numeric.
  - positive-basis magnitude bins
  - wrong-way rows versus non-wrong-way rows
  - basis effect controlled by Binance momentum buckets
  - basis effect controlled by Hyperliquid book-state buckets
- Wrong-way tail rows:
  - primary horizon `1000ms`
  - define wrong-way as `hyperliquid_future_mid_move_ticks < 0` for `basis > 0`
  - report p95/max wrong-way loss drivers using decision-time-visible fields only

required decomposition dimensions：
- Spread bucket from `context_hyperliquid_spread_ticks`.
- Basis magnitude bucket from `context_basis_mid_ticks`.
- Binance momentum sign/magnitude from decision-time Binance movement fields, primarily `input_binance_mid_move_ticks_from_prev`; if unavailable, emit an explicit unavailable bucket rather than substituting a future label.
- Hyperliquid top5 imbalance sign and magnitude.
- Hyperliquid microprice-minus-mid sign and magnitude.
- Join age from `context_hyperliquid_join_age_ms`.
- Future-independent volatility / movement context where available.
- Sample and coarse time-window concentration.

required diagnostics：
- Baseline comparison:
  - rows, samples, hit rate, mean future move, tail loss for `basis > 0` and `basis <= 0`.
- Magnitude monotonicity:
  - small / medium / large positive-basis bins.
  - check whether future move strengthens monotonically or only appears in one bin.
- Controlled effects:
  - within same Binance momentum bucket, does basis sign/magnitude add information?
  - within same Hyperliquid book-state bucket, does basis sign/magnitude add information?
- Tail filter feasibility:
  - identify whether p95/max wrong-way rows concentrate in decision-time-visible states.
  - classify candidate filters as `promising_visible_filter`, `needs_more_samples`, or `not_filterable`.
- Targeted sample design:
  - specify what sample regimes are needed next: active/high-vol, normal, quiet, wide-spread, basis-large, HL-book-conflict, or other visible regimes.
  - define minimum evidence needed to reduce sample concentration and validate or reject proposed tail filters.
  - must state that any collection is a separate future task requiring its own dispatch and QA.

allowed final recommendations only：
- `targeted_collection_ready`
- `needs_more_existing_decomposition`
- `reject_tail_not_filterable`

recommendation rules：
- `targeted_collection_ready` requires:
  - wrong-way tail has at least one plausible decision-time-visible explanation or filter hypothesis.
  - basis-positive retains nontrivial signal after controlling for Binance momentum and Hyperliquid book-state buckets.
  - targeted sample plan gives concrete regimes and evidence gates.
  - This is a recommendation that a later separately scoped collection task can be designed; it does not authorize collection inside T001.
- `needs_more_existing_decomposition` applies when current artifacts are insufficient to judge filterability but do not reject the hypothesis.
- `reject_tail_not_filterable` applies when wrong-way tail is not concentrated in visible states, basis adds no controlled effect, or the signal is explained by a redundant proxy.

forbidden：
- New data collection.
- Strategy implementation, directional strategy implementation, order behavior implementation, case-library implementation, shadow decision generation, live/default-on/tiny-live, promotion.
- Hyperliquid or Binance private/account/order endpoints.
- Order lifecycle, submit/cancel/fill logic, production config/default changes, parameter search or optimization sweep.
- Connector/core API changes, standard npz schema changes, canonical Binance maker audit schema changes.
- Treating ordinary synthetic fixed-grid artifacts as formal evidence.
- Treating `100/250ms` evidence as canonical independent support.
- Outputting executable trading instructions, actual order side, quote price, size, leverage, stop rule, take-profit rule, or deployment recommendation.

outputs：
- `basis_positive_wrong_way_manifest.json`
- `basis_positive_vs_nonpositive_baseline.csv`
- `basis_positive_magnitude_bins.csv`
- `basis_positive_wrong_way_rows.csv`
- `basis_positive_tail_state_decomposition.csv`
- `basis_positive_controlled_effects.csv`
- `basis_positive_tail_filter_feasibility.csv`
- `basis_positive_targeted_collection_plan.md`
- `basis_positive_next_step_recommendation.md`

acceptance criteria：
- Runner consumes only accepted local canonical event-mode artifacts and T006 artifacts.
- Runner validates the T006 prerequisite manifest fields listed in read-only inputs before computing decomposition.
- Runner refuses non-canonical or diagnostic-only synthetic formal inputs through the accepted loader/source-lock path.
- Outputs include all required artifact files and use only the allowed final recommendation taxonomy.
- Report explicitly separates economic idea / read-only context evidence / execution-PnL caveat.
- Report explicitly states no new data was collected and no strategy/private/order/live/case-library/shadow/promotion action is authorized.
- Tests cover baseline comparison, magnitude bins, wrong-way row extraction, controlled effects, tail filter feasibility taxonomy, targeted collection plan generation, and forbidden-boundary text.

verify：
- `python examples/hyperliquid/canonical_basis_positive_wrong_way_decomposition.py --help`
- `python -m py_compile examples/hyperliquid/canonical_basis_positive_wrong_way_decomposition.py examples/hyperliquid/test_canonical_basis_positive_wrong_way_decomposition.py`
- `python -m pytest examples/hyperliquid/test_canonical_basis_positive_wrong_way_decomposition.py`
- `python examples/hyperliquid/canonical_basis_positive_wrong_way_decomposition.py --input-dir local_live_analysis/event_mode_canonical_pricing_signal_0604T003 --t006-dir local_live_analysis/canonical_basis_positive_robustness_0608T006 --output-dir local_live_analysis/canonical_basis_positive_wrong_way_decomposition_0609T001`
- `python -m json.tool local_live_analysis/canonical_basis_positive_wrong_way_decomposition_0609T001/basis_positive_wrong_way_manifest.json`
- Scripted or manual boundary check that outputs contain no executable trading instruction, order side, quote price, size, private/order endpoint, case-library trigger, shadow decision, live/default-on/tiny-live, or promotion claim.
- `git diff --check`

done：
- Business report must include method, input source-lock, basis-positive economic hypothesis, baseline comparison, magnitude monotonicity, wrong-way tail decomposition, controlled-effect checks, tail filter feasibility, targeted sample design, final recommendation, artifact paths, verification commands, blockers/caveats if any, commit id, and commit message.
- Business report should end in `待验收` if verification passes.
```
