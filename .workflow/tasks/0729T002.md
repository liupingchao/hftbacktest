# 0729T002 C6IN-RETAIN-FINALIZER-OFFLINE-REPAIR

执行线程：
- 总控 / 业务执行线程

任务ID：
- 0729T002

标题：
- 修复 c6in 胜者保留顺序、成功路径测试与源码 provenance

简短描述：
- 纯离线修复 T001 QA 指出的 finalizer mutation 顺序、成功路径测试、
  staged diff hygiene 和 rack-level 命名问题。
- 保留 T001 已接受的 7 台 runtime 数据与当前 AWS 双机终态；不执行任何
  AWS mutation，不重跑 hunting，不修改、重启或停止两台 running 实例。

状态：
- 已通过

执行顺序：
- 当前唯一正式任务

前置任务：
- `0729T001`（未通过；runtime 数据与 AWS 终态已被 QA 接受）

规则更新提醒：
- 无

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `AGENTS.md`

是否需要提交代码：
- 需要实现并加入 Git 跟踪；本任务不要求创建 commit。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `latency-probe/ec2_spread_hunt.py`
- `latency-probe/test_ec2_hunt.py`
- `docs/evidence/ec2_latency_hunt_20260729/runtime_control_source.tar.gz`
- `docs/evidence/ec2_latency_hunt_20260729/execution_manifest.json`
- `docs/evidence/ec2_latency_hunt_20260729/README.md`
- `.workflow/tasks/0729T001.md`
- `.workflow/tasks/0729T002.md`
- `.workflow/reports/0729T001-business.md`
- `.workflow/reports/0729T001-qa.md`
- `.workflow/reports/0729T002-business.md`
- `.workflow/reports/0729T002-qa.md`
- `docs/qa-acceptance-report.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

action：
- 在修改前封存 T001 实际 runtime control source，验证 archive 内各文件
  SHA 与 T001 execution manifest 的 runtime source hashes 一致。
- 调整 `finalize_winner`：先写 winner tags、启用 termination
  protection，并只读验证 protection=true；之后才允许 terminate 六台
  losers 和删除临时 bucket。
- 保持 failure cleanup 可先解除 task candidate protection 再清理，但
  protected baseline `awsserver1` 永不进入任何 mutation ID 集合。
- 新增 finalizer 成功路径测试，验证 mutation 调用顺序、唯一 winner、
  六台 losers、winner tags/protection、保留 spread group、bucket 清理和
  `awsserver1` 排除。
- 修正 rack-level 测试命名和两处 EOF 空行。
- execution manifest 区分 T001 runtime source hashes 与 T002
  post-QA repair source hashes；不得把离线修复后的代码声明为 7 台 run
  实际执行代码。
- 业务回报改为事实准确的 staged diff 结果。

verify：
- Python focused tests 全通过，并包含 success finalizer mutation-order
  断言。
- T001 runtime source archive 解包文件 hashes 与原 manifest 一致。
- T002 repaired source hashes 与当前文件一致。
- Rust focused test/clippy、Python compile、shell syntax 通过。
- `git diff --check` 和 `git diff --cached --check` 均通过。
- AWS 只允许只读复核当前两台 running、winner protection、六台
  terminated、bucket 404；不得发起 mutation。

done：
- staged controller 对未来同类 hunting 使用正确的先保护后清理顺序。
- success/failure 路径均有 focused regression。
- T001 runtime provenance 与 T002 offline repair provenance 可明确区分。
- 当前 `awsserver1 + c6in winner` 终态保持不变。

blockers：
- 本任务不改变 T001 已发生的实际 mutation 顺序，只修复未来执行代码并
  如实保留历史事实。
- CloudTrail `LookupEvents` 权限缺失不在本任务修复范围。
