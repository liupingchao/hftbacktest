# 0718T024 T023-EXACT-ENVELOPE-IDENTITY-REPAIR-RERUN

执行线程：
- 总控 auto-loop / 业务实现线程

任务ID：
- 0718T024

标题：
- T023 exact envelope and artifact identity repair/re-run

简短描述：
- Repair the remaining preorder-blocked artifact identity path, prove the exact task-scoped live envelope and identity without network access, then execute one clean replacement window for the blocked Principal Alignment Task 12 acceptance.
- This task supersedes only the invalid T023 evidence window; it does not broaden strategy behavior or live authorization.

状态：
- 执行中

执行顺序：
- 当前唯一任务

前置任务：
- 0718T023（阻塞，诊断窗口不可作为 accepted baseline）

规则更新提醒：
- Standing authorization permits uninterrupted task-scoped live work within BTC, Binance lead / Hyperliquid maker, `0.005 BTC` per order, `0.01 BTC` aggregate position delta, `1 USDC` max loss, `2` submissions and `1800s` for this bounded window.
- Live is the main data source. Shadow/replay prevents obvious errors, reproduces live decisions and controls variables; it is not a long pre-live research stage.
- Execution-safety and evidence-integrity gates remain mandatory before any real order.
- Single-level fixed-spread post-only behavior remains authoritative. Dynamic spread, fill feedback, inventory skew, multi-level activation and actual quote behavior changes remain off.
- Any exact envelope, identity, reconciliation, termination or checksum mismatch is a stop condition; no second live window is allowed inside this task.

是否需要提交代码：
- 需要

提交要求：
- 离线修复、自测和 preflight 通过后提交，再从该 exact commit 创建 clean remote source archive。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

files：
- Modify: `examples/hyperliquid/hyperliquid_tiny_live_m2_fill_window.py`
- Modify if required: `examples/hyperliquid/cross_exchange_live_remote_orchestrator.py`
- Add/modify focused tests under `examples/hyperliquid/`
- Add a narrow same-window acceptance helper and focused tests under `examples/hyperliquid/` if existing T010 tooling cannot consume the event-driven live artifact layout.
- Evidence: `local_live_analysis/principal_alignment_task12_repair_0718T024/`
- Workflow: this task, business report, QA report, `docs/qa-acceptance-report.md`, `task_plan.md`, `progress.md`, `findings.md`

action：
- Remove stale constant task/window identity from every preorder-blocked artifact emitted by the T024 fill-window path.
- Add focused regression coverage proving both successful and fail-closed paths use caller-supplied task/window identity.
- Construct the remote watcher command without network/order access and record a preflight artifact proving:
  - `artifact_task_id=0718T024`
  - `artifact_window_id=1`
  - `max_order_size_btc=0.005`
  - `max_loss_usdc=1.0`
  - `max_position_btc=0.01`
  - `max_real_order_submissions=2`
  - all strategy activation flags remain false.
- Commit the repair and use that exact commit to create an isolated clean remote source archive.
- Before live, verify remote source marker/import provenance, no conflicting service/process, kill-switch clear, account BTC position/open orders compatible with the envelope, SDK/env-file availability, output path isolation and terminal cleanup commands.
- Run exactly one bounded event-driven single-level fixed-spread post-only Hyperliquid BTC live window, at most `1800s`, `0.005 BTC` per order and `2` submissions.
- Pull artifacts locally, verify remote/local SHA-256, child exit/reap, final owned open orders, independent private open-orders proof, account position and max-loss reconciliation.
- Run a same-window acceptance that reproduces source/config/decision/control facts and keeps live order/fill facts authoritative.
- Preserve no-fill/single-digit-fill boundaries: mechanism and evidence integrity may pass without stable economics; profitability, fill-rate, fee/rebate calibration, queue priority and maker viability remain unsupported unless directly proven.

verify：
- Focused preorder-blocked identity tests.
- Existing orchestrator, event-driven watcher, executor, integrated acceptance and fill attribution regressions.
- Modified Python `py_compile`, relevant CLI `--help`, and `git diff --check`.
- Preflight artifact exact-match assertions for task/window identity, envelope and activation-off state.
- Remote source commit equals the committed local source used for the run.
- Endpoint call counts, order lifecycle, fill identity/role/fee evidence if present, final open orders, account position and max-loss reconciliation.
- Terminal artifact checksum and remote/local SHA-256 equality.
- Same-window decision/config/control diff and anti-optimism checks.
- Full `python -m pytest examples/hyperliquid -q` after implementation and artifact-reader changes.

stop conditions：
- exact task/window identity mismatch in command, config snapshot, attempt key, manifest or blocked artifacts;
- runtime max loss/position/order-size/submission/duration envelope mismatch or breach;
- duplicate/ambiguous fill or fill attribution conflict;
- unknown order state;
- owned open orders cannot be proven empty;
- account position cannot be reconciled;
- price precision/post-only invariant failure;
- process termination/reap failure;
- status writer failure;
- source marker/import provenance mismatch;
- artifact checksum failure;
- concurrent account activity contamination.

done：
- Every reachable T024 live artifact path uses exact T024 identity and task-scoped caps.
- One clean bounded replacement window is collected and accepted for lifecycle/evidence integrity, or this task records the first formal stop condition without relaxing any gate.
- QA may close Principal Alignment Task 12 on mechanism/evidence integrity with zero or single-digit fills, but must keep unsupported economics explicit.
- Multi-level may only be reconsidered later if QA accepts sufficient real single-level lifecycle evidence; it is not activated by this task.

blockers：
- Any stop condition ends this task's live execution and prevents a second window or strategy-variable expansion.

commit：
- 待业务线程提交

提交信息：
- 待业务线程填写
