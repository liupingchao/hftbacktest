# 任务派发

执行线程：
- 业务线程-remote-public-data

任务ID：
- 0729T005

标题：
- BINANCE-SNAPSHOT-REPAIR-60S-30M-LIVE-VALIDATION

简短描述：
- 将 `0729T004` 已通过的 Binance snapshot/buffer/bridge 修复部署到保留的
  `c6in-winner / i-0a962e47210528526`。
- 依次完成 SKHYNIX 双交易所 `60` 秒 canary 和 `1800` 秒 live public-data
  采集，并拉回本地验证 replay readiness、Hyperliquid fast L2 与产物完整性。

状态：
- 未通过

执行顺序：
- 当前唯一任务

前置任务：
- `0729T004` 已通过。

规则更新提醒：
- 请先阅读规则文档后再执行本任务。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 不需要

提交要求：
- 当前工作区已有未提交 workflow、hunting 与 collector 工作。
- 本任务不创建混合提交，回报使用 `commit：无`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0729T005.md`
- `.workflow/reports/0729T005-business.md`
- `.workflow/reports/0729T005-qa.md`
- `docs/qa-acceptance-report.md`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `local_live_analysis/cross_exchange_public_sample_skhynix_0729T005_60s_<UTC>/`
- `local_live_analysis/cross_exchange_public_sample_skhynix_0729T005_30m_<UTC>/`

action：
- 只读取 AWS 实例状态，不停止、重启、替换或终止现有实例。
- 在独立远端 runtime 目录部署当前修复后的 public collector，并记录本地、
  传输包和远端关键源码 SHA256。
- 使用 Binance `SKHYNIXUSDT`：
  - `trade`
  - `depth@0ms`
  - `bookTicker`
  - REST depth snapshot limit `100`
- 使用 Hyperliquid `xyz:SKHX`：
  - `l2Book`
  - `trades`
  - `l2Book fast=true`
- 先运行 `60` 秒 canary。只有以下 gate 全部通过才运行 `1800` 秒窗口：
  - 两个采集子进程退出码为 `0`
  - Binance `depth_replay_ready=true`
  - Binance snapshot valid、bridge valid、continuity gap `0`
  - Hyperliquid `l2Book` 和 `trades` 均非空
  - Hyperliquid subscription ACK 包含 `l2Book fast=true`
  - 双交易所有效 overlap 达到请求窗口
- 两个窗口均为 raw-only，跳过远端 alignment。
- 完成后将完整目录拉回本地，核验 manifest、SHA256、gzip、JSONL、
  timestamp、消息计数、ACK、重连、连续性与 overlap。

hard boundary：
- Public market data only.
- No private keys, account, order, cancel or private endpoint.
- No strategy process or live trading.
- No instance stop, reboot, terminate or replacement.
- Preserve API termination protection.
- No alignment on c6in-winner.
- No fill, PnL, strategy promotion or cross-disconnect exact replay claim.
- Canary gate 未通过时禁止启动 30 分钟窗口。

verify：
- AWS instance identity/state/type/AZ and termination protection。
- Remote disk, clock sync, Python/WebSocket dependency and source hashes。
- `python -m py_compile` and focused collector tests before remote run。
- 60-second canary strict gate。
- 30-minute process liveness and output growth checks。
- Remote and local raw SHA256/gzip integrity。
- Full local JSONL parse and manifest count reconciliation。
- Binance snapshot bridge, `pu` continuity and `depth_replay_ready`。
- Hyperliquid exact subscription ACK and observed fast-L2 density。
- Effective two-venue overlap, reconnect/disconnect and close reason。
- `git diff --check` for task/report/controller documents。

done：
- Report exact instance, UTC/CST windows, output paths, commands, source hashes,
  process exits, per-venue counts, duration, overlap, snapshot/bridge/continuity,
  Hyperliquid fast ACK, reconnects, raw bytes/hashes, local parse results,
  blockers and QA readiness。
