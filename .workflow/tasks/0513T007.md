```md
执行线程：
- 业务线程-python

任务ID：
- 0513T007

标题：
- Binance raw provenance / top5 sidecar 与 decision join 实现

简短描述：
- 实现“不改标准 npz 主事件，新增 provenance/top5 sidecar，并把它 join 到 decision audit”的数据可追溯层。必须建立 `raw_seq -> npz rows -> reconstructed top5 book -> decision rows` 的显式映射和 join-age 验收，解决 T006 暴露的 Binance provenance 缺失问题。T007 的研究边界是 top5-only，不做 full L2 provenance，也不做精确 queue-position 证明。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- 0513T006 QA 通过后执行

规则更新提醒：
- 本任务只解决 Binance raw provenance / top5 sidecar / decision join 可追溯问题。
- 不允许修改 hftbacktest 标准 `data` npz 主事件 schema，不允许修改 core `Event` / `hbt.depth(0)` API。
- 不允许启动 live，不允许采集新样本，不允许做 microprice / OFI / queue 策略研究。
- 后续 pricing / OFI / microprice proxy 研究只基于 top5 可追溯视图；本任务不要求、不实现 full L2 provenance。
- 本任务先做 standalone sidecar / join 工具与 bounded smoke，不要求接入 `align_live_run.py` 标准流程。
- 如果后续需要策略实时使用这些字段，必须另开 connector/core API 任务。

按线程规则执行：
- .workflow/workflow-kit/thread-report-template.md
- docs/thread-playbook.md
- .workflow/reports/0513T006-business.md
- findings.md
- task_plan.md

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

Allowed files：
- `.workflow/tasks/0513T007.md`
- `.workflow/reports/0513T007-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `.workflow/dashboard.html`
- `.workflow/dispatch_suggestions.md`
- `examples/binance_tick_mm/pipeline_live_raw.py`
- `examples/binance_tick_mm/test_pipeline_live_raw.py`
- 新增 `examples/binance_tick_mm/*provenance*.py`
- 新增 `examples/binance_tick_mm/test_*provenance*.py`
- 新增 `examples/binance_tick_mm/*top5*.py`
- 新增 `examples/binance_tick_mm/test_*top5*.py`
- 新增 `examples/binance_tick_mm/*join*.py`
- 新增 `examples/binance_tick_mm/test_*join*.py`
- 新增/更新 `docs/*binance*provenance*.md`
- `local_live_analysis/0513T007_provenance_sidecar_smoke/**`

Allowed read-only inputs：
- `local_live_analysis/5-13-day-control-15min/**`
- `local_live_analysis/5-11-night-active/**`
- `local_live_analysis/5-10-day-control-1h-06/**`
- `local_live_analysis/5-9-noon/**`
- `local_live_analysis/5-9-small/**`
- existing analysis scripts under `examples/binance_tick_mm/`

Forbidden files / areas：
- hftbacktest core Rust `Event` / data reader / `hbt.depth(0)` API
- py-hftbacktest global `event_dtype`
- standard converted npz `data` schema or existing event semantics
- connector local book management
- `examples/binance_tick_mm/align_live_run.py`
- full L2 book persistence beyond top5 sidecar
- strategy rule logic, fair/target formula, risk guards, quote placement
- `examples/binance_tick_mm/deploy/*.sh`
- AWS/remote deployment state
- live run directories outside the T007 smoke output path
- archive tarballs and raw input artifacts

Requires separate task：
- 修改 core `Event` / `hbt.depth(0)` / connector API，让策略实时读取 update ids 或 top5 provenance。
- 修改 live strategy 使用 provenance/top5 字段决策。
- 修改 `maker_acceptance.py` 的正式 market-view quality gate。
- 做 microprice / OFI / queue feature research。
- 启动 fresh live no-rule run。
- 做 full L2 provenance、full-depth OFI 或精确 queue-position research。
- 将 sidecar / join 强制接入 `align_live_run.py` 标准流程。

Fatal Failures：
- 修改标准 npz 主事件 schema 或破坏现有 hftbacktest replay 兼容性。
- 用 `ival/fval` 暗塞 Binance `U/u/pu`、`lastUpdateId`、bookTicker provenance，导致字段语义不透明。
- 只按“最近 timestamp”做松散 join，却没有 `raw_seq -> npz rows -> reconstructed top5 book -> decision rows` 显式映射和 join-age 指标。
- 把 bookTicker/depth mismatch 直接解释为策略信号或 PnL 结论。
- 把 top5 sidecar 解释成 full L2 或精确 queue-position 证明。
- 输出 mapping 指向 converter 排序前临时 row，而不是最终标准 `data` array row index。
- 使用未来 book/top5 row join decision row，或不报告是否使用未来值。
- 启动 live、采集新样本、运行策略 replay/sweep、修改策略行为。

action：
- 设计并实现 Binance live raw sidecar 生成逻辑：
  - 保留现有标准 `*.npz` 的 `data` 主数组不变。
  - 新增 message-level provenance sidecar，建议输出为 `.npz` 或 `.csv`，优先选不新增重依赖的格式。
  - 每条 raw message 必须有稳定 `raw_seq`。
  - depthUpdate sidecar 字段必须覆盖 `raw_seq`、raw/local/exchange timestamps、`U/u/pu`、event type、line/order metadata、gap/mismatch flags。
  - snapshot sidecar 字段必须覆盖 `raw_seq`、`lastUpdateId`、timestamp、snapshot top levels metadata。
  - bookTicker sidecar 字段必须覆盖 `raw_seq`、bookTicker `u`、bid/ask px/qty、timestamp。
- 建立 `raw_seq -> npz rows` 显式映射：
  - 对每个 raw message 记录它生成的标准 npz row range：`event_row_start`、`event_row_end`、`row_count`。
  - mapping 必须指向 `correct_local_timestamp()` 和 `correct_event_order()` 之后的最终标准 `data` array row index。
  - 因为 `correct_event_order()` 可能把一个原始 event 拆成 exchange/local 两类最终 rows，mapping 必须支持一条 raw message 对应多个最终 row ranges，或明确记录最终 row index list。
  - depthUpdate 拆成多条 price-level rows 时，必须映射到同一个 `raw_seq`。
  - bookTicker 如果启用 `--opt t`，映射到其生成的 BBO event rows；如果未启用，sidecar 记录 `row_count=0` 和未写入 npz rows 的原因即可。
- 实现 reconstructed book / top5 sidecar：
  - 使用 raw snapshot + depthUpdate 重建 Binance local book。
  - 输出 per-update top5 snapshot；不要为了 T007 扩展到 full L2 持久化。
  - 每行必须记录 `raw_seq`、`last_u`、`prev_u/pu`、snapshot `lastUpdateId`、top5 ticks/qtys、top5 px、book age / source timestamp。
  - 必须记录 Binance local book sync 状态：`sync_waiting_snapshot`、`sync_aligned`、`sync_gap`、`startup_excluded`、`snapshot_lastUpdateId`。
  - 必须校验并报告 first valid depth update 是否满足 `U <= lastUpdateId + 1 <= u`。
  - bookTicker/depth BBO consistency 必须有 match/mismatch/age 字段。
- 实现 read-only decision join 工具：
  - 输入 live audit decision rows + provenance/top5 sidecar。
  - 输出 joined decision diagnostic artifact，不直接改变策略行为。
  - join 策略必须是 as-of join：使用 `top5.local_ts <= decision.ts_local` 的最近历史 top5 row，不能使用未来 top5 row。
  - 每个 decision row 必须记录 join key、joined `raw_seq`、joined depth `u`、joined bookTicker `u`、是否超过 max age、是否跨过 sequence gap、joined top5 source。
  - 每个 decision row 必须记录 `depth_join_age_ms`、`bookticker_join_age_ms`、`top5_join_age_ms`、`max_join_age_ms`、`join_used_future=false`。
  - 如果不能可靠 join，必须显式标记 `join_missing` / `join_stale` / `join_gap_crossed`，不能静默填值。
- 明确 top5-only 后续研究边界：
  - pricing 可以使用 top5 mid/microprice/weighted-mid/imbalance proxy。
  - OFI 只能做 top5 price-level delta proxy。
  - queue 只能做 top-of-book/top5 size and age proxy，不能称为精确 queue position。
  - 不允许把 T007 输出宣传为 full L2 provenance。
- 产出 T007 smoke artifacts 到 `local_live_analysis/0513T007_provenance_sidecar_smoke/`，至少覆盖一个小样本片段或测试 fixture。
- T007 smoke 范围必须 bounded：fixture + 一个小片段 smoke 即可，不要求一次跑完五个历史大样本；全样本重跑/重新分类属于后续任务。
- 更新文档说明：
  - 标准 npz 主事件继续用于 hftbacktest replay。
  - sidecar 用于 data-quality / provenance / decision join，不代表策略已实时使用这些字段。
  - 长期如果策略需要实时字段，另开 connector/core API 任务。
  - T007 不修改 canonical `audit_schema.py` 或 live audit CSV schema；joined decision output 是独立 diagnostic artifact。正式加入 audit schema 属于后续任务。
- 每个 sidecar 输出必须带 schema / run manifest metadata：
  - schema version。
  - raw file path / size / mtime。
  - converter opt。
  - top5 levels = 5。
  - tick_size 来源。
  - generated_at。
  - input sample id。

verify：
- Focused tests:
  - `python -m pytest examples/binance_tick_mm/test_pipeline_live_raw.py`
  - `python -m pytest examples/binance_tick_mm/test_binance_top5_provenance.py`
- CLI/help smoke：
  - 新增工具的 `--help` 通过。
- Smoke artifact check：
  - T007 smoke 输出目录存在。
  - provenance sidecar 存在。
  - raw-to-npz mapping 存在。
  - reconstructed top5 sidecar 存在。
  - joined decision artifact 存在，或在 fixture 不含 decision audit 时有明确 skip reason。
- Acceptance metrics 必须在报告中列出：
  - raw message count。
  - npz row coverage / mapped row coverage。
  - final `data` row mapping coverage after `correct_event_order()`。
  - depth `pu` mismatch count。
  - snapshot alignment status。
  - first valid update alignment status: `U <= lastUpdateId + 1 <= u`。
  - bookTicker/depth BBO match and mismatch buckets。
  - decision join coverage。
  - decision join age p50/p90/p99, split into depth/bookTicker/top5 age where available。
  - stale join count。
  - gap-crossed join count。
  - future join count must be `0`。
- Regression boundary：
  - 现有标准 `*.npz` `data` array dtype 未改变。
  - 不修改 core `Event` / py `event_dtype`。
  - 不修改 canonical `audit_schema.py` / live audit CSV schema。
  - 不修改 `align_live_run.py`。
  - 不修改 strategy behavior。
  - 不启动 live，不运行策略 replay/sweep。
- Dashboard:
  - `python3 .workflow/build_dashboard.py`

done：
- 回报必须说明新增了哪些 sidecar / mapping / join 工具和测试。
- 回报必须明确 `raw_seq -> npz rows -> reconstructed top5 book -> decision rows` 显式映射如何实现。
- 回报必须说明 mapping 是否指向最终标准 `data` array rows，以及 `correct_event_order()` 后的覆盖率。
- 回报必须说明 join-age 验收结果和任何 known limitation。
- 回报必须说明 as-of join 未使用未来值，且 future join count 为 `0`。
- 回报必须说明 sidecar schema/version/manifest metadata。
- 回报必须明确后续研究是 top5-only，不能证明 full L2 或精确 queue position。
- 回报必须明确标准 npz 主事件未修改，core/connector API 未修改，策略未实时使用这些字段。
- 回报必须写 `commit id` 和提交信息。
```
