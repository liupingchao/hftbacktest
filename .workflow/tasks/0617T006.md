```md
执行线程：
- 业务线程-research

任务ID：
- 0617T006

标题：
- Hyperliquid tiny-live read-only optimistic PnL proxy

简短描述：
- 在 `0617T005` read-only signal / quote replay 基础上，对采集的 cross-exchange public/read-only pricing-signal 数据做最乐观理论 PnL proxy 估计。
- 本任务假设理论 maker intent 100% 成交，并用未来 Hyperliquid mid / basis response 做上界估计；不处理成交概率、排队、真实手续费、账户库存、private/order/economics source，也不声称真实 PnL。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- `0617T005` business artifacts must exist locally.
- If `0617T005` QA is not yet `已通过`, this task may only produce pre-QA dependent read-only proxy evidence and must not authorize live execution.

规则更新提醒：
- 请先阅读 `.workflow/workflow-kit/workflow-manual.md`、`.workflow/workflow-kit/task-dispatch-template.md`、`.workflow/workflow-kit/thread-report-template.md`、`.workflow/workflow-kit/qa-acceptance-template.md`、`task_plan.md`、`progress.md`、`findings.md`、`docs/hyperliquid_tiny_live_0616T006_T008_auto_loop.md` 后再执行本任务。
- 本任务是 read-only optimistic PnL proxy，不是 execution evidence、真实 PnL、成交概率、maker viability 或 `0616T008` live execution。

按线程规则执行：
- `AGENTS.md`
- `docs/hyperliquid_tiny_live_0616T006_T008_auto_loop.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0617T006.md`
- `.workflow/reports/0617T006-business.md`
- `docs/hyperliquid_tiny_live_optimistic_pnl_proxy.md`
- `examples/hyperliquid/**`
- `local_live_analysis/hyperliquid_tiny_live_optimistic_pnl_proxy_0617T006/**`
- `task_plan.md`
- `progress.md`
- `findings.md`

action：
- Consume only local public/read-only cross-exchange artifacts already accepted or already used by `0617T005`; do not collect new data.
- Resolve and document the input sample set before computing PnL:
  - Start from `local_live_analysis/hyperliquid_tiny_live_signal_quote_replay_0617T005/replay_manifest.json` and the relevant source manifests.
  - The user requested "6 datasets"; if accepted artifacts expose `7` canonical event-mode samples or `8` total replay inputs, write a `sample_set_reconciliation.csv` and report estimates separately instead of silently mixing sets.
  - If an exact 6-sample set cannot be unambiguously reconstructed from accepted artifacts, produce a fail-closed note for `requested_six` and still allow a clearly labeled `canonical_7` / `0617T005_8_input` diagnostic estimate if useful.
- Consume the `0617T004` signal / quote policy and the `0617T005` threshold candidates as rule sources; do not invent a different side mapping:
  - positive eligible signal -> Hyperliquid maker buy intent
  - negative eligible signal -> Hyperliquid maker sell intent
  - primary calibration candidate: threshold `75` ticks and persistence `2`
  - stricter fallback: threshold `75` ticks and persistence `3`
  - sensitivity grid should include at least thresholds `50,75,100` and persistence `1,2,3`
- Compute optimistic theoretical quote PnL under explicit assumptions:
  - assume every eligible theoretical maker intent is filled at the theoretical maker quote;
  - ignore fill probability, queue priority, post-only reject probability, live order lifecycle, private/order response, account inventory, fees/rebates, spread-capture settlement, and real execution costs;
  - use Hyperliquid public mid labels only as read-only future labels.
- For each accepted theoretical intent and each supported horizon, compute at least:
  - `quote_side`
  - `horizon_ms`
  - `quote_distance_ticks`
  - `future_mid_move_ticks`
  - `optimistic_mid_pnl_ticks`
  - `optimistic_mid_pnl_usdc` using tick size `0.1` and size `0.01 BTC`
  - optional separate basis-response proxy, clearly labeled if implemented
- Suggested primary formula:
  - buy intent: `optimistic_mid_pnl_ticks = hyperliquid_future_mid_move_ticks + context_hyperliquid_spread_ticks / 2`
  - sell intent: `optimistic_mid_pnl_ticks = -hyperliquid_future_mid_move_ticks + context_hyperliquid_spread_ticks / 2`
  - `optimistic_mid_pnl_usdc = optimistic_mid_pnl_ticks * 0.1 * 0.01`
- Output both:
  - fixed-horizon optimistic PnL by sample / side / threshold / persistence / horizon
  - oracle-best-horizon optimistic PnL, labeled `non_tradeable_oracle_upper_bound`
- If cap/risk policy is simulated, keep it separate from the main `unconstrained_all_intents` optimistic result.
- Preserve proof boundaries: this task estimates a theoretical public-data upper bound only. It must not claim real fills, real PnL, fee/rebate settlement, spread capture, account inventory, execution quality, maker viability, deployment readiness, or live authorization.
- Do not implement a live bot, connector, signed client, private endpoint call, account query, order placement, order cancellation, order amendment, user stream, nonce/signing logic, deployment, promotion, real PnL proof, or maker viability claim.

acceptance criteria：
- Task explicitly reconciles the user's "6 datasets" wording against the actual local accepted sample manifests and does not silently change the sample count.
- Outputs include a machine-readable manifest, sample-set reconciliation, fixed-horizon PnL summary, oracle-best-horizon upper-bound summary, and row-level audit samples.
- Outputs report by sample, side, threshold, persistence, and horizon; aggregate-only reporting is insufficient.
- Main result clearly states whether the optimistic upper bound is materially positive, weak, unstable, or blocked under the selected sample set.
- Report text distinguishes `optimistic_pnl_proxy` from real PnL and rejects any live/execution interpretation.
- Final recommendation must be exactly one of:
  - `hyperliquid_tiny_live_optimistic_pnl_proxy_ready_for_qa`
  - `hyperliquid_tiny_live_optimistic_pnl_proxy_needs_input_clarification`
  - `hyperliquid_tiny_live_optimistic_pnl_proxy_blocked`

verify：
- Run focused tests for any new implementation with `/home/liushuai/workspace/hftbacktest/.conda-envs/hft-py38/bin/python` if available; if that path is unavailable on this host, record the actual Python path/version used.
- Run the optimistic PnL proxy generator.
- Validate any generated JSON manifest with `python -m json.tool <artifact>.json`.
- Check required CSV / markdown artifacts are non-empty.
- Boundary text check for no live/order/private authorization and no real PnL/fill/maker-viability overclaim.
- `git diff --check`

done：
- 回报 input sample set reconciliation，尤其说明用户口径的 `6` 个数据集是否可精确定位，以及实际输出是否包含 `requested_six`、`canonical_7` 或 `0617T005_8_input`。
- 回报 signal / threshold / persistence source、PnL proxy formula、tick/size/USDC conversion、fixed-horizon summary、oracle-best-horizon summary、sample-level stability、row-level audit 输出位置。
- 明确说明本任务结果只是 optimistic public-data upper bound，不是成交概率、真实手续费、真实 PnL、账户库存、maker viability 或 live authorization。
- 明确说明没有读取凭证、没有调用私有接口、没有账号查询、没有下单/撤单/改单、没有 live bot。
```
