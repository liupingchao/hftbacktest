# 任务派发

执行线程：
- 业务线程-python/cross-exchange-research

任务ID：
- 0801T006

标题：
- HYPERLIQUID-EPISODE-V2-PHASE-AND-FREEZE-REPAIR

简短描述：
- 修复 `0801T003` review 发现的 Episode phase 与 boundary freeze 缺陷，
  发布独立 `episode_v2` 产物，不覆盖历史 `episode_boundary_v1`。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- `0801T002` QA `已通过`。
- `0801T003` 历史产物保留，但其 phase/freeze 结论由本任务重新验收。

规则更新提醒：
- 请先阅读 workflow 规则和修复计划后执行本任务。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `docs/hyperliquid_liquidity_response_case_hierarchy_plan.md`

是否需要提交代码：
- 不需要

提交要求：
- 不需要提交
- 回报写 `commit：无`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `examples/hyperliquid/cross_exchange_liquidity_response_case_hierarchy.py`
- `examples/hyperliquid/test_cross_exchange_liquidity_response_case_hierarchy.py`
- `local_live_analysis/skhynix_liquidity_response_case_hierarchy/episode_v2/`
- `.workflow/tasks/0801T006.md`
- `.workflow/reports/0801T006-business.md`
- `.workflow/reports/0801T006-qa.md`
- `docs/qa-acceptance-report.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

action：
- 保留 `atom/` 和历史 `episode/` 字节不变。
- 新增 `episode_boundary_v2` builder 和独立 `episode_v2/` 发布路径。
- phase 必须基于 rolling `100ms` signed shock impact；rolling sign 发生变化且
  至少两个连续 atom 支持新方向后，才建立 reversal phase。
- boundary sensitivity 只使用 discovery segments `0001-0003`。
- manifest 记录 discovery atom count、discovery input SHA、phase algorithm 和
  sensitivity SHA。
- 若已有 `episode_boundary_v2` 使用不同参数或 phase algorithm，构建必须在
  发布前失败，不得覆盖。
- 保持每个 atom 唯一属于一个 full-data primary cluster 和 episode。
- 本轮不得执行 baseline、motif、regime、held-out outcome 读取或策略评估。

verify：
- focused pytest 覆盖 rolling-window phase、两 atom reversal、discovery-only
  sensitivity 和同版本参数漂移失败。
- `python -m py_compile`。
- CLI `--stage episode-v2 --help`/主 help。
- 在真实 T002 atom package 上构建 `episode_v2`。
- 独立复核 membership conservation、历史 `episode/` SHA 不变、discovery
  sensitivity scope、manifest/output SHA 和重复构建确定性。
- `git diff --check`。

done：
- `episode_v2` 独立发布并通过 focused verification。
- 历史 `episode_boundary_v1` 未被覆盖。
- 业务报告状态为 `待验收`，随后进入独立 QA。
- `0801T007` 在本任务 QA `已通过` 前保持锁定。
