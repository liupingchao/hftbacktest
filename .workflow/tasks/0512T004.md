```md
执行线程：
- 测试线程

任务ID：
- 0512T004

标题：
- Stage 6J / live adverse-selection 观测门禁改进合同

简短描述：
- 基于 `0512T001` 的结论，补齐 Stage 6J replay 与 live/current-format adverse-selection source-path 之间的观测门禁。目标不是优化策略，而是定义后续 toxic timing rule 如何被验收：哪些指标只能说明 replay-model regression，哪些指标可以说明 action-path coverage，哪些证据必须来自 live-derived source-path analysis。本任务不改策略逻辑、不启动 live。

状态：
- 待验收

执行顺序：
- `0512T001` 完成并通过 QA 后执行；必须早于 `0512T002`

前置任务：
- 0512T001

规则更新提醒：
- 本任务处理验收/观测能力，不处理策略收益优化。
- Stage 6J replay 当前不能单独证明 live adverse-selection source-path 改善。
- 本任务必须产出 `0512T002` 可引用的观测门禁合同。
- 样本角色必须显式区分：`5-11-night-active` 是主要开发/诊断样本；`5-10-day-control-1h-06`、`5-9-noon`、`5-9-small` 是 cross-sample sanity check。

按线程规则执行：
- .workflow/workflow-kit/thread-report-template.md
- docs/thread-playbook.md
- docs/stage6i-cancel-requested-fill-risk-plan.md
- docs/5-8-future-plan.md

是否需要提交代码：
- 默认不需要；如确需补充只读分析脚本或 runner，必须只限观测/报告，不得改策略逻辑。

提交要求：
- 回报必须写 `commit：无`，除非本任务合同被总控更新为允许只读分析代码。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- .workflow/reports/0512T001-business.md
- .workflow/reports/0511T004-business.md
- .workflow/reports/0512T003-business.md
- local_live_analysis/5-11-night-active/
- local_live_analysis/stage6j_cross_sample_0511T002/stage6j_replay_summary.csv
- local_live_analysis/stage6j_cross_sample_0511T002/*/*/audit_bt_stage6j.csv
- local_live_analysis/stage6j_regime_control_cancel_fill_risk/stage6i_cancel_fill_summary.csv
- local_live_analysis/stage6j_regime_control_cancel_fill_risk/*/cancel_fill_events.csv
- examples/binance_tick_mm/analyze_cancel_fill_risk.py
- examples/binance_tick_mm/stage6j_replay.py
- findings.md
- progress.md

Fatal Failures：
- 把观测门禁任务变成策略优化任务。
- 把 Stage 6J replay 的 `guard_candidate_adverse_selection_count=0` 当作 live 风险不存在。
- 只看 PnL 或 cancel-fill 总数，不检查 action-path coverage / source-path 语义。
- 把 `5-11-night-active` 单样本结果当成 live promotion 证据。
- 修改策略逻辑、默认开启规则或启动 live。

MUST NOT：
- 不改 `strategy_core.py`、`live_tick_mm.py`、`backtest_tick_mm.py` 的策略决策逻辑。
- 不启动真实 live，不连接交易所，不改 AWS 状态。
- 不覆盖 `0511T004` / `0512T001` / `0512T003` 既有证据。
- 不使用 future markout 作为 live 决策输入；markout 只能作为诊断/验收指标。

核心问题：
- Stage 6J replay 会重新生成模拟订单生命周期，因此它的 source-path 统计不能直接代表 live cancel-to-fill race。
- 后续 toxic timing rule 仍需要可观测验收：至少要证明 add-side submit/re-add action-path 被覆盖，并定义 live-derived source-path 证据如何补充 replay。
- `5-11-night-active` 事件量更大，适合作为主要开发/诊断样本，但它是 night-active regime，不能替代跨样本 sanity check。

action：
- 从 `0512T001` 报告提取 Stage 6J replay 可证明与不可证明的边界。
- 明确样本分层：
  - 主开发/诊断样本：`5-11-night-active`。
  - cross-sample sanity check：`5-10-day-control-1h-06`、`5-9-noon`、`5-9-small`。
- 定义三层观测门禁：
  - action-path coverage：eligible submit、blocked submit、reduce-side allowed、action/planned_action diff。
  - replay-model regression：PnL、position、drop/churn、replay fill/cancel-fill、same-side worsening。
  - live-derived source-path proof：live/current-format cancel-fill events、inventory-reducing cancel race、adverse-selection candidate、cancel-to-fill latency/markout。
- 定义 replay 与 live 之间的可比字段和不可比字段，明确哪些字段不能跨模型硬比较。
- 设计最小证据表：`run_id | candidate | action coverage | replay regression | live-derived source-path evidence | promotion status`。
- 最小证据表必须按样本角色分组，先展示 `5-11-night-active`，再展示 cross-sample sanity check，避免单样本 overfit。
- 明确 `0512T002` 的验收口径：T002 可以设计规则和 action-path 验收，但不得承诺 live source-path improvement，除非本任务定义的 live-derived evidence 后续通过。
- 输出 `.workflow/reports/0512T004-business.md`，作为 `0512T002` 的前置观测合同。
- 更新 `findings.md`，写入 durable observability constraints。

verify：
- `python3 .workflow/build_dashboard.py`
- 报告必须包含三层观测门禁定义。
- 报告必须包含样本角色定义：`5-11-night-active` 为主要开发/诊断样本，`5-10-day-control-1h-06`、`5-9-noon`、`5-9-small` 为 cross-sample sanity check。
- 报告必须包含 Stage 6J replay 能证明/不能证明的边界。
- 报告必须包含 `0512T002` 可引用的验收指标表。
- 报告必须明确后续是否需要只读 runner 或 instrumentation；默认不授权策略实现。

done：
- 回报必须明确：Stage 6J replay 在后续规则验收中的角色。
- 回报必须明确：live adverse-selection source-path proof 需要哪些 live-derived evidence。
- 回报必须明确：如何用 `5-11-night-active` 做主诊断、如何用 `5-10-day-control-1h-06` / `5-9-noon` / `5-9-small` 防止单样本 overfit。
- 回报必须明确：`0512T002` 是否可以启动，以及启动后不得声明哪些结论。
- 回报必须明确 no live micro test。
```
