```md
执行线程：
- 业务线程-python

任务ID：
- 0604T015

标题：
- Diagnose live shutdown cancel wait-result ambiguity and missing cancel-ack proof

简短描述：
- 复现并归因 `0604T013` shutdown cancel wait 仍不能证明收到 cancel acknowledgement 的问题。重点检查 `wait_order_response()` 返回值语义、batch/timeout 二义性、shutdown helper 日志/统计误导、以及“收到 order response 不等于确认 canceled/inactive”的状态确认缺口。本任务只做诊断、稳定复现、范围检查和归因，不做修复。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0604T012` QA 已通过，已证明原 shutdown 是 fire-and-forget。
- `0604T013` 业务执行已完成并待 QA，当前分支已把 shutdown 推进到显式 `cancel(..., False)` 后 `wait_order_response(...)`。
- 本任务针对 `0604T013` 后的新 review：shutdown 会等待，但仍不能证明等待结果是 cancel ack，而不是 timeout / batch 折叠 / 普通 order response。

规则更新提醒：
- 请先阅读规则文档后再执行本任务。
- 本任务只做诊断、稳定复现、范围检查和归因；不得修改 production 行为。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `docs/thread-playbook.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 提交任务文件、业务诊断报告；如为稳定复现新增 focused 诊断测试或只读复现脚本，也应提交。
- 不得提交 production 修复代码。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0604T015.md`
- `.workflow/reports/0604T015-business.md`
- `examples/binance_tick_mm/live_tick_mm.py` 只读检查；禁止修复。
- `examples/binance_tick_mm/test_backtest_tick_mm.py` 或更合适的 focused test 文件，仅允许新增诊断/复现测试，不允许修复 production 行为。
- `py-hftbacktest/src/live.rs` 只读检查。
- `py-hftbacktest/src/backtest.rs` 只读对照检查。
- `hftbacktest/src/live/bot.rs` 只读检查。
- `hftbacktest/src/backtest/mod.rs` 只读对照检查。
- `hftbacktest/src/types.rs` 只读检查。
- 必要 workflow tracking 文件仅限本任务状态/结论更新。

forbidden：
- 不修复 `examples/binance_tick_mm/live_tick_mm.py` shutdown helper。
- 不修改 connector、py binding、Rust live bot、Rust backtest、生产配置、audit schema、normal loop cancel 语义。
- 不启动 live / tiny-live / default-on。
- 不做参数搜索、回放实验扩展、promotion。
- 不把 REST final open-orders check、状态复查、cancel retry、timeout 分类或 audit tail 写入做成本任务实现。
- 不静默扩大到无关 shutdown / live safety 重构。

review under test：
- `0604T013` 后的 shutdown 已经不再只是 fire-and-forget；它会在 `hbt.cancel(asset_no, order_id, False)` 后调用 `hbt.wait_order_response(asset_no, order_id, timeout_ns)`。
- 但当前 helper 只保存原始 `wait_result`，没有证明：
  - 等待结果是否明确是 order response。
  - `0` 是否 timeout，还是 batch path 中收到响应后折叠成 `Ok`。
  - 收到 order response 是否就是 cancel ack。
  - 对应订单最终是否 canceled / inactive / 不在交易所 open orders。
- 当前 shutdown 日志统计 `ack_waits` 本质上是 wait requested count，不是 confirmed cancel ack count。

action：
- 只读确认当前 shutdown helper 行为：
  - `cancel_working_orders_for_shutdown()` 是否逐单调用 `cancel(..., False)` 后 `wait_order_response(...)`。
  - `ShutdownCancelResult` 是否只记录原始 `wait_result` / `error`，没有分类 `confirmed_response`、`timeout_or_ok`、`cancel_ack_confirmed`。
  - shutdown summary 是否只统计 `wait_requested`，没有统计 confirmed ack / timeout / unknown。
  - 现有 focused tests 是否把 fake `wait_order_response()` 返回 `0` 当作成功，从而掩盖真实语义。
- 只读确认底层返回语义：
  - Python live binding `handle_result()` 中 `ElapseResult::Ok -> 0`、`OrderResponse -> 3`。
  - Rust live bot 非 batch 路径收到指定 order response 是否返回 `OrderResponse`。
  - Rust live bot timeout 是否返回 `Ok`，即 Python `0`。
  - Rust live bot batch 路径中收到 order response 后，在 `BatchEnd` 是否返回 `Ok`，即 Python `0`，造成“已收到响应”和“超时”同值。
  - 对照 backtest `wait_order_response()` 是否也存在 `Ok` / `OrderResponse` 表达差异，避免把 live/backtest 语义混淆。
- 稳定复现以下 case matrix；可用 focused diagnostic tests、small fake/stub objects、或只读 Rust/Python semantic tests，但不得修复 production 行为：
  - `live_wait_response_non_batch_order_response_returns_3`：指定订单响应非 batch 路径应映射为 `3`。
  - `live_wait_response_timeout_returns_0`：timeout / no matching response 映射为 `0`。
  - `live_wait_response_batch_received_response_returns_0`：batch 内收到指定响应但 `BatchEnd` 后映射为 `0`，与 timeout 同值。
  - `shutdown_helper_records_raw_zero_as_no_error`：helper 对 `wait_result=0` 只记录原始值，不标 timeout/unknown。
  - `shutdown_helper_records_three_but_not_cancel_ack_state`：helper 对 `wait_result=3` 也只能证明 order response，不证明 canceled/inactive。
  - `shutdown_summary_ack_waits_is_wait_requested_not_ack_confirmed`：日志字段名/统计口径存在误导，`ack_waits` 只是 wait attempts。
  - `existing_shutdown_tests_fake_zero_masks_semantics`：现有 fake 返回 `0` 且测试断言为成功，和 live binding 语义不一致。
  - `order_response_status_not_checked`：收到 order response 后没有检查 local order status、`order.active()`、`cancellable` 后状态、REST open orders 或 final exchange state。
  - `audit_tail_status_not_proven`：shutdown cancel/wait 结果没有形成可审计的 final cancel-ack / open-order reconciliation proof。
- 对每个 case 输出：
  - 是否可稳定复现。
  - 复现方式和命令。
  - 实际返回值 / 日志统计 / helper 结果。
  - 根因代码位置。
  - 影响边界：live safety、shutdown log、audit/alignment tail、测试覆盖缺口。
  - 是否属于本任务发现的问题，或只是后续修复候选。
- 输出根因分层：
  - API 语义层：`0` 是 `Ok`，不是 confirmed ack；`3` 是 order response，不是 cancel-state proof。
  - live bot batch 层：batch path 会把已收到 response 折叠成 `Ok`。
  - shutdown helper 层：只保存 raw int，不分类，不复查订单状态。
  - logging/reporting 层：`ack_waits` 命名/统计口径误导。
  - tests 层：fake 返回值与真实 binding 语义不一致，当前测试只能证明调用了 wait，不能证明 ack。
  - audit/live-safety 层：没有最终 canceled/inactive/open-order reconciliation proof。
- 给出后续修复任务建议边界，但不得在本任务内实施：
  - 分类 `wait_order_response` 返回值。
  - 区分 timeout / response / unknown。
  - 明确 `wait_result == 3` 仍需状态复查。
  - 改名或重写 shutdown log counters。
  - 新增 final local/REST order-state proof 或 audit tail record。
  - 调整 tests fake semantics。

acceptance criteria：
- 明确判断本次 review 是否为事实，并指出哪些表述需要更精确。
- 必须稳定复现 `wait_result=0` 的二义性来源：timeout 与 batch received-response 折叠。
- 必须稳定复现或只读证明 `wait_result=3` 只能代表 order response，不能代表 cancel ack / canceled state。
- 必须证明当前 shutdown helper/logging 不能区分 confirmed ack、timeout、unknown。
- 必须检查并记录现有 tests 是否把 fake `0` 当成功导致覆盖缺口。
- 必须说明是否存在 audit tail / final live state proof 缺口。
- 不提交 production 修复。
- 业务报告写入 `.workflow/reports/0604T015-business.md`，状态为 `待验收`。

verify：
- `/home/molly/anaconda3/envs/hftbacktest/bin/python -m pytest examples/binance_tick_mm/test_backtest_tick_mm.py -k "live_shutdown or shutdown_cancel or wait_order_response"`，如新增 focused 诊断测试应包含在 filter 内。
- 如新增单独诊断 test 文件，运行对应 pytest 文件。
- `/home/molly/anaconda3/envs/hftbacktest/bin/python -m pytest examples/binance_tick_mm/test_*.py`，如 scope/耗时允许。
- `cargo test -p hftbacktest live::bot` 或更小的 Rust focused test/filter；如无法精确过滤或耗时过大，报告原因并以只读代码证据 + Python focused tests 补足。
- `git diff --check`

done：
- 回报必须说明改动点、稳定复现矩阵、根因分层、所有发现的问题位置、不可复现/未覆盖项、推荐后续修复范围、验证结果、commit id 和 commit message。
- 回报必须明确本任务未修复 shutdown helper、未修改 binding/Rust live bot/connector、未启动 live、未做 promotion。
```
