# 0722T061 BASIS-REGRESSION-ALPHA-ACCEPTANCE

执行线程：
- 总控 / 业务执行线程

任务ID：
- 0722T061

标题：
- Implement and accept the cross-exchange basis regression alpha candidate

简短描述：
- 在 QA-accepted `0627T001` 三窗 public package 上正式实现 basis linear
  regression alpha acceptance。
- 固定比较 Binance lead baseline、pure basis、Binance lead plus basis，
  只有严格 held-out gate 通过才生成 shadow-only frozen contract。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- `0722T060`（已通过）

规则更新提醒：
- offline/public-only。
- 不修改 shared kernel、watcher、live quote、order、risk 或 credentials。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交并进入独立 QA。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `examples/hyperliquid/cross_exchange_basis_regression_acceptance.py`
- `examples/hyperliquid/test_cross_exchange_basis_regression_acceptance.py`
- `local_live_analysis/cross_exchange_basis_regression_acceptance_0722T061/`
- workflow/report/tracking files

action：
- 只读取 `0627T001` accepted synchronized rows。
- 固定模型：
  - `binance_lead_regression`
  - `basis_regression`
  - `binance_lead_plus_basis_regression`
- 每个 fold 仅用另外两个窗口计算 normalization 和 OLS coefficient。
- 输出 standardized/raw coefficients、held-out metrics、source-age/basis/
  observed-regime stability、model comparison、leakage/boundary manifest。
- 若 held-out coefficient direction、RMSE、direction hit、signed edge 和
  contribution gates 通过，使用全部 accepted rows 拟合 shadow-only frozen
  contract；同时显式保留 MAE/intercept drift warnings。

verify：
- Synthetic stable/unstable/boundary/leakage/determinism tests。
- Official package runner。
- Focused pytest、full Hyperliquid、py_compile、artifact contract checks。

done：
- Final recommendation 必须是以下之一：
  - `accept_basis_regression_for_shadow`
  - `basis_context_only_keep`
  - `reject_basis_alpha`
  - `needs_more_samples`
- 不能把 offline regression、replay 或 public shadow 声称为 live economics
  或 promotion proof。

blockers：
- 无。

提交信息：
- `Implement basis regression alpha acceptance`
