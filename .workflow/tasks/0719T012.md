# 0719T012 LIVE-EVIDENCE-SUMMARY-PATH-PORTABILITY-REPAIR

执行线程：
- 总控 auto-loop / 业务执行线程

任务ID：
- 0719T012

标题：
- Repair row-derived live evidence summaries and remote/local path portability

简短描述：
- Close the evidence-integrity and portability defects found by `0719T011` QA without running another live window.
- Make producer summaries exact derivatives of raw decision evidence and make Task 12 acceptance independently reconstruct the same facts.

状态：
- 执行中

执行顺序：
- 当前唯一任务

前置任务：
- `0719T011`（未通过）

规则更新提醒：
- 本任务是 offline-only repair；不得调用 live、private、account、order、cancel、network、remote 或 service。
- T011 的 exact source、terminal checksum、账户安全和唯一窗口结论保持有效，但不得把 T011 解释为已接受的 two-sided lifecycle。
- Multi-level、dynamic spread、fill feedback、inventory skew 和 actual quote behavior activation 继续关闭。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `.workflow/reports/0719T011-qa.md`
- `docs/superpowers/plans/2026-07-17-principal-alignment-p0-p3.md`

是否需要提交代码：
- 需要

提交要求：
- 先提交 implementation，再提交 business report。
- 回报必须带 implementation `commit id`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- Modify: `examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- Modify: `examples/hyperliquid/cross_exchange_t024_same_window_acceptance.py`
- Modify/add focused tests under `examples/hyperliquid/`
- Reuse read-only artifact: `local_live_analysis/principal_alignment_single_level_two_sided_0719T011/`
- Workflow: this task, business report, QA report, `docs/qa-acceptance-report.md`, `task_plan.md`, `progress.md`, `findings.md`

producer contract：
- Derive top-level decision summary from every row of `event_driven_trigger_decision_matrix.csv`.
- Persist exact counts for:
  - candidate evaluations;
  - trigger rows;
  - anti-drift blocks;
  - immediate reprice guard passes and failures;
  - edge-gate evaluations, passes and blocks;
  - each anti-drift, immediate-guard and edge-block reason;
  - order-authorized rows.
- Do not use a boolean or first-hit value as `trigger_count`.
- Preserve all contributing no-submit reasons. Do not label one edge reason as the complete or unique stop condition.
- Separate candidate evaluations, manager attempts and actual submission/order endpoint calls.
- Skipped candidate rows must not be reported as completed canonical requote attempts.
- Record private read-only calls separately from order endpoint calls and cancel endpoint calls.
- Top-level endpoint summaries must agree exactly with raw row/nested producer evidence.

acceptance contract：
- Independently parse the raw decision matrix and reconstruct every producer decision-summary count/reason map.
- Independently reconstruct private-read, order and cancel endpoint facts from raw row and nested evidence.
- Exact-compare producer summaries against independent reconstruction; missing, stale, booleanized or conflicting summaries fail closed.
- Validate remote canonical provenance paths as remote facts:
  - remote command run root;
  - remote runtime source root;
  - remote output directory;
  - internal task/window/path relationships.
- Validate the local physical pullback independently:
  - current local bytes;
  - exact source blobs;
  - terminal manifest and current file set;
  - local artifact structure.
- Do not require a remote absolute path string to equal the local pullback absolute path.
- Remote internal path rewrites, synchronized tampering, source mismatch or local byte mutation must still fail closed.
- A byte-exact local copy of T011 must not fail solely because remote and local physical roots differ.
- T011 must remain blocked for zero submissions, edge/guard blockers and missing two-sided lifecycle.

tests：
- A copied or fixture-backed T011 artifact reconstructs exactly `26` trigger rows:
  - `5` anti-drift blocks;
  - `10` immediate-guard-pass edge blocks;
  - `11` immediate-guard failures;
  - edge reasons `7 fair_mid_source_stale` and `3 edge_below_required_buffer`.
- The T011 private-read evidence is reported truthfully while order/cancel calls remain zero.
- Candidate rows do not inflate manager-attempt or submission counters.
- Booleanized/stale trigger summary and private-endpoint conflicts fail closed.
- Remote/local path differences pass when remote relationships and local bytes are independently valid.
- Tampered remote command/source/output relationships and local byte/checksum mutations fail closed.
- T011 remains mechanism/evidence blocked after path-only portability repair.

verify：
- Focused producer/acceptance/orchestrator/manager tests pass.
- Full `python -m pytest examples/hyperliquid` passes.
- Modified Python modules pass `py_compile`.
- Modified CLIs pass `--help`.
- `git diff --check` passes.
- No live/private/account/order/cancel/network/remote/service action occurs.

done：
- Producer summaries are exact row-derived evidence, not optimistic labels.
- Acceptance independently reproduces and exact-compares decision and endpoint summaries.
- Remote canonical provenance and local physical pullback verification are both strict without requiring identical absolute paths.
- Candidate, manager-attempt and submission cardinalities are unambiguous.
- Independent QA accepts the repair before any new live task is dispatched.

stop conditions：
- Any test requires live/private/account/order/cancel/network/remote/service access.
- Repair weakens source, terminal checksum, command identity, raw fill, reference or lifecycle validation.
- T011 path portability can pass only by suppressing a provenance check rather than splitting remote/local facts.
- T011 becomes accepted despite zero submissions and missing lifecycle.
- Strategy formula, threshold, risk cap or activation behavior changes.

blockers：
- Independent QA failure keeps new live and multi-level locked.

commit：
- 待回报

提交信息：
- 待回报
