# 任务派发

执行线程：
- 业务线程-remote-public-data

任务ID：
- 0729T003

标题：
- C6IN-SKHYNIX-2H-SYNCHRONIZED-PUBLIC-L2-COLLECTION

简短描述：
- 在已保留且 termination-protected 的 c6in 胜者
  `i-0a962e47210528526` 上，按 amdserver 历史成功配置采集 2 小时
  Binance `SKHYNIXUSDT` 与 Hyperliquid `xyz:SKHX` public L2/trades 数据。
- 采集完成后将完整原始数据、manifest、日志与哈希拉回本地并做独立检查。

状态：
- 未通过

执行顺序：
- 当前唯一任务

前置任务：
- `0729T002` 已通过。

规则更新提醒：
- 请先阅读规则文档后再执行本任务。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 不需要

提交要求：
- 不需要提交
- 回报使用 `commit：无`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0729T003.md`
- `.workflow/reports/0729T003-business.md`
- `local_live_analysis/cross_exchange_public_sample_skhynix_0729T003_2h_<UTC>/`

action：
- 只读取 AWS 实例和网络状态，不停止、不重启、不替换胜者实例。
- 在 `i-0a962e47210528526` 上部署当前采集器所需的最小 public-data
  source package。
- 使用 Binance `SKHYNIXUSDT`：
  - `trade`
  - `depth@0ms`
  - `bookTicker`
  - 启动时获取 REST depth snapshot。
- 使用 Hyperliquid `xyz:SKHX`：
  - `l2Book`
  - `trades`
  - `l2Book fast=true`
- 采集时长冻结为 `7200` 秒，raw-only，跳过远端 alignment。
- 采集完成后把完整目录拉回本地，并检查 manifest、时长、消息量、
  subscription ack、重连/断线、双边 overlap、raw SHA256、gzip/JSONL
  可解析性和磁盘文件完整性。

hard boundary：
- Public market data only.
- No private keys or credential read.
- No account, order, cancel or private endpoint.
- No strategy process or live trading.
- No instance stop, reboot, terminate or replacement.
- Preserve API termination protection.
- No alignment on the c6in winner.
- No claim of 8H stability, replay acceptance, fill simulation, PnL or strategy
  promotion from this 2H sample.

verify：
- AWS instance identity/state/type/AZ and termination-protection check.
- Remote preflight for disk, Python runtime, DNS/TLS and collector CLI.
- Remote process liveness and output growth checks during the 2H window.
- Remote terminal manifest/hash/file checks.
- Local transfer completion and file-size reconciliation.
- Local SHA256 verification for both raw streams.
- Local gzip/JSONL parse and count reconciliation against collection manifests.
- Confirm effective two-venue overlap and report any reconnect/disconnect gap.
- `git diff --check -- .workflow/tasks/0729T003.md .workflow/reports/0729T003-business.md`

done：
- Report exact instance, UTC/CST start/end, output paths, commands, source
  identity, per-venue counts, durations, overlap, reconnects, acknowledgements,
  raw sizes and hashes, local parse results, blockers and QA readiness.
