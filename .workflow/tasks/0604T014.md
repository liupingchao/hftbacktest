```md
执行线程：
- 业务线程-python

任务ID：
- 0604T014

标题：
- Diagnose non-fatal confirmed position mismatch trading pause gap

简短描述：
- 检查 `[P2] confirmed position mismatch 在 non-fatal 模式下仍可能继续交易` 是否为事实，复现所有相关 case，定位根本原因，并检查 live safety 中是否存在同类 confirmed/pending 状态语义漏洞。本任务只诊断和设计回归 case，不做修复。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- `0604T013` 已完成业务执行并待 QA；本任务为新的诊断任务，不依赖 T013 修复内容。

规则更新提醒：
- 请先阅读规则文档后再执行本任务。
- 本任务只做诊断、复现和根因定位；不得修改 production 行为。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 提交任务文件和业务诊断报告；如为了复现新增 focused tests，应提交测试文件，但不得提交修复代码。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0604T014.md`
- `.workflow/reports/0604T014-business.md`
- `examples/binance_tick_mm/live_tick_mm.py` 只读检查。
- `examples/binance_tick_mm/strategy_core.py` 只读检查。
- `examples/binance_tick_mm/test_backtest_tick_mm.py` 或更合适的 focused test 文件，仅允许新增诊断/复现测试，不允许修复 production 行为。
- 必要 workflow tracking 文件仅限本任务状态/结论更新。

forbidden：
- 不修复 `live_tick_mm.py` 的 pause 条件。
- 不修改 strategy/live production 行为、connector、py binding、Rust live bot、配置默认值、audit schema、normal loop cancel 语义。
- 不启动 live / tiny-live / default-on。
- 不做参数搜索、回放实验扩展、promotion。
- 不把诊断范围扩大到无关 live safety 设计重构。

action：
- 检查 `evaluate_live_safety()` 的 position mismatch 状态迁移：`position_mismatch_pending`、`position_mismatch`、`position_mismatch_confirmations`。
- 检查 `live_tick_mm.py` 中 `fail_on_mismatch` fatal path 和 `position_mismatch_pause_trading` non-fatal pause path 的实际条件。
- 复现并记录以下 case matrix：
  - `fail_on_mismatch=true`：pending 是否不 fatal，confirmed 是否 fatal break。
  - `fail_on_mismatch=false + position_mismatch_pause_trading=true + confirmations=2`：pending 是否暂停，confirmed 是否继续交易。
  - `fail_on_mismatch=false + position_mismatch_pause_trading=true + confirmations=1`：第一次即 confirmed 是否绕过暂停。
  - `fail_on_mismatch=false + position_mismatch_pause_trading=false`：pending/confirmed 继续交易是否符合显式配置语义。
  - mismatch 恢复为 ok 后计数是否重置。
  - `rest_error`、`open_order_mismatch_pending/open_order_mismatch` 是否存在同类 confirmed 后反而更不保守的路径。
- 如新增测试，测试应只证明当前 bug 存在或同类 case 是否存在，不得把测试写成修复后期望除非标记为 xfail 或诊断断言。
- 输出根本原因、影响边界、所有可复现/不可复现 case、建议修复方案和后续回归测试清单。

acceptance criteria：
- 明确判断 review 是否为事实。
- 明确定位根本原因到具体状态迁移与 pause/fatal 条件。
- 覆盖并记录上述 case matrix 的实际结果。
- 明确说明是否存在 `confirmations=1` 的直接 confirmed 漏暂停 case。
- 明确说明 open-order mismatch 或 rest-error 是否存在同类 confirmed/pending 语义漏洞。
- 不提交 production 修复。
- 业务报告写入 `.workflow/reports/0604T014-business.md`，状态为 `待验收`。

verify：
- `python -m pytest examples/binance_tick_mm/test_backtest_tick_mm.py -k "live_safety or position_mismatch or safety_pause"`，优先使用 `/home/molly/anaconda3/envs/hftbacktest/bin/python`。
- 如新增 focused 诊断测试，运行对应 pytest filter。
- `/home/molly/anaconda3/envs/hftbacktest/bin/python -m pytest examples/binance_tick_mm/test_*.py`，如 scope/耗时允许。
- `git diff --check`

done：
- 回报必须说明改动点、复现 case matrix、根本原因、是否发现同类漏洞、推荐修复方案、测试结果、commit id 和 commit message。
```
