# 任务派发

执行线程：
- 业务线程-python/cross-exchange-research

任务ID：
- 0801T009

标题：
- HYPERLIQUID-TEMPORARY-REGIME-V2-SURROGATE-REPAIR

简短描述：
- 基于已验收 T006-T008 构建独立 `regime_v2/`，修复 context transform、
  real/surrogate score 同尺度、block-shuffle 边界校准和下游 motif linkage。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- `0801T008` QA `已通过`。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `docs/hyperliquid_liquidity_response_case_hierarchy_plan.md`

是否需要提交代码：
- 不需要

是否进行QA验收：
- 是

files：
- `examples/hyperliquid/cross_exchange_liquidity_response_regime_v2.py`
- `examples/hyperliquid/test_cross_exchange_liquidity_response_regime_v2.py`
- `local_live_analysis/skhynix_liquidity_response_case_hierarchy/regime_v2/`
- `.workflow/tasks/0801T009.md`
- `.workflow/reports/0801T009-business.md`
- `.workflow/reports/0801T009-qa.md`
- `docs/qa-acceptance-report.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

action：
- 严格验证 T006 Episode v2、T007 baseline v2 和 T008 motif v2 contracts。
- 每段独立构造 non-overlapping `1min` context windows。
- 只在 discovery `0001-0003` 冻结 context median/IQR、change metric 和
  discovery candidate threshold。
- Real sequence 与每组 block-shuffle surrogate 必须使用完全相同的冻结
  transform 和 change-score 公式。
- 使用不少于 `100` 组 surrogate，输出 candidate-level empirical p-value
  和 segment-level null summary。
- 只有优于 surrogate null 的边界可进入正式 regime intervals；其余只留
  audit。
- Regime boundaries 冻结后才链接 episode 和 motif_v2 membership。
- Motif-by-regime 只能报告 prevalence/distance 诊断，不得升级 T008 的
  `not_supported` classification。
- 输出 exact input/output closure、deterministic rebuild 和 hostile drift
  gates。

verify：
- focused tests 覆盖同尺度 score、block shuffle、empirical p-value、
  boundary publication、freeze-before-linkage、input/output drift。
- `ruff`、`py_compile`、CLI help。
- 真实 build 与重复构建；独立复核 boundary/null/interval/membership。
- `git diff --check`。

done：
- `regime_v2/` 独立发布，旧 `regime/` 保持不变。
- 不声明 permanent ontology、formal held-out motif support、tradable
  signal、maker identity、exact fill 或 maker PnL。
- 业务报告进入 `待验收`，独立 QA 后由总控收口。
