# 0722T057 FIXTURE-CONTAINMENT-AND-SKIP-QUALIFICATION-REPAIR

执行线程：
- 总控 / 业务执行线程

任务ID：
- 0722T057

标题：
- Repair artifact containment, malformed-package visibility and full-suite timing determinism

简短描述：
- 修复 T056 QA 的两个 P1：legacy path 必须严格 containment；只有真实
  缺失才能 skip，已存在但损坏的 package 必须失败。
- 固化 watcher interval test 的毫秒时钟，消除与 T056 无关但阻塞 full-green
  验收的测试时序抖动。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0722T056`（未通过）

规则更新提醒：
- offline-only；不得执行 network、remote、private/account、order/cancel、
  live 或 service 操作。
- 不改变 production timestamp、strategy、quote、risk 或 lifecycle 语义；
  watcher 修复只允许 test clock injection。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交。
- 回报必须带 exact implementation commit id 和提交信息。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `examples/hyperliquid/basis_positive_row_level_read_only_generator.py`
- `examples/hyperliquid/test_basis_positive_row_level_read_only_generator.py`
- `examples/hyperliquid/test_hyperliquid_tiny_live_m2_event_driven_watcher.py`
- `.workflow/tasks/0722T057.md`
- `.workflow/reports/0722T057-business.md`
- T056 exact SHA workflow correction
- controller tracking files

action：
- 拒绝 legacy path 的 `..` traversal 和 symlink escape；resolved candidate
  必须位于 resolved `PROJECT_ROOT`。
- 引入 missing-artifact 专用异常；availability probe 只捕获真实缺失，
  JSON/schema/canonical/sample contract 错误必须传播。
- 增加 traversal、symlink escape 和 malformed-existing package hostile
  regressions。
- 给 resting-interval contract test 注入严格递增毫秒时钟，不改生产时钟。
- 修正 T056 business/controller 中错误的 implementation full SHA。

verify：
- Focused portability hostile tests。
- Exact watcher resting-interval test 重复运行。
- Full Hyperliquid 至少连续两次全绿。
- `python -m py_compile` 和 `git diff --check`。

done：
- 两个 T056 P1 均有 hostile regression 并修复。
- Semantic corruption 不再被 absent-package skip 隐藏。
- Full suite 不再依赖真实毫秒边界碰运气。
- 经独立 QA 接受后，fixture portability 阶段关闭。

blockers：
- 无。

提交信息：
- `Harden artifact portability gates`
