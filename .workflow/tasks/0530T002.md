```md
执行线程：
- 业务线程-python

任务ID：
- 0530T002

标题：
- Stage 9M targeted clean-fill evidence collection / read-only rerun

简短描述：
- 基于 `0529T005` Stage 9L QA 结论，围绕最高优先级 clean-fill 缺口 regime 做一次窄范围、只读证据采集与派生链 rerun 规划/执行任务。本任务目标是补足自然 clean fills 并重新运行既有只读分析链，判断是否出现 `ready_for_policy_design_after_coarsening`；不改变策略、不启用候选、不放宽 guard、不做参数搜索、不做 tiny-live/default-on/promotion。

状态：
- 执行中

执行顺序：
- 可并行；本任务是当前唯一 Binance 任务，与 Hyperliquid `0530T001` 独立并可并行执行，二者不得互相修改、依赖或混入对方工作。

前置任务：
- `0529T002` QA 已通过。
- `0529T005` QA 已通过。
- 直接事实源：
  - `.workflow/tasks/0529T005.md`
  - `.workflow/reports/0529T005-business.md`
  - `.workflow/reports/0529T005-qa.md`
  - `local_live_analysis/stage9l_fill_quality_rejection_decomposition_0529T005/**`

规则更新提醒：
- 请先阅读规则文档后再执行本任务。
- 本任务是 current-format no-rule/default-off control evidence collection plus read-only rerun，不是策略实现、候选启用、guard 放宽、参数搜索、tiny-live、default-on 或 promotion 任务。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `docs/thread-playbook.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 提交任务文件、业务报告、必要 workflow 更新，以及本任务生成的 task-scoped read-only artifacts。
- 仅当发现既有 runner 无法完成 accepted rerun chain 且修复属于本任务边界时，才允许改 Python 源码并补 focused tests；否则不需要代码实现变更。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

Allowed files：
- `.workflow/tasks/0530T002.md`
- `.workflow/reports/0530T002-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `.workflow/dashboard.html` and `.workflow/dispatch_suggestions.md` only if they can be regenerated without pulling unrelated task state into this task.
- New task-scoped local output directories under `local_live_analysis/**` for the Stage 9M collection/rerun artifacts.

Allowed read-only inputs：
- `.workflow/tasks/0529T002.md`
- `.workflow/tasks/0529T005.md`
- `.workflow/reports/0529T002-business.md`
- `.workflow/reports/0529T002-qa.md`
- `.workflow/reports/0529T005-business.md`
- `.workflow/reports/0529T005-qa.md`
- `docs/qa-acceptance-report.md`
- `local_live_analysis/stage9k_fill_quality_bucket_synthesis_0529T002/**`
- `local_live_analysis/stage9l_fill_quality_rejection_decomposition_0529T005/**`
- Existing current-format Binance sample directories under `local_live_analysis/**`
- Existing Binance maker scripts under `examples/binance_tick_mm/**` needed to rerun accepted maker acceptance, T009 sidecar/join, Stage 5, Stage 5C, Stage 6, Stage 9K, and Stage 9L.

Forbidden files / areas：
- Strategy behavior, quote placement implementation, fair/target formula, inventory logic, risk guards, production defaults, or live configs except a no-rule/default-off control collection config already required by the accepted workflow.
- Candidate enablement, guard relaxation, parameter search, default-on behavior, tiny-live, promotion claim, or production behavior change.
- New strategy implementation or policy design based on Stage 9M output before QA.
- Replay lifecycle semantic changes, queue/touch repair, exact queue position claims, hidden queue assumptions, connector/core API changes, standard npz schema changes, or canonical audit schema changes.
- Treating caveated samples as strict-clean proof.
- Treating future fill, future markout, future spread capture, same-sample PnL feedback, exact queue position, or hidden queue assumptions as strategy trigger inputs.
- Hyperliquid public/private work, including the independent `0530T001` task.

Problem statement：
- `0529T005` Stage 9L final classification is `needs_targeted_clean_fills`.
- Shape A / Shape B candidates remain `0`, and churn-warning sensitivity alone created `0` ready candidates.
- The top Stage 9L clean-fill gap is:
  - `churn_warning_coarsened / large_skew_or_low_score / add_side / step_back_gt1 / edge_non_adverse / market_view_usable / post_only_clean / warning_churn_context`
  - observed rows: `2605`
  - fills: `34`
  - samples: `7`
  - fill samples: `6`
  - Stage 9L minimum gap: `6` additional clean fills
- The next useful Binance task is not policy design. It is a targeted current-format control collection plus read-only rerun to test whether natural clean-fill evidence can move any coarsened bucket into a policy-design-ready classification.

Execution scope：
1. Confirm current Stage 9L top-gap facts from the accepted artifacts before any collection/rerun.
2. First scan existing accepted current-format control samples that are not already included in the accepted Stage 9K/9L input set, if any. If an existing sample can add relevant clean-fill evidence without new collection, prefer that route and document the sample-selection reason.
3. If existing samples cannot add relevant clean-fill evidence, prepare exactly one new current-format no-rule/default-off control collection. Remote/live startup requires a separate explicit user/controller approval before execution. The planned collection window is `120min`, with no candidate enablement, no strategy-policy change, no guard relaxation, no parameter sweep, and no promotion-readiness claim.
4. Prioritize natural evidence for the Stage 9L top-gap regime:
   - `large_skew_or_low_score`
   - `add_side`
   - `step_back_gt1`
   - `edge_non_adverse`
   - `market_view_usable`
   - `post_only_clean`
   - `warning_churn_context`
5. Target evidence threshold, for measurement and recommendation only, not as a hard QA pass/fail gate:
   - At least `+20` clean fills in the top-gap regime if feasible, or
   - At least `+60` clean fills across Stage 9L gap regimes, with exact achieved counts reported.
6. Whether or not the target evidence threshold is reached, continue to the accepted derived-chain rerun whenever a valid existing or newly collected current-format control sample is available:
   - maker acceptance
   - T009 sidecar/join
   - Stage 5 execution labels / fill markouts
   - Stage 5C safety diagnostics
   - Stage 6 lifecycle calibration
   - Stage 9K fill-quality bucket synthesis
   - Stage 9L rejection decomposition / coarsening
7. Produce a task-scoped Stage 9M summary that compares before/after:
   - top-gap rows/fills/samples/fill_samples
   - ready / needs-more / reject bucket counts
   - Shape A / Shape B candidate counts
   - any `ready_for_policy_design_after_coarsening` bucket, if present
   - market-view / post-only / churn / fill-quality caveats
8. If no new collection is run because an existing sample is sufficient, the business report must state that reason and classify the task as existing-sample rerun.
9. If no valid existing sample is available and remote/live collection approval is not granted, the business report must classify the task as blocked before collection, with no invented passing rerun result.

Required outputs：
- `.workflow/reports/0530T002-business.md`
- A task-scoped Stage 9M output directory under `local_live_analysis/**` containing:
  - run manifest
  - sample/source manifest
  - rerun command log or command summary
  - before/after Stage 9K/9L comparison
  - targeted gap summary
  - recommendation markdown
- Updates to `task_plan.md`, `progress.md`, and `findings.md`.

Acceptance criteria：
- The task preserves the no-rule/default-off control boundary.
- The task first checks whether an existing accepted current-format sample can supply relevant clean-fill evidence before requesting new collection.
- Any new remote/live collection is limited to one `120min` current-format no-rule/default-off control run and requires separate explicit approval before startup.
- The task reports whether targeted evidence actually reduced the Stage 9L top-gap clean-fill shortage.
- The task reruns or explicitly blocks the accepted maker acceptance -> T009 -> Stage 5 -> Stage 5C -> Stage 6 -> Stage 9K -> Stage 9L chain.
- The task states whether any coarsened bucket reached `ready_for_policy_design_after_coarsening`.
- The `+20` top-gap clean fills / `+60` gap-regime clean fills threshold is treated as a target for interpretation, not a hard QA pass/fail gate.
- The task does not modify strategy behavior, enable candidates, relax guards, run parameter search, default-on anything, start tiny-live, or claim promotion.
- The task does not modify Binance strategy/core semantics or Hyperliquid files.
- The business report ends in `待验收`.

verify：
- Focused help/smoke commands for any runner invoked, as applicable.
- Focused pytest only if source code or tests are changed.
- `python -m py_compile` for any touched Python files, if source code is changed.
- JSON/CSV artifact existence and schema sanity checks for generated Stage 9M outputs.
- `git diff --check`.
- Manual consistency check that no strategy behavior, guard, parameter, default-on, tiny-live, promotion, or Hyperliquid changes were made.

done：
- Business report lists files read, files changed, commands run, generated artifacts, final classification, and explicit forbidden-boundary confirmation.
- Report includes `commit id` and `提交信息`.
- Report explicitly states:
  - whether new control evidence was collected or why it was blocked
  - whether the top Stage 9L gap crossed the clean-fill threshold
  - whether Shape A / Shape B candidates remain `0` or changed
  - whether policy design remains blocked or can be proposed as a later design-only task
```
