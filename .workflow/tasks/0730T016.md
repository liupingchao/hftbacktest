# 任务派发

执行线程：
- 业务线程-python/cross-exchange-research

任务ID：
- 0730T016

标题：
- R1-ALIGNMENT-AUDIT-GATE-REPAIR

状态：
- 已通过

前置任务：
- `0730T015` QA `未通过`。

action：
- 生成逐 decision/horizon 冻结 label table，包含 primary response 和
  diagnostic wall-clock label、source timestamp/age/no-new-information。
- 从 eligible denominator 排除无 prior Hyperliquid BBO state 的 decision。
- 将 source provenance、raw/output row/SHA、exact masks 纳入 pass gate。
- 增加 reconciliation mismatch duration、near-time/source-age 分层及
  明确 acceptance gate。
- 为 dedupe、join、horizon、mask、provenance、reconciliation 增加
  failure-injection tests。

hard boundary：
- 仅使用现有本地数据。
- R2/R3 继续锁定。
- 不新增采集；任何采集仍需用户明确授权和活跃交易时段确认。
