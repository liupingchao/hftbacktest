```md
执行线程：
- 测试线程

任务ID：
- 0512T007

标题：
- T005 toxic timing attribution 实验

简短描述：
- 基于 `0512T006` 计划，对既有 `0512T005` Stage 6J replay CSV/audit 输出做只读 attribution / experiment implementation，解释 pure toxic timing candidates 为什么有 action-path coverage 但没有 replay risk improvement。本任务不改策略代码、不运行新 Stage 6J replay、不设计更严格候选规则、不启动 live。

状态：
- 已通过

执行顺序：
- 当前唯一任务；`0512T006` QA 通过后执行

前置任务：
- 0512T006

规则更新提醒：
- 本任务是只读 attribution / experiment implementation。
- 只读取已有 T005 Stage 6J 输出并生成 T007 attribution 表和报告。
- 不修改策略代码，不运行新 Stage 6J replay，不设计更严格候选规则，不启动 live。
- Stage 6J 仍只能作为 replay-model regression / attribution evidence，不能声明 live-derived source-path proof。

按线程规则执行：
- .workflow/workflow-kit/thread-report-template.md
- docs/thread-playbook.md
- .workflow/reports/0512T006-business.md
- local_live_analysis/stage6j_cross_sample_0512T005/STAGE6J_B_REPLAY_SUMMARY.md

是否需要提交代码：
- 不需要

提交要求：
- 不需要提交
- 回报必须写 `commit：无`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- .workflow/tasks/0512T007.md
- .workflow/reports/0512T007-business.md
- local_live_analysis/stage6j_cross_sample_0512T005/stage6j_replay_summary.csv
- local_live_analysis/stage6j_cross_sample_0512T005/stage6j_replay_summary.json
- local_live_analysis/stage6j_cross_sample_0512T005/stage6j_replay_decision.json
- local_live_analysis/stage6j_cross_sample_0512T005/*/*/audit_bt_stage6j.csv
- local_live_analysis/stage6j_cross_sample_0512T005/t007_attribution/
- findings.md
- progress.md
- task_plan.md

Fatal Failures：
- 修改策略代码或策略配置默认值。
- 启动 live、连接交易所、修改 AWS 状态。
- 运行新的 Stage 6J replay 或长耗时实验。
- 在本任务中设计、实现或推荐更严格候选规则作为已验证结论。
- 把 Stage 6J source-path 指标当成 live-derived source-path proof。
- 跳过 `0512T006` 的三类计划：blocked-row strata、submit-removal linkage、reason/window attribution。

MUST NOT：
- 不改策略代码。
- 不运行 live。
- 不运行新的 Stage 6J replay。
- 不新增候选规则。
- 不把 attribution 结果写成 live promotion 依据。

action：
- 读取 `0512T006` business report 和 T005 Stage 6J replay summary/audit CSV。
- 生成 blocked-row strata 表和聚合表，区分 true submit removal、baseline no submit、no action diff、non-submit diff 等类别。
- 生成 submit-removal rows 与 replay risk lifecycle linkage 表。
- 生成 reason attribution、reason risk overlap、50/100/200ms window equivalence 表。
- 输出 `local_live_analysis/stage6j_cross_sample_0512T005/t007_attribution/T007_ATTRIBUTION_SUMMARY.md`。
- 输出 `.workflow/reports/0512T007-business.md`，更新 `findings.md` 和 `progress.md`。

verify：
- 检查 T007 输出目录和 CSV/markdown 文件存在。
- 检查 blocked-row strata 总量、submit_removed 总量、blocked reduce-side 口径与 T005 summary 对齐。
- `python3 .workflow/build_dashboard.py`

done：
- 回报必须给出实验结果和分析。
- 回报必须解释 pure toxic timing 有 coverage 但没有 replay risk improvement 的直接原因。
- 回报必须说明 50/100/200ms 是否等价以及原因。
- 回报必须明确没有规则 redesign、没有新 replay、没有 live、没有 live-derived source-path proof。
- 回报必须明确下一步是否需要另建规则设计任务。
```
