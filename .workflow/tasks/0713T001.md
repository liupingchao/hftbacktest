# 任务派发

执行线程：
- 业务线程-python

任务ID：
- 0713T001

标题：
- T011-RESTING-INTERVAL-PUBLIC-FLOW-CAPTURE-INSTRUMENTATION

简短描述：
- 在 `0712T001` QA 已通过后，给受控 live watcher/artifact path 增加 resting-interval public-flow capture instrumentation，使未来 same-envelope live evidence 能输出 `cross_exchange_resting_interval_public_flow_contract_v1` 所需字段。
- 本任务只做实现和离线/mock 验证；不运行 live、不调用 endpoint、不改阈值、报价包络、订单大小、提交次数或策略行为。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- 0712T001 QA 已通过

规则更新提醒：
- 请先阅读规则文档后再执行本任务。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `docs/cross_exchange_resting_interval_public_flow_auto_loop_plan.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `examples/hyperliquid/`
- `.workflow/tasks/0713T001.md`
- `.workflow/reports/0713T001-business.md`
- `.workflow/reports/0713T001-qa.md`
- `docs/qa-acceptance-report.md`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `local_live_analysis/cross_exchange_resting_interval_public_flow_capture_instrumentation_0713T001/`

action：
- Extend the existing Hyperliquid controlled watcher/artifact writer path under `examples/hyperliquid/` so a future same-envelope live run can emit resting-interval public-flow artifacts keyed by exact attempt.
- Add or update offline/mock artifact generation logic for:
  - `order_resting_exchange_time_ms` when available from exchange response or nearest defensible source.
  - `order_resting_local_receive_ts_ns`.
  - `cancel_request_time_ms`.
  - `cancel_ack_exchange_time_ms_or_shutdown_proof_time_ms`.
  - `resting_interval_public_trades.csv` keyed by exact `attempt`.
  - `resting_start_l2_book_snapshot_at_or_after_order_resting`.
  - `resting_interval_depth_depletion_matrix.csv` inputs sufficient to estimate trade-through/depletion during the actual resting interval.
  - a boundary manifest confirming no threshold, quote-envelope, size, max-submission, strategy behavior, live, remote/AWS, credential, private/account/order/cancel endpoint, or market-data collection occurred in this task.
- Preserve exact/proxy status fields. If an exact exchange timestamp is unavailable, write an explicit source/status field and keep downstream routing conservative.
- Generate a task-scoped offline/mock artifact package under `local_live_analysis/cross_exchange_resting_interval_public_flow_capture_instrumentation_0713T001/`.

hard boundary：
- No live-submit.
- No live retry.
- No remote/AWS execution.
- No credential read.
- No private/account/order/cancel endpoint.
- No new market-data collection.
- No threshold change.
- No quote-envelope change.
- No order-size increase.
- No max-submission increase.
- No strategy behavior change.
- No fill-probability model.
- No fill-seeking placement.
- No synthetic fill, queue-priority claim, fee/rebate, realized PnL, maker viability, T012, promotion, or final MVP claim.

verify：
- Focused pytest for new/changed artifact writer logic.
- `python -m py_compile` for changed runner(s).
- CLI `--help` for changed runner(s).
- Synthetic/mock artifact test proving keyed public trades are captured only for the matching attempt.
- Synthetic/mock artifact test proving proxy lifecycle/depth does not route to `offline_repair_sufficient`.
- JSON/CSV schema validation for generated mock artifacts.
- `git diff --check`.

done：
- Report changed files and generated artifact paths.
- Report the exact new/updated artifact schema.
- Report how future live capture should record remote `awsserver1` source artifacts and local pullback paths, without executing live in this task.
- Report verification commands and results.
- Report whether any blocker remains before Step 3 live evidence authorization.
- Confirm no threshold/quote-envelope/live/endpoint/strategy behavior change occurred.
