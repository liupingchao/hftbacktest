```md
执行线程：
- 测试线程

任务ID：
- 0526T008

标题：
- Immediate 30min current-format no-rule/default-off live test collection

简短描述：
- 以 `0526T002` 为蓝本，立即采集一个新的 `30min` current-format no-rule / default-off active control live test 样本，用自然市场条件补充 maker-edge / min-move / inventory-aware quote-placement 后续研究所需的 fills、market-view quality 和 Stage 5/5C/6/9 输入。本任务只做采集、同步、归档、audit replay 和既有诊断重跑；不启用 candidate、不放宽 guard、不做 tiny live 或 promotion。

状态：
- 已通过

执行顺序：
- 当前唯一新采集任务；用户明确要求忽略尚未完成的前置任务并立即派发执行。`0526T006` / `0526T007` 不作废，但本任务不等待它们收口。

前置任务：
- 用户 override：忽略未完成前置，立即派发执行。
- `0526T002` QA 已通过，可作为流程蓝本。
- `0519T006` QA 已通过，T006 quote-update audit fields 已落地。

规则更新提醒：
- 请先阅读规则文档后再执行本任务。
- 本任务是 30min live control sample collection，不是策略实现、参数搜索、tiny live、promotion 或 default-on 任务。
- 不允许为了增加 fills 放宽 quote throttle、min-move、latency、post-only、stale、inventory、risk、API 或 order sizing guard。

按线程规则执行：
- `.workflow/workflow-kit/thread-report-template.md`
- `docs/thread-playbook.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 采集前提交/推送可部署 workflow commit，并同步到 `awsserver1` 的 task-scoped clean worktree。
- 采集启动后先回报 run id、deployed commit、remote worktree、run dir、preflight / audit header 结果和预计结束时间。
- 采集、拉回、重跑、报告完成后提交 workflow/report/artifacts。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

Allowed files：
- `.workflow/tasks/0526T008.md`
- `.workflow/reports/0526T008-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `.workflow/dashboard.html`
- `.workflow/dispatch_suggestions.md`
- `local_live_analysis/5-26-active-minmove-control-30min-b/**`
- `local_live_analysis/archive/5-26-active-minmove-control-30min-b.tar.gz`
- `local_live_analysis/archive/5-26-active-minmove-control-30min-b.tar.gz.sha256`

Allowed read-only inputs：
- `.workflow/tasks/0526T002.md`
- `.workflow/reports/0526T002-business.md`
- `.workflow/reports/0526T002-qa.md`
- `local_live_analysis/5-26-active-minmove-control-60min-a/**`
- `local_live_analysis/5-26-active-minmove-control-30min-a/**`
- `examples/binance_tick_mm/deploy/run_live.sh`
- `examples/binance_tick_mm/deploy/preflight_live_run.py`
- `examples/binance_tick_mm/audit_schema.py`
- `examples/binance_tick_mm/strategy_core.py`
- `examples/binance_tick_mm/live_tick_mm.py`
- `examples/binance_tick_mm/backtest_tick_mm.py`
- `examples/binance_tick_mm/align_live_run.py`
- `examples/binance_tick_mm/maker_acceptance.py`
- `examples/binance_tick_mm/binance_top5_provenance.py`
- `examples/binance_tick_mm/execution_outcome_labels.py`
- `examples/binance_tick_mm/quote_anchor_safety.py`
- `examples/binance_tick_mm/execution_outcome_calibration.py`
- `examples/binance_tick_mm/quote_adjustment_replay.py`
- `examples/binance_tick_mm/candidate_bucket_refinement.py`

Allowed remote paths：
- `awsserver1:/home/admin/hft_live/worktrees/0526T008-active-minmove-30min-b/**`
- `awsserver1:/home/admin/hft_live/runs/5-26-active-minmove-control-30min-b/**`
- `awsserver1:/home/admin/hft_live/config/config_live.toml`
- `awsserver1:/home/admin/hft_live/config/binancefutures.toml`

Forbidden files / areas：
- Strategy rule logic, fair/target formula, quote behavior, production defaults, live production configs beyond task-scoped run dirs.
- Core/connector APIs, canonical data conversion, standard hftbacktest npz schema.
- Any candidate enablement, default-on behavior, guard relaxation, Step 5C promotion, inventory-control implementation, queue/touch repair, runner modification, parameter sweep implementation, tiny live, or promotion claim.

Requires separate task：
- Implementing any maker-edge candidate.
- Running parameter search.
- Promoting any candidate to live or production.
- Running a tiny live candidate test.
- Changing strategy behavior or production defaults.
- Generalizing or fixing `align_live_run.py` remote raw path behavior; this task may use a task-scoped remote symlink if needed.

Fatal Failures：
- Enabling any quote-adjustment or maker-edge candidate.
- Relaxing quote throttle, min-move, latency, post-only, stale, inventory, risk, API or order sizing guards to manufacture fills.
- Starting while another live connector/bot/collector run is still active.
- Continuing after early audit header check shows missing T006 fields.
- Accepting raw gzip corruption without documented repair and preserved original.
- Accepting sidecar/join quality with `future_join_count > 0` or `gap_crossed_join_count > 0` as clean input.
- Interpreting this 30min control sample as live strategy promotion proof or `ready_for_tiny_live_design`.

dataset：
- Run id: `5-26-active-minmove-control-30min-b`
- Duration target: about `30min`
- Stopper seconds: `1800`
- Target window: active / non-calm market conditions if available; if market turns calm, do not compensate by changing strategy parameters.
- Purpose: collect natural current-format evidence for maker-edge / min-move / inventory-aware quote-placement research, especially fills, inventory states, quote distance, spread capture, adverse markout, churn/API/stale regimes, and market-view quality.

live-test parameter policy：
- Allowed live-test parameter changes:
  - `run_id_prefix = "5-26-active-minmove-control-30min-b"`
  - `audit_csv = "/home/admin/hft_live/runs/5-26-active-minmove-control-30min-b/output/audit_live_5-26-active-minmove-control-30min-b.csv"`
  - `[paths].output_root = "/home/admin/hft_live/runs/5-26-active-minmove-control-30min-b/output"`
  - `[live_safety].connector_config = "/home/admin/hft_live/runs/5-26-active-minmove-control-30min-b/binancefutures.toml"`
  - remote stopper sleep seconds to `1800`
  - task-scoped run dir and log/raw/output paths
- Disallowed live-test parameter changes:
  - `quote_throttle_enabled`
  - `min_quote_update_interval_ms`
  - `min_quote_move_ticks`
  - `two_phase_replace_enabled`
  - `latency_guard_ms`
  - `api_limit.*`
  - `risk.*`
  - `fair.*`
  - `impact.*`
  - `order_notional`
  - `max_position_qty`
  - any candidate or default-on control
- Baseline config source should be the accepted current-format no-rule/default-off config from `5-26-active-minmove-control-60min-a` if available, otherwise `5-26-active-minmove-control-30min-a`. The report must state the source and show the diff only contains allowed run-path / run-id / duration changes.

AWS sync / deploy action：
- Push the local task commit to origin.
- On `awsserver1`, create or update clean detached worktree:
  - `/home/admin/hft_live/worktrees/0526T008-active-minmove-30min-b`
- Checkout the pushed commit in that worktree.
- Ensure shared release binaries are available via:
  - `target -> /home/admin/hft_live/hftbacktest/target`
- Confirm no existing `tmux` live session or live collector/connector/bot process is running before start.
- Prepare remote run dir:
  - `/home/admin/hft_live/runs/5-26-active-minmove-control-30min-b/`
- Copy baseline config into remote run dir and apply only allowed path/run-id changes.
- If `align_live_run.py` requires remote `/data/*.gz`, create task-scoped symlink:
  - `data -> raw_market_data`
  after collection or before local pull; do not move or delete raw files.
- Start one no-rule / default-off control live run for about `30min`.
- Early-check live audit header for all T006 quote-update fields.
- Stop cleanly through a `1800s` stopper and preserve deployment manifest, start/stop markers, audit, raw gzip, connector logs, collector logs, and bot logs.

action：
- Run focused local preflight checks before deploy:
  - `python -m pytest examples/binance_tick_mm/test_deploy_preflight.py`
  - `python -m pytest examples/binance_tick_mm/test_quote_adjustment_replay.py`
  - `python examples/binance_tick_mm/deploy/preflight_live_run.py --help`
  - `python examples/binance_tick_mm/align_live_run.py --help`
  - `python examples/binance_tick_mm/maker_acceptance.py --help`
  - `python examples/binance_tick_mm/quote_adjustment_replay.py --help`
  - `python examples/binance_tick_mm/candidate_bucket_refinement.py --help`
  - `bash -n examples/binance_tick_mm/deploy/run_live.sh`
- Start remote collection.
- During execution, report:
  - run id
  - deployed commit
  - remote worktree
  - remote run dir
  - preflight status
  - audit header coverage
  - expected stop time
- After completion, pull artifacts and run the accepted current-format pipeline:
  - `align_live_run.py`
  - `maker_acceptance.py`
  - T009 sidecar build and decision join
  - Stage 5 labels
  - Step 5C diagnostics
  - Stage 6 calibration
  - Step 9B runner
  - Step 9D single-sample and aggregate diagnostics as needed
- Archive local run dir and write sha256.

verify：
- Local focused checks pass before deploy.
- Remote preflight has `git.dirty=false`, `compatibility.passed=true`, and T006 audit fields available.
- Early audit header contains all T006 quote-update fields.
- Start/stop markers exist and duration is about `30min`; stop marker exit code is `0`.
- Raw gzip exists and passes `gzip -t`, or recovery is documented and original preserved.
- `align_live_run.py` exits 0.
- `maker_acceptance.py` exits 0 with hard failures `[]`, or any caveat is explicitly documented.
- T009 sidecar/join reports `future_join_count=0`, `gap_crossed_join_count=0`, and `join_missing_count=0` for clean classification.
- Stage 5 / 5C / 6 / 9B outputs exist after completion.
- `python3 .workflow/build_dashboard.py`
- `git diff --check`

done：
- Initial execution report must state whether the 30min collection has started and is still running, with expected stop time.
- Final execution report must list run id, start/stop time, deployed commit, remote worktree path, baseline config source, allowed config diff, preflight results, T006 header coverage, acceptance results, sidecar/join metrics, Stage 5 counts, Step 5C key metrics, Stage 6 summary, Step 9B classification, archive sha256.
- Final report must explicitly state that this is current-format no-rule/default-off control data only: no guard relaxation, no candidate enablement, no parameter sweep, no live/default-on/promotion authorization.
```
