```md
执行线程：
- 业务线程-python

任务ID：
- 0526T004

标题：
- Step 9G narrow min_move_quote_age_churn_guard read-only parameter-sweep implementation

简短描述：
- 按 `0526T003` 已通过的 design contract，实现并运行窄范围 read-only parameter sweep。只评估 `min_move_quote_age_churn_guard`，seed 只围绕 `inventory_state=large_skew_or_low_score` 和 `latency_stale_age=stale_latency_medium`，输出 clean-only / all-sample sweep metrics、stability summary 和 recommendations。不得 default-on、不得 live、不得放宽 guard、不得 promotion。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0526T003` QA 已通过
- `0526T002` QA 已通过
- `0525T001` QA 已通过

规则更新提醒：
- 请先阅读规则文档后再执行本任务。
- 本任务是 read-only offline sweep implementation，不是 live、tiny-live、default-on、guard relaxation 或 promotion 任务。

按线程规则执行：
- `.workflow/workflow-kit/thread-report-template.md`
- `docs/thread-playbook.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

Allowed files：
- `.workflow/tasks/0526T004.md`
- `.workflow/reports/0526T004-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `.workflow/dashboard.html`
- `.workflow/dispatch_suggestions.md`
- `examples/binance_tick_mm/min_move_parameter_sweep.py`
- `examples/binance_tick_mm/test_quote_adjustment_replay.py`
- `local_live_analysis/stage9g_min_move_parameter_sweep_0526T004/**`

Allowed read-only inputs：
- `.workflow/tasks/0526T003.md`
- `.workflow/reports/0526T003-business.md`
- `.workflow/reports/0526T003-qa.md`
- `local_live_analysis/stage9d_candidate_bucket_refinement_0526T002_aggregate/**`
- `local_live_analysis/5-19-day-control-30min/**`
- `local_live_analysis/5-19-night-active-30min-a/**`
- `local_live_analysis/5-19-night-active-30min-b/**`
- `local_live_analysis/5-19-night-active-30min-c/**`
- `local_live_analysis/5-21-day-control-60min/**`
- `local_live_analysis/5-26-active-minmove-control-30min-a/**`
- `local_live_analysis/5-26-active-minmove-control-60min-a/**`
- `examples/binance_tick_mm/quote_adjustment_replay.py`
- `examples/binance_tick_mm/candidate_bucket_refinement.py`

Forbidden files / areas：
- Strategy behavior, production defaults, live configs, fair/target formula, risk/position logic, quote placement logic.
- Core/connector APIs, standard npz schema, converter behavior, replay lifecycle repair, queue/touch repair.
- Live collection, tiny-live, default-on behavior, guard relaxation, or promotion claims.

Fatal Failures：
- Expanding candidate scope beyond `min_move_quote_age_churn_guard`.
- Expanding seed scope beyond `inventory_state=large_skew_or_low_score` and `latency_stale_age=stale_latency_medium`.
- Using future fill, future markout, same-sample PnL feedback, or fill-after-cancel outcome as decision inputs.
- Treating caveated samples as strict-clean proof.
- Emitting `ready_for_tiny_live_design`, default-on, or live-promotion recommendations.

action：
- Implement a focused read-only sweep runner.
- Generate deterministic parameter grid:
  - maximum `432` unique parameter sets
  - maximum `756` seed-slice evaluations
- Evaluate slices:
  - A only: `inventory_state=large_skew_or_low_score`
  - B only: `latency_stale_age=stale_latency_medium`
  - A/B intersection
  - A/B union only as context aggregation, not a separate sweep pass
- Use seven current-format samples:
  - `5-19-day-control-30min`
  - `5-19-night-active-30min-a`
  - `5-19-night-active-30min-b`
  - `5-19-night-active-30min-c`
  - `5-21-day-control-60min`
  - `5-26-active-minmove-control-30min-a`
  - `5-26-active-minmove-control-60min-a`
- Treat caveated samples only as sensitivity:
  - `5-19-night-active-30min-a`
  - `5-26-active-minmove-control-60min-a`
- Use local `amdserver` resources through shard/reducer design; support worker/chunk options and deterministic outputs.
- Emit:
  - `run_manifest.json`
  - `parameter_grid.csv`
  - `parameter_grid.json`
  - `sweep_shards/*.csv`
  - `sweep_metrics.csv`
  - `sweep_stability_summary.csv`
  - `sweep_stability_summary.json`
  - `seed_slice_coverage.csv`
  - `candidate_recommendations.md`

verify：
- `python examples/binance_tick_mm/min_move_parameter_sweep.py --help`
- smoke run on focused local fixture / tiny grid through pytest
- full read-only run over seven current-format samples
- `python -m pytest examples/binance_tick_mm/test_quote_adjustment_replay.py`
- `python3 .workflow/build_dashboard.py`
- `git diff --check`

done：
- State whether any parameter set is `sweep_seed_promising`.
- State whether the promising effect is churn reduction, toxic-fill reduction, or simple fill suppression.
- State whether results support a later narrower implementation / more strict-clean sampling / reject.
- State explicitly that no tiny-live, default-on, live promotion, guard relaxation, or `ready_for_tiny_live_design` is authorized.
```
