# 0719T008 RAW-FILL-ALL-TOKEN-CANONICAL-PATH-REPAIR

执行线程：
- 总控 auto-loop / 业务实现线程

任务ID：
- 0719T008

标题：
- Repair raw fill reconstruction, all-token identity and canonical executable/output binding

简短描述：
- Close all three `0719T007` QA P1 findings before any bounded two-sided live task.
- This task is offline-only.

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0719T007`（未通过）

规则更新提醒：
- T007 accepted sub-results remain authoritative: raw order-response binding, unified partial/no-fill terminal checks, pre-submit exact side gate and real manager-watcher artifact generation.
- No live/private/account/order/cancel/network/remote/service action is allowed in this task.
- Multi-level, dynamic spread, fill feedback, inventory skew and actual quote behavior changes remain off.

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `.workflow/reports/0719T007-qa.md`

是否需要提交代码：
- 需要

提交要求：
- 离线 focused/full regression 通过后提交。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- Modify: `examples/hyperliquid/cross_exchange_live_remote_orchestrator.py`
- Modify: `examples/hyperliquid/hyperliquid_tiny_live_m2_fill_window.py`
- Modify: `examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- Modify: `examples/hyperliquid/cross_exchange_t024_same_window_acceptance.py`
- Modify: focused orchestrator/fill/watcher/acceptance tests
- Workflow: this task, business report, QA report, `docs/qa-acceptance-report.md`, `task_plan.md`, `progress.md`, `findings.md`

action：
- Persist raw user-fill pullbacks with redaction-safe oid/cloid identity tokens before generic redaction.
- Independently rebuild fill rows from persisted raw pullbacks in Task 12 acceptance:
  - bind each raw fill to one canonical attempt through exact reference identity;
  - rebuild side, quantity, liquidity role and fill identity;
  - require exact agreement with ledger, attribution and liquidity-role artifacts;
  - reject missing/empty raw pullbacks when CSV evidence claims fills.
- Enforce all-token same-reference semantics:
  - when both oid and cloid are present, both must resolve to the same attempt;
  - one matching identifier may not override one conflicting identifier;
  - apply independently in producer attribution and acceptance reconstruction.
- Disable argparse long-option abbreviation for orchestrator and watcher CLIs.
- Define the exact allowed watcher argv grammar and reject unknown, abbreviated, duplicate, equals-style or multiple-mode flags.
- Seal the exact watcher command list in runtime source provenance before child start.
- Bind runner/preflight command, Python executable, watcher script, task/window run-id and canonical output directory to the sealed runtime provenance command.
- Add complete adversarial regressions for all three T007 QA findings.
- Keep quote formulas, thresholds, risk caps, activation flags and non-exact legacy behavior unchanged.

verify：
- Empty raw pullbacks plus forged full-fill CSV evidence fails.
- Raw pullback/ledger quantity, side, role, fill identity or attempt mismatch fails.
- Correct oid plus unrelated cloid fails in producer and acceptance.
- Exact all-token reference match passes.
- Abbreviated value/boolean/path flags fail parser or acceptance.
- Python executable, watcher script or output-dir mismatch fails even when preflight and runner are forged together.
- Runtime provenance contains the exact canonical watcher command used by the child.
- Fresh real manager-watcher zero-fill producer output still passes complete same-window acceptance.
- Existing T005 cancel proof, T006/T007 raw-order/terminal/side-gate and orchestrator provenance/timeout regressions pass.
- Modified Python `py_compile`, CLI `--help`, `git diff --check`.
- Full `python -m pytest examples/hyperliquid -q`.

stop conditions：
- Any forged fill CSV can pass without matching raw pullback evidence.
- Any conflicting fill reference can be accepted.
- Any abbreviated or unbound command can change runtime behavior while acceptance passes.
- Any strategy/risk/activation/live boundary changes.

done：
- All three T007 QA findings are closed.
- Real producer zero-fill output and independently reconstructed filled fixtures pass the exact acceptance contract.
- Independent QA accepts the offline repair.

blockers：
- No live task may start before this task is QA accepted.

commit：
- 待填写

提交信息：
- 待填写
