```md
执行线程：
- 业务线程-research

任务ID：
- 0617T003

标题：
- Hyperliquid tiny-live final live-capable preflight / operator packet on awsserver1

简短描述：
- 在 `0617T002` QA 通过后，创建并执行 `0616T008` 前的最终 live-capable preflight/operator packet，显式绑定 `awsserver1` 新路径 `/home/admin/hftbacktest-cross-exchange`、远端 `/usr/bin/python3`、`scp` artifact pullback 和已批准 tiny-live caps。本任务仍是 preflight/operator readiness，不真实下单、不撤单、不改单、不启动 live bot。

状态：
- 待执行

执行顺序：
- 当前唯一任务

前置任务：
- `0617T002` QA 必须为 `已通过`。

规则更新提醒：
- 请先阅读 `.workflow/workflow-kit/workflow-manual.md`、`.workflow/workflow-kit/task-dispatch-template.md`、`.workflow/workflow-kit/thread-report-template.md`、`.workflow/workflow-kit/qa-acceptance-template.md`、`task_plan.md`、`progress.md`、`findings.md`、`docs/hyperliquid_tiny_live_0616T006_T008_auto_loop.md` 后再执行本任务。
- 本任务是最终 live-capable preflight/operator readiness，不是 `0616T008` live execution。

按线程规则执行：
- `AGENTS.md`
- `docs/hyperliquid_tiny_live_0616T006_T008_auto_loop.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0617T003.md`
- `.workflow/reports/0617T003-business.md`
- `docs/hyperliquid_tiny_live_0616T006_T008_auto_loop.md`
- `examples/hyperliquid/**`
- `local_live_analysis/hyperliquid_tiny_live_final_live_capable_preflight_0617T003/**`
- `task_plan.md`
- `progress.md`
- `findings.md`

action：
- Consume accepted `0616T006`, `0617T001`, and `0617T002` facts.
- Bind the final preflight/operator packet to:
  - remote host: `awsserver1`
  - remote repo path: `/home/admin/hftbacktest-cross-exchange`
  - remote branch/commit baseline: `cross-exchange:7642b16`
  - remote selected Python: `/usr/bin/python3`
  - artifact pullback method: `scp`
- Preserve the existing Binance maker route `/home/admin/hft_live/hftbacktest`; do not modify, clean, switch, or reuse it.
- Materialize the approved `0616T008` caps into an operator packet as approved-but-not-executed fields:
  - symbol: `BTC`
  - max order size: `0.01 BTC`
  - max order notional: `700 USDC`
  - max position: `0.04 BTC`
  - max position notional: `2800 USDC`
  - max notional: `3000 USDC`
  - max loss: `30 USDC`
  - duration: `10 minutes`
  - host / machine: `awsserver1`
  - account scope: Hyperliquid account configured on `awsserver1`
  - maker-only / post-only: `true`
  - real orders allowed: `true` only inside a later separately dispatched `0616T008`
- Create remote preflight artifacts that contain no secrets and no private/account/order data.
- Pull artifacts back locally with `scp` and validate them locally.
- Fail closed on host mismatch, path mismatch, branch mismatch, dirty worktree, missing Python, cap mismatch, missing post-only/maker-only requirement, missing shutdown/cancel-all evidence placeholder, missing pullback/checksum, any private/order/account/live claim, or any attempt to place/cancel/amend orders.

acceptance criteria：
- The final operator packet explicitly names `/home/admin/hftbacktest-cross-exchange`, `/usr/bin/python3`, and `scp` pullback.
- The final operator packet preserves all approved `0616T008` caps exactly and marks them as approved for `0616T008` only, not for this task.
- The final operator packet proves no real order placement, cancellation, amendment, account query, private endpoint call, credential read, signing, nonce, user stream, live bot startup, deployment, promotion, PnL proof, or maker viability proof occurred.
- The remote preflight artifacts and local validation artifacts exist and pass checksum / schema validation.
- The existing Binance maker route remains untouched.
- Final recommendation must be exactly one of:
  - `hyperliquid_tiny_live_final_live_capable_preflight_ready_for_qa`
  - `hyperliquid_tiny_live_final_live_capable_preflight_needs_revision`
  - `hyperliquid_tiny_live_final_live_capable_preflight_blocked`

verify：
- Remote host/path/branch/commit/dirty/Python check.
- Remote no-order dry-run artifact generation.
- `scp` pullback and checksum verification.
- Local JSON validation with `/home/liushuai/workspace/hftbacktest/.conda-envs/hft-py38/bin/python -m json.tool <artifact>.json`.
- Boundary text check for no live/order/private execution in this task.
- `git diff --check`

done：
- 回报 remote path、branch/commit/dirty、Python、pullback method、approved caps snapshot、artifact path、本地验证结果。
- 明确说明没有改动 Binance maker `master` 路径。
- 明确说明没有读取凭证、没有调用私有接口、没有账号查询、没有下单/撤单/改单、没有 live bot。
- 明确说明本任务通过 QA 后，下一步才可以创建 `0616T008` live execution；本任务本身不执行 live。
```
