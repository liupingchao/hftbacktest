```md
执行线程：
- 业务线程-python

任务ID：
- 0518T002

标题：
- Step 5A BBO quote-anchor / post-only design contract

简短描述：
- 为 Step 5 制定 quote-anchor / post-only 设计合同。目标是明确报价锚点应该使用 fast BBO/bookTicker、depth/top5，还是分层组合；同时固定 GTX/post-only、tick rounding、stale quote、latency guard 和 reject path 的设计边界。本任务只做设计，不实现策略。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- `0514T001` QA 已通过
- `0514T003` QA 已通过
- `0514T005` QA 已通过
- `0518T001` QA 已通过

规则更新提醒：
- 本任务是 design-only，不是 implementation。
- 不允许修改 strategy behavior、fair/reservation、quote placement、risk guards 或 live scripts。
- 不允许启动 live、采集样本、运行 replay experiment 或做 promotion 判断。

按线程规则执行：
- .workflow/workflow-kit/thread-report-template.md
- docs/thread-playbook.md
- task_plan.md
- progress.md
- findings.md

是否需要提交代码：
- 需要

提交要求：
- 自查通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

Allowed files：
- `.workflow/tasks/0518T002.md`
- `.workflow/reports/0518T002-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `.workflow/dashboard.html`
- `.workflow/dispatch_suggestions.md`

Allowed read-only inputs：
- `.workflow/reports/0514T001-business.md`
- `.workflow/reports/0514T001-qa.md`
- `.workflow/reports/0514T003-business.md`
- `.workflow/reports/0514T003-qa.md`
- `.workflow/reports/0514T005-business.md`
- `.workflow/reports/0514T005-qa.md`
- `.workflow/reports/0518T001-business.md`
- `.workflow/reports/0518T001-qa.md`
- `docs/hft-share.md`
- `docs/binance_tick_mm.md`
- `local_live_analysis/5-13-day-control-30min/stage4_pricing_research_0514T003/**`
- `local_live_analysis/5-13-day-control-30min/stage5_execution_outcome_labels_0514T005/**`
- `local_live_analysis/5-13-day-control-30min/t009_fixed_sidecar/**`

Forbidden files / areas：
- `examples/binance_tick_mm/*.py`
- strategy behavior, fair/target formulas, configs, risk guards, quote placement behavior
- live scripts, AWS/remote state, live collection
- replay runner behavior or replay generation
- core Rust, connector, py `event_dtype`, standard npz schema
- `local_live_analysis/**` generated outputs beyond reading existing artifacts

Requires separate task：
- Read-only diagnostic runner implementation for Step 5B.
- Any default-off strategy implementation.
- Any quote-anchor / post-only code change.
- Any live or replay experiment.

Fatal Failures：
- 在 design-only 任务里实现策略或 quote placement 代码。
- 把 existing top5 / BBO evidence 写成 production-ready live promotion。
- 把 top5 / L2 proxy 写成 exact queue or exact quote-anchor proof。

action：
- 产出 Step 5A quote-anchor / post-only design contract。
- 必须回答：
  1. quote anchor 应该优先使用 fast BBO/bookTicker、depth BBO、top5 reconstructed BBO，还是分层 fallback。
  2. top5 在 Step 5 中到底是 pricing input、risk input，还是 quote-anchor input。
  3. `bid <= best_bid` / `ask >= best_ask` 的 hard protection 应该以哪个 BBO source 为准。
  4. tick rounding 应该如何避免 crossed / post-only reject risk。
  5. stale quote prevention、latency guard、join-age guard 应该如何进入报价控制。
  6. GTX / post-only reject、API reject、throttle、drop 后的处理路径如何设计。
  7. 哪些结论需要 Step 5B read-only diagnostic 验证后才能进入 implementation。
- 明确输出：
  - `BBO_QUOTE_ANCHOR_POST_ONLY_DESIGN.md`
  - `quote_anchor_decision_matrix.csv`
  - `post_only_protection_contract.md`
  - `step5b_diagnostic_requirements.md`
  - `run_manifest.json`

verify：
- 人工检查 `.workflow/tasks/0518T002.md` 和 `.workflow/reports/0518T002-business.md`
- `python3 .workflow/build_dashboard.py`
- `git diff --check`

done：
- 回报必须明确 quote-anchor 推荐设计。
- 回报必须明确 top5 的角色定位。
- 回报必须明确 post-only / rounding / stale / latency / reject path 设计。
- 回报必须明确 Step 5B 需要验证的指标。
- 回报必须明确未实现策略、未改代码、未启动 live。
- 回报必须写 `commit id` 和提交信息。
```
