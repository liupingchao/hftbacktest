```md
执行线程：
- 业务线程-python

任务ID：
- 0515T001

标题：
- Read-only replay lifecycle mismatch diagnosis implementation

简短描述：
- 基于 `0514T008` 已通过的 planning-only 合同，实现一个只读 replay fill/cancel lifecycle mismatch diagnosis runner，对 `0514T007` 暴露的 replay-only fills、cancel timeline mismatch、terminal-state mismatch、markout observability mismatch 和 strata hot spots 做归因表与摘要。本任务只实现诊断表和摘要，不修改 replay、不新跑 replay sweep、不补采样本、不改策略。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- `0514T008` QA 已通过

规则更新提醒：
- 本任务是 read-only diagnosis implementation。
- 不允许修改 replay fill model / lifecycle state machine / cancel-ack handling。
- 不允许新跑 replay sweep、不允许 live 采集、不允许策略变更。
- 目标是先把 mismatch root-cause evidence 做成可核对的表和 summary，再决定是否需要后续 repair task。

按线程规则执行：
- .workflow/workflow-kit/thread-report-template.md
- docs/thread-playbook.md
- task_plan.md
- progress.md
- findings.md

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

Allowed files：
- `.workflow/tasks/0515T001.md`
- `.workflow/reports/0515T001-business.md`
- `examples/binance_tick_mm/replay_lifecycle_mismatch_diagnosis.py`
- `examples/binance_tick_mm/test_replay_lifecycle_mismatch_diagnosis.py`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `.workflow/dashboard.html`
- `.workflow/dispatch_suggestions.md`
- `docs/qa-acceptance-report.md`

Allowed generated outputs：
- `local_live_analysis/5-13-day-control-30min/stage6c_replay_lifecycle_mismatch_0515T001/**`

Allowed read-only inputs：
- `.workflow/reports/0514T008-business.md`
- `.workflow/reports/0514T008-qa.md`
- `.workflow/reports/0514T007-business.md`
- `.workflow/reports/0514T007-qa.md`
- `local_live_analysis/5-13-day-control-30min/stage6_execution_calibration_0514T007/**`
- `local_live_analysis/5-13-day-control-30min/stage5_execution_outcome_labels_0514T005/**`
- `local_live_analysis/5-13-day-control-30min/audit_live_5-13-day-control-30min.csv`
- `local_live_analysis/5-13-day-control-30min/out/backtest_audit_replay/audit_bt_audit_replay.csv`
- `local_live_analysis/5-13-day-control-30min/alignment_report_audit_replay.json`
- `local_live_analysis/5-13-day-control-30min/maker_acceptance_stage3.json`
- `local_live_analysis/5-13-day-control-30min/t009_fixed_sidecar/**`

Forbidden files / areas：
- replay engine behavior / backtest fill model implementation
- strategy behavior, fair/target formulas, configs, risk guards, quote placement behavior
- live scripts, AWS/remote state, live collection
- new replay generation / Stage 6J sweep / long experiment matrix
- core Rust, connector, py `event_dtype`, standard npz schema

Requires separate task：
- 修改 replay fill model / lifecycle state machine / cancel-ack handling。
- 新跑 replay sweep 或更多 replay experiments。
- 新采集 current-format 样本。
- 策略实现、quote-adjustment promotion、inventory redesign。

Fatal Failures：
- 在本任务里直接修 replay / fill model / strategy。
- 直接把 diagnosis 结论升级成 quote-adjustment promotion 或 live readiness。
- 不区分 submit coverage mismatch 与 lifecycle mismatch。
- 写入既有 Stage 6B / Stage 5 / T009 产物目录，覆盖历史研究结果。

action：
- 实现 read-only CLI runner：
  - 读取 Stage 6B matched submit universe与 live / replay execution rows。
  - 生成 matched submit state-diff tables。
  - 生成 replay-only fill cases 与 live-cancel/replay-fill cases。
  - 重建 cancel-request / cancel-ack / fill / terminal timeline diff。
  - 生成 placement / inventory / latency / horizon 的 mismatch attribution tables。
- 至少输出这些产物：
  - `REPLAY_LIFECYCLE_MISMATCH_DIAGNOSIS_SUMMARY.md`
  - `matched_submit_state_diff.csv`
  - `replay_only_fill_cases.csv`
  - `live_cancel_replay_fill_cases.csv`
  - `cancel_fill_timeline_diff.csv`
  - `terminal_state_transition_diff.csv`
  - `cancel_ack_delay_diff.csv`
  - `state_diff_by_placement.csv`
  - `state_diff_by_inventory.csv`
  - `state_diff_by_latency.csv`
  - `replay_only_fill_by_horizon.csv`
  - `cancel_race_gap_by_bucket.csv`
  - `run_manifest.json`
- 重点回答：
  - replay-only fills 是否主要来自 long-horizon persistence
  - fill-after-cancel 放大是否更像 cancel-ack / terminal timing 问题
  - final-state 偏差是否集中在某些 placement / inventory / latency bucket
  - markout observability mismatch 是否主要是 lifecycle-induced side effect
- 补充 focused tests，覆盖 submit matching reuse、timeline diff aggregation、state-diff attribution 和 required outputs。

verify：
- `python -m pytest examples/binance_tick_mm/test_replay_lifecycle_mismatch_diagnosis.py`
- `python examples/binance_tick_mm/replay_lifecycle_mismatch_diagnosis.py --help`
- `python examples/binance_tick_mm/replay_lifecycle_mismatch_diagnosis.py --run-dir local_live_analysis/5-13-day-control-30min --output-dir local_live_analysis/5-13-day-control-30min/stage6c_replay_lifecycle_mismatch_0515T001`
- 人工检查输出目录包含 required artifacts。
- `python3 .workflow/build_dashboard.py`
- `git diff --check`

done：
- 回报必须明确 replay-only fills / cancel timeline / terminal state / strata hot spots 的主要归因。
- 回报必须明确哪些现象更像 replay repair 问题，哪些还不能解释。
- 回报必须明确本任务未修 replay、未新跑 replay sweep、未补样本、未改策略、未启动 live。
- 回报必须写 `commit id` 和 `提交信息`。
```
