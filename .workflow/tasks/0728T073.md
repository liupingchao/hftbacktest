# 0728T073 EC2-HUNT-CONTROL-PLANE-FAIL-CLOSED-REPAIR

执行线程：
- 总控 / 业务执行线程

任务ID：
- 0728T073

标题：
- 修复 EC2 hunt 上传、清理和构建 provenance 的 fail-closed 语义

简短描述：
- 保留 T072 已可信的 10 台 900 秒数据，不重复全量行情采集。
- 修复 archive upload 状态、对象校验、bucket planned/tag discovery、
  cleanup 删除后核验、失败 clock evidence 和 deterministic build source。
- 用 2 台短 canary 验证修复后的完整控制路径。

状态：
- 未通过

执行顺序：
- 当前唯一正式任务

前置任务：
- `0728T072`（未通过；数据层通过，控制面未通过）

是否需要提交代码：
- 需要实现并加入 Git 跟踪；本任务不要求创建 commit。

是否进行QA验收：
- 是

files：
- `latency-probe/ec2_hunt_remote.sh`
- `latency-probe/ec2_hunt_orchestrator.py`
- `latency-probe/ec2_hunt_build.py`
- `latency-probe/test_ec2_hunt.py`
- `docs/evidence/ec2_latency_hunt_20260728/`
- `docs/evidence/ec2_latency_hunt_20260728/build_source.tar.gz`
- `.workflow/tasks/0728T073.md`
- `.workflow/reports/0728T073-business.md`
- `.workflow/reports/0728T073-qa.md`
- `docs/qa-acceptance-report.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

action：
- 远端显式检查 archive PUT，输出 archive SHA/size；失败必须使 SSM
  command 非零。
- orchestrator 对 S3 object 做存在、size 和下载后 SHA 校验。
- bucket 在创建前写 planned state，创建后立即写 task/run tags；cleanup
  通过 state 和 bucket tags 双轨发现。
- cleanup 尝试所有资源，任何删除失败或删除后仍存在都写
  `cleanup_failure` 并抛错；只有复核为零才写 verified cleanup。
- setup gate 无论通过失败都把 clock JSON 输出到 SSM receipt。
- 构建 source archive 改为 deterministic，并把 exact bytes、Cargo.lock
  和 Cargo.toml hashes 加入 tracked evidence。
- 运行 2 台 60 秒 canary，验证新的 upload/object/cleanup receipts。

verify：
- 故障测试覆盖 upload failure、delete failure、signal/finally、
  create-before-record 和 setup clock receipt。
- deterministic build archive 可独立重建同一 binary SHA。
- canary 2/2 Success，对象 size/SHA 校验通过。
- cleanup state 标记 `verified=true`，AWS 零遗留。
- 保留 T072 `10/10` 数据层 acceptance，不重新宣称未测控制路径。

done：
- T071/T072 的数据和控制面缺陷均关闭。
- 目标2可由 tracked code 复现、失败、恢复和审计。

blockers：
- Hyperliquid feed-network 继续降级。
- 仍不证明订单 ACK/fill/PnL。
