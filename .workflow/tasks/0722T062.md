# 0722T062 BASIS-REGRESSION-PRODUCTION-SHADOW

执行线程：
- 总控 / 业务执行线程

任务ID：
- 0722T062

标题：
- Integrate the accepted basis regression contract into the shared kernel and public shadow

简短描述：
- 以 default-off 可选分支将 T061 frozen basis contract 接入 shared kernel。
- 在 accepted public package 上运行 production-equivalent no-submit shadow，
  并原样传播 T061 的全部 warnings。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0722T061`（已通过）

规则更新提醒：
- public/offline/no-submit。
- 不修改 watcher live 默认配置，不读取 credentials，不调用 private/order/
  cancel endpoint。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交并进入独立 QA。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `examples/hyperliquid/cross_exchange_shared_signal_kernel.py`
- `examples/hyperliquid/test_cross_exchange_shared_signal_kernel.py`
- `examples/hyperliquid/cross_exchange_basis_regression_production_shadow.py`
- `examples/hyperliquid/test_cross_exchange_basis_regression_production_shadow.py`
- `local_live_analysis/cross_exchange_basis_regression_production_shadow_0722T062/`
- workflow/report/tracking files

action：
- 新增严格 validator/hash 和 basis regression signal evaluation。
- `evaluate_shared_kernel()` 增加显式 optional basis contract 参数；默认
  `None`，旧 signal path 完全不变。
- Basis mode 使用 contract frozen normalization、intercept/coefficient，
  alpha adjustment 单位直接为 forecast move ticks。
- 新 public shadow runner 必须调用同一个 shared kernel entry point。
- 输出 contract hash、forecast/fair/reservation/quote、would-submit、
  counterfactual markout、per-window/stability 和 warning propagation。

verify：
- Legacy kernel parity tests。
- Basis contract valid/missing/wrong-hash/wrong-feature fail-closed tests。
- Public shadow positive/negative/deterministic/boundary tests。
- Official artifact deterministic rebuild、contract hash、no-submit gates。
- Focused/full Hyperliquid、py_compile、diff checks。

done：
- Shadow recommendation 明确区分 mechanism/counterfactual evidence 与新 OOS
  economics。
- T061 六项 warning 全部可审计。
- 不授权 live、promotion 或 default-on。

blockers：
- 无。

提交信息：
- `Run basis regression production shadow`
