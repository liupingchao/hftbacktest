```md
执行线程：
- 测试线程

任务ID：
- 0512T001

标题：
- 对齐 Stage 6J replay 与 live adverse-selection source-path

简短描述：
- 基于 `0511T004` 诊断，查清为什么 live/current-format 风险诊断能看到 adverse-selection / inventory-reducing cancel race candidate，而 Stage 6J replay summary 中 baseline 和 adverse timing candidates 的 `guard_candidate_adverse_selection_count` 均为 `0`。本任务只做 source-path 对齐诊断和报告，不改策略规则，不启动 live。

状态：
- 待验收

执行顺序：
- `0511T004` 通过后执行；必须早于 `0512T002`

前置任务：
- 0511T004

规则更新提醒：
- 本任务目标是修复/解释验收观测能力，不是优化策略。
- 先比较 live audit 与 replay audit 的 lifecycle/source-path 输入差异，再判断 replay 是否需要 instrumentation。
- 不允许把 Stage 6J replay 的 `guard_candidate_adverse_selection_count=0` 解释成真实 adverse-selection 风险不存在。

按线程规则执行：
- .workflow/workflow-kit/thread-report-template.md
- docs/thread-playbook.md
- docs/stage6i-cancel-requested-fill-risk-plan.md
- docs/5-8-future-plan.md

是否需要提交代码：
- 不需要，除非诊断发现必须补一个只读分析脚本；若补脚本则需要更新本任务合同并说明。

提交要求：
- 回报必须写 `commit：无`，除非本任务合同被总控更新为允许脚本代码。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- .workflow/reports/0511T004-business.md
- local_live_analysis/stage6j_cross_sample_0511T002/stage6j_replay_summary.csv
- local_live_analysis/stage6j_cross_sample_0511T002/*/*/audit_bt_stage6j.csv
- local_live_analysis/stage6j_regime_control_cancel_fill_risk/stage6i_cancel_fill_summary.csv
- local_live_analysis/stage6j_regime_control_cancel_fill_risk/*/cancel_fill_events.csv
- examples/binance_tick_mm/analyze_cancel_fill_risk.py
- examples/binance_tick_mm/stage6j_replay.py
- findings.md
- progress.md

Fatal Failures：
- 把 replay adverse-selection count 为 0 当成真实风险消失。
- 不比较 live audit 与 replay audit 的 lifecycle 字段，直接猜测原因。
- 在观测能力未对齐前继续设计/实现 adverse timing rule。
- 改策略代码或启动 live。

MUST NOT：
- 不改 `strategy_core.py`、`live_tick_mm.py`、`backtest_tick_mm.py` 策略逻辑。
- 不启动真实 live，不连接交易所，不改 AWS 状态。
- 不覆盖 `0511T002` / `0511T004` 既有证据。
- 不使用 future markout 作为 live 决策输入；markout 只能作为诊断/验收指标。

核心问题：
- 为什么 live/current-format risk 里 `guard_candidate_adverse_selection_count` 是正数：
  - `5-10-day-control-1h-06=14`
  - `5-9-small=11`
  - `5-9-noon=13`
  - `5-8-stage3-15m-livetest-v4=25`
- 但 Stage 6J replay summary 中 baseline 和 adverse timing candidates 的 `guard_candidate_adverse_selection_count=0`？

action：
- 构建对比表：`run_id | live adverse count | replay adverse count | live cancel-fill | replay cancel-fill | mismatch reason`。
- 对每个 run 比较 live audit 与 Stage 6J baseline replay audit 的 cancel-request/fill lifecycle 字段：`cancel_requested`、`cancel_request_ts`、`fill_ts`、`fill_after_cancel_request`、`position_before/after` 可推导字段、same-side readd 标记来源。
- 抽样对比 live `cancel_fill_events.csv` 中 inventory-reducing cancel race 事件，在 replay audit 里是否存在对应 order/fill/lifecycle 行。
- 检查 `analyze_cancel_fill_risk.py` 对 live audit 和 replay audit 的输入假设是否一致，特别是 `_source_path(...)` 和 `_guard_candidate_path(...)`。
- 检查 `stage6j_replay.py` 是否在 replay 输出中丢失、重写或弱化了 source-path 所需 lifecycle 信息。
- 输出 `.workflow/reports/0512T001-business.md`，明确根因分类：数据缺失、fill model 差异、字段语义不一致、source-path 分类不适配 replay，或证据不足。
- 更新 `findings.md`，只写可复现的 durable finding。

verify：
- `python3 .workflow/build_dashboard.py`
- 报告中必须包含 live vs replay source-path 对比表。
- 报告中必须包含至少 1 个 adverse-selection live event 的 replay 对应关系检查，或说明为什么无法建立对应关系。
- 报告中必须明确 Stage 6J replay 当前是否能作为 adverse-selection timing rule 的晋级门禁。

done：
- 回报必须回答：Stage 6J replay 为什么没有表达 live adverse-selection source-path。
- 回报必须说明下一步是补 replay instrumentation、调整 source-path 分类、还是允许 `0512T002` 设计替代验收指标。
- 回报必须明确 no live micro test。
- 回报必须明确是否允许启动 `0512T002`。默认只有在观测合同清楚后才允许。
```
