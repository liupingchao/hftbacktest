# 任务派发

执行线程：
- 业务线程-python/public-data-infra

任务ID：
- 0807T001

标题：
- SKHYNIX-4H-CONTINUOUS-PUBLIC-COLLECTION-AND-AMD-TRANSFER

简短描述：
- 在 `c6in-winner` 启动一次连续 `14400s` 的 SKHYNIX
  Binance/Hyperliquid public market-data 采集。
- 采集成功后自动传输完整 raw campaign 到 `amdserver`，并执行逐文件
  SHA256/大小对账。

状态：
- 待验收

执行顺序：
- 可与 `0806T001` 的只读 QA 验收并行；当前唯一执行任务。

前置任务：
- `0805T001` 已通过，提供 bounded recovered core-L2 reconnect 门禁。
- `0806T001` 已完成采集和 AMD 传输，等待 QA。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `AGENTS.md`

是否需要提交代码：
- 不需要

提交要求：
- 当前工作区包含已验收但未提交的前置工作，不创建混合提交。
- 回报使用 `commit：无`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 采集和传输完成后，复核 campaign/run status、runtime source SHA、
  reconnect recovery、两端 inventory 及 AMD 原子发布结果。

files：
- c6in runtime：`/home/admin/0807T001-hftbacktest`
- c6in raw campaign：`/home/admin/0807T001_skhynix_4h_continuous`
- AMD raw campaign：
  `/home/molly/project/hftbacktest/local_live_analysis/0807T001_skhynix_4h_continuous`
- `.workflow/runners/0807T001_transfer_to_amdserver.sh`
- `.workflow/runners/com.hftbacktest.0807T001.transfer.plist`
- workflow task/progress/findings/report files

action：
- 使用 source-pinned runtime 和单一连续 `14400s` segment。
- Hyperliquid 使用 research-max 多轨道；Binance 采集
  trade、depth@0ms、bookTicker 和 depth snapshot。
- 启用 recovered core-L2 reconnect 合同：单次不超过 `15s`、累计不
  超过 `30s`，且必须闭合 transport/re-ACK/resume/recovery snapshot
  证据。
- 云端仅采集，不运行 timeline、R0、R1、basis 或其他研究计算。
- one-shot LaunchAgent 在持久化终态门禁通过后传输到 AMD 临时目录，
  对账完整 inventory 后原子发布。

verify：
- 本地 focused pytest、远端 py_compile/CLI/source SHA。
- systemd active、MainPID、heartbeat、双 venue child process 和 raw
  数据增长。
- 终态要求 complete run status、passing collection-only manifest、
  abort absent。
- c6in 与 AMD relative path、size、SHA256 inventory 完全一致。

done：
- 4H public collection 已完成，systemd `Result=success`、child return
  code `0`、collection-only manifest 与 run status 均通过。
- LaunchAgent 因一次 SSM banner timeout 退出；恢复流程使用分片传输、
  逐文件 inventory 对账和临时目录 rename 完成原子发布。
- c6in、本机和 AMD 的 39 文件相对路径、大小和 SHA256 完全一致，
  inventory SHA256 为
  `a30654f32cea375c49e4b26e39ed3c0de436e6d1f08de959601db749f262a003`。

hard boundary：
- 仅 public market data；不访问 credential/private/account/order/cancel
  endpoint，不下单。
- 用户已在 2026-08-07 明确授权立即开始本次 4H 采集。
- 不覆盖历史 campaign 或 AMD 数据目录。
- 不形成 signal、arbitrage、exact fill、maker PnL 或 live promotion
  结论。
