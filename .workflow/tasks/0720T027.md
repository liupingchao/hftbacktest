# 0720T027 SUBMIT-REJECT-TERMINAL-FILL-FEEDBACK-DETERMINISM-REPAIR

执行线程：
- 总控 auto-loop / 业务执行线程

任务ID：
- 0720T027

标题：
- Repair submit-response rejected terminal evidence, canonical fill-feedback rows and deterministic acceptance

简短描述：
- Close the three T026 QA findings without changing raw T026 evidence or starting another live window。
- Treat one exact synchronous submit-response rejection as an authoritative request-bound terminal path，while preserving fail-closed behavior for ambiguous responses。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- `0720T026`（未通过；QA findings accepted as repair input）

规则更新提醒：
- This task is offline-only。
- T026 raw artifacts and remote checksums are immutable。
- No live、private/account、order/cancel、network、remote or service action is authorized or needed。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `.workflow/reports/0720T026-qa.md`
- `docs/superpowers/plans/2026-07-17-principal-alignment-p0-p3.md`

是否需要提交代码：
- 需要

提交要求：
- 先提交 implementation。
- 再提交 task/business/tracker report。
- 独立 QA 单独验收。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

source：
- Branch: `cross-exchange`
- Dispatch base: `9932c00`
- Immutable evidence source commit: `40dc56a3225df4afb0f2185873c91f17b578f550`
- Immutable evidence root: `local_live_analysis/principal_alignment_single_level_two_sided_0720T026/`

files：
- Modify: `examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- Modify: `examples/hyperliquid/hyperliquid_tiny_live_m2_fill_window.py`
- Modify: `examples/hyperliquid/cross_exchange_t024_same_window_acceptance.py`
- Modify: `examples/hyperliquid/cross_exchange_online_estimators.py`
- Test: `examples/hyperliquid/test_hyperliquid_tiny_live_m2_event_driven_watcher.py`
- Test: `examples/hyperliquid/test_hyperliquid_tiny_live_m2_fill_attribution.py`
- Test: `examples/hyperliquid/test_cross_exchange_t024_same_window_acceptance.py`
- Test: `examples/hyperliquid/test_cross_exchange_fill_feedback.py`
- Workflow: this task, business report, QA report, fixed QA document, `task_plan.md`, `progress.md`, `findings.md`

action：
- Define one independently rebuildable submit-terminal response contract keyed by task/window/attempt/side/intent cloid token。
- Accept `rejected` only when one submitted attempt has one synchronous outer-success response with one exact non-empty exchange error status and manager action/state/query classification all agree on rejected。
- Preserve rejected attempts in the submitted ledger and per-attempt terminal matrix，but exclude them from cancel/history and ordinary no-fill exposure obligations。
- Keep exception、outer failure、empty/non-string error、multiple statuses、resting/rejected conflict、attempt/side/key/cloid mismatch and forged manager classification fail closed。
- Extend producer and independent acceptance to support mutually exclusive terminal paths：
  - exact submit-response rejected；
  - resting/partial plus authoritative cancel/query；
  - complete reference-bound fill；
  - partial fill plus remaining-quantity terminal reconciliation。
- Make post-only reject counts derive from independently validated raw responses，not only optional retry rows。
- In fill-feedback normalization，prefer exactly one submitted lifecycle row per attempt key；earlier no-submit candidate evaluations are decision evidence，not conflicting lifecycle duplicates。
- Multiple conflicting submitted lifecycle rows remain quarantined。
- Make every acceptance comparison/output byte-deterministic across Python hash seeds；never serialize an unordered set directly。
- Add an exact-source/task bridge only if required to replay immutable T026 producer v4 artifacts；the bridge must be pinned to `0720T026` plus source `40dc56...` and must independently reconstruct the terminal fact from raw response evidence。

verify：
- Focused tests cover positive mixed `buy rejected / sell resting+cancel` and two-resting cases。
- Hostile tests cover malformed/multiple/empty error statuses，outer failure，attempt/side/key/cloid mismatch，forged manager action，resting/rejected conflict and unbound rejection。
- Producer and acceptance independently derive the same per-attempt terminal result and reject count。
- Rejected lifecycle is terminal but excluded from no-fill denominator/resting exposure；sell resting lifecycle remains censored or included according to existing evidence。
- Fill-feedback selects the T026 submitted buy row，records `submitted=true/rejected=true`，and no longer emits candidate-row duplicate conflicts。
- Conflicting duplicate submitted rows still quarantine。
- Acceptance output is byte-identical under at least two distinct `PYTHONHASHSEED` values。
- Exact T026 replay exits `0` only if all other provenance/config/lifecycle/economics/optimism gates pass；otherwise it retains the first unrelated blocker。
- T016/T022 immutable replays remain blocked and are not upgraded by the T026-specific bridge。
- Focused and full Hyperliquid tests pass。
- No input evidence file changes；offline boundary records network/private/order/cancel/remote/new-live as false。

stop conditions：
- Any repair infers rejection from account-wide absence、unknown status or generic cancel failure。
- Any malformed or multi-status response becomes accepted。
- Any bridge is not pinned to exact T026 task/source。
- T016/T022 evidence is upgraded。
- Rejected attempts disappear from submitted-attempt evidence。
- Rejected attempts enter ordinary no-fill/fill-rate denominator。
- Conflicting submitted lifecycle rows stop quarantining。
- Acceptance bytes still vary by hash seed。
- Any live/private/network/remote action occurs。

done：
- T026 raw buy rejection and sell cancel are independently reconstructed as two complete terminal attempts without editing the evidence root。
- Producer/future artifacts support the same terminal contract。
- Fill-feedback and deterministic acceptance findings are closed。
- Independent QA decides whether Principal Task 7 is accepted and Task 8 may unlock。

blockers：
- 无；T027 独立 QA 已通过。Task 8 可按独立 formal task 解锁，实际 dynamic spread、fill-feedback、inventory skew 和 multi-level 激活仍须遵循各自 gate。

commit：
- `b8e008afc90a25ffb155f09bb5d77aa90cdbb4c4`
- report `326b30fd1610f24b921ae5e02678853104220226`

提交信息：
- `Repair submit rejection terminal evidence`
- `Report T027 rejected terminal repair`
