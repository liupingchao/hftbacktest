```md
执行线程：
- 业务线程-docs

任务ID：
- 0519T003

标题：
- Step 7 inventory and execution model redesign contract

简短描述：
- 基于 `0519T002` 已通过的 Step 6 closure boundary，planning-only 收口 Step 7：定义 one-way maker inventory / execution model 的目标、候选机制、禁区、验收标准和后续实现边界。本任务只做设计合同，不改策略、不跑实验、不启动 live。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- `0519T002` QA 已通过

规则更新提醒：
- 本任务是 planning-only design contract，不允许改代码、不允许跑新实验、不允许启动 live。

按线程规则执行：
- .workflow/workflow-kit/thread-report-template.md
- docs/thread-playbook.md
- task_plan.md
- progress.md
- findings.md

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

Allowed files：
- `.workflow/tasks/0519T003.md`
- `.workflow/reports/0519T003-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `.workflow/dashboard.html`
- `.workflow/dispatch_suggestions.md`

Allowed read-only inputs：
- `.workflow/reports/0519T002-qa.md`
- `.workflow/reports/0519T001-qa.md`
- `.workflow/reports/0514T005-qa.md`
- `.workflow/reports/0514T006-qa.md`
- `.workflow/reports/0518T004-qa.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

Forbidden files / areas：
- Any strategy, replay, runner, live, connector, schema, or generated sample implementation file.
- Any live scripts, AWS/remote state, live collection, replay sweep, or promotion action.
- Any default-on strategy behavior.

Requires separate task：
- Any Step 7 inventory / execution implementation.
- Any Step 7 read-only replay diagnostic or experiment.
- Any Step 8 quote-update mechanics design or implementation.
- Any Step 9 replay experiment.
- Any live sample collection or live micro test.

Fatal Failures：
- 修改策略代码、配置默认行为、replay 行为、live 行为或 schema。
- 将 Step 6 single-sample lifecycle closure 解释为 live readiness 或 quote-adjustment promotion。
- 将 queue/priority、opportunity cost、realized PnL decomposition 写成 exact proof。
- 将 `4948` residual 升级为当前 repair authorization。

action：
- 读取 `0519T002` QA 结果和 Step 7 计划边界。
- 制定 Step 7 inventory / execution model redesign contract，必须覆盖：
  - inventory target and hard boundary
  - position skew and quote-side suppression semantics
  - zero-crossing / inventory-cycle reset semantics
  - AS-style dynamic spread and dynamic order amount candidate boundary
  - TTL / triple-barrier exit handling boundary
  - decision-time-visible inputs only
  - audit / diagnostic fields required before implementation
  - replay / live evidence gates required before any default-off experiment or live decision
- 必须明确后续是否进入 Step 8，或是否还需要 Step 7 implementation / diagnostic task。

verify：
- 文档一致性检查：确认 `task_plan.md`、`progress.md`、`findings.md` 对 Step 7 design contract 不冲突。
- `python3 .workflow/build_dashboard.py`
- `git diff --check`

done：
- 回报必须明确 Step 7 是否已完成 planning/design。
- 回报必须明确下一步建议。
- 回报必须明确不允许的事项：no code, no experiment, no live, no default-on, no promotion.
- 回报必须写 `commit id` 和提交信息。
```
