```md
执行线程：
- 测试线程

任务ID：
- 0527T001

标题：
- Replay audit cancel_ack bloat planning/diagnosis for 0526T008

简短描述：
- 基于 `0526T008` 的 `5-26-active-minmove-control-30min-b` 后处理 replay audit 膨胀问题，做一个很小的 planning/diagnosis-only 任务。目标是定位重复 `cancel_ack` lifecycle rows 的来源、判断最小修复边界，并产出后续 repair / implementation 任务合同。不得做代码修复、不得重跑 live、不得改变 Stage 6 结论。

状态：
- 待验收

执行顺序：
- 当前可作为下一步诊断任务派发；本任务只做 planning/diagnosis，不做 repair。

前置任务：
- `0526T008` QA 已通过。
- `0528T001` QA 已通过；与本任务独立。

规则更新提醒：
- 请先阅读规则文档后再执行本任务。
- 本任务只研究 `0526T008` 暴露的 replay audit bloat；不得扩展到策略、candidate、live、promotion 或 generalized replay lifecycle repair。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `docs/thread-playbook.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 只提交 planning/diagnosis report、workflow/task/progress/findings/dashboard 更新。
- 如有临时诊断输出，只能放在任务限定输出目录；不得提交大 CSV 或 full replay audit。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

Allowed files：
- `.workflow/tasks/0527T001.md`
- `.workflow/reports/0527T001-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `.workflow/dashboard.html`
- `.workflow/dispatch_suggestions.md`
- `local_live_analysis/stage6_audit_bloat_diagnosis_0527T001/**`

Allowed read-only inputs：
- `.workflow/tasks/0526T008.md`
- `.workflow/reports/0526T008-business.md`
- `.workflow/reports/0526T008-qa.md`
- `local_live_analysis/5-26-active-minmove-control-30min-b/**`
- `local_live_analysis/archive/5-26-active-minmove-control-30min-b.tar.gz.sha256`

Forbidden files / areas：
- Strategy behavior, live/backtest quote placement behavior, production defaults, live configs, fair/target formula, risk/position logic.
- Replay lifecycle repair implementation.
- Stage 6 calibration logic implementation.
- Core/connector APIs, standard npz schema, converter behavior.
- Any live/tiny-live run, default-on behavior, parameter search, guard relaxation, or promotion claim.
- Modifying or deleting preserved full replay audit artifacts.

Fatal Failures：
- Editing strategy behavior or replay lifecycle logic.
- Replacing the documented `0526T008` Stage 6 result.
- Treating lifecycle-min workaround as an accepted long-term fix without diagnosing the source.
- Loading or writing the 21GB full replay audit in a way that risks another memory-killed run when lightweight sampling/streaming would answer the planning question.
- Claiming the diagnosis authorizes implementation, live, default-on, promotion, or broader queue/touch repair.

Input sample：
- `0526T008`
- run id: `5-26-active-minmove-control-30min-b`
- issue observed during replay/post-processing, not during live collection.

Current known facts：
- The live 30min collection itself was not 21GB; bloat happened in post-processing generated replay audit CSV.
- Preserved full replay audit:
  - `local_live_analysis/5-26-active-minmove-control-30min-b/out/backtest_audit_replay/audit_bt_audit_replay.full.csv`
  - about `21GB`
  - about `33,293,775` rows
  - about `32,893,719` `cancel_ack` rows
- Same canceled orders appear to emit `cancel_ack` repeatedly across later `strategy_seq` values.
- Full-event compare and Stage 6 over the full replay audit hit memory pressure / exit `137`.
- `0526T008` completed by preserving the full audit and using a lifecycle-minimized canonical replay audit for Stage 6:
  - canonical Stage 6 input: `out/backtest_audit_replay/audit_bt_audit_replay.csv`
  - lifecycle-min rows about `401,642`
  - retained decision, submit, fill, cancel_sent, first cancel_ack per order, expired, and required lifecycle facts.

Diagnosis goals：
1. Determine whether repeated `cancel_ack` rows originate from replay audit writer/export behavior, replay lifecycle state emission, or Stage 6 input assumptions.
2. Identify the minimal repeat key(s), for example order id, event type, state transition timestamp, cancel request timestamp, cancel ack timestamp, strategy sequence, or lifecycle state tuple.
3. Check whether duplicates carry any unique information needed by Stage 6 or are repeated state snapshots that can be safely compacted.
4. Determine which downstream consumers need full forensic audit versus compact lifecycle audit.
5. Recommend the smallest follow-up implementation boundary:
   - repair replay audit output writer,
   - or add Stage 6 streaming/filter/dedup input adapter,
   - or formalize two outputs: full forensic audit plus compact lifecycle audit.

Required diagnosis outputs：
- `.workflow/reports/0527T001-business.md`
- Optional local output directory:
  - `local_live_analysis/stage6_audit_bloat_diagnosis_0527T001/`
- If output directory is used, keep artifacts small and diagnostic-only, for example:
  - `run_manifest.json`
  - `cancel_ack_bloat_summary.csv`
  - `duplicate_key_samples.csv`
  - `consumer_input_contract.md`
  - `repair_recommendation.md`

Required questions to answer：
1. Is this mainly an audit export repetition problem, replay lifecycle state repetition problem, or Stage 6 input-scaling problem?
2. What exact key best defines duplicate `cancel_ack` rows?
3. Are repeated rows semantically redundant for Stage 6 labels?
4. Should the next task repair the replay audit writer, Stage 6 input layer, or both through a formal compact lifecycle artifact?
5. What is the narrow acceptance criterion for a later implementation task?

Non-goals：
- No code repair.
- No live run.
- No replay lifecycle semantic repair.
- No Stage 6 methodology change beyond diagnosing input shape.
- No strategy/candidate/guard/default/promotion change.
- No parameter search.
- No attempt to re-QA or reinterpret `0526T008` collection validity.

verify：
- If only document diagnosis is produced:
  - `python3 .workflow/build_dashboard.py`
  - `git diff --check`
- If small diagnostic artifacts/scripts are used, report exact commands and ensure they do not require loading the full 21GB CSV into memory.

done：
- Business report is written and ends in `待验收`.
- Report answers all required questions above.
- Report recommends a concrete next implementation boundary or explicitly says more diagnosis is needed.
- `task_plan.md`, `progress.md`, and `findings.md` reflect that `0527T001` is now a planning/diagnosis-only task rather than a placeholder.
- No strategy, live, replay lifecycle repair, Stage 6 implementation, or promotion-related files are modified.
```
