```md
执行线程：
- 待定

任务ID：
- 0527T001

标题：
- Placeholder: replay audit cancel_ack bloat repair or Stage 6 scalable input layer

简短描述：
- 占位任务。后续需要单独修复 `audit_replay` 输出或 Stage 6 输入层，避免 replay audit 因重复 `cancel_ack` lifecycle rows 膨胀到不可处理。本任务当前只记录问题、风险和后续待细化方向，不启动实现。

状态：
- 待执行

执行顺序：
- 待后续总控细化后派发；当前不是执行任务。

前置任务：
- `0526T008` 业务线程结果待 QA。

规则更新提醒：
- 请先阅读规则文档后再执行本任务。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 待定

提交要求：
- 当前占位任务不要求代码提交。
- 后续细化为 repair / implementation 任务时必须明确提交要求、允许文件、输入数据和验收标准。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

当前已知问题：
- `0526T008` 的 30min live 采集本身不是 21GB；异常膨胀发生在后处理生成的 audit replay CSV。
- `5-26-active-minmove-control-30min-b/out/backtest_audit_replay/audit_bt_audit_replay.full.csv` 约 `21GB`。
- 该 full replay audit 共有约 `33293775` 行，其中 `cancel_ack` 约 `32893719` 行，占绝大多数。
- 抽样显示同一个已取消订单会在后续多个 `strategy_seq` 上重复写出 `cancel_ack` 行；这更像 replay audit 导出层重复落盘生命周期状态，而不是实际产生了数千万次新的 cancel ack。
- 直接对 21GB full replay audit 跑 full-event `compare_audit.py` 会被内存压力杀掉。
- 直接对 21GB full replay audit 跑 `execution_outcome_calibration.py` / Stage 6 也会 exit `137`。
- `0526T008` 为完成验收，临时保留原始 full 文件并生成 lifecycle-min replay audit：
  - full 原件：`audit_bt_audit_replay.full.csv`
  - Stage 6 canonical 输入：`audit_bt_audit_replay.csv`
  - lifecycle-min 行数约 `401642`
  - 保留所有 decision、submit、fill、cancel_sent、每个订单首个 cancel_ack、expired 等 Stage 6 标签构建需要的生命周期事实。
- 这个 workaround 是后处理输入规模控制，不是策略行为变化，也不是 replay 决策语义修复。

后续待细化方向：
- 方向 A：修 `audit_replay` 输出层，避免重复 `cancel_ack` lifecycle rows 被写入 full audit CSV。
- 方向 B：修 Stage 6 输入层，使 `execution_outcome_calibration.py` 能流式读取或只读取生命周期必要事件，而不是把 full replay audit 全量读入内存。
- 方向 C：同时保留 full forensic audit 与 compact lifecycle audit 两套产物，但必须明确哪个是验收输入、哪个是追溯证据。
- 后续任务需要先定义输入数据集、最小复现样本、重复行判定 key、是否允许生成 compact audit、以及是否要修改 runner API。

建议后续验收问题：
- 30min / 60min / 180min replay audit 在正常情况下是否不会生成千万级重复 lifecycle rows？
- 同一个 `order_id` 的 cancel ack 应该在 replay audit 中出现多少次，重复 key 如何定义？
- 修复后 full-event compare 是否可在合理内存内完成，或是否明确改为 compact lifecycle compare？
- Stage 6 是否能在不依赖手工 lifecycle-min 文件的情况下完成？
- 修复是否保持 maker acceptance、action/planned/reject/throttle、working lifecycle semantic/blocking gates 不回退？

当前非目标：
- 不在本占位任务中修改代码。
- 不在本占位任务中重跑 live。
- 不在本占位任务中重新解释 `0526T008` 的 QA 结论。
- 不在本占位任务中改变策略、candidate、guard、production defaults 或 promotion 状态。
- 不把 lifecycle-min workaround 直接升级为长期设计；长期方案需要后续任务重新定义。

files：
- `.workflow/tasks/0527T001.md`
- 后续细化时再补充 allowed files。

verify：
- 当前占位任务只要求文档存在且问题记录清楚。
- 后续细化时再定义命令级验证。

done：
- 新任务文件存在，状态为 `待执行`。
- 文件中记录 `0526T008` 暴露的 `cancel_ack` 重复膨胀问题、影响、临时 workaround 和后续待细化方向。
```
