# 0717T006 LIVE-EVIDENCE-INTEGRITY-REPAIR-PLAN

## Recovery Notice

This task file was reconstructed on 2026-07-28. The original task file was not
recoverable from the canonical checkout or searched Git history. The content
below is derived from durable controller summaries and the surviving
`0717T007` through `0717T011` task/QA chain. It is not a verbatim historical
copy and grants no current authorization.

执行线程：
- 总控 / 业务线程-planning

任务ID：
- 0717T006

标题：
- LIVE-EVIDENCE-INTEGRITY-REPAIR-PLAN

简短描述：
- 将 `0717T005` 留下的 live evidence integrity 缺陷拆成严格串行的离线
  repair phases，并定义每阶段边界和验收顺序。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- `0717T005` remote-update review，状态 `未通过`。

是否需要提交代码：
- 不需要运行时代码；需要 plan/workflow 文档。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `docs/cross_exchange_live_evidence_integrity_repair_plan.md`
- `.workflow/tasks/0717T006.md`
- `.workflow/reports/0717T006-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

action：
- 将 immediate repair scope 限定为 window/attempt identity、idempotent
  attempt-bounded fill attribution、watcher termination/timeout 和 terminal
  artifact seal。
- 定义依赖顺序：
  identity -> fill attribution -> process lifecycle -> terminal seal ->
  integrated offline acceptance。
- 记录 controller scope adjustment：当时的 runtime max-loss/max-position
  defaults 不在该 immediate repair route 内。

verify：
- 每个 defect 映射到一个后续 formal task。
- 每个后续 task 都是 offline、fail-closed、单任务串行。
- 不修改 strategy、quote policy、risk limit、live endpoint 或 service。

done：
- Repair plan 可直接派生 `0717T007` 到 `0717T011`。
- Plan acceptance 只允许后续离线 repair task，不允许 live 重试或 promotion。

blockers：
- 无。
