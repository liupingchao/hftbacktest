```md
执行线程：
- 业务线程-python

任务ID：
- 0528T003

标题：
- Hyperliquid live/replay alignment design

简短描述：
- 设计 Hyperliquid live/replay alignment 的第一阶段边界：确认现有 Binance workflow 哪些部分应抽象为 exchange-neutral pipeline，哪些部分应新建 Hyperliquid adapter。本任务只做设计，不修改交易逻辑、不实现 connector、不启动 live。

状态：
- 已通过

执行顺序：
- 可并行；本任务与 Binance `0528T002` compact replay audit repair 独立，必须保持文件和产物边界隔离。

前置任务：
- 无。
- 参考事实：Binance live/replay 对齐链路当前位于 `examples/binance_tick_mm/`；Hyperliquid 已有 raw-to-hftbacktest converter 基础，但没有已接入当前 live/replay workflow 的 live connector。
- 参考优先级：Hyperliquid 官方 GitBook API 文档和官方 `hyperliquid-python-sdk` 是 schema / endpoint / rounding / subscription 行为的一级事实源；本地 XEMM 项目如后续被纳入，只能作为接口接线和工程实践参考，不能替代官方文档或当前 hftbacktest alignment 证据链。

规则更新提醒：
- 请先阅读规则文档后再执行本任务。
- 本任务是 Hyperliquid alignment 设计任务，不是交易逻辑实现、live collection、connector implementation、strategy port、parameter search、tiny-live 或 promotion 任务。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `docs/thread-playbook.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 自测/静态检查通过后提交设计文档、业务报告和必要 workflow/dashboard 更新。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

Allowed files：
- `.workflow/tasks/0528T003.md`
- `.workflow/reports/0528T003-business.md`
- `docs/hyperliquid_live_replay_alignment_design.md`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `.workflow/dashboard.html`
- `.workflow/dispatch_suggestions.md`

Allowed read-only inputs：
- Hyperliquid official docs:
  - `https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api`
  - `https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket`
  - `https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket/subscriptions`
  - `https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/tick-and-lot-size`
- Hyperliquid official Python SDK:
  - `https://github.com/hyperliquid-dex/hyperliquid-python-sdk`
- Optional local reference only:
  - `/home/molly/project/XEMM_CROSS_EXCHANGE_MARKET_MAKING_PACIFICA_HYPERLIQUID/**`
- `.workflow/tasks/0528T002.md`
- `examples/binance_tick_mm/align_live_run.py`
- `examples/binance_tick_mm/deploy/run_live.sh`
- `examples/binance_tick_mm/deploy/preflight_live_run.py`
- `examples/binance_tick_mm/audit_schema.py`
- `examples/binance_tick_mm/pipeline_live_raw.py`
- `examples/binance_tick_mm/binance_top5_provenance.py`
- `examples/binance_tick_mm/maker_acceptance.py`
- `examples/binance_tick_mm/compare_audit.py`
- `examples/binance_tick_mm/live_tick_mm.py`
- `examples/binance_tick_mm/backtest_tick_mm.py`
- `examples/binance_tick_mm/execution_outcome_labels.py`
- `examples/binance_tick_mm/quote_anchor_safety.py`
- `examples/binance_tick_mm/execution_outcome_calibration.py`
- `examples/binance_tick_mm/quote_adjustment_replay.py`
- `examples/binance_tick_mm/candidate_bucket_refinement.py`
- `py-hftbacktest/hftbacktest/data/utils/hyperliquid.py`
- `examples/hyperliquid/**`
- `connector/README.md`
- `connector/Cargo.toml`
- `connector/src/**`
- `collector/**`
- `hftbacktest/src/live/**`
- `py-hftbacktest/src/live.rs`

Forbidden files / areas：
- Strategy behavior, quote placement, fair/target formula, inventory logic, risk guards, production defaults, live configs.
- Binance strategy changes under `examples/binance_tick_mm/` except read-only analysis.
- Hyperliquid connector implementation or order-routing code.
- Core/connector API changes, standard npz schema changes, canonical audit schema changes.
- Any live/tiny-live run, remote deploy, parameter search, default-on behavior, guard relaxation, or promotion claim.

Requires separate task：
- Implementing a Hyperliquid collector or connector.
- Adding Hyperliquid private order lifecycle, order submit/cancel, position, fills, or post-only/ALO handling.
- Changing `align_live_run.py` into an exchange-neutral pipeline.
- Creating Hyperliquid provenance sidecar/join tooling.
- Porting the Binance maker strategy to Hyperliquid.
- Running live market-data collection or live trading on Hyperliquid.

Design questions to answer：
0. What does the official Hyperliquid reference require for a replay-alignment-safe first stage?
   - confirm read-only market-data channels and payload contracts from official docs / SDK before using local examples
   - treat `l2Book` and `trades` as the first-stage raw market-data inputs unless the design identifies a documented reason to add another channel
   - preserve exchange event time, local receive time, channel, coin, raw payload, session/reconnect metadata, and snapshot/bootstrap provenance
   - document REST/info `l2Book` snapshot usage as bootstrap/reconnect/validation support, not as a replacement for raw WebSocket replay evidence
   - document Hyperliquid price/size precision and tick/lot rounding assumptions from official sources before any later private/order lifecycle task
1. Which parts of the Binance workflow are exchange-neutral and should be abstracted?
   - run artifact layout
   - latency extraction from live audit
   - raw-to-npz manifest contract
   - normal replay / audit replay orchestration
   - audit compare / maker acceptance hard gates
   - Stage 5 / 5C / 6 / 9 diagnostic runners
   - archive/checksum/reporting
2. Which parts are Binance-specific and must not be reused blindly?
   - `binancefutures` connector and `binancefuturesum` collector launch
   - Binance raw gzip format
   - `depthUpdate` update-id continuity (`U/u/pu`)
   - snapshot `lastUpdateId` bootstrap
   - `bookTicker` semantics and quote-anchor assumptions
   - `binance_top5_provenance.py`
   - Binance REST/account/order lifecycle assumptions
3. What Hyperliquid adapters are required?
   - raw market-data collector/input contract for `l2Book` and `trades`
   - raw-to-npz adapter using existing `py-hftbacktest/hftbacktest/data/utils/hyperliquid.py`
   - Hyperliquid top-N provenance / decision as-of join sidecar
   - market-view acceptance metrics suitable for Hyperliquid, without Binance `pu/lastUpdateId/bookTicker` gates
   - deployment/preflight contract for read-only market-data alignment
   - later live connector/private lifecycle contract, explicitly out of scope for this task
   - reference-only inventory of future private lifecycle surfaces (`userEvents`, fills, open orders, order status, `Alo`/`Ioc`/`Gtc`, `cloid`, signing, nonce, rate limits) without implementing or changing trading logic
4. What should the first Hyperliquid implementation task be after this design?
   - Prefer a read-only raw sample / converter / sidecar validation path before any live trading connector work.

Acceptance criteria：
- Produce a design document at `docs/hyperliquid_live_replay_alignment_design.md`.
- The design must include:
  - current Binance live/replay pipeline decomposition
  - exchange-neutral boundary proposal
  - Binance-specific code/assumptions that should stay behind a Binance adapter
  - Hyperliquid adapter inventory and sequencing
  - official-reference mapping for `l2Book`, `trades`, REST snapshot/bootstrap usage, precision/rounding, and later private lifecycle surfaces
  - explanation of which XEMM ideas, if any, are safe to use only as engineering reference rather than as schema truth
  - first read-only implementation task recommendation
  - explicit non-goals and risk boundaries
- The design must not require modifying trading behavior or running live.
- The business report must end in `待验收`.

verify：
- No live command.
- No remote deploy.
- No strategy behavior change.
- Static/read-only verification only, at minimum:
  - `python -m py_compile py-hftbacktest/hftbacktest/data/utils/hyperliquid.py`
  - `python examples/binance_tick_mm/align_live_run.py --help`
  - `python examples/binance_tick_mm/maker_acceptance.py --help`
  - `python3 .workflow/build_dashboard.py`
  - `git diff --check`

done：
- `.workflow/reports/0528T003-business.md` is written and ends in `待验收`.
- Report lists the files read, design output path, verification commands, and recommended next task.
- Report explicitly states:
  - no Binance strategy behavior changed
  - no Hyperliquid connector was implemented
  - no live or remote deploy was run
  - no default-on, guard relaxation, parameter search, tiny-live, or promotion claim was made
```
