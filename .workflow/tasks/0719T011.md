# 0719T011 SINGLE-LEVEL-TWO-SIDED-BOUNDED-LIVE

执行线程：
- 总控 auto-loop / 业务执行线程

任务ID：
- 0719T011

标题：
- Run one isolated single-level two-sided Binance-lead Hyperliquid-maker bounded live lifecycle

简短描述：
- Exercise the QA-accepted single-level two-sided exchange-reconciled manager with real Hyperliquid post-only orders under the standing conservative envelope.
- Collect one complete same-window lifecycle/evidence package for Principal Task 7 and Task 12; this task does not activate multi-level or any adaptive controller.

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0719T010`（已通过）

规则更新提醒：
- Standing authorization allows the exact live/private/read/submit/cancel/remote actions in this task without another approval.
- Execution-safety and evidence-integrity gates remain mandatory before the only live window.
- Live is the main data source; shadow/replay is limited to obvious-error prevention, decision reproduction and control-variable comparison.
- No second live window is allowed in this task.

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `.workflow/reports/0719T010-qa.md`
- `docs/superpowers/plans/2026-07-17-principal-alignment-p0-p3.md`

是否需要提交代码：
- 不需要；live source is the exact committed tree below.

提交要求：
- Source commit is fixed before remote preflight.
- Evidence and workflow report are committed after local pullback/acceptance.
- 回报必须带 source `commit id` 和业务报告提交信息。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

source：
- Branch: `cross-exchange`
- Exact source commit: `d8e22c2d9288fef86707d9b26f7791d7d8711c09`
- Remote source: `/home/admin/hftbacktest-cross-exchange-0719T011`
- Source marker: `/home/admin/hftbacktest-cross-exchange-0719T011/source_commit.txt`

envelope：
- Host: `awsserver1`
- Account scope: Hyperliquid account configured by `/home/admin/XEMM_rust_latest/.env`
- Venue/symbol: Hyperliquid `BTC`
- Lead source: Binance public book ticker through event-driven edge gate
- Normal order: limit post-only `Alo`
- Exact profile: `two-sided-manager`
- Windows: `1`
- Max duration: `900s`
- Max order size: `0.005 BTC` per order
- Max aggregate position delta: `0.01 BTC`
- Max loss: `1 USDC`
- Max submissions: `2`
- Requote attempts: `2`
- Quote hold: `3s`
- Wait after cancel/fill pullback: `10s`
- Kill-switch flatten only: reduce-only IOC; normal taker/crossing remains prohibited

files：
- Execute: `examples/hyperliquid/cross_exchange_live_remote_orchestrator.py`
- Execute: `examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- Accept: `examples/hyperliquid/cross_exchange_t024_same_window_acceptance.py`
- Remote evidence: `/home/admin/hftbacktest-cross-exchange-artifacts/principal_alignment_single_level_two_sided_0719T011/`
- Local evidence: `local_live_analysis/principal_alignment_single_level_two_sided_0719T011/`
- Workflow: this task, business report, QA report, `docs/qa-acceptance-report.md`, `task_plan.md`, `progress.md`, `findings.md`

action：
- Build an isolated remote source archive from the exact source commit and write the full commit marker.
- Run exact local/remote orchestrator preflight without watcher, credential or endpoint calls.
- Run a read-only account/service preflight:
  - exact source/import/Python/env;
  - persistent kill-switch clear;
  - owned open orders empty;
  - BTC position within the aggregate envelope and unambiguous;
  - no conflicting account-trading process;
  - no credential value persisted.
- Start exactly one detached orchestrator window only after every preflight check passes.
- The watcher must use `event-driven-edge-gate-live`, `--exchange-reconciled-manager`, fast Hyperliquid L2 and exactly two requote/submission budgets.
- The manager must validate the exact `{buy,sell}` desired side set before the first order endpoint call.
- Persist one intent, actual order response, status, tracked reference and terminal cancel/full-fill proof per submitted side.
- Pull the complete artifact root locally and independently verify remote/local bytes and terminal manifest.
- Run same-window acceptance against the exact source commit.
- Record post-live open orders, BTC position, process termination, writer, kill-switch and unsupported economics claims.

verify：
- Preflight command and artifact exact-match every envelope/source/activation field.
- Source provenance is sealed before watcher start and independently reproducible from the exact Git commit.
- Both sides reach actual order endpoint calls unless a formal stop condition blocks before the first call.
- Resting/rejected/filled state is independently bound to each side/attempt/reference.
- Every submitted attempt has authoritative cancel or complete reference-bound maker-fill terminal proof.
- Raw fill direction, all-token identity, quantity, price limit, fee and maker/taker role are independently reconstructed.
- Final and independent owned open orders are empty.
- BTC position and realized loss remain within envelope; any residual is reconciled or kill-switch flattened.
- Child process is reaped without unknown state; status writer and heartbeat remain healthy.
- Terminal SHA-256 and remote/local artifact equality pass.
- Same-window acceptance passes mechanism/evidence integrity or records the first exact stop condition without optimism.
- No multi-level, dynamic spread, fill feedback, inventory skew or actual quote behavior activation.

stop conditions：
- Any source, task/window, command, envelope or activation mismatch.
- Account ownership, concurrent service, open-order or position state is ambiguous.
- Kill-switch is not clear before live.
- Desired side set is not exactly `{buy,sell}` before first order call.
- Price precision/post-only invariant fails.
- Submission, size, position, loss or duration cap is exceeded.
- Duplicate/ambiguous/unattributed/conflicting fill evidence.
- Unknown order state or any submitted attempt lacks authoritative terminal proof.
- Owned open orders cannot be proven empty.
- Position cannot be reconciled or safe flatten cannot be proven.
- Process termination/reap, status writer or terminal checksum fails.
- Concurrent account activity contaminates the window.

done：
- One isolated single-level two-sided bounded live lifecycle is locally preserved and independently accepted, or the first formal stop condition is recorded.
- Principal Task 7/Task 12 mechanism and evidence conclusions remain separate from unsupported stable economics.
- Multi-level remains locked until this task is QA accepted.

blockers：
- Any stop condition ends the task and prevents a second live window.

commit：
- source `d8e22c2d9288fef86707d9b26f7791d7d8711c09`

提交信息：
- source `Accept T010 fill direction manifest repair`
