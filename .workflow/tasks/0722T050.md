# 0722T050 OFFLINE-FILL-PROFILE-PROVENANCE-REPAIR

执行线程：
- 总控 auto-loop / 业务执行线程

任务ID：
- 0722T050

标题：
- Repair fill-feedback exact-profile lead-source provenance contract

简短描述：
- 修复 T049 发现的 fill-feedback exact profile preflight `lead_source`
  漏记问题，并补 focused regression。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0722T049`（阻塞；只读引用其 QA findings，不修改其 sealed evidence）

规则更新提醒：
- offline-only；禁止 live、private account、order、cancel、remote、
  service 和 immutable artifact 修改。
- 只修复 profile provenance/test contract，不改变 fill-feedback quote
  behavior、target policy 或 acceptance 对 terminal evidence 的 fail-closed
  语义。

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交。
- 回报必须带 commit id 和提交信息。

是否进行QA验收：
- 是

files：
- `examples/hyperliquid/cross_exchange_live_remote_orchestrator.py`
- `examples/hyperliquid/test_cross_exchange_live_remote_orchestrator.py`
- workflow task/business/QA/fixed QA records

action：
- 把 `two-sided-fill-feedback-manager` 纳入 Binance public lead profile 集合。
- 增加 exact fill profile preflight 的 `lead_source` focused assertion。
- 保持 T049 acceptance 对 ambiguous cancel/unknown terminal query 的阻塞判定不变。

verify：
- focused orchestrator tests。
- `python -m pytest examples/hyperliquid -q`。
- `py_compile`、`git diff --check`。
- 不读取 credentials，不启动 remote/live。

done：
- 新 profile preflight 对 Binance lead provenance 严格通过。
- T049 sealed evidence 不被修改或重解释。

blockers：
- 任何 live/private/remote 操作请求均超出本任务范围。

提交信息：
- `Repair fill profile lead-source provenance`
