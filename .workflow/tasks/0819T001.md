# 任务派发

执行线程：
- 总控-git/repository-hygiene

任务ID：
- 0819T001

标题：
- MACMINI-AMD-WORKTREE-BRANCH-CONSOLIDATION

简短描述：
- 审计并整理 macmini 与 amdserver 的 hftbacktest 工作区，将未提交工作
  保全到 Binance、cross-exchange 或 SKHYNIX Episode 对应开发线上，
  同步 origin，并使两端所有已登记 worktree 达到 clean 状态。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0815T003` 已完成 Episode Stage 4 第六轮 QA 修复。
- `codex/0814t001-skhynix-episode-research` 已有远端保全提交
  `67f7575574acf6afe2c4c26c4b4bff7cda02a79c`。

规则更新提醒：
- 无

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `AGENTS.md`

是否需要提交代码：
- 需要

提交要求：
- 不丢弃任一机器上的独有代码、文档或测试。
- 先建立文件归属和内容校验，再提交、变基、同步或清理副本。
- 所有新提交回报必须带 commit id 和提交信息。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 核对三个目标开发线的 ancestry、提交范围和 origin SHA。
- 核对两端所有登记 worktree 的 `git status --porcelain` 均为空。
- 核对跨机器独有文件已经提交或进入明确的保全分支。
- 核对 Episode 变基后仍包含已验收 Stage 1-4 文件与测试。

files：
- `/Users/liu/Documents/hftbacktest`
- `/Users/liu/Documents/hftbacktest-0814t001-skhynix-episode-research`
- `/home/molly/project/hftbacktest`
- `.workflow/tasks/0819T001.md`
- `.workflow/reports/0819T001-business.md`

action：
- 审计 macmini 和 amdserver 的分支、worktree、未提交文件与远端状态。
- 将 0801-0809 通用跨交易所工作提交到 `cross-exchange`。
- 将 Episode 分支变基到新的 cross-exchange 基线并保留研究专属增量。
- 保全 amdserver 旧 Binance/core worktree 的独有改动，验证后提交到其
  明确功能分支；不把未验证实验直接并入三条主线。
- 删除或恢复 `.DS_Store` 等操作系统元数据，清除失效 worktree 登记。
- 推送相关分支并复核两端 clean 状态。

verify：
- `git diff --check`
- 变更范围对应的 focused pytest / cargo test。
- 三条目标分支的 `git log --left-right --cherry-pick` 与 merge-base 检查。
- macmini 与 amdserver 所有登记 worktree 的
  `git status --porcelain=v1 --untracked-files=all`。
- origin 各目标 ref 与本地 SHA 精确一致。

done：
- 所有独有工作均有可追溯 commit。
- Binance、cross-exchange、Episode 三条开发线归属明确。
- macmini 与 amdserver 已登记 worktree 全部 clean。
- 执行报告记录提交、测试、保全分支和剩余风险。
