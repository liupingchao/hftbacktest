# 0721T044 CURRENT-SOURCE-FIXED-QUOTE-TINY-LIVE-RERUN

执行线程：
- 总控 auto-loop / 业务执行线程 / live-awsserver1

任务ID：
- 0721T044

标题：
- Run one current-source fixed-quote tiny-live window for Task 8 acceptance

简短描述：
- 使用 T043 QA 已验收源码运行一次保守 single-level two-sided manager tiny-live。
- 报价、订单、风控和所有 adaptive activation保持固定；本任务只验证当前源码的decision consistency、reference-bound lifecycle、confirmed resting exposure、censor/quarantine和same-window acceptance。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0721T043`（已通过）

规则更新提醒：
- Standing authorization允许本任务exact live/private/read/submit/cancel/remote/service-isolation操作，无需再次询问。
- 不需要等待大量shadow/replay证据才开始tiny-live；但exact source、执行安全和证据完整性gate必须先通过。
- Live是主要数据来源；replay只用于复现live决策和控制变量。
- 本任务只允许一个live window，不得因stop condition启动第二窗。
- Dynamic spread、fill feedback、inventory skew、multi-level和actual quote behavior change全部保持关闭。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `.workflow/reports/0721T043-qa.md`
- `docs/superpowers/plans/2026-07-17-principal-alignment-p0-p3.md`

是否需要提交代码：
- 不需要；live source固定为下述exact commit。

提交要求：
- Source commit必须在remote preflight前固定。
- Evidence和workflow report在pullback/acceptance后提交。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

source：
- Branch: `cross-exchange`
- Exact source commit: `260f8964812eb20506eb221333e6aca17da20b4d`
- Remote source: `/home/admin/hftbacktest-cross-exchange-0721T044`
- Source marker: `/home/admin/hftbacktest-cross-exchange-0721T044/source_commit.txt`

envelope：
- Host: `awsserver1`
- Account scope: Hyperliquid account configured by `/home/admin/XEMM_rust_latest/.env`
- Venue/symbol: Hyperliquid `BTC`
- Lead source: Binance public book ticker through event-driven edge gate
- Normal order: limit post-only `Alo`
- Exact profile: `two-sided-manager`
- Windows: `1`
- Max duration: `1800s`
- Max order size: `0.005 BTC` per order
- Max aggregate position delta: `0.01 BTC`
- Max loss: `1 USDC`
- Max submissions: `2`
- Requote attempts: `2`
- Quote hold: `3s`
- Wait after cancel/fill pullback: `10s`
- Kill-switch flatten only: reduce-only IOC；normal taker/crossing禁止

control variables：
- 沿用T037/T031/T026的fixed half-spread、accepted normalization、signal freshness、edge threshold、quality gate、quote size、hold/wait和manager logic。
- Dynamic spread activation: off。
- Fill feedback activation: off。
- Inventory skew activation: off。
- Multi-level activation: off。
- Actual quote behavior change: false。
- Estimator output observe-only，不得改变desired prices、sizes、side set、requote或cancel行为。
- Primary evidence variable只有当前QA源码下的decision/lifecycle/exposure/censor/quarantine/same-window consistency。

files：
- Execute: `examples/hyperliquid/cross_exchange_live_remote_orchestrator.py`
- Execute: `examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- Replay: `examples/hyperliquid/cross_exchange_online_estimators.py`
- Accept: `examples/hyperliquid/cross_exchange_t024_same_window_acceptance.py`
- Remote evidence: `/home/admin/hftbacktest-cross-exchange-artifacts/principal_alignment_estimator_observe_only_0721T044/`
- Local evidence: `local_live_analysis/principal_alignment_estimator_observe_only_0721T044/`
- Workflow: task、business、QA、fixed QA、`task_plan.md`、`progress.md`、`findings.md`

action：
- 从exact source构建隔离remote archive并写完整commit marker。
- 验证local/remote archive SHA-256、runtime imports和exact no-start orchestrator preflight。
- 记录冲突account service/process状态；只在必要时隔离，并仅在安全终态被独立证明后恢复。
- 执行一次read-only account/service preflight：exact source/import/env、kill-switch clear、empty open orders、finite position within cap、无并发account trader。
- 仅在全部preflight通过后启动一个detached orchestrator。
- 要求event-driven edge gate、exchange-reconciled two-sided manager、fast Hyperliquid L2、requote `2`和exact duration `1800s`。
- 保留raw decision、submit/resting/reject/cancel/fill、direct/history terminal、manager interval、estimator、censor/quarantine和final account evidence。
- Pull完整remote artifact root，验证terminal checksum和remote/local SHA-256。
- 使用exact task/source/root/duration运行same-window acceptance和estimator replay。
- 记录final open orders、BTC position、service/process、kill-switch、writer health和unsupported claims。

verify：
- Source provenance在watcher启动前sealed并可从exact Git blobs独立复现。
- Preflight exact匹配task/window/source/profile/duration/caps/activation，且不启动watcher或调用endpoint。
- 两侧到达真实order endpoint，除非首个formal stop condition在授权前阻止。
- 每个submitted attempt都有authoritative reference-bound rejected、canceled或complete maker-fill terminal proof。
- Unknown/generic cancel和account-wide absence不得替代target-bound terminal proof。
- Historical fallback如被调用，必须满足exact marker、`4.0s` delay、`0.5s` reserve、最多五轮direct和一次history。
- T040 accepted probe保持完整delayed-history read-path的独立机制证据；T044不得声称未实际发生的history call。
- 至少一侧exchange-confirmed resting，并产生至少一条positive-duration confirmed exposure。
- Leading censor显式记录；interior invalid evidence进入quarantine，不得静默丢弃。
- Estimator replay exit `0`并精确匹配source snapshot、exposure、censor、quarantine、fit status/reason和dynamic candidate。
- Producer、replay和acceptance均证明所有adaptive activation false且actual quote behavior unchanged。
- Final owned/account open orders为空；BTC position和loss在envelope内。
- Child reaped、status writer、heartbeat、runtime source verification、terminal checksum和transfer equality通过。
- Same-window mechanism/evidence integrity必须pass，或记录首个formal stop condition且不乐观升级。

stop conditions：
- 任一source、task/window、command、duration、envelope或activation mismatch。
- Account ownership、并发service、open-order或position状态不明确。
- Kill-switch在live前不clear。
- Desired side set在首个order call前不是exact `{buy,sell}`。
- Price precision/post-only invariant失败。
- Submission、size、position、loss或duration超限。
- Duplicate、ambiguous、unattributed或conflicting fill/terminal evidence。
- Unknown order state或submitted attempt缺少authoritative terminal proof。
- Owned/account open orders无法证明为空。
- Position无法对账或safe flatten无法证明。
- Process termination/reap、status writer、source verification或checksum失败。
- Manager hold shutdown、delayed history、interval/exposure/censor/quarantine或decision evidence malformed/non-reproducible。
- Producer、replay或acceptance显示任何adaptive activation或actual quote behavior change。
- Concurrent account activity污染window。

done：
- 一个且仅一个current-source fixed-quote manager tiny-live window被保存并独立接受，具备一致decision evidence、authoritative terminal proof、confirmed resting exposure和exact replay；或首个formal stop condition被记录。
- Principal Task 8 evidence gate与unsupported stable A/k、fill-rate、profitability、multi-level或promotion claims分离。
- 本任务内不启动第二window或任何adaptive/multi-level activation。

blockers：
- 无。

提交信息：
- `Dispatch T044 current-source fixed-quote live`
