# 任务派发

执行线程：
- 业务线程-python/research

任务ID：
- 0814T001

标题：
- SKHYNIX-EPISODE-V3-INPUT-FREEZE-AND-DATA-ADMISSION

简短描述：
- 执行 trigger-aligned Episode v3 研究队列第 1 阶段：冻结四个
  session 的输入身份、采集拓扑、KRX underlying state、Hyperliquid
  feed cadence、测量限制、证据标签、评分、假设和 outcome contract。
- 本任务只建立可审计的数据准入与研究合同，不构建 Episode v3，不读取
  Aug07 完整事件结果，不运行触发密度或 outcome 模型。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- `docs/skhynix_trigger_aligned_episode_research_implementation_plan.md`
  已完成 review remediation。
- Jul30、Aug03、Aug04 的历史数据和 Aug07 的已验收 compact
  metadata/inventory 已存在。

规则更新提醒：
- 请先阅读规则文档和本任务引用的研究方案后再执行本任务。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `AGENTS.md`
- `docs/skhynix_trigger_aligned_episode_research_implementation_plan.md`
- `docs/binance_hyperliquid_slice_v1.md`
- `docs/binance_hyperliquid_slice_v2_appendix.md`

是否需要提交代码：
- 不需要

提交要求：
- 保留本 worktree 的任务范围改动供 QA 检查。
- 不提交从主工作区同步而来的既有未提交文件。
- 回报使用 `commit：无`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 核对四个 session 的 input identity、manifest/hash 和 acquisition
  topology provenance。
- 核对 KRX calendar/state contract、HL cadence 统计、历史
  `@trade q`/no-`nq`/RPI unavailable/`depth@0ms` 限制。
- 核对评分、hypothesis、outcome、censoring 和 evidence-label freeze。
- 核对两次构建字节一致、源输入未修改、Aug07 first-read 边界未越过。

files：
- `examples/hyperliquid/cross_exchange_trigger_aligned_episode_contract.py`
- `examples/hyperliquid/test_cross_exchange_trigger_aligned_episode_contract.py`
- `local_live_analysis/skhynix_trigger_aligned_episode_research_v1/`
- `.workflow/reports/0814T001-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

read-only inputs：
- `/Users/liu/Documents/hftbacktest/local_live_analysis/cross_exchange_collection_campaign_0730T011_skhynix_4h_8x30m/`
- `/Users/liu/Documents/hftbacktest/local_live_analysis/skhynix_cross_exchange_research_0730T013/`
- `/Users/liu/Documents/hftbacktest/local_live_analysis/cross_exchange_collection_campaign_0802T001_skhynix_5h_10x30m/`
- `/Users/liu/Documents/hftbacktest/local_live_analysis/skhynix_cross_exchange_research_0803T001/`
- `/Users/liu/Documents/hftbacktest/local_live_analysis/skhynix_cross_exchange_research_0804T001_old5h_replay/alignment/`
- `/Users/liu/Documents/hftbacktest/local_live_analysis/cross_exchange_collection_campaign_0804T001_skhynix_2h_continuous/`
- `/Users/liu/Documents/hftbacktest/local_live_analysis/skhynix_cross_exchange_research_0804T001/`
- `/Users/liu/Documents/hftbacktest/local_live_analysis/0807T001_skhynix_4h_continuous/`
- `/Users/liu/Documents/hftbacktest/local_live_analysis/0807T002_skhynix_postprocess_compact/`

action：
- 新增纯研究合同/准入模块，使用结构化 parser 和 canonical
  serialization；不得以 ad hoc 文本匹配重建 provenance。
- 冻结四个 session 的 canonical source paths、manifest identity、文件
  count/size/SHA、collection start/end、collector/runtime identity 和
  collection topology fingerprint。
- 将 research execution host 与 acquisition topology 分开记录。
- 从实际归档发布 Binance/Hyperliquid channel inventory；确认历史
  Binance 为 `@trade + @depth@0ms + @bookTicker`，并冻结
  `nq/RPI unavailable` 测量边界。
- 计算并发布 HL BBO/trade/fast-L2 的 inter-arrival、source-age、
  no-new-information 与 cadence-resolution 统计；缺字段必须显式
  unavailable，不得补造。
- 冻结 versioned KRX timezone/calendar/state contract，并发布每个
  session 的 underlying-state duration/coverage。无法权威确定的状态
  必须标为 `unknown_calendar_state`。
- 冻结 Family A/Family B、Candidate/Confirmed landmarks、
  `observed_at <= decision_landmark`、interval censoring、episode
  horizon、outcome list、evidence labels、primary hypotheses、CRPS/Brier/
  interval-log-loss 和 A0/B0 normalized-loss contract。
- 生成 content-addressed research manifest、input inventory、session
  topology、underlying regime coverage、HL cadence 和 data-admission
  报告；原子发布，失败不得暴露部分目录。
- 两次独立完整构建必须产生相同核心文件 bytes/SHA。
- 记录执行前后 read-only input inventory，证明源数据未改变。

verify：
- `python examples/hyperliquid/cross_exchange_trigger_aligned_episode_contract.py --help`
- `python -m pytest examples/hyperliquid/test_cross_exchange_trigger_aligned_episode_contract.py`
- 对 Jul30/Aug03/Aug04 和 Aug07 compact metadata 运行两次独立构建。
- 比较两次构建的 required-output key/path/schema/count/SHA。
- 比较 read-only inputs 执行前后 inventory。
- hostile tests 至少覆盖：缺 manifest/hash、伪造 topology、未来
  observation contract、RPI/nq 错标、`depth@0ms` 错标、HL cadence
  缺失后静默补值、KRX unknown state 被未来价格推断、评分 metric 漂移、
  Aug07 完整事件文件被提前打开、部分发布可见。
- `python -m compileall examples/hyperliquid/cross_exchange_trigger_aligned_episode_contract.py`
- `git diff --check`

done：
- 业务线程回报状态必须为 `待验收`，并写入
  `.workflow/reports/0814T001-business.md`。
- 回报必须列出输出路径、核心 SHA、四 session 准入结论、不可用字段、
  两次构建一致性、源输入前后 inventory 结果和测试命令。
- 本任务通过 QA 前，不得开始 trigger density、detector parity、
  Episode v3、case retrieval、outcome modeling 或 Aug07 first-read。

hard boundary：
- 不启动新采集。
- 不访问 private/account/order/cancel endpoint。
- 不提交、修改或取消订单。
- 不修改任何 read-only input。
- 不打开 Aug07 AMD 完整 R0/R1/basis event rows；只允许读取本任务列出的
  local raw manifest 和 compact metadata/inventory。
- 不构建 Episode v3，不运行 trigger density、outcome/model/actionability。
- 不修改现有 v1/v2 artifact。

业务执行结果：
- 已实现纯 input-freeze/data-admission 合同生成器和 `22` 个聚焦/hostile
  tests。
- 已发布
  `local_live_analysis/skhynix_trigger_aligned_episode_research_v1/`；
  research manifest 的核心包 SHA256 为
  `4267c9506499082bef157991757899601d66741a665345fe75522adf27122752`。
- 两次独立完整构建的全部 `14` 个文件路径、bytes 和 SHA256 一致。
- 源输入前后 inventory 均为 `961` files、`2,835,271,526` bytes，
  inventory SHA256 均为
  `fc94f6d3dd8b0d1527c03dae839201ca494807264ecfdaa1218ecdb24aaff3a0`。
- Aug07 access ledger 记录 `event_rows_opened=false`；未打开 Aug07 完整
  raw/R0/R1/basis event rows。
- 未构建 Episode v3，未运行 trigger density、outcome、model 或
  actionability，未启动采集，未访问 private/order/cancel。
- 业务报告见 `.workflow/reports/0814T001-business.md`；等待总控另行派发
  QA。

QA验收结果：
- 2026-08-15 独立 QA 结论为 `未通过`。
- 正式包、两次 QA 完整构建和全部 `14` 个文件 determinism 通过；源
  inventory 不变，当前 Aug07 完整事件 rows 未被打开。
- P1 x4：Aug07 compact event-row guard fail-open、cadence 缺失值可被
  补造、topology fingerprint 未重算、Family/landmark/interval freeze
  只做部分语义校验。
- 详见 `.workflow/reports/0814T001-qa.md`。
- 后续 trigger density、detector parity、Episode v3、outcome/model/
  actionability 和 Aug07 first-read 继续锁定。

第一轮修复派发：
- 保持任务 ID `0814T001`，不创建新的研究阶段。
- 修复 `Aug07AccessPolicy`：raw 与 compact 一律先拒绝 full-event
  文件名、压缩事件后缀和非 metadata allowlist；ledger 必须由实际读取
  记录生成，不能接受自报 `event_rows_opened=false`。
- 将 compact 准入改为 exact relative-path metadata allowlist；新增或
  移入 compact 根目录的 `decision_labels.csv.gz`、
  `basis_features.csv.gz`、任意 `raw.gz` 或未知文件必须 fail closed。
- 修复 cadence validation：解析每行 `missing_fields`，要求所有命名
  metric 精确为空；`availability=unavailable` 时全部 metric 为空；
  `partial_compact_metadata` 只允许显式 available metric 非空，并与
  missing set 精确互补。
- 修复 topology validation：从每行完整 canonical acquisition facts
  重建 payload 并 exact recompute
  `collection_topology_fingerprint`；要求 session set/cardinality、
  physical topology 和 provenance facts 精确闭合。
- 修复 frozen-contract validation：对 Family A/B、decision landmark、
  rejected confirmation nullability、observation-time invariant、outcome、
  censoring、hypotheses、scoring formula、A0/B0 和 Aug07 first-read
  contract进行完整 canonical exact-freeze，不接受同 schema 语义漂移。
- 将 QA 四个 `ACCEPTED_FAIL_OPEN` 反例加入正式测试，并补充组合漂移、
  extra-key/missing-key 和同 schema mutation。
- 修复后重新生成正式包，完成两次独立构建、source inventory
  before/after、runtime-source binding 和 package comparison。
- 第一轮修复业务报告写入
  `.workflow/reports/0814T001-business-r1.md`，状态回到 `待验收`。
- 修复期间不得执行 trigger density、detector parity、Episode v3、
  outcome/model/actionability 或 Aug07 full-event first-read。

第一轮修复业务结果：
- 2026-08-15 第一轮修复已完成，任务状态回到 `待验收`。
- 四个 P1 已关闭：Aug07 raw/compact exact metadata allowlist 和实际读取
  ledger、cadence missing-value exact partition、四 session canonical
  acquisition topology exact binding/fingerprint recomputation、完整 frozen
  contract canonical equality。
- 正式回归测试现为 `36 passed`，包含 QA 四个
  `ACCEPTED_FAIL_OPEN` 反例、missing/extra-key、same-schema 和组合漂移。
- 正式包和两次独立完整构建的全部 `14` 个文件路径、bytes、SHA256
  一致；核心包 SHA256 为
  `9c2756f966a1a05816f9e6a91d7361d76cfea10fc3c587944ca2f0c1d2eb7c96`。
- repaired exact compact allowlist 下，三次构建的 source inventory 均为
  `890` files、`2,834,242,005` bytes、SHA256
  `4b9c6f53ff43cefa3f606c8c8d78e403e5eda230d0044dc35e0b1ed1d40d8908`，
  每次 before/after 相同；count 从旧包 `961` 降低是因为不再将 compact
  根目录中的非 allowlist metadata 计为 admitted input，不是源文件变化。
- 三次构建的 Aug07 ledger 均由实际成功读取生成：`51` 次 metadata
  reads、`13` 个唯一 allowlisted paths、`39` 个 raw stat-only paths、
  `event_row_read_count=0`、`event_rows_opened=false`。
- 未运行任何后续研究阶段，未打开 Aug07 full-event rows。
- 业务报告见 `.workflow/reports/0814T001-business-r1.md`；等待独立 re-QA。

第二轮 QA验收结果：
- 2026-08-15 独立 re-QA 结论为 `已通过`。
- 三份独立 evidence reports 与 formal manifest、formal Aug07 ledger、
  业务 r1 报告一致；第一轮 P1 x4 均已关闭。
- focused `--verify-only` spot check 继续通过，输出核心 SHA256 为
  `9c2756f966a1a05816f9e6a91d7361d76cfea10fc3c587944ca2f0c1d2eb7c96`，
  `aug07_full_event_rows_opened=false`，
  `source_inventory_unchanged=true`。
- 最新 accepted source inventory 为 `890` files、`2,834,242,005` bytes、
  SHA256 `4b9c6f53ff43cefa3f606c8c8d78e403e5eda230d0044dc35e0b1ed1d40d8908`；
  repaired package 与两次独立完整构建的全部 `14` 个文件路径、bytes、
  SHA256 一致。
- P0：无；P1：无；P2：无；P3：无。
- 详见 `.workflow/reports/0814T001-qa-round2.md`。
