# 任务派发

执行线程：
- 业务线程-live-evidence

任务ID：
- 0717T002

标题：
- T011-SSM-FIRST-CONTROLLED-ROLE-EVIDENCE-RERUN

简短描述：
- 使用 0717T001 新增的 SSM-first remote orchestrator 执行一次受控 live role-evidence rerun。
- 本任务目标是补充 fill source / maker-taker role 证据；不改变策略参数、quote policy、threshold、order size、max submissions 或 max loss。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0717T001` QA 已通过，允许未来 live evidence run 使用 SSM-friendly remote orchestrator。
- `0716T006` 当前 blocker 仍为 `no_fill_role_evidence_absent`。

规则更新提醒：
- 请先阅读规则文档后再执行本任务。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

live authorization：
- controller authorization: user said "好 继续做live test" on 2026-07-17.
- start: immediate, no earlier than `2026-07-17T04:47:27Z`.
- host: `awsserver1`, EC2 instance `i-02c64c088f311cbc1`, region `ap-northeast-1`.
- control plane: SSM-first. SSH/scp/rsync may be used only for short sync or artifact pullback and are not the evidence source of truth.
- venue/symbol: Hyperliquid `BTC`.
- post-only behavior: `Alo`.
- windows: `3` sequential windows.
- window duration: `1800s`.
- max order size: `0.005 BTC`.
- max submissions: `2` per window.
- max position delta: `0.01 BTC`.
- max loss: `1 USDC`.
- source branch: local `cross-exchange`.
- source commit at dispatch: `ab2661fc2377425974c284dd5c71e583baebc39b`.
- credential boundary: use existing awsserver1 env file `/home/admin/XEMM_rust_latest/.env`; do not print, copy, or commit credential values.
- real order submit/cancel: allowed only inside this envelope.

files：
- `.workflow/tasks/0717T002.md`
- `.workflow/reports/0717T002-business.md`
- `.workflow/reports/0717T002-qa.md`
- `docs/qa-acceptance-report.md`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `local_live_analysis/cross_exchange_controlled_role_evidence_0717T002*/`

action：
- Confirm SSM connectivity and remote preflight.
- Ensure awsserver1 has the 0717T001 remote orchestrator.
- Launch the live run through `examples/hyperliquid/cross_exchange_live_remote_orchestrator.py`.
- Monitor `run_status.json` and `heartbeat.json` through SSM-first recovery.
- Pull artifacts back locally when complete or blocked.
- Validate JSON/CSV parseability, final open-orders proof, window statuses, role-evidence files, and boundary manifest.

verify：
- SSM instance online check.
- Remote `py_compile` / `--help` for orchestrator and watcher.
- Remote/live artifact status checks.
- Local JSON/CSV parse checks on pulled artifacts.
- Final open-orders proof must be recorded.
- `git diff --check`.

done：
- Report whether fill role evidence was produced.
- If fills exist, classify liquidity role as `confirmed_maker`, `confirmed_taker`, or `unknown_liquidity_role`.
- If no fills exist, report artifacts as complete but keep P0 blocked.
- Report all deviations, blockers, validation commands, artifact root, commit id, and unsupported claims.

not authorized：
- No threshold change.
- No quote-envelope change.
- No order-size expansion.
- No max-submissions expansion.
- No max-loss expansion.
- No fill-seeking quote-policy change.
- No fee/PnL calibration.
- No maker viability, T012, promotion, or final MVP claim.
