```md
执行线程：
- 业务线程-research

任务ID：
- 0617T004

标题：
- Hyperliquid tiny-live signal / quote policy protocol

简短描述：
- 在 `0617T003` QA 通过后，定义 `0616T008` tiny-live 前必须具备的最小 Binance-lead / Hyperliquid-lag maker 下单信号和报价策略协议。本任务只做策略协议设计和本地 artifact，不真实下单、不撤单、不调用私有接口、不读取凭证、不启动 live bot。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0617T003` QA 必须为 `已通过`。

规则更新提醒：
- 请先阅读 `.workflow/workflow-kit/workflow-manual.md`、`.workflow/workflow-kit/task-dispatch-template.md`、`.workflow/workflow-kit/thread-report-template.md`、`.workflow/workflow-kit/qa-acceptance-template.md`、`task_plan.md`、`progress.md`、`findings.md`、`docs/hyperliquid_tiny_live_0616T006_T008_auto_loop.md` 后再执行本任务。
- 本任务是 signal / quote policy protocol，不是 `0616T008` live execution。

按线程规则执行：
- `AGENTS.md`
- `docs/hyperliquid_tiny_live_0616T006_T008_auto_loop.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0617T004.md`
- `.workflow/reports/0617T004-business.md`
- `docs/hyperliquid_tiny_live_signal_quote_policy_protocol.md`
- `local_live_analysis/hyperliquid_tiny_live_signal_quote_policy_protocol_0617T004/**`
- `task_plan.md`
- `progress.md`
- `findings.md`

action：
- Consume accepted cross-exchange public/read-only evidence chain, especially `0601T004`, `0601T005`, `0604T003`, and the current `0617T003` final preflight/operator packet.
- Define the minimal tiny-live signal policy for Binance lead / Hyperliquid lag:
  - inputs: Binance public mid, Hyperliquid public mid, local/as-of timestamps, join age, spread/tick size, and public top-of-book state where available.
  - signal: `basis_mid = binance_mid - hyperliquid_mid` and/or normalized tick basis.
  - trigger persistence: require signal threshold persistence over multiple observations before any quote intent.
  - side mapping: positive lead/lag signal may only create Hyperliquid maker buy intent; negative lead/lag signal may only create Hyperliquid maker sell intent.
  - quote policy: post-only/maker-only, no crossing, no taker fallback, bounded quote distance from Hyperliquid BBO/mid.
  - size policy: max order size `0.01 BTC`, max position `0.04 BTC`, reduce-side-only behavior at or near position limit.
  - cancel/stop policy: signal decay, stale join, public data gap, post-only reject, cap breach, max loss proximity, duration expiry, and operator stop must cancel/stop.
- Use existing local artifacts to choose conservative thresholds where possible. If the available accepted artifacts do not contain enough distribution data for a defensible threshold, explicitly classify the threshold as `blocked_for_live_execution` and recommend the smallest read-only threshold calibration task.
- Define required runtime audit fields for `0616T008`: signal inputs, timestamps, basis ticks, threshold state, side intent, quote price, quote distance, post-only flag, size, position before/after, cap checks, cancel reason, reject reason, and shutdown evidence.
- Preserve all approved `0616T008` caps from `0617T003`.
- Do not implement a live bot, connector, signed client, private endpoint call, account query, order placement, order cancellation, order amendment, user stream, nonce/signing logic, deployment, promotion, PnL proof, or maker viability claim.

acceptance criteria：
- The protocol gives an explicit signal formula, side mapping, quote placement rule, size/cap policy, cancel/stop policy, and required audit fields.
- Any threshold used for live must have a defensible source in accepted local artifacts; otherwise the protocol must fail closed and recommend a threshold calibration task before `0616T008`.
- The protocol explicitly rejects discretionary/manual market-direction entries and taker fallback.
- The protocol preserves `0617T003` approved caps and states they apply only to the later separately dispatched `0616T008`.
- Final recommendation must be exactly one of:
  - `hyperliquid_tiny_live_signal_quote_policy_ready_for_qa`
  - `hyperliquid_tiny_live_signal_quote_policy_needs_threshold_calibration`
  - `hyperliquid_tiny_live_signal_quote_policy_blocked`

verify：
- Validate any generated JSON manifest with `/home/liushuai/workspace/hftbacktest/.conda-envs/hft-py38/bin/python -m json.tool <artifact>.json`.
- Check required CSV/markdown artifacts are non-empty.
- Boundary text check for no live/order/private authorization.
- `git diff --check`

done：
- 回报 signal formula、threshold source/status、side mapping、quote distance/post-only rule、size/cap policy、cancel/stop policy、required audit fields。
- 明确说明本任务是否允许下一步创建 `0616T008`，或是否必须先做 threshold calibration。
- 明确说明没有读取凭证、没有调用私有接口、没有账号查询、没有下单/撤单/改单、没有 live bot。
```
