# 0721T046 EXACT-JOIN-PRIORITY-AND-MANAGER-IDENTITY-REPAIR

执行线程：
- 总控 auto-loop / 业务执行线程

任务ID：
- 0721T046

标题：
- Repair exact freshness priority and submitted manager identity contract

简短描述：
- 修复T045 QA发现的两个P2：exact two-row freshness被event-level bridge预检查错误阻断，以及bridge资格允许skipped/empty status和缺失window_id。
- 仅离线修复与测试；继续在immutable T044上回放，不新增live。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0721T045`（未通过，QA findings已记录）

规则更新提醒：
- 本任务offline-only；不得执行live/private/account/network/remote/service/order/cancel操作。
- 不得修改或重新seal T044 immutable evidence。
- 不得放宽任何submit-decision、lifecycle、terminal或legacy join。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `.workflow/reports/0721T045-qa.md`
- `docs/superpowers/plans/2026-07-17-principal-alignment-p0-p3.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交。
- 回报必须带`commit id`和`提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

source：
- Dispatch base：`104bdd5`
- Immutable target evidence：
  `local_live_analysis/principal_alignment_estimator_observe_only_0721T044/`

files：
- Modify: `examples/hyperliquid/cross_exchange_t024_same_window_acceptance.py`
- Modify: `examples/hyperliquid/test_cross_exchange_t024_same_window_acceptance.py`
- Modify: `examples/hyperliquid/test_hyperliquid_tiny_live_m2_event_driven_watcher.py`
- Workflow: task、business、QA、fixed QA、`task_plan.md`、`progress.md`、`findings.md`

action：
- 在manager-batch freshness precheck前计算每个canonical manager attempt的exact freshness identity。
- 若所有canonical attempts均有exact freshness row，完全跳过event-level batch bridge cardinality/precondition；逐attempt exact validation继续执行。
- 只有至少一个canonical submitted attempt缺少exact freshness时，才进入batch bridge fallback。
- Bridge canonical identity必须要求：
  - `order_endpoint_called`严格为true；
  - `order_status_types`非空且不为`skipped`；
  - `window_id`显式存在且等于expected window；
  - attempt/legacy attempt/key/task/window/side为完整、连续、唯一的buy/sell pair。
- 保持unique freshness、首attempt、status/time/projection exact checks。
- 增加双exact freshness positive regression，以及skipped、empty status、missing window hostile regression。

verify：
- `test_manager_batch_attempt_bridge_requires_exact_two_sided_identity`及新增exact/hostile断言。
- `test_task7_explicit_manager_mode_uses_two_sided_path` strict positive及hostile。
- Acceptance测试文件、watcher测试文件、完整`examples/hyperliquid` regression。
- `py_compile`和`git diff --check`。
- Immutable T044 acceptance必须exit `0`、decision `43/0`、lifecycle `78/0`、mechanism/evidence pass、offline-only。
- 旧`0719T001`兼容边界保持green。

done：
- T045 QA三个P2全部关闭；T044 exact two-row和single shared-row两种valid contract均可按预期处理。
- 真实 duplicate identity、missing、cross-event、wrong task/window、skipped/empty status、missing window、empty/mismatched projection均继续fail-closed。
- 不修改immutable evidence，不启动live，不解锁dynamic/fill-feedback/inventory/multi-level。

blockers：
- 无。

提交信息：
- `Dispatch T046 exact join priority repair`
