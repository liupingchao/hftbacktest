# 任务派发

执行线程：
- 业务线程-python/cross-exchange-research

任务ID：
- 0801T004

标题：
- HYPERLIQUID-CONDITIONAL-BASELINE-RESIDUAL-MOTIF-PROTOTYPE

简短描述：
- 按 `docs/hyperliquid_liquidity_response_case_hierarchy_plan.md` 执行
  Goal 3，在已验收 continuous-flow episodes 上构建 conditional baseline、
  residual features、motif communities 和 prototypes。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- `0801T003` QA `已通过`。

规则更新提醒：
- 无

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `docs/hyperliquid_liquidity_response_case_hierarchy_plan.md`

是否需要提交代码：
- 不需要

提交要求：
- 不需要提交
- 回报写 `commit：无`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `examples/hyperliquid/cross_exchange_liquidity_response_case_hierarchy.py`
- `examples/hyperliquid/test_cross_exchange_liquidity_response_case_hierarchy.py`
- `local_live_analysis/skhynix_liquidity_response_case_hierarchy/`
- `.workflow/tasks/0801T004.md`
- `.workflow/reports/0801T004-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

action：
- 验证 T003 episode manifest、membership、episode catalog 和 output SHA。
- 构建 discovery `0001-0003` / held-out `0004-0008` split 和 frozen research
  contract。
- 用 discovery folds 冻结 matched-neighbor median baseline，并输出 held-out
  first-read manifest。
- 输出 baseline predictions、episode residual features、baseline calibration、
  motif membership、prototypes、response curves、counterexamples、walk-forward
  stability 和 surrogate/test result summary。
- Motif 输入不得包含 segment ID、absolute timestamp、PnL-like outcome 或
  adverse markout selection field。

verify：
- `python -m pytest examples/hyperliquid/test_cross_exchange_liquidity_response_case_hierarchy.py -q`
- `python examples/hyperliquid/cross_exchange_liquidity_response_case_hierarchy.py --help`
- 真实 Goal 3 builder。
- 独立脚本复核 split、frozen contract、heldout consumption、baseline rows、
  motif graph degree、prototype identity 和 output SHA。

done：
- Goal 3 artifacts published under `baseline/` and `motif/`。
- Held-out usage is recorded exactly once for this frozen contract。
- 若统计支持不足，只输出 `response_structure_only` / `not_supported`，不声称
  tradable signal、maker identity、exact fill 或 maker PnL。
