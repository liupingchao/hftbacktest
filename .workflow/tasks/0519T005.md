```md
执行线程：
- 测试线程

任务ID：
- 0519T005

标题：
- Step 8B quote-update churn/API/stale-price read-only diagnostic and implementation-planning

简短描述：
- 基于 `0519T004` 已通过的 Step 8 design contract，read-only 量化当前样本里的 quote churn、API/throttle/reject、stale-anchor 和 bad-price regimes，并给出后续实现边界判断。本任务只做诊断和 implementation-planning，不改策略、不跑新 replay sweep、不启动 live。

状态：
- 待执行

执行顺序：
- 当前唯一任务

前置任务：
- `0519T004` QA 已通过
- `0519T003` QA 已通过
- `0518T004` QA 已通过
- `0519T002` QA 已通过

规则更新提醒：
- 本任务是 read-only diagnostic / implementation-planning，不允许改策略、不允许启动 live、不允许把 Step 5C 或 Step 8 设计升级为 default-on。

按线程规则执行：
- .workflow/workflow-kit/thread-report-template.md
- docs/thread-playbook.md
- task_plan.md
- progress.md
- findings.md

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

Allowed files：
- `.workflow/tasks/0519T005.md`
- `.workflow/reports/0519T005-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `.workflow/dashboard.html`
- `.workflow/dispatch_suggestions.md`
- `local_live_analysis/5-13-day-control-30min/stage8b_quote_update_diagnostic_0519T005/**`

Allowed read-only inputs：
- `.workflow/reports/0519T004-qa.md`
- `.workflow/reports/0519T004-business.md`
- `.workflow/reports/0519T003-qa.md`
- `.workflow/reports/0518T004-qa.md`
- `.workflow/reports/0518T003-qa.md`
- `.workflow/reports/0519T002-qa.md`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `local_live_analysis/5-13-day-control-30min/**`
- `examples/binance_tick_mm/**` only for schema / field discovery, not for edits

Forbidden files / areas：
- Any strategy, replay, connector, schema, live, deployment, or core implementation file.
- Any default config or default-on behavior.
- Any AWS/remote state, live collection, live micro test, or replay sweep.
- Any repair of Step 5C source-level drift, top5 hard-anchor promotion, queue/touch repair, or inventory-control implementation.

Requires separate task：
- Any quote-update implementation.
- Any default-off helper implementation.
- Any replay candidate experiment.
- Any new audit schema field implementation.
- Any live collection or live validation.

Fatal Failures：
- 修改策略、replay、live、connector、schema 或配置默认行为。
- 将 GTX/post-only reject 当作正常 quote-update 控制流。
- 将 Step 5C default-off / diagnostic-first safety layer 升级为 default-on。
- 使用当前单样本结论授权 Step 9、live readiness 或 promotion。

scope：
- 只回答当前 accepted sample 上 Step 8 需要的实施前问题：
  - 当前 cancel/re-add churn 主要来自哪些 regime？
  - 当前 reject/throttle/drop/API-limit 风险是否可观测？缺哪些字段？
  - stale anchor、missing anchor、join-age、latency 和 bad-price ticks 是否足以解释 quote update pressure？
  - min quote move / quote age gate 可能会挡住多少无意义 update，又可能放过多少 bad-price exposure？
  - Step 7 inventory request 如果以后接入，会在哪些 regime 与 anti-churn/API/post-only gates 冲突？
  - 当前证据支持 no-change、default-off helper implementation、full default-off replay candidate，还是 needs-more-instrumentation？

action：
- 读取 T004 design contract、Step 5C safety result、Step 6 closure result 和 Step 7 design boundary。
- 在 `5-13-day-control-30min` 现有 artifacts 上做 read-only diagnostic，优先复用已有 audit / acceptance / Stage 5C / Stage 6 / Stage 7 产物。
- 量化并分桶：
  - quote churn / cancel-readd regime：side、quote age、join age、latency bucket、placement distance、stale/missing anchor、post-only risk、inventory / position bucket when available
  - API hygiene regime：reject、throttle、drop、request spacing、cancel/re-add bursts、in-flight / cancel-pending proxy when available
  - stale / bad-price regime：anchor age、missing anchor、bad-price ticks、min quote move eligibility、post-clamp crossed/post-only risk
  - implementation readiness：available fields、missing fields、observed-only proxies、fields that need future audit instrumentation
- 产出一个 implementation-planning conclusion，四选一：
  - `no_change`
  - `default_off_helper_candidate`
  - `full_default_off_replay_candidate`
  - `needs_more_instrumentation`
- 明确 Step 9 是否仍需等待 default-off helper / instrumentation，还是可以作为后续 offline replay task 被新建。

verify：
- 文档一致性检查：确认 `task_plan.md`、`progress.md`、`findings.md` 对 Step 8B / Step 9 边界不冲突。
- 如果生成诊断 artifacts，必须记录输入路径、输出路径、样本名和命令。
- `python3 .workflow/build_dashboard.py`
- `git diff --check`

done：
- 回报必须明确 Step 8B 的 conclusion 四选一结果。
- 回报必须列出 current churn/API/stale/bad-price regimes 中哪些可观测、哪些字段缺失。
- 回报必须明确是否建议开后续 implementation task，且不得直接授权 Step 9 / live / promotion。
- 回报必须写 `commit id` 和提交信息。
```
