# 任务派发

执行线程：
- 业务线程-python/cross-exchange-research

任务ID：
- 0801T007

标题：
- HYPERLIQUID-CONDITIONAL-BASELINE-V2-FREEZE-AND-POST-SELECTION

简短描述：
- 基于已验收 `episode_boundary_v2` 构建真正的 liquidity-response
  conditional baseline，严格拆分 discovery freeze 与已消耗数据的
  post-selection evaluation。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- `0801T006` QA `已通过`。

规则更新提醒：
- 请先阅读 workflow 规则和 Goal 3 修复计划。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `docs/hyperliquid_liquidity_response_case_hierarchy_plan.md`

是否需要提交代码：
- 不需要

提交要求：
- 不需要提交
- 回报写 `commit：无`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `examples/hyperliquid/cross_exchange_liquidity_response_baseline_v2.py`
- `examples/hyperliquid/test_cross_exchange_liquidity_response_baseline_v2.py`
- `local_live_analysis/skhynix_liquidity_response_case_hierarchy/baseline_v2/`
- `.workflow/tasks/0801T007.md`
- `.workflow/reports/0801T007-business.md`
- `.workflow/reports/0801T007-qa.md`
- `docs/qa-acceptance-report.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

action：
- 提供独立 `discovery-freeze` 和 `post-selection-evaluate` 命令。
- discovery 命令只允许打开 M1 `segment_0001-0003` episode/timeline 文件；
  不得打开 `0004-0008` 数据文件。
- 从 discovery M1 rows 重建已冻结 Episode v2，并与 T006 discovery atom
  count/SHA 对账。
- 构建 pre-state、shock path、intervening flow 和 data-quality features。
- response targets 覆盖 Hyperliquid spread、queue ratios、fast top-5
  response、latency 和 directional midpoint response。
- adverse markout 仅作为独立 evaluation outcome，不进入 residual feature
  allowlist。
- 实现 matched-neighbor evidence/label interval purge、ACF embargo、
  `30/60/120s` sensitivity、minimum `50`/maximum `200` neighbor contract。
- 实现冻结参数的 quantile HistGradientBoosting，并仅以 discovery
  leave-one-segment-out CV 选择 official baseline。
- residual scale floor 为 discovery target MAD 的 `10%`。
- freeze contract 绑定 discovery inputs、features、targets、models、metrics
  和 outputs SHA。
- `0004-0008` 评估标签固定为 `post_selection`；任何输出不得标记正式
  `held_out`。
- R2 matched-neighbor 的 query/candidate interval 右端均固定为
  `max(episode_end, outcome_known_from)`，再施加冻结 embargo。
- R2 discovery contract、manifest、input provenance 和 consumption
  manifest 必须执行 exact-key、exact-cardinality、actual-row-count 和
  actual-SHA 闭包，未知/缺失字段 fail closed。
- consumption manifest 必须在 outcome 文件首次读取前持久化 intent，并为
  1+5+5 输入冻结 `row_count`；持久化后立即扫描十份 CSV 校验实际行数，
  失败时保留 consumed 事实但不得发布 post-selection manifest。
- `baseline_manifest.json` 必须冻结 unavailable-query count 与实际
  neighbor-count distribution。

verify：
- focused tests 覆盖 held-out path open guard、split drift、embargo overlap、
  HGB selection discovery-only、MAD floor、adverse feature rejection、
  frozen-contract drift和 post-selection 标签。
- `ruff`、`py_compile`、CLI help。
- 真实 discovery freeze 与 post-selection evaluation。
- 独立复核 open-file audit、CV/model selection、neighbor eligibility、
  prediction/calibration rows、output SHA 和重复构建确定性。
- `git diff --check`。

done：
- `baseline_v2` discovery contract 和 post-selection artifacts 独立发布。
- 不生成正式 held-out 验收结论。
- 业务报告进入 `待验收`，独立 QA 通过后才解锁 T008 motif repair。
