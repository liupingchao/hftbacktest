```md
执行线程：
- 测试线程

任务ID：
- 0519T009

标题：
- 5-19-day-control-30min current-format T006 audit collection and T008 rerun

简短描述：
- 采集一次 30min no-rule / default-off current-format live control 样本 `5-19-day-control-30min`，目标是补齐 T006 quote-update audit 字段证据。拉回后做 audit replay / maker acceptance，并在新数据集上重跑 Step 9B quote-adjustment offline runner。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- `0519T006` QA 已通过
- `0519T007` QA 已通过
- `0519T008` business-thread 已完成并发现旧样本缺少 T006 字段；本任务用于补 current-format 样本证据

规则更新提醒：
- 本任务允许启动一次 no-rule / default-off live 数据采集，但不允许策略推广、不允许启用新规则、不允许改变 quote 行为、不允许把单样本结果解释为 live readiness。
- 采集 dataset 名固定为 `5-19-day-control-30min`，不要使用带空格的目录名。
- 若 preflight 或早期 audit header 检查发现 T006 字段缺失，必须停止采集并回报阻塞。

按线程规则执行：
- `.workflow/workflow-kit/thread-report-template.md`
- `docs/thread-playbook.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 采集前提交/推送可部署 commit；采集、拉回、重跑、报告完成后提交 workflow/report/artifacts。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

Allowed files：
- `.workflow/tasks/0519T009.md`
- `.workflow/reports/0519T009-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `.workflow/dashboard.html`
- `.workflow/dispatch_suggestions.md`
- `docs/qa-acceptance-report.md`
- `local_live_analysis/5-19-day-control-30min/**`
- `local_live_analysis/archive/5-19-day-control-30min.tar.gz`
- `local_live_analysis/archive/5-19-day-control-30min.tar.gz.sha256`

Allowed read-only inputs：
- `.workflow/tasks/0519T006.md`
- `.workflow/reports/0519T006-qa.md`
- `.workflow/tasks/0519T008.md`
- `.workflow/reports/0519T008-business.md`
- `examples/binance_tick_mm/deploy/run_live.sh`
- `examples/binance_tick_mm/deploy/preflight_live_run.py`
- `examples/binance_tick_mm/audit_schema.py`
- `examples/binance_tick_mm/strategy_core.py`
- `examples/binance_tick_mm/live_tick_mm.py`
- `examples/binance_tick_mm/backtest_tick_mm.py`
- `examples/binance_tick_mm/align_live_run.py`
- `examples/binance_tick_mm/maker_acceptance.py`
- `examples/binance_tick_mm/quote_adjustment_replay.py`
- `local_live_analysis/5-13-day-control-30min/**`

Allowed remote paths：
- `awsserver1:/home/admin/hft_live/**`

Forbidden files / areas：
- Strategy rule logic, fair/target formula, risk guard behavior, quote placement behavior, production defaults.
- Core/connector APIs, standard hftbacktest npz schema, canonical data conversion.
- Any live promotion, default-on candidate, sample-expansion claim, Step 5C promotion, queue/touch repair, inventory-control implementation.
- Any current-format sample result interpreted as generalized profitability proof.

Requires separate task：
- Enabling any quote-adjustment candidate.
- Running multi-sample validation or a second current-format sample.
- Promoting Step 9B candidates beyond default-off offline diagnostics.
- Repairing queue/touch residuals or anchor source drift.

Fatal Failures：
- 未通过 T004/T006 preflight 仍启动 live。
- 采集开始后发现 15 个 T006 字段缺失仍继续跑满 30min。
- 启用非 baseline/no-rule control 策略规则。
- 把本次采集或 T008 rerun 解释为 live readiness、promotion 或 generalized PnL proof。
- 修改生产策略行为或远端生产默认配置。

action：
- 本地确认当前 `strategy_core.AUDIT_FIELDS` 包含全部 15 个 T006 quote-update 字段。
- 本地运行 focused pre-deploy checks：
  - `python -m pytest examples/binance_tick_mm/test_deploy_preflight.py`
  - `python -m pytest examples/binance_tick_mm/test_quote_adjustment_replay.py`
  - `python examples/binance_tick_mm/deploy/preflight_live_run.py --help`
  - `python examples/binance_tick_mm/align_live_run.py --help`
  - `python examples/binance_tick_mm/maker_acceptance.py --help`
  - `python examples/binance_tick_mm/quote_adjustment_replay.py --help`
  - `bash -n examples/binance_tick_mm/deploy/run_live.sh`
- 推送部署 commit，并在 `awsserver1` 用 clean worktree 指向该 commit。
- 远端准备 run dir `/home/admin/hft_live/runs/5-19-day-control-30min/`。
- 使用 T004 `run_live.sh` 标准流程启动 no-rule / default-off control live 采集。
- 采集开始后做轻量 early audit header check：
  - 15 个 T006 字段必须出现在 live audit header。
  - 若缺失，停止采集并标记阻塞。
- 若 header 检查通过，继续跑满约 30min，保留 deployment manifest、start/stop marker、live audit、raw gzip、logs。
- 拉回到 `local_live_analysis/5-19-day-control-30min/`。
- 运行 audit replay / alignment：
  - `python examples/binance_tick_mm/align_live_run.py --run-id 5-19-day-control-30min --local-root local_live_analysis --remote-host admin@awsserver1 --remote-root /home/admin/hft_live`
- 运行 maker acceptance：
  - `python examples/binance_tick_mm/maker_acceptance.py --alignment-report local_live_analysis/5-19-day-control-30min/alignment_report_audit_replay.json --backtest-result local_live_analysis/5-19-day-control-30min/backtest_audit_replay_result.json --out local_live_analysis/5-19-day-control-30min/maker_acceptance.json`
- 在新样本上重跑 T008 runner：
  - `python examples/binance_tick_mm/quote_adjustment_replay.py --run-dir local_live_analysis/5-19-day-control-30min --output-dir local_live_analysis/5-19-day-control-30min/stage9b_quote_adjustment_replay_0519T009`
- 生成 archive 和 sha256。

verify：
- Local checks 全部通过，或报告明确 blocker。
- Remote preflight manifest:
  - `decision=preflight_passed`
  - `git.dirty=false`
  - `compatibility.passed=true`
  - `audit_field_count >= 159`
- Early audit header check:
  - all 15 T006 fields present.
- Live collection:
  - `start_marker.json` exists
  - `stop_marker.json` exists
  - duration approximately 30min unless explicit blocker
  - live audit and raw gzip exist
- Alignment / acceptance:
  - `align_live_run.py` exits 0
  - `maker_acceptance.py` exits 0 and writes `maker_acceptance.json`
- T008 rerun:
  - stage9b output directory exists under the new dataset
  - `candidate_summary.json` and `audit_field_coverage.csv` exist
  - `missing_t006_field_count` should be `0`; if not, explain exact missing fields
  - classification must be one of T008 allowed classes and must not be claimed as promotion
- Dashboard:
  - `python3 .workflow/build_dashboard.py`
  - `git diff --check`

done：
- 回报必须说明 dataset 名、deployed commit、run start/stop、preflight result、early T006 header check result、align/acceptance result、T008 rerun classification、archive sha256。
- 回报必须列出 15 个 T006 字段是否全部覆盖。
- 回报必须明确这是 no-rule / default-off control collection + offline audit rerun，不是策略收益证明，不授权 live promotion。
```
