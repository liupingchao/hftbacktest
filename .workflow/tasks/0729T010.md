# 任务派发

执行线程：
- 业务线程-python/public-data-infra

任务ID：
- 0729T010

标题：
- SKHYNIX-4H-CLOUD-COLLECTION-SOAK

简短描述：
- 在保留的 c6in winner 上启动一次 4 小时 SKHYNIX 双交易所 public-data
  采集，使用已验收的 supervisor 和严格质量门禁。
- 采集按 8 个连续 30 分钟 segment 执行，由 systemd 托管。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0729T009` 已通过。

规则更新提醒：
- 无

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 不需要

提交要求：
- 本任务只启动正式采集并记录运行证据，不创建混合提交。
- 回报使用 `commit：无`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 采集结束、数据回传并完成完整质量检查后再进行正常验收。

files：
- `.workflow/tasks/0729T010.md`
- `.workflow/reports/0729T010-business.md`
- remote output:
  `/home/admin/0729T010_skhynix_4h_8x30m`
- `task_plan.md`
- `progress.md`
- `findings.md`

action：
- Host: `c6in-winner / i-0a962e47210528526`.
- Profile: `skhynix`.
- Binance: `SKHYNIXUSDT`.
- Hyperliquid: `xyz:SKHX`, retaining research-max fast and standard L2 tracks.
- Total duration: `14400s`.
- Segment duration: `1800s`.
- Segment count: `8`.
- Start as systemd unit
  `hftbacktest-0729t010-skhynix-4h.service`.
- Do not stop or mutate `awsserver1`.
- Public market data only; no private/account/order endpoints.

verify：
- Preflight disk, memory, sudo, conflicting process and unit state.
- Verify the four runtime source SHA-256 values match the accepted T009 source.
- Verify the systemd start request succeeds and the unit enters active state.
- After four hours, separately inspect terminal state, all eight segment
  manifests, strict quality gates, timelines, hashes and local pullback.

done：
- All eight collection windows completed, but the campaign failed closed while
  post-processing `segment_0002`.
- The strict failure is limited to one reconnect each on Hyperliquid
  `asset_context` and `main_all_mids`.
- The task does not claim a successful campaign and the data was not pulled
  locally under the user's success-only transfer condition.
