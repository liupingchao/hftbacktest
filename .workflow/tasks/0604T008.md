```md
执行线程：
- 业务线程-research

任务ID：
- 0604T008

标题：
- Repair T006 signal ranking report bucket consistency

简短描述：
- 基于 `0604T006` QA 未通过结论，窄修复 `canonical_signal_quality_ranking.py` 的 report generation：`Controller Interpretation Check` 必须与 `signal_quality_ranking.csv` 的 `final_bucket` 保持一致，避免静态文案把 watch feature 写成 keep。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0604T006` QA 未通过。

规则更新提醒：
- 请先阅读规则文档后再执行本任务。
- 本任务只修复 T006 report 文案/生成逻辑一致性和刷新 task-scoped report artifact；不得改 ranking scoring、allowlist、source-lock guard、canonical loader、regime selection、strategy/private/order/live/default-on/tiny-live/parameter search/promotion。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `docs/thread-playbook.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交任务文件、业务报告、代码修改、刷新后的 T006 task-scoped report artifact 和必要 workflow tracking 更新。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0604T008.md`
- `.workflow/reports/0604T008-business.md`
- `examples/hyperliquid/canonical_signal_quality_ranking.py`
- `examples/hyperliquid/test_canonical_signal_quality_ranking.py` only if needed to lock report consistency behavior.
- `local_live_analysis/canonical_signal_quality_ranking_0604T006/signal_quality_ranking_report.md`
- `local_live_analysis/canonical_signal_quality_ranking_0604T006/signal_quality_ranking_manifest.json` only if regenerated by the runner.
- `local_live_analysis/canonical_signal_quality_ranking_0604T006/signal_quality_ranking.csv` / `signal_quality_reject_watch_list.csv` only if regenerated unchanged by the runner.
- 必要 workflow tracking 文件仅限本任务状态/结论更新。

read-only inputs：
- Formal canonical source:
  - `local_live_analysis/event_mode_canonical_pricing_signal_0604T003/**`
- T006 ranking artifacts:
  - `local_live_analysis/canonical_signal_quality_ranking_0604T006/**`
- T006 QA failure source:
  - `.workflow/reports/0604T006-qa.md`
  - `docs/qa-acceptance-report.md`

forbidden：
- Ranking scoring changes.
- Primary allowlist changes.
- `0604T004` canonical loader changes.
- `0604T005` source-lock guard changes.
- New data collection.
- Regime selection, case-library construction, shadow decision generation.
- Hyperliquid or Binance private/account/order endpoints.
- Order lifecycle, submit/cancel/fill logic.
- Strategy implementation, production config/default changes, parameter search, default-on, tiny-live, promotion.
- Connector/core API changes, standard npz schema changes, canonical Binance maker audit schema changes.

action：
- Fix `examples/hyperliquid/canonical_signal_quality_ranking.py` report generation so `Controller Interpretation Check` is derived from `ranking_rows` or otherwise mechanically tied to each feature's actual `final_bucket`.
- Ensure `binance_top5_imbalance` is described consistently with its actual `watch_regime_dependent` bucket unless the ranking CSV itself changes for a justified reason.
- Prefer adding or adjusting a focused test that fails if the generated report says a watch/reject feature is kept for read-only research.
- Regenerate the T006 ranking artifacts by running the existing T006 runner on `local_live_analysis/event_mode_canonical_pricing_signal_0604T003/` and writing `local_live_analysis/canonical_signal_quality_ranking_0604T006/`.
- Keep all outputs as read-only research evidence only.

acceptance criteria：
- Generated `signal_quality_ranking_report.md` contains no keep/watch/reject contradiction with `signal_quality_ranking.csv`.
- For every feature in `signal_quality_ranking.csv`, the report's interpretation text matches that feature's actual `final_bucket`.
- `binance_top5_imbalance`, `binance_top5_bid_qty`, and `binance_microprice_minus_mid_ticks` remain described as watch/regime-dependent if their CSV bucket remains `watch_regime_dependent`.
- Ranking order, scoring, allowlist, source-lock guard behavior, and boundary flags are unchanged unless a clearly documented no-op regeneration timestamp/commit metadata change occurs.
- No new collection, regime selection, strategy/private/order/live/default-on/tiny-live/parameter search/promotion is introduced.

verify：
- `python examples/hyperliquid/canonical_signal_quality_ranking.py --help`
- `python -m py_compile examples/hyperliquid/canonical_signal_quality_ranking.py examples/hyperliquid/test_canonical_signal_quality_ranking.py`
- `python -m pytest examples/hyperliquid/test_canonical_signal_quality_ranking.py`
- `python examples/hyperliquid/canonical_signal_quality_ranking.py --input-dir local_live_analysis/event_mode_canonical_pricing_signal_0604T003 --output-dir local_live_analysis/canonical_signal_quality_ranking_0604T006`
- `python -m json.tool local_live_analysis/canonical_signal_quality_ranking_0604T006/signal_quality_ranking_manifest.json`
- Manually or scripted check that `signal_quality_ranking_report.md` interpretation text matches `signal_quality_ranking.csv` buckets.
- `git diff --check`

done：
- Business report must include the exact QA failure fixed, code/report artifact paths changed, verification commands and outputs, whether ranking CSV semantics changed or remained unchanged, blockers/caveats if any, commit id, and commit message.
- Business report must explicitly state this task only fixes T006 report consistency and does not authorize scoring changes, regime selection, strategy implementation, private/order endpoints, live/default-on/tiny-live, parameter search, or promotion.
- Business report should end in `待验收` if verification passes.
```
