# 任务派发

执行线程：
- 业务线程-python/offline-design

任务ID：
- 0714T001

标题：
- T011-PUBLIC-FLOW-INTERVAL-ARTIFACT-REPAIR-DESIGN-V2

简短描述：
- 在 `0713T003` QA 已接受 `route_to_public_flow_artifact_repair` 之后，新建一个单独的 public-flow interval artifact repair/design 任务。
- 本任务目标不是提高成交率、继续 live retry、调整阈值或设计 quote policy；只把“订单 resting 期间到底发生了什么”需要怎样记录说清楚，并产出可验收的离线 design artifact。

状态：
- 执行中

执行顺序：
- 当前唯一任务

前置任务：
- 0713T003 QA 已通过

规则更新提醒：
- 请先阅读规则文档后再执行本任务。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `docs/cross_exchange_resting_interval_public_flow_auto_loop_plan.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0714T001.md`
- `.workflow/reports/0714T001-business.md`
- `docs/qa-acceptance-report.md`
- `docs/cross_exchange_resting_interval_public_flow_auto_loop_plan.md`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `examples/hyperliquid/`
- `local_live_analysis/cross_exchange_quote_fill_probability_evidence_0713T003/`
- `local_live_analysis/cross_exchange_public_flow_interval_artifact_repair_design_0714T001/`

accepted input：
- Latest accepted QA:
  - `.workflow/reports/0713T003-qa.md`
  - `docs/qa-acceptance-report.md`
- Accepted evidence package:
  - `local_live_analysis/cross_exchange_quote_fill_probability_evidence_0713T003/`

action：
- Offline only. Use accepted local `0713T003` artifacts and QA report as source of truth.
- Build a deterministic repair/design runner if needed.
- Produce a design package that states:
  - what exact resting interval lifecycle fields are missing or proxy-only
  - what public trade/depletion/depth fields must be captured per attempt
  - how to distinguish `no public trades observed in artifact` from `exchange had no trades`
  - how future artifacts should prove interval coverage before quote/fill probability evidence can advance
  - which route/gates remain blocked until interval evidence is exact or explicitly bounded
- Generate output package under:
  - `local_live_analysis/cross_exchange_public_flow_interval_artifact_repair_design_0714T001/`
- Generate at minimum:
  - artifact gap matrix
  - required artifact contract JSON
  - instrumentation design matrix
  - acceptance gate matrix
  - source package manifest
  - boundary manifest
  - validation report
  - machine-readable final route enum

hard boundary：
- No live-submit.
- No live retry.
- No remote/AWS execution.
- No credential reads.
- No private/account/order/cancel endpoint calls.
- No new market-data collection.
- No threshold change.
- No quote-envelope change.
- No order-size or max-submission change.
- No strategy behavior change.
- No quote policy design.
- No fill-probability, queue-priority, fee/rebate, realized-PnL, maker-viability, T012, promotion, or final MVP claim.
- Do not treat proxy-only lifecycle/depth as exact exchange resting timestamp, exact queue position, or queue-priority proof.
- Do not interpret zero captured interval public-trade rows as proof that no exchange public trades occurred.

allowed final route enums：
- `route_to_resting_interval_capture_contract_repair`
- `route_to_controlled_same_envelope_live_evidence_with_resting_interval_public_flow_artifacts`
- `stop_for_human_strategy_decision`

route restrictions：
- `route_to_controlled_same_envelope_live_evidence_with_resting_interval_public_flow_artifacts` is allowed only if this design package defines the exact artifact contract and acceptance gates needed by the next controlled collection.
- `route_to_resting_interval_capture_contract_repair` is required if code/schema instrumentation work is still needed before the next controlled collection.
- `stop_for_human_strategy_decision` is required if the repair/design cannot separate actual interval evidence from proxy artifacts.

verify：
- Focused pytest for any new/changed runner.
- `python -m py_compile` for any new/changed runner.
- Runner `--help` if a runner is added.
- Run against accepted local `0713T003` artifacts.
- JSON/CSV parse and row-count checks for generated artifacts.
- Deterministic rerun check if generated artifacts are written.
- Boundary manifest confirms offline-only and no endpoint/live/parameter changes.
- `git diff --check`.

done：
- Report generated artifact paths, row counts, parse checks, final route enum, blockers, and unsupported claims.
- Explicitly state the next task type recommended by this design.
- Confirm no threshold/quote-envelope/live retry behavior occurred.
