```md
执行线程：
- 业务线程-live

任务ID：
- 0622T002

标题：
- M2 time-boxed public watcher with gated maker live trigger

简短描述：
- 在 `0622T001` fresh-touch / dynamic-size / session-gate 基础上，实现并执行一个限时 public-only L2/trades watcher：先等待合格盘口窗口，只有当前窗口满足既有 fresh-touch quality gate 时，才触发一次受限 maker-only live micro-window。
- 本任务目标是用尽量少的任务继续推进 M2 live calibration：要么拿到 live maker fill + T008 fee/inventory/realized PnL 证据，要么证明在限时时间内没有合格盘口窗口；不得通过 taker、crossing、放宽 cap 或绕过 T008 来强行完成。

状态：
- 阻塞

执行顺序：
- 当前唯一任务

前置任务：
- `0622T001` business execution 为 `待验收`，其 fresh-touch implementation / fail-closed artifacts 是本任务输入。
- `0618T008` QA 为 `已通过`，T008 ledger/reconciler 必须继续作为 M2 PnL 事实源。
- 如果总控要求严格恢复默认链路，本任务执行前应先完成 `0622T001` QA；若总控明确继续推进，则本任务仍必须保留 `0622T001` 的安全边界，不得放宽。

规则更新提醒：
- 请先阅读 `.workflow/workflow-kit/workflow-manual.md`、`.workflow/workflow-kit/task-dispatch-template.md`、`.workflow/workflow-kit/thread-report-template.md`、`.workflow/workflow-kit/qa-acceptance-template.md`、`task_plan.md`、`progress.md`、`findings.md` 后再执行本任务。

按线程规则执行：
- `AGENTS.md`
- `docs/hyperliquid_tiny_live_real_order_executor.md`
- `docs/hyperliquid_tiny_live_final_go_no_go_gate.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `examples/hyperliquid/hyperliquid_tiny_live_m2_fill_window.py`
- `examples/hyperliquid/hyperliquid_tiny_live_m2_fill_loop.py`
- `examples/hyperliquid/test_hyperliquid_tiny_live_m2_fill_loop.py`
- 可新增一个窄范围 watcher entrypoint，例如 `examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- 可新增对应 focused tests，例如 `examples/hyperliquid/test_hyperliquid_tiny_live_m2_public_watcher.py`
- `local_live_analysis/hyperliquid_tiny_live_m2_timeboxed_watcher_0622T002/**`
- `.workflow/reports/0622T002-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

read-only inputs：
- `.workflow/tasks/0622T001.md`
- `.workflow/reports/0622T001-business.md`
- `local_live_analysis/hyperliquid_tiny_live_m2_fresh_touch_live_0622T001/**`
- `local_live_analysis/hyperliquid_tiny_live_m2_pnl_ledger_0618T008/**`

hard boundary：
- Watcher phase is public-only: L2 / trades / public stream only. It must not read credentials, call private/account/order endpoints, submit orders, cancel orders, or start a live bot during public waiting.
- Live trigger is allowed only after local focused verification passes, remote checkout refresh succeeds, final gate returns go, watcher observes an eligible current-window candidate, and the existing fresh-touch quality gate permits submission.
- Keep Hyperliquid `Alo` post-only. No taker/crossing, no `Ioc`, no one-tick-back workaround, no cap relaxation, no default-on behavior, no promotion, no stable PnL claim, and no M3 readiness claim.
- Live execution is limited to one triggered micro-window and at most `2` real order submissions. Dynamic order size hard cap remains `<=0.005 BTC`; smaller size is allowed and preferred when throughput is low.
- Preserve tracked cancel, final open-orders proof, independent remote open-orders check, redaction, and T008 ledger fail-closed.
- Do not print or persist credential values, private keys, raw signatures, nonces, wallet secrets, or unredacted account identifiers.

implementation action：
- Add a time-boxed public watcher mode around the existing `0622T001` fresh-touch / dynamic-size / session-gate logic.
- Default watcher runtime should be configurable, with a controller-facing default in the `30-90 minute` range; recommended default is `60 minutes` unless runtime constraints require a shorter explicitly recorded value.
- The watcher must continuously evaluate public L2/trades candidates without submitting orders and record:
  - same-side top depth multiple
  - same-side top order count
  - touch freshness / queue-reset evidence
  - side
  - quote age / BBO drift status
  - strict-through and touch-trade support
  - recent same-side at-or-through trade throughput
  - dynamic size decision
  - candidate allowed / rejected reason
  - timestamp/session bucket
- Trigger exactly one live micro-window only when the same gate that would permit `quality_a` or `quality_b` in `0622T001` returns allowed for a current candidate.
- Carry the selected candidate context into the triggered live window artifact so QA can prove why this order was allowed.
- If the watcher times out with no eligible candidate, do not submit any order; emit a no-eligible-window report and end `阻塞`.
- If a live trigger occurs but no fill or incomplete economics/inventory evidence appears, run T008 and end `阻塞`.
- If at least one live maker fill occurs and T008 proves fee/inventory/mark/realized PnL evidence, report exact facts without claiming stable PnL.

required artifacts：
- `watcher_manifest.json`
- `public_stream_summary.json`
- `candidate_window_log.csv`
- `trigger_decision_matrix.csv`
- `watcher_no_eligible_window_report.md` when no trigger occurs
- selected-candidate context copied into triggered window artifacts when a trigger occurs
- `touch_freshness_matrix.csv`
- `dynamic_size_decision_matrix.csv`
- `session_side_eligibility.csv`
- `time_gate_decision_matrix.csv`
- `quote_attempt_matrix.csv`
- `window_result_matrix.csv`
- final gate manifest and remote facts
- aggregate live fill ledger
- T008 ledger manifest/output
- independent remote open-orders proof

live execution rules：
- Run focused local tests and CLI checks before any remote/live-capable step.
- Refresh `/home/admin/hftbacktest-cross-exchange` by git-safe incremental bundle / `merge --ff-only`; fail if branch is not `cross-exchange` or dirty count is nonzero.
- Rerun final gate; stop before watcher/live if `allow_create_0617T008 != true`.
- Run the watcher on `awsserver1` using the refreshed checkout and configured Python path.
- During watcher phase, only public L2/trades collection is allowed.
- If watcher finds an eligible current-window candidate, run exactly one controlled live micro-window under the existing `fresh_touch` policy.
- Always tracked-cancel live orders and prove final open orders are empty.
- Always run T008 ledger after pullback; no fill or incomplete economics/inventory evidence must return fail-closed.

expected outcomes：
- `已通过` is possible only if the task implementation and QA criteria pass and either:
  - T008 proves live maker fill + fee/inventory/mark/realized PnL evidence; or
  - QA accepts that the task correctly produced complete no-eligible-window evidence under the bounded watcher contract.
- M2 completion is possible only in the first case: live maker fill plus complete T008 PnL evidence.
- If no eligible window appears, if no order fills, if final gate fails, if watcher collection fails, if open-orders proof fails, or if T008 cannot prove realized live PnL, report `阻塞` for M2 and do not enter M3.

verify：
- `python -m pytest examples/hyperliquid/test_hyperliquid_tiny_live_m2_fill_loop.py -q`
- If a new watcher test file is added: `python -m pytest examples/hyperliquid/test_hyperliquid_tiny_live_m2_public_watcher.py -q`
- `python -m pytest examples/hyperliquid/test_hyperliquid_tiny_live_m2_pnl_ledger.py -q`
- `python -m py_compile examples/hyperliquid/hyperliquid_tiny_live_m2_fill_window.py examples/hyperliquid/hyperliquid_tiny_live_m2_fill_loop.py`
- If a new watcher entrypoint is added: `python -m py_compile examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- `python examples/hyperliquid/hyperliquid_tiny_live_m2_fill_window.py --help`
- `python examples/hyperliquid/hyperliquid_tiny_live_m2_fill_loop.py --help`
- If a new watcher entrypoint is added: `python examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py --help`
- `git diff --check`
- If watcher/live reaches remote execution: remote refresh evidence, final gate go/no-go, watcher artifacts, pulled-back live artifacts if triggered, independent remote open-orders check, and T008 ledger output.

done：
- 回报 watcher runtime、public message counts、eligible candidate count、trigger count、是否进入 live、live submissions 数、fill_count / maker_fill_count、T008 ledger 结论、artifact 路径、blockers、commit id、提交信息。
- 明确说明 M2 是否仍 blocked；除非 T008 ledger 证明 live fill / fee / inventory / realized PnL，否则不得声明 M2 完成、稳定 PnL、或 M3 ready。
```
