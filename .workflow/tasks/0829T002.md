# 任务派发

执行线程：
- SKHYNIX Fresh-Channel Consensus M-State V1 A-1 业务线程

任务ID：
- 0829T002

标题：
- 注册并审查 precision-first fresh-channel consensus M-state A-1

简短描述：
- 注册一个减少同步完整路径依赖、但不降低三通道方向阈值的新 M-state
  hypothesis。trade、depletion、OFI 使用带严格 TTL 的独立 causal
  memory，只有 fresh three-channel consensus 才能形成 alignment anchor。

状态：
- 待验收

执行顺序：
- 当前唯一 formal task

前置任务：
- 0829T001（QA 已通过；科学分类
  `Aminus1_structural_support_not_estimable`）

规则更新提醒：
- 已阅读 AGENTS.md、workflow-kit、task_plan.md、progress.md、
  findings.md、conditional-risk methodology kernel 和 0829T001
  plan/execution/QA。

按线程规则执行：
- AGENTS.md
- .workflow/workflow-kit/workflow-manual.md

是否需要提交代码：
- 需要

提交要求：
- 先提交 candidate plan。
- 独立 plan review 达到 `P0/P1/P2/P3=0/0/0/0` 前禁止运行 29-cache
  audit。
- 后续实现、执行和 QA 必须另行形成 Git 历史。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- docs/skhynix_binance_precision_first_fresh_channel_consensus_mstate_v1_a_minus1_audit_plan_20260829.md
- examples/hyperliquid/skhynix_fresh_channel_consensus_mstate_a_minus1.py
- examples/hyperliquid/test_skhynix_fresh_channel_consensus_mstate_a_minus1.py
- local_live_analysis/skhynix_fresh_channel_consensus_mstate_a_minus1_0829T002/
- .workflow/tasks/0829T002.md
- .workflow/reports/0829T002-*.md

action：
- 冻结 channel observation、fresh-memory、M-state、anchor 和 filter family。
- 保持 `cost(FP) >> cost(FN)`、三通道一致、null-only selection 和
  outcome-blind boundary。
- 先完成独立合同 review；通过前不得执行数据。

Plan review history：
- Round 1：`P0/P1/P2/P3=0/4/2/0`，未通过。
- Revision 2 将 freshness 绑定到底层 channel contribution event，
  冻结 conditional H0/full null recomputation、zero precedence、raw
  estimator、source/callable identities 和 orphan strict-onset diagnostics。
- Data execution lock 保持。
- Round 2：`P0/P1/P2/P3=0/1/2/0`，未通过。
- Revision 3 将四个 channel contribution magnitudes 明列为 permitted
  causal values；非法 source contribution 唯一失败于 A-1-0；三个
  new-evidence masks 成为 checkpoint-exact null invariants，并冻结
  channel-state evidence schema。
- Data execution lock 继续保持。
- Round 3：`P0/P1/P2/P3=0/1/0/0`，未通过。
- Revision 4 将 `trade_signed` 和 `ofi` 的 finite preflight 加入 A-1-0；
  完整 source preflight 后的任何 `NEW_INVALID` action 唯一失败于
  A-1-2，不得成为普通 abstention。
- Data execution lock 继续保持。
- Round 4：`P0/P1/P2/P3=0/0/0/0`，已通过。
- Frozen reviewed plan SHA256：
  `9f214893d7d4ef0431efaf6641c52c84d2fe4f0b516897818b92671355343de5`。
- Outcome-blind 29-cache A-1 execution lock 已解除。
- Future outcome、A0 execution 和 live trading 仍禁止。

verify：
- Plan review 检查 state semantics、causality、null、exposure、
  selection、gates、classification、Required Outputs 和 hostile tests。
- Review 未通过时只修改合同，不运行研究数据。

done：
- 形成可审查、可复现的新 hypothesis/version。
- 明确 execution lock、outcome lock 和下一权限边界。

明确禁止：
- 不放宽 0.50/0.25 directional thresholds。
- 不使用 2-of-3 quorum。
- 不把 UNKNOWN 当 background。
- 不根据 observed candidate count 选 TTL/filter。
- 不读取 future price、target、fill、fee 或 PnL。
- 不运行 live/private/order API。
