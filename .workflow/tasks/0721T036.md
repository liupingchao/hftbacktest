# 0721T036 TOTAL-METHOD-HISTORICAL-ENVELOPE-REPAIR

执行线程：
- 总控 auto-loop / 业务执行线程

任务ID：
- 0721T036

标题：
- Make raw method parsing total and validate the complete historical result envelope

简短描述：
- 修复 T035 QA 的两个 P1：任意 hostile JSON `method` 都必须 fail closed 而不能抛异常。
- Historical method/result coherence 必须覆盖完整 `result.status/orders/row` envelope，不能只检查 status 标签。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- `0721T035`（未通过）

规则更新提醒：
- 本任务 offline-only。
- T031 live artifacts immutable。
- T033-T035 已接受的 exact timing/caps、raw semantic trigger、malformed attempt handling、manager/quarantine、Task 12 layering、legacy compatibility 和历史边界不得回退。
- 独立 QA 通过前不得进行 private read、新 live 或任何 adaptive/multi-level activation。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `.workflow/reports/0721T035-qa.md`

是否需要提交代码：
- 需要

提交要求：
- 业务实现和业务报告分开提交。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

files：
- Modify: `examples/hyperliquid/hyperliquid_tiny_live_m2_fill_window.py`
- Modify: `examples/hyperliquid/cross_exchange_t024_same_window_acceptance.py`
- Test: `examples/hyperliquid/test_hyperliquid_tiny_live_m2_fill_attribution.py`
- Test: `examples/hyperliquid/test_cross_exchange_t024_same_window_acceptance.py`
- Workflow: task、business、QA、fixed QA、`task_plan.md`、`progress.md`、`findings.md`

action：
- Parse raw method only when it is an exact string; never apply set membership to list/dict/container values。
- Keep raw historical semantic detection total over arbitrary dict and non-dict rows。
- Define historical result envelope coherence: result dict、exact historical status、orders list and structurally valid order rows。
- Treat missing/null/bool/scalar/dict `orders` and non-dict/missing-shape order rows as explicit method/result envelope mismatch。
- Preserve an empty `orders=[]` as a structurally valid unknown historical response。
- Keep strict reference classification and terminal-proof requirements separate from basic envelope coherence。
- Ensure producer、independent helper and Task 12 return fail-closed artifacts instead of exceptions。

verify：
- Method values `[]`、`{}`、nested containers and other unhashable JSON values fail producer/independent identically without exception。
- Task 12 T036+ container method returns blocked manifest and exit `2` rather than crashing。
- Historical result missing `orders` or using null/bool/number/string/dict orders fails explicitly。
- Historical `orders=[null/list/scalar/{missing order/status}]` fails explicit envelope coherence。
- Empty historical `orders=[]` remains structurally valid and classifies terminal status as unknown without false terminal proof。
- Valid exact/foreign historical rows retain T024 classification behavior。
- T035 raw semantic trigger、malformed attempt and method/result label mismatch tests remain pass。
- Consistent legacy direct-only v4 remains pass。
- Focused/full Hyperliquid regression passes。
- T031 blocked、T026 pass、T016/T022 blocked。
- No live/private/network/remote/service operation occurs。

stop conditions：
- Any weakening of T033 exact timing、manager or quarantine contracts。
- Any weakening of T034/T035 marker/raw semantic triggers or Task 12 layering。
- Any conversion of valid empty historical response into terminal proof。
- Any live executor timing、endpoint or strategy behavior change。
- Any historical evidence modification。
- Any live/private/network/remote/service action。

done：
- Raw method and historical result envelope parsing are total over hostile JSON。
- Malformed evidence yields explicit, reproducible producer/independent failure and a blocked Task 12 manifest。
- Source is ready for independent QA and only then a fixed-quote observe-only live task。

blockers：
- 无。

commit：
- implementation `ba379819964a31d3e763586440a16c66185ec9d2`

提交信息：
- implementation `Make history envelope parsing fail closed`
