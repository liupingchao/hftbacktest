```md
执行线程：
- 业务线程-python

任务ID：
- 0529T003

标题：
- Hyperliquid read-only raw sample / converter / sidecar validation

简短描述：
- 按 `0528T003` 的设计推荐，实现 Hyperliquid 第一阶段只读 market-data alignment：围绕 `l2Book + trades` raw sample、现有 Hyperliquid converter、top-N sidecar 和 market-view validation 产出最小证据链。本任务不实现交易 connector、不接私有生命周期、不下单/撤单、不启动策略 live。

状态：
- 待验收

执行顺序：
- 当前唯一任务；正式派发后执行。

前置任务：
- `0528T003` 已产出设计文档和业务报告；正式执行前以 `0528T003` QA 结果和 `docs/hyperliquid_live_replay_alignment_design.md` 为准。
- `0529T002` 是 Binance fill-quality 方向的独立候选任务；本任务不得修改或依赖 `0529T002`。

规则更新提醒：
- 请先阅读规则文档后再执行本任务。
- 本任务是 Hyperliquid read-only market-data alignment，不是交易逻辑实现、private connector、order lifecycle、strategy port、parameter search、tiny-live 或 promotion 任务。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `docs/thread-playbook.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交任务文件、实现代码、测试、只读 alignment artifacts、业务报告和必要 workflow/dashboard 更新。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

Allowed files：
- `.workflow/tasks/0529T003.md`
- `.workflow/reports/0529T003-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `.workflow/dashboard.html`
- `.workflow/dispatch_suggestions.md`
- `examples/hyperliquid/**`
- `py-hftbacktest/hftbacktest/data/utils/hyperliquid.py`，仅限 converter wrapper / validation / provenance 支持，不得改变标准 npz schema。
- `collector/src/hyperliquid/**`，仅限只读 public market-data collection metadata preservation；不得加入 private/trading behavior。
- `collector/src/main.rs`，仅限 Hyperliquid read-only collector 参数或 metadata 接线所必需的最小改动。
- `local_live_analysis/hyperliquid_raw_alignment_0529T003/**`

Allowed read-only inputs：
- `docs/hyperliquid_live_replay_alignment_design.md`
- `.workflow/tasks/0528T003.md`
- `.workflow/reports/0528T003-business.md`
- `.workflow/reports/0528T003-qa.md` if available
- Official Hyperliquid docs, which must be rechecked before relying on schema facts:
  - `https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api`
  - `https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket`
  - `https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket/subscriptions`
  - `https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint`
  - `https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/tick-and-lot-size`
- Official SDK:
  - `https://github.com/hyperliquid-dex/hyperliquid-python-sdk`
- Existing local Hyperliquid sample:
  - `examples/hyperliquid/btcusd_20250126.gz`
- Existing Hyperliquid converter:
  - `py-hftbacktest/hftbacktest/data/utils/hyperliquid.py`
- Existing read-only collector code:
  - `collector/src/hyperliquid/**`
- Binance alignment files for pattern reference only:
  - `examples/binance_tick_mm/align_live_run.py`
  - `examples/binance_tick_mm/binance_top5_provenance.py`
  - `examples/binance_tick_mm/maker_acceptance.py`
  - `examples/binance_tick_mm/compare_audit.py`
  - `examples/binance_tick_mm/pipeline_live_raw.py`
- Optional local XEMM reference only:
  - `/home/molly/project/XEMM_CROSS_EXCHANGE_MARKET_MAKING_PACIFICA_HYPERLIQUID/**`

Forbidden files / areas：
- Strategy behavior, quote placement, fair/target formula, inventory logic, risk guards, production defaults, live configs.
- Hyperliquid private trading connector, signing, nonce handling, private order lifecycle, order submit/cancel, fills reconciliation, position/account state.
- Binance strategy changes or Binance acceptance-rule changes.
- Exchange-neutral refactor of `examples/binance_tick_mm/align_live_run.py`.
- Standard npz schema changes and canonical audit schema changes.
- Binance-specific sidecar gates copied into Hyperliquid validation, including `U/u/pu`, `lastUpdateId`, `bookTicker`, and Binance snapshot bootstrap semantics.
- Any trading live/tiny-live run, remote deploy, parameter search, default-on behavior, guard relaxation, or promotion claim.

Implementation scope：
1. Confirm the current official Hyperliquid schema facts for `l2Book`, `trades`, optional REST/info `l2Book`, and tick/lot precision before implementing validation assumptions.
2. Add a narrow CLI runner under `examples/hyperliquid/`, tentatively:
   - `python examples/hyperliquid/hyperliquid_raw_alignment.py --help`
   - accepts an existing raw gzip input and/or a task-scoped short read-only public market-data sample.
   - writes outputs under `local_live_analysis/hyperliquid_raw_alignment_0529T003/` by default or via `--output-dir`.
3. Use read-only public market data only:
   - required channels: `l2Book` and `trades`
   - no private keys
   - no account endpoints
   - no order submit/cancel
   - no strategy live process
   - if network collection is unavailable, use the existing local sample and report the collection blocker instead of fabricating current-sample evidence.
4. Produce or normalize a collection manifest with:
   - exchange, network, coin, channel set, source raw files
   - local start/end timestamps
   - WebSocket URL or local-sample provenance
   - session id / connection attempt / reconnect count when known
   - subscription ack presence or absence
   - recovery snapshot presence or absence
5. Convert raw data to hftbacktest `npz` through `py-hftbacktest/hftbacktest/data/utils/hyperliquid.py`.
6. Build minimal Hyperliquid provenance outputs:
   - `raw_provenance.csv`
   - `raw_to_npz_mapping.csv`
   - `topn_sidecar.csv`
   - `synthetic_joined_views.csv` or equivalent market-data-only as-of join when no Hyperliquid strategy audit exists
   - `metrics.json`
7. Top-N sidecar semantics:
   - reconstruct top-N from `l2Book` snapshot messages
   - include bid/ask px, size, and `n` order-count fields where present
   - include raw sequence, line number, channel, coin, event time, local receive time, session/reconnect fields when known
   - do not require Binance diff-depth continuity fields.
8. Market-view validation must classify the sample as one of:
   - `unusable`
   - `compressed_action_path_only`
   - `limited_pricing_research`
   - `passes_pricing_research_market_view`
9. Produce a short acceptance report explaining:
   - sample source and whether it was freshly collected or existing local input
   - raw parse / conversion / event-order result
   - `l2Book` and `trades` availability
   - top-N coverage
   - synthetic/as-of join coverage and join-age distribution
   - reconnect/session evidence limitations
   - why no private lifecycle, strategy, PnL, exact queue, or live-trading claim is made.

Required outputs：
- `local_live_analysis/hyperliquid_raw_alignment_0529T003/run_manifest.json`
- `local_live_analysis/hyperliquid_raw_alignment_0529T003/collection_manifest.json`
- `local_live_analysis/hyperliquid_raw_alignment_0529T003/converter_manifest.json`
- `local_live_analysis/hyperliquid_raw_alignment_0529T003/data.npz`
- `local_live_analysis/hyperliquid_raw_alignment_0529T003/raw_provenance.csv`
- `local_live_analysis/hyperliquid_raw_alignment_0529T003/raw_to_npz_mapping.csv`
- `local_live_analysis/hyperliquid_raw_alignment_0529T003/topn_sidecar.csv`
- `local_live_analysis/hyperliquid_raw_alignment_0529T003/synthetic_joined_views.csv` or documented equivalent
- `local_live_analysis/hyperliquid_raw_alignment_0529T003/metrics.json`
- `local_live_analysis/hyperliquid_raw_alignment_0529T003/acceptance_report.md`
- `.workflow/reports/0529T003-business.md`

Acceptance criteria：
- Runner is reproducible from CLI and can operate on an existing local Hyperliquid raw sample without private credentials.
- `l2Book` and `trades` raw messages are parsed, counted, and validated separately.
- Existing Hyperliquid converter produces non-empty `data.npz`, or the report gives a concrete blocker tied to input/schema/environment.
- Event-order validation is run through the converter path.
- Top-N sidecar and raw provenance are generated without Binance `U/u/pu`, `lastUpdateId`, or `bookTicker` assumptions.
- Market-data-only as-of join / synthetic decision validation reports join coverage, future joins, missing joins, and join-age distribution.
- Report clearly states whether the output is enough for `limited_pricing_research` or only lower classifications.
- Report explicitly states no strategy behavior, private connector, order lifecycle, live trading, parameter search, default-on behavior, guard relaxation, tiny-live, or promotion was changed or authorized.

verify：
- `python examples/hyperliquid/hyperliquid_raw_alignment.py --help`
- Focused tests if a runner is added:
  - `python -m pytest examples/hyperliquid/test_hyperliquid_raw_alignment.py -q`
- Converter static check:
  - `python -m py_compile py-hftbacktest/hftbacktest/data/utils/hyperliquid.py`
- Run the runner on `examples/hyperliquid/btcusd_20250126.gz` or on the task-scoped read-only raw sample.
- `python3 .workflow/build_dashboard.py`
- `git diff --check`
- No strategy live command.
- No remote deploy.
- No private key / order endpoint command.

done：
- Business report ends in `待验收`.
- Report lists exact official references checked, files read, code changed, input sample path, generated artifacts, and key metrics.
- Report includes the final sample classification and the next recommended task.
- Report includes `commit id` and `提交信息`.
- Report explicitly states:
  - no Binance strategy behavior changed
  - no Hyperliquid private connector was implemented
  - no order submit/cancel/fill lifecycle code was implemented
  - no trading live or remote deploy was run
  - no default-on, guard relaxation, parameter search, tiny-live, or promotion claim was made
```
