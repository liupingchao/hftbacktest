# 任务派发

执行线程：
- 业务线程-cross-exchange-research-design

任务ID：
- 0730T012

标题：
- SKHYNIX-CROSS-EXCHANGE-SIGNAL-RESEARCH-PLAN

简短描述：
- 基于已拉回的四小时 SKHYNIX 双交易所 public-data package，制定
  alignment、basis、lead-lag、套利和 Hyperliquid maker 信号的有序研究方案。
- 方案必须使用当前数据的真实事件密度、source age、segment gap 和能力边界，
  不能直接套用 BTC 已验收系数或声称 exact fill/arbitrage。

状态：
- 已通过

执行顺序：
- 可并行；本任务是只读数据审查和文档设计，不修改 T011 数据与实现。

前置任务：
- `0730T011` 业务执行完成，数据已拉回本地；独立 QA 仍待执行。

规则更新提醒：
- 无

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 不需要

提交要求：
- 当前工作区包含未提交前置任务，不创建混合提交。
- 回报使用 `commit：无`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 检查方案是否与实际数据、现有 cross-exchange 合约和能力边界一致。

files：
- `.workflow/tasks/0730T012.md`
- `.workflow/reports/0730T012-business.md`
- `docs/skhynix_cross_exchange_research_plan.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

action：
- 审查本次八段四小时数据的 schema、事件密度、source age、segment gap、
  degraded intervals 和价格尺度。
- 定义无 future leakage 的事件视图、固定网格视图和 response-event 视图。
- 依次定义 alignment、basis、lead-lag、套利和 Hyperliquid maker 信号实验。
- 为每一层定义输入、指标、walk-forward split、验收门槛、失败分类和产物。
- 明确 basis 预测性、经济等价性、可执行套利和 maker PnL 是不同证据层。

hard boundary：
- 不运行参数搜索或正式信号研究。
- 不访问 private/account/order endpoints。
- 不授权 live order、public shadow promotion 或策略 promotion。
- 不复用 BTC basis coefficient 作为 SKHYNIX 结论。
- 不声称 exact queue、exact fill 或 exact maker PnL。
- 第七步只能输出新增采集建议，不能直接启动采集。
- 任何新增 AWS/public-data 采集必须另建 formal task，获得用户明确授权，
  并且只在用户确认的活跃交易时段启动。
- 新增采集授权必须冻结 symbol、venue mapping、host、duration、成本边界和
  output path。

verify：
- 从本地 campaign manifests 和 timeline 实测行数、事件率、source age、
  segment gap 和 basis 描述统计。
- 对照现有 cross-exchange MVP、shortfall、timeline 和 acceptance 文档。
- 检查文档包含五个用户指定重点和有前置门禁的执行顺序。
- `git diff --check`。

done：
- 形成可直接派发后续实现任务的研究方案。
- 方案给出 dataset-specific alignment 和 signal acceptance 门槛。
- 方案把最终结论限制为 reject、needs-more-samples 或
  candidate-for-multi-window-public-shadow。
