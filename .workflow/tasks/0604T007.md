```md
执行线程：
- 业务线程-research

任务ID：
- 0604T007

标题：
- Canonical horizon / regime diagnostics

简短描述：
- Milestone 1 read-only worker：基于 `0604T004` canonical evidence loader foundation 和 `0604T003` canonical event-mode artifacts，诊断有效 horizon 独立性、venue-state / decision-time-visible regime 条件下的信号稳定性，并输出 watch-only 诊断表；不得做最终 regime selection、maker action 或 strategy。

状态：
- 待验收

执行顺序：
- 可并行

前置任务：
- `0604T004` QA 已通过。

规则更新提醒：
- 请先阅读规则文档后再执行本任务。
- 本任务只做 read-only horizon / regime diagnostics；不做最终 regime selection、不做 case-library、不做 shadow decisions、不实现 maker strategy、不接 private/order、不做参数搜索、不 live/default-on/tiny-live、不 promotion。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `docs/thread-playbook.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交任务文件、业务报告、代码/test 修改、task-scoped verification artifacts 和必要 workflow tracking 更新。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0604T007.md`
- `.workflow/reports/0604T007-business.md`
- `examples/hyperliquid/canonical_event_mode_evidence.py` only if a small reusable import/API adjustment is needed.
- `examples/hyperliquid/canonical_horizon_regime_diagnostics.py`
- `examples/hyperliquid/test_canonical_horizon_regime_diagnostics.py`
- `local_live_analysis/canonical_horizon_regime_diagnostics_0604T007/**`
- 必要 workflow tracking 文件仅限本任务状态/结论更新。

read-only inputs：
- Canonical loader foundation:
  - `local_live_analysis/canonical_event_mode_evidence_0604T004/**`
- Formal canonical source:
  - `local_live_analysis/event_mode_canonical_pricing_signal_0604T003/**`

forbidden：
- New data collection.
- Final regime selection, case-library construction, shadow decision generation.
- Hyperliquid or Binance private/account/order endpoints.
- Order lifecycle, submit/cancel/fill logic.
- Strategy implementation, production config/default changes, parameter search, default-on, tiny-live, promotion.
- Connector/core API changes, standard npz schema changes, canonical Binance maker audit schema changes.
- Treating ordinary synthetic fixed-grid samples as canonical short-horizon independent evidence.

action：
- Implement a read-only diagnostics runner that consumes only canonical event-mode evidence.
- Diagnose horizon quality and aliasing:
  - nominal horizon coverage
  - effective future age
  - effective future-row-delta count
  - alias groups by sample/horizon
  - `100/250ms` weak-independence caveats
  - `500ms+` and especially `1000ms+` support.
- Diagnose decision-time-visible venue/regime conditioning:
  - use only fields already present in canonical aggregate artifacts
  - summarize row counts, sample counts, direction consistency, effect/correlation stability, and concentration by regime bucket
  - explicitly flag insufficient-support buckets.
- Generate task-scoped outputs:
  - `horizon_independence_diagnostics.csv`
  - `regime_conditioning_diagnostics.csv`
  - `regime_watch_list.csv`
  - `horizon_regime_diagnostics_manifest.json`
  - `horizon_regime_diagnostics_report.md`
- Keep outputs watch-only:
  - allowed diagnostic labels: `diagnostic_supported`, `watch_needs_more_samples`, `reject_insufficient_support`, `reject_aliased_or_concentrated`
  - no final high-confidence regime definition, maker side, order behavior, default-on, tiny-live, or promotion classification.

acceptance criteria：
- Runner refuses non-canonical or diagnostic-only synthetic inputs through the `0604T004` loader/guard path.
- Horizon diagnostics explicitly show which nominal horizons are independent enough for formal interpretation and which are weak because of future-row aliasing.
- Regime diagnostics include sample count, row count, effect/correlation direction, concentration, and support status for each reported bucket.
- Report identifies whether any regime bucket is only watch-worthy versus insufficient, without promoting a final trading regime.
- Tests cover horizon alias classification, minimum-support classification, concentration penalty, non-canonical input refusal, and report artifact generation.
- Boundary flags explicitly remain no collection, no final regime selection, no strategy/private/order/live/parameter/default-on/tiny-live/promotion.

verify：
- `python examples/hyperliquid/canonical_horizon_regime_diagnostics.py --help`
- `python -m py_compile examples/hyperliquid/canonical_horizon_regime_diagnostics.py examples/hyperliquid/test_canonical_horizon_regime_diagnostics.py`
- `python -m pytest examples/hyperliquid/test_canonical_horizon_regime_diagnostics.py`
- Run the diagnostics runner on `local_live_analysis/event_mode_canonical_pricing_signal_0604T003/` and write `local_live_analysis/canonical_horizon_regime_diagnostics_0604T007/`.
- Parse generated JSON manifests with `python -m json.tool`.
- `git diff --check`

done：
- Business report must include horizon independence findings, regime conditioning findings, artifact paths, verification commands, blockers/caveats if any, commit id, and commit message.
- Business report must explicitly state this is only read-only horizon / regime diagnostics and does not authorize final regime selection, strategy implementation, private/order endpoints, live/default-on/tiny-live, parameter search, or promotion.
- Business report should end in `待验收` if verification passes.
```
