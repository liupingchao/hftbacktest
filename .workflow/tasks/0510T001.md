```md
执行线程：
- 测试线程

任务ID：
- 0510T001

标题：
- 建立 binance_tick_mm live/backtest 闭环任务模板

简短描述：
- 把已完成的 live 样本闭环固化成 workflow 任务模板，覆盖采集 live 样本、拉回归档、replay/acceptance 验收、cancel-fill 风险诊断、问题分类、离线规则 replay、跨样本验证和 live micro test 决策。本轮不修改业务代码、不启动真实 live，只沉淀可复用流程和验收口径。

状态：
- 待执行

执行顺序：
- 当前唯一任务

前置任务：
- 无

规则更新提醒：
- 请先阅读 .workflow/workflow-kit/workflow-manual.md 和 docs/thread-playbook.md 后再执行本任务。

按线程规则执行：
- .workflow/workflow-kit/thread-report-template.md
- docs/thread-playbook.md

是否需要提交代码：
- 不需要

提交要求：
- 不需要提交
- 回报必须写 `commit：无` 和 `提交信息：无`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- examples/binance_tick_mm/README.md
- examples/binance_tick_mm/*.py
- examples/binance_tick_mm/test_*.py
- examples/binance_tick_mm/config.example.toml
- docs/5-8-future-plan.md
- docs/binance_tick_mm_alignment_execution_plan.md
- docs/maker_optimization_acceptance.md
- docs/5-4-plan.md
- docs/5-8-terminal-reconcile-plan.md
- docs/stage6f-5-8-night-plan.md
- docs/stage6i-cancel-requested-fill-risk-plan.md
- docs/dry-run-plan-5-8.md
- local_live_analysis/*
- baselines/*

action：
- 阅读 examples/binance_tick_mm/README.md、docs/5-8-future-plan.md、docs/maker_optimization_acceptance.md 和现有 run/archive 结构。
- 把完整闭环拆成 8 个阶段，并为每个阶段写清输入、动作、产物、验收标准和失败分支。
- 阶段 1：采集 live 样本。固定策略参数，不开新规则；使用清楚 run id，例如 `5-10-day-control-1h-06`；跑 1H；保存 live audit、collector raw gzip、connector/bot 日志。
- 阶段 2：拉回并归档。从 `awsserver1` 拉回到 `local_live_analysis/<run_id>/`；生成 `local_live_analysis/archive/<run_id>.tar.gz`；保留 start/stop 时间，避免名字误导。
- 阶段 3：replay/acceptance 验收。用 raw market data 跑 normal replay 和 audit replay；跑 `maker_acceptance.py`；确认 action/planned/reject/throttle 为 `1.0`，working semantic/blocking mismatch 为 `0`，API/throttle mismatch 为 `0`，strict replay lag post-startup breach/drop/fail 为 `0`。
- 阶段 4：风险诊断。跑 `analyze_cancel_fill_risk.py`；检查 cancel-fill count、notional rate、source-path、markout 和 cancel-to-fill latency bucket。
- 阶段 5：判断问题类型。如果 replay/working/API 不对齐，先修框架，不做策略优化；如果对齐通过但 cancel-fill 风险高，进入规则设计；记录本样本更偏 adverse-selection 还是 same-side readd 等类型。
- 阶段 6：离线规则 replay。用同一批样本跑 Stage 6J replay；比较 baseline、add-side guard、cooldown、后续 adverse-selection timing rule；确认改善来自目标 source-path，而不是简单减少交易。
- 阶段 7：跨样本验证。至少覆盖 daytime 和 night-active 两类样本；比较 PnL、max position、drop rate、churn、cancel-fill source-path 是否稳定改善。
- 阶段 8：live micro test 决策。只有通过多样本 replay 后，才把规则开到小 notional live；live 后回到阶段 1 形成闭环。
- 产出一份执行回报模板或操作清单，后续每个 run 可以按同一结构记录。
- 本轮不修改业务代码、不实际启动 live 交易。

verify：
- 至少运行 import/help 级别的安全检查，确认关键脚本入口存在：
  - `python examples/binance_tick_mm/align_live_run.py --help`
  - `python examples/binance_tick_mm/maker_acceptance.py --help`
  - `python examples/binance_tick_mm/analyze_cancel_fill_risk.py --help`
  - `python examples/binance_tick_mm/stage6j_replay.py --help`
- 如果某个脚本没有 `--help` 或因依赖失败，记录失败原因，不把它当作业务逻辑失败。
- 不运行真实 live，不连接交易所，不改动 AWS 状态。

done：
- 在 .workflow/reports/0510T001-business.md 写执行回报。
- 回报必须包含 8 阶段闭环模板、每阶段产物与验收标准、安全检查结果、已知阻塞、下一步建议。
```
