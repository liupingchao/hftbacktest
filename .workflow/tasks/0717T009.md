# 任务派发

执行线程：
- 业务线程-watcher-termination

任务ID：
- 0717T009

标题：
- WATCHER-TERMINATION-TIMEOUT-REPAIR

简短描述：
- 实现 `0717T006` Phase 3，修复 remote orchestrator 对 watcher child 的 signal、timeout、process-group termination、reap 和生命周期证据闭环。
- 保持 watcher 策略逻辑、live 参数、订单边界和 terminal checksum 行为不变。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0717T008 / IDEMPOTENT-FILL-ATTRIBUTION-REPAIR` QA 为 `已通过`。

规则更新提醒：
- 无

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- focused tests 通过后提交。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `examples/hyperliquid/cross_exchange_live_remote_orchestrator.py`
- `examples/hyperliquid/test_cross_exchange_live_remote_orchestrator.py`
- `docs/cross_exchange_live_collection_resilience.md`
- `.workflow/tasks/0717T009.md`
- `.workflow/reports/0717T009-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

action：
- Replace blocking watcher `subprocess.run()` with a process-group-aware `Popen` lifecycle.
- Start each watcher in a new session/process group and retain the active child handle.
- Poll child state without blocking signal handling.
- On `SIGTERM`, `SIGINT`, or timeout, record intent in normal control flow, send `SIGTERM`, wait the configured grace period, escalate to `SIGKILL` if needed, and reap the child.
- Prevent subsequent windows after abort or timeout.
- Run independent open-orders proof only after child exit/reap.
- Emit child pid/process-group, termination, timeout, returncode, reap and proof-order fields in window/root evidence.
- Keep successful multi-window behavior and live strategy arguments unchanged.

verify：
- `python -m pytest examples/hyperliquid/test_cross_exchange_live_remote_orchestrator.py`
- focused existing orchestrator/window tests
- `python -m py_compile examples/hyperliquid/cross_exchange_live_remote_orchestrator.py examples/hyperliquid/test_cross_exchange_live_remote_orchestrator.py`
- `git diff --check`

done：
- SIGTERM terminates a sleeping watcher and writes abort evidence.
- SIGINT terminates a sleeping watcher and writes abort evidence.
- Timeout terminates a hung watcher.
- SIGKILL escalation handles a watcher ignoring SIGTERM.
- Abort/timeout prevents the next window.
- Child is fully reaped before open-orders proof.
- Successful multi-window path remains accepted.
- No live/private/order/cancel/remote action is performed.

not authorized：
- No terminal artifact checksum/seal repair.
- No live or remote execution.
- No strategy, quote-policy, order-size, max-submission, max-loss or threshold change.
