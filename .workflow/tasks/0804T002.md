# 任务派发

执行线程：
- 业务线程-python/tutorial-reproduction

任务ID：
- 0804T002

标题：
- EXAMPLES-TUTORIAL-TARDIS-REPRODUCTION

简短描述：
- 按“基础用法 -> 数据准备 -> 回测能力”的顺序，复现 `examples/`
  下对应九个 notebook 的核心实验。
- 本机使用 `~/Documents/tardis` 单日样本开发验证；代码通过 Git 同步
  到 amdserver，并使用 `/home/molly/data/tardis/binance-futures`
  及 `/mnt/4t_sda1` 的既有 Tardis 挂载执行。

状态：
- 待验收

执行顺序：
- 与 `0804T001` 可并行；本任务内部严格串行。

前置任务：
- 无

规则更新提醒：
- 无

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `AGENTS.md`

是否需要提交代码：
- 需要

提交要求：
- 仅提交本任务新增文件，不混入当前工作树已有改动。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 核对九个 notebook 的映射、Tardis 输入身份、串行执行结果、失败边界
  和本机/amdserver 复现证据。

files：
- `examples/tutorial_reproduction/`
- `.workflow/tasks/0804T002.md`
- `.workflow/reports/0804T002-business.md`

action：
- 新增可重复执行的 tutorial runner。
- 支持本机扁平 sample `.csv.zst` 布局与 amdserver 按日目录布局。
- 固定短窗口流式解压，生成 fused/non-fused 数据、快照和延迟模型。
- 依次运行基础 API、深度/成交、数据转换、深度融合、延迟、延迟影响、
  accelerated、L3 边界和 custom-data 实验。
- `Level-3 Backtesting` 不把 Tardis L2 冒充 MBO；输出阻塞证据并运行
  L2 控制实验。
- custom-data 实验使用 Tardis `derivative_ticker.index_price` 作为外部
  定价序列。

verify：
- runner `--help`。
- focused pytest。
- 本机 `~/Documents/tardis` 5 分钟样本执行。
- Git push 后 amdserver fetch/pull。
- amdserver Tardis 挂载上的顺序执行和结果 manifest 检查。
- `git diff --check`。

done：
- 本机和 amdserver 均有可审计的执行结果。
- 每个 notebook 都有 `passed / adapted / blocked` 明确状态和关键指标。
- 大体积 Tardis 原始数据、NPZ、Parquet 和图表不进入 Git。

hard boundary：
- 不使用 Tardis Key，不下载新数据。
- 不访问交易所 private/account/order/cancel 接口，不下单。
- 不修改原始 notebook。
- 不把 L2 数据声明为真实 L3/MBO 复现。
