# 任务派发

执行线程：
- 业务线程-python/cross-exchange-research

任务ID：
- 0801T002

标题：
- HYPERLIQUID-SHOCK-ATOM-CATALOG

简短描述：
- 按 `docs/hyperliquid_liquidity_response_case_hierarchy_plan.md` 执行
  Goal 1，把 QA 已通过的 M1 primary records 派生成不可变
  `ShockAtom` catalog。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- `0801T001` QA `已通过`。

规则更新提醒：
- 无

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `docs/hyperliquid_liquidity_response_case_hierarchy_plan.md`

是否需要提交代码：
- 不需要

提交要求：
- 不需要提交
- 回报写 `commit：无`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `examples/hyperliquid/cross_exchange_liquidity_response_case_hierarchy.py`
- `examples/hyperliquid/test_cross_exchange_liquidity_response_case_hierarchy.py`
- `local_live_analysis/skhynix_liquidity_response_case_hierarchy/`
- `.workflow/tasks/0801T002.md`
- `.workflow/reports/0801T002-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

action：
- 验证 M1 manifest 为 `0801T001` / `hyperliquid_liquidity_response_motif_v2`
  且 `passes=true`。
- 将每条 M1 primary episode 一对一派生成 `ShockAtom`，`atom_id` 复用
  M1 `episode_id`。
- 在 atom catalog 中记录 M1 episode 文件、行号、文件 SHA、manifest SHA
  和行级 fingerprint，保留方向、时间戳、segment identity 与核心证据字段。
- 记录 `visible_from=decision_ts` 与 `h2000` outcome visibility；未覆盖 outcome
  保持 missing，不做 forward-fill。
- 构建前后 rehash M1 manifest 与 episode 文件，输出 manifest 记录 row count、
  SHA、visibility 语义、边界声明和原子发布 contract。

verify：
- `python -m pytest examples/hyperliquid/test_cross_exchange_liquidity_response_case_hierarchy.py -q`
- `python examples/hyperliquid/cross_exchange_liquidity_response_case_hierarchy.py --help`
- 真实八段 M1 -> atom builder。
- 独立脚本复核 atom count、M1 identity、row SHA、无重复/遗漏和 source
  timestamp tolerance。

done：
- `atom/shock_atom_catalog.csv.gz` 精确包含 `141,768` 条 atom。
- 每个 atom 一对一引用 M1 primary row，且 source identity/SHA 可复核。
- 本轮不做 cluster、baseline、motif、regime、信号拟合、策略回测、maker
  identity、exact fill 或 maker PnL 推断。
