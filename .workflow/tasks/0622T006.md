```md
执行线程：
- 业务线程-live

任务ID：
- 0622T006

标题：
- M2 anti-drift maker-only submit gate / live calibration

简短描述：
- 在 `0622T005` 已证明 watcher-local inline reprice 路径足够快之后，修复剩余的 exchange-side fast BBO drift / post-only validation reject 问题：提交前增加 maker-only adverse-drift / touch-stability gate，只在盘口没有朝会导致 post-only reject 的方向快速漂移时才提交。
- 本任务目标是降低 `Alo` post-only would-immediately-match reject 概率，并用一次受控 live calibration 验证；不是通过 taker、crossing、one-tick-back 或放宽风险上限强行成交。

状态：
- 待执行

执行顺序：
- 当前唯一任务

前置任务：
- `0622T005` QA 为 `已通过`。它完成 inline reprice / post-only reject repair，但两次有效 `Alo` buy 均因 exchange-side BBO drift 被 post-only 拒绝，没有 resting/fill。
- `0618T008` QA 为 `已通过`，T008 ledger/reconciler 必须继续作为 M2 PnL 事实源。
- `0619T002` QA 为 `已通过`，fresh-touch / dynamic-size / session-gate 策略合同仍然有效。

规则更新提醒：
- 请先阅读 `.workflow/workflow-kit/workflow-manual.md`、`.workflow/workflow-kit/task-dispatch-template.md`、`.workflow/workflow-kit/thread-report-template.md`、`.workflow/workflow-kit/qa-acceptance-template.md`、`task_plan.md`、`progress.md`、`findings.md` 后再执行本任务。

按线程规则执行：
- `AGENTS.md`
- `docs/hyperliquid_tiny_live_real_order_executor.md`
- `docs/hyperliquid_tiny_live_final_go_no_go_gate.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- `examples/hyperliquid/hyperliquid_tiny_live_m2_fill_loop.py`
- `examples/hyperliquid/test_hyperliquid_tiny_live_m2_event_driven_watcher.py`
- `examples/hyperliquid/test_hyperliquid_tiny_live_m2_public_watcher.py`
- `examples/hyperliquid/test_hyperliquid_tiny_live_m2_fill_loop.py`
- `local_live_analysis/hyperliquid_tiny_live_m2_anti_drift_gate_0622T006/**`
- `.workflow/reports/0622T006-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

read-only inputs：
- `.workflow/tasks/0622T005.md`
- `.workflow/reports/0622T005-business.md`
- `.workflow/reports/0622T005-qa.md`
- `local_live_analysis/hyperliquid_tiny_live_m2_inline_reprice_0622T005/**`
- `local_live_analysis/hyperliquid_tiny_live_m2_pnl_ledger_0618T008/**`

hard boundary：
- Keep Hyperliquid `Alo` post-only. No taker/crossing, no `Ioc`, no one-tick-back workaround, no cap relaxation, no default-on behavior, no promotion, no stable PnL claim, and no M3 readiness claim.
- Dynamic order size hard cap remains `<=0.005 BTC`; smaller size is allowed and preferred when current rolling throughput is low.
- Live execution is limited to one event-driven anti-drift-gated micro-window and at most `2` real order endpoint submissions total.
- Public waiting / candidate discovery remains public-only until a current event-driven candidate passes fresh-touch and anti-drift gates. Before trigger it must not read credentials, call private/account/order endpoints, submit orders, cancel orders, or start a continuous live bot.
- After trigger, the only required pre-submit private call is `open_orders` 防重 / shutdown safety. Do not reintroduce slow pre-submit `all_mids` / `meta` / `user_state` / `user_fees` calls.
- Reprice must happen after `open_orders` using the latest in-memory BBO/current L2 snapshot, then anti-drift must be checked again immediately before submit.
- If BBO changes, if adverse drift is detected, or if touch stability is not proven, re-evaluate from latest public state or fail closed. Do not reuse old trigger quotes.
- On Hyperliquid post-only reject / would-immediately-match, do not switch to taker, crossing, one-tick-back, larger size, or looser queue bands. Wait for the next public L2/trade event, re-evaluate fresh-touch / dynamic-size / session-gate / anti-drift, and submit at most one retry while staying inside the same total `<=2` order endpoint calls.
- Selected quote must be current touch, post-only/non-crossing, size must still be `<=0.005 BTC`, queue-depth/order-count must still satisfy accepted `quality_a` / `quality_b` bands, and `Alo` must be the only TIF.
- Preserve tracked cancel, final open-orders proof, independent remote open-orders check, redaction, and T008 ledger fail-closed.
- Do not print or persist credential values, private keys, raw signatures, nonces, wallet secrets, or unredacted account identifiers.

implementation action：
- Add an anti-drift / touch-stability gate to the existing event-driven inline reprice path, for example `--event-driven-anti-drift-live`.
- Keep the watcher-local inline path from `0622T005`; do not restore `watcher -> fill_window.run_window` for triggered submission.
- Maintain a rolling public microstructure state from recent L2/trades events:
  - current BBO and last BBO changes
  - elapsed time and event count since last adverse BBO move
  - same-side and opposite-side trade pressure over a short window
  - whether the current touch has persisted long enough to submit without obvious adverse drift
- For buy candidates, treat these as adverse and skip/fail closed:
  - best ask moving down toward or to the buy limit
  - bid/ask stepping down in the most recent events
  - recent sell pressure / bid trade-through indicating immediate ask-down risk
  - current spread/touch state that would make `limit_px >= current_ask`
- For sell candidates, apply the symmetric adverse checks:
  - best bid moving up toward or to the sell limit
  - bid/ask stepping up in the most recent events
  - recent buy pressure / ask trade-through indicating immediate bid-up risk
  - current spread/touch state that would make `limit_px <= current_bid`
- Gate shape should be parameterized and recorded, with conservative defaults such as a short BBO-stability window in milliseconds/events and an adverse-flow threshold. Exact constants may be chosen conservatively from the existing T005 artifact evidence.
- Flow after candidate:
  1. current candidate from in-memory L2/BBO + rolling trades
  2. anti-drift / touch-stability pre-trigger gate on public state
  3. private `open_orders` 防重 / safety check only after public gates pass
  4. immediate reprice from latest in-memory BBO/current L2 snapshot
  5. anti-drift / touch-stability gate again after `open_orders`
  6. strict immediate guard on current-touch, age, non-crossing, size, queue band
  7. submit post-only `Alo`
  8. if resting, quote-aging cancel with existing fast/public-event-aware behavior
  9. if post-only reject, wait next public event and allow at most one maker-only retry after all gates pass again
  10. tracked cancel and final open-orders proof
- Record skip reasons as first-class outcomes. A no-submit result because anti-drift correctly blocked all candidates is an acceptable fail-closed task result.
- Keep T008 ledger as the final realized PnL fact source. If no maker fill or incomplete fee/inventory/realized PnL evidence appears, M2 remains blocked.

required artifacts：
- `anti_drift_gate_manifest.json`
- `anti_drift_gate_matrix.csv`
- `bbo_stability_matrix.csv`
- `adverse_flow_state.csv`
- `anti_drift_submit_decision_matrix.csv`
- `inline_reprice_manifest.json`
- `inline_reprice_latency_matrix.csv`
- `inline_reprice_attempt_matrix.csv`
- `inline_reprice_guard_matrix.csv`
- `inline_reprice_post_only_reject_matrix.csv`
- `event_driven_watcher_manifest.json`
- `event_driven_latency_matrix.csv`
- `event_driven_trigger_decision_matrix.csv`
- `current_candidate_audit.csv`
- `rolling_flow_state.csv`
- `order_intent_audit.csv`
- `quote_attempt_matrix.csv`
- `window_result_matrix.csv`
- `public_stream_summary.json`
- final gate manifest and remote facts
- tracked cancel / shutdown proof
- final remote open-orders proof
- independent remote open-orders check
- aggregate live fill ledger
- T008 ledger manifest/output
- `anti_drift_no_submit_report.md` when no candidate passes anti-drift / touch-stability
- `event_driven_no_current_candidate_report.md` when no current candidate appears

live execution rules：
- Run focused local tests and CLI checks before any remote/live-capable step.
- Refresh `/home/admin/hftbacktest-cross-exchange` by git-safe incremental bundle / `merge --ff-only`; fail if branch is not `cross-exchange` or dirty count is nonzero.
- Rerun final gate; stop before watcher/live if `allow_create_0617T008 != true`.
- Run the anti-drift-gated inline watcher/live controller on `awsserver1` using the refreshed checkout and configured Python path.
- During watcher waiting, only public L2/trades collection is allowed.
- After trigger, submit at most `2` post-only `Alo` orders and only when fresh-touch, dynamic-size, session, immediate current-candidate, and anti-drift gates pass.
- Always tracked-cancel live orders and prove final open orders are empty.
- Always run T008 ledger after pullback; no fill or incomplete economics/inventory evidence must return fail-closed.

expected outcomes：
- `已通过` is possible only if implementation and QA criteria pass and either:
  - T008 proves live maker fill + fee/inventory/mark/realized PnL evidence; or
  - QA accepts complete no-current-candidate / anti-drift-blocked / post-only-reject / no-fill fail-closed evidence under the unchanged risk contract.
- M2 completion is possible only if T008 proves live maker fill plus complete fee/inventory/realized PnL evidence.
- If no current candidate appears, if anti-drift blocks all candidates, if inline guard blocks, if post-only rejects without resting, if no order fills, if final gate fails, if open-orders proof fails, or if T008 cannot prove realized live PnL, report M2 as blocked and do not enter M3.

verify：
- `python -m pytest examples/hyperliquid/test_hyperliquid_tiny_live_m2_event_driven_watcher.py -q`
- `python -m pytest examples/hyperliquid/test_hyperliquid_tiny_live_m2_public_watcher.py -q`
- `python -m pytest examples/hyperliquid/test_hyperliquid_tiny_live_m2_fill_loop.py examples/hyperliquid/test_hyperliquid_tiny_live_m2_pnl_ledger.py -q`
- `python -m py_compile examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py examples/hyperliquid/hyperliquid_tiny_live_m2_fill_window.py examples/hyperliquid/hyperliquid_tiny_live_m2_fill_loop.py`
- `python examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py --help`
- `python examples/hyperliquid/hyperliquid_tiny_live_m2_fill_window.py --help`
- `python examples/hyperliquid/hyperliquid_tiny_live_m2_fill_loop.py --help`
- `git diff --check`
- If watcher/live reaches remote execution: remote refresh evidence, final gate go/no-go, anti-drift-gated watcher/live artifacts, pulled-back artifacts, independent remote open-orders check, and T008 ledger output.

done：
- 回报 anti-drift 参数、event runtime、public message counts、event-driven evaluation count、current candidate count、anti-drift pass/block count、trigger count、trigger_to_open_orders_start、open_orders_elapsed、open_orders_end_to_reprice、reprice_to_order_submit、guard BBO、submit-intent BBO、anti-drift decision/reason、exchange reject reason、post-only retry matrix、live submissions 数、fill_count / maker_fill_count、T008 ledger 结论、artifact 路径、blockers、commit id、提交信息。
- 明确说明 M2 是否仍 blocked；除非 T008 ledger 证明 live fill / fee / inventory / realized PnL，否则不得声明 M2 完成、稳定 PnL、或 M3 ready。
```
