# 0722T052 BOUNDED-FILL-FEEDBACK-IDENTITY-RETRY-LIVE-RERUN

执行线程：
- 总控 auto-loop / 业务执行线程

任务ID：
- 0722T052

标题：
- Run one new exact-envelope fill-feedback window with bounded cancel identity retry

简短描述：
- 在 T050/T051 离线修复通过后，建立一个全新、隔离的
  `two-sided-fill-feedback-manager` tiny-live window，验证 exact oid-to-cloid
  cancel retry 是否改善 T049 attempt 1 的 terminal evidence；不复用或修改
  T049 sealed evidence。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- `0722T050`（已通过）
- `0722T051`（已通过）
- `0722T049`（阻塞；sealed evidence 只读）

规则更新提醒：
- 允许且只允许一个新的 exact-envelope live window。
- live 是主要数据来源；shadow/replay 仅用于明显错误防护、决策复现和
  控制变量，不作为长时间前置研究阶段。
- 必须先通过 source、execution-safety、evidence-integrity、account/service
  和 same-window acceptance gate。
- 本任务只验证现有 bounded fill-feedback manager 的证据修复；不宣称稳定
  PnL、fill rate、queue priority、maker viability、promotion、multi-level
  或最终 MVP。

Exact envelope：
- host：`awsserver1`
- venue/symbol：Hyperliquid `BTC`
- lead/lag：Binance public book ticker lead / Hyperliquid lag
- mode：`event-driven-edge-gate-live`
- profile：`two-sided-fill-feedback-manager`
- post-only：`Alo`
- window：1 个，最多 `1800s`
- max order size：`0.005 BTC`
- max aggregate position delta：`0.01 BTC`
- max loss：`1 USDC`
- max submissions：`2`
- requote attempts：`2`
- quote hold：`3s`
- wait：`10s`
- fast L2：开启
- exchange-reconciled manager：开启
- fill feedback：开启
- target fill ratio：不配置，保持 neutral/fixed fallback
- dynamic spread、inventory skew、multi-level：关闭

是否需要提交代码：
- 需要提交 workflow task、business/QA/fixed QA records；不修改策略代码。

是否进行QA验收：
- 是

files：
- `.workflow/tasks/0722T052.md`
- `local_live_analysis/principal_alignment_bounded_fill_feedback_0722T052/`
- `.workflow/reports/0722T052-business.md`
- `.workflow/reports/0722T052-qa.md`
- `docs/qa-acceptance-report.md`
- `task_plan.md`、`progress.md`、`findings.md`

action：
- 先在 remote source 上确认 exact source commit、runtime source scope、
  account/service/open-orders/position/kill-switch preflight。
- 仅 preflight 全部通过后启动一个新的 bounded live window。
- 对 remote/local evidence 做 source、checksum、decision、lifecycle、
  economics、optimism、estimator/fill-feedback replay、post-live account
  proof 和 same-window acceptance。
- 若任一 execution/evidence/account/same-window gate 失败，立即停止，不得
  开启第二窗口，也不得以空仓或空订单替代 per-reference terminal proof。

verify：
- orchestrator preflight and account/service preflight。
- one live window with child reap and post-live account proof。
- independent local checksum verification and
  `cross_exchange_t024_same_window_acceptance.py`。
- focused/full offline regression only if evidence schema requires it。

done：
- 新窗口产生可复现、task-scoped、source-pinned 的 live evidence。
- 记录 retry 是否发生、每次 attempt identity/result、terminal reconciliation
  和最终 acceptance 结论。

blockers：
- source mismatch、conflicting service/account contamination、envelope drift、
  ambiguous terminal evidence、checksum failure 或 same-window acceptance
  failure 均为本任务终止条件；不自动追加 live window。

提交信息：
- `Run bounded fill-feedback identity-retry live rerun`
