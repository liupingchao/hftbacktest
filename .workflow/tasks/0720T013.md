# 0720T013 DECISION-EVIDENCE-STRICT-JOIN-REPAIR

执行线程：
- 总控 auto-loop / 业务执行线程

任务ID：
- 0720T013

标题：
- Enforce fail-closed event, attempt, authorization and causal evidence joins

简短描述：
- Close the three evidence-join defects found by `0719T012` independent QA without changing quote strategy behavior.
- Require every decision-stage row to participate in one strict, independently reconstructed causal chain before another bounded live window is allowed.

状态：
- 待执行

执行顺序：
- 当前唯一任务

前置任务：
- `0719T012`（未通过）

规则更新提醒：
- 本任务是 offline-only repair；不得调用 live、private、account、order、cancel、network、remote 或 service。
- T012 已接受的 path portability、source/checksum/fill/cancel 和逐行汇总边界必须保持。
- Multi-level、dynamic spread、fill feedback、inventory skew 和 actual quote behavior activation 继续关闭。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `.workflow/reports/0719T012-qa.md`
- `docs/superpowers/plans/2026-07-17-principal-alignment-p0-p3.md`

是否需要提交代码：
- 需要

提交要求：
- 先提交 implementation，再提交 business report。
- 回报必须带 implementation `commit id`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- Modify: `examples/hyperliquid/cross_exchange_t024_same_window_acceptance.py`
- Modify only if identity persistence is required: `examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- Modify/add focused tests under `examples/hyperliquid/`
- Reuse read-only artifact: `local_live_analysis/principal_alignment_single_level_two_sided_0719T011/`
- Workflow: this task, business report, QA report, `docs/qa-acceptance-report.md`, `task_plan.md`, `progress.md`, `findings.md`

identity contract：
- Every trigger, anti-drift, immediate-guard, edge-gate and attempt row must have a canonical positive event identity after reconstruction.
- Every row carrying an attempt must have a canonical positive attempt identity.
- Malformed non-empty identity values must never be treated as missing and must never skip validation.
- New producer artifacts must persist direct event identity on immediate-guard rows.
- A legacy row lacking direct event identity may use only an explicit deterministic bridge already present in raw evidence, such as a unique candidate exchange timestamp.
- Legacy bridge resolution must be one-to-one and ambiguity-rejecting. Missing, zero-match or multi-match resolution fails closed.
- No synthetic event identity may be invented for legacy evidence.

causal join contract：
- Every non-trigger decision row must join to exactly one existing trigger row.
- Anti-drift block rows must exact-match trigger `guard_status=anti_drift_block` and exact reason.
- Immediate-guard fail rows must exact-match trigger `guard_status=fail_closed` and exact reason.
- Immediate-guard pass rows must join a trigger that proceeds to edge evaluation or order authorization.
- Edge block rows must exact-match trigger `guard_status=edge_gate_block` and exact reason.
- Edge pass rows must join a trigger that is order-authorized.
- Attempt row event, attempt, guard status/reason, edge status/reason and skip reason must exactly match the joined causal chain.
- Orphan, duplicate, missing, malformed or conflicting joins fail closed.

authorization contract：
- Every submitted attempt must join exactly one trigger with `trigger_found=true` and `live_window_called=true`.
- Zero order-authorized trigger rows cannot coexist with a submitted attempt.
- A two-sided manager may persist two side-specific submitted rows for one authorized event, but both rows must share the same canonical manager attempt identity and event identity.
- Order/cancel endpoint counts remain independently reconstructed from raw attempt rows.

legacy T011 contract：
- T011's immediate-guard rows must be joined through existing raw fields only and only when the mapping is unique.
- T011 must reconstruct its existing counts and reasons without path-only failures.
- T011 must remain mechanism/evidence blocked for zero submissions, real guard/edge blockers and missing two-sided lifecycle.

tests：
- Malformed trigger/guard/edge/attempt event identities fail closed after summaries and checksums are synchronously rebuilt.
- Malformed attempt identity fails closed and cannot be skipped.
- Zero authorized trigger rows with two submitted side rows fail closed.
- Orphan, duplicate and ambiguous event joins fail closed.
- Anti-drift status/reason drift fails closed.
- Immediate-guard status/reason drift fails closed.
- Edge status/reason drift fails closed.
- Attempt-to-trigger guard/edge/skip-reason drift fails closed.
- A valid two-sided manager fixture with two submissions joined to one authorized event passes.
- T011 legacy guard identity bridge is deterministic, reconstructs exact T011 counts and remains lifecycle blocked.

verify：
- Focused producer/acceptance/orchestrator/manager tests pass.
- Full `python -m pytest -q examples/hyperliquid` passes.
- Modified Python modules pass `py_compile`.
- Modified CLIs pass `--help`.
- T011 byte-exact local replay preserves source/checksum/fill/cancel validation and has no path-only failures.
- `git diff --check` passes.
- No live/private/account/order/cancel/network/remote/service action occurs.

done：
- All event and attempt values are strictly parsed and every row is causally joined.
- Submission evidence cannot exist without exact trigger authorization.
- Trigger, anti-drift, immediate-guard, edge and attempt status/reason evidence is bidirectionally consistent.
- Legacy evidence is accepted only through explicit unique raw-field bridges.
- Independent QA accepts the repair before any new live task is dispatched.

stop conditions：
- Any test requires live/private/account/order/cancel/network/remote/service access.
- Repair weakens source, terminal checksum, command identity, path portability, raw fill, reference or lifecycle validation.
- Legacy T011 can pass only by inventing identity or allowing ambiguous joins.
- T011 becomes accepted despite zero submissions and missing lifecycle.
- Strategy formula, threshold, risk cap or activation behavior changes.

blockers：
- Independent QA failure keeps new live and multi-level locked.

commit：
- 待业务线程提交

提交信息：
- 待业务线程填写
