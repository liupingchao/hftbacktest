# 0719T010 FILL-DIRECTION-MANIFEST-BLANK-REPAIR

执行线程：
- 总控 auto-loop / 业务实现线程

任务ID：
- 0719T010

标题：
- Repair exact Hyperliquid fill direction semantics and blank manifest records

简短描述：
- Close both `0719T009` QA findings before any bounded two-sided live task.
- This task is offline-only.

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0719T009`（未通过）

规则更新提醒：
- T009 accepted sub-results remain authoritative: side-aware fill-limit validation, independent raw/full-fill reconstruction and independent current run-root SHA-256 recomputation.
- No live/private/account/order/cancel/network/remote/service action is allowed in this task.
- Multi-level, dynamic spread, fill feedback, inventory skew and actual quote behavior changes remain off.

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `.workflow/reports/0719T009-qa.md`

是否需要提交代码：
- 需要

提交要求：
- 离线 focused/full regression 通过后提交。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- Modify: `examples/hyperliquid/hyperliquid_tiny_live_m2_fill_window.py`
- Modify: `examples/hyperliquid/cross_exchange_t024_same_window_acceptance.py`
- Modify: focused fill/acceptance/orchestrator/watcher tests
- Workflow: this task, business report, QA report, `docs/qa-acceptance-report.md`, `task_plan.md`, `progress.md`, `findings.md`

action：
- Decode Hyperliquid fill direction by exact normalized values in producer:
  - `Open Long` and `Close Short` map to buy;
  - `Open Short` and `Close Long` map to sell;
  - accepted exact buy/sell aliases, if retained for compatibility, must not use substring matching;
  - unknown direction values fail closed;
  - when explicit side and direction are both present, they must resolve to the same side or fail closed.
- Independently implement the same exact direction contract in Task 12 acceptance without importing producer helpers.
- Ensure a direction or side conflict cannot become attributed fill evidence or full-fill terminal proof, including when all derived artifacts are synchronized and the run is resealed.
- Treat every explicit blank or whitespace-only terminal manifest record as malformed instead of skipping it.
- Add producer and acceptance regressions for all four Hyperliquid direction values, unknown values, explicit side conflicts and appended blank/whitespace manifest records.
- Keep quote formulas, thresholds, risk caps, activation flags and non-exact legacy behavior unchanged.

verify：
- Side-less `Open Long` and `Close Short` fills resolve only to buy.
- Side-less `Open Short` and `Close Long` fills resolve only to sell.
- Explicit side plus agreeing direction passes; conflicts and unknown direction values fail closed.
- Synchronized and resealed conflicting-direction raw/ledger/role/summary evidence cannot pass complete acceptance.
- Any blank or whitespace-only manifest record makes independent terminal verification and complete acceptance fail.
- Fresh real manager-watcher zero-fill producer output still passes complete same-window acceptance.
- T005-T009 cancel/raw-order/raw-fill/limit/terminal/canonical-command regressions pass.
- Modified Python `py_compile`, CLI `--help`, `git diff --check`.
- Full `python -m pytest examples/hyperliquid -q`.

stop conditions：
- Any reversed or conflicting direction can serve as terminal proof.
- Any explicit blank manifest record can pass independent verification.
- Any strategy/risk/activation/live boundary changes.

done：
- Both T009 QA findings are closed.
- Real producer zero-fill output and valid four-direction fixtures pass the exact acceptance contract.
- Independent QA accepts the offline repair.

blockers：
- No live task may start before this task is QA accepted.

commit：
- `542319128b661058fee111a9a7886c534fb12fcb`

提交信息：
- `Close T010 fill direction manifest gaps`
