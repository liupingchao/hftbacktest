```md
执行线程：
- 业务线程-python

任务ID：
- 0530T001

标题：
- Hyperliquid public market-data research consumer design contract

简短描述：
- 基于 `0529T004` 已通过 QA 的 fresh Hyperliquid public-only BTC sample，设计一个只读 public market-data research consumer 合同，用作后续 pricing / market-view 研究入口。本任务只做 design/read-only planning，不实现 private connector、订单生命周期、策略 live、参数搜索、default-on、tiny-live 或 promotion。

状态：
- 已通过

执行顺序：
- 当前唯一任务；`0529T005` 已通过，本任务仍与 Binance Stage 9L 工作独立，执行时不得修改、依赖或混入 Binance Stage 9L artifacts。

前置任务：
- `0529T004` QA 已通过。
- 直接事实源：
  - `.workflow/tasks/0529T004.md`
  - `.workflow/reports/0529T004-business.md`
  - `.workflow/reports/0529T004-qa.md`
  - `docs/qa-acceptance-report.md` 仅在仍指向 `0529T004` 时作为补充参考；`0529T004` 的固定 QA 事实源是 `.workflow/reports/0529T004-qa.md`
  - `local_live_analysis/hyperliquid_public_sample_0529T004/**`

规则更新提醒：
- 请先阅读规则文档后再执行本任务。
- 本任务是 Hyperliquid public market-data research consumer design，不是实现任务、private connector 任务、order lifecycle 任务、strategy live 任务、parameter search 任务、default-on/tiny-live/promotion 任务。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `docs/thread-playbook.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 提交任务文件、设计文档、业务报告和必要 workflow 更新；不需要提交 consumer 实现代码。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

Allowed files：
- `.workflow/tasks/0530T001.md`
- `.workflow/reports/0530T001-business.md`
- `docs/hyperliquid_public_market_data_research_consumer_design.md`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `.workflow/dashboard.html` and `.workflow/dispatch_suggestions.md` only if they can be regenerated without pulling unrelated task state into this task.

Allowed read-only inputs：
- `.workflow/tasks/0529T003.md`
- `.workflow/tasks/0529T004.md`
- `.workflow/reports/0529T003-business.md`
- `.workflow/reports/0529T004-business.md`
- `.workflow/reports/0529T004-qa.md`
- `examples/hyperliquid/hyperliquid_public_sample.py`
- `examples/hyperliquid/hyperliquid_raw_alignment.py`
- `examples/hyperliquid/test_hyperliquid_public_sample.py`
- `examples/hyperliquid/test_hyperliquid_raw_alignment.py`
- `local_live_analysis/hyperliquid_public_sample_0529T004/**`
- `local_live_analysis/hyperliquid_raw_alignment_0529T003/**`
- `py-hftbacktest/hftbacktest/data/utils/hyperliquid.py`
- `docs/hyperliquid_live_replay_alignment_design.md`
- Official Hyperliquid public API docs, rechecked if network access is available before relying on current schema facts:
  - `https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api`
  - `https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket`
  - `https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket/subscriptions`
  - `https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint`
- If official docs cannot be reached from the task environment, do not block this design-only task. The business report must state that official docs were not rechecked, rely only on already accepted `0529T004` local artifacts for the current design, and make successful official-doc recheck an explicit prerequisite for any later implementation task.

Forbidden files / areas：
- Any Binance strategy behavior, Binance acceptance-rule changes, Binance live/deploy files, or Binance Stage 9L artifacts.
- Hyperliquid private connector, signing, nonce handling, private account endpoints, user stream, order updates, user fills, open orders, account state, positions, submit/cancel, order status, fills reconciliation, or order lifecycle code.
- Strategy behavior, quote placement, fair/target formula, inventory logic, risk guards, production defaults, live configs, tiny-live configs, or remote deploy scripts.
- Standard npz schema changes, canonical audit schema changes, connector/core API changes.
- Any fresh public collection beyond referencing the already accepted `0529T004` sample.
- Any parameter search, replay/live trading experiment, default-on behavior, guard relaxation, tiny-live, or promotion claim.

Problem statement：
- `0529T004` proved that one fresh Hyperliquid public-only BTC sample can be collected, provenanced, converted, sidecar-joined, and classified as `passes_pricing_research_market_view`.
- That evidence is sufficient to start public market-data pricing / market-view research design.
- It is not evidence for private connectivity, order execution, fill lifecycle, live strategy behavior, parameter search, default-on, tiny-live, or promotion.
- The next useful step is to define the research consumer contract: what it reads from the public sample, what market-view/pricing features it emits, what quality gates it applies, and what later implementation task may build.

Design scope：
1. Define the public market-data research consumer goal and non-goals.
2. Define required inputs from `0529T004`:
   - `raw.gz`
   - `raw.sha256`
   - `collection_manifest.json`
   - `recovery_snapshots.jsonl`
   - alignment `run_manifest.json`
   - alignment `converter_manifest.json`
   - `data.npz`
   - `raw_provenance.csv`
   - `raw_to_npz_mapping.csv`
   - `topn_sidecar.csv`
   - `synthetic_joined_views.csv`
   - alignment `metrics.json`
   - alignment `acceptance_report.md`
3. Define the consumer output artifact contract, including at minimum:
   - run manifest
   - market-view time series
   - pricing feature table
   - feature quality summary
   - sample/session quality summary
   - recommendation markdown
4. Define pricing / market-view features that are allowed because they are public-data-only and decision-time-visible, such as:
   - BBO, mid, spread
   - top-N depth and imbalance
   - top-N microprice proxy
   - trade pressure / signed trade proxy where schema supports it
   - book/trade freshness and event cadence
   - synthetic join age and missing/future join diagnostics
   - recovery snapshot context and session/reconnect provenance
5. Define fields that are explicitly diagnostic-only and cannot become trading proof:
   - single-sample markout or future return labels
   - future price movement
   - sample-local outcome summaries
   - stale/recovery/session health flags
6. Define quality gates for later implementation:
   - raw sha256 / manifest consistency
   - parse error count
   - required channel coverage
   - event-order validation
   - top-N coverage
   - synthetic join coverage
   - future/missing join counts
   - join-age and cadence distributions
   - recovery snapshot presence
   - explicit absence of private/order endpoints
7. Define final research classification taxonomy:
   - `passes_pricing_research_market_view`
   - `limited_pricing_research`
   - `market_view_diagnostic_only`
   - `unusable`
8. Define the later implementation task boundary:
   - may implement a read-only consumer over accepted public artifacts
   - may generate deterministic pricing / market-view feature artifacts
   - may run only on local public sample artifacts
   - must remain default-off/read-only/public-only
9. Define what evidence would be required before any future non-read-only Hyperliquid task can even be planned. The answer must explicitly say that `0529T004` plus this design task is not enough for that transition.

Required outputs：
- `docs/hyperliquid_public_market_data_research_consumer_design.md`
- `.workflow/reports/0530T001-business.md`
- Updates to `task_plan.md`, `progress.md`, and `findings.md` summarizing the accepted boundary.
- Optional regenerated `.workflow/dashboard.html` / `.workflow/dispatch_suggestions.md` if safe.

Acceptance criteria：
- The design uses `0529T004` as the fresh public sample entry point and states the exact artifact inputs.
- The design defines a concrete consumer input/output contract sufficient for a later read-only implementation task.
- The design separates public decision-time-visible features from after-the-fact labels and diagnostics.
- The design defines quality gates and classification outcomes.
- The design explicitly forbids private connector, order lifecycle, strategy live, parameter search, default-on, tiny-live, and promotion.
- The design does not modify implementation code, strategy behavior, configs, connector/core APIs, standard npz schema, or canonical audit schema.
- The report recommends exactly one next task: a later read-only consumer implementation, or states why implementation should remain blocked.
- If official Hyperliquid docs were unreachable, the report records the failed access path and marks official-doc recheck as a later implementation prerequisite rather than pretending schema facts were freshly verified.
- If official Hyperliquid docs were unreachable, the design document and business report conclusion must explicitly state this limitation and must not present the schema facts as freshly verified.

verify：
- `git diff --check`
- `python3 .workflow/build_dashboard.py` only if unrelated task files are not present; otherwise document the isolation-preserving skip.
- Manual consistency check that only task/design/report/workflow files changed.
- Manual consistency check that no live, remote deploy, public collection, private endpoint, order endpoint, parameter search, default-on, tiny-live, or promotion command was run.

done：
- Business report ends in `待验收`.
- Report lists files read, files changed, generated documents, final recommendation, and explicit forbidden-boundary confirmation.
- Report states whether official Hyperliquid docs were rechecked successfully; if not, it records the limitation and carries it into the next implementation-task preconditions.
- Report conclusion must include a dedicated sentence on official-doc accessibility: either `official docs rechecked successfully` or `official docs unavailable; implementation must recheck before coding`.
- Report includes `commit id` and `提交信息`.
- Report explicitly states:
  - no Binance strategy behavior changed
  - no Hyperliquid private connector was implemented or designed as current work
  - no order submit/cancel/fill lifecycle code was implemented or authorized
  - no strategy live, tiny-live, default-on, parameter search, guard relaxation, or promotion was run or authorized
```
