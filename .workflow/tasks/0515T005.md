```md
执行线程：
- 业务线程-python

任务ID：
- 0515T005

标题：
- Narrow cancel-race residual repair

简短描述：
- 基于 `0515T004` 的 residual diagnosis，只修 `cancel_race_window_too_short` 这一类 residual replay mismatch。任务边界必须保持很窄：仅针对 live cancel-request 后短窗口内 live filled / replay canceled 的 replay-side建模缺口，不得扩展到 touch fill assumption、queue exposure proxy、策略逻辑或样本扩展。

状态：
- 已通过

执行顺序：
- 当前唯一后续任务

前置任务：
- `0515T004` QA 已通过

规则更新提醒：
- 本任务只允许修 short cancel-race miss，不允许顺手修其他 residual class。
- 明确禁止把 `28940|sell` / `4948` 这类 `residual_replay_fill_trigger_uncertain` 当作同一问题一起修。
- 不允许修改策略、fair/reservation、quote placement、queue/touch fill general model、sample policy。

按线程规则执行：
- .workflow/workflow-kit/thread-report-template.md
- docs/thread-playbook.md
- task_plan.md
- progress.md
- findings.md

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

Allowed files：
- `.workflow/tasks/0515T005.md`
- `.workflow/reports/0515T005-business.md`
- `examples/binance_tick_mm/backtest_tick_mm.py`
- `examples/binance_tick_mm/strategy_core.py`
- `examples/binance_tick_mm/test_backtest_tick_mm.py`
- `examples/binance_tick_mm/test_replay_lifecycle_mismatch_diagnosis.py`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `.workflow/dashboard.html`
- `.workflow/dispatch_suggestions.md`
- `docs/qa-acceptance-report.md`

Allowed generated outputs：
- `local_live_analysis/5-13-day-control-30min/stage6f_cancel_race_residual_repair_0515T005/**`

Allowed read-only inputs：
- `.workflow/reports/0515T004-business.md`
- `.workflow/reports/0515T003-business.md`
- `.workflow/reports/0515T002-business.md`
- `local_live_analysis/5-13-day-control-30min/stage6e_residual_replay_fill_diagnosis_0515T004/**`
- `local_live_analysis/5-13-day-control-30min/stage6d_replay_lifecycle_repair_0515T003/**`
- `local_live_analysis/5-13-day-control-30min/audit_live_5-13-day-control-30min.csv`
- `local_live_analysis/5-13-day-control-30min/out/backtest_audit_replay/audit_bt_audit_replay.csv`
- `local_live_analysis/5-13-day-control-30min/t009_fixed_sidecar/**`

Forbidden files / areas：
- generalized touch-fill model changes
- queue / priority / exposure proxy broad modeling
- strategy behavior, fair/target formulas, configs, risk guards, quote placement behavior
- live scripts, AWS/remote state, live collection
- sample expansion tasks
- core Rust, connector, py `event_dtype`, standard npz schema

Requires separate task：
- 修改 touch fill assumption。
- 修改 queue exposure proxy / trade-consumption general model。
- 处理 `residual_replay_fill_trigger_uncertain` 这类非 cancel-race residual。
- 新采集 current-format 样本。
- quote-adjustment promotion / strategy redesign。

Fatal Failures：
- 将本任务扩大成泛 residual repair。
- 用 broad touch/queue logic 改动去“顺便”修 `4948` 这类 uncertain case。
- repair 后不做 same-sample regression validation 就声称 residual cancel-race 已解决。

action：
- 只针对 `cancel_race_window_too_short` 类 residual 实施最小 replay-side repair。
- 当前已知目标 case 是：
  - `3879|buy` / order `572`
  - live `cancel_request -> fill` 约 `9.43ms`
  - live fill 前 `10ms` 内有高密度 supportive trades
  - replay 走成 `cancel_ack`
- 实现时必须：
  - 保持作用域只限 cancel-requested short window residual fill handling
  - 不扩大到一般 touch fill / queue logic
- 回归验证至少包含：
  - 同样本 `backtest_audit_replay` 重跑
  - Stage 6B calibration 重算
  - Stage 6C mismatch diagnosis 重算
  - residual diagnosis 重算
- 回报必须对比修复前后：
  - `live_filled_replay_canceled` residual 是否消失
  - `3879|buy` / `572` case 是否对齐
  - aggregate final-state gaps 是否维持不回退
  - `28940|sell` / `4948` 是否保持不被误处理

verify：
- `python -m pytest examples/binance_tick_mm/test_backtest_tick_mm.py`
- `python -m pytest examples/binance_tick_mm/test_replay_lifecycle_mismatch_diagnosis.py`
- `python3 .workflow/build_dashboard.py`
- `git diff --check`

done：
- 回报必须明确只修了 `cancel_race_window_too_short`。
- 回报必须明确 `4948` 这类 uncertain residual 没有被纳入本任务修复范围。
- 回报必须明确 same-sample regression 后，`572` residual 是否消失，以及 aggregate 指标是否维持。
- 回报必须明确本任务未改策略、未补样本、未启动 live。
- 回报必须写 `commit id` 和 `提交信息`。
```
