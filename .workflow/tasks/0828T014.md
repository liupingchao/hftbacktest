# 任务派发

执行线程：
- SKHYNIX Flow Coherence A-1 Primitive Support Audit 业务线程

任务ID：
- 0828T014

标题：
- 执行 FLOW_COHERENCE_TRANSITION_V1 A-1 primitive support audit

简短描述：
- 在不起草完整 A0、不读取任何未来价格响应的边界内，审计
  `discordant_active -> coherent_active_d` 是否具有跨日、非重连驱动、
  非结构伪相关且对邻近参数稳定的原始支持。

状态：
- 未通过

执行顺序：
- 当前唯一 formal task

前置任务：
- 0828T013（执行验收已通过，scientific status rejected_at_A0）

规则更新提醒：
- 已阅读 AGENTS.md、workflow-kit manual、task_plan.md、progress.md、
  findings.md、methodology kernel、0828T013 plan/execution/QA。

按线程规则执行：
- AGENTS.md
- .workflow/workflow-kit/workflow-manual.md

是否需要提交代码：
- 需要

提交要求：
- audit contract 通过独立 review 后再执行。
- 自测、29-capture build、deterministic rebuild 和独立 QA 通过后提交。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

合同 review 历史：
- Round 1 独立只读 review：未通过，
  `P0/P1/P2/P3 = 1/5/1/1`。
- Revision 2 独立 review 未通过，
  `P0/P1/P2/P3 = 1/4/1/0`。
- Revision 3 独立 review 未通过，
  `P0/P1/P2/P3 = 1/4/2/0`。
- Revision 4 独立 review 未通过，
  `P0/P1/P2/P3 = 1/2/1/0`。
- Revision 5 独立 review 未通过，
  `P0/P1/P2/P3 = 0/1/2/0`。
- Revision 6 使用真实非零的 microblock path orientation，将同父块内
  活动匹配的正负方向块固定配对，并在每个 replicate 对每对执行
  独立 Bernoulli(0.5) label swap；随机化律为 `2^K` swap vectors 上
  的均匀分布；bitmask DP 唯一定义 maximum-cardinality、
  minimum-distance 和 lexicographic pairing，并冻结数值/ID/row order。
- Round 6 独立 review 已通过，
  `P0/P1/P2/P3 = 0/0/0/0`；数据执行锁已解除。

files：
- docs/skhynix_binance_flow_coherence_transition_v1_a_minus1_primitive_support_audit_plan_20260828.md
- docs/skhynix_flow_internal_directional_alpha_a0_cache_authority_20260828.csv
- examples/hyperliquid/skhynix_flow_coherence_a_minus1_audit.py
- examples/hyperliquid/test_skhynix_flow_coherence_a_minus1_audit.py
- local_live_analysis/skhynix_flow_coherence_a_minus1_audit_0828T014/
- .workflow/tasks/0828T014.md
- .workflow/reports/0828T014-business.md

action：
- 绑定 accepted 0828T013 source/cache authority。
- 验证 29 raw captures 与 primary/determinism replay-cache identity。
- 实现 20ms causal coherence primitives、30s nuisance cooldown、
  provisional confirmation 和 1000ms refractory。
- 执行 exact 9-variant one-factor envelope。
- 执行 199-replicate、五分钟父块内活动匹配的三十秒方向路径置换
  structural null，并对 observed/null 使用相同的微块边界 censor。
- 对 10s/30s/60s 各执行完整 separation、balance、conservation 和
  comparable-support gates；任一 sensitivity 只能 invalidate。
- 执行除 refractory=0 外完全相同的 V0 shadow detector。
- 执行 artificial-slice/reset invariance。
- 输出唯一 A-1 classification 和 A0 drafting authority。

verify：
- frozen plan SHA binding。
- source/raw/cache size and SHA closure。
- zero-outcome ledger。
- primitive, candidate, confirmation and cooldown focused tests。
- variant and PRNG identity。
- structural-null determinism。
- null 逐点 magnitude/zero/missingness/denominator invariance、
  pair label conservation、assignment diversity 和
  per-date/per-replicate balance。
- 10s/30s/60s comparable coverage 与完整 separation。
- no-refractory shadow detector focused tests。
- slice-invariance exact comparison。
- canonical/fresh build equality。
- Ruff、compile、pytest、git diff check。

done：
- 明确 `draft_a0_contract`。
- 明确所有 failed gates 和 atomic conditions。
- 不拟合 H0/H1，不生成未来价格 target。

明确禁止：
- 不根据未来价格修改 primitive、variant、cooldown 或 gates。
- 不将 diagnostic D1/D2 用于 rescue primary P1。
- 不收集新数据，不访问 private/order API。
