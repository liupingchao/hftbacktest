```md
执行线程：
- 业务线程-python

任务ID：
- 0520T002

标题：
- Step 9C candidate-bucket decisionability artifact hardening

简短描述：
- 在 `0520T001` 已完成多样本验证并暴露出 bucket 判定缺口的前提下，补齐 quote_adjustment_replay.py 的分桶证据和稳定性汇总，让当前 8 个 Step 9 candidate family 能对现有 current-format 样本一次性落到可判定结果，重点把 `spread_widening_stale_latency` 这类混合桶拆到能给出明确 verdict。

状态：
- 已通过

执行顺序：
- 当前唯一任务；必须在 `0520T001` QA 已通过后执行

前置任务：
- `0520T001` QA 已通过
- `0519T011` QA 已通过
- `0519T010` QA 已通过
- `0519T009` QA 已通过
- `0519T008` QA 已通过

规则更新提醒：
- 本任务只允许补齐 validator / artifact 判定层。
- 只使用现有 current-format no-rule/default-off 样本，不采集新样本，不改策略，不改 live/default-on 行为。
- 目标是把当前 8 个 candidate family 一次性落到可判定状态，不再只给单样本 proxy 结论。

按线程规则执行：
- `.workflow/workflow-kit/thread-report-template.md`
- `docs/thread-playbook.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0520T002.md`
- `.workflow/reports/0520T002-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `.workflow/dashboard.html`
- `.workflow/dispatch_suggestions.md`
- `examples/binance_tick_mm/quote_adjustment_replay.py`
- `examples/binance_tick_mm/test_quote_adjustment_replay.py`
- `local_live_analysis/stage9c_multi_sample_validation_0520T002/**`
- `local_live_analysis/5-19-day-control-30min/**`
- `local_live_analysis/5-19-night-active-30min-a/**`
- `local_live_analysis/5-19-night-active-30min-b/**`
- `local_live_analysis/5-19-night-active-30min-c/**`

Allowed read-only inputs：
- `.workflow/reports/0520T001-business.md`
- `.workflow/reports/0520T001-qa.md`
- `.workflow/reports/0519T011-business.md`
- `.workflow/reports/0519T011-qa.md`
- `.workflow/reports/0519T010-business.md`
- `.workflow/reports/0519T010-qa.md`
- `.workflow/reports/0519T009-business.md`
- `.workflow/reports/0519T009-qa.md`
- `examples/binance_tick_mm/quote_adjustment_replay.py`
- `examples/binance_tick_mm/test_quote_adjustment_replay.py`
- `local_live_analysis/5-19-day-control-30min/**`
- `local_live_analysis/5-19-night-active-30min-a/**`
- `local_live_analysis/5-19-night-active-30min-b/**`
- `local_live_analysis/5-19-night-active-30min-c/**`
- `local_live_analysis/stage9c_multi_sample_validation_0520T001/**`

Forbidden files / areas：
- Strategy code, quote behavior, production defaults, live configs, live scripts, AWS/remote state.
- Sample collection or sample expansion.
- Default-on behavior, promotion decisions, live micro tests, or Step 10 live execution.
- Any new candidate family or change to the Step 9A candidate set.

action：
- Extend the current quote-adjustment validator so it emits bucket-level evidence and stability summaries for each candidate family across the existing current-format samples.
- Make the following families and mixed regimes explicitly decisionable instead of proxy-only:
  - `baseline_control`
  - `fair_reservation_shift_edge_25`
  - `inventory_reservation_shift_band`
  - `spread_widening_stale_latency`
  - `size_reduction_or_add_side_suppression_pressure`
  - `stale_latency_no_fresh_add`
  - `min_move_quote_age_churn_guard`
  - `post_only_safety_interaction`
- Add or harden artifact outputs that distinguish:
  - per-sample bucket evidence
  - accepted-set versus clean-only sensitivity
  - `observed_and_worse` / `suppressed_by_guard` / `needs_more_data` / `needs_runner_or_artifact_change` style bucket verdicts
  - candidate-level `reject` / `keep_for_research` / `ready_for_tiny_live_design` / `blocked_by_data_quality` / `needs_runner_or_artifact_change`
- If a family currently mixes multiple mechanisms, split only as much as needed to make the verdict unambiguous; do not expand the candidate set or collect new samples.

verify：
- `python -m pytest examples/binance_tick_mm/test_quote_adjustment_replay.py -q`
- `python examples/binance_tick_mm/quote_adjustment_replay.py --help`
- Run the hardened validator against the 4 existing current-format samples and produce task-scoped multi-sample artifacts under `local_live_analysis/stage9c_multi_sample_validation_0520T002/`
- `python3 .workflow/build_dashboard.py`
- `git diff --check`

done：
- 回报必须说明当前 8 个 family 是否都已变成可判定。
- 回报必须明确 `spread_widening_stale_latency` 及同类桶最终落到什么 verdict。
- 回报必须说明是否还需要补样本，还是只是 artifact / runner 缺口。
- 回报必须明确 no live, no default-on, no promotion, no sample expansion, no strategy change.
```
