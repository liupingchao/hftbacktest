```md
执行线程：
- 业务线程-live

任务ID：
- 0622T003

标题：
- M2 same-process watcher-triggered maker order repair

简短描述：
- 修复 `0622T002` 暴露的 trigger-to-order staleness：把 public watcher、candidate selection、即时下单 guard、post-only `Alo` submit/cancel、artifact 写入放到同一个 `awsserver1` 远端进程内，避免 watcher 触发后先 pullback 再启动新 live window。
- 本任务目标是用同一份 current candidate 快速进入 maker-only live micro-test，同时继续 fail-closed：没有当前 L2/BBO/post-only guard 通过、没有 live maker fill、或 T008 没有 fee/inventory/realized PnL 证据时，M2 仍阻塞。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0622T002` QA 为 `阻塞`，但 QA 接受其实现/安全边界并确认当前瓶颈是 watcher-to-live handoff staleness。
- `0618T008` QA 为 `已通过`，T008 ledger/reconciler 必须继续作为 M2 PnL 事实源。
- `0619T002` QA 为 `已通过`，fresh-touch / dynamic-size / session-gate 策略合同仍然有效。

规则更新提醒：
- 请先阅读 `.workflow/workflow-kit/workflow-manual.md`、`.workflow/workflow-kit/task-dispatch-template.md`、`.workflow/workflow-kit/thread-report-template.md`、`.workflow/workflow-kit/qa-acceptance-template.md`、`task_plan.md`、`progress.md`、`findings.md` 后再执行本任务。

按线程规则执行：
- `AGENTS.md`
- `docs/hyperliquid_tiny_live_real_order_executor.md`
- `docs/hyperliquid_tiny_live_final_go_no_go_gate.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- `examples/hyperliquid/hyperliquid_tiny_live_m2_fill_window.py`
- `examples/hyperliquid/hyperliquid_tiny_live_m2_fill_loop.py`
- `examples/hyperliquid/test_hyperliquid_tiny_live_m2_public_watcher.py`
- `examples/hyperliquid/test_hyperliquid_tiny_live_m2_fill_loop.py`
- 可新增窄范围 focused tests，例如 `examples/hyperliquid/test_hyperliquid_tiny_live_m2_same_process_watcher.py`
- `local_live_analysis/hyperliquid_tiny_live_m2_same_process_watcher_0622T003/**`
- `.workflow/reports/0622T003-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

read-only inputs：
- `.workflow/tasks/0622T002.md`
- `.workflow/reports/0622T002-business.md`
- `.workflow/reports/0622T002-qa.md`
- `local_live_analysis/hyperliquid_tiny_live_m2_timeboxed_watcher_0622T002/**`
- `local_live_analysis/hyperliquid_tiny_live_m2_pnl_ledger_0618T008/**`

hard boundary：
- Keep Hyperliquid `Alo` post-only. No taker/crossing, no `Ioc`, no one-tick-back workaround, no cap relaxation, no default-on behavior, no promotion, no stable PnL claim, and no M3 readiness claim.
- Dynamic order size hard cap remains `<=0.005 BTC`; smaller size is allowed and preferred when throughput is low.
- Live execution is limited to one triggered same-process micro-window and at most `2` real order submissions.
- The same-process remote process may read credentials and call private/order/cancel/open-orders endpoints only after local focused verification passes, remote checkout refresh succeeds, final gate returns go, public watcher finds an eligible current candidate, and the immediate pre-submit guard passes.
- The watcher waiting phase remains public-only until a candidate triggers. Before trigger it must not read credentials, call private/account/order endpoints, submit orders, cancel orders, or start a continuous live bot.
- Triggered submission must use the selected current candidate context from the same process. It may not discard the trigger and run another long public precheck before submission.
- A short immediate pre-submit guard is still required inside the same process: current L2/BBO must still make the selected candidate post-only/non-crossing, quote age must be within a strict configured threshold, selected quote must still be at touch or otherwise explicitly fail closed, size must still be `<=0.005 BTC`, and `Alo` must be the only TIF.
- If the immediate guard fails, do not submit an order; emit `trigger_candidate_stale_before_order` or a more specific fail-closed reason.
- Preserve tracked cancel, final open-orders proof, independent remote open-orders check, redaction, and T008 ledger fail-closed.
- Do not print or persist credential values, private keys, raw signatures, nonces, wallet secrets, or unredacted account identifiers.

implementation action：
- Add a same-process remote mode to the `0622T002` watcher path.
- In controller mode, refresh `/home/admin/hftbacktest-cross-exchange`, rerun final gate, then start one remote process that performs:
  1. public watcher loop,
  2. fresh-touch / dynamic-size / session-gate candidate selection,
  3. immediate same-process pre-submit guard using the selected current candidate,
  4. private preflight only after trigger+guard,
  5. `Alo` post-only order submission at the selected candidate quote/side/size,
  6. bounded wait/cancel/shutdown proof,
  7. final open-orders proof artifacts.
- Remove the `0622T002` latency path where controller pulls watcher artifacts before starting a separate live window.
- Record latency metrics:
  - candidate source exchange time -> watcher decision time
  - watcher decision -> immediate guard start
  - guard pass -> private preflight start
  - private preflight -> order submit call
  - submit response -> cancel/shutdown/open-orders proof
- Carry the exact selected candidate context into order intent artifacts, including side, quote, size, quality bucket, same-side top depth/order count, strict-through / at-or-through quantities, quote age, and guard decision.
- If a live order is submitted, run T008 ledger after pullback and report exact fill/fee/inventory/realized PnL facts. If no fill or incomplete economics/inventory evidence appears, T008 must fail closed.

required artifacts：
- `same_process_watcher_manifest.json`
- `same_process_latency_matrix.csv`
- `candidate_window_log.csv`
- `trigger_decision_matrix.csv`
- `selected_candidate_context.json`
- `immediate_pre_submit_guard_matrix.csv`
- `order_intent_audit.csv`
- `quote_attempt_matrix.csv`
- `window_result_matrix.csv`
- `public_stream_summary.json`
- final gate manifest and remote facts
- tracked cancel / shutdown proof
- final remote open-orders proof
- independent remote open-orders check
- aggregate live fill ledger
- T008 ledger manifest/output
- `same_process_no_submit_report.md` when trigger occurs but immediate guard blocks submission
- `watcher_no_eligible_window_report.md` when no trigger occurs

live execution rules：
- Run focused local tests and CLI checks before any remote/live-capable step.
- Refresh `/home/admin/hftbacktest-cross-exchange` by git-safe incremental bundle / `merge --ff-only`; fail if branch is not `cross-exchange` or dirty count is nonzero.
- Rerun final gate; stop before watcher/live if `allow_create_0617T008 != true`.
- Run the same-process watcher/live controller on `awsserver1` using the refreshed checkout and configured Python path.
- During watcher waiting, only public L2/trades collection is allowed.
- After trigger, submit at most `2` post-only `Alo` orders and only when the immediate guard passes.
- Always tracked-cancel live orders and prove final open orders are empty.
- Always run T008 ledger after pullback; no fill or incomplete economics/inventory evidence must return fail-closed.

expected outcomes：
- `已通过` is possible only if task implementation and QA criteria pass and either:
  - T008 proves live maker fill + fee/inventory/mark/realized PnL evidence; or
  - QA accepts that the task correctly produced complete no-eligible-window / stale-before-order fail-closed evidence under the same-process contract.
- M2 completion is possible only if T008 proves live maker fill plus complete fee/inventory/realized PnL evidence.
- If no eligible window appears, if immediate guard blocks, if no order fills, if final gate fails, if open-orders proof fails, or if T008 cannot prove realized live PnL, report `阻塞` for M2 and do not enter M3.

verify：
- `python -m pytest examples/hyperliquid/test_hyperliquid_tiny_live_m2_public_watcher.py -q`
- If a same-process watcher test file is added: `python -m pytest examples/hyperliquid/test_hyperliquid_tiny_live_m2_same_process_watcher.py -q`
- `python -m pytest examples/hyperliquid/test_hyperliquid_tiny_live_m2_fill_loop.py examples/hyperliquid/test_hyperliquid_tiny_live_m2_pnl_ledger.py -q`
- `python -m py_compile examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py examples/hyperliquid/hyperliquid_tiny_live_m2_fill_window.py examples/hyperliquid/hyperliquid_tiny_live_m2_fill_loop.py`
- `python examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py --help`
- `python examples/hyperliquid/hyperliquid_tiny_live_m2_fill_window.py --help`
- `python examples/hyperliquid/hyperliquid_tiny_live_m2_fill_loop.py --help`
- `git diff --check`
- If watcher/live reaches remote execution: remote refresh evidence, final gate go/no-go, same-process watcher/live artifacts, pulled-back artifacts, independent remote open-orders check, and T008 ledger output.

done：
- 回报 watcher runtime、public message counts、eligible candidate count、trigger count、immediate guard status、guard-to-submit latency、live submissions 数、fill_count / maker_fill_count、T008 ledger 结论、artifact 路径、blockers、commit id、提交信息。
- 明确说明 M2 是否仍 blocked；除非 T008 ledger 证明 live fill / fee / inventory / realized PnL，否则不得声明 M2 完成、稳定 PnL、或 M3 ready。
```
