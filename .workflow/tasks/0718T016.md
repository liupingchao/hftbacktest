# 0718T016 RESERVATION-INVENTORY-SKEW-C12

执行线程：
- 总控 auto-loop / 业务实现线程

任务ID：
- 0718T016

标题：
- Reservation、inventory skew 与 C12 前置验收

简短描述：
- Implement Principal Alignment Task 5 (C10/C11/C12): make reservation price and two-sided post-only quote construction typed, inventory-aware, hard-cap preserving, and auditable.
- Add an offline same-universe alpha-plus-zero-skew versus alpha-plus-bounded-skew acceptance runner. This task does not enable skew in production or start live.

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- 0718T015

规则更新提醒：
- 无

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `docs/superpowers/plans/2026-07-17-principal-alignment-p0-p3.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `examples/hyperliquid/cross_exchange_shared_signal_kernel.py`
- `examples/hyperliquid/test_cross_exchange_shared_signal_kernel.py`
- `examples/hyperliquid/cross_exchange_skew_alpha_interaction_acceptance.py`
- `examples/hyperliquid/test_cross_exchange_skew_alpha_interaction_acceptance.py`
- Local repository mapping: the plan names `test_cross_exchange_pricing_stack.py`, but the focused pricing tests in this checkout are in `test_cross_exchange_shared_signal_kernel.py`.

action：
- Add typed `ReservationResult` and `TwoSidedQuotes` interfaces for reservation and post-only quote construction.
- Compute position ratio, position notional, inventory penalty ticks/price, and reservation price; long must lower reservation and short must raise it.
- Preserve hard position cap as an independent gate. Near/over cap, block or remove inventory-worsening quote sides while retaining reduce-side participation.
- Generate reservation-derived prices before the single authoritative `post_only_price()` helper; record desired/final prices, clamp reasons, post-only invariant, and edge changes.
- Keep `PricingConfigV1.enable_inventory_skew=False` as the default. Any bounded skew comparison is offline acceptance only.
- Create deterministic same-universe A/B acceptance artifacts with separate observed-fill and conservative proxy-fill/censored labels. Include quote distance, fill coverage, 1s/5s markout, spread retention, peak inventory, recovery duration, and add/reduce outcomes.
- No live/private/order/cancel/network/remote/service action, promotion, dynamic spread, fill feedback, or multi-level implementation.

verify：
- Focused reservation tests for zero/positive/negative position, ratio/notional audit, skew sign and bounded cap.
- Near/over-cap add-side suppression with reduce-side preservation.
- Post-only desired/final/clamp/edge-change and non-crossing invariants.
- C12 acceptance runner tests for same decision universe, separate observed/proxy/censored evidence, deterministic A/B artifacts, and skew-disabled recommendation when evidence is insufficient.
- `python -m pytest examples/hyperliquid/test_cross_exchange_shared_signal_kernel.py examples/hyperliquid/test_cross_exchange_skew_alpha_interaction_acceptance.py -q`
- `python -m py_compile examples/hyperliquid/cross_exchange_shared_signal_kernel.py examples/hyperliquid/cross_exchange_skew_alpha_interaction_acceptance.py`
- `git diff --check`
- Explicitly confirm no live/private/order/cancel/network/remote/service action.

done：
- Report typed reservation/quote semantics, cap and side eligibility rules, C12 artifact metrics/recommendation, focused test counts, commit id, and endpoint-action confirmation.
