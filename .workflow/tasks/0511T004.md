```md
执行线程：
- 测试线程

任务ID：
- 0511T004

标题：
- adverse timing trigger 未命中原因诊断

简短描述：
- 基于 `0511T002` 的 Stage 6J replay 输出和新增 audit 字段，诊断 pure adverse timing candidates 为什么与 baseline 完全一致。核心问题是区分：`target_deterioration` 条件本身没有触发、触发了但没有落到 add-side action path、还是当前 replay / source-path 指标对 adverse-selection timing 风险不敏感。本任务只做诊断和报告，不改策略代码，不启动 live。

状态：
- 待验收

执行顺序：
- `0511T002` 完成并保留 replay 输出后执行

前置任务：
- 0511T002

规则更新提醒：
- 本任务是诊断任务，不做策略优化。
- 先用事实计数，再给解释。
- 必须把 trigger 未命中拆成可证伪分支，而不是直接凭直觉判断。
- 不允许因为单样本或 PnL 变化直接得出 live micro test 结论。

按线程规则执行：
- .workflow/workflow-kit/thread-report-template.md
- docs/thread-playbook.md
- docs/5-8-future-plan.md
- docs/stage6i-cancel-requested-fill-risk-plan.md

是否需要提交代码：
- 不需要

提交要求：
- 回报必须写 `commit：无`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- .workflow/reports/0511T002-business.md
- local_live_analysis/stage6j_cross_sample_0511T002/
- examples/binance_tick_mm/stage6j_replay.py
- examples/binance_tick_mm/strategy_core.py
- examples/binance_tick_mm/analyze_cancel_fill_risk.py
- findings.md
- progress.md

Fatal Failures：
- 把 replay 结果不变直接解释为策略无效，但没有验证 trigger 是否实际触发。
- 把 trigger active count 当作有效性证据，但不检查是否影响 planned action / add-side submission。
- 把 adverse-selection source-path 的 replay 低敏感性误判为真实风险不存在。
- 诊断过程中修改策略代码或启动 live。

MUST NOT：
- 不改 `strategy_core.py`、`live_tick_mm.py`、`backtest_tick_mm.py` 的策略逻辑。
- 不启动真实 live，不连接交易所，不改 AWS 状态。
- 不改变 `0511T002` 输出目录里的既有证据。
- 不使用 future markout / audit overlay 作为 live 决策输入。
- 不把单样本结论推广为上线结论。

诊断假设：
- H1：`target_deterioration` 条件过窄或方向/阈值不对，导致 pure adverse timing candidates 几乎没有 active rows。
- H2：trigger 有 active rows，但 active rows 没有覆盖 add-side action path，因此不会改变 cancel/place 行为。
- H3：trigger 影响了 action path，但 Stage 6J 当前 cancel-fill / source-path 指标没有捕捉 adverse-selection timing 风险。
- H4：live 诊断里的 adverse-selection source-path 与 replay 当前 source-path 分类定义不一致，导致 replay 对目标风险不敏感。

action：
- 读取 `0511T002` cross-sample replay 输出，按 run id 和 candidate 统计 `adverse_timing_guard_buy_active`、`adverse_timing_guard_sell_active`、reason、until_ts 和 target move ticks。
- 对比 baseline、`adverse_timing_target_deterioration_50ms`、`100ms`、`200ms`、`add_side_guard_only`、`add_side_guard_plus_adverse_timing_100ms` 的 action/planned action、cancel-fill count、source-path 和 PnL。
- 抽样检查 active rows 附近的 planned buy/sell、desired action、cancel/place 行为，判断 trigger 是否真的落到 add-side block 决策路径。
- 对比 `analyze_cancel_fill_risk.py` 的 adverse-selection candidate / source-path 分类和 Stage 6J 输出指标，判断当前 replay 是否能度量目标风险。
- 输出 `.workflow/reports/0511T004-business.md`，明确归类为 H1/H2/H3/H4 中哪些成立、哪些被证伪。
- 更新 `findings.md`，只写经过证据支持的 durable finding。

verify：
- `python3 .workflow/build_dashboard.py`
- 报告中必须包含每个候选的 trigger active count / reason count / target move tick 摘要。
- 报告中必须包含 active rows 是否改变 action path 的证据。
- 报告中必须包含 replay source-path 是否足够敏感的判断依据。
- 报告中必须包含下一步建议：改 trigger、补 replay instrumentation、还是暂不推进。

done：
- 回报必须回答：`target_deterioration` 是没触发、方向/阈值不对，还是触发但没有作用到 add-side action path。
- 回报必须回答：当前 Stage 6J replay 是否对 adverse-selection source-path 足够敏感。
- 回报必须给出证据表，而不是只给解释。
- 回报必须明确是否允许进入下一轮策略实现。默认不允许，除非诊断证据给出明确下一步合同。
- 回报必须明确 no live micro test。
```
