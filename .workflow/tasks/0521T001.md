```md
执行线程：
- 业务线程-python

任务ID：
- 0521T001

标题：
- 5-21-day-control-60min live test collection

简短描述：
- 采集一个 `5-21-day-control-60min` 的 current-format live test 样本，保持 no-rule / default-off，不引入新 candidate，不修改策略行为，只完成 live 采集与归档。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0520T002` QA 已通过

规则更新提醒：
- 请先阅读规则文档后再执行本任务。

按线程规则执行：
- `.workflow/workflow-kit/thread-report-template.md`
- `docs/thread-playbook.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0521T001.md`
- `.workflow/reports/0521T001-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `.workflow/dashboard.html`
- `.workflow/dispatch_suggestions.md`
- `local_live_analysis/5-21-day-control-60min/**`
- `local_live_analysis/archive/5-21-day-control-60min.*`

action：
- 使用当前已通过的 live/test 流程采集 `5-21-day-control-60min`。
- 只做 no-rule / default-off control live 采集，不添加新 candidate，不做策略修改，不做 tiny live，不做 promotion。
- 完成后拉回本地、归档、并为后续 replay / acceptance / diagnostics 留下完整 artifacts。

verify：
- 采集完成后确认 live audit、raw gzip、connector logs、bot logs、start/stop markers、archive/checksum 存在。
- 运行必要的 replay / acceptance / diagnostics 验证采集样本可用。
- `python3 .workflow/build_dashboard.py`
- `git diff --check`

done：
- 回报必须说明 `5-21-day-control-60min` 是否采集成功。
- 回报必须说明 archive / raw / audit / markers 是否完整。
- 回报必须明确 no live promotion, no default-on, no strategy change, no sample expansion.
- 回报必须带 `commit id` 和 `提交信息`。
```
