# 0721T041 DELAYED-HISTORY-EXACT-ENVELOPE-METADATA-REPAIR

执行线程：
- 总控 auto-loop / 业务执行线程

任务ID：
- 0721T041

标题：
- Repair delayed-history direct envelopes and independently bind history metadata

简短描述：
- 离线修复 T040 QA 发现的两个 P1 fail-open。
- Direct result 使用 exact allowlist；history target、alias、attempt 和 classifications 必须从 deterministic synthetic reference 与 raw history 独立重建。

状态：
- 未通过

执行顺序：
- 当前唯一任务

前置任务：
- `0721T040`（未通过）

规则更新提醒：
- T040 live evidence immutable；本任务不得重跑 live、private read、network、remote 或 service。
- 本任务只修复 observe-only probe producer/verifier evidence contract，不改变 endpoint timing、策略行为或任何 quote/submit/cancel 路径。
- Dynamic spread、fill feedback、inventory skew、multi-level 和任何新 live window 继续锁定。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `.workflow/reports/0721T040-qa.md`
- `docs/superpowers/plans/2026-07-17-principal-alignment-p0-p3.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交。
- 回报必须带 `commit id` 和提交信息。
- 业务报告、QA报告和固定QA文档分别按workflow提交。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- Modify: `examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- Modify: `examples/hyperliquid/cross_exchange_delayed_history_probe_acceptance.py`
- Modify/Test: `examples/hyperliquid/test_hyperliquid_tiny_live_m2_event_driven_watcher.py`
- Modify/Test: `examples/hyperliquid/test_cross_exchange_delayed_history_probe_acceptance.py`
- Workflow: task、business、QA、fixed QA、`task_plan.md`、`progress.md`、`findings.md`

source：
- Branch: `cross-exchange`
- Dispatch base: `006e188`
- Immutable live artifact: `local_live_analysis/principal_alignment_delayed_history_probe_0721T040/`

action：
- Producer direct result必须为exact object `{"status": "unknownOid"}`；拒绝`error`、`order`、`orders`和任何未知字段。
- Verifier direct result必须使用相同exact allowlist，不得以通用classifier或blacklist替代。
- 每个direct/history row的`attempt`必须为strict integer `1`；`bool`、缺失、字符串或其他值均fail closed。
- 每个direct/history row的canonical cloid token和alias token必须exact等于由task/run/window重建的synthetic token。
- History raw result必须保持exact `historical_orders` envelope；从raw `result.orders`逐行重建`exact_synthetic/foreign` classification。
- Rebuilt classifications必须exact等于producer的`historical_row_classifications`；malformed、conflicting reference或synthetic exact match均fail closed。
- `query_results`继续exact复制history row，不能作为独立真值来源。
- 加入T040 QA六个hostile fixtures：`direct_extra_orders`、`direct_error`、`foreign_history_target`、`forged_history_classification`、`false_attempt`、`conflicting_alias`。
- 原始T040 evidence不修改，只使用修复后的acceptance离线重跑。

verify：
- Focused producer/verifier tests通过。
- 上述六个hostile fixtures全部blocked，且原始valid fixture仍pass。
- Immutable T040 live artifact由修复后的verifier通过。
- Full Hyperliquid pytest通过。
- T037/T031/T026/T022/T016 exact historical boundaries不变；T026必须使用`1800s`。
- 所有验证均记录`offline_only=true`，且无live/private/order/cancel/network/remote/service动作。

done：
- Direct exact envelope和history metadata independent binding均由producer、verifier和hostile regression覆盖。
- Immutable T040 live artifact无需重跑live即可通过修复后的acceptance。
- 独立QA通过后，T040 mechanism evidence gate才可关闭，并继续执行Principal Task 8剩余项。

stop conditions：
- 修复需要修改immutable T040 evidence。
- 修复需要private endpoint、第二live window或service启动。
- 任一hostile fixture仍pass。
- 原始T040 artifact或历史exact boundary发生非预期变化。

blockers：
- Verifier未要求persisted nested `oid/cloid` identity alias严格为`<redacted>`；raw identity加匹配token/alias map可错误通过。
- 修复必须offline-only；不得重跑T040 live。

commit：
- implementation `2717d1c9a720dc6e8b399091f8008b840204e8eb`

提交信息：
- `Repair delayed history evidence binding`
