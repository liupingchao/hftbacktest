```md
执行线程：
- 业务线程-research

任务ID：
- 0617T005

标题：
- Hyperliquid tiny-live read-only signal / quote replay

简短描述：
- 在 `0617T004` signal / quote policy protocol 完成并通过 QA 后，使用已有 cross-exchange public/read-only 数据逐行回放协议规则，验证信号、持久性、报价意图、post-only 边界和 cap 触发情况。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- `0617T004` QA 必须为 `已通过`，且其最终建议必须允许 read-only replay / threshold calibration。

规则更新提醒：
- 请先阅读 `.workflow/workflow-kit/workflow-manual.md`、`.workflow/workflow-kit/task-dispatch-template.md`、`.workflow/workflow-kit/thread-report-template.md`、`.workflow/workflow-kit/qa-acceptance-template.md`、`task_plan.md`、`progress.md`、`findings.md`、`docs/hyperliquid_tiny_live_0616T006_T008_auto_loop.md` 后再执行本任务。
- 本任务是 read-only replay / calibration，不是 `0616T008` live execution。

按线程规则执行：
- `AGENTS.md`
- `docs/hyperliquid_tiny_live_0616T006_T008_auto_loop.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0617T005.md`
- `.workflow/reports/0617T005-business.md`
- `docs/hyperliquid_tiny_live_signal_quote_replay.md`
- `examples/hyperliquid/**`
- `local_live_analysis/hyperliquid_tiny_live_signal_quote_replay_0617T005/**`
- `task_plan.md`
- `progress.md`
- `findings.md`

action：
- Consume only accepted local public/read-only cross-exchange artifacts, especially the six existing cross-exchange data groups referenced by `0601T002`, `0601T003`, `0604T003`, and downstream accepted read-only artifacts.
- Consume the final `0617T004` signal / quote policy protocol as the rule source. Do not invent a different trading rule.
- Implement or run a read-only replay that processes eligible joined rows in timestamp order and, per row:
  - compute `basis_mid = binance_mid - hyperliquid_mid`;
  - compute normalized `basis_mid_ticks` using the accepted tick-size policy;
  - apply the `0617T004` threshold and trigger-persistence rule;
  - map positive eligible signal only to Hyperliquid maker buy intent;
  - map negative eligible signal only to Hyperliquid maker sell intent;
  - generate theoretical quote price, quote distance, post-only flag, and crossing check from Hyperliquid public BBO/mid;
  - apply stale join, data-gap, public-book quality, spread, quote-distance, post-only, cap, duration-window, and signal-decay rejection/cancel reasons;
  - simulate cap checks for per-order size `0.01 BTC` and max position `0.04 BTC` without creating real orders or claiming fills.
- Output replay artifacts covering at minimum:
  - trigger counts by sample, side, threshold state, and persistence state;
  - direction distribution and no-signal / rejected / cancel-reason distribution;
  - stale/data-gap rejection counts;
  - quote crossing / post-only violation counts;
  - quote distance distribution;
  - theoretical cap breach / reduce-side-only trigger counts;
  - threshold coverage and sensitivity only if `0617T004` explicitly allows calibration;
  - row-level audit samples sufficient to inspect signal inputs, timestamps, basis ticks, threshold state, side intent, quote price, quote distance, post-only flag, size, cap checks, and cancel reason.
- Preserve proof boundaries: replayed intents are not real orders, simulated caps are not account inventory proof, and public-only replay is not PnL, fill-probability, post-only-reject, queue-priority, or maker-viability proof.
- Do not implement a live bot, connector, signed client, private endpoint call, account query, order placement, order cancellation, order amendment, user stream, nonce/signing logic, deployment, promotion, PnL proof, or maker viability claim.

acceptance criteria：
- Replay consumes the `0617T004` protocol as the rule source and documents any parameter defaults used.
- Replay covers the accepted existing cross-exchange public/read-only datasets available locally, or explicitly lists any missing/invalid sample with fail-closed reason.
- Outputs include trigger counts, side distribution, stale/data-gap rejection, quote crossing/post-only violation, quote distance, cap-trigger diagnostics, and row-level audit samples.
- The task clearly states whether current `0617T004` thresholds are mechanically replayable, need calibration, or remain blocked.
- The task does not claim real fills, real PnL, real inventory, real post-only reject behavior, queue priority, deployment readiness, or maker viability.
- Final recommendation must be exactly one of:
  - `hyperliquid_tiny_live_signal_quote_replay_ready_for_qa`
  - `hyperliquid_tiny_live_signal_quote_replay_needs_threshold_calibration`
  - `hyperliquid_tiny_live_signal_quote_replay_blocked`

verify：
- Run focused tests for any new replay implementation with `/home/liushuai/workspace/hftbacktest/.conda-envs/hft-py38/bin/python`.
- Validate any generated JSON manifest with `/home/liushuai/workspace/hftbacktest/.conda-envs/hft-py38/bin/python -m json.tool <artifact>.json`.
- Check required CSV/markdown artifacts are non-empty.
- Boundary text check for no live/order/private authorization and no PnL/fill/maker-viability overclaim.
- `git diff --check`

done：
- 回报 replay 输入数据范围、样本数量、signal formula、threshold/persistence source、trigger counts、方向分布、stale/data-gap 拒绝、quote crossing/post-only 检查、quote distance、cap-trigger 诊断、row-level audit 输出位置。
- 明确说明本任务是否支持后续创建 `0616T008`，或是否必须先做 threshold calibration / protocol repair。
- 明确说明没有读取凭证、没有调用私有接口、没有账号查询、没有下单/撤单/改单、没有 live bot。
```
