# 0720T019 FINAL-OPEN-ORDER-SNAPSHOT-DECOUPLING

执行线程：
- 总控 auto-loop / 业务执行线程

任务ID：
- 0720T019

标题：
- Decouple final open-order reconciliation from position snapshot failure

简短描述：
- Close the two remaining T018 QA defects without changing trading behavior。
- A valid final open-order snapshot must always refresh visible order state and working exposure；position snapshot failure remains explicit and blocking but must not hide known working orders。

状态：
- 未通过

执行顺序：
- 当前唯一任务

前置任务：
- `0720T018`（未通过；malformed/query-filled defects accepted, final snapshot semantics remain open）

规则更新提醒：
- 本任务 offline-only。
- No live/private/account/order/cancel/network/remote/service action is authorized。
- New bounded live、Task 8 and adaptive/multi-level activation remain locked。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `.workflow/reports/0720T018-qa.md`
- `docs/superpowers/plans/2026-07-17-principal-alignment-p0-p3.md`

是否需要提交代码：
- 是

提交要求：
- 实现和测试提交。
- 业务报告单独提交。
- 回报必须带 implementation commit id 和提交信息。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

source：
- Branch: `cross-exchange`
- Dispatch base: `bb801a1`

files：
- `examples/hyperliquid/hyperliquid_maker_order_manager.py`
- `examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- Focused manager/watcher tests
- `.workflow/tasks/0720T019.md`
- `.workflow/reports/0720T019-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

action：
- Make supplied final snapshot reconciliation accept a valid final open-order list independently of user-state validity。
- When final open-orders are valid:
  - restore every reappearing managed reference from exchange row to resting/partial according to remaining size；
  - override local `cancel_requested` display state while preserving cancel request timestamps/audit fields；
  - calculate owned count and working exposure from those rows；
  - keep an explicit blocker/diagnostic if the position snapshot is unavailable。
- When final user-state is invalid, do not fabricate a zero position or discard valid order facts。
- Keep T018 malformed parser、query-filled unresolved、T017 v2/v3/cardinality and T016 replay behavior unchanged。
- Add adversarial tests for partial final snapshot and cancel-ack-but-still-open reappearance。

verify：
- Focused manager/watcher/fill/acceptance tests pass。
- Full `python -m pytest -q -p no:cacheprovider examples/hyperliquid` passes。
- Modified modules/tests pass `py_compile`。
- Acceptance CLI `--help` passes。
- Partial final snapshot produces nonzero final owned/working exposure and a position-evidence blocker rather than stale zero exposure。
- Final exchange row overrides `cancel_requested` to resting/partial while cancel audit metadata remains。
- T018 malformed/query-filled and T017 valid v3/v2/cardinality tests remain green。
- Exact T016 replay remains byte-preserving, decision `43/43` pass and lifecycle blocked for historical attempt 2。
- `git diff --check` and implementation `git show --check` pass。
- No live/private/account/order/cancel/network/remote/service action occurs。

done：
- Final operator order/exposure facts cannot disagree with a valid final open-order snapshot。
- Position snapshot failure remains explicit and blocking without fabricating position data。
- Exchange-present orders are shown as resting/partial even if a prior local cancel request existed, with audit metadata preserved。
- Independent QA accepts T019 before any new bounded live task。

stop conditions：
- A valid final open-order list can be ignored because position read failed。
- Final status reports zero working exposure while tracked final open orders are present。
- Exchange-present order remains displayed as cancel_requested。
- Any position data is fabricated after user-state failure。
- T017/T018 accepted behavior regresses, historical T016 is upgraded, or strategy/risk/activation changes。
- Any verification requires live/private/account/order/cancel/network/remote/service。

blockers：
- Independent QA failure keeps new bounded live、Task 8 and adaptive/multi-level locked。

commit：
- implementation `b0115d7cfe09f4612f68581481215f79026f0bfc`

提交信息：
- `Decouple final open-order snapshot reconciliation`
