```md
执行线程：
- 业务线程-docs

任务ID：
- 0511T003

标题：
- 升级 workflow dashboard 为实验决策看板

简短描述：
- 将当前 `.workflow/dashboard.html` 从任务索引升级为实验决策看板。看板应同时展示任务状态、业务结果摘要、QA 结论、关键实验指标、是否允许 live / 是否 no-promotion、下一步建议和 durable findings。第一版只改 markdown 解析和静态 HTML 生成，不引入数据库、不引入前端框架、不增加交互执行能力。

状态：
- 待执行

执行顺序：
- 可独立执行，不依赖 0511T001/0511T002

前置任务：
- 无

规则更新提醒：
- 本任务只改 workflow 展示层。
- 不修改策略代码。
- 不修改已有 business/QA report 的事实内容。
- dashboard 仍然是静态 HTML，由 `.workflow/build_dashboard.py` 从 markdown 自动生成。

按线程规则执行：
- .workflow/workflow-kit/thread-report-template.md
- .workflow/workflow-kit/workflow-web-field-mapping.md
- docs/thread-playbook.md

是否需要提交代码：
- 需要

提交要求：
- 提交前必须包含 dashboard 生成和 smoke check 证据
- 回报必须写 `commit：<hash>` 或 `commit：无`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- .workflow/build_dashboard.py
- .workflow/dashboard.html
- .workflow/dispatch_suggestions.md
- .workflow/tasks/*.md
- .workflow/reports/*.md
- docs/qa-acceptance-report.md
- findings.md
- progress.md

Fatal Failures：
- 看板仍然只能看到需求/输入，看不到任务结果和决策含义。
- dashboard 生成依赖手工编辑 HTML，而不是从 markdown 自动生成。
- 看板展示错误地暗示可以 live micro test 或可以上线规则。

MUST NOT：
- 不改策略代码。
- 不改已有 report 的事实内容。
- 不引入数据库。
- 不引入前端框架。
- 不做交互编辑。
- 不把 dashboard 变成任务执行器。
- 不把自由文本启发式提取结果伪装成强事实；提取不到时必须显示未知或无明确记录。

目标展示能力：
- 展示每个任务的状态、QA 状态、执行线程、是否需要提交代码、前置任务。
- 展示最新 business report 的状态、done 摘要、verify 摘要、blockers、commit。
- 展示最新 QA report 的状态、验收结论、通过项、不通过项、阻塞项、建议总控下一步。
- 展示关键实验指标：decision、samples、candidates、hard failures、run id、acceptance passed、cancel-fill count/rate、`diagnostic_only_no_promotion`、是否允许 live micro test、下一步建议。
- 展示 durable findings 或从 business/QA report 中抽取出的关键发现。

建议页面结构：
- 任务ID + 标题 + 状态 badge。
- 状态摘要：执行线程、QA 状态、是否需要提交代码、前置任务。
- 决策摘要：latest business status、key decision、samples/candidates/hard failures、live micro test allowed。
- 关键指标：run id、acceptance、cancel-fill、Stage 6J decision、hard failures。
- 业务结果：从 `done` 中抽取前 3-5 条高价值摘要。
- QA 结论：QA 状态、验收结论、不通过项、阻塞项。
- 下一步：从 QA report 或 business report 提取建议。

action：
- 在 `.workflow/build_dashboard.py` 增加摘要提取函数，例如 `extract_decision_summary(record)`、`extract_metric_chips(record)`、`extract_next_steps(record)`、`extract_findings(record)`。
- 增强 `TaskView`，为每个任务提供 business summary、QA summary、decision summary、metric chips、next steps、risk flags。
- 修改 dashboard HTML，让任务卡新增“决策摘要”“关键指标”“业务结果”“QA 结论”“下一步”等 section。
- 保持旧 report 兼容：如果没有结构化字段，则从 `done`、`verify`、`验收结论`、`建议总控下一步` 中启发式提取；提取不到显示 `无明确记录`。
- 保持 dispatch suggestions 可生成，必要时补充对已通过/待验收/待执行任务的建议文案。
- 运行 dashboard 生成，并用 smoke check 确认关键结果出现在 HTML 中。

verify：
- `python3 .workflow/build_dashboard.py`
- `rg "diagnostic_only_no_promotion|hard failures|cancel-requested|live micro test|0510T001|0510T002|0511T001|0511T002" .workflow/dashboard.html`
- 打开 `.workflow/dashboard.html`，确认每张任务卡能看到状态、结果、QA 和下一步。

验收标准：
- `0510T001` 看板卡片能直接看出：已通过、单样本闭环跑通、run id 为 `5-10-day-control-1h-06`、acceptance passed、cancel-requested fills 为 `21 / 46`、风险更偏 adverse-selection / inventory-reducing cancel race、不建议 live micro test。
- `0510T002` 看板卡片能直接看出：跨样本 Stage 6J 完成、samples 为 `4`、candidates 为 `6`、hard failures 为 `0`、decision 为 `diagnostic_only_no_promotion`、不自动进入 live micro test。
- `0511T001` 看板卡片能直接看出：待执行、设计任务、不改策略代码。
- `0511T002` 看板卡片能直接看出：待执行、前置为 `0511T001`、实现任务、必须 default-off。
- 看板没有把未知/启发式提取内容当作确定事实。

done：
- 回报必须说明 dashboard 新增了哪些展示区域。
- 回报必须说明关键字段是结构化读取还是启发式提取。
- 回报必须给出 smoke check 命令和关键输出。
- 回报必须说明是否仍有需要结构化 report 字段的后续改进。
```
