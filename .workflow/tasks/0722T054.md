# 0722T054 P3-MULTI-LEVEL-FAIL-CLOSED-REPAIR

执行线程：
- 总控 auto-loop / 业务执行线程

任务ID：
- 0722T054

标题：
- Repair multi-level envelope, unknown-stop, watcher consumption and restart gates

简短描述：
- 修复 T053 独立 QA 的两个决定性 P1，并收口总控保留的正常 watcher
  consumer、restart provenance 和 per-submit halt 边界；保持 default-off、
  offline-only 和 standing live envelope 不变。

状态：
- 未通过

执行顺序：
- 当前唯一任务

前置任务：
- `0722T053`（未通过；implementation `68416c1`，QA records `598216d`）
- `0722T052`（已通过；单层双边 lifecycle prerequisite）

规则更新提醒：
- offline-only；禁止 live、private/account、真实 order/cancel、network、
  remote、service 和 cap expansion。
- 所有 endpoint tests 仅允许 fake client。
- Standing envelope 保持 `0.005 BTC/order`、`0.01 BTC position`、
  `1 USDC loss`、`2 submissions/window`、`1800s/window`。
- Multi-level live、经济性、promotion 和完整 restart readiness 均不得宣称。

是否需要提交代码：
- 需要

是否进行QA验收：
- 是

files：
- `examples/hyperliquid/hyperliquid_maker_order_manager.py`
- `examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- `examples/hyperliquid/test_hyperliquid_maker_order_manager.py`
- `examples/hyperliquid/test_hyperliquid_tiny_live_m2_event_driven_watcher.py`
- 必要的 focused watcher test
- workflow task/business/QA/fixed QA records

action：
- Active multi-level manager 必须获得显式 `runtime_config`；不得继承 legacy
  max-30 fallback。Standing cap `2` 下四层目标必须零 order calls。
- Batch submit 任一 addition 最终 unresolved `unknown` 后立即停止剩余
  additions，持有 unknown exposure，并要求 authoritative reconcile。
- Manager 提供 per-submit `before_submit` callback；normal live watcher 在每个
  fake/real endpoint 调用前重新执行 persistent halt gate。Live-mode manager
  缺少 callback 必须 fail-closed。
- `build_task7_desired_quotes` 和 `build_task7_order_manager` 接受 guarded
  ladder config；default 路径保持 single-level，active 路径通过严格
  ladder conversion 和 matching multi-order manager。
- Normal manager cycle 将同一 ladder config 同时传给 quote builder 和
  manager，并保持 dynamic spread/fill feedback 与首次 multi-level
  activation 互斥。
- Startup 恢复未知 generation/submission provenance 时允许 hold/cancel，
  但在没有 durable provenance 时禁止任何新增 submit，避免 historical
  cloid 或 submission budget 重用。
- Business/QA 报告必须记录 exact implementation commit。

verify：
- Hostile omitted-runtime-config：active multi-level construction 在任何
  endpoint call 前失败。
- Hostile unknown-submit：四层目标只发生一次 fake order call，后续 additions
  标记 blocked，unknown leaves 继续计入 exposure。
- Hostile halt callback：每个 submit 前调用；第二次 halt 时只允许首个
  endpoint call。
- Watcher default single-level behavior unchanged；active guarded config 构造
  多层 desired 和 matching manager；standing cap `2` 零调用。
- Restart generation-1 hostile：startup 后 hold/cancel 可用；cancel-confirmed
  后 readd 被 provenance gate 阻止，不复用 cloid。
- Focused manager/ladder/watcher、full `python -m pytest
  examples/hyperliquid -q`、py_compile、`git diff --check`。

done：
- T053 两个 QA P1 有精确 hostile regression 并修复。
- Normal watcher offline action path、restart fail-closed 和 per-submit halt
  gate 通过独立 QA。
- Principal Task 10 可关闭为 code/action-path readiness；multi-level live
  仍需后续独立风险 envelope 和 durable restart task。

blockers：
- 若修复需要扩大 submission cap 或执行 multi-level live，则本任务必须
  fail-closed；不得自行授权。

提交信息：
- `Fail closed guarded multi-level execution`
