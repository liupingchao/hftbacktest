# 0718T022 P3-REAL-TIME-STATUS-FILE

执行线程：
- 总控 auto-loop / 业务实现线程

任务ID：
- 0718T022

标题：
- P3 real-time status file expansion and writer failure boundary

简短描述：
- Extend the existing Task 7 atomic throttled `live_status.json` into the complete Task 11 monitoring contract without changing quote, order, risk, estimator or activation behavior.

状态：
- 执行中

执行顺序：
- 当前唯一任务

前置任务：
- 0718T021

规则更新提醒：
- Task 7 already provides an atomic monotonic-throttled writer and a minimal status payload.
- T021 proves only a default-off multi-level gate; this task must expose that gate and current level state, not activate multi-level.
- Live remains the main data source for later tiny-live work, but this task is an offline contract/instrumentation task. No live run is required.

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

files：
- Modify: `examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- Test: `examples/hyperliquid/test_hyperliquid_tiny_live_m2_event_driven_watcher.py`

action：
- Extend the versioned status schema with run/window/attempt/config/model identity; source exchange/local timestamps and freshness; mid/micro/forecast/reservation; desired/final bid/ask; fixed and dynamic spread components; fill offset; signal score/confidence.
- Expose position, working/inflight exposure, open orders grouped by side/level/state, fill exposure and partial/full fill state, toxicity/risk/kill-switch, last action/block/error, heartbeat and writer health.
- Keep `LiveStatusWriter(path, min_interval_seconds, clock)` stateful; preserve temp-file plus `os.replace`, monotonic throttling and redaction.
- Define writer failure as observable in the main audit/status path and fail-closed/degraded according to an explicit policy; a dashboard must not be required for correctness.
- Keep status writes bounded and non-blocking for the existing watcher paths; do not alter quote selection, order submission/cancel behavior, estimator candidates or activation flags.

verify：
- Focused tests for full schema, defaults, nested order/fill state, writer monotonic throttling, atomic replacement, cleanup and failure/degraded boundary.
- Existing event-driven watcher and manager/status regressions.
- Modified Python `py_compile`, watcher CLI `--help`, and `git diff --check`.

done：
- A complete, versioned, remotely readable status artifact is emitted by the existing watcher/status paths.
- Writer failures are represented as an explicit degraded/fail-closed status and do not silently disappear.
- No quote/order behavior or activation status changes.

blockers：
- Do not claim monitoring completeness if a required field is omitted, writer failure is silent, or any live quote/order behavior changes.

commit：
- 无

提交信息：
- 无
