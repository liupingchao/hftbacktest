# 0718T023 P3-CUMULATIVE-TINY-LIVE-SAME-WINDOW-ACCEPTANCE

执行线程：
- 总控 auto-loop / 业务实现线程

任务ID：
- 0718T023

标题：
- P3 cumulative tiny-live and same-window acceptance

简短描述：
- Execute Principal Alignment Task 12 with live as the primary evidence source and focused same-window replay as decision/config/control reproduction evidence.
- Because Task 10 activation is still blocked, the first cumulative window remains single-level, fixed-spread, post-only and activation-off.

状态：
- 阻塞

执行顺序：
- 当前唯一任务

前置任务：
- 0718T022

规则更新提醒：
- Standing authorization permits uninterrupted task-scoped live work within BTC, Binance lead / Hyperliquid maker, `0.005 BTC` per order, `0.01 BTC` aggregate position delta, `1 USDC` max loss, `2` submissions and `1800s` per bounded window.
- Live is the main data source. Shadow/replay prevents obvious errors, reproduces live decisions and controls variables; it is not a long pre-live research stage.
- Execution-safety and evidence-integrity gates remain mandatory before any real order.
- T021 did not unlock multi-level. Dynamic spread, fill feedback, inventory skew and multi-level activation remain off in this task.

是否需要提交代码：
- 视执行中发现的 acceptance/tooling gap 决定；如无代码 gap，可只提交 workflow/evidence report。

提交要求：
- 如有代码改动，自测通过后提交
- 回报必须带 `commit id` 和 `提交信息`；无代码改动时写无

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

files：
- Inspect/modify only if required: existing `examples/hyperliquid` live orchestrator, watcher, replay/acceptance helpers and focused tests.
- Evidence: `local_live_analysis/principal_alignment_task12_0718T023/`

action：
- Freeze exact authorization envelope, source commit, config/model hashes and activation-off state.
- Run local focused preflight: kill-switch clear, status writer healthy, account/service provenance contract, price/post-only/runtime exposure and artifact path checks.
- Use an isolated clean remote clone and existing approved Hyperliquid SDK environment.
- Run one bounded single-level fixed-spread post-only tiny-live window. Do not relax edge, queue, freshness, risk, size, submission or max-loss gates merely to force a fill.
- Collect order/fill/fee/role/inventory lifecycle, `live_status.json`, writer audit status, final owned open-orders proof, account position reconciliation and terminal checksum.
- Pull artifacts locally and verify remote/local SHA-256.
- Run focused same-window replay reproducing decision/config/control variables. Exact queue-fill prediction is not required; live order/fill facts remain authoritative.
- State unsupported claims explicitly. Single-digit fills support mechanism/path conclusions, not stable economics.

verify：
- Exact source commit and clean isolated remote worktree.
- Exact task envelope and all activation flags false.
- Preflight account/service/kill-switch/status-writer/risk/evidence gates.
- Endpoint call counts, lifecycle, liquidity role/fee evidence, final open orders, position and max-loss reconciliation.
- Terminal artifact checksum and remote/local SHA-256 equality.
- Same-window replay decision/config/control diff and anti-optimism checks.
- Existing focused/full regressions if code changes occur.

stop conditions：
- duplicate/ambiguous fill;
- window/attempt identity mismatch;
- unknown order state;
- owned open orders cannot be proven empty;
- account position cannot be reconciled;
- price precision/post-only invariant failure;
- process termination failure;
- runtime envelope or max-loss breach;
- status writer failure;
- artifact checksum failure;
- concurrent account activity contamination.

done：
- QA may accept mechanism/evidence integrity even with zero or single-digit fills, but must not claim stable profitability or maker viability.
- Multi-level can be reconsidered only if this task obtains and QA accepts a real single-level resting/fill lifecycle; otherwise T021 gate remains blocked.

blockers：
- Any stop condition ends the current window and prevents further strategy-variable expansion.

commit：
- a57c7da

提交信息：
- Enforce task-scoped live envelope and artifact identity
