# 0720T014 ATTEMPT-KEY-BOUNDED-PARSER-REPAIR

执行线程：
- 总控 auto-loop / 业务执行线程

任务ID：
- 0720T014

标题：
- Bound attempt-key suffix parsing and preserve fail-closed acceptance

简短描述：
- Repair the sole `0720T013` QA finding by removing the unbounded integer conversion from canonical attempt-key validation.
- Malformed, oversized and out-of-range key suffixes must return a blocked acceptance manifest without raising.

状态：
- 待执行

执行顺序：
- 当前唯一任务

前置任务：
- `0720T013`（未通过）

规则更新提醒：
- 本任务是 offline-only minimal repair。
- 不得调用 live、private、account、order、cancel、network、remote 或 service。
- T013 已通过的 identity、authorization、causal join、legacy bridge、path/source/checksum/fill/cancel 行为不得改变。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `.workflow/reports/0720T013-qa.md`

是否需要提交代码：
- 需要

提交要求：
- 先提交 implementation，再提交 business report。
- 回报必须带 implementation `commit id`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- Modify: `examples/hyperliquid/cross_exchange_t024_same_window_acceptance.py`
- Modify: `examples/hyperliquid/test_cross_exchange_t024_same_window_acceptance.py`
- Workflow: this task, business report, QA report, `docs/qa-acceptance-report.md`, `task_plan.md`, `progress.md`, `findings.md`

action：
- Parse attempt-key suffix with the same bounded contract as `raw_strict_positive_attempt()`.
- Do not call unbounded `int()` on attacker-controlled numeric strings.
- Keep exact task, `window_01`, row window and attempt-id equality checks.
- Add synchronized/resealed regressions for:
  - 5000-digit suffix;
  - suffix above `RAW_MAX_CANCEL_REFERENCE_ATTEMPT`;
  - valid maximum-range behavior where applicable.

verify：
- Acceptance returns a blocked manifest and `attempt_key_mismatch` for oversized/out-of-range keys; it does not raise.
- Existing valid two-sided writer fixture passes.
- Focused acceptance/watcher/orchestrator/manager tests pass.
- Full `python -m pytest -q examples/hyperliquid` passes.
- Modified modules/tests pass `py_compile`.
- Acceptance CLI `--help` passes.
- T011 byte-exact replay with explicit legacy bridge remains `validation_reasons=[]`, provenance `112/112`, lifecycle blocked.
- `git diff --check` passes.
- No live/private/account/order/cancel/network/remote/service action occurs.

done：
- Every attempt-key suffix uses bounded parsing and malformed evidence fails closed as a manifest.
- No T013 accepted sub-result regresses.
- Independent QA accepts T014 before any new live task is dispatched.

stop conditions：
- Fix requires changing strategy formula, thresholds, risk caps, activation or producer quote behavior.
- T011 loses deterministic replay or becomes lifecycle accepted.
- Any verification requires live/private/account/order/cancel/network/remote/service.

blockers：
- Independent QA failure keeps new live and multi-level locked.

commit：
- 待业务线程提交

提交信息：
- 待业务线程填写
