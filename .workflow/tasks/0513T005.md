```md
执行线程：
- 业务线程-python

任务ID：
- 0513T005

标题：
- Latency and market-data integrity baseline 计划

简短描述：
- 基于十步计划 Step 2，制定 latency / market-data integrity baseline 的实施计划和验收方案。只做计划，不实现代码、不运行新 replay、不启动 live。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- 0513T004

规则更新提醒：
- Step 1 的标准 run live 流程已由 `0513T004` 建立：任何后续 live 样本必须先通过 preflight manifest / schema compatibility gate。
- 本任务只规划 Step 2，不采集新样本、不改代码、不做 live promotion。

按线程规则执行：
- .workflow/workflow-kit/thread-report-template.md
- docs/thread-playbook.md
- task_plan.md
- .workflow/reports/0513T004-business.md
- .workflow/reports/0513T004-qa.md

是否需要提交代码：
- 不需要

提交要求：
- 不需要提交
- 回报必须写 `commit：无`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

Allowed files：
- `.workflow/tasks/0513T005.md`
- `.workflow/reports/0513T005-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `.workflow/dashboard.html`
- `.workflow/dispatch_suggestions.md`

Forbidden files / areas：
- `examples/binance_tick_mm/*.py`
- `examples/binance_tick_mm/deploy/*.sh`
- strategy rule logic, fair/target formula, risk guards, quote placement
- core `hbt.depth(0)` API, converter/npz schema, connector local book management
- AWS/remote deployment state
- live samples, raw market-data artifacts, archive tarballs

Requires separate task：
- 实现 latency/data-quality 分析脚本。
- 修改 audit schema、converter/npz schema 或 connector。
- 启动 fresh live no-rule run。
- 修改 `maker_acceptance.py` 的 market-view gate。
- 做 microprice / OFI / queue feature research。

Fatal Failures：
- 本任务中启动 live 或采集新样本。
- 本任务中修改策略、部署脚本、core/data schema 或 acceptance 脚本。
- 把当前已有 top5 mismatch 或 action-path acceptance 直接解释成 pricing/queue research 可用。

action：
- 制定 Step 2 的实施计划，拆清楚后续应收集/计算的指标：
  - feed latency、event-to-order latency、strategy compute latency、order entry/response latency、jitter。
  - Binance depth reconstruction quality：update-id continuity、depth/bookTicker consistency、best bid/ask drift、top5 tick/qty mismatch buckets、startup exclusion window。
  - per-decision market-view provenance：`market_view_source`、`top5_source`、overlay source、top5 source、book view timestamps。
  - raw data / converted npz / audit 三层之间的可追溯关系。
- 指定可复用样本和优先顺序：
  - `5-13-day-control-15min` 作为 T002/T004 后的最新 no-rule sanity sample。
  - `5-11-night-active` 作为长样本诊断参考。
  - `5-10-day-control-1h-06` / `5-9-noon` / `5-9-small` 作为 cross-sample sanity checks。
- 定义后续实现任务的输出格式：
  - sample-level latency summary。
  - market-data integrity summary。
  - sample usability classification：compressed action-path only / pricing research candidate / queue-fill research candidate / unusable。
  - explicit later core/data requirements if Python strategy layer cannot expose required Binance provenance。
- 定义验收方案和 fail/pass/block 条件。
- 明确 Step 2 结束后是否进入 Step 3 market-view acceptance gate、core/data task，或 fresh live sample task。

verify：
- 只做文档一致性检查：
  - `python3 .workflow/build_dashboard.py`
  - 人工检查任务边界：无代码实现、无 live、无 replay。

done：
- 回报必须给出 Step 2 的可执行计划、指标清单、样本优先级、输出 artifact 设计、验收标准、后续任务拆分建议。
- 回报必须明确本任务未实现代码、未运行 live、未运行 replay。
- 回报必须写 `commit：无`。
```
