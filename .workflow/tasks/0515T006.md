```md
执行线程：
- 业务线程-python

任务ID：
- 0515T006

标题：
- Replay fill trigger diagnosis for residual case 4948

简短描述：
- 针对 `0515T005` 之后唯一剩余的 `28940|sell` / `4948` replay-only fill residual，做一个更细的只读 trigger diagnosis。任务只研究这一个 case 的 replay fill 触发证据，不做 repair，不改策略，不补样本，不启动 live。

状态：
- 已通过

执行顺序：
- 当前唯一后续任务

前置任务：
- `0515T005` QA 已通过

规则更新提醒：
- 本任务是单-case read-only diagnosis，不是 repair。
- 只允许研究 `4948` 这一条 residual replay fill 的触发证据。
- 不允许修改 replay fill model / touch fill logic / queue exposure model / strategy logic。

按线程规则执行：
- .workflow/workflow-kit/thread-report-template.md
- docs/thread-playbook.md
- task_plan.md
- progress.md
- findings.md

是否需要提交代码：
- 需要

提交要求：
- 自测/检查通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

Allowed files：
- `.workflow/tasks/0515T006.md`
- `.workflow/reports/0515T006-business.md`
- `examples/binance_tick_mm/replay_lifecycle_mismatch_diagnosis.py`
- `examples/binance_tick_mm/test_replay_lifecycle_mismatch_diagnosis.py`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `.workflow/dashboard.html`
- `.workflow/dispatch_suggestions.md`
- `docs/qa-acceptance-report.md`

Allowed generated outputs：
- `local_live_analysis/5-13-day-control-30min/stage6g_single_residual_fill_diagnosis_0515T006/**`

Allowed read-only inputs：
- `.workflow/reports/0515T005-business.md`
- `.workflow/reports/0515T004-business.md`
- `local_live_analysis/5-13-day-control-30min/stage6f_cancel_race_residual_repair_0515T005/**`
- `local_live_analysis/5-13-day-control-30min/stage6e_residual_replay_fill_diagnosis_0515T004/**`
- `local_live_analysis/5-13-day-control-30min/audit_live_5-13-day-control-30min.csv`
- `local_live_analysis/5-13-day-control-30min/out/backtest_audit_replay/audit_bt_audit_replay.csv`
- `local_live_analysis/5-13-day-control-30min/raw_market_data/**`
- `local_live_analysis/5-13-day-control-30min/t009_fixed_sidecar/**`

Forbidden files / areas：
- `examples/binance_tick_mm/backtest_tick_mm.py`
- `examples/binance_tick_mm/strategy_core.py`
- strategy behavior, fair/target formulas, configs, risk guards, quote placement behavior
- replay engine behavior / backtest fill model implementation
- live scripts, AWS/remote state, live collection
- sample expansion tasks
- core Rust, connector, py `event_dtype`, standard npz schema

Requires separate task：
- 修改 replay touch fill assumption。
- 修改 queue exposure / trade-consumption general model。
- 针对 `4948` 的 repair implementation。
- 新采集 current-format 样本。

Fatal Failures：
- 在 diagnosis 任务里直接改 replay repair 代码。
- 将 `4948` 的孤例 diagnosis 扩展成 generalized queue/touch repair。
- 只凭不足的 trigger evidence 就下精确 root-cause 结论。

action：
- 只分析 `28940|sell` / `4948` 这一条 residual case。
- 必须进一步拆清：
  - replay fill 前后的 raw trade / raw depth / bookTicker 触发链
  - top5 sidecar 对应时刻是否显示真实可成交迹象
  - 当前 supportive trade 计数为何为 `0`
  - replay fill 更像：
    1. hidden trigger path not covered by current supportive trade heuristic
    2. trade-consumption approximation issue
    3. touch fill assumption optimism
    4. other unexplained replay-only artifact
- 回报必须明确：
  - 当前证据是否足够支持一个专门的 `4948` repair task
  - 如果还不够，缺的具体证据是什么

verify：
- `python -m pytest examples/binance_tick_mm/test_replay_lifecycle_mismatch_diagnosis.py`
- `python examples/binance_tick_mm/replay_lifecycle_mismatch_diagnosis.py --help`
- `python3 .workflow/build_dashboard.py`
- `git diff --check`

done：
- 回报必须明确 `4948` residual 的 trigger diagnosis 结论。
- 回报必须明确是否值得新开 repair task，还是应停止在 diagnosis。
- 回报必须明确本任务未修改 replay、未改策略、未补样本、未启动 live。
- 回报必须写 `commit id` 和 `提交信息`。
```
