# 任务派发

执行线程：
- Target Project Argv Contract Repair 业务线程

任务ID：
- 0902T001

标题：
- 修复 Python argv contract 并完成 effect-free qualification

简短描述：
- 在从精确提交
  `1051f2b29059e6b7465fe8051de01072f9ff7e19`
  创建的独立 worktree 中，修复 runner/checker 将完整 `exec_argv` 直接与
  Python `sys.argv` 比较的确定性缺陷。
- 只完成软件 qualification、证据和独立 QA，不执行任何业务任务或 Q0。

状态：
- 已通过

执行顺序：
- 当前唯一活动工程任务

前置任务：
- `0901T001-SCOPE-REBASELINE-01`
- scope rebaseline commit：
  `d6f5147f4fd8d98b1a15d4cd90706f015fcc033a`
- controller boundary commit：
  `45514a9a380552fe03c1a6c5fbdb7a7bf7585d86`

规则更新提醒：
- 请先阅读 `AGENTS.md`、workflow-kit、`task_plan.md`、`progress.md`、
  `findings.md` 和原 `0831T001` closure/QA evidence。

按线程规则执行：
- `AGENTS.md`
- `.workflow/workflow-kit/workflow-manual.md`

是否需要提交代码：
- 需要

提交要求：
- 治理注册、实现和测试/业务报告按职责边界分别提交。
- 回报必须带完整 commit id、文件范围和验证结果。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 新的独立 QA actor 只读审查精确修复提交、测试和 effect-free 证据；
  不执行业务计算。

files：
- `AGENTS.md`
- `.workflow/tasks/0902T001.md`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `examples/hyperliquid/skhynix_trade_led_depth_follower_q0_pipeline_qualification.py`
- `examples/hyperliquid/test_skhynix_trade_led_depth_follower_q0_pipeline_qualification.py`
- `.workflow/reports/0902T001-business.md`
- `.workflow/reports/0902T001-qa.md`
- `docs/qa-acceptance-report.md`

action：
- 保留完整 `exec_argv = [python_executable, script_path, ...args]`。
- 派生 `program_argv = [script_path, ...args]`。
- 要求 observed Python `sys.argv == program_argv` 且不等于完整
  `exec_argv`。
- 固定并验证 runtime、script、argv、cwd 和 `shell = false`。
- 增加 effect-free qualification 与原失败模式回归测试。
- 不创建或执行任何 Q0、attempt、claim、arming 或业务输出。

verify：
- 完整 `exec_argv` 与 `sys.argv` 比较稳定失败。
- 派生 `program_argv` 比较通过。
- 缺少解释器、解释器路径/bytes 漂移、脚本路径/bytes 漂移、argv 漂移、
  cwd 漂移和 shell 模式全部 fail closed。
- qualification 不创建业务输出或 mutable execution identity。
- 适用的 V2.1.1 predecessor baseline tests 保持通过。
- 独立 QA。

done：
- 修复提交和 effect-free 证据完整。
- 业务线程状态进入 `待验收`。
- Independent QA 只有在
  `P0/P1/P2/P3 = 0/0/0/0` 且
  `business_execution = false`、`successor_Q0 = absent` 时才能接受。

禁止事项：
- 不修改 Workflow Kit 或发布 V2.1.2/V2.2。
- 不新增 schema、GovernanceModel、Invocation Plane、fresh adoption 或
  Closure Root WAL。
- 不修改科学/业务逻辑。
- 不访问 historical cache、future outcome、private 或 live 数据。
- 不复用 `0831T001` 的 task、candidate、attempt、claim、arming、
  execution 或 output identity。
- 不注册或执行 successor Q0。
