# 任务派发

执行线程：
- 业务线程-python/live-awsserver1

任务ID：
- 0714T003

标题：
- T011-CONTROLLED-SAME-ENVELOPE-LIVE-EVIDENCE-WITH-V2-RESTING-INTERVAL-CAPTURE

简短描述：
- 在 `0714T002` QA 已通过后，使用修复后的 v2 resting-interval capture contract 跑一次 controlled same-envelope live evidence。
- 本任务目标不是提高成交率，而是采集可解释的 resting interval evidence，验证 v2 artifact contract 在真实 controlled live window 中能记录 lifecycle、coverage、public trades、L2/depth、depletion 和 zero-row interpretation。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- 0714T002 QA 已通过

授权记录：
- 用户在 2026-07-14 指示按顺序执行：
  - `0714T003 controlled live evidence with v2 artifacts`
  - `0714T003 QA`
  - `0714T004 offline quote/fill evidence rerun`
  - `0714T004 QA`
  - route decision
- 总控将该指示解释为仅授权本任务的默认同包络 controlled live evidence：Hyperliquid `BTC`、post-only `Alo`、fast Hyperliquid `l2Book`、max order size `0.005 BTC`、max submissions per window `2`、不改阈值、不改 quote envelope、不扩大 size、不扩大提交次数、不做 fill-seeking placement。

规则更新提醒：
- 请先阅读规则文档后再执行本任务。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `docs/cross_exchange_resting_interval_public_flow_auto_loop_plan.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- task dispatch 先提交。
- live evidence、业务报告和 tracking 更新另行提交。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0714T003.md`
- `.workflow/reports/0714T003-business.md`
- `.workflow/reports/0714T003-qa.md`
- `docs/qa-acceptance-report.md`
- `docs/cross_exchange_resting_interval_public_flow_auto_loop_plan.md`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `local_live_analysis/cross_exchange_resting_interval_v2_live_evidence_0714T003_<UTC>/`

action：
- 在 `awsserver1` 上同步到包含 `0714T002` v2 capture contract repair 的 `cross-exchange` 提交。
- live-related collection 只允许在 `awsserver1` 执行；本地只允许做拉回后的处理、校验和报告，不得本地 live-submit、读 credential、调用 private/account/order/cancel endpoint 或新采集 live market data。
- 使用 `examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py --event-driven-edge-gate-live --hyperliquid-l2book-fast` 顺序执行最多 3 个 controlled live windows，不并行。
- 每个 window 使用 distinct remote artifact dir，且都位于 `/home/admin/hftbacktest-cross-exchange-artifacts/cross_exchange_resting_interval_v2_live_evidence_0714T003_<UTC>/` 下。
- 一旦获得至少 1 个 `submitted_resting*` lifecycle 且 v2 resting-interval public-flow artifacts 完整，即停止后续 live windows，进入 pullback/local validation/report。
- 每个 window 结束后运行独立 read-only final `open_orders()` proof，并写入同一 remote window artifact dir。
- 采集完成后用 `scp` 将完整 remote artifact package 拉回本地 `local_live_analysis/cross_exchange_resting_interval_v2_live_evidence_0714T003_<UTC>/`，本地处理和 QA 只能以拉回后的 local package 为 accepted source。

authorized live envelope：
- Host: `awsserver1`
- Remote repo: `/home/admin/hftbacktest-cross-exchange`
- Remote artifact parent: `/home/admin/hftbacktest-cross-exchange-artifacts/`
- Local artifact parent after pullback: `local_live_analysis/`
- Pullback method: `scp`
- Interpreter: `/home/admin/.venvs/hyperliquid-sdk-0618T002/bin/python`
- Env file: `/home/admin/XEMM_rust_latest/.env`
- Runner: `examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- Required mode: `--event-driven-edge-gate-live`
- Required feed option: `--hyperliquid-l2book-fast`
- Symbol: Hyperliquid `BTC`
- Order type: limit
- TIF: post-only `Alo`
- Side policy: `fresh_touch`
- Quote offset: `0` tick touch-only
- Window count target: up to `3`, stop early after first accepted submitted/resting v2 artifact set
- Max duration per window: `1800s`
- Max submissions per window: `2`
- Max order size: `0.005 BTC`
- Quote hold seconds: `3`
- Wait seconds: `10`

hard boundary：
- No default-on or continuous bot operation.
- No parallel live windows.
- No threshold change.
- No quote-envelope change.
- No order size above `0.005 BTC`.
- No max submissions above `2` per live window.
- No taker, crossing, inside-spread, one-tick-back, or fill-seeking placement.
- Live orders, if any, must be post-only `Alo`.
- Fast Hyperliquid `l2Book` must be enabled.
- Every submitted order must have tracked cancel/shutdown evidence and independent final open-orders proof.
- If any window ends with nonzero open orders, missing cancel proof, malformed lifecycle evidence, malformed v2 resting-interval artifacts, or unexpected endpoint usage, stop the auto-loop and do not run later windows.
- Do not claim fill probability, queue priority, fee/rebate, realized PnL, profitability, stable PnL, maker viability, `T012`, promotion, final MVP pass, or parameter expansion.

required evidence：
- Per-window lifecycle classification.
- v2 `resting_interval_capture_manifest.json`.
- `public_stream_coverage.csv` and zero-row interpretation.
- Exact or explicitly-statused resting timestamp fields.
- Exact or explicitly-statused cancel/shutdown timestamp fields.
- Public trades during the actual/proxy-statused resting interval, keyed by attempt.
- Resting-start L2/depth snapshot or explicit not-available status.
- Depletion/trade-through matrix over the interval.
- Feed health and reconnect/timeout summaries.
- Runner final open-orders `0` proof and independent final open-orders `0` proof.
- Boundary manifest / endpoint boundary evidence.

per-window classification：
- `submitted_resting_no_fill`
- `submitted_filled`
- `submitted_rejected`
- `no_submit_fail_closed`
- `blocked_or_malformed`

verify：
- Local preflight:
  - `python examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py --help`
  - `python -m py_compile examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- Remote preflight:
  - `/home/admin/.venvs/hyperliquid-sdk-0618T002/bin/python examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py --help`
  - `/home/admin/.venvs/hyperliquid-sdk-0618T002/bin/python -m py_compile examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- Remote controlled live windows under the authorized envelope.
- Pullback artifact checks:
  - JSON parse
  - CSV parse/schema spot check
  - v2 resting-interval artifact existence and row-count checks
  - `public_stream_coverage.csv` existence and zero-row interpretation check
  - empty-file scan
  - redaction / true secret-write scan
  - per-window runner final open-orders proof
  - per-window independent final open-orders proof
  - endpoint boundary manifest review
  - sha256 manifest and reconciliation over pulled-back files
- Remote no-watcher-process check after completion.
- `git diff --check`

done：
- Report dispatch commit, business evidence commit, remote commit, local commit, remote source root, local artifact root, pullback method, pulled-back file counts, sha256 reconciliation status, window count attempted/completed, per-window classification, feed health, trigger/submission/fill counts, v2 resting-interval artifact row counts/statuses, `public_stream_coverage.csv` zero-row interpretation, lifecycle/cancel/open-orders outcomes, endpoint boundary status, blockers, and whether QA may route to `0714T004` offline quote/fill evidence rerun or to repair/controller decision.
