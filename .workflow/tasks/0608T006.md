```md
执行线程：
- 业务线程-research

任务ID：
- 0608T006

标题：
- Basis-positive independent robustness diagnosis outside Regime 011

简短描述：
- 基于 `0608T005` 对 `context_basis_mid_ticks > 0` 的 visibility / lineage 验收结论，脱离 `regime_011_1000_spread_10_20_ticks` 壳层，独立只读诊断 basis-positive pattern 是否跨 spread、join-age、volatility、Hyperliquid book state 稳健。本任务只做 read-only robustness diagnosis，不实现策略、不生成 case-library 或 shadow decisions、不启动 live。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0604T003` QA 已通过，canonical event-mode artifacts 是正式证据源。
- `0604T004` QA 已通过，canonical event-mode evidence loader / validator foundation 可复用。
- `0604T005` QA 已通过，canonical source-lock guard 可复用。
- `0608T003` QA 已通过，Regime 011 overall directional momentum 为 `reject_directional_edge_unstable`。
- `0608T004` QA 已通过，Regime 011 feature-conditioned validity 无 supported/watch-valid pattern。
- `0608T005` QA 已通过，`context_basis_mid_ticks > 0` 可作为 read-only decision-time context-only supported 字段，但保留 execution-PnL caveat。

规则更新提醒：
- 请先阅读规则文档后再执行本任务。
- 本任务只做 read-only basis-positive robustness diagnosis；不得实现策略、不得生成 case-library 或 shadow decisions、不得输出交易动作/下单方向/quote/order 行为、不得接 private/order endpoint、不得 live/default-on/tiny-live、不得 promotion。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `docs/thread-playbook.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交任务文件、业务报告、read-only runner/tests、task-scoped artifacts 和必要 workflow tracking 更新。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0608T006.md`
- `.workflow/reports/0608T006-business.md`
- `examples/hyperliquid/canonical_basis_positive_robustness.py`
- `examples/hyperliquid/test_canonical_basis_positive_robustness.py`
- `local_live_analysis/canonical_basis_positive_robustness_0608T006/**`
- 必要 workflow tracking 文件仅限本任务状态/结论更新。

read-only inputs：
- Formal canonical source:
  - `local_live_analysis/event_mode_canonical_pricing_signal_0604T003/**`
  - Row-level canonical sample files must be resolved from `multi_sample_manifest.json` via `samples[].pricing_signal_rows`.
- Basis visibility / lineage prerequisite:
  - `local_live_analysis/canonical_basis_context_visibility_0608T005/**`
- Feature-conditioned validity context:
  - `local_live_analysis/canonical_feature_conditioned_signal_validity_0608T004/**`
- Decision-time input contract:
  - `local_live_analysis/binance_led_hyperliquid_data_contract_0601T004/**`
- Loader / source-lock foundation:
  - `examples/hyperliquid/canonical_event_mode_evidence.py`
  - `local_live_analysis/canonical_event_mode_evidence_0604T004/**`
  - `local_live_analysis/canonical_evidence_source_lock_0604T005/**`

candidate scope：
- Pattern:
  - `context_basis_mid_ticks > 0`
- Scope is explicitly not limited to:
  - `regime_011_1000_spread_10_20_ticks`
- Required base rows:
  - canonical event-mode rows only
  - `joined_row_quality=primary_usable`
  - `context_hyperliquid_context_quality=primary_usable`
  - no future input joins
  - no missing decision-time inputs
- Primary horizon:
  - `1000ms`
- Diagnostic persistence horizons:
  - `5000ms`
  - `10000ms`
- `100/250ms` may be parsed only as watch/alias context and must not be used as independent support.

required stratification：
- Spread:
  - use `context_hyperliquid_spread_ticks` and spread buckets.
- Join age:
  - use `context_hyperliquid_join_age_ms` and existing / deterministic join-age buckets.
- Volatility:
  - use available Binance volatility / movement context where present, or deterministic row-level future-independent volatility buckets only.
- Hyperliquid book state:
  - use `context_hyperliquid_top5_imbalance`.
  - use `context_hyperliquid_microprice_minus_mid_ticks`.
  - include a collinearity/redundancy check against these fields.

required diagnostics：
- Independent robustness:
  - row count
  - sample count
  - sample concentration / max sample row share
  - per-sample effect and hit rate
  - spread-bucket effect
  - join-age-bucket effect
  - volatility-bucket effect
  - Hyperliquid book-state-bucket effect
- Collinearity / proxy check:
  - determine whether `basis > 0` is merely a proxy for Hyperliquid microprice / top5 imbalance.
  - report Pearson/Spearman or deterministic rank-correlation where appropriate.
  - report overlap/conflict with Hyperliquid book-state sign buckets.
- Cost / tail proxy:
  - use fixed conservative public-data proxy assumptions.
  - do not optimize assumptions to pass.
  - report gross edge, cost-adjusted edge, wrong-way rate, p95 wrong-way loss, max wrong-way loss.
- Horizon persistence:
  - primary `1000ms`
  - diagnostic `5000/10000ms`
  - do not count `100/250ms` as independent support.

allowed final recommendations only：
- `keep_for_read_only_context_research`
- `needs_more_samples`
- `reject`

recommendation rules：
- `keep_for_read_only_context_research` requires:
  - multi-sample support, not single-sample dominated
  - effect persists across more than one spread / join-age / venue-state bucket or the report clearly narrows the valid context
  - not explained solely by Hyperliquid microprice/top5 imbalance collinearity
  - conservative cost/tail proxy is not clearly negative/rejected
  - 5000/10000ms persistence does not reverse direction
  - all conclusions remain read-only context, not strategy or case-library
- `needs_more_samples` applies when the pattern is not rejected but sample or bucket coverage is too thin.
- `reject` applies when effect is unstable, sample-concentrated, explained by a redundant venue-state proxy, cost/tail rejected, or horizon persistence reverses.

forbidden：
- New data collection.
- Strategy implementation, directional strategy implementation, order behavior implementation, case-library implementation, shadow decision generation, live/default-on/tiny-live, promotion.
- Hyperliquid or Binance private/account/order endpoints.
- Order lifecycle, submit/cancel/fill logic, production config/default changes, parameter search or optimization sweep.
- Connector/core API changes, standard npz schema changes, canonical Binance maker audit schema changes.
- Treating ordinary synthetic fixed-grid artifacts as formal evidence.
- Treating `100/250ms` evidence as canonical independent support.
- Outputting executable trading instructions, actual order side, quote price, size, leverage, stop rule, take-profit rule, or deployment recommendation.

outputs：
- `basis_positive_robustness_manifest.json`
- `basis_positive_overall_summary.csv`
- `basis_positive_by_sample.csv`
- `basis_positive_by_spread.csv`
- `basis_positive_by_join_age.csv`
- `basis_positive_by_volatility.csv`
- `basis_positive_by_hl_book_state.csv`
- `basis_positive_collinearity.csv`
- `basis_positive_cost_tail.csv`
- `basis_positive_horizon_persistence.csv`
- `basis_positive_recommendation.md`

acceptance criteria：
- Runner refuses non-canonical or diagnostic-only synthetic inputs through the accepted loader/source-lock path.
- Runner validates `0608T005` prerequisite and requires `final_contract_decision=upgrade_to_context_only_supported`.
- Runner resolves row-level inputs from `multi_sample_manifest.json` `samples[].pricing_signal_rows`.
- Assessment is not limited to `regime_011_1000_spread_10_20_ticks`.
- Outputs include all required artifact files and use only the allowed final recommendation taxonomy.
- Report explicitly states conclusions are read-only public-data proxies, not executable strategy PnL or private execution proof.
- Report does not output actual order side, quote price, size, leverage, stop rule, take-profit rule, strategy action, case-library trigger, shadow decision, private/order endpoint, live/default-on/tiny-live, or promotion claim.
- Tests cover canonical input acceptance, diagnostic input refusal, T005 prerequisite validation, not-regime011-only scope, collinearity/proxy classification, cost/tail classification, horizon persistence classification, and final taxonomy generation.

verify：
- `python examples/hyperliquid/canonical_basis_positive_robustness.py --help`
- `python -m py_compile examples/hyperliquid/canonical_basis_positive_robustness.py examples/hyperliquid/test_canonical_basis_positive_robustness.py`
- `python -m pytest examples/hyperliquid/test_canonical_basis_positive_robustness.py`
- `python examples/hyperliquid/canonical_basis_positive_robustness.py --input-dir local_live_analysis/event_mode_canonical_pricing_signal_0604T003 --basis-visibility-dir local_live_analysis/canonical_basis_context_visibility_0608T005 --data-contract-dir local_live_analysis/binance_led_hyperliquid_data_contract_0601T004 --output-dir local_live_analysis/canonical_basis_positive_robustness_0608T006`
- `python -m json.tool local_live_analysis/canonical_basis_positive_robustness_0608T006/basis_positive_robustness_manifest.json`
- Scripted or manual check that the report contains no executable trading instruction, order side, quote price, size, private/order endpoint, case-library trigger, shadow decision, live/default-on/tiny-live, or promotion claim.
- `git diff --check`

done：
- Business report must include method, input source-lock, T005 prerequisite check, non-Regime-011 scope proof, stratified robustness results, HL book-state collinearity/proxy diagnosis, cost/tail proxy, horizon persistence, final recommendation, artifact paths, verification commands, blockers/caveats if any, commit id, and commit message.
- Business report must explicitly state this is only read-only basis-positive robustness diagnosis and does not authorize strategy implementation, private/order endpoints, order lifecycle, live/default-on/tiny-live, parameter search, case-library implementation, shadow decisions, or promotion.
- Business report should end in `待验收` if verification passes.
```
