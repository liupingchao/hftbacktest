```md
执行线程：
- 业务线程-research

任务ID：
- 0618T007

标题：
- M1 Hyperliquid repeated tiny-live canary loop with git-safe refresh

简短描述：
- 在 `0618T006` final gate 已 go 的前提下，执行 M1：用一个 formal task 自动完成 git-safe 远端刷新、final gate 复核和 3 个独立 tiny-live canary window。
- 本任务允许真实 post-only canary 下单，但只允许在固定 caps 内执行最小 order -> tracked cancel -> final open_orders=[] 证据链；不依赖 scheduled-cancel，不跑连续 live 策略，不声明 PnL / maker viability。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- `0618T006` QA 已通过，final gate 为 `allow_create_0617T008=true`。
- `0618T004` QA 已通过，真实 order/cancel/private-read canary interface 已验证。

规则更新提醒：
- 请先阅读 `.workflow/workflow-kit/workflow-manual.md`、`.workflow/workflow-kit/task-dispatch-template.md`、`.workflow/workflow-kit/thread-report-template.md`、`.workflow/workflow-kit/qa-acceptance-template.md`、`task_plan.md`、`progress.md`、`findings.md` 后再执行本任务。
- 本任务是 M1 repeated tiny-live canary windows，不是 M2 PnL accounting，不是连续策略，不是 default-on / promotion / scale-up。

按线程规则执行：
- `AGENTS.md`
- `docs/hyperliquid_tiny_live_real_order_executor.md`
- `docs/hyperliquid_tiny_live_final_go_no_go_gate.md`

official references rechecked：
- Hyperliquid exchange endpoint: `https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/exchange-endpoint`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0618T007.md`
- `.workflow/reports/0618T007-business.md`
- `.workflow/reports/0618T007-qa.md`
- `docs/qa-acceptance-report.md`
- `examples/hyperliquid/hyperliquid_tiny_live_real_order_executor.py`
- `examples/hyperliquid/hyperliquid_tiny_live_m1_canary_loop.py`
- `examples/hyperliquid/test_hyperliquid_tiny_live_real_order_executor.py`
- `examples/hyperliquid/test_hyperliquid_tiny_live_m1_canary_loop.py`
- `local_live_analysis/hyperliquid_tiny_live_m1_canary_loop_0618T007/**`
- `findings.md`
- `progress.md`
- `task_plan.md`

approved live envelope：
- `host_machine=awsserver1`
- `remote_path=/home/admin/hftbacktest-cross-exchange`
- `credential_source=/home/admin/XEMM_rust_latest/.env` preferred, fallback only if this source is unavailable and the fallback path is recorded without secret values.
- `symbol=BTC`
- `windows=3`
- `duration<=10 minutes` per window; actual expected duration is one minimal canary lifecycle per window.
- `max_order_size=0.01 BTC`
- `max_order_notional=700 USDC`
- `max_position=0.04 BTC`
- `max_position_notional=2800 USDC`
- `max_notional=3000 USDC`
- `max_loss=30 USDC`
- `maker_only/post_only=true`, Hyperliquid `Alo` only
- `real_orders_allowed=true` only inside this task and this envelope
- `schedule_cancel_required=false`; do not rely on scheduled-cancel / dead-man switch
- `tracked_cancel_required=true`
- `final_open_orders_empty_required=true`

git-safe loop policy：
- Allowed git operations: local branch/head/status checks, remote branch/head/status checks, `git bundle create`, `scp` bundle upload, remote `git fetch <bundle> cross-exchange`, remote `git merge --ff-only FETCH_HEAD`.
- Forbidden git operations: `git reset --hard`, forced checkout, dirty-worktree overwrite, merge conflict resolution, branch rewrite, force push, modifying `/home/admin/hft_live/hftbacktest`.
- If remote dirty count is nonzero, branch is not `cross-exchange`, or fast-forward cannot complete, stop fail-closed before any order.

action：
- Add a `--disable-schedule-cancel` path to the existing real-order canary executor so M1 can prove tracked cancel + final open-orders without relying on account-ineligible scheduled-cancel.
- Add a M1 loop orchestrator that:
  - verifies local/remote git state,
  - refreshes remote via git-safe bundle + `merge --ff-only` when needed,
  - writes refreshed remote facts,
  - reruns final go/no-go gate,
  - executes exactly 3 independent remote canary windows with `--disable-schedule-cancel`,
  - pulls back each window's artifacts with `scp`,
  - validates each window and writes aggregate M1 manifests/matrices.
- Stop immediately on any failed gate, cap violation, order/cancel/shutdown ambiguity, redaction failure, artifact failure, remote dirty state, or unexpected schedule-cancel use.

acceptance criteria：
- Final gate passes immediately before live canary windows.
- Exactly 3 canary windows complete, unless the loop stops fail-closed and reports blocker.
- Each passing window records `order_submission_attempted=true`, order status includes `resting`, `real_cancel_endpoint_called=true`, `shutdown_proof_status=pass`, and final `open_orders=[]`.
- Each passing window records `schedule_cancel_endpoint_called=false`, `schedule_cancel_required=false`, and `use_schedule_cancel=false`.
- No credential value, private key, raw signature, nonce, oid, cloid, or account address is written unredacted.
- Aggregate manifest recommendation is exactly one of:
  - `hyperliquid_tiny_live_m1_repeated_canary_ready_for_qa`
  - `hyperliquid_tiny_live_m1_repeated_canary_blocked`
- The task does not claim real PnL, stable PnL, maker viability, queue priority, fill probability, deployment readiness, default-on readiness, promotion, or scale-up.

verify：
- `python -m pytest examples/hyperliquid/test_hyperliquid_tiny_live_real_order_executor.py examples/hyperliquid/test_hyperliquid_tiny_live_m1_canary_loop.py examples/hyperliquid/test_hyperliquid_tiny_live_final_go_no_go_gate.py -q`
- `python -m py_compile examples/hyperliquid/hyperliquid_tiny_live_real_order_executor.py examples/hyperliquid/hyperliquid_tiny_live_m1_canary_loop.py`
- `python examples/hyperliquid/hyperliquid_tiny_live_real_order_executor.py --help`
- `python examples/hyperliquid/hyperliquid_tiny_live_m1_canary_loop.py --help`
- `python examples/hyperliquid/hyperliquid_tiny_live_real_order_executor.py --self-test --output-dir local_live_analysis/hyperliquid_tiny_live_m1_canary_loop_0618T007/selftest`
- `python examples/hyperliquid/hyperliquid_tiny_live_m1_canary_loop.py --output-dir local_live_analysis/hyperliquid_tiny_live_m1_canary_loop_0618T007 --windows 3`
- JSON manifest validation with `python -m json.tool` for aggregate and each window manifest.
- Artifact non-empty check.
- Redaction pattern scan.
- `git diff --check`

done：
- 回报 git-safe refresh result、final gate result、3 个 window 的 order/cancel/shutdown/redaction 结果、aggregate artifact path、验证命令、blockers、commit id、提交信息。
- 明确说明本任务实际调用了 private/order/cancel endpoint，但没有调用 schedule-cancel，没有启动 continuous live bot，没有修改 Binance maker route，没有证明 PnL / maker viability。
```
