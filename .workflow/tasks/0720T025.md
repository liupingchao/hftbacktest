# 0720T025 COMPLETE-FOREIGN-IDENTITY-STATUS-CLASSIFICATION

执行线程：
- 总控 auto-loop / 业务执行线程

任务ID：
- 0720T025

标题：
- Require complete disjoint identities and valid status for every foreign historical row

简短描述：
- Close the single T024 QA P1 in manager、producer and independent acceptance。
- A row is foreign only when it supplies every expected identity kind、all identities are valid and all are disjoint；historical status must also be a supported non-empty exchange state。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0720T024`（未通过；strict shared-identity/redaction/OID results accepted，partial foreign/status classification remains open）

规则更新提醒：
- 本任务 offline-only。
- No live/private/account/order/cancel/network/remote/service action is authorized。
- Private historical read、new bounded live、Task 8 and adaptive/multi-level activation remain locked。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `.workflow/reports/0720T024-qa.md`
- `docs/superpowers/plans/2026-07-17-principal-alignment-p0-p3.md`

是否需要提交代码：
- 是

提交要求：
- 实现和测试提交。
- 业务报告单独提交。
- 回报必须带 implementation commit id 和提交信息。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

source：
- Branch: `cross-exchange`
- Dispatch base: `5d27d465a88cc22edfa1b0372781431eb8f0d712`

files：
- `examples/hyperliquid/hyperliquid_maker_order_manager.py`
- `examples/hyperliquid/hyperliquid_tiny_live_m2_fill_window.py`
- `examples/hyperliquid/cross_exchange_t024_same_window_acceptance.py`
- `examples/hyperliquid/test_hyperliquid_maker_order_manager.py`
- `examples/hyperliquid/test_hyperliquid_tiny_live_m2_fill_attribution.py`
- `examples/hyperliquid/test_cross_exchange_t024_same_window_acceptance.py`
- `.workflow/tasks/0720T025.md`
- `.workflow/reports/0720T025-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

action：
- Require every historical row status to be a non-empty supported exchange order state before identity classification。
- Require actual identity kinds to exactly cover the expected identity kinds before a row may be exact or foreign。
- Classify a row missing any expected oid/cloid identity as malformed/indeterminate，even when every supplied identity differs。
- Classify as foreign only when all expected identity kinds are present、valid and each differs from the expected reference。
- Preserve T024 shared-identity conflict、alias/redaction schema and canonical ASCII uint64 behavior。
- Apply the same rule independently in manager、producer and acceptance。
- Add adversarial tests for：
  - foreign oid only；
  - foreign cloid only；
  - empty/whitespace/unknown historical status；
  - complete exact plus complete valid foreign；
  - all T024 conflict and malformed cases。

verify：
- Focused manager/fill/acceptance tests pass。
- Full `python -m pytest -q -p no:cacheprovider examples/hyperliquid` passes。
- Modified modules/tests pass `py_compile`。
- Acceptance CLI `--help` passes。
- Manager、producer and acceptance reject partial foreign and invalid-status rows。
- Complete valid fully-disjoint foreign rows remain accepted beside one exact row。
- T024 accepted redaction/OID and T023 bounded recovery results remain green。
- T016/T022 exact replay remains byte-preserving、offline and blocked with decision `43/43` and lifecycle `49 pass / 12 fail`。
- `git diff --check` and implementation `git show --check` pass。
- No live/private/account/order/cancel/network/remote/service action occurs。

done：
- No historical row can be ignored unless its status and every expected identity kind are well formed and fully disjoint。
- Manager、producer and acceptance independently enforce the same complete-foreign contract。
- Independent QA accepts T025 before any private historical read or new bounded live。

stop conditions：
- A partial foreign row can coexist with an accepted exact terminal row。
- Empty、whitespace or unsupported historical status can be ignored。
- A complete valid fully-disjoint foreign row blocks a valid exact result。
- T024 redaction/OID、T023 bounded recovery、SDK、title、T016/T022 replay or strategy/risk/activation behavior regresses。
- Any verification requires live/private/account/order/cancel/network/remote/service。

blockers：
- Independent QA failure keeps private historical recovery、new bounded live、Task 8 and adaptive/multi-level activation locked。

commit：
- `87c9639ec49717a4dcaaa3545e5165b0803aef5f`

提交信息：
- `Require complete foreign historical identities`
