```md
执行线程：
- 业务线程-live

任务ID：
- 0623T003

标题：
- M2 flow taxonomy / anti-drift fill-support split repair

简短描述：
- 修复 fresh-touch fill-support 与 anti-drift adverse-flow 互相冲突的问题：对 buy maker，稳定 BBO 下的 sell-at-bid touch trade 应被视为 queue depletion / fill support，而不是直接当作 adverse drift。
- 本任务只修 public flow taxonomy 和 anti-drift 判定，不改变 quote distance、不接入 alpha、不扩大风险上限。

状态：
- 已通过

执行顺序：
- 当前唯一任务；必须在 `0623T002` 完成并经总控/QA确认后执行。

前置任务：
- `0623T002` 已通过或总控确认 fresh-touch evidence hardening 可作为输入。
- `0622T006` anti-drift / touch-stability gate 的业务/QA结果已由总控吸收。

规则更新提醒：
- 请先阅读 `.workflow/workflow-kit/workflow-manual.md`、`.workflow/workflow-kit/task-dispatch-template.md`、`.workflow/workflow-kit/thread-report-template.md`、`.workflow/workflow-kit/qa-acceptance-template.md`、`task_plan.md`、`progress.md`、`findings.md` 后再执行本任务。

按线程规则执行：
- `AGENTS.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- `examples/hyperliquid/hyperliquid_tiny_live_m2_public_flow_diagnosis.py`
- `examples/hyperliquid/test_hyperliquid_tiny_live_m2_event_driven_watcher.py`
- 可新增窄范围 taxonomy tests
- `local_live_analysis/hyperliquid_tiny_live_m2_flow_taxonomy_0623T003/**`
- `.workflow/reports/0623T003-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

read-only inputs：
- `.workflow/tasks/0623T002.md`
- `.workflow/reports/0623T002-business.md`
- `local_live_analysis/hyperliquid_tiny_live_m2_fresh_touch_evidence_0623T002/**`
- `.workflow/tasks/0622T006.md`
- `.workflow/tasks/0618T012.md`
- `.workflow/reports/0618T012-qa.md`

hard boundary：
- Keep `Alo`, maker-only, no taker/crossing, no `Ioc`, no one-tick-back workaround, no cap relaxation, no default-on behavior, no stable PnL / M3 claim.
- Do not make touch trades sufficient by themselves; they remain public proxy evidence and must still pass fresh-touch, current BBO, size, queue, and anti-drift gates.
- This task should be local/focused verification first. Do not run live orders unless separately dispatched.

action：
- Split public flow labels into at least:
  - `fill_support_touch`
  - `fill_support_visible_queue_depletion`
  - `adverse_strict_through`
  - `adverse_bbo_move`
  - `neutral_or_opposite_flow`
- For buy candidates:
  - `trade.side == A` at `px == bid` is fill support / touch queue-depletion evidence under stable BBO.
  - `trade.side == A` at `px < bid`, bid down, or ask down to/through limit is adverse.
- For sell candidates, implement the symmetric taxonomy.
- Update anti-drift gate so touch-flow support alone does not block; block only on strict-through, adverse BBO move, or adverse pressure combined with adverse BBO evidence.
- Add artifacts showing support vs adverse classification counts separately.
- Add tests for stable BBO + sell-at-bid not blocked, strict-through + BBO adverse blocked, and mixed-flow cases.

verify：
- `python -m pytest examples/hyperliquid/test_hyperliquid_tiny_live_m2_event_driven_watcher.py -q`
- Run any newly added taxonomy-focused tests.
- `python -m py_compile examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py examples/hyperliquid/hyperliquid_tiny_live_m2_public_flow_diagnosis.py`
- `python examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py --help`
- `git diff --check`

done：
- 回报 flow taxonomy 新字段、buy/sell 对称规则、anti-drift block 条件、关键单测结果、artifact 路径、blockers、commit id、提交信息。
```
