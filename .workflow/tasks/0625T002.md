```md
执行线程：
- 业务线程-research-aws

任务ID：
- 0625T002

标题：
- Synchronized public sample expansion

简短描述：
- 在 `awsserver1` 收集三个新的、相互分离的 Binance `BTCUSDT` top5 + Hyperliquid `BTC` top5/trades public-only 同步窗口，并在本地完成 raw 校验、market-view alignment、as-of join 和 T003 输入覆盖验收。
- 本任务只扩充 decision-time-clean public evidence，不做信号接受裁决、不冻结 side mapping、不修改 watcher、不下单。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0625T001` QA 已通过。
- T001 recommendation 为 `needs_more_public_samples`。

规则更新提醒：
- 请先阅读 `.workflow/workflow-kit/workflow-manual.md`、`.workflow/workflow-kit/task-dispatch-template.md`、`.workflow/workflow-kit/thread-report-template.md`、`.workflow/workflow-kit/qa-acceptance-template.md`、`task_plan.md`、`progress.md`、`findings.md`、`docs/cross_exchange_maker_mvp_plan.md` 后再执行本任务。

按线程规则执行：
- `AGENTS.md`
- `docs/thread-playbook.md`

是否需要提交代码：
- 需要

提交要求：
- 完成采集、本地处理、task-scoped manifests 和业务报告后提交。
- 优先复用现有 accepted collectors/runners；只有现有产物无法表达本任务验收字段时，才允许增加最小 task-scoped validator/index runner 和 focused tests。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0625T002.md`
- `.workflow/reports/0625T002-business.md`
- `local_live_analysis/cross_exchange_public_sample_xemm_0625_t002_*/**`
- `local_live_analysis/cross_exchange_lead_lag_join_xemm_0625_t002_*/**`
- `local_live_analysis/cross_exchange_lead_lag_analysis_xemm_0625_t002_*/**`
- `local_live_analysis/binance_led_hyperliquid_pricing_signal_xemm_0625_t002_*/**`
- `local_live_analysis/cross_exchange_mvp_sample_expansion_0625T002/**`
- Minimal task-scoped validator/index runner and focused test only if required.
- `task_plan.md`
- `progress.md`
- `findings.md`

read-only inputs / reusable code：
- `local_live_analysis/cross_exchange_mvp_alpha_edge_decomposition_0625T001/**`
- Existing accepted reference samples and multi-sample evidence may be used for comparison, but do not count as the three new T002 windows.
- `examples/hyperliquid/synchronized_public_collection.py`
- `examples/hyperliquid/hyperliquid_public_sample.py`
- `examples/hyperliquid/hyperliquid_raw_alignment.py`
- `examples/binance_tick_mm/binance_top5_provenance.py`
- `examples/hyperliquid/cross_exchange_lead_lag_join.py`
- `examples/hyperliquid/cross_exchange_lead_lag_analysis.py`
- `examples/hyperliquid/binance_led_pricing_signal_runner.py`

hard boundary：
- Public-only collection and offline local processing.
- No live orders, no credentials, no private/account/order/cancel endpoints, no user stream, no live client initialization for trading, no remote final gate, no T008 ledger claim.
- No watcher strategy change, signal threshold tuning, side-mapping freeze, edge-buffer reduction, quote-distance change, cap relaxation, taker/crossing, canary, M3, stable-PnL, default-on or promotion.
- Future Hyperliquid values are labels only and must never be decision inputs.
- Remote automatically generated alignment/analysis outputs are diagnostic-only; accepted alignment/join/analysis must be rerun locally from copied raw artifacts.
- Do not count an incomplete or poor-quality window merely to reach three samples.

sample contract：
- Collect exactly three new accepted candidate windows on `awsserver1`.
- Requested duration per window: `1800s`.
- Each accepted window must have at least `1500s` synchronized overlap; target remains full `1800s`.
- Window start times must be separated by at least `30` minutes; prefer different UTC sessions or dates.
- The three accepted windows must cover at least two observed volatility/liquidity regime buckets.
- Regime classification must use decision-time/public fields only and must be declared before inspecting future-label or signal-performance outcomes.
- Naming convention:
  - `xemm_0625_t002_<utc-session-or-regime>_<a|b|c>`
- Existing historical samples may be included as reference rows in the final manifest, but they do not satisfy the three-new-window requirement.

required raw/provenance per window：
- Binance:
  - public `raw.gz`
  - `raw.sha256`
  - `collection_manifest.json`
  - `depth_snapshot.json`
  - session id, stream list, start/end local receive timestamps, exchange timestamps, reconnect count and close reason
- Hyperliquid:
  - public `raw.gz`
  - `raw.sha256`
  - `collection_manifest.json`
  - `recovery_snapshots.jsonl`
  - session id, channel/message counts, start/end local receive timestamps, exchange timestamps, reconnect count and close reason
- Window-level:
  - requested/actual duration
  - synchronized overlap
  - host, interpreter, repo commit and collection command
  - raw checksum verification after local copy

action：
- Use a clean task-specific worktree on `awsserver1`; do not reset or overwrite an unrelated dirty live worktree.
- For each window, run the existing synchronized public collector for Binance `BTCUSDT` and Hyperliquid `BTC`.
- Copy back only public raw/provenance artifacts required by this task.
- Verify copied raw checksums locally.
- From copied raw artifacts, run locally:
  - Binance top5 provenance/alignment
  - Hyperliquid raw alignment/top5 reconstruction
  - local-observation-time as-of join with `binance_local_ts <= hyperliquid_decision_ts`
  - existing lead-lag analysis and pricing-signal runner for field completeness, not signal acceptance
- Produce a task-scoped sample expansion package:
  - `sample_expansion_manifest.json`
  - `sample_quality_matrix.csv`
  - `field_coverage_matrix.csv`
  - `regime_summary.csv`
  - `effective_horizon_coverage.csv`
  - `symmetric_edge_context_coverage.csv`
  - `boundary_manifest.json`
  - `recommendation.md`
- `symmetric_edge_context_coverage.csv` must count rows that preserve both Hyperliquid touch alternatives rather than selecting a strategy side:
  - current HL bid/ask and tick size
  - dual-exchange top5/mid/microprice
  - signal component inputs
  - basis and HL venue-state context
  - local/exchange timestamps and source ages
  - nominal/effective future-label ages
  - future HL mid and microprice labels
- Do not bind buy/sell side or claim executable edge in T002. T003 owns out-of-sample signal/side acceptance.

acceptance criteria：
- Three new windows pass raw/provenance/checksum validation.
- Each accepted window has synchronized overlap `>=1500s`.
- At least two observed public volatility/liquidity regime buckets are represented.
- Every included window has:
  - Binance top5 rows and Hyperliquid top5/trades rows
  - local receive and exchange timestamps
  - reconnect/session provenance
  - no future Binance join
  - explicit missing/stale/source-age counts
  - nominal and effective future-label age coverage
- Aggregate symmetric edge-evaluable contexts:
  - at least `100` complete rows across the three windows
  - at least `20` complete rows per window
- Complete-row coverage must include dual top5, both HL touch alternatives, signal inputs, basis/venue context, source age and future labels.
- `future_join_count=0` for every accepted window.
- Missing/stale rows are reported and excluded, never silently imputed.
- Final recommendation must be exactly one of:
  - `sample_contract_ready_for_signal_acceptance`
  - `needs_more_public_samples`
  - `sample_collection_invalid`
- Only `sample_contract_ready_for_signal_acceptance` may unlock creation of `0625T003`.

verify：
- `python -m pytest examples/hyperliquid/test_synchronized_public_collection.py examples/hyperliquid/test_cross_exchange_lead_lag_join.py examples/hyperliquid/test_cross_exchange_lead_lag_analysis.py examples/hyperliquid/test_binance_led_pricing_signal_runner.py -q`
- `python examples/hyperliquid/synchronized_public_collection.py --help`
- Help/import/focused pytest for any new task-scoped validator.
- Parse every JSON manifest with `python -m json.tool`.
- Verify every copied `raw.gz` against its recorded SHA256.
- Validate required CSV schemas, per-window/aggregate row counts, non-empty files and boundary flags.
- Reproduce the task-scoped sample expansion package into a temporary directory from the accepted local sample paths.
- `git diff --check`

done：
- Report all three sample ids, requested/actual duration, overlap, intended and observed regime, host/interpreter/commit, stream counts, reconnects, checksum results and artifact paths.
- Report per-window Binance/HL alignment rows, join rows, primary/excluded rows, future/missing/stale join counts and source-age distributions.
- Report nominal/effective horizon coverage, field completeness, symmetric edge-context rows per window and aggregate, and regime coverage.
- State the exact final recommendation and whether T003 may be created.
- Explicitly state that T002 did not freeze a signal/side contract, change strategy/live behavior, use private/order endpoints, place orders or authorize canary/promotion.
- End in `待验收` if all execution and local verification complete; use `阻塞` if three valid windows/two regimes cannot be obtained; use `未通过` for invalid evidence or boundary violation.
- Include commit id and commit message.
```
