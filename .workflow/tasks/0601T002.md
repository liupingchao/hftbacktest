```md
执行线程：
- 业务线程-python

任务ID：
- 0601T002

标题：
- Binance lead to Hyperliquid lag as-of join implementation

简短描述：
- 构建 Binance-lead / Hyperliquid-lag 的只读 cross-exchange as-of join，将 `0602T001` 已验收同步样本中的 Binance public lead features 映射到 Hyperliquid lag-venue synthetic decision timestamps。本任务只建立跨所数据输入链路，不做 lead-lag 稳定性统计结论、不做策略、不下单、不做参数搜索、不 live。

状态：
- 待验收

执行顺序：
- 当前唯一任务；位于 `0602T001` QA 已通过之后、`0601T003` lead-lag stability analyzer 之前。

前置任务：
- `0531T001` QA 已通过，证明 Hyperliquid public market-data research consumer 的 read-only/local/public-only 边界可用。
- `0601T001` QA 已通过，证明 Hyperliquid lag-side public sample/alignment 链路可用。
- `0602T001` QA 已通过，提供同时间窗口的 Binance lead public data 和 Hyperliquid lag public data。
- 本任务固定使用 Binance USD-M Futures `BTCUSDT` 作为 lead venue/source，Hyperliquid `BTC` 作为 lag venue/source。
- 本任务固定使用 `local_live_analysis/cross_exchange_public_sample_0602T001/**` 作为唯一实证输入样本；其他历史样本只能作为代码/schema/质量口径参考，不得混入本轮输出。

规则更新提醒：
- 请先阅读规则文档后再执行本任务。
- 本任务的核心不是 Hyperliquid 单所 pricing，也不是 lead-lag 统计裁决，而是把 accepted synchronized public sample 转换成后续 `0601T003` 可消费的 no-future-leakage joined-feature 输入。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `docs/thread-playbook.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交任务文件、业务报告和本任务代码/测试/产物变更。
- 回报必须带 `commit id` 和 `提交信息`；如本轮未提交，报告必须明确 `commit：无` 并说明原因。
- 不得触碰 private/order/strategy/live/parameter/default-on/tiny-live/promotion 边界。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0601T002.md`
- `.workflow/reports/0601T002-business.md`
- `examples/hyperliquid/cross_exchange_lead_lag_join.py`
- `examples/hyperliquid/test_cross_exchange_lead_lag_join.py`
- `local_live_analysis/cross_exchange_lead_lag_join_0601T002/**`
- 必要 workflow tracking 文件仅限本任务状态/结论更新。

read-only inputs：
- `.workflow/tasks/0531T001.md`
- `.workflow/reports/0531T001-qa.md`
- `.workflow/tasks/0601T001.md`
- `.workflow/reports/0601T001-qa.md`
- `.workflow/tasks/0602T001.md`
- `.workflow/reports/0602T001-qa.md`
- Accepted synchronized cross-exchange public sample, as the only empirical sample input:
  - `local_live_analysis/cross_exchange_public_sample_0602T001/sample_manifest.json`
  - `local_live_analysis/cross_exchange_public_sample_0602T001/run_manifest.json`
  - `local_live_analysis/cross_exchange_public_sample_0602T001/synchronization_quality_summary.json`
  - `local_live_analysis/cross_exchange_public_sample_0602T001/binance_public_raw/**`
  - `local_live_analysis/cross_exchange_public_sample_0602T001/binance_alignment/**`
  - `local_live_analysis/cross_exchange_public_sample_0602T001/hyperliquid_public_sample/**`
- Existing Binance and Hyperliquid public-data converter/alignment code as implementation reference only:
  - `examples/binance_tick_mm/pipeline_live_raw.py`
  - `examples/binance_tick_mm/binance_top5_provenance.py`
  - `examples/hyperliquid/hyperliquid_raw_alignment.py`
  - `examples/hyperliquid/hyperliquid_market_data_research.py`

forbidden files / areas：
- Fresh sample collection.
- Hyperliquid public/private network connection.
- Binance public/private network connection.
- Hyperliquid private connector, account/order endpoints, order lifecycle, submit/cancel/fill logic.
- Binance private/account/order endpoints, connector account state, user streams, or live trading bot requirements.
- Binance or Hyperliquid strategy behavior changes.
- Strategy live, parameter search, default-on, tiny-live, promotion.
- Lead-lag statistical conclusion, pricing-model decision, strategy signal decision, or candidate promotion.
- Standard npz schema, canonical audit schema, connector/core API changes.

timestamp / join policy：
- Cross-venue join must use local controller capture timestamps in nanoseconds as the comparable time domain.
- Hyperliquid lag decision grid is `hyperliquid_public_sample/alignment/synthetic_joined_views.csv` using `decision_ts`.
- Binance lead features must be joined as-of each Hyperliquid `decision_ts` using only Binance source rows with `local_ts <= decision_ts`.
- Binance and Hyperliquid exchange/event timestamps may be preserved as diagnostics, but must not be used as the primary cross-venue join clock.
- Future leakage is a hard failure: any joined Binance source timestamp greater than the Hyperliquid decision timestamp must be counted and rejected.

feature policy：
- Binance lead features allowed in this task:
  - BBO / mid / spread from accepted Binance public bookTicker/depth sidecar evidence.
  - top5 imbalance and top5 microprice proxy from `binance_alignment/top5_sidecar.csv`.
  - past-only rolling realized volatility / mid-move features computed without future rows.
  - source-age / bookTicker-depth-age / sidecar sync diagnostics.
- Binance public trade pressure is disabled / diagnostic-only in this task unless an already accepted local evidence document explicitly proves side semantics. This task must not treat Binance trade pressure as a candidate-ready lead feature.
- Hyperliquid lag context allowed in this task:
  - Hyperliquid mid / spread from `synthetic_joined_views.csv`.
  - top-N depth / book pressure from `topn_sidecar.csv`.
  - join-age / cadence / recovery / missing/future diagnostics.
- Hyperliquid trade pressure remains disabled because public trade side semantics remain unverified in accepted evidence.
- Basis / dislocation fields such as Binance mid or microprice proxy minus Hyperliquid mid are diagnostic only and must include Binance USD-M futures `BTCUSDT` versus Hyperliquid `BTC` contract-basis caveats.

quality policy：
- The output must report overlap, source row counts, feature row counts, joined row counts, missing joins, future joins, stale/source-age buckets, and disabled feature counts.
- `future_join_count` must be `0` for accepted joined outputs.
- Missing joins and stale/source-age rows must be reported explicitly. They may be kept in the CSV with quality flags, but the report must separate primary usable rows from watch-only / stale rows.
- This task must not claim lead-lag effect stability, predictive edge, strategy readiness, tiny-live readiness, or promotion.

action：
- Implement `examples/hyperliquid/cross_exchange_lead_lag_join.py` as a local read-only join runner over the accepted `0602T001` synchronized sample.
- Add focused tests in `examples/hyperliquid/test_cross_exchange_lead_lag_join.py` for:
  - as-of join no-future-leakage behavior
  - local timestamp clock policy
  - Binance lead feature / Hyperliquid lag context schema separation
  - disabled trade-pressure handling
  - manifest and quality-summary generation
- Define a task-scoped cross-exchange sample manifest mapping:
  - Binance lead sample/artifacts from accepted `0602T001`
  - Hyperliquid lag sample/artifacts from accepted `0602T001`
  - symbol / contract / venue mapping
  - timestamp domains and local/event timestamp fields
  - no-private/no-order/no-live/no-parameter/no-promotion flags
- Build Binance lead pricing / volatility feature rows using only decision-time-visible Binance public data.
- Build Hyperliquid lag venue context rows using only accepted Hyperliquid public artifacts.
- As-of join Binance lead features onto Hyperliquid synthetic decision timestamps without future leakage.
- Emit join-age / missing / future-join / stale-source diagnostics for both venues.
- Produce basis / dislocation fields with explicit contract-basis caveats.
- Write a short report explaining exactly what the joined data can and cannot support.

required outputs：
- `local_live_analysis/cross_exchange_lead_lag_join_0601T002/sample_manifest.json`
- `local_live_analysis/cross_exchange_lead_lag_join_0601T002/run_manifest.json`
- `local_live_analysis/cross_exchange_lead_lag_join_0601T002/binance_lead_features.csv`
- `local_live_analysis/cross_exchange_lead_lag_join_0601T002/hyperliquid_lag_context.csv`
- `local_live_analysis/cross_exchange_lead_lag_join_0601T002/cross_exchange_joined_features.csv`
- `local_live_analysis/cross_exchange_lead_lag_join_0601T002/join_quality_summary.json`
- `local_live_analysis/cross_exchange_lead_lag_join_0601T002/basis_dislocation_summary.csv`
- `local_live_analysis/cross_exchange_lead_lag_join_0601T002/cross_exchange_join_report.md`
- `.workflow/reports/0601T002-business.md`

acceptance criteria：
- `examples/hyperliquid/cross_exchange_lead_lag_join.py` exists, is importable, and exposes a useful CLI.
- Focused tests cover no-future-leakage, timestamp policy, schema separation, disabled trade pressure, and manifest/quality output behavior.
- The runner reads only accepted local `0602T001` synchronized sample artifacts as empirical input.
- Binance lead source and Hyperliquid lag source are explicitly listed and read-only.
- Cross-exchange as-of join uses local timestamps and has no future leakage by construction and by reported metrics.
- Binance lead features and Hyperliquid lag venue-state/context fields are separated in the schema.
- Binance and Hyperliquid trade pressure are disabled / diagnostic-only unless separately proven by accepted evidence.
- Basis / dislocation fields include symbol / contract caveats.
- Aggregate manifest records all no-private/no-order/no-live/no-parameter/no-default-on/no-tiny-live/no-promotion flags.
- Result remains a data-join input task for `0601T003`, not a lead-lag statistical conclusion, strategy, or pricing-signal decision task.

verify：
- `python examples/hyperliquid/cross_exchange_lead_lag_join.py --help`
- `python -m pytest examples/hyperliquid/test_cross_exchange_lead_lag_join.py -q`
- `python examples/hyperliquid/cross_exchange_lead_lag_join.py --sample-dir local_live_analysis/cross_exchange_public_sample_0602T001 --output-dir local_live_analysis/cross_exchange_lead_lag_join_0601T002`
- `python -m json.tool local_live_analysis/cross_exchange_lead_lag_join_0601T002/sample_manifest.json`
- `python -m json.tool local_live_analysis/cross_exchange_lead_lag_join_0601T002/run_manifest.json`
- `python -m json.tool local_live_analysis/cross_exchange_lead_lag_join_0601T002/join_quality_summary.json`
- `python -m py_compile examples/hyperliquid/cross_exchange_lead_lag_join.py examples/hyperliquid/test_cross_exchange_lead_lag_join.py`
- `git diff --check`

done：
- Business report must include implementation summary, exact input sample path, timestamp/as-of policy, generated artifacts, row counts, future/missing/stale-source metrics, disabled feature list, boundary flags, verification commands, blockers if any, commit id, and commit message.
- Business report must explicitly state that the output is synchronized public-data joined-feature input for `0601T003` only.
- Business report must end in `待验收` if implementation and local run complete, or `阻塞` / `未通过` if criteria above cannot be met.
```
