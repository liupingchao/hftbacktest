# 任务派发

执行线程：
- 业务线程-python

任务ID：
- 0712T001

标题：
- T011-PUBLIC-FLOW-INTERVAL-ARTIFACT-REPAIR-DESIGN

简短描述：
- 在 `0710T001` 已接受的 `route_to_public_flow_artifact_repair` 之后，设计/修复 resting interval public-flow artifact，让后续 quote/fill probability evidence 能看清订单从 resting 到 cancel/shutdown 期间到底发生了什么。
- 本任务目标不是提高成交率，不改阈值、不改 quote envelope、不做 live retry；只把 resting 期间的 trade-through/depletion 证据记录清楚。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- 0710T001 QA 已通过

规则更新提醒：
- 请先阅读规则文档后再执行本任务。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `docs/cross_exchange_quote_fill_probability_evidence_plan.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `examples/hyperliquid/`
- `.workflow/tasks/0712T001.md`
- `.workflow/reports/0712T001-business.md`
- `.workflow/reports/0712T001-qa.md`
- `docs/qa-acceptance-report.md`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `local_live_analysis/cross_exchange_public_flow_interval_artifact_repair_0712T001/`

action：
- Define and, where feasible from existing artifacts, implement an offline resting-interval public-flow artifact contract.
- For each accepted resting/no-fill attempt, bind:
  - `resting_start_ts`
  - `cancel_or_shutdown_ts`
  - `side`
  - `quote_px`
  - `size_btc`
  - public trades during the actual resting interval
  - same-side visible depth at resting start
  - depletion/trade-through estimate during the actual resting interval
  - censoring / not-reconstructable reason
- If existing T011 artifacts cannot reconstruct a field, mark it explicitly as `not_reconstructable_from_current_artifact` rather than inventing fill probability.
- Produce a route recommendation for whether offline repair is sufficient or a later separately authorized controlled evidence collection task is needed.

hard boundary：
- No live-submit.
- No live retry.
- No remote/AWS execution.
- No credential read.
- No private/account/order/cancel endpoint.
- No new market-data collection.
- No threshold change.
- No quote-envelope change.
- No order size or max-submission change.
- No strategy behavior change.
- No fill-probability claim without actual resting-interval evidence.
- No synthetic fill, queue-priority claim, fee/rebate, realized PnL, maker viability, T012, promotion, or final MVP claim.

verify：
- Focused pytest for any new/changed runner.
- `python -m py_compile` for any new/changed runner.
- Runner `--help` if a runner is added.
- Run against accepted local `0710T001` / T011 artifacts.
- JSON/CSV parse and row-count checks for generated artifacts.
- Deterministic rerun check if generated artifacts are written.
- `git diff --check`.

done：
- Report whether resting-interval trade-through/depletion can be reconstructed from current artifacts.
- Report generated artifact paths and schema.
- Report per-attempt reconstruction status.
- Report final route enum.
- Confirm no threshold/quote-envelope/live retry behavior occurred.
