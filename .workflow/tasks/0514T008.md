```md
执行线程：
- 业务线程-python

任务ID：
- 0514T008

标题：
- Replay fill/cancel lifecycle mismatch diagnosis / repair plan

简短描述：
- 基于 `0514T007` 已通过但结论为 `diagnostic_only_gap_too_large` 的结果，制定一个 planning-only 诊断合同，查清为什么 replay 在 matched submit coverage 完全对齐的情况下，仍然系统性高估 fills、fill-after-cancel-request 和 cancel-to-fill delay。本任务只做诊断/修复计划，不实现 runner、不修改 replay、不补采样本、不改策略。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0514T007` QA 已通过

规则更新提醒：
- 本任务是 planning-only 诊断合同。
- 不允许实现 replay repair、不允许新跑 replay sweep、不允许 live 采集、不允许策略变更。
- 目标是先定位 replay fill/cancel lifecycle mismatch 的来源，再决定是否需要新的实现任务或补样本任务。

按线程规则执行：
- .workflow/workflow-kit/thread-report-template.md
- docs/thread-playbook.md
- task_plan.md
- progress.md
- findings.md

是否需要提交代码：
- 需要

提交要求：
- 自测/检查通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

Allowed files：
- `.workflow/tasks/0514T008.md`
- `.workflow/reports/0514T008-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `.workflow/dashboard.html`
- `.workflow/dispatch_suggestions.md`
- `docs/qa-acceptance-report.md`

Allowed read-only inputs：
- `.workflow/reports/0514T007-business.md`
- `.workflow/reports/0514T007-qa.md`
- `.workflow/reports/0514T006-business.md`
- `.workflow/reports/0514T006-qa.md`
- `.workflow/reports/0514T005-business.md`
- `.workflow/reports/0514T005-qa.md`
- `local_live_analysis/5-13-day-control-30min/stage6_execution_calibration_0514T007/**`
- `local_live_analysis/5-13-day-control-30min/stage5_execution_outcome_labels_0514T005/**`
- `local_live_analysis/5-13-day-control-30min/audit_live_5-13-day-control-30min.csv`
- `local_live_analysis/5-13-day-control-30min/out/backtest_audit_replay/audit_bt_audit_replay.csv`
- `local_live_analysis/5-13-day-control-30min/alignment_report_audit_replay.json`
- `local_live_analysis/5-13-day-control-30min/maker_acceptance_stage3.json`
- `local_live_analysis/5-13-day-control-30min/t009_fixed_sidecar/**`

Forbidden files / areas：
- `examples/binance_tick_mm/*.py`
- `local_live_analysis/**` generated outputs beyond reading existing artifacts
- strategy behavior, fair/target formulas, configs, risk guards, quote placement behavior
- live scripts, AWS/remote state, live collection
- replay engine behavior / backtest fill model implementation
- core Rust, connector, py `event_dtype`, standard npz schema

Requires separate task：
- 实现 replay fill/cancel lifecycle diagnostics runner。
- 修改 replay fill model / lifecycle state machine / cancel-ack handling。
- 新跑 replay sweep、Stage 6J matrix 或更多 replay experiments。
- 新采集 current-format 样本或 live micro test。
- 策略实现、quote-adjustment promotion、inventory redesign。

Fatal Failures：
- 在 planning-only 任务里直接修改 replay / fill model / strategy。
- 把 `0514T007` 的差异解释成“只要补样本就能解决”的纯样本量问题。
- 忽略 `0514T007` 已经证明的 matched submit coverage 完整性。
- 把本任务扩展为 quote-adjustment 设计或 live promotion 判断。

action：
- 明确问题定义：
  - 在 matched submit coverage `2516/2516`、price tick/qty equality `2516/2516` 的情况下，replay 仍出现：
    - fills `172` vs live `53`
    - fill-after-cancel-request `133` vs live `16`
    - canceled / filled / open_or_missing state gaps
    - large cancel-to-fill delay gap
  - 这说明主要问题是 replay fill/cancel lifecycle mismatch，而不是 comparison unit 缺失或样本覆盖不足。
- 明确为什么下一步应先诊断 replay mismatch，而不是先补 current-format samples。
- 制定 diagnosis-only plan，至少覆盖这些疑点：
  1. submit matched but fill state diverged：
     - 哪些 matched submit keys 在 live no-fill / replay fill
     - 哪些 matched submit keys 在 live cancel / replay non-terminal-or-fill
  2. cancel-request timeline mismatch：
     - cancel request、cancel ack、fill 的时序在 replay 中如何被重建
     - replay 是否系统性把 cancel-requested orders 保持得更久
  3. lifecycle terminal-state mismatch：
     - replay `filled` / `canceled` / `open_or_missing` 如何偏移
     - 是否存在 terminal condition / remaining qty / partial state 解释问题
  4. markout observability mismatch：
     - 为什么 matched submit 覆盖完整，但 fill markout observable rows 差异很大
  5. strata concentration：
     - 哪些 placement / inventory / latency / join-age bucket 是 mismatch hot spots
- 定义后续实现任务需要的输入、输出、验证口径和 non-goals。

必须回答的问题：
- 为什么 matched submit coverage 完整，却仍然有大量 replay-only fills？
- 为什么 replay fill-after-cancel-request 放大到 live 的多个数量级？
- 为什么 cancel-to-fill delay 的 aggregate distribution 差异如此大？
- 问题更像来自：
  - replay fill generation
  - cancel request / cancel ack timing
  - terminal state transition
  - remaining qty / partial lifecycle semantics
  - long-horizon order persistence
  - 还是某些 placement / inventory / latency buckets 的系统性偏差？
- 在这些问题没搞清之前，为什么补样本不能有效推进 quote-adjustment promotion？

输出要求：
- 生成 planning-only 业务回报，至少包含：
  - root-cause hypothesis 列表
  - priority-ranked diagnosis questions
  - required future artifacts
  - recommended implementation task boundary
  - why not sample-first 的判断
- 推荐后续实现任务应是：
  - read-only mismatch diagnosis runner / tables first
  - replay repair task second
  - sample expansion task later if needed

verify：
- 人工检查 `.workflow/tasks/0514T008.md` 和 `.workflow/reports/0514T008-business.md` 是否完整。
- `python3 .workflow/build_dashboard.py`
- `git diff --check`

done：
- 回报必须明确：下一步为什么应先做 replay fill/cancel lifecycle mismatch diagnosis，而不是先补样本。
- 回报必须明确 diagnosis scope、疑点分类、优先级、required artifacts、后续实现任务边界。
- 回报必须明确本任务未实现代码、未运行新 replay、未采样、未改策略、未启动 live。
- 回报必须写 `commit id` 和提交信息。
```
