# 任务派发

执行线程：
- 业务线程-python/public-collector

任务ID：
- 0729T008

标题：
- HYPERLIQUID-RESEARCH-MAX-MULTI-TRACK-COLLECTION

简短描述：
- 将 Hyperliquid public collector 从单一 `fast l2Book + trades` 升级为
  可区分的多轨研究采集。
- 信息量为强约束，兼容性不是强约束；同时保留 fast L2 的时间分辨率、
  standard L2 的空间深度和公开市场上下文，以最大化 lead-lag maker
  策略研究所需的原始信息。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- `0729T007` 已通过。

规则更新提醒：
- 请先阅读规则文档后再执行本任务。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 不需要

提交要求：
- 当前工作区已有未提交 workflow、collector 和 hunting 工作。
- 本任务不创建混合提交，回报使用 `commit：无`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0729T008.md`
- `.workflow/reports/0729T008-business.md`
- `.workflow/reports/0729T008-qa.md`
- `examples/hyperliquid/hyperliquid_public_sample.py`
- `examples/hyperliquid/test_hyperliquid_public_sample.py`
- `examples/hyperliquid/synchronized_public_collection.py`
- `examples/hyperliquid/test_synchronized_public_collection.py`
- `docs/qa-acceptance-report.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

action：
- 高时间分辨率浅档轨道：
  - `l2Book fast=true`
  - `trades`
  - `bbo`
  - 当前实测 fast L2 为 5x5，约 0.5s cadence；原始消息独立落盘。
- 高空间分辨率深档轨道：
  - standard `l2Book`，不设置 `fast`。
  - 当前实测 standard L2 为 20x20，约 5.4s cadence；原始消息独立落盘。
- 公开上下文轨道：
  - `activeAssetCtx`
  - target dex `allMids`
  - main-dex `allMids`
  - `candle 1m`
- 所有轨道并发运行，分别保留 raw、manifest、recovery snapshot 和 SHA。
- fast 与 standard L2 不得混写到同一 raw；接收 payload 不携带订阅 variant，
  混写会破坏来源归因。
- 每个 manifest 保存精确 subscription payload、ACK payload 和按订阅身份
  统计的 ACK。
- 新增 aggregate research bundle manifest，并在同步采集总 manifest 中
  暴露两个轨道、各自起止时间、计数、哈希和 overlap。
- synchronized public collection 默认启用 research-max；提供显式关闭参数
  以保留兼容回退。
- 保持 Binance、alignment、策略、私有接口和交易行为不变。

hard boundary：
- Public market data and public Info endpoint only。
- No private keys、user address、account、order、cancel or strategy process。
- No order-book-server/node deployment in this task。
- No claim of full L3/L4 queue reconstruction or fill simulation。
- No storage-size optimization；保留全部原始响应。

verify：
- 精确 subscription payload unit tests。
- fast 与 standard L2 独立输出和归因测试。
- duplicate subscription type ACK identity tests。
- aggregate manifest、hash、duration 和 failure propagation tests。
- synchronized collector command/manifest compatibility tests。
- focused and environment-compatible Hyperliquid regression。
- 独立 60 秒 public live canary，验证：
  - fast L2 为高频浅档
  - standard L2 提供更多档位
  - trades、bbo、activeAssetCtx、allMids、candle 均收到 ACK 和数据
  - 两轨 zero reconnect、raw SHA/gzip/JSONL/count reconciliation 通过
- `git diff --check`。

done：
- Report exact subscriptions、track layout、实际档位、频率、字段、测试计数、
  live canary、兼容边界、blockers and QA readiness。
