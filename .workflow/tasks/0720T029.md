# 0720T029 MANAGER-HOLD-DEADLINE-REPLAY-FAIL-CLOSED-REPAIR

执行线程：
- 总控 auto-loop / 业务执行线程

任务ID：
- 0720T029

标题：
- Bound manager hold reads by cancel deadline and make estimator replay fail closed

简短描述：
- Repair the two T028 QA findings without changing quote calculation, order envelope, risk logic or activation state。
- Ensure public observation cannot delay manager cancel beyond the hold deadline plus the 0.25s cancel-check bound, and ensure replay distinguishes contract absence from present-but-empty while rejecting quarantine。

状态：
- 未通过

执行顺序：
- 当前唯一任务

前置任务：
- `0720T028`（未通过；QA findings only）

规则更新提醒：
- 本任务 offline-only。
- Observe-only tiny-live、dynamic spread、fill feedback、inventory skew 和 multi-level 继续锁定。
- 不得弱化 T028 已接受的 interval、exposure、acceptance 或 historical replay contracts。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `.workflow/reports/0720T028-qa.md`
- `docs/superpowers/plans/2026-07-17-principal-alignment-p0-p3.md`

是否需要提交代码：
- 需要

提交要求：
- 先提交 implementation。
- 再提交 task/business/tracker report。
- 独立 QA 单独验收。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

source：
- Branch: `cross-exchange`
- Dispatch base: `edefc09`

files：
- Modify: `examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- Modify: `examples/hyperliquid/cross_exchange_online_estimators.py`
- Test: `examples/hyperliquid/test_hyperliquid_tiny_live_m2_event_driven_watcher.py`
- Test: `examples/hyperliquid/test_cross_exchange_online_estimators.py`
- Workflow: this task, business report, QA report, fixed QA document, `task_plan.md`, `progress.md`, `findings.md`

action：
- Replace synchronous manager-hold `next(source_iter)` waits with a single deadline-aware public-event pump。
- The trading thread must check the hold deadline at least every `INLINE_REPRICE_CANCEL_CHECK_SECONDS` and begin cancel no later than deadline plus that bound, even if the underlying iterator blocks。
- Keep public state mutation on the trading thread；the pump may only fetch the next source item and communicate result/exception/exhaustion。
- For the built-in live websocket source under manager mode, cap per-read timeout at the cancel-check bound so any background read exits promptly。
- Preserve immediate cancel on observer exception、disconnect、source exhaustion and all existing fail-closed evidence reasons。
- In estimator replay, determine manager contract presence from file existence/schema contract, not row-list truthiness。
- If the contract is present, exclude every persisted confirmed exposure from replay input and rebuild only from event rows plus the contract, including when the contract has zero rows。
- Require zero rebuilt confirmed-resting quarantine rows for `snapshot_match=true` and CLI success。
- Persist explicit replay fields for contract presence and quarantine-empty status。
- Preserve v1 behavior only when the contract file is actually absent。

verify：
- A 3s hold with a source blocking longer than 3s starts first cancel no later than deadline plus 0.25s。
- No-event blocking source records fail-closed exposure evidence while cancel still executes twice and final owned orders are empty。
- Built-in manager live source receives websocket timeout no greater than 0.25s；non-manager modes preserve configured timeout。
- Invalid interval/quarantine makes replay `snapshot_match=false` and CLI nonzero。
- Present-but-empty contract plus forged persisted confirmed exposure makes replay `snapshot_match=false` and excludes the forged row。
- Absent contract preserves legacy v1 replay behavior。
- Existing exposure reconstruction, Task 12 acceptance, T026 zero exposure and T016/T022 blocked replay remain unchanged。
- Focused and full Hyperliquid tests pass。
- No live/private/network/remote/service action occurs。

stop conditions：
- Public pump mutates trading state outside the trading thread。
- Cancel can still wait on a blocking public iterator beyond deadline plus 0.25s。
- A background public read can place/cancel/read private state or continue consuming after the manager cycle without a bounded stop path。
- Present-but-empty contract is treated as absence。
- Quarantine non-empty replay can return success。
- Any strategy/risk/activation behavior changes。
- Any live/private/network/remote/service action occurs。

done：
- T028 P1/P2 findings are repaired with focused hostile proof。
- Manager cancel timing is independent of public-source blocking。
- Estimator replay is fail closed for contract/quarantine evidence。
- A later formal Task 8 observe-only tiny-live may be dispatched only after independent QA accepts this task。

blockers：
- Observe-only live remains locked until T029 independent QA passes。
