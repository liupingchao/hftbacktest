# 任务派发

执行线程：
- SKHYNIX Precision-First Flow Coherence V2 A-1 业务线程

任务ID：
- 0829T001

标题：
- 执行 PRECISION_FIRST_FLOW_COHERENCE_V2 A-1 structural false-fire control audit

简短描述：
- 将 entry detector 的损失函数冻结为 `cost(FP) >> cost(FN)`，允许大量
  `ABSTAIN`，不再以覆盖率或最低触发频率否决 detector。
- 使用 outcome-blind structural null、三状态输出、null-only threshold
  selection 和 leave-one-date-out cross-fitting，检验 structural-null
  false-fire rate 与 historical count separation。

状态：
- 待验收

执行顺序：
- 当前唯一 formal task

前置任务：
- 0828T014（执行与 remediation QA 已通过；科学分类为
  `Aminus1_feature_support_failed`）

规则更新提醒：
- 已阅读 AGENTS.md、workflow-kit、task_plan.md、progress.md、
  findings.md、conditional-risk methodology kernel 和 0828T014 全部
  plan/execution/QA。

按线程规则执行：
- AGENTS.md
- .workflow/workflow-kit/workflow-manual.md

是否需要提交代码：
- 需要

提交要求：
- 新版本合同必须先经过独立只读 review。
- Review 通过前禁止运行新的 29-cache detector/null audit。
- 执行、自测、deterministic rebuild 和独立 QA 通过后提交。

合同 review 历史：
- Round 1：未通过，`P0/P1/P2/P3=0/5/2/0`。
- Revision 2 移除 FDP/precision 置信界声明，拆分 selection/evaluation
  null banks，将 comparison mask 降为外部对称 audit censor，要求完整
  trade/depletion/OFI support，并冻结 common cluster IDs 与同一
  30s-selected filter 的三 duration evaluation。
- Round 2：未通过，`P0/P1/P2/P3=0/1/3/1`。
- Revision 3 冻结 filter-duration 对称 exposure、duration-indexed
  estimator、segment-safe fixed clusters 和 inherited callable AST binding。
- Round 3：未通过，`P0/P1/P2/P3=0/1/1/1`。
- Revision 4 将 novelty checkpoint 的 2s state influence 以加法扩展进
  censor/exposure，并将 sparse guard 固定为 censor 前 raw detector 行为。
- Round 4：未通过，`P0/P1/P2/P3=0/0/1/0`。
- Revision 5 将所有 exposure 明确冻结为 checkpoint、seconds 和 hours
  三种单位，所有 `per hour` rate 只能使用 hours denominator。
- Round 5：未通过，`P0/P1/P2/P3=0/0/1/0`。
- Revision 6 冻结 zero/non-finite exposure 的 fail-closed 语义：
  selection 拒绝 filter，evaluation 为 not estimable，raw sparse gate
  不得通过。
- Round 6：未通过，`P0/P1/P2/P3=0/0/3/0`。
- Revision 7 区分 30s primary 与 10s/60s sensitivity 的 zero-support
  classification，冻结 `f_j=NONE/META_ABSTAIN` fold sentinel，并将负值、
  non-finite 或单位转换不一致统一归入 selection integrity failure。
- Round 7：未通过，`P0/P1/P2/P3=0/0/1/0`。
- Revision 8 将 raw exposure 恰好为零与数值损坏分离：前者使 A-1-7
  不可通过，后者提前归入 A-1-4 selection integrity failure。
- Round 8：通过，`P0/P1/P2/P3=0/0/0/0`。
- outcome-blind 29-cache A-1 execution lock 已解除；future outcome、A0
  execution 和 live trading 仍禁止。
- Frozen plan SHA256：
  `6e6e1af47dbf0c982ee83654c60f054aac6d81452b1246d65a39123a7d384593`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- docs/skhynix_binance_precision_first_flow_coherence_v2_a_minus1_false_positive_control_audit_plan_20260829.md
- examples/hyperliquid/skhynix_precision_first_flow_coherence_a_minus1.py
- examples/hyperliquid/test_skhynix_precision_first_flow_coherence_a_minus1.py
- local_live_analysis/skhynix_precision_first_flow_coherence_a_minus1_0829T001/
- .workflow/tasks/0829T001.md
- .workflow/reports/0829T001-business.md

action：
- 冻结 `SIGNAL/BACKGROUND/ABSTAIN` 三状态 detector。
- 特征不完整时只允许 `ABSTAIN`，不得计为 noise 或 false negative。
- 冻结一个有限、单调的 27-member precision filter family。
- 仅使用 structural-null false-fire rate 选择每个 outer fold 的 filter；
  禁止使用 observed anchor count、future price 或交易 outcome 调参。
- 执行 leave-one-date-out cross-fitting；所有日期均标记为
  `historically_reused_post_selection`。
- 对独立 selection/evaluation null pipeline 计算 null false-fire rate、
  描述性 null-burden ratio、null-tail p-value、跨日集中度和独立
  cluster support。
- Coverage、最低 firing rate 和 recall 只做诊断，不做 hard gate。

verify：
- Frozen plan SHA binding。
- Source/cache/raw closure 和 source Git blob binding。
- Zero-outcome consumed-field whitelist。
- Tri-state partition 与 `ABSTAIN -> no signal` hostile tests。
- Null-only filter selection hostile tests。
- Filter monotonicity、refractory、cluster identity 和 slice invariance。
- Structural-null magnitude/zero/missingness/denominator conservation。
- Leave-one-date-out row/date isolation。
- Build A/B exact equality。
- Ruff、py_compile、pytest、git diff check。

done：
- 输出唯一 A-1 classification。
- 明确 structural false-fire control 是否通过。
- 明确是否只允许起草 exploratory A0。
- 无论是否通过，均不得声称 prospective support 或授权 live trading。

执行结果：
- Frozen implementation commit：`734bf9d5`。
- Canonical evidence：
  `local_live_analysis/skhynix_precision_first_flow_coherence_a_minus1_0829T001/`。
- Build A/B 非-cache artifact difference count：`0`。
- Focused regression：`30 passed`。
- 唯一 classification：
  `Aminus1_structural_support_not_estimable`。
- 350 个 common base candidates 中，所有 27 个 filters 的 admitted
  candidate/cluster count 均为 0。
- 最宽松 `F000` 的取消原因：
  `ABSTAIN=279`、`not_novel=34`、`conflict=22`、
  `coherence_lost=8`、`opposite_coherence=5`、`margin=2`。
- A-1-0 至 A-1-4 通过；A-1-5 首先失败。
- A0 drafting/execution、future outcome 与 live trading 均未授权。

QA Round 1：
- `P0/P1/P2/P3 = 0/1/2/0`，未通过。
- P1：negative/non-finite numeric corruption 没有动态、唯一地进入
  A-1-4 `Aminus1_selection_integrity_failed`。
- P2：produced artifact set 与 frozen Required Outputs 的精确命名和
  tri-state-by-date 证据不一致。
- P2：deterministic finalization 缺少 pending-stage comparison 与
  持久化 difference-count evidence。
- 负科学结果保持不变；remediation 不得改变 detector、null、threshold
  或 observed/null counts。

QA Round 1 remediation：
- 修复 commit：`e56bb5a5`。
- negative/non-finite count、exposure、rate 现在动态且优先进入
  A-1-4 `Aminus1_selection_integrity_failed`；raw、primary 和
  sensitivity hostile tests 均通过。
- 非-cache artifact set 现在严格等于 frozen Required Outputs 的
  21 个文件；旧替代名称及额外 predecessor-binding 文件已迁移/移除。
- predecessor blob/AST binding 已并入
  `contracts/execution_evidence_contract.json`。
- Build A/B 已持久化 preseal、pending、final 三阶段 difference
  count，三者均为 0。
- 科学结果、350 个 candidate、candidate SHA、27-filter admissions、
  observed/null counts 与所有阈值均未改变。
- Combined regression：`34 passed`；等待 QA Round 2。

明确禁止：
- 不把 availability、最低 firing rate 或 recall 作为主要成功标准。
- 不根据 observed anchor count 选择 filter。
- 不读取 future return、markout、fill、fee、PnL 或 model loss。
- 不收集新数据，不访问 private/order API。
