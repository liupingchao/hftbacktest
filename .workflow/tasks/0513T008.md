```md
执行线程：
- 测试线程

任务ID：
- 0513T008

标题：
- 5-13-day-control-30min T004/T007 full-run data-quality collection

简短描述：
- 使用 Step 1/T004 live 启动前 preflight 框架，更新 `awsserver1` 代码并采集 30 分钟 no-rule control 样本 `5-13-day-control-30min`。拉回后验证 T004 deployment manifest + T007 full-run provenance/top5 sidecar/as-of decision join quality，最后将样本分类为 `compressed_action_path_only`、`pricing_research_candidate`、`queue_fill_proxy_candidate` 或 `unusable`。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- 0513T004 QA 已通过
- 0513T007 已执行并等待 QA；本任务只使用其 standalone sidecar/join 工具做数据质量验证，不把它解释为策略收益或 live promotion 证明

规则更新提醒：
- 本任务允许启动一次 no-rule control live 数据采集，但不允许策略推广、不允许启用新规则、不允许改变策略参数语义。
- 本任务目标很窄：采集 `5-13-day-control-30min`，验证 T004 preflight 和 T007 full-run sidecar/join quality，输出样本分类。
- 若 T004 preflight 失败，必须停止，不得绕过 preflight 启动 live。

按线程规则执行：
- .workflow/workflow-kit/thread-report-template.md
- docs/thread-playbook.md
- task_plan.md
- progress.md

是否需要提交代码：
- 需要

提交要求：
- 为 git 同步到 `awsserver1`，必须提交并推送需要部署的 T004/T007 文件；自测/采集/拉回后回报 commit id
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

Allowed files：
- `.workflow/tasks/0513T008.md`
- `.workflow/reports/0513T008-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `.workflow/dashboard.html`
- `.workflow/dispatch_suggestions.md`
- `docs/qa-acceptance-report.md`
- `examples/binance_tick_mm/deploy/run_live.sh`
- `examples/binance_tick_mm/deploy/preflight_live_run.py`
- `examples/binance_tick_mm/test_deploy_preflight.py`
- `examples/binance_tick_mm/binance_top5_provenance.py`
- `examples/binance_tick_mm/test_binance_top5_provenance.py`
- `docs/binance_top5_provenance.md`
- `local_live_analysis/5-13-day-control-30min/**`
- `local_live_analysis/archive/5-13-day-control-30min.tar.gz`
- `local_live_analysis/archive/5-13-day-control-30min.tar.gz.sha256`

Allowed remote paths：
- `awsserver1:/home/admin/hft_live/**`

Forbidden files / areas：
- strategy rule logic, fair/target formula, risk guard behavior, quote placement behavior
- core `hbt.depth(0)` API
- connector local book management
- standard hftbacktest npz `data` schema or py `event_dtype`
- canonical live audit schema changes beyond already-deployed committed code
- live promotion scripts or production defaults
- archive/raw input modifications outside the new run output path

Requires separate task：
- 修改 T007 sidecar/join 工具逻辑。
- 将 T007 sidecar/join 集成进 `align_live_run.py` 标准流程。
- 修改 `maker_acceptance.py` 的正式 market-view gate。
- 做 pricing / OFI / microprice feature research。
- 设计或启用策略规则。
- 做 live micro promotion。

Fatal Failures：
- 未通过 T004 preflight 仍启动 live。
- 启用非 baseline/no-rule control 策略规则。
- 把本次 no-rule data collection 解释为 live promotion。
- 使用未来 top5 row join decision row，或不报告 `future_join_count`。
- 只报告 action-path acceptance，不报告 T007 full-run sidecar/join quality。
- 将 top5-only sidecar 解释为 full L2 equivalence 或 exact queue-position proof。

action：
- 本地确认 T004/T007 必需文件可通过 focused tests/help。
- 用 git 提交并推送当前需要部署到 AWS 的 T004/T007 文件。
- 在 `awsserver1` 通过 git 拉取/切换到目标 commit，不使用 scp 管理代码。
- 在远端准备 run-local 目录 `/home/admin/hft_live/runs/5-13-day-control-30min/` 和 run-local config。
- 通过 T004 `run_live.sh` 标准流程启动 no-rule control live 采集 30 分钟，生成：
  - `deployment_manifest.json`
  - `start_marker.json`
  - `stop_marker.json`
  - live audit
  - raw market data gzip/manifest
  - connector/bot/collector logs
- 停止后拉回 `5-13-day-control-30min` 到本地 `local_live_analysis/5-13-day-control-30min/`。
- 运行 `align_live_run.py` 和 `maker_acceptance.py`。
- 在完整 run 上运行 T007 sidecar + as-of decision join，输出 full-run provenance artifacts。
- 生成 archive 和 sha256。
- 给样本分类：
  - `compressed_action_path_only`
  - `pricing_research_candidate`
  - `queue_fill_proxy_candidate`
  - `unusable`

verify：
- Local pre-deploy：
  - `python -m pytest examples/binance_tick_mm/test_deploy_preflight.py`
  - `python -m pytest examples/binance_tick_mm/test_binance_top5_provenance.py`
  - `python examples/binance_tick_mm/deploy/preflight_live_run.py --help`
  - `python examples/binance_tick_mm/binance_top5_provenance.py --help`
  - `bash -n examples/binance_tick_mm/deploy/run_live.sh`
- Remote/live：
  - `deployment_manifest.json` exists and records target commit, dirty status, config/code hashes, schema compatibility passed.
  - `start_marker.json` and `stop_marker.json` exist.
  - run duration is approximately 30 minutes unless stopped by explicit blocker.
- Local pull/replay:
  - `python examples/binance_tick_mm/align_live_run.py --run-id 5-13-day-control-30min --local-root local_live_analysis --remote-host admin@awsserver1 --remote-root /home/admin/hft_live`
  - `python examples/binance_tick_mm/maker_acceptance.py --alignment-report local_live_analysis/5-13-day-control-30min/alignment_report_audit_replay.json --backtest-result local_live_analysis/5-13-day-control-30min/backtest_audit_replay_result.json --out local_live_analysis/5-13-day-control-30min/maker_acceptance.json`
- T007 full-run sidecar/join:
  - provenance sidecar exists.
  - raw-to-npz mapping exists.
  - reconstructed top5 sidecar exists.
  - joined decision artifact exists.
  - report raw message count, npz row mapping coverage, final data row mapping coverage, depth `pu` mismatch count, snapshot alignment status, first valid update alignment, bookTicker/depth BBO match/mismatch, decision join coverage, join age p50/p90/p99, stale join count, gap-crossed join count, future join count.
  - `future_join_count` must be `0` for any usable classification.
- Dashboard:
  - `python3 .workflow/build_dashboard.py`

done：
- 回报必须说明 deployed commit、run start/stop、manifest/preflight 结果、align/acceptance 结果、T007 full-run sidecar/join metrics、archive sha256、最终样本分类。
- 回报必须明确这是 no-rule control data-quality collection，不是策略收益证明，不授权 live promotion。
- 回报必须说明样本能否进入后续 top5 pricing / OFI proxy / microprice proxy 研究；若不能，说明 blocker 是 preflight、raw completeness、book sync、join age、BBO mismatch、acceptance 还是 artifact 缺失。
```
