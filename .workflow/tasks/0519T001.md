```md
执行线程：
- 测试线程

任务ID：
- 0519T001

标题：
- Step 6 final lifecycle calibration rerun

简短描述：
- 基于 `0515T003` 和 `0515T005` 的 replay lifecycle 修复结果，read-only 重跑 Stage 6 replay/live lifecycle calibration，正式量化修复后的 fill/cancel lifecycle 对齐程度。本任务只做验证和证据整理，不做 repair、不改策略、不启动 live。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- `0518T004` QA 已通过
- `0515T003` QA 已通过
- `0515T005` QA 已通过

规则更新提醒：
- 本任务只允许做 Step 6 收口验证，不允许扩展为新的 replay repair、queue repair、quote-adjustment experiment 或 live promotion。

按线程规则执行：
- .workflow/workflow-kit/thread-report-template.md
- docs/thread-playbook.md
- task_plan.md
- progress.md
- findings.md

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

Allowed files：
- `.workflow/tasks/0519T001.md`
- `.workflow/reports/0519T001-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `.workflow/dashboard.html`
- `.workflow/dispatch_suggestions.md`
- existing read-only Stage 6 runner / report scripts under `examples/binance_tick_mm/`

Allowed generated outputs：
- `local_live_analysis/5-13-day-control-30min/stage6_final_calibration_0519T001/**`

Allowed read-only inputs：
- `.workflow/reports/0514T006-business.md`
- `.workflow/reports/0514T006-qa.md`
- `.workflow/reports/0514T007-business.md`
- `.workflow/reports/0514T007-qa.md`
- `.workflow/reports/0515T003-business.md`
- `.workflow/reports/0515T003-qa.md`
- `.workflow/reports/0515T005-business.md`
- `.workflow/reports/0515T005-qa.md`
- `.workflow/reports/0516T001-qa.md`
- `.workflow/reports/0516T002-qa.md`
- `.workflow/reports/0518T001-qa.md`
- `local_live_analysis/5-13-day-control-30min/**`

Forbidden files / areas：
- live scripts, live collection, AWS/remote state, deployment state
- fair/reservation, pricing model, quote placement, inventory logic, quote update mechanics
- replay lifecycle repair code or queue/touch repair code
- core Rust, connector, py `event_dtype`, standard npz schema
- source-level market-view drift repair
- default-on behavior or live promotion

Requires separate task：
- Any new repair after seeing residual gaps.
- Any Step 7 inventory/execution redesign.
- Any Step 8 quote-update mechanics change.
- Any Step 9 quote-adjustment replay experiment.
- Any new live sample collection or live micro test.

Fatal Failures：
- 修改策略行为、replay repair 行为、live 行为或 schema。
- 把 `4948` / queue-ahead residual 当作本任务 repair 范围。
- 用单样本结论声明 live readiness 或 quote-adjustment promotion readiness。
- 启动 live 或连接交易所。

action：
- 读取并复用现有 Stage 6B / lifecycle calibration runner 和 artifacts。
- 在 `5-13-day-control-30min` 上重跑修复后的 replay/live lifecycle calibration。
- 必须报告至少以下指标的 live vs replay 对齐情况：
  - matched submit coverage
  - fill count / fill rate
  - fill-by-100ms / 500ms / 1s / 5s
  - time-to-fill distribution
  - final order state gap
  - fill-after-cancel-request count/rate
  - cancel-to-fill delay
  - fast-cancel-churn
  - markout / spread-retention observability coverage
  - placement / distance-to-BBO / edge-vs-fair / inventory / top-of-book-top5-size-age / latency strata gaps
  - remaining residual cases
- 必须明确 queue/priority、opportunity cost、realized PnL decomposition 仍是 observed-only proxy，不是 exact queue proof。

verify：
- 运行现有 focused tests / runner help，至少覆盖 Stage 6 lifecycle calibration runner。
- 重跑 `5-13-day-control-30min` Step 6 final calibration 并生成 `stage6_final_calibration_0519T001` 产物。
- `python3 .workflow/build_dashboard.py`
- `git diff --check`

done：
- 回报必须说明修复后的 replay/live lifecycle 是否已经从 `diagnostic_only_gap_too_large` 改善到可进入 Step 6 closure decision。
- 回报必须列出仍未对齐的字段和 residual cases。
- 回报必须说明是否仍需要更多 current-format samples。
- 回报必须说明没有 repair、没有策略变化、没有 live、没有 default-on。
- 回报必须写 `commit id` 和提交信息。
```
