```md
执行线程：
- 业务线程-python

任务ID：
- 0511T001

标题：
- adverse-selection timing rule 设计规格与验收合同

简短描述：
- 基于 0510T001/0510T002 的 cancel-fill 风险诊断、跨样本 Stage 6J 结果，以及当前 `examples/binance_tick_mm` live/backtest 共享策略结构，制定 adverse-selection timing rule 的设计规格、输入边界、审计字段、测试合同和 replay 验收标准。本任务只产出设计合同，不修改策略代码，不启动 live。

状态：
- 待执行

执行顺序：
- 0511 当日第一个任务

前置任务：
- 0510T001
- 0510T002

规则更新提醒：
- 本任务是设计任务，不实现代码。
- 任何后续 adverse-selection timing rule 都必须 default-off。
- live/backtest 必须共用同一个 strategy core helper，禁止两边各自实现不同逻辑。
- 规则输入只能来自决策时可见状态；未来 markout 只能作为验收指标。
- 第一版规则只允许 block add-side，不允许阻断 reduce-side。

按线程规则执行：
- .workflow/workflow-kit/thread-report-template.md
- docs/thread-playbook.md
- docs/5-8-future-plan.md
- docs/stage6i-cancel-requested-fill-risk-plan.md

是否需要提交代码：
- 不需要

提交要求：
- 不需要提交
- 回报必须写 `commit：无` 和 `提交信息：无`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- examples/binance_tick_mm/strategy_core.py
- examples/binance_tick_mm/live_tick_mm.py
- examples/binance_tick_mm/backtest_tick_mm.py
- examples/binance_tick_mm/audit_schema.py
- examples/binance_tick_mm/stage6j_replay.py
- examples/binance_tick_mm/test_backtest_tick_mm.py
- examples/binance_tick_mm/test_stage6j_replay.py
- docs/5-8-future-plan.md
- docs/stage6i-cancel-requested-fill-risk-plan.md
- findings.md
- progress.md

Fatal Failures：
- 设计合同允许 live 和 backtest 使用不同 adverse-selection timing 逻辑。
- 设计合同允许使用未来 markout、future mid 或 replay-only 信息作为 live 决策输入。
- 验收标准只看 PnL，不要求 source-path 风险下降。

MUST NOT：
- 不修改策略代码。
- 不启动真实 live，不连接交易所，不改 AWS 状态。
- 不默认开启新规则。
- 不扩大为 maker 参数搜索或全面策略重构。
- 不使用 audit overlay 做优化 replay。

action：
- 梳理当前 shared strategy core 分层，明确 signal、decision、execution、audit 的现状和边界。
- 定义 adverse-selection timing rule 的目标 source-path：cancel-requested fill / inventory-reducing cancel race / target deterioration 等。
- 定义允许使用的决策时输入，明确禁止未来 markout、future mid、replay-only overlay。
- 定义第一版触发条件候选：cancel-requested fill timing、target deterioration、pending cancel toxic window。
- 定义 add-side only / reduce-side allowed 的行为合同。
- 定义后续实现需要的 helper 形状、配置项、audit 字段和 Stage 6J replay 候选。
- 定义 unit test 合同和 replay 验收标准。
- 明确是否建议进入 `0511T002` 实现任务。

verify：
- 人工检查设计合同是否覆盖：输入边界、触发条件、默认关闭、live/backtest 共享路径、audit 字段、unit tests、Stage 6J replay、no-live 决策。
- 检查设计合同是否不要求任何策略代码改动。
- 检查设计合同是否明确禁止未来数据作为 live 决策输入。

done：
- 回报必须说明当前分层是否足以支撑后续规则接入，哪些部分仍需要后续重构。
- 回报必须说明 adverse timing rule 的候选触发条件、冷却窗口、add-side/reduce-side 行为。
- 回报必须列出后续 `0511T002` 需要新增的配置项、audit 字段、unit tests 和 Stage 6J 候选。
- 回报必须明确是否建议进入实现；如果不建议，说明阻断原因。
- 回报必须把 durable finding 写入 `findings.md`。
```
