# 任务派发

执行线程：
- 业务线程-python/research

任务ID：
- 0822T002

标题：
- SKHYNIX-C6IN-HYPERLIQUID-EXECUTION-LATENCY-MEASUREMENT-REVISION-2

简短描述：
- 按已关闭 review 的 c6in Hyperliquid execution-latency contract，在
  `c6in-winner` 上测量
  `risk_decision_ready -> authoritative_terminal_confirm`，并在打开 H0-B
  outcomes 前给出保留 100ms、修订 tuple 或继续锁定的可审计建议。
- 当前 GLFT 为 `production_dry` / `DryActionTransport`，不能提供真实
  passive cancel，因此本任务采用 separately authorized active calibration。
- 本 revision 仅把 monetary envelope 从 `5 / 10 / 1 USDC` 精确扩大为
  `15 / 30 / 3 USDC`；attempt、batch、duration、10-tick quote distance、
  sample gate 和 latency statistic 均保持不变。

状态：
- 执行中

历史阻塞与恢复记录：
- source commit：
  `0640c1502527a369e82987e81cefbeda8101f3dd`。
- `2026-08-22` c6in fresh Gate 0 通过：
  `15` surfaces、`15` distinct negative mutations、`7` exit criteria。
- Gate 1 通过 `217` tests；hostile current/frozen 共 `70` executions，
  fail-open `0`。
- 900 秒 public quote-safety preflight 通过：
  `1658` samples、`1657` valid pairs、nearest-rank p99
  `0.8033096356983497 bps`、minimum safe distance
  `1.6066192713966994 bps`、10-tick distance
  `8.030515960650472 bps`。
- 私有只读 account baseline 随后以
  `LATENCY_AUTHORIZATION_MISMATCH` fail closed：
  credential owner/mode/key names 均通过，open orders 为 `0`，
  SKHX position 为 `0`，但 target `xyz` DEX available margin
  非正，无法满足当前 implementation 的
  `available_margin_at_least_aggregate_cap` Gate。
- `credential_file_read=true`、`private_endpoint_called=true`；
  `order_endpoint_called=false`、`cancel_endpoint_called=false`、
  `fill_observed=false`。没有 active attempt、L1、formal package、
  amdserver archive 或 QA。
- authoritative blocker evidence：
  `.workflow/reports/0822T002-c6in-gate2-0640c1502527/`。
- `2026-08-23` 复核确认上述资金解释错误：`HL_WALLET` 是已批准的
  `hp1` API wallet/agent，不是统一主账户。错误 implementation 把 agent
  地址用于 `user_state`、`open_orders` 和 account token，因此得到空历史
  与零 perp account value。
- source commit
  `46ac34e4048e1c88f5d648dc9249dd6ed2ddabbf` 已修复为：
  signer/configured agent -> `Info.user_role(agent)` -> unified master；
  再以 master 执行 account queries 和
  `Exchange(account_address=master)`。
- c6in exact-commit read-only probe 通过：
  configured/signer role=`agent`、master role=`user`、
  abstraction=`unifiedAccount`、agent approved=`true`、
  expired=`false`、open orders=`0`、SKHX position=`0`；
  `available_margin_at_least_aggregate_cap=true`，来源为
  `unified_spot_usdc_available`。
- 正确 master 的历史汇总为 `682` orders、`348` fills，其中
  `xyz:SKHX` 为 `12` orders、`11` fills。探针没有调用 order/cancel
  endpoint，也没有持久化原始账户地址或订单引用。
- redacted recovery evidence：
  `.workflow/reports/0822T002-c6in-account-probe-46ac34e4/`。
- 资金 blocker 已解除；任务恢复为 `执行中`。仍需重新冻结 future UTC
  windows，并从新 source commit 执行 fresh full Gate 2 和正式 active
  measurement。用户对 private read、post-only submit、cancel 和
  reduce-only flatten 的既有授权继续有效，不需要再次授权。
- 恢复 runner 必须在 Gate 0、Gate 1、hostile、notional subgate 和
  schedule freeze 通过后、full Gate 2 开始前执行
  `/home/admin/trading/inspect --repo <detached-clean-task-repo>
  --env-file /home/admin/trading/credentials.env
  --python <pinned-task-venv-python> --json`。该 discovery receipt 必须证明
  clean exact commit、Hyperliquid SDK `0.24.0`、order/cancel/query
  surfaces、secure credential symlink 和零 account/order/cancel endpoint
  调用。共享 `/home/admin/trading/repo` 的 dirty state 不得作为任务 source。
- H0-B 继续锁定。

superseded task record：
- `0822T001` 保持 `阻塞` 且不得修改。其 c6in Gate 0 和 Gate 1 已通过；
  最终执行 commit 为
  `136157ba54aaea4bd347095fa1996c44919cf20a`。
- `0822T001` Gate 2 在凭据读取前以
  `LATENCY_AUTHORIZATION_MISMATCH` fail closed：
  venue minimum 为 `10 USDC`，高于 frozen
  `per_order_notional_cap_usdc=5`；当前 `0.001` lot 和
  `1246.1` reference mid 下最小可执行 `0.009 SKHX =
  11.2149 USDC`，也高于 frozen
  `aggregate_position_cap_usdc=10`。
- 旧 receipt 位于
  `.workflow/reports/0822T001-c6in-gate2-136157ba54aa/gate2/gate2_preflight_receipt.json`。
- `credential_file_read=false`，private/order/cancel endpoint 均未调用；
  900 秒 public quote-safety collection 因更早的 authorization block
  未启动。
- `0822T002` 是 outcome-blind superseding contract。旧市场观察只说明
  revision 的必要性，不能替代新 Gate 2 的实时重算。H0-B 继续锁定。

执行顺序：
- 当前唯一任务

前置任务：
- `0821T001` 已通过并由总控接受 H0-A authoritative package、`50ms`
  horizon 和 exact R/C/E/composite。
- `0820T001` 已通过并由总控接受 Research Package Trust Kernel v1。

规则更新提醒：
- 请先阅读 workflow、accepted H0-A closure、execution-latency contract、
  accepted Trust Kernel v1 pin 和本任务 canonical Surface Matrix。

按线程规则执行：
- `AGENTS.md`
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `.workflow/workflow-kit/research-package-task-template.md`
- `docs/skhynix_c6in_hyperliquid_execution_latency_measurement_plan_v2.md`
- `docs/skhynix_continuous_hazard_maker_research_framework_v2.md`
- `.workflow/reports/0821T001-controller-closure.md`
- `.workflow/contracts/0822T002-surface-matrix.json`

task_type：
- research_package

produces_research_package：
- true

kernel pin：
- mode=accepted
- kernel_name=research_package_trust_kernel
- kernel_version=v1
- registry_path=baselines/research_package_trust_kernel/accepted_versions.json
- registry_revision=1
- registry_entry_sha256=cae21d65bf447435bafc37508b8ca00643a0742b37e0f404148cab92818c90c9
- kernel_source_tree_sha256=cee2395afad9420c38235ba195bf030e92330015e1a15937ebc22fa707c80203
- kernel_api_contract_sha256=2cd5a67ba15d67e59bcddcdbb21593696d3dc3dc27d81986c39ddf3e91e91f5f
- kernel_negative_matrix_sha256=f6247594b6f024945a52c0dccf421bac93538d9357f92ff6027d024199fc6b97
- kernel_qa_report_sha256=8fe01f85f8a68581b79ee410167769f2a105d9cc74ca6528af9496808a626be8
- kernel_acceptance_task_id=0820T001
- acceptance_package_path=baselines/research_package_trust_kernel/v1/v1_acceptance_package
- 业务线程和 QA 不得修改 accepted registry 或重新发布 kernel v1。

accepted H0-A pin：
- task_id=0821T001
- selected_horizon_ms=50
- gate_latency_ms=100
- primary_tuple_sha256=e5d1b132248ff1a6933678c32a54e6b4147c1c6f47dab25103011ecbd7a68eca
- R=7176c78c2b6bfadf11432e9f5a1c1eaf9627c8028a4a22257d67d5a5404dc8fd
- C=4e8ccc7466f84d9eb2f71f681557424b59ca58249d67426325ecdbd661189636
- E=8745458fcf4e0ab31f8ad3b2bc2d1d93704f18b13a776c515f14195b51213969
- composite=2682c32eefac427eed1899a3d492b3fc7545520f72021723e8fef7a0d4d8d9d0
- controller_closure_sha256=9cc1b1d24d29cd2b55a8c1774a9d9e6e59338242c95c3af861460a0b8b07aded
- H0-A package and tuple remain immutable.

execution contract pin：
- execution_plan_sha256=9f29b45aaba532922fe21c65d46623748da66b3338e5ff6ca5fa153e38c91391
- revision=execution contract 2
- primary_latency=risk_decision_ready_to_authoritative_terminal_confirm
- primary_quantile=nearest_rank_p95
- bucket_rule=max_100ms_then_round_up_50ms
- H0-B outcome access=false

active authorization：
- authorization_source=user instruction on `2026-08-22` approving
  `15 / 30 / 3 USDC` and explicitly authorizing subsequent private reads,
  post-only orders, cancels and reduce-only flatten without another
  authorization checkpoint
- active_private_read_authorized=true
- active_order_submit_authorized=true
- active_cancel_authorized=true
- reduce_only_flatten_authorized=true
- target_account_identity_token_rule=sha256_of_normalized_derived_unified_master_address
- configured_identity_role=approved_agent_or_unified_master
- account_role=user
- account_abstraction=unifiedAccount
- agent_master_match=true
- agent_approved=true
- agent_expired=false
- unified_collateral_sources=target_dex_account_value_or_withdrawable_or_spot_usdc_available
- target_market=Hyperliquid mainnet xyz DEX SKHX canonical order asset
- post_only_required=true
- time_in_force=Alo
- max_open_orders=1
- max_attempts_per_batch=10
- max_total_attempts=120
- max_total_batches=12
- max_batch_duration_seconds=900
- fixed_pre_cancel_settle_ms=250
- minimum_inter_attempt_seconds=20
- per_order_notional_cap_usdc=15
- aggregate_position_cap_usdc=30
- max_loss_usdc=3
- max_loss_basis=realized_reduce_only_flatten_slippage
- quote_distance_ticks=10
- current_active_only_eligible_target=100
- no_top_up_after_attempt_120=true

quote-distance safety freeze：
- public_preflight_duration_seconds=900
- public_preflight_horizon_ms=250
- public_preflight_min_valid_pairs=1000
- public_preflight_quantile=nearest_rank_p99_abs_mid_move_bps
- minimum_safe_quote_distance_bps_formula=
  `max(1.0, 2 * nearest_rank_p99_abs_250ms_mid_move_bps)`
- active submit requires
  `10 * tick_size / reference_mid_price * 10000 >=
  minimum_safe_quote_distance_bps`.
- failure blocks before the first submit; quote distance cannot be changed
  inside this task.

host freeze：
- ssh_alias=c6in-winner
- cloud_instance_id=i-0a962e47210528526
- region=ap-northeast-1
- expected_user=admin
- task-owned runtime root=`/tmp/0822T002-<source_commit_prefix>` because the
  c6in root filesystem has zero free bytes; unrelated `/home` data must not be
  deleted to make room.
- exact machine, boot, network, clock and runtime identities are emitted by
  Gate 2 before any private endpoint call.

whether live starts：
- 用户已明确授权本 revision 的后续真实下单，不需要再次取得授权。
- Gate 0 must validate the task/matrix/pins.
- Gate 1 must pass all offline fake-client and hostile tests.
- Gate 2 must pin a clean c6in runtime, secure credential source, account
  token, exact market/tick/lot/minimum size, 10-tick bps safety, empty owned
  open orders and baseline position.
- Active private/order/cancel calls are forbidden until all three gates pass.

是否需要提交代码：
- 需要

提交要求：
- Gate 0 dispatch contract must be committed before active execution.
- Business implementation, offline tests, c6in evidence, formal package,
  archive and handoff must be committed before QA.
- 回报必须带 commit id、R/C/E/composite、sample/reliability facts、p95、
  recommendation、archive identity 和 QA entrypoint。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 不下单、不撤单；独立验证 sealed business evidence。
- 重跑 Gate 0、current/frozen hostile matrix、focused tests、L1 rebuild、
  Trust Kernel admission、exact-tree archive admission 和 H0-B lock。
- 独立重建 attempts、eligibility、terminal authority、flatten loss、
  nearest-rank p95、50ms bucket 和最终 recommendation。

files：
- `.workflow/tasks/0822T002.md`
- `.workflow/contracts/0822T002-surface-matrix.json`
- `.workflow/reports/0822T002-*`
- `.workflow/runners/0822T002_run_c6in_latency.sh`
- `.workflow/runners/0822T002_archive_latency_to_amdserver.sh`
- `docs/skhynix_c6in_hyperliquid_execution_latency_measurement_plan_v2.md`
- `examples/hyperliquid/skhynix_c6in_latency_v2.py`
- `examples/hyperliquid/skhynix_c6in_latency_contracts_v2.py`
- `examples/hyperliquid/test_skhynix_c6in_latency_v2.py`
- `examples/hyperliquid/test_skhynix_c6in_latency_package_v2.py`
- `local_live_analysis/skhynix_c6in_hyperliquid_execution_latency_0822T002/`
- `task_plan.md`
- `progress.md`
- `findings.md`

formal package root：
- `local_live_analysis/skhynix_c6in_hyperliquid_execution_latency_0822T002`

exact package tree：
- directories:
  `contracts`, `reports`, `runtime_source`, `runtime_tests`
- root files:
  `measurement_manifest.json`, `frozen_measurement_contract.json`,
  `accepted_h0a_pin.json`, `host_identity.json`, `runtime_identity.json`,
  `market_identity.json`, `authorization_envelope.json`,
  `collection_window_schedule.csv`, `attempt_ledger.csv`,
  `lifecycle_events.csv`, `failure_and_censoring.csv`,
  `latency_by_attempt.csv`, `latency_summary.csv`,
  `reliability_summary.json`, `historical_context_awsserver.csv`,
  `controller_latency_recommendation.json`, `boundary_manifest.json`,
  `sha256_inventory.csv`
- contract files:
  `contracts/task.md`, `contracts/surface_matrix.json`,
  `contracts/execution_plan.md`, `contracts/v2_framework.md`,
  `contracts/accepted_kernel_pin.json`
- report file:
  `reports/execution_latency_measurement.md`
- runtime source:
  `runtime_source/skhynix_c6in_latency_v2.py`,
  `runtime_source/skhynix_c6in_latency_contracts_v2.py`
- runtime tests:
  `runtime_tests/test_skhynix_c6in_latency_v2.py`,
  `runtime_tests/test_skhynix_c6in_latency_package_v2.py`
- no credential, raw private payload, raw oid/cloid, symlink, special entry or
  H0-B outcome file is allowed.
- full package size must be at most 64 MiB.

action：
1. Validate Gate 0 before credential/private access.
2. Implement L0 lifecycle instrumenter, exact-reference terminal classifier,
   flatten safety, redaction and L1 no-network summarizer.
3. Execute all declared negative mutations on current/frozen runtime with
   fail-open count zero.
4. Pin c6in host/runtime/market/account identities and run the 900s public-only
   quote-distance safety preflight.
5. Fail closed before submit if credentials, account, market precision,
   minimum size/notional, empty-order baseline, position baseline or 10-tick
   safety cannot be proven.
6. Run at most 120 active attempts across three preselected UTC windows,
   reconciling each exact order before the next.
7. Stop on any fill; run only authorized reduce-only flatten and compute
   realized flatten slippage after authoritative completion.
8. Seal L0 artifacts, run fresh no-network L1 summarization, build isolated
   A/B/formal packages and require deterministic result parity.
9. Publish with accepted Trust Kernel v1, archive to amdserver and write the
   business report as `待验收`.

verify：
- `python3 .workflow/workflow-kit/validate_research_package_task.py --task .workflow/tasks/0822T002.md --matrix .workflow/contracts/0822T002-surface-matrix.json`
- `python3 -m pytest examples/hyperliquid/test_skhynix_c6in_latency_v2.py examples/hyperliquid/test_skhynix_c6in_latency_package_v2.py`
- `python3 examples/hyperliquid/skhynix_c6in_latency_v2.py hostile-preflight --task .workflow/tasks/0822T002.md --matrix .workflow/contracts/0822T002-surface-matrix.json --output .workflow/reports/0822T002-hostile-preflight.json`
- `ruff check examples/hyperliquid/skhynix_c6in_latency_v2.py examples/hyperliquid/skhynix_c6in_latency_contracts_v2.py examples/hyperliquid/test_skhynix_c6in_latency_v2.py examples/hyperliquid/test_skhynix_c6in_latency_package_v2.py`
- `PYTHONPYCACHEPREFIX=/tmp/0822T002-pycache python3 -m compileall -q examples/hyperliquid/skhynix_c6in_latency_v2.py examples/hyperliquid/skhynix_c6in_latency_contracts_v2.py`
- `bash -n .workflow/runners/0822T002_run_c6in_latency.sh`
- `bash -n .workflow/runners/0822T002_archive_latency_to_amdserver.sh`
- `git diff --check`

Surface Matrix：

| Surface | Authoritative source | Decision/as-of time | Exact fields/keys | Rebuild oracle | Negative mutation | Durable evidence | Identity layer |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `kernel_pin` | accepted Trust Kernel v1 registry entry | before any task-source access | exact accepted pin | workflow validator | alter pin -> `KERNEL_PIN_MISMATCH` | accepted v1 QA | `C` |
| `accepted_h0a_pin` | accepted 0821T001 closure and formal package | before measurement design access | exact tuple and R/C/E/composite | pin verifier | alter tuple/composite -> `LATENCY_H0A_PIN_MISMATCH` | 0821 closure | `C` |
| `authorization_envelope` | user authorization plus frozen task envelope | before private access | exact booleans and caps | contract validator | enable unapproved action/cap -> `LATENCY_AUTHORIZATION_MISMATCH` | new task surface | `C` |
| `c6in_host_identity` | EC2/OS/boot/network facts on c6in-winner | Gate 2 before private access | exact host and boot fields | remote preflight | alter host/boot -> `LATENCY_HOST_IDENTITY_DRIFT` | new task surface | `E` |
| `runtime_transport_identity` | clean deployed runtime and transport boundary | Gate 2 and every batch | exact source/dependency/client/route fields | runtime preflight | admit dry transport as live -> `LATENCY_PASSIVE_SOURCE_NOT_LIVE` | GLFT dry boundary | `C` |
| `market_quote_safety` | Hyperliquid public metadata/BBO on c6in | before first private call | exact asset/tick/lot/minimum/10-tick bps formula | public preflight | unresolved/insufficient safety -> `LATENCY_QUOTE_DISTANCE_SAFETY_UNVERIFIED` | new task surface | `E` |
| `monotonic_lifecycle` | process-local monotonic timestamps | every attempt | exact event order and intervals | fake clock and L1 replay | reorder timestamp -> `LATENCY_MONOTONIC_ORDER_INVALID` | new task surface | `R` |
| `terminal_reference` | exact oid/cloid queries and terminal classifier | cancel through final proof | exact reference and authority order | fake/live lifecycle replay | response promoted to terminal -> `LATENCY_CANCEL_RESPONSE_NOT_TERMINAL` | accepted prior classifier evidence | `C` |
| `active_safety_flatten` | authorization envelope plus private reconciliation | before/after each attempt | caps, fill stop, reduce-only flatten and realized loss | fake/live safety replay | use MTM loss basis -> `LATENCY_LOSS_CAP_CONTRACT_MISMATCH` | new task surface | `E` |
| `attempt_population` | sealed L0 attempt/event ledger | every real attempt | exact row universe and eligibility classes | L1 reconstruction | omit real attempt -> `LATENCY_REAL_ATTEMPT_OMITTED` | new task surface | `R` |
| `sample_reliability` | reconstructed target attempt population | final summary | 100/120/windows/terminal/fill/unresolved formulas | L1 reconstruction | top-up attempt 121 -> `LATENCY_ATTEMPT_CAP_EXHAUSTED` | new task surface | `R` |
| `quantile_recommendation` | eligible target cancel-effective latencies | after sealed L0 | nearest-rank p95 and upward 50ms bucket | L1 reconstruction | substitute percentile/round down -> `LATENCY_QUANTILE_CONTRACT_MISMATCH` | new task surface | `R` |
| `sealed_l0_l1_boundary` | fsynced redacted L0 root | before L1 starts | exact inventory, no secrets/network | guarded summarizer | open external/network path -> `LATENCY_L1_BOUNDARY_VIOLATION` | new task surface | `E` |
| `package_identity_archive` | Trust Kernel v1 package and amdserver archive | after summarization | exact tree, R/C/E/composite and remote parity | kernel admission | alter tree/binding -> `COMPOSITE_IDENTITY_BINDING_MISMATCH` | accepted kernel v1 | `E` |
| `h0b_nonaccess` | v2/H0-A outcome lock | entire task | zero H0-B outcome opens and no tuple mutation | guarded opener/boundary ledger | open outcome path -> `LATENCY_H0B_OUTCOME_ACCESS_FORBIDDEN` | accepted H0-A closure | `E` |

Canonical matrix：
- .workflow/contracts/0822T002-surface-matrix.json

七条 exit criteria：
- `EC1` every latency, authority, safety and identity surface has an exact
  source or frozen unavailable reason.
- `EC2` every emitted field is source-derived or uses a frozen explicit null.
- `EC3` every JSON/CSV/report/receipt has an exact key/type/value universe.
- `EC4` L0, L1, staging, formal and archive trees reject unknown paths/types.
- `EC5` unchanged Trust Kernel/H0-A/GLFT-boundary claims bind durable evidence.
- `EC6` every declared mutation executes with its exact stable code on current
  and frozen runtime.
- `EC7` hostile preflight and offline safety tests pass before credentials or
  private endpoints are accessed.

done：
- Business report ends at `待验收`.
- Report records Gate 0-7, c6in/market/runtime identities, exact attempts,
  sample/reliability gates, p95/bucket recommendation, R/C/E/composite,
  archive identity and QA entrypoint.
- Any blocker is truthful; sample/cap/quote-distance/loss contracts are not
  relaxed.
- H0-B remains locked pending independent QA and controller latency decision.
