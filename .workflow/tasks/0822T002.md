# 0822T002 C6IN Trading Runtime Discovery And Short Aliases

执行线程：
- 业务线程

任务ID：
- 0822T002

标题：
- C6IN-TRADING-RUNTIME-DISCOVERY-AND-SHORT-ALIASES

简短描述：
- 修复 c6in 上因 checkout、credential env 位于 `/srv/crypto-bot/...`
  symlink 后方以及 venv 命名不含 `hyperliquid` 而产生的漏检。
- 建立受限权限的 `~/trading` 短入口，并提供只读、脱敏、symlink-aware
  的 Binance/Hyperliquid runtime discovery。

状态：
- 待验收

执行顺序：
- 可与 `0822T001` 并行；本任务只建立别名和只读发现能力，不调用
  private/account/order/cancel endpoint，不修改 live runtime source。

前置任务：
- 无

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `AGENTS.md`

是否需要提交代码：
- 不需要

提交要求：
- 本轮保留工作树供总控与 QA 审阅；`commit：无`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

files：
- `.workflow/tasks/0822T002.md`
- `.workflow/reports/0822T002-business.md`
- `examples/hyperliquid/trading_runtime_discovery.py`
- `examples/hyperliquid/test_trading_runtime_discovery.py`
- `docs/trading_runtime_discovery.md`
- `.workflow/runners/0822T002_deploy_c6in_runtime_discovery.sh`
- `AGENTS.md`
- `task_plan.md`
- `progress.md`
- `findings.md`
- remote `/home/admin/trading/**`

action：
1. 建立 canonical aliases：
   - `~/trading/repo`
   - `~/trading/credentials.env`
   - `~/trading/.env`
   - `~/trading/venv`
   - `~/trading/python`
   - `~/trading/inspect`
2. Alias 只指向现有 source of truth，不复制或改写 credential values。
3. Discovery 必须显式解析 symlink，并同时记录 alias path 与 resolved path。
4. Credential 输出只允许 key 名、是否非空、权限和 exchange coverage；
   禁止输出 value、hash、address、private key 或 reusable secret material。
5. Python discovery 必须证明 Hyperliquid SDK version 及 order/cancel/info
   method surface，不构造 wallet client、不调用 endpoint。
6. Repo discovery 必须记录 branch、commit、dirty count 和 Binance/
   Hyperliquid order runtime source presence。

verify：
- `python -m pytest examples/hyperliquid/test_trading_runtime_discovery.py`
- `python examples/hyperliquid/trading_runtime_discovery.py inspect --help`
- `python examples/hyperliquid/trading_runtime_discovery.py install --help`
- `python -m py_compile examples/hyperliquid/trading_runtime_discovery.py`
- `bash -n .workflow/runners/0822T002_deploy_c6in_runtime_discovery.sh`
- c6in `~/trading/inspect --json`
- c6in alias/resolved-path/permission checks
- secret sentinel absence check
- `git diff --check`

done：
- c6in 上 Binance 和 Hyperliquid 下单相关 credential candidate、checkout
  与 Python runtime 可通过 `~/trading/inspect` 一次发现。
- symlink 后方 source 不再漏检，且发现输出不含任何 secret value。
- 业务回报状态结束于 `待验收`。
