```md
执行线程：
- 业务线程-research

任务ID：
- 0618T012

标题：
- M2 read-only public L2/trades flow diagnosis

简短描述：
- 基于 Hyperliquid public L2/trades 做只读 flow diagnosis，拆分 M2 no-fill 的主要原因：队列太深、没有 trade-through、时段不对、side 不对、quote 老化 / 市场快速漂移。
- 本任务只采集/分析 public market data；不下单、不读取凭证、不调用 private/account/order endpoint、不刷新 remote checkout、不重跑 final gate。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- `0618T011` QA 为 `已通过`。

规则更新提醒：
- 请先阅读 `.workflow/workflow-kit/workflow-manual.md`、`.workflow/workflow-kit/task-dispatch-template.md`、`.workflow/workflow-kit/thread-report-template.md`、`.workflow/workflow-kit/qa-acceptance-template.md`、`task_plan.md`、`progress.md`、`findings.md` 后再执行本任务。

按线程规则执行：
- `AGENTS.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0618T012.md`
- `.workflow/reports/0618T012-business.md`
- `.workflow/reports/0618T012-qa.md`
- `docs/qa-acceptance-report.md`
- `examples/hyperliquid/hyperliquid_tiny_live_m2_public_flow_diagnosis.py`
- `examples/hyperliquid/test_hyperliquid_tiny_live_m2_public_flow_diagnosis.py`
- `local_live_analysis/hyperliquid_tiny_live_m2_public_flow_diagnosis_0618T012/**`
- `findings.md`
- `progress.md`
- `task_plan.md`

hard boundary：
- Public L2/trades only.
- No live order.
- No remote checkout refresh.
- No final gate rerun.
- No credential read.
- No private/account/order endpoint call.
- No taker/crossing recommendation.
- No cap relaxation recommendation.
- No stable PnL or maker viability claim.
- Queue and fill results must remain public-flow proxy only, not exact queue position or real fill proof.

action：
- Implement a public-only diagnosis runner that can either collect a short Hyperliquid public `l2Book` / `trades` sample or analyze an existing public raw sample.
- Generate passive touch-quote candidates for both buy-at-bid and sell-at-ask under the existing M2 size proxy.
- For each candidate, compute:
  - same-side top depth / order-count proxy;
  - touch and strict trade-through flow within the quote hold window;
  - public queue-depletion proxy versus same-side top depth plus order size;
  - quote aging / BBO drift / rapid loss of touch;
  - side-level and sample-window summaries.
- Output a hypothesis decision matrix for:
  - `queue_too_deep`
  - `no_trade_through`
  - `wrong_time_of_day`
  - `wrong_side`
  - `quote_aging_or_fast_drift`
- Preserve the T008 ledger fail-closed / M2 blocked boundary.

acceptance criteria：
- Public collection, if run, uses only Hyperliquid public WebSocket / public Info `l2Book` behavior.
- Artifacts explicitly state no credentials, no private/account/order endpoints, no live orders.
- The decision matrix must distinguish observed public-flow facts, proxy inferences, and inconclusive areas.
- The task must answer whether each no-fill cause is supported, rejected, partial, or inconclusive for the sampled window.
- QA must verify artifacts, focused tests, JSON validity, artifact non-empty checks, boundary/redaction scan, and `git diff --check`.

verify：
- `python -m pytest examples/hyperliquid/test_hyperliquid_tiny_live_m2_public_flow_diagnosis.py examples/hyperliquid/test_hyperliquid_public_sample.py -q`
- `python -m py_compile examples/hyperliquid/hyperliquid_tiny_live_m2_public_flow_diagnosis.py`
- `python examples/hyperliquid/hyperliquid_tiny_live_m2_public_flow_diagnosis.py --help`
- `python examples/hyperliquid/hyperliquid_tiny_live_m2_public_flow_diagnosis.py --duration-seconds 120 --output-dir local_live_analysis/hyperliquid_tiny_live_m2_public_flow_diagnosis_0618T012`
- JSON validation for the manifest.
- Artifact non-empty check.
- Boundary/redaction scan.
- `git diff --check`

done：
- 回报 public-flow 诊断结论、artifact path、每个 hypothesis 的状态、验证命令、blockers、commit id、提交信息。
- 明确说明 M2 是否仍 blocked，以及下一步是否允许 maker-only retry 或仍需补 public samples。
```
