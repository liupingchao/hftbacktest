# 任务派发

执行线程：
- 业务线程-python/research-postprocess + cross-exchange-signals

任务ID：
- 0805T003

标题：
- SINGLE-DATASET-BASIS-DISLOCATION-STAGE

简短描述：
- 从已通过的 R0/R1 构建单数据集、无未来信息、epoch/mask-aware 的
  basis 与方向性 BBO dislocation 特征。
- 将该 stage 接入统一 postprocess pipeline，形成可恢复的
  `basis-research` profile，并在 Aug05 4H 数据上验证。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- `0805T002` 已通过，提供统一 pipeline、stage fingerprint、resume、
  provenance 和报告契约。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 不需要

提交要求：
- 当前工作区包含已验收但未提交的前置工作，不创建混合提交。
- 回报使用 `commit：无`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 检查无未来信息、strict as-of、reconnect epoch reset、exact masks、
  spread identity、rolling-window trailing semantics、determinism 和
  source/R0/R1 immutability。

files：
- `examples/hyperliquid/cross_exchange_postprocess/basis_dislocation.py`
- `examples/hyperliquid/cross_exchange_postprocess/pipeline.py`
- `examples/hyperliquid/cross_exchange_postprocess/profiles.py`
- `examples/hyperliquid/cross_exchange_postprocess/reporting.py`
- basis/pipeline focused tests
- `.agents/skills/cross-exchange-postprocess/`
- `docs/cross_exchange_postprocess_pipeline.md`
- amdserver Aug05 pipeline output
- workflow task/report/QA/progress files

action：
- 构建 Binance bookTicker 与 Hyperliquid BBO 的 local-receipt-time union。
- 所有 as-of state 必须满足 source timestamp 不晚于 decision timestamp。
- core reconnect 起点插入显式 BBO reset；新 epoch 第一条 BBO 前禁止旧
  Hyperliquid 状态 forward-fill。
- 输出 midpoint basis、`d_bh`、`d_hb`、spread、100ms change、15min
  trailing median/MAD/z、60s Binance volatility 和 source age。
- 分开记录 core、fast-stale、auxiliary masks；basis feature eligibility
  只使用该特征家族实际依赖的 core/fast-L2 边界。
- 对每行验证
  `d_bh + d_hb + Binance spread + Hyperliquid spread = 0`。
- 生成 deterministic gzip、segment quality、summary、manifest 和报告。
- 接入 pipeline stage cache；`basis-research` profile 可执行，
  `signal-research` 仍因 lead-lag/maker 未实现而 fail closed。

verify：
- focused tests 覆盖 strict as-of、同 timestamp 排序、reconnect reset、
  inclusive exact masks、old-state leakage、future join、crossed book、
  spread identity、trailing-only rolling、deterministic gzip、tamper 和
  profile resume。
- local relevant regression、py_compile、Skill validation 和
  `git diff --check`。
- amdserver Aug05 4H 执行两次隔离 basis build，核心输出 byte-identical。
- source campaign、R0、R1 输入在运行前后 SHA 不变。

done：
- `basis-research` profile 可从 campaign 一次生成 R0/R1 与 basis state。
- 输出可作为后续 lead-lag stage 的唯一规范输入。

hard boundary：
- 不实现 lead-lag outcome、maker diagnostics 或 hierarchy stages。
- 不做未来 horizon、alpha 拟合、参数搜索、交易或新数据采集。
- 不形成 exact fill、可执行套利、maker PnL、因果 Binance lead 或
  live promotion 结论。
