# 0718T017 EXCHANGE-RECONCILED-SINGLE-LEVEL-ORDER-MANAGER

执行线程：
- 总控 auto-loop / 业务实现线程

任务ID：
- 0718T017

标题：
- Exchange-Reconciled 单层双边订单状态机

简短描述：
- Implement Principal Alignment Task 6 (C13/C14): a strategy-owned, exchange-reconciled, single-level two-sided order manager with stable logical quote keys and deterministic lifecycle generations.
- The manager must recover owned exchange orders, prevent duplicate/retry ambiguity, preserve cancel-pending/inflight exposure, and enforce anti-churn without using `run_order_once` as its continuous loop.

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- 0718T016

规则更新提醒：
- 无

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `docs/superpowers/plans/2026-07-17-principal-alignment-p0-p3.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `examples/hyperliquid/hyperliquid_maker_order_manager.py`
- `examples/hyperliquid/test_hyperliquid_maker_order_manager.py`
- `examples/hyperliquid/hyperliquid_tiny_live_real_order_executor.py`
- `examples/hyperliquid/test_hyperliquid_tiny_live_real_order_executor.py`

action：
- Implement states: `desired`, `submit_inflight`, `resting`, `partial_fill`, `cancel_requested`, `cancel_confirmed`, `filled`, `rejected`, `unknown`.
- Use logical quote key `(symbol, side, canonical_price_key)`; enforce one active owned order per side/price and `max_levels_per_side=1`.
- Generate deterministic SDK-compatible cloids containing a stable ownership prefix plus task/run/window/side/price/generation hash; cancel/re-add at the same price uses a new generation/cloid.
- On startup/reconnect, read exchange open orders, manage only owned prefix orders, recover locally missing owned orders, and fail closed on duplicate owned logical keys.
- Ambiguous submit results must query by cloid/oid before any retry; cancel remains `cancel_requested` and in working exposure until exchange absence/confirmation.
- Apply partial fills to leaves and position evidence.
- Enforce min price move, min quote age, submit-inflight/cancel-pending guards, post-only reject cooldown, and per-side cancel/re-add rate limits.
- Emergency risk may bypass normal hold/cooldown, but submit still must pass existing executor price and runtime exposure caps.
- Do not implement multi-level, watcher wiring, live status, live execution, or replace `run_order_once` as the manager loop in this task.

verify：
- Deterministic cloid/ownership/canonical-price tests.
- Startup/restart/reconnect recovery, foreign-order ignore, duplicate-owned fail-closed.
- Same-key dedupe, same-price new-generation after confirmed cancel, ambiguous-submit query-before-retry.
- Partial-fill leaves/position update and cancel-pending working exposure.
- Anti-churn guards and emergency bypass with exposure cap retained.
- `python -m pytest examples/hyperliquid/test_hyperliquid_maker_order_manager.py examples/hyperliquid/test_hyperliquid_tiny_live_real_order_executor.py -q`
- `python -m py_compile examples/hyperliquid/hyperliquid_maker_order_manager.py examples/hyperliquid/hyperliquid_tiny_live_real_order_executor.py`
- `git diff --check`
- Explicitly confirm no live/private/order/cancel/network/remote/service action.

done：
- Report state-machine/reconciliation/ownership semantics, anti-churn and exposure behavior, focused/regression counts, commit id, and endpoint-action confirmation.
