```md
执行线程：
- 业务线程-live-awsserver1

任务ID：
- 0623T009

标题：
- M2 AWS live public no-submit shadow soak + canary preflight ledger

简短描述：
- 跳过单独的 `0623T008` public connectivity 专项任务，把原建议中的 live public no-submit shadow soak 和 tiny real canary preflight ledger 合并为一个 `awsserver1` 远端执行任务。
- 本任务只在 `awsserver1` 上运行 public-only / no-submit shadow soak，并生成 canary preflight / dry-run ledger；不读凭据、不调用 private/account/order/cancel endpoint、不下单。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- `0623T007` business execution complete and reported with implementation commit `92ec133` and report commit `46c26af`.

规则更新提醒：
- 请先阅读 `.workflow/workflow-kit/workflow-manual.md`、`.workflow/workflow-kit/task-dispatch-template.md`、`.workflow/workflow-kit/thread-report-template.md`、`.workflow/workflow-kit/qa-acceptance-template.md`、`task_plan.md`、`progress.md`、`findings.md` 后再执行本任务。

按线程规则执行：
- `AGENTS.md`

是否需要提交代码：
- 需要

提交要求：
- 自测 / 远端执行证据生成后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

host / execution location：
- `awsserver1`
- Remote repo: `/home/admin/hftbacktest-cross-exchange`
- Local machine may only create the task/report, perform git-safe bundle sync, pull back artifacts, and record results.

files：
- `.workflow/tasks/0623T009.md`
- `.workflow/reports/0623T009-business.md`
- `examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py` if task-scoped artifact helpers are needed
- `examples/hyperliquid/test_hyperliquid_tiny_live_m2_event_driven_watcher.py` if helper behavior changes
- `local_live_analysis/hyperliquid_tiny_live_m2_aws_public_shadow_soak_0623T009/**`
- `task_plan.md`
- `progress.md`
- `findings.md`

read-only inputs：
- `.workflow/tasks/0623T007.md`
- `.workflow/reports/0623T007-business.md`
- `local_live_analysis/hyperliquid_tiny_live_m2_public_shadow_source_0623T007/**`

hard boundary：
- No real orders, no credential reads, no private/account/order endpoints, no real order endpoint, no cancel endpoint, no live client initialization, no final gate rerun, no T008 live ledger claim, no quote-distance change, no one-tick-back, no inside-spread, no cap relaxation, no taker/crossing, no default-on behavior, no M3/stable PnL/promotion.
- Public live source use is allowed only for Hyperliquid public L2/trades and Binance public market data.
- Git-safe remote sync to the task commit is allowed only to run this public-only no-submit task on `awsserver1`.
- If remote live public source cannot be collected, record the exact blocker and still produce a fail-closed preflight ledger; do not fabricate live pass evidence.

action：
- Sync `awsserver1:/home/admin/hftbacktest-cross-exchange` to the task commit using a git-safe bundle / fast-forward path.
- On `awsserver1`, run a short live public no-submit shadow soak using `m2_live_public_source_shadow_v1`.
- Capture Hyperliquid public stream summary, Binance public source freshness, fair-mid source rows, edge gate rows, current-candidate audit rows, no-submit proof, and boundary manifest.
- Generate a canary preflight / dry-run ledger from the shadow outputs that records:
  - would-submit count and candidate rows if any
  - proposed tiny canary risk envelope remains disabled / not authorized
  - required real-order ledger fields that are unavailable in no-submit mode
  - explicit `live_realized_pnl_proof=false`
  - explicit `next_real_canary_authorized=false`
- Pull the remote artifacts back to local `local_live_analysis/hyperliquid_tiny_live_m2_aws_public_shadow_soak_0623T009/`.

verify：
- Local before remote sync:
  - `python -m pytest examples/hyperliquid/test_hyperliquid_tiny_live_m2_event_driven_watcher.py -q`
  - `python -m py_compile examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
  - `python examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py --help`
- Remote:
  - `python examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py --event-driven-public-shadow-source-live --output-dir <remote-output-dir> --watcher-seconds <short-window>`
  - Validate JSON/CSV outputs on `awsserver1`.
- Local after pullback:
  - Validate pulled-back manifest JSON.
  - Check required artifact files are present and non-empty.
  - `git diff --check`

done：
- 回报 remote commit, remote output path, pulled-back local artifact path, public stream counts, whether real live public source was observed, would-submit count, fair-mid source pass/block count, edge pass/block count, endpoint boundary proof, preflight ledger result, blockers, whether next real canary is authorized, commit id, 提交信息。
- 明确说明本任务不授权真实下单、读凭据、private/account/order/cancel endpoint、remote final gate、T008 live ledger claim、quote-distance change、one-tick-back、inside-spread、cap relaxation、M3/stable PnL/default-on/promotion。
```
