# 0719T003 RAW-CANCEL-PROOF-INDEPENDENT-RECONCILIATION-REPAIR

执行线程：
- 总控 auto-loop / 业务实现线程

任务ID：
- 0719T003

标题：
- Raw cancel proof independent reconciliation and all-token identity repair

简短描述：
- Repair the two T026 QA P1 findings: producer partial-conflicting target acceptance and downstream acceptance of synchronized forged summaries.
- This task is offline-only. It does not authorize live/private/account/order/cancel/network/remote/service actions.

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0719T002`（未通过；original cross-attempt bug fixed, raw-proof independence and all-token consistency still fail-open）

规则更新提醒：
- Live remains the primary strategy data source, but execution-safety and evidence-integrity gates must pass first.
- No new live task may be created or started until this task is independently QA accepted.
- Multi-level, dynamic spread, fill feedback, inventory skew and actual quote behavior changes remain off.

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`

是否需要提交代码：
- 需要

提交要求：
- 离线 focused/full regression 通过后提交。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- Modify: `examples/hyperliquid/hyperliquid_tiny_live_m2_fill_window.py`
- Modify: `examples/hyperliquid/cross_exchange_t024_same_window_acceptance.py`
- Modify/add focused tests under `examples/hyperliquid/`
- Workflow: this task, business report, QA report, `docs/qa-acceptance-report.md`, `task_plan.md`, `progress.md`, `findings.md`

action：
- Producer matching:
  - keep attempt mandatory;
  - for every nonempty target token in a cancel row, independently resolve matching submitted references;
  - require every token to resolve uniquely to the same single reference;
  - fail closed if any token is unknown, duplicated, or conflicts with another token;
  - preserve nominal oid-only, cloid-only and consistent oid+cloid behavior.
- Acceptance independent reconstruction:
  - use raw `cancel_shutdown_proof.tracked_refs` and `cancel_shutdown_proof.cancel_results` as the primary input;
  - independently normalize attempt/oid/cloid and generate deterministic reference keys;
  - independently parse raw exchange cancel responses for authoritative success;
  - independently classify generic `already canceled, or filled` evidence;
  - independently apply all-token unique same-reference matching;
  - independently require authoritative success for every submitted reference and no unidentified/unknown/ambiguous evidence;
  - compare the independently rebuilt result with both producer summaries in fill manifest and cancel proof;
  - do not import or call the producer reconciliation helper.
- Add adversarial regressions:
  - correct oid plus unknown cloid fails producer and acceptance;
  - correct cloid plus unknown oid fails;
  - oid and cloid pointing to different known references fail;
  - both copied summaries forged while raw target is unrelated fails acceptance;
  - both copied summaries forged while raw exchange response is ambiguous-only fails acceptance;
  - raw refs/cancel rows missing or malformed fail acceptance;
  - nominal standalone multi-attempt and two-sided manager artifacts continue to pass.
- Keep all quote, strategy, risk, envelope and activation behavior unchanged.

verify：
- QA reproductions from `.workflow/reports/0719T002-qa.md` fail closed.
- Original `attempt 1 success + attempt 2 ambiguous-only` remains fail closed.
- Same-reference success followed by redundant generic remains pass.
- Oid-only, cloid-only and consistent oid+cloid success pass.
- Producer and acceptance reconstructed outputs are deterministic and equal on valid fixtures.
- Forged summaries cannot override contradictory raw proof.
- Modified Python `py_compile`, CLI `--help`, `git diff --check`.
- Focused fill attribution/fill loop/manager/watcher/acceptance regressions.
- Full `python -m pytest examples/hyperliquid -q`.

stop conditions：
- Acceptance still relies on producer `matched_reference_key` or `authoritative_success` without raw reconstruction.
- Acceptance imports/calls the producer reconciliation helper.
- Any provided target token may be ignored because another token matches.
- Any change to quote selection, strategy thresholds, live envelope, activation state or actual quote behavior.

done：
- Producer rejects all partial/conflicting target identities.
- Acceptance independently rebuilds per-reference terminal proof from raw refs and raw exchange responses.
- Both T026 QA P1 reproductions are covered and fail closed.
- Independent QA accepts this offline evidence-integrity repair.

blockers：
- No new live task may start before this task is QA accepted.

commit：
- `ba220c52a4e0cfa6f045cabcbc1ab034acd3f2f9`

提交信息：
- `Rebuild cancel proof from raw evidence`
