# 任务派发

执行线程：
- 业务线程-python/research

任务ID：
- 0815T003

标题：
- SKHYNIX-JUL30-FAMILY-A-B-EPISODE-V3-BUILD

简短描述：
- 执行 Episode v3 Ordered Research Queue 第 4 阶段：仅用 Jul30 的
  immutable public-data inputs，为全部 Candidate 构建共享 candidate
  record，并发布 Candidate-aligned Family A 与 Confirmed-time Family B
  两个 linked views。
- 本任务构建 episode/path/feature-ledger/market-outcome truth，但不做
  case retrieval、模型拟合、score、incremental test、actionability，
  不读取 Aug03/Aug04 的 future event rows，也不打开 Aug07 event rows。

状态：
- 已通过

返修轮次：
- 第一轮独立 QA 于 `2026-08-15 23:59 CST` 返回 `未通过`。
- QA report/mirror SHA256：
  `578ea754c76f442f466a1be4e451faad30bfbb3e45ba4ec24ba8c4edc7ec3d52`。
- 当前进入同一任务的第一轮有界返修；Stage 5 继续锁定。
- 第一轮有界返修业务报告已完成：
  `.workflow/reports/0815T003-business-r1.md`。
- 返修后正式 package core/full：
  - `5134f1fd31333f0c4c6a972e524b03e2a34bb4435abe6beddda4c62e34e63586`
  - `c581b65420a08ee2ec658369ecbfde240228127d1a70a243829fa1279feb77e6`
- 当前等待第二轮全新独立 QA；Stage 5 继续锁定。
- 第二轮独立 QA 返回 `未通过`；report/mirror SHA256：
  `f647346d5abe9662e2ea03bea7205decf9ebdf45130a5a048f6ea713b1b92b0a`。
- 当前进入第二轮有界返修；Stage 5 继续锁定。

第二轮 QA 唯一必修缺陷：
- P1：source-semantic admission 仅 exact 重建 `14` 个 Family B
  confirmation features，没有覆盖完整 Family A/B decision feature ledger。
- 已复现的 production-size coherent mutation：
  `family_a/pre_binance_bid_px 948.13 -> 948.14`，同步重算 artifact/core/
  manifest 后 source/archive verifier 均错误返回
  `source_semantic_verified=true`。
- 第二轮返修必须从 immutable accepted sources exact 重建全部
  `23092892` Family A 与 `15310944` Family B feature rows 的完整
  `FEATURE_FIELDS`，并将 Family A/B view load-bearing rows 纳入 exact
  source-derived projection或证明其由已验证 rows 唯一派生。
- Frozen contract 必须声明完整 projection universe；source/archive
  verifier 必须拒绝 non-confirmation feature value/provenance/
  availability/calculation-version 及 view payload 的 coherent rehash。
- 第二轮有界返修业务报告已完成：
  `.workflow/reports/0815T003-business-r2.md`。
- 最终 provisional package identities：
  - core：
    `4fc6ce074200a3c08f7a504f0ce2412f5dad8d7ad51aa44e1799f00f6dd886c9`
  - full：
    `1f3ddd61fa0bbbf3268ae6f09b303d4876e83f456d9e6ce63956b1306707dc72`
  - contract：
    `7e07af13742edadddcda32784b64e398586b7cff94c7fa9fdeb2d880b41bfb5c`
  - manifest：
    `898d5143a8a47a36b8ec0f8572621cd7a8bc542b692425feba9fcfe4efd08165`
- 当前等待第三轮全新独立 QA；Stage 5 继续锁定。
- 第三轮独立 QA 返回 `未通过`；report/mirror SHA256：
  `5498dd3c6dbd16ff9b70fd23dbdbe6493b5d16470cdc37a0b7ed9c23d27fd830`。
- 当前进入第三轮有界返修；Stage 5 继续锁定。

第三轮 QA 唯一必修缺陷：
- P1：standalone admission 只验证 aggregate 是 `dict`，没有验证完整
  `12` projection universe、frozen fields/counts、digest equality/format 和
  `mismatch_rows=0`。
- 伪造 aggregate 缺失 `11` 个 projections、count/digest 冲突且
  `mismatch_rows=999` 时，source/archive CLI 均错误返回
  `source_semantic_verified=true`。
- 第三轮返修仅允许新增唯一 expected aggregate contract、完整
  standalone evidence assertions 和 negative tests；不得改变 Episode v3
  data rows、feature/view/outcome semantics。
- 第三轮有界返修业务报告已完成：
  `.workflow/reports/0815T003-business-r3.md`。
- Provisional final package identities：
  - core：
    `95dac8f94b6d8223d51b91c95cdea74963e62b9a940526f740fd104b062b9f7b`
  - full：
    `49cab05e39a74e6fd120088d19fcbf657739e64f8dadba33b267edb4c8d9272c`
  - contract：
    `ff2b80e627415b8fc88d05a1f08c7d740c612cfdf13a26915293bc15e862b867`
  - manifest：
    `7b165ff841d3829b88362bd7ea36e6546d415ec4b9e4b2637b4489cd9ee3b30b`
- 第四轮独立 QA 返回 `未通过`；report/mirror SHA256：
  `a5215818986be563fdb346d8864b57d17a2c0b812c0bda1f556c7250c35c06ef`。
- 当前进入第四轮有界返修；Stage 5 继续锁定。

第四轮 QA 唯一必修缺陷：
- P1：standalone attestation 的 `manifest["aggregate_output_counts"]`
  只校验 required bindings，没有校验 exact key universe；额外加入
  `unexpected=0` 后，source/archive CLI 均错误返回
  `source_semantic_verified=true`。
- 第四轮返修必须从 frozen aggregate contract 的
  `manifest_count_bindings` 唯一派生 expected
  `aggregate_output_counts` keys，并要求 observed key set exact equality。
- 必须新增 source/archive `aggregate_count_extra` permanent regression。
- 不得改变 Episode v3 research rows、feature/view/outcome semantics。

第四轮返修前 durable research-data evidence：
- inventory：
  `.workflow/reports/0815T003-round4-pre-repair-research-inventory.csv`
- inventory file SHA256：
  `07521dbc2c8bfe5f41a3ab14ac5493591dad878d52053b2d74aad251b9f022df`
- research files / bytes：
  `99 / 1560514934`
- canonical inventory SHA256：
  `bb5aed2099b1a97da5b476d4b09dfe4a7bac06f0bf331f8ca864a88daf5c9232`
- 下一轮独立 QA 必须将返修后正式包和 fresh rebuild 的同口径逐文件
  path/bytes/raw SHA 与本 inventory 比较；不得依赖旧 `/tmp`。

第四轮有界返修业务结果：
- 业务报告：
  `.workflow/reports/0815T003-business-r4.md`
- source-owned aggregate evidence contract 升级为
  `skhynix_jul30_source_semantic_aggregate_evidence_v2`。
- `aggregate_output_counts` 的 `28` 个合法 keys 及 expected values 现由
  frozen projection bindings 唯一派生；missing/extra/bool/string/value
  contradiction 均 fail closed。
- `aggregate_count_extra` 已进入 current-source 与 archived-runtime
  permanent negative matrix。
- Formal / Build A / Build B：
  - files / bytes：`107 / 1561299482`
  - core：
    `77ee81b62458a0885521e42eea7db66042efa2e41e742cb943f24f2f11bb7cac`
  - full：
    `fda845f542668ffb593964dfdd2e199f37761fc4cae991d45b3a3dc2bcba983a`
  - contract：
    `2bc440f4db882c28ec95fccbb8f032da4d3a73214cad8763e25e57685a419d79`
  - manifest：
    `03f73fd162bb8736f356ed60a499ed65d28af395e0e78318854f8cc462e3d8fe`
- 三包逐文件 exact identical；全部 `99` 个 research CSV/GZ 与返修前
  durable inventory exact identical，canonical SHA 仍为
  `bb5aed2099b1a97da5b476d4b09dfe4a7bac06f0bf331f8ca864a88daf5c9232`。
- 业务证据：current related `380 passed`、archived Stage 4
  `81 passed`、gzip `96/96`；总控 focused 复核 current/archive 各
  `81 passed`，Ruff 和 gzip 均通过。
- 当前等待第五轮全新独立 QA；Stage 5 继续锁定。

第五轮独立 QA：
- 状态：`未通过`
- P0/P1/P2/P3：`0/1/0/0`
- report/mirror SHA256：
  `2978c9dd825d0b740a1416063b74f34695ee1ae379382eab61ea5f0b64b2600f`
- Round4 aggregate extra-key P1 与 durable inventory P2 均已独立关闭。
- Formal/Fresh identity、data semantics、true positive admission、
  `380 + 81` tests、zero-write、atomic、dependencies/inputs 和 hard
  boundaries 均通过。

第五轮 QA 唯一必修缺陷：
- P1：`_directory_inventory()` 与 `_verify_artifact_closure()` 使用
  `Path.is_file()/is_dir()` 构造 universe，忽略 dangling symlink。
- 在攻击包新增指向不存在 target 的 `unexpected-link` 后，current 与
  archived closure 均错误接受；current 真正完整 admission 仍返回
  `rc=0 / verified=true`，并继续声明原 `107` files/core/full identity。
- 第五轮有界返修必须在 artifact closure 和 directory inventory 入口用
  `lstat` exact type closure：
  - package root 必须是真实 directory；
  - descendants 只允许真实 regular file 或 directory；
  - symlink、FIFO、socket、device 和任何 special entry 一律 fail closed；
  - 不得跟随 symlink target 将其归类为普通 file/directory。
- 必须增加 current/archived permanent regressions，至少覆盖：
  dangling symlink、指向现有 file 的 symlink、指向 directory 的
  symlink和一个 unknown special entry。
- 不得改变 99 个 research CSV/GZ、Episode rows、feature/view/outcome
  semantics 或 accepted inputs。
- 第五轮有界返修业务报告：
  `.workflow/reports/0815T003-business-r5.md`
- 单一 source-owned `_exact_tree_entries()` 现对 root/descendants 使用
  `lstat`，仅允许真实 directory/regular file；所有 symlink 与 special
  entry 在 manifest/identity 信任前 fail closed。
- `_directory_inventory()`、`_artifact_records()`、
  `_verify_artifact_closure()` 和 `_fsync_tree()` 共用同一 exact universe。
- current/archive permanent regressions 覆盖 dangling/file/directory
  symlink、FIFO、root symlink 与正常 tree。
- Formal / Build A / Build B：
  - files / artifacts / bytes：`107 / 106 / 1561307420`
  - core：
    `78be6559c7ac5aaec4d5411b042f7ddcce522473f60f987237bcaa9d2dd5e157`
  - full：
    `669fb7d12f25cfa7828aec0fb1546398b1def754952de2290cd19784a477a433`
  - contract：
    `b80b9bae2d6cf18cb6e7be4f133467138527fec20eef7054ab38b7f8c70cefde`
  - manifest：
    `2c802336c1446eaf50eaf5ef546b115046abe3a1b69fdd1fcaea31d22a8e64a6`
- 三包逐文件 exact；全部 `99` 个 research CSV/GZ 与 durable inventory
  exact，canonical SHA 保持 `bb5aed...9232`。
- 业务证据：current/archive Stage 4 各 `97 passed`、current unique
  related `396 passed`、production symlink/special attacks `10/10`
  fail closed、双侧 true positive admission、Ruff/compile/gzip 通过。
- 总控 focused 复核 current/archive 各 `97 passed`，identity、research
  inventory、Ruff 与 diff check 通过。
- 当前等待第六轮全新独立 QA；Stage 5 继续锁定。

第六轮独立 QA 最终结果：
- 状态：`已通过`
- 日期：`2026-08-16`
- P0/P1/P2/P3：`0/0/0/0`
- report/mirror SHA256：
  `0b85ae527f7e91f40f3e4401969596ff6462964a7e05cb8d0de73e763ad3e592`
- direct tree attacks：current/archive `36/36` fail closed。
- production package tree attacks：current/archive `12/12` fail closed。
- Formal/Fresh：
  `107 files / 106 artifacts / 1561307420 bytes`，完整 tree 和四项
  identities exact。
- durable/Formal/Fresh `99` research files逐文件 exact。
- aggregate v2 `12 projections / 28 exact keys`，source/archive
  negative calls `98/98` fail closed。
- current related `396 passed`，archived Stage 4 `97 passed`。
- `0815T003` 于总控记录中关闭为 `已通过`。
- Ordered Research Queue Stage 5 已满足技术解锁前提，但根据用户
  `2026-08-17` 的指令暂停派发，先完成 Stage 4 全历史设计/执行 review。

第一轮 QA 必修缺陷：
- P1：Family B confirmation-derived features 必须绑定真正 detector
  confirmation timeline state，不能借用更早的 Binance BBO-asof
  provenance。
- P1：`confirmed_burst_*_through_decision` 必须复用 frozen detector burst
  membership/prefix，不能纳入 burst 已因 fixed-origin gap/opposite trade
  终止后的成交。
- P1：post-trigger target-trade first-event interval lower bound 必须从
  Candidate 左截断，不能落在 `t_candidate_ns` 之前。
- P1：source 和 archived admission 必须从 accepted Jul30 source truth
  重建并核验 classification/quality evidence、sparse range、event-count
  identity 和 outcome semantics，使 coherent rehash 攻击 fail closed。
- P2：vulnerable outcome baseline 必须使用 strict-pre source ordering，
  并新增 same-receipt-timestamp hostile regression。

执行顺序：
- 当前唯一任务

前置任务：
- `0814T001` 已通过；Stage 1 core/full：
  - `9c2756f966a1a05816f9e6a91d7361d76cfea10fc3c587944ca2f0c1d2eb7c96`
  - `c540cc056313716b3bdd2b9c0fe076cda15a7f152b399ae6a1283b3aa8aa6590`
- `0815T001` 已通过；Stage 2 core/full：
  - `7b3d06c3225f77c9929cdd3fa40d69c0866d83dbb75dd584dcb7db6db22ad3f8`
  - `bdade16c53aed7bba54fdb9408ba3a8a03036d183720f589bda2a762448a3833`
- `0815T002` 第三轮独立 QA 已通过；Stage 3 core/full：
  - `4939d1c1addce493edb2f368297d56b37edd0b123de01497dcdee6e77637eb9b`
  - `ff8e3434672226371051151cea838503877dca79ac7860cf179256362d75e404`
- Stage 3 QA report/mirror SHA256：
  `96d379061b2dbae0001e56050260fd60f38394fca1ab4ccdf60b1dd2f243b4d9`。

规则更新提醒：
- 请先阅读 workflow、Stage 1/2/3 accepted QA、本任务和主实施方案。

按线程规则执行：
- `AGENTS.md`
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `docs/skhynix_trigger_aligned_episode_research_implementation_plan.md`
- `.workflow/reports/0814T001-qa.md`
- `.workflow/reports/0815T001-qa-round8.md`
- `.workflow/reports/0815T002-qa-round3.md`

是否需要提交代码：
- 不需要

提交要求：
- 保留任务范围改动供独立 QA 检查。
- 回报使用 `commit：无`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 独立从冻结 Jul30 inputs 重建完整 Episode v3 package。
- 独立检查 Family A 全 candidate 覆盖、Family B confirmed linkage、
  anchor causal ordering、feature observed-at invariant、interval censoring、
  segment/epoch boundaries、source lineage、determinism 和 hard boundary。
- 对 confirmed-only Family A、删除 rejected row、synthetic confirm time、
  future-feature leakage、point-coerced interval、cross-boundary path、
  outcome-zero fill、unknown artifact、dependency drift 和 partial
  publication做 production-size hostile tests。

files：
- `examples/hyperliquid/cross_exchange_trigger_aligned_episodes.py`
- `examples/hyperliquid/test_cross_exchange_trigger_aligned_episodes.py`
- 可新增一个范围内的 Stage 4 admission CLI/test：
  - `examples/hyperliquid/cross_exchange_jul30_episode_v3_admission.py`
  - `examples/hyperliquid/test_cross_exchange_jul30_episode_v3_admission.py`
- 可复用但不得修改已验收语义：
  - `examples/hyperliquid/cross_exchange_trigger_aligned_episode_contract.py`
  - `examples/hyperliquid/cross_exchange_liquidity_response_trigger.py`
  - `examples/hyperliquid/cross_exchange_candidate_episode_merging.py`
- 新 package：
  `local_live_analysis/skhynix_trigger_aligned_episode_research_v1_stage04_jul30_episode_v3/`
- `.workflow/reports/0815T003-business.md`
- `.workflow/tasks/0815T003.md`

accepted immutable dependencies：
- `local_live_analysis/skhynix_trigger_aligned_episode_research_v1/`
- `local_live_analysis/skhynix_trigger_aligned_episode_research_v1_stage02_density/`
- `local_live_analysis/skhynix_trigger_aligned_episode_research_v1_stage03_detector_parity/`

read-only Jul30 inputs：
- `/Users/liu/Documents/hftbacktest/local_live_analysis/cross_exchange_collection_campaign_0730T011_skhynix_4h_8x30m/`
- `/Users/liu/Documents/hftbacktest/local_live_analysis/skhynix_cross_exchange_research_0730T013/`
- Stage 3 `candidate_audit_projection.csv.gz` 的 Jul30 rows。
- Stage 2 `candidate_episode_membership.csv.gz` 的 Jul30 rows。

forbidden event-row inputs：
- 不解析
  `/Users/liu/Documents/hftbacktest/local_live_analysis/skhynix_liquidity_response_0730T017/episodes/*.csv.gz`；
  这些 legacy response rows 不能成为 Episode v3 truth。
- 不读取 Aug03/Aug04 raw、R0、R1、basis、legacy episode 的 future
  event/response/outcome rows。
- 不读取任何 Aug07 raw/R0/R1/basis event rows；只允许复核 accepted
  Stage 1 ledger/package identity。

action：
- 先验证 Stage 1/2/3 fixed-root package identities、canonical manifests、
  full inventories、runtime bindings 和 source immutability；任何 drift
  在创建 output 前 fail closed。
- 冻结独立 Stage 4 contract，至少包含：
  - `episode_v3_jul30_v1` schema/version；
  - Family A/B population 与 landmark；
  - anchor、sparse path、fixed grid、event-count、feature ledger、
    response/outcome 和 interval-censoring schemas；
  - direction normalization、source event identity、source book version、
    source age/no-new-information、epoch/degraded semantics；
  - exact Jul30 segment order、evidence labels、horizon/grid/event counts；
  - public-market-only naming和 own-order/fill/PnL forbidden fields；
  - canonical serialization、artifact allowlist、atomic publication 和
    dependency/source binding。
- 建立一个 append-only shared candidate anchor record；Family A 与
  Family B 必须是同一 candidate identity 的 linked views，不能成为两个
  不相关的数据集。
- Family A 必须精确覆盖 Jul30 全部 `268522` candidates：
  - confirmed 与所有 rejected/unconfirmed rows 都存在；
  - 每个 candidate 恰好一个 anchor、Family A view 和
    market-outcome/censor row；
  - `t_candidate_ns=shock_ts_ns`；
  - rejected/unconfirmed 的 `t_confirm_ns` 保持 null；
  - 保存原始 `classification`、`rejection_reason`、quality/censor fields。
- Family B 必须精确覆盖同一 IDs 中 `primary_episode=true` 的
  `141768` confirmed candidates：
  - 每个 confirmed candidate 恰好一个 linked Family B view；
  - `t_confirm_ns=decision_ts_ns` 且严格晚于 Candidate；
  - 不允许不透明 quality filter；任何 subset eligibility 都必须显式字段化。
- Anchor table 至少实现方案 §7.3 字段，并闭合：
  - campaign/session/segment/candidate/episode/flow/cluster IDs；
  - burst start、Candidate、Confirm、confirmation lag；
  - Family A/B availability、confirmed、classification/rejection；
  - censor time/reason、quality flags、source manifest SHA。
- 从结构化 Jul30 public event sources 构建信息完整的 event-time truth：
  - exact sparse event store；
  - Candidate 与 Confirmed 两种 landmark 的 fixed grid；
  - Candidate 与 Confirmed 后 BBO/trade/fast-L2 的 `1/2/3/5/10/20`
    event-count views；
  - Binance/Hyperliquid BBO、trades、fast-L2、cross-spread 和可得 depth
    response字段；
  - 每个 source event 绑定 canonical source event ID、absolute receipt
    timestamp、source timestamp/book version、connection epoch、degraded
    mask和 raw/source manifest lineage。
- 由于 Jul30 trigger 近连续且 outcome windows 高度重叠，exact sparse
  events 应优先使用按 segment/channel 分区的共享 immutable event store
  与 membership/range index；不得为了节省空间删除 rejected candidates，
  也不得把同一 source event 为每个重叠 episode 物理复制。
- Fixed grid 精确使用
  `-2000,-1000,-500,-250,-100,-50,-25,-10,0,10,25,50,100,250,500,1000,2000ms`。
  每个点携带 value/source identity/observed_at/strict-as-of timestamp/
  effective relative time/source age/no-new-information/epoch/degraded/
  inside-segment 状态。Forward-filled repeated state 必须标为
  no-new-information，不能计为新事件。
- 构建 Family A Candidate-time 和 Family B Confirmed-time decision
  feature ledgers。每个非空 feature 必须逐行携带：
  `value, observed_at_ns, source_event_id, source_book_version,
  calculation_version, availability_reason`，并在写入前断言：
  - Family A `observed_at_ns <= t_candidate_ns`
  - Family B `observed_at_ns <= t_confirm_ns`
- Feature contract 至少覆盖方案 §8 当前 Jul30 可得字段；不可得字段显式
  标记，不得填零。必须绑定：
  - `trade_quantity_source=binance_trade_q`
  - `rpi_adjustment_status=unavailable_historical_feed`
  - `depth_stream=depth@0ms`
  - `underlying_market_state=unknown_calendar_state` 作为 formal state，
    因 Stage 1 没有 authoritative holiday calendar；可另报 nominal
    clock state，但不能用未来价格推断。
- 构建 §9 的 public-market response/outcomes，不得包含或暗示 own fill：
  - 三个 first-event outcomes 使用
    `(last_non_event_observed_at, first_event_observed_at]` interval；
  - 未在 2000ms/segment/epoch/quality horizon 内观察到的分别记录
    right/segment/epoch/quality censor，不得转换为 event=false 或 `0`；
  - gap survival `100/250/500/1000/2000ms`；
  - target midpoint markout `250/500/1000/2000ms`；
  - 0-2000ms MAE/MFE；
  - source/target leg gap-closure contribution；
  - adverse-before-confirmed；
  - public quote-risk proxy 只用
    `public_trade_reaches_quote`、`public_bbo_moves_through_quote`、
    `public_quote_survives_horizon`、`public_adverse_exposure`。
- Direction-normalized signs、`d_bh/d_hb/risk_gap` identity、strict-as-of
  joins、segment/epoch boundaries、source ages 和 degraded masks必须来自
  accepted structured readers/parsers；不得 ad hoc 按字符串猜测。
- Stage 2 的 Jul30 cluster/flow/overlap membership 必须 exact join 到
  每个 candidate，预期 `39928` clusters、`10536` flows、`9`
  2000ms overlap blocks；Stage 4 不重新选择 merging 参数。
- 输出按 segment 分区、deterministic gzip (`mtime=0`, empty filename)、
  canonical CSV/JSON text、content-addressed manifest和 exact artifact
  allowlist。正式 package 必须 staging build + fsync + atomic rename；
  中断不能暴露 partial output。
- 归档并绑定所有 Stage 4 runtime source 和 focused tests。Source 与
  archived verifier 都必须提供 `--verify-only`，默认 Python zero-write。
- 完成 Formal 与两个隔离完整构建；三者全部 relative paths/raw bytes/
  file SHA/core/full inventory byte-identical。

verify：
- `python examples/hyperliquid/cross_exchange_trigger_aligned_episodes.py --help`
- Stage 4 admission CLI（如单独实现）`--help` 和 `--verify-only`。
- `python -B -m pytest -q
  examples/hyperliquid/test_cross_exchange_trigger_aligned_episodes.py
  examples/hyperliquid/test_cross_exchange_jul30_episode_v3_admission.py`
- Existing Stage 1/2/3 focused regressions needed to detect dependency、
  detector、membership 或 contract drift。
- 全量 Jul30 cardinality/join checks：
  - Family A/anchor/outcome `268522`
  - Family B `141768`
  - rejected Family A `126754`
  - missing/duplicate/extra ID `0`
  - synthetic rejected confirm timestamp `0`
  - future feature observation `0`
  - cross-segment/cross-epoch path `0`
  - outcome silently zeroed `0`
- Hostile tests至少覆盖：
  confirmed-only Family A、删除 rejected row、synthetic confirm timestamp、
  confirmation leak into Family A、feature `observed_at` after landmark、
  interval point coercion、cross-boundary event、source-age/degraded deletion、
  forward-fill counted as event、direction/risk-gap swap、unavailable-to-zero、
  forbidden own-order/fill/PnL field、Aug03/Aug04/Aug07 event path、
  dependency/source drift、unknown artifact、noncanonical JSON/CSV、
  archived verifier/test coherent rehash、default-bytecode write 和 partial
  publication。
- Ruff、外置 pycache compileall、全部 gzip test、scoped
  `git diff --check`、verify-only zero-write。
- Accepted Stage 1/2/3、Jul30 raw/R0/R1 和 source inputs before/after
  inventories byte-identical。

done：
- 业务报告状态为 `待验收`，写
  `.workflow/reports/0815T003-business.md`。
- 报告列出：
  - contract/package core/full/manifest SHA；
  - artifact/file/byte counts；
  - Family A/B/confirmed/rejected/censor cardinalities；
  - per-segment rows、event-store/index/grid/ledger/outcome counts；
  - interval/right/quality/segment/epoch censor counts；
  - feature observed-at、anchor ordering、boundary mismatch counts；
  - source/dependency identities和 before/after immutability；
  - Formal/A/B determinism、测试和 hostile evidence。
- 状态只能由业务线程置为 `待验收`。独立 QA `已通过` 前，不得开始
  Ordered Research Queue Stage 5。

hard boundary：
- 不读取 Jul30 legacy `episodes/*.csv.gz` response/outcome rows。
- 不读取 Aug03/Aug04 future event/response/outcome rows。
- 不读取 Aug07 raw/R0/R1/basis event rows。
- 不做 case retrieval、distance、baseline、model、score、calibration、
  incremental test 或 actionability。
- 不访问网络、AWS、SSH、private/account/order/cancel endpoint。
- 不启动采集、不下单、不修改 live/active config。
- 不修改 accepted Stage 1/2/3 packages、历史 v1/v2 artifacts 或只读输入。
