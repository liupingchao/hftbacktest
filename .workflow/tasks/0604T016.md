```md
执行线程：
- 业务线程-python

任务ID：
- 0604T016

标题：
- Fix live shutdown cancel acknowledgement proof semantics

简短描述：
- 修复 `0604T015` 诊断出的 live shutdown cancel wait-result ambiguity：把 `wait_order_response()` 原始返回值分类为独立的 order-response 维度，同时新增独立 final terminal-state proof 维度，修正 shutdown 日志/测试，防止把 `order_response_received` 滑回 `terminal_confirmed`。本任务只修复 shutdown helper 语义和 focused tests，不改 py binding / Rust live bot / connector。

状态：
- 未通过

执行顺序：
- 当前唯一任务

前置任务：
- `0604T012` QA 已通过，已证明原 shutdown 是 fire-and-forget。
- `0604T013` 业务执行已完成，已把 shutdown 推进到 bounded wait。
- `0604T015` 业务执行已完成并待 QA，已诊断：`wait_result=0` 存在 timeout / batch 折叠二义性，`wait_result=3` 只能证明 order response，不能证明 cancel ack / canceled state。

规则更新提醒：
- 请先阅读规则文档后再执行本任务。
- 本任务是修复任务，但范围只限 `live_tick_mm.py` shutdown helper 结果语义、日志和 focused tests；不得扩大到底层 API/connector/live 行为重构。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `docs/thread-playbook.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交任务文件、业务报告、`live_tick_mm.py` shutdown helper 修改和 focused tests。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0604T016.md`
- `.workflow/reports/0604T016-business.md`
- `examples/binance_tick_mm/live_tick_mm.py`
- `examples/binance_tick_mm/test_backtest_tick_mm.py` 或更合适的 focused shutdown test 文件。
- 必要 workflow tracking 文件仅限本任务状态/结论更新。

forbidden：
- 不修改 `py-hftbacktest/src/live.rs` / `py-hftbacktest/src/backtest.rs`。
- 不修改 `hftbacktest/src/live/bot.rs` / `hftbacktest/src/backtest/mod.rs`。
- 不修改 connector、Rust `close()`、生产配置、normal trading loop cancel 语义、audit schema、strategy quote/submit/cancel 行为。
- 不启动 live / tiny-live / default-on。
- 不做 REST private/order endpoint 集成，除非当前本地对象已有可测试、安全、无网络的订单状态接口；默认只使用本地 order snapshot / working-order status proof。
- 不做参数搜索、回放实验扩展、promotion。

required semantic model：
- `order_response_received` 和 `terminal_confirmed` 必须是两个独立维度。
- `order_response_received`：
  - 只描述 `wait_order_response()` 是否明确返回 order response。
  - `wait_result == 3` 可设置 `order_response_received=True`。
  - `wait_result == 0` 必须设置为 `False` 或 `unknown`，不得当作 confirmed ack。
  - 它不表达订单已撤、已终止或不在交易所 open orders。
- `terminal_confirmed`：
  - 只能由独立 final order-state proof 设置。
  - 可接受 proof 示例：
    - wait 后本地 `hbt.orders(asset_no)` / order snapshot 中该订单已不存在于 active working set。
    - 该订单 status 明确是 terminal/inactive/canceled/filled/expired/rejected。
    - 后续单独任务若引入 REST open-orders reconciliation，可由交易所 open orders 证明该 order_id 不存在；本任务默认不做 REST 集成。
  - 不允许仅由 `wait_result == 3`、`order_response_received=True`、`wait_requested=True`、`error==""` 推导。
- No code path may set `terminal_confirmed=True` solely from `wait_result == 3` or `order_response_received=True`; terminal confirmation must require independent final order-state proof.

action：
- 在 `ShutdownCancelResult` 中新增或等价表达以下字段：
  - `wait_result_raw`
  - `order_response_received`
  - `wait_outcome`，建议取值至少包含 `order_response_received`、`ok_unknown_or_timeout`、`wait_error`、`not_requested`
  - `terminal_confirmed`
  - `terminal_confirmation_source`
  - `final_order_status` 或等价 final-state detail
- 固定枚举值，不允许自由拼写或临时扩展：
  - `wait_outcome` 只能使用：
    - `order_response_received`
    - `ok_unknown_or_timeout`
    - `wait_error`
    - `not_requested`
  - `terminal_confirmation_source` 只能使用：
    - `local_orders`
    - `rest_open_orders`
    - `none`
  - 本任务默认不得产出 `rest_open_orders`，除非任务执行中证明已有安全、无网络依赖、可本地测试的接口；否则 REST reconciliation 必须留给后续任务。
- 保留 `cancel_sent` / `wait_requested` / `error`，但不要让这些字段承载 ack-confirmed 语义。
- 修改 `cancel_working_orders_for_shutdown(...)`：
  - `wait_result == 3`：只设置 `order_response_received=True` / `wait_outcome=order_response_received`。
  - `wait_result == 0`：设置 `order_response_received=False` 或 `unknown` / `wait_outcome=ok_unknown_or_timeout`。
  - wait exception：设置 `wait_outcome=wait_error` 并保留现有错误容错，不阻止后续订单处理。
  - wait 后执行独立 terminal-state proof 检查；如果无法证明 terminal，则 `terminal_confirmed=False`。
  - 独立 proof 的实现应保持本地、可测试、bounded，不引入网络依赖。
- 修改 shutdown summary log：
  - 不再使用误导性的 `ack_waits` 作为 confirmed ack 口径。
  - 推荐输出：`attempts`、`sent`、`wait_requests`、`order_responses`、`terminal_confirmed`、`unknown_or_timeout`、`failed`。
  - 如保留旧字段名，必须明确不是 confirmed ack；优先改名。
- 修改/补充 focused tests，覆盖 `0604T015` case matrix 和本任务新增独立维度风险。

required tests：
- `wait_result=3 + local order still active`：
  - `order_response_received=True`
  - `terminal_confirmed=False`
  - 证明不能从 order response 推导 terminal proof。
- `wait_result=0 + local order terminal/absent`：
  - `order_response_received=False` 或 `unknown`
  - `terminal_confirmed=True`
  - 证明 terminal proof 可独立于 order response。
- `wait_result=3 + local order terminal/absent`：
  - `order_response_received=True`
  - `terminal_confirmed=True`
  - 证明两个维度可以同时为 true，但来源不同。
- `wait_result=0 + no final status proof`：
  - `order_response_received=False` 或 `unknown`
  - `terminal_confirmed=False`
- `wait_result=3 + no final status proof`：
  - `order_response_received=True`
  - `terminal_confirmed=False`
- wait exception：
  - `order_response_received=False`
  - `terminal_confirmed` 只能由独立 final-state proof 决定。
  - 后续 orders 仍继续尝试 shutdown cancel。
- enum regression：
  - 测试必须断言 `wait_outcome` 只出现固定枚举值。
  - 测试必须断言 `terminal_confirmation_source` 只出现 `local_orders` / `rest_open_orders` / `none`。
  - 如本任务未做 REST reconciliation，测试或报告必须证明没有 result 使用 `terminal_confirmation_source=rest_open_orders`。
- summary/log counter：
  - `wait_requests` 与 `order_responses` / `terminal_confirmed` 分开统计。
  - 不存在把 `order_response_received` 计入 `terminal_confirmed` 的路径。
- regression：
  - 测试应显式失败任何仅凭 `wait_result == 3` 设置 `terminal_confirmed=True` 的实现。

acceptance criteria：
- `order_response_received` 和 `terminal_confirmed` 是独立字段/维度，报告和日志中不混用。
- No code path may set `terminal_confirmed=True` solely from `wait_result == 3` or `order_response_received=True`; terminal confirmation must require independent final order-state proof.
- `wait_result=0` 不再被默认为成功 ack；必须分类为 unknown/timeout-like，除非另有独立 terminal proof。
- `wait_result=3` 不再被称为 cancel ack confirmed；只能称为 order response received。
- `wait_outcome` 和 `terminal_confirmation_source` 使用固定枚举；本任务不允许新增未列出的状态名。
- 若未实现 REST final open-orders reconciliation，则不得在结果或日志中使用 `terminal_confirmation_source=rest_open_orders`。
- shutdown summary 不再用 `ack_waits` 表示 confirmed ack；必须分开统计 wait requests、order responses、terminal confirmations 和 unknown/timeout。
- focused tests 覆盖 required tests 全部 case。
- 不修改 py binding、Rust live bot/backtest、connector、production config、audit schema、normal loop cancel semantics。
- 不启动 live / tiny-live / default-on，不做 promotion。

verify：
- `/home/molly/anaconda3/envs/hftbacktest/bin/python -m pytest examples/binance_tick_mm/test_backtest_tick_mm.py -k "live_shutdown or shutdown_cancel or wait_order_response or terminal_confirmed or order_response_received"`
- `/home/molly/anaconda3/envs/hftbacktest/bin/python -m pytest examples/binance_tick_mm/test_*.py`
- `git diff --check`
- 如 base `python -m pytest examples/binance_tick_mm` 因已知 numba/tardis collection 问题失败，报告具体错误，并以 conda env 的 `test_*.py` 结果作为本任务 package-level verification。

done：
- 业务报告写入 `.workflow/reports/0604T016-business.md`。
- 回报必须说明语义模型、改动点、required tests 每个 case 的实际结果、日志/summary 口径、未做 REST final open-orders check 的边界、验证命令、commit id 和 commit message。
- 业务报告应以 `待验收` 收口。
```
