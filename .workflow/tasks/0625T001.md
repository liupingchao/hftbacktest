```md
执行线程：
- 业务线程-research

任务ID：
- 0625T001

标题：
- MVP public alpha / edge decomposition

简短描述：
- 使用现有 QA-accepted Binance-lead / Hyperliquid-lag public artifacts，分解当前 fair-mid / edge path 为什么在 `0624T003` 中 `edge_gate_pass_count=0`。
- 本任务只做离线、public-only、no-submit 研究和必要的分析 runner，不调整 live 策略、不下单、不读凭据、不调用 private/order endpoint。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0624T003` QA 已通过。

规则更新提醒：
- 请先阅读 `.workflow/workflow-kit/workflow-manual.md`、`.workflow/workflow-kit/task-dispatch-template.md`、`.workflow/workflow-kit/thread-report-template.md`、`.workflow/workflow-kit/qa-acceptance-template.md`、`task_plan.md`、`progress.md`、`findings.md`、`docs/cross_exchange_maker_mvp_plan.md` 后再执行本任务。

按线程规则执行：
- `AGENTS.md`

是否需要提交代码：
- 需要

提交要求：
- 自测和离线 artifact 生成通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0625T001.md`
- `.workflow/reports/0625T001-business.md`
- `examples/hyperliquid/alpha_edge_decomposition.py`
- `examples/hyperliquid/test_alpha_edge_decomposition.py`
- `local_live_analysis/cross_exchange_mvp_alpha_edge_decomposition_0625T001/**`
- `task_plan.md`
- `progress.md`
- `findings.md`

read-only inputs：
- `local_live_analysis/hyperliquid_tiny_live_m2_aws_repaired_public_shadow_funnel_0624T003_20260624T063826Z/**`
- `local_live_analysis/hyperliquid_tiny_live_m2_aws_candidate_funnel_0623T010_20260623T064432Z/**`
- `local_live_analysis/cross_exchange_lead_lag_join_0601T002/**`
- `local_live_analysis/cross_exchange_lead_lag_analysis_0601T003/**`
- `local_live_analysis/binance_led_hyperliquid_pricing_signal_0601T005/**`
- `local_live_analysis/canonical_event_mode_evidence_0604T003/**`
- `local_live_analysis/canonical_maker_executability_0608T002/**`

hard boundary：
- Offline/public-only/no-submit.
- No network collection, no live orders, no credential reads, no private/account/order/cancel endpoints, no live client initialization, no remote refresh, no remote final gate, no T008 live ledger claim.
- No quote-distance change, no one-tick-back, no inside-spread, no cap relaxation, no taker/crossing, no default-on behavior, no M3/stable PnL/promotion.
- Do not lower the current `7` tick edge requirement and call the resulting rows strategy-ready. Buffer sensitivity is diagnostic only.
- Do not use future Hyperliquid outcomes as decision inputs. Future rows may only be labels with explicit nominal and effective horizon.
- If existing artifacts do not contain enough decision-time and future-label coverage, return `needs_more_public_samples`; do not fabricate joins or infer live profitability.

action：
- Inventory accepted input artifacts and record which have:
  - Binance top5 / mid / microprice state
  - Hyperliquid top5 / mid / microprice state
  - local receive timestamps
  - exchange timestamps
  - candidate side / quote
  - anti-drift result
  - fair-mid / lead_move_ticks
  - future Hyperliquid labels
- Implement a deterministic offline decomposition runner that can consume the accepted artifacts without changing the production watcher.
- Preserve local-observation-time as-of rules and report future-join, missing-join, stale-source and effective-horizon counts.
- Analyze, where supported by data:
  - Binance lead feature direction versus future Hyperliquid mid/microprice move at `100/250/500/1000ms`
  - current `lead_move_ticks` sign and magnitude error
  - basis and Hyperliquid venue-state conditioning
  - anti-drift pass/block interaction with future markout
  - fair-mid source age and stale-source impact
  - candidate-side quote edge before and after fee/adverse-selection buffers
  - diagnostic edge threshold sensitivity at `0/1/2/3/5/7` ticks
- Separate these root-cause classes:
  - wrong direction
  - wrong horizon/timing
  - magnitude over/under projection
  - stale/missing source
  - anti-drift removing favorable candidates
  - anti-drift correctly removing adverse candidates
  - quote policy / buffer dominates otherwise positive signal
  - insufficient sample coverage
- Produce at minimum:
  - `alpha_edge_decomposition_manifest.json`
  - `source_coverage_matrix.csv`
  - `signal_response_by_horizon.csv`
  - `lead_move_calibration.csv`
  - `anti_drift_markout_interaction.csv`
  - `edge_buffer_sensitivity.csv`
  - `root_cause_summary.csv`
  - `recommendation.md`
- Final recommendation must be exactly one of:
  - `signal_contract_candidate`
  - `needs_more_public_samples`
  - `reject_current_signal_shape`

verify：
- `python -m pytest examples/hyperliquid/test_alpha_edge_decomposition.py -q`
- `python -m py_compile examples/hyperliquid/alpha_edge_decomposition.py`
- `python examples/hyperliquid/alpha_edge_decomposition.py --help`
- Run the decomposition over the accepted local artifact set.
- Validate JSON output, CSV schemas/counts, no future joins, non-empty required artifacts, deterministic rerun output and boundary manifest.
- `git diff --check`

done：
- 回报每个输入 artifact 的可用字段和样本量、primary/excluded rows、future/missing/stale join counts、各 horizon 的方向和幅度结果、anti-drift pass/block markout、edge threshold sensitivity、当前 `edge_gate_pass_count=0` 的主因排序。
- 明确说明证据是否足以形成 `signal_contract_candidate`；若不足，列出 `0625T002` 必须采集的字段、窗口数量、最小样本量和 regime 覆盖。
- 明确说明本任务没有修改 live 策略行为，没有下单、读凭据、调用 private/order endpoint，也没有授权 quote-distance/cap/post-only 放松、canary、M3、stable PnL、default-on 或 promotion。
- 回报 commit id 和提交信息。
```
