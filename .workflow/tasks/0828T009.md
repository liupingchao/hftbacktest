# 任务派发

执行线程：
- SKHYNIX Liquidity Break Onset A1 Target Support Audit 业务线程

任务ID：
- 0828T009

标题：
- 冻结并预检 LIQUIDITY_BREAK_ONSET_V1 A1 Target Support Audit

简短描述：
- 冻结 A1 competing-risk target materialization、timeliness、cause
  support、censoring、common-risk-set gates；执行 upstream authorization
  preflight。若 A0 未通过，必须在读取 future target 前阻塞。

状态：
- 阻塞

执行顺序：
- 0828T008 已完成 QA 后的当前 formal task

前置任务：
- 0828T008

规则更新提醒：
- 已阅读 AGENTS.md、workflow-kit manual、0828T007 A0 contract、
  0828T008 execution/QA 和 methodology kernel。

按线程规则执行：
- AGENTS.md
- .workflow/workflow-kit/workflow-manual.md

是否需要提交代码：
- 需要提交 plan、task 和 blocked preflight report

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

files：
- docs/skhynix_binance_liquidity_break_onset_v1_a1_target_support_audit_plan_20260828.md
- .workflow/tasks/0828T009.md
- .workflow/reports/0828T009-business.md

action：
- 冻结 A1 population、targets、event ordering、censoring 和 timeliness。
- 冻结 target-support、ambiguity 和 common-risk-set gates。
- 检查 A0 canonical classification 和 `A1_authorized`。
- 若未授权，停止在 preflight，不读取 future midpoint/best-price。

verify：
- A0 classification 为 `A0_anchor_near_continuous`。
- `A1_authorized=false`。
- outcome ledger future fields `[]`。
- targets materialized `false`；H0/H1 fitted `false`。
- Markdown fence parity 和 `git diff --check`。

done：
- A1 plan 已冻结。
- A1 preflight 已执行。
- A1 target materialization 未执行。

blockers：
- 0828T008 A0 未通过 passing classification。
- 当前版本不得打开 A1 target access。

明确禁止：
- 不读取 future midpoint/best-price transition。
- 不物化 adverse/recovery labels。
- 不修改 A0 detector 或 thresholds。
- 不拟合 H0/H1。
- 不访问 private API、订单或新采集。
