# 任务派发

执行线程：
- SKHYNIX Safe Reentry After Flow Excursion A0 Contract 业务线程

任务ID：
- 0828T010

标题：
- 起草并冻结 SAFE_REENTRY_AFTER_FLOW_EXCURSION_V1 A0 方案

简短描述：
- 将失败的高频 pressure crossing 降级为 micro-observation，引入
  outcome-blind novelty、causal persistence 和 bilateral refractory，
  冻结 flow excursion 与 safe-reentry anchor 的 zero-target A0
  contract。

状态：
- 待验收

执行顺序：
- 当前唯一 formal task

前置任务：
- 0828T009

规则更新提醒：
- 已阅读 AGENTS.md、workflow-kit manual、methodology kernel、
  LIQUIDITY_BREAK_ONSET_V1 A0/A1 contracts、0828T008 execution/QA 和
  0828T009 blocked preflight。

按线程规则执行：
- AGENTS.md
- .workflow/workflow-kit/workflow-manual.md

是否需要提交代码：
- 需要提交 task、A0 plan/contract 和 business report

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

files：
- docs/skhynix_binance_safe_reentry_after_flow_excursion_v1_a0_plan_20260828.md
- .workflow/tasks/0828T010.md
- .workflow/reports/0828T010-business.md

action：
- 注册新 hypothesis identifier。
- 冻结 V1 micro-pressure projection 的继承边界。
- 冻结 novelty、persistence 和 refractory 的因果定义。
- 冻结 excursion state machine 和 safe-reentry alignment。
- 冻结当前 spread/depth/OBI opportunity filter。
- 冻结 controls、A0 gates、downstream maker target stub 和 H0/H1
  information boundary。
- 明确 A0 不执行 detector、不读取 outcome。

verify：
- hypothesis/version boundary 完整。
- 状态机不存在 backdating 或 future-confirmed onset。
- opposite pressure 只更新 episode，不产生新 episode。
- safe-reentry anchor 在 refractory 完成时因果发出。
- outcome-access boundary、support gates 和 forbidden rescue 完整。
- Markdown fence parity、constant presence 和 `git diff --check`。

done：
- A0 design contract 已起草并冻结，待 QA。
- 未实现或执行 detector。
- 未读取 future price、contact、fill、markout 或 PnL。

明确禁止：
- 不把当前方案称为 LIQUIDITY_BREAK_ONSET_V1 robustness。
- 不通过调高 z threshold 降低 anchor rate。
- 不用 future return/fill/markout 选择 novelty、persistence 或
  refractory。
- 不访问 private API、订单或新采集。
