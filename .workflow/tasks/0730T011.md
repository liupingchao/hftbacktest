# 任务派发

执行线程：
- 业务线程-python/public-data-infra

任务ID：
- 0730T011

标题：
- AUXILIARY-RECONNECT-DEGRADED-INTERVAL-AND-POSTPROCESS-REPAIR

简短描述：
- 修复 T010 暴露的 Hyperliquid 辅助研究轨道重连门禁，把核心 L2
  零重连与辅助 snapshot 轨道可恢复重连分级处理。
- 在不重新采集、不修改 raw 字节的前提下，对现有 4H SKHYNIX 数据
  重新执行严格后处理，成功后拉回本地。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0729T010` 已完成 8 个 raw segment，但严格 campaign 终态失败。

规则更新提醒：
- 无

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 不需要

提交要求：
- 当前工作区包含未提交前置任务，不创建混合提交。
- 回报使用 `commit：无`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0730T011.md`
- `.workflow/reports/0730T011-business.md`
- `examples/hyperliquid/cross_exchange_collection_supervisor.py`
- `examples/hyperliquid/test_cross_exchange_collection_supervisor.py`
- `docs/cross_exchange_collection_supervisor_l2_timeline.md`
- remote T010 campaign:
  `/home/admin/0729T010_skhynix_4h_8x30m`
- local pullback under `local_live_analysis/`
- `task_plan.md`
- `progress.md`
- `findings.md`

action：
- Keep zero reconnect as a hard failure for:
  - Binance
  - Hyperliquid `fast_market`
  - Hyperliquid `standard_l2`
- Permit reconnects for auxiliary snapshot tracks only:
  - `asset_context`
  - `main_all_mids`
  - `target_dex_all_mids`
- An auxiliary reconnect passes only when:
  - the track's existing quality contract passes
  - connection/reconnect/disconnect counts reconcile
  - every expected subscription is acknowledged after every disconnect
  - every required data channel resumes after every disconnect
  - coverage, head/tail freshness and maximum arrival gap gates pass
  - raw SHA and row reconciliation pass
- Persist exact degraded intervals derived from raw local receipt timestamps.
- Add postprocess-only mode for an existing fully collected campaign.
- Preserve original collection runtime source and terminal failure provenance.
- Re-run strict quality and timeline generation for all eight T010 segments.
- Pull the successful package locally and verify transfer integrity.

hard boundary：
- Do not recollect or mutate raw gzip bytes.
- Do not weaken core L2 reconnect, replay, continuity or source-age gates.
- Do not silently forward-fill auxiliary state through degraded intervals.
- Public data only; no credentials, private endpoints or order actions.

verify：
- Focused synthetic tests for accepted and rejected auxiliary reconnects.
- Core L2 reconnect remains a hard failure.
- Missing ACK, missing resumed channel, count mismatch and excessive gap fail.
- Postprocess-only refuses missing/incomplete campaigns and never spawns
  collectors.
- Postprocess provenance/history and stale-abort cleanup tests.
- Focused Hyperliquid suite in local Conda env.
- Remote postprocess-only execution over the T010 raw campaign.
- Eight strict segment manifests, eight timelines and one timeline index pass.
- Remote/local archive and per-file integrity checks pass.

done：
- Core and auxiliary reconnect policies are separated and covered by failure
  injection.
- The existing T010 raw campaign was postprocessed without recollection or raw
  mutation.
- All eight segments and timelines pass; the complete package is present
  locally with matching archive and per-file hashes.
- Exact results and remaining capability limits are recorded in the business
  report.
