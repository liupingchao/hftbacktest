# 任务派发

执行线程：
- SKHYNIX OBI Reversal Track A0 业务线程

任务ID：
- 0828T004

标题：
- 执行 OBI_REVERSAL_V1 A0 Support And Tuple Freeze

简短描述：
- 使用已关闭身份的 29 个历史 Binance public captures 和 100ms causal
  top-5 reconstruction cache，执行 zero-future-price-target A0，冻结 OBI、
  reversal state machine、current-OBI-matched controls、first-passage
  target contract、H0/H1、support-selected tau_max 和 numeric gates。

状态：
- 待验收

执行顺序：
- 当前唯一正式任务

前置任务：
- 0828T003

规则更新提醒：
- 已阅读 AGENTS.md、workflow-kit manual、task/report/QA templates、
  Conditional-Risk Research Methodology Kernel v1、canonical OBI reversal
  Track A 方案和 0827T004 reconstruction artifacts。

按线程规则执行：
- AGENTS.md
- .workflow/workflow-kit/workflow-manual.md

是否需要提交代码：
- 需要

提交要求：
- 聚焦测试和 artifact closure 后提交
- 回报必须带 `commit id` 和提交信息

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- examples/hyperliquid/skhynix_obi_reversal_track_a0.py
- examples/hyperliquid/test_skhynix_obi_reversal_track_a0.py
- docs/skhynix_binance_continuous_background_interpretable_m_state_competing_risk_track_a_plan_20260828.md
- local_live_analysis/skhynix_obi_reversal_track_a0_0828T004/
- .workflow/tasks/0828T004.md
- .workflow/reports/0828T004-business.md

action：
- 验证前序 source inventory、feature cache size/SHA/schema/role identity。
- 仅加载 L1-L5 bid/ask quantity fields、timestamp、valid、capture metadata。
- 计算 equal-weight L1-L5 standard OBI 和 marginal/support diagnostics。
- 冻结 old/new band、neutral band、pre/confirm dwell、path timeout、
  cross/detect semantics 和 quality handling。
- 生成 reversal entries 和 zero-target matched-control candidate support。
- 仅根据 capture/future-coverage geometry 选择 tau_max。
- 冻结 midpoint one-tick first-passage contract但不读取或生成 price target。
- 冻结 H0/H1、proper score、uncertainty、null 和 gate thresholds。
- 生成 artifact manifest 和 A0 classification。

verify：
- focused pytest。
- synthetic deterministic build 两次 hash 一致。
- forbidden future-price feature mutation 不改变 A0 artifacts。
- full historical execution 与 artifact size/SHA closure。
- `git diff --check`。

done：
- 回报 source/cache closure、OBI support、state-machine candidate counts、
  matched-control overlap、tau_max、frozen tuple、A0 gate/classification、
  artifact 路径、测试和下一 A1/A2 边界。

明确禁止：
- 不读取 `midpoint_delta_ticks`、microprice displacement 的未来路径、
  future return、follow/fail labels、markout、fill 或 PnL。
- 不生成 reversal-aligned future-price plots。
- 不访问 private API，不下单，不采集新数据。
- 不把历史 replay 宣称为 prospective。
