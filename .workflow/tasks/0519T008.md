```md
执行线程：
- 业务线程-python

任务ID：
- 0519T008

标题：
- Step 9B default-off quote-adjustment offline replay runner implementation

简短描述：
- 基于 `0519T007` Step 9A design contract，实施一个 default-off、offline-only quote-adjustment replay runner，用 `5-13-day-control-30min` 先验证 runner / metrics / artifacts 链路。任务目标是生成 diagnostic classification 和可复核产物，不做 live、default-on、promotion 或 sample expansion。

状态：
- 已通过

执行顺序：
- 当前唯一任务；`0519T007` QA 已通过后可派发执行

前置任务：
- `0519T007` QA 已通过
- `0519T006` QA 已通过
- `0519T005` QA 已通过
- `0519T004` QA 已通过
- `0519T003` QA 已通过
- `0519T002` QA 已通过
- `0518T004` QA 已通过

规则更新提醒：
- 本任务是 Step 9B default-off offline runner implementation。不得启动 live、不得开启 default-on、不得修改生产 quote 行为、不得把单样本结果解释为 promotion 或 live readiness。

按线程规则执行：
- `.workflow/workflow-kit/thread-report-template.md`
- `docs/thread-playbook.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

Allowed files：
- `.workflow/tasks/0519T008.md`
- `.workflow/reports/0519T008-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `.workflow/dashboard.html`
- `.workflow/dispatch_suggestions.md`
- `examples/binance_tick_mm/quote_adjustment_replay.py`
- `examples/binance_tick_mm/test_quote_adjustment_replay.py`
- `local_live_analysis/5-13-day-control-30min/stage9b_quote_adjustment_replay_0519T008/**`

Allowed read-only inputs：
- `.workflow/tasks/0519T007.md`
- `.workflow/reports/0519T007-business.md`
- `.workflow/reports/0519T007-qa.md`
- `.workflow/reports/0519T006-qa.md`
- `.workflow/reports/0519T005-business.md`
- `.workflow/reports/0519T005-qa.md`
- `.workflow/reports/0519T004-qa.md`
- `.workflow/reports/0519T003-qa.md`
- `.workflow/reports/0519T002-qa.md`
- `.workflow/reports/0518T004-qa.md`
- `examples/binance_tick_mm/audit_schema.py`
- `examples/binance_tick_mm/strategy_core.py`
- `examples/binance_tick_mm/backtest_tick_mm.py`
- `examples/binance_tick_mm/live_tick_mm.py`
- `examples/binance_tick_mm/maker_acceptance.py`
- `local_live_analysis/5-13-day-control-30min/**`
- `local_live_analysis/5-13-day-control-30min/stage8b_quote_update_diagnostic_0519T005/**`
- `local_live_analysis/5-13-day-control-30min/stage5c_quote_anchor_safety_0518T004/**`
- `local_live_analysis/5-13-day-control-30min/stage6_final_lifecycle_calibration_0519T001/**`
- `local_live_analysis/5-13-day-control-30min/stage5_execution_labels_0514T005/**`
- `local_live_analysis/5-13-day-control-30min/stage4_pricing_research_0514T003/**`

Forbidden files / areas：
- Any live run, live collection, AWS/remote state, deployment, connector/core, canonical data conversion, or production config change.
- Any default-on behavior or production strategy behavior change.
- Any Step 5C promotion, inventory-control implementation, queue/touch repair, or top5 hard-anchor promotion.
- Any current-format sample expansion or live micro test.
- Any claim that single-sample replay PnL proves promotion or live readiness.

Requires separate task：
- Promoting any candidate beyond diagnostic default-off offline replay.
- Enabling a candidate in live or production configs.
- Multi-sample current-format validation.
- Inventory-control implementation beyond diagnostic candidate simulation.
- Step 5C default-on promotion or hard-anchor source repair.

Fatal Failures：
- 修改 live/backtest production quote behavior outside the offline runner.
- 跑 live 或触碰 AWS/remote state。
- 默认开启任何 candidate。
- 使用 future markout、future fill outcome、audit replay overlays、exact queue claims、`4948`-specific logic 或 same-sample PnL feedback 作为 candidate decision input。
- 将 `5-13-day-control-30min` 单样本结果解释为 promotion、live readiness 或 generalized profitability proof。

scope：
- 实施 Step 9B default-off offline runner。
- 首轮只使用 `5-13-day-control-30min` 做 runner validation 和 diagnostic classification。
- Runner 可以基于 existing audit / labels / diagnostics 离线模拟 candidate decisions，也可以组织 default-off candidate configs；但不能改变默认生产行为。
- 重点是 artifacts / metrics / coverage / classification 的可复核性，不是追求单样本 PnL。

candidate matrix：
- 必须至少覆盖 `0519T007` 定义的八类：
  - `baseline_control`
  - `fair_reservation_shift`
  - `inventory_reservation_shift`
  - `spread_widening`
  - `size_reduction_or_add_side_suppression`
  - `stale_latency_no_fresh_add`
  - `min_move_quote_age_churn_guard`
  - `post_only_safety_interaction`
- 每个 candidate 必须记录：
  - candidate id / family / parameters
  - decision-time-visible inputs used
  - disallowed inputs check
  - expected effect / risk
  - related T006 audit fields
  - whether it changes simulated quote intent, simulated quote action, size, spread, suppress state, or safety outcome

metrics：
- 必须输出：
  - gross/net PnL proxy, fee, spread capture
  - fill probability, time-to-fill, side-adjusted markout at 100ms/500ms/1s/5s
  - cancel-fill, fill-after-cancel, cancel-to-fill delay
  - inventory cycle, max excursion, time in band, zero-crossing, recovery quality
  - API request count, token pressure, throttle/reject/drop, min-move pass/fail, quote age, cancel-readd/churn
  - stale/missing anchor, clamp/suppress, post-only pre/post-check, bad-price/crossed-risk counters
  - action-path coverage, audit-field coverage, candidate-decision coverage, market-view gate status, lifecycle label coverage
- Metrics may be diagnostic/proxy when exact replay/live proof is unavailable; the output must label proxy metrics clearly.

required artifacts：
- Write outputs under `local_live_analysis/5-13-day-control-30min/stage9b_quote_adjustment_replay_0519T008/`.
- Required files:
  - `run_manifest.json`
  - `candidate_matrix.csv`
  - `candidate_matrix.json`
  - `candidate_summary.json`
  - `candidate_metrics.csv`
  - `fill_quality_by_candidate.csv`
  - `inventory_cycle_metrics.csv`
  - `api_churn_metrics.csv`
  - `post_only_safety_metrics.csv`
  - `action_path_coverage.csv`
  - `audit_field_coverage.csv`
  - `candidate_decision_samples.csv`
  - `acceptance_decision.md`

acceptance / classification：
- The runner must end with one of:
  - `no_effect`
  - `worse_due_to_churn_or_fill_quality`
  - `promising_but_single_sample`
  - `blocked_by_replay_or_market_view`
  - `needs_more_instrumentation`
- `promising_but_single_sample` still does not authorize live or promotion; it only supports later multi-sample validation planning.
- If required T006 fields or Stage 5/6/7/8 diagnostics are missing, classify as `needs_more_instrumentation` or `blocked_by_replay_or_market_view` instead of inventing evidence.

verify：
- `python -m pytest examples/binance_tick_mm/test_quote_adjustment_replay.py`
- `python examples/binance_tick_mm/quote_adjustment_replay.py --help`
- `python examples/binance_tick_mm/quote_adjustment_replay.py --run-dir local_live_analysis/5-13-day-control-30min --output-dir local_live_analysis/5-13-day-control-30min/stage9b_quote_adjustment_replay_0519T008`
- `python3 .workflow/build_dashboard.py`
- `git diff --check`

done：
- 回报必须说明 runner 是 default-off offline-only。
- 回报必须列出 candidate matrix、生成 artifacts、classification 和关键 metrics。
- 回报必须明确是否建议后续开 multi-sample validation planning，或者是否 blocked / needs_more_instrumentation。
- 回报必须明确：no live, no default-on, no promotion, no sample expansion, no production behavior change。
```
