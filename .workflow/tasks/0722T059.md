# 0722T059 LEGACY-SOURCE-CONTAINMENT-FINAL-REPAIR

执行线程：
- 总控 / 业务执行线程

任务ID：
- 0722T059

标题：
- Validate existing legacy source containment and stabilize shutdown test tolerance

简短描述：
- 修复 T058 QA 的最后一个 P1：existing legacy source symlink 在映射前
  必须验证仍属于 resolved legacy root。
- 将 watcher deadline hostile test 的调度容差改为明确的 test-only
  scheduler tolerance。

状态：
- 未通过

执行顺序：
- 当前唯一任务

前置任务：
- `0722T058`（未通过）

规则更新提醒：
- offline-only。
- 不修改 production deadline、timestamp、strategy、quote、risk 或
  lifecycle 行为。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交并进入独立 QA。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `examples/hyperliquid/basis_positive_row_level_read_only_generator.py`
- `examples/hyperliquid/test_basis_positive_row_level_read_only_generator.py`
- `examples/hyperliquid/test_hyperliquid_tiny_live_m2_event_driven_watcher.py`
- workflow/report/tracking files

action：
- Existing legacy recorded path 在 lexical mapping 前 resolve，并验证属于
  resolved legacy root。
- 新增 legacy source symlink escape hostile，current checkout 同 relative
  target 保持合法，确保不能由 destination containment 掩盖 hostile source。
- 为 blocking-source deadline test 使用明确 `0.1s` test scheduler
  tolerance；production constants 不变。

verify：
- Focused hostile matrix。
- Deadline hostile repeated。
- Full Hyperliquid 连续两次。
- py_compile / diff checks。

done：
- Fixture portability 阶段所有 QA P1/P2 关闭。
- 经独立 QA 接受后进入 basis regression alpha。

blockers：
- 无。

提交信息：
- `Validate legacy source containment`
