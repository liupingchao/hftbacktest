# 任务派发

执行线程：
- 业务线程-python/public-data-infra + research-postprocess

任务ID：
- 0805T002

标题：
- VERSIONED-CROSS-EXCHANGE-POSTPROCESS-PIPELINE-AND-SKILL

简短描述：
- 将现有 campaign quality、R0、R1 后处理入口编排为可版本化、可恢复、
  可验证的统一 pipeline，并生成固定 dataset manifest 和简要报告。
- 以 amdserver 上已验收的 Aug05 数据作为 golden dataset，验证新入口不
  改写 source campaign 且核心数据产物与 `0805T001` 一致。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- `0805T001` 已通过，提供 recovered reconnect、epoch-aware replay、
  R0 exact masks 和 R1 exact horizon masks 的 golden artifacts。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 不需要

提交要求：
- 当前工作区包含已验收但未提交的前置工作，不创建混合提交。
- 回报使用 `commit：无`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 检查 source immutability、stage fingerprint、resume、artifact SHA、
  fail-closed validation、report capability boundary 和 Aug05 golden
  reconciliation。

files：
- `examples/hyperliquid/cross_exchange_postprocess/`
- `examples/hyperliquid/test_cross_exchange_postprocess.py`
- `.agents/skills/cross-exchange-postprocess/`
- `docs/cross_exchange_postprocess_pipeline.md`
- amdserver Aug05 source/golden artifacts
- `.workflow/reports/0805T002-business.md`
- `.workflow/reports/0805T002-qa.md`
- `docs/qa-acceptance-report.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

action：
- 建立 raw/campaign audit、R0、R1 的有向 stage pipeline。
- 每个 stage 记录 schema version、input fingerprint、runtime source、
  output row count/SHA、状态和耗时。
- 支持 `run`、`resume`、`inspect`、`validate`、`report`。
- 使用临时目录和原子 manifest 发布；不修改 source campaign。
- 输出 `pipeline_manifest.json`、`quality_summary.json`、
  `provenance_lock.json` 和 `dataset_report.md`。
- 建立 dataset、signal-research、full-research profile 注册表；本任务只
  宣告 dataset profile executable，未迁移的研究 stages 必须明确
  `not_implemented`，不得静默跳过后宣称完成。
- 创建 repo-scoped Codex Skill，Skill 只负责工作流选择、调用和解释；
  确定性计算继续位于仓库 Python pipeline。

verify：
- focused pytest 覆盖 fresh run、resume reuse、fingerprint invalidation、
  source mutation detection、artifact tamper detection、failed stage report
  和 profile capability boundaries。
- `py_compile`、CLI `--help`、Skill validation、`git diff --check`。
- 在 amdserver 用 Aug05 campaign 完整执行 dataset profile。
- 对比 `0805T001` golden timeline/R0/R1 核心文件 SHA 和 row counts。
- source campaign 全树 SHA 在运行前后完全一致。

done：
- 后续输入一个已 postprocess 的 raw campaign，可通过一个统一命令得到
  可审计 R0/R1 dataset 与简要报告。
- 新增研究阶段可以通过 stage contract 继续接入，而不破坏历史产物。

hard boundary：
- 不进行新 live collection。
- 不修改历史 raw bytes 或已验收 golden artifacts。
- 不将未接入的 signal/hierarchy stages 声称为已完成。
- 不形成 exact fill、可执行套利、maker PnL、因果 Binance lead 或
  live promotion 结论。
