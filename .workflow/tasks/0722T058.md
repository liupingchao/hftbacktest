# 0722T058 UNIFIED-ARTIFACT-PATH-CONTAINMENT-REPAIR

执行线程：
- 总控 / 业务执行线程

任务ID：
- 0722T058

标题：
- Enforce one resolved project-root containment invariant for artifact paths

简短描述：
- 修复 T057 QA 的剩余两个 P1：existing absolute fast path 和 relative path
  均必须服从同一个 resolved `PROJECT_ROOT` containment invariant。

状态：
- 未通过

执行顺序：
- 当前唯一任务

前置任务：
- `0722T057`（未通过）

规则更新提醒：
- offline-only；不得执行 network、remote、private/account、order/cancel、
  live 或 service 操作。
- 不改变 historical artifact 统计、strategy 或 production timing。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交。
- 回报必须带 exact implementation commit id 和提交信息。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `examples/hyperliquid/basis_positive_row_level_read_only_generator.py`
- `examples/hyperliquid/test_basis_positive_row_level_read_only_generator.py`
- `.workflow/tasks/0722T058.md`
- `.workflow/reports/0722T058-business.md`
- controller tracking files

action：
- 所有返回路径先 resolve，再验证属于 resolved `PROJECT_ROOT`。
- Relative traversal、relative symlink escape、current-root symlink escape、
  existing legacy traversal/symlink 和 unknown external absolute path 均
  fail-closed。
- Legacy path 只允许 lexical child path 映射到当前 repo-relative target。
- 保持 true missing skip、malformed existing fail 和 T057 test-clock 修复。

verify：
- 新增全部路径类别 hostile tests。
- Focused portability tests。
- Full Hyperliquid 连续两次。
- `python -m py_compile` 和 `git diff --check`。

done：
- Resolver 所有分支共享一个 containment invariant。
- T056/T057 fixture portability 缺陷全部关闭并重新进入独立 QA。

blockers：
- 无。

提交信息：
- `Enforce artifact root containment`
