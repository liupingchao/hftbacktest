# 任务派发

执行线程：
- 业务线程-python/multi-market-queue-notebook-copies

任务ID：
- 0804T005

标题：
- MULTI-MARKET-QUEUE-EXCHANGE-TARDIS-NOTEBOOK-COPIES

简短描述：
- 为多市场、队列模型和交易所对比五个教程建立独立可运行副本，
  使用本机 sample Tardis 和 amdserver 已挂载数据依次复现实验。

状态：
- 已通过

执行顺序：
- 当前唯一任务。
- 本任务内部按用户列出的五个 notebook 顺序串行。

前置任务：
- `0804T002`：Tardis 数据准备与基础 runner。
- `0804T003`：可 Run All notebook 结构、共享缓存和服务器验收方式。
- `0804T004`：Grid/GLFT helper 与 point-in-time 验收经验。

规则更新提醒：
- 无

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `AGENTS.md`

是否需要提交代码：
- 需要

提交要求：
- 仅提交本任务新增的 notebook 副本、market/queue helper、共享映射、
  测试和报告。
- 不修改或提交五个原始 notebook。
- 不混入工作区已有 Hyperliquid 或其他任务改动。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 核对原始 notebook 不变、副本 Run All、Tardis 输入身份、多市场与
  queue-model 映射、point-in-time 边界、适配声明和 amdserver 执行证据。

files：
- 只读原始文件：
  - `examples/Making Multiple Markets.ipynb`
  - `examples/Making Multiple Markets - Introduction.ipynb`
  - `examples/Probability Queue Models.ipynb`
  - `examples/Queue-Based Market Making in Large Tick Size Assets.ipynb`
  - `examples/High-Frequency Grid Trading - Comparison Across Other Exchanges.ipynb`
- 新增副本目录：
  - `examples/tutorial_reproduction/notebooks/0804T005/`
- `examples/tutorial_reproduction/market_queue_experiments.py`
- `examples/tutorial_reproduction/notebook_support.py`
- `examples/tutorial_reproduction/refresh_notebooks.py`
- focused tests and reports。

action：
- Making Multiple Markets Introduction：用确定性组合实验说明市场数量和
  相关性对组合波动/Sharpe 的影响。
- Making Multiple Markets：在 amdserver 上使用 Binance、Bybit、OKX、
  Bitget、Gate 五个真实 Tardis BTC 永续市场运行统一做市策略并组合权益。
- Probability Queue Models：在相同 Binance 输入和策略下比较
  Square/Log/Power 三种概率队列模型。
- Queue-Based Market Making：比较 mid、book pressure、causal trade
  impulse 和薄队列 backoff；由于指定挂载无 CRVUSDT，明确标记为 BTC
  数据适配，不声称等价复现大 tick 资产。
- Exchange Comparison：同一日期、窗口、BTC 永续和参数下比较五交易所
  订单流与回测输出。
- 本机仅有 Binance sample 时允许多市场/跨交易所项以单市场 `adapted`
  状态完成；amdserver 验收要求读取五个挂载。

verify：
- focused pytest、py_compile、notebook `nbformat.validate` 和生成器确定性。
- 本机 sample 冒烟运行五项副本。
- 原始五本 notebook SHA256、Git blob 和工作树状态不变。
- Git push 后 amdserver fast-forward。
- amdserver 2025-08-01、300 秒窗口按顺序逐本
  `jupyter nbconvert --execute`。
- 检查 executed notebook 无 error output、manifest 状态和关键指标。
- 对 queue signal、组合收益和交易所结果执行 future-mutation/
  input-identity 检查。
- `git diff --check`。

done：
- 五个副本可在本机和 amdserver 打开并 Run All。
- amdserver 多市场与交易所对比使用五个真实挂载，无下载和 Tardis Key。
- 每项有独立 manifest、输入身份、参数和结果摘要。
- 原始 notebook 未修改。

hard boundary：
- 不使用 Tardis Key，不下载新数据。
- 不访问交易所 private/account/order/cancel 接口，不下单。
- 不把 BTCUSDT 队列实验声明为 CRVUSDT 大 tick 资产的完全等价复现。
- 不把同一标的的五交易所组合声明为原教程多资产横截面的完全等价复现。
