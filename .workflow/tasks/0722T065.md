# 0722T065 T064-SOURCE-MATERIALIZATION-AND-DERIVATION-REPAIR

执行线程：
- 总控 / 业务执行线程

任务ID：
- 0722T065

标题：
- Materialize T047/T052 source bytes and derive adaptive reachability locally

简短描述：
- 通过 SSM read-only structured receipts 拉回 T047/T052 的 10 个 redacted
  estimator/lifecycle 文件，逐文件验 hash，并从真实 CSV/JSON 重算 T064
  counts、eligibility、blocking reasons 和 recommendation。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0722T064`（未通过）

规则更新提醒：
- 只读取已存在 artifacts。
- 不读取 credentials，不调用 private/account/order/cancel endpoint，不启动
  service/watcher/orchestrator，不产生订单。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交并进入独立 QA。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- T064 reachability runner/tests
- structured SSM invocation receipts and redacted source files under T064
  artifact root
- T064 regenerated derived artifacts
- workflow/report/tracking files

action：
- Issue one structured read-only SSM command per prior package.
- Receipt payload must include instance/task/root, exact expected filenames,
  each file's base64 bytes and remote SHA-256.
- Save AWS invocation receipts locally and materialize the 10 redacted files.
- Verify receipt status, command identity, hex hashes and local bytes.
- Parse estimator snapshot/intensity/exposure/lifecycle/feedback files to
  derive all observations, distance sets, eligible counts and exposure.
- Compute source eligibility, combined eligibility, blocking reasons and route
  from derived values.
- Add eligible-source positive regression and hostile receipt/hash/count tests.

verify：
- Local materialized file SHA-256 matches receipt and T064 remote audit.
- Focused deterministic/positive/negative tests.
- Official deterministic rebuild and repo-relative boundary.
- Full Hyperliquid, py_compile and diff checks.
- No live/private/order/cancel/service side effect.

done：
- Independent QA can reproduce every T064 fact without AWS access.
- No trusted copied count remains in the decision path.
- T064 recommendation is computed from source bytes.

blockers：
- SSM output truncation, receipt mismatch, source file hash mismatch or parser
  ambiguity must fail closed.

执行结果：
- Structured SSM invocation receipts for `0721T047` and `0722T052` are
  materialized with exact base64 source bytes.
- All ten source files match their remote SHA-256 audit values.
- Counts, distance variation, lifecycle eligibility, blockers and
  recommendation are derived locally from the materialized CSV/JSON files.
- Current evidence remains ineligible:
  combined dynamic buy `4 observations / 1 distance`; fill feedback
  `0 observations / 0 seconds`.
- Focused tests: `8 passed`.
- Full Hyperliquid regression: `1291 passed, 2 skipped`.

提交信息：
- implementation commit:
  `44da8a50f1953f116ea6c89a5c6204a5bf71e405`
- `Materialize adaptive reachability source evidence`
