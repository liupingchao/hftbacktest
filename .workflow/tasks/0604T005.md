```md
执行线程：
- 业务线程-research

任务ID：
- 0604T005

标题：
- Canonical evidence source lock / guard hardening

简短描述：
- Milestone 0 read-only worker：在 `0604T004` canonical event-mode evidence loader foundation 上增加 source-lock / guard hardening，确保后续 ranking、horizon、regime worker 只能消费 `0604T003` canonical event-mode evidence，不能把 ordinary synthetic fixed-grid artifacts 当作 formal evidence。

状态：
- 已通过

执行顺序：
- 可并行

前置任务：
- `0604T004` QA 已通过。

规则更新提醒：
- 请先阅读规则文档后再执行本任务。
- 本任务是 read-only guard hardening；不做 signal ranking、不做 regime selection、不实现 maker strategy、不接 private/order、不做参数搜索、不 live/default-on/tiny-live、不 promotion。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `docs/thread-playbook.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交任务文件、业务报告、代码/test 修改、task-scoped verification artifacts 和必要 workflow tracking 更新。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0604T005.md`
- `.workflow/reports/0604T005-business.md`
- `examples/hyperliquid/canonical_event_mode_evidence.py`
- `examples/hyperliquid/test_canonical_event_mode_evidence.py`
- `local_live_analysis/canonical_evidence_source_lock_0604T005/**`
- 必要 workflow tracking 文件仅限本任务状态/结论更新。

read-only inputs：
- Canonical loader foundation:
  - `local_live_analysis/canonical_event_mode_evidence_0604T004/**`
- Canonical source:
  - `local_live_analysis/event_mode_canonical_pricing_signal_0604T003/**`
- Synthetic diagnostic comparison for negative guard validation only:
  - `local_live_analysis/event_mode_canonical_pricing_signal_0604T003/synthetic_diagnostic_comparison/**`

forbidden：
- New data collection.
- Signal ranking, regime selection, case-library construction, shadow decision generation.
- Hyperliquid or Binance private/account/order endpoints.
- Order lifecycle, submit/cancel/fill logic.
- Strategy implementation, production config/default changes, parameter search, default-on, tiny-live, promotion.
- Connector/core API changes, standard npz schema changes, canonical Binance maker audit schema changes.
- Treating ordinary synthetic fixed-grid samples as canonical short-horizon independent evidence.

action：
- Harden the `0604T004` loader / validator so downstream workers have an explicit canonical source lock.
- Add or refine guard checks that fail clearly when:
  - the input is not `decision_mode=event`
  - the input is not `canonical_status=canonical_event_mode`
  - canonical sample count is zero for a worker that requires formal evidence
  - the input path points at `synthetic_diagnostic_comparison` or another diagnostic-only fixed-grid source unless the caller explicitly requests negative validation
  - required source-lock metadata is missing or inconsistent.
- Emit task-scoped guard artifacts under `local_live_analysis/canonical_evidence_source_lock_0604T005/`, including:
  - `canonical_source_lock_manifest.json`
  - `canonical_guard_check_report.md`
  - `negative_guard_validation_report.csv`
- Keep the API narrow and reusable by later workers:
  - signal quality ranking worker must be able to call the guard before ranking
  - horizon/regime diagnostics worker must be able to call the guard before diagnostics

acceptance criteria：
- Guard accepts the `0604T003` canonical event-mode aggregate and reports exactly `canonical_sample_count=3`.
- Guard rejects or diagnostic-only classifies the `0604T003` synthetic diagnostic comparison and reports `canonical_sample_count=0`, `diagnostic_rejection_count=3`.
- Focused tests cover accepted canonical source, rejected diagnostic source, missing source-lock metadata, zero-canonical formal-evidence failure, and a downstream-worker-style guard call.
- Task-scoped artifacts exist, are parseable, and explicitly name `0604T003` / `0604T004` as the only formal evidence sources.
- Boundary flags explicitly remain no new collection, no ranking, no regime selection, no private/order/strategy/live/parameter/default-on/tiny-live/promotion.

verify：
- `python examples/hyperliquid/canonical_event_mode_evidence.py --help`
- `python -m py_compile examples/hyperliquid/canonical_event_mode_evidence.py examples/hyperliquid/test_canonical_event_mode_evidence.py`
- `python -m pytest examples/hyperliquid/test_canonical_event_mode_evidence.py`
- Run the guard on `local_live_analysis/event_mode_canonical_pricing_signal_0604T003/` and write `local_live_analysis/canonical_evidence_source_lock_0604T005/`.
- Run the guard negative validation on `local_live_analysis/event_mode_canonical_pricing_signal_0604T003/synthetic_diagnostic_comparison/` and prove formal canonical sample count is `0`.
- Parse generated JSON manifests with `python -m json.tool`.
- `git diff --check`

done：
- Business report must include implemented guard/API behavior, canonical sample count, diagnostic rejection count, output artifact paths, verification commands, blockers/caveats if any, commit id, and commit message.
- Business report must explicitly state this is only a read-only canonical evidence source lock / guard hardening task, and does not authorize signal ranking, regime selection, strategy implementation, private/order endpoints, live/default-on/tiny-live, parameter search, or promotion.
- Business report should end in `待验收` if verification passes.
```
