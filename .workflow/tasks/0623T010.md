```md
执行线程：
- 业务线程-live-awsserver1

任务ID：
- 0623T010

标题：
- M2 AWS public-only candidate funnel diagnosis

简短描述：
- 在 `0623T009` 的 public-only no-submit shadow 基础上，做更长窗口或更细粒度的 candidate funnel 诊断，解释为什么 fresh-touch / Binance freshness / fair-mid source / edge gate 没有形成 would-submit 路径。
- 本任务继续只允许 public-only、no-submit、no-private / no-order / no-final-gate 边界，不放松 quote distance、cap、post-only 或任何私有端点限制。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0623T009`

规则更新提醒：
- 请先阅读 `.workflow/workflow-kit/workflow-manual.md`、`.workflow/workflow-kit/task-dispatch-template.md`、`.workflow/workflow-kit/thread-report-template.md`、`.workflow/workflow-kit/qa-acceptance-template.md`、`task_plan.md`、`progress.md`、`findings.md` 后再执行本任务。

按线程规则执行：
- `AGENTS.md`

是否需要提交代码：
- 需要

提交要求：
- 自测 / 远端执行证据生成后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

host / execution location：
- `awsserver1`
- Remote repo: `/home/admin/hftbacktest-cross-exchange`
- Local machine may only create the task/report, perform git-safe bundle sync, pull back artifacts, and record results.

files：
- `.workflow/tasks/0623T010.md`
- `.workflow/reports/0623T010-business.md`
- `local_live_analysis/hyperliquid_tiny_live_m2_aws_candidate_funnel_0623T010/**`
- `task_plan.md`
- `progress.md`
- `findings.md`

read-only inputs：
- `.workflow/tasks/0623T009.md`
- `.workflow/reports/0623T009-business.md`
- `.workflow/reports/0623T009-qa.md`
- `local_live_analysis/hyperliquid_tiny_live_m2_aws_public_shadow_soak_0623T009/**`
- `local_live_analysis/hyperliquid_tiny_live_m2_aws_public_shadow_soak_0623T009_rerun_venv_20260623T042422Z/**`

hard boundary：
- No live orders, no credential reads, no private/account/order endpoints, no real order endpoint, no cancel endpoint, no live client initialization, no remote final gate rerun, no T008 live ledger claim, no quote-distance change, no one-tick-back, no inside-spread, no cap relaxation, no taker/crossing, no default-on behavior, no M3/stable PnL/promotion.
- Public live source use is allowed only for Hyperliquid public L2/trades and Binance public market data.
- The task may use a longer live public shadow window or finer-grained post-processing, but it must stay fail-closed and must not fabricate would-submit evidence.

action：
- Run a public-only candidate funnel diagnosis on `awsserver1` with the existing `m2_live_public_source_shadow_v1` path.
- Prefer a longer live public shadow window than T009, and/or post-process row-level artifacts into stage-level diagnostics for:
  - fresh-touch
  - Binance freshness
  - fair-mid source
  - edge gate
  - would-submit
- Capture the dominant block reasons and the first stage at which candidates stop advancing toward would-submit.
- Keep the path no-submit and strictly public-only.

verify：
- `python -m pytest examples/hyperliquid/test_hyperliquid_tiny_live_m2_event_driven_watcher.py -q`
- `python -m py_compile examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- `python examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py --help`
- Remote longer-window public shadow run on `awsserver1`
- Post-process row-level CSVs into stage-level funnel counts / reason histograms
- Validate JSON/CSV outputs and funnel summary artifacts
- `git diff --check`

done：
- 回报 longer-window duration, public stream counts, fresh-touch block/pass counts, Binance freshness reasons, fair-mid source pass/block counts, edge pass/block counts, would-submit count, artifact path, dominant block reasons, whether real live public source was observed, whether any private/order endpoint was touched, whether the next real canary is authorized, commit id, 提交信息。
- 明确说明本任务不授权真实下单、读凭据、private/account/order endpoint、remote final gate、T008 live ledger claim、quote-distance change、one-tick-back、inside-spread、cap relaxation、M3/stable PnL/default-on/promotion。
```
