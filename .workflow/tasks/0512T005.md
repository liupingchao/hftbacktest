```md
执行线程：
- 业务线程-python

任务ID：
- 0512T005

标题：
- add-side toxic timing guard default-off 实现与离线 replay

简短描述：
- 基于 `0512T002` 设计合同，实现 default-off `add_side_toxic_timing_guard`，覆盖 add-side submit/re-add path，并用 Stage 6J replay 与历史 live counterfactual overlay 证明 action-path coverage 和 replay-model regression。本任务不启动 live，不默认开启规则，不声明 live source-path improvement。

状态：
- 已通过

执行顺序：
- `0512T002` 完成并通过 QA 后执行；必须早于任何 live micro test 或 promotion 任务

前置任务：
- 0512T002

规则更新提醒：
- 本任务是实现与离线验证任务，不是 live promotion。
- 必须严格按 `0512T002` 设计合同实现；不得重新退回旧 `target_deterioration` threshold tuning。
- 必须使用 `0512T004` 三层观测门禁：action-path coverage、replay-model regression、live-derived source-path proof。
- Stage 6J replay 只能作为 replay-model regression gate；不能单独证明 live adverse-selection source-path 改善。
- `5-11-night-active` 是主开发/诊断样本；`5-10-day-control-1h-06`、`5-9-noon`、`5-9-small` 是 cross-sample sanity check。

按线程规则执行：
- .workflow/workflow-kit/thread-report-template.md
- docs/thread-playbook.md
- docs/5-8-future-plan.md
- docs/stage6i-cancel-requested-fill-risk-plan.md

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- .workflow/tasks/0512T005.md
- .workflow/reports/0512T002-business.md
- .workflow/reports/0512T004-business.md
- examples/binance_tick_mm/strategy_core.py
- examples/binance_tick_mm/live_tick_mm.py
- examples/binance_tick_mm/backtest_tick_mm.py
- examples/binance_tick_mm/audit_schema.py
- examples/binance_tick_mm/stage6j_replay.py
- examples/binance_tick_mm/analyze_cancel_fill_risk.py
- examples/binance_tick_mm/test_backtest_tick_mm.py
- examples/binance_tick_mm/test_stage6j_replay.py
- local_live_analysis/5-11-night-active/
- local_live_analysis/stage6j_regime_control_cancel_fill_risk_5-11/
- findings.md
- progress.md

Fatal Failures：
- 启动 live、连接交易所、修改 AWS 状态或默认开启规则。
- 把本任务变成 live promotion 或 maker 参数优化任务。
- 继续沿用旧 `target_deterioration` working-quote-only trigger，只调 threshold/cooldown。
- 阻断 reduce-side submit 或无法证明 blocked reduce-side count 为 `0`。
- 把 Stage 6J source-path 指标当成 live source-path proof。
- 只在 `5-11-night-active` 单样本上定义成功，缺少 cross-sample sanity check。
- 使用 future mid、future markout、audit overlay、replay-only overlay 或 live 当时不可见诊断输出作为 live 决策输入。
- 大幅减少交易量、扩大 drop、削弱 reduce-side 来制造表面改善。

MUST NOT：
- 不启动真实 live，不连接交易所，不改 AWS 状态。
- 不默认开启 `add_side_toxic_timing_guard`。
- 不修改任务范围外代码。
- 不把 PnL 作为唯一晋级指标。
- 不跳过 `0512T002` 的 helper/config/audit/replay/QA 合同。
- 不跳过 `0512T004` 对三层证据的边界。

实现目标：
- 实现 default-off `add_side_toxic_timing_guard`。
- 只阻断 add-side submit/re-add，不阻断 reduce-side submit。
- 覆盖 side 当前无 working order 准备 submit，或 stale same-side working quote cancel 后准备 re-add submit 的路径。
- 先计算 submit eligibility，再决定是否 suppress submit。
- 对 cancel+re-add path，允许 cancel stale same-side working quote，但 toxic window 内 suppress paired re-add submit。
- 在 live/backtest 共享 helper 中实现核心逻辑，禁止复制两套业务逻辑。

允许输入：
- 当前 side 是否有 working order。
- 当前 side 是否准备 `submit_buy` / `submit_sell`。
- position 与 add-side/reduce-side 判定。
- recent same-side cancel request age。
- recent same-side cancel-fill age。
- target tick 自上次 quote / cancel / decision 的变化。
- cancel-requested in-flight exposure。
- feed/order latency signal。
- rolling historical toxicity state，只限当前时点之前。

禁止输入：
- future mid。
- future markout。
- future fill 后价格路径。
- audit overlay state。
- replay-only working overlay。
- replay-only position overlay。
- live 当时不可见的诊断输出。

配置合同：
- `risk.add_side_toxic_timing_guard_enabled = false`
- `risk.add_side_toxic_timing_guard_window_ms`
- `risk.add_side_toxic_timing_guard_min_target_move_ticks`
- `risk.add_side_toxic_timing_guard_latency_threshold_ms`
- `risk.add_side_toxic_timing_guard_pending_cancel_enabled`
- `risk.add_side_toxic_timing_guard_post_cancel_fill_enabled`
- `risk.add_side_toxic_timing_guard_target_move_enabled`
- `risk.add_side_toxic_timing_guard_block_mode = "add_side_submit_only"`

audit 字段合同：
- `add_side_submit_eligible_buy`
- `add_side_submit_eligible_sell`
- `add_side_submit_blocked_buy`
- `add_side_submit_blocked_sell`
- `add_side_submit_block_reason_buy`
- `add_side_submit_block_reason_sell`
- `add_side_submit_reduce_side_allowed_buy`
- `add_side_submit_reduce_side_allowed_sell`
- `target_move_since_last_quote_or_cancel_buy`
- `target_move_since_last_quote_or_cancel_sell`
- `last_cancel_request_age_ms_buy`
- `last_cancel_request_age_ms_sell`
- `last_cancel_fill_age_ms_buy`
- `last_cancel_fill_age_ms_sell`
- `toxic_timing_guard_until_ts_buy`
- `toxic_timing_guard_until_ts_sell`

action：
- 读取 `0512T002` 和 `0512T004`，把实现限制写入代码注释/测试名/报告，不得扩大范围。
- 在 `strategy_core.py` 设计共享 helper 和 result dataclass，核心逻辑只实现一次。
- 在 `decide_actions(...)` 或等价 shared decision path 中支持 submit-leg suppression，并证明 reduce-side 不被阻断。
- 在 `live_tick_mm.py` 与 `backtest_tick_mm.py` 接入同一 helper/config/state/audit 字段。
- 更新 `audit_schema.py`，确保新增 audit 字段在 live/backtest audit 中一致。
- 更新 `stage6j_replay.py` candidate matrix：baseline、add-side guard、toxic timing 50/100/200ms、add-side guard + toxic timing 100ms、broad cooldown control。
- 增加 focused unit tests：default-off、add-side eligible、blocked submit、reduce-side allowed、cancel+re-add submit suppression、no-working-order submit suppression、forbidden future input absence、Stage 6J config injection。
- 运行主样本 `5-11-night-active` 和 sanity checks `5-10-day-control-1h-06`、`5-9-noon`、`5-9-small` 的 Stage 6J replay。
- 生成 action-path coverage 表：eligible rows、blocked rows、submit overlap、blocked reduce-side rows、action/planned_action diff。
- 生成 replay-model regression 表：PnL、max position、drop latency/API、action churn、cancel-fill、same-side worsening。
- 如实现只读 historical live counterfactual overlay/tagger，必须明确它只能证明 would-block overlap，不能证明 live source-path improvement。
- 输出 `.workflow/reports/0512T005-business.md`，更新 `findings.md` 和 `progress.md`。

verify：
- `python3 -m py_compile examples/binance_tick_mm/strategy_core.py examples/binance_tick_mm/live_tick_mm.py examples/binance_tick_mm/backtest_tick_mm.py examples/binance_tick_mm/stage6j_replay.py examples/binance_tick_mm/audit_schema.py`
- `python3 -m pytest examples/binance_tick_mm/test_backtest_tick_mm.py examples/binance_tick_mm/test_stage6j_replay.py`
- `python3 examples/binance_tick_mm/stage6j_replay.py --local-root local_live_analysis --out-dir local_live_analysis/stage6j_cross_sample_0512T005 --run-id 5-11-night-active --run-id 5-10-day-control-1h-06 --run-id 5-9-noon --run-id 5-9-small`
- `python3 .workflow/build_dashboard.py`
- 若任一命令因环境或数据缺失无法运行，必须记录 blocker，不得编造通过结果。

done：
- 回报必须明确 default-off 是否保持。
- 回报必须明确新增 helper/config/audit 字段。
- 回报必须明确旧 `target_deterioration` 未作为主 trigger。
- 回报必须给出 action-path coverage 表，至少包含主样本和 sanity checks。
- 回报必须证明 blocked reduce-side submit rows = `0`，或说明阻塞原因并不得进入下一阶段。
- 回报必须给出 Stage 6J replay regression 表。
- 回报必须明确哪些证据是 action-path coverage，哪些是 replay-model regression，哪些仍缺 live-derived source-path proof。
- 回报必须明确是否允许创建后续 QA/implementation follow-up。默认只允许 QA；不允许 live micro test。
- 回报必须明确 no live micro test。
```
