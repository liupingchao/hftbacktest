```md
执行线程：
- 业务线程-research

任务ID：
- 0604T004

标题：
- Canonical event-mode evidence loader contract

简短描述：
- 为后续可并行的 high-confidence regime maker research 开发建立一个很薄的串行基础层：实现 `0604T003` canonical event-mode artifacts 的 loader / validator contract，统一识别 canonical event-mode samples、拒绝 ordinary synthetic fixed-grid samples 进入正式证据集，并输出可被后续 ranking / horizon / regime runners 共用的 task-scoped manifest。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0604T003` QA 已通过。

规则更新提醒：
- 请先阅读规则文档后再执行本任务。
- 本任务只建立 read-only evidence loading / validation 基础接口，不做 signal ranking、不做 regime 选择、不实现 maker strategy、不接 private/order、不做参数搜索、不 live/default-on/tiny-live、不 promotion。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `docs/thread-playbook.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交任务文件、业务报告、代码/test 修改、task-scoped verification artifacts 和必要 workflow tracking 更新。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0604T004.md`
- `.workflow/reports/0604T004-business.md`
- `examples/hyperliquid/canonical_event_mode_evidence.py`
- `examples/hyperliquid/test_canonical_event_mode_evidence.py`
- `local_live_analysis/canonical_event_mode_evidence_0604T004/**`
- 必要 workflow tracking 文件仅限本任务状态/结论更新。

read-only inputs：
- Canonical source:
  - `local_live_analysis/event_mode_canonical_pricing_signal_0604T003/**`
- Event-mode rerun source paths referenced by the canonical manifest:
  - `local_live_analysis/event_horizon_comparison_0604T002/**`
- Synthetic diagnostic comparison for negative validation only:
  - `local_live_analysis/event_mode_canonical_pricing_signal_0604T003/synthetic_diagnostic_comparison/**`

forbidden：
- New data collection.
- Signal ranking, regime selection, case-library construction, shadow decision generation.
- Hyperliquid or Binance private/account/order endpoints.
- Order lifecycle, submit/cancel/fill logic.
- Strategy implementation, production config/default changes, parameter search, default-on, tiny-live, promotion.
- Connector/core API changes, standard npz schema changes, canonical Binance maker audit schema changes.
- Treating ordinary synthetic fixed-grid samples as canonical short-horizon independent evidence.

action：
- Implement a small read-only canonical evidence loader / validator module.
- The loader must read at least:
  - `multi_sample_manifest.json`
  - `sample_quality_matrix.csv`
  - `feature_horizon_stability_across_samples.csv`
  - `effective_horizon_aliasing_by_sample.csv`
  - `venue_state_conditioning_across_samples.csv`
- The validator must enforce:
  - canonical samples have `decision_mode=event`
  - canonical samples have `canonical_status=canonical_event_mode`
  - `diagnostic_only_synthetic_decision_grid` samples are excluded from the canonical set
  - missing required files/columns fail clearly
  - mixed canonical/diagnostic inputs are allowed only when diagnostics are marked and excluded from canonical outputs
- Generate task-scoped outputs:
  - `canonical_sample_manifest.json`
  - `canonical_sample_quality_summary.csv`
  - `diagnostic_rejection_report.csv`
  - `canonical_evidence_validation_report.md`
- Keep the API narrow enough for later parallel workers:
  - guard hardening worker consumes the same loader
  - signal quality ranking worker consumes the same canonical sample set
  - horizon/regime diagnostics worker consumes the same canonical sample set

acceptance criteria：
- Loader can validate the accepted `0604T003` canonical event-mode aggregate and returns exactly the canonical event-mode samples.
- Loader can parse the `0604T003` synthetic diagnostic comparison but excludes all synthetic fixed-grid samples from canonical outputs.
- Tests cover:
  - accepted event-mode sample
  - rejected synthetic fixed-grid sample
  - mixed canonical/diagnostic manifest
  - missing required column/file error
- Task-scoped artifacts exist and are parseable.
- The module provides a stable command-line entry point or importable API suitable for later parallel runners.
- Boundary flags explicitly remain no new collection, no strategy, no private/order, no parameter search, no live/default-on/tiny-live, no promotion.

verify：
- `python examples/hyperliquid/canonical_event_mode_evidence.py --help`
- `python -m py_compile examples/hyperliquid/canonical_event_mode_evidence.py examples/hyperliquid/test_canonical_event_mode_evidence.py`
- `python -m pytest examples/hyperliquid/test_canonical_event_mode_evidence.py`
- Run the loader on `local_live_analysis/event_mode_canonical_pricing_signal_0604T003/` and write `local_live_analysis/canonical_event_mode_evidence_0604T004/`.
- Run the loader on `local_live_analysis/event_mode_canonical_pricing_signal_0604T003/synthetic_diagnostic_comparison/` and prove canonical sample count is `0`.
- Parse generated JSON manifests with `python -m json.tool`.
- `git diff --check`

done：
- Business report must include implemented API/CLI, canonical sample count, diagnostic rejection count, output artifact paths, verification commands, blockers/caveats if any, commit id, and commit message.
- Business report must explicitly state that this is only a read-only loader/validator foundation for later parallel analysis, and does not authorize signal ranking, regime selection, strategy implementation, private/order endpoints, live/default-on/tiny-live, parameter search, or promotion.
- Business report should end in `待验收` if verification passes.
```
