# 0720T023 OFFICIAL-ORDER-STATUS-BOUNDED-HISTORY-FALLBACK-REPAIR

执行线程：
- 总控 auto-loop / 业务执行线程

任务ID：
- 0720T023

标题：
- Repair official Hyperliquid order-status parsing and bounded reference-history fallback

简短描述：
- Close the T022 QA P1 without weakening terminal proof。
- Parse the official nested `orderStatus` response，continue bounded terminal reconciliation while a reference is unresolved，and use exact-reference `historicalOrders` only as a final authoritative fallback。
- Fix the task-aware human-readable acceptance title。

状态：
- 执行中

执行顺序：
- 当前唯一任务

前置任务：
- `0720T022`（未通过；execution/account safety accepted，buy reference terminal history unresolved）

规则更新提醒：
- 本任务 offline-only。
- No live/private/account/order/cancel/network/remote/service action is authorized。
- T022 evidence is immutable and must remain blocked unless a separately authorized authoritative supplemental fact is collected later。
- Task 8 and every adaptive/multi-level activation remain locked。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `.workflow/reports/0720T022-qa.md`
- `docs/superpowers/plans/2026-07-17-principal-alignment-p0-p3.md`

是否需要提交代码：
- 是

提交要求：
- 实现和测试提交。
- 业务报告单独提交。
- 回报必须带 implementation commit id 和提交信息。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

source：
- Branch: `cross-exchange`
- Dispatch base: `7d8509f6ec94f1c65ef56c061f58045d53763233`

files：
- `examples/hyperliquid/hyperliquid_tiny_live_real_order_executor.py`
- `examples/hyperliquid/hyperliquid_maker_order_manager.py`
- `examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- `examples/hyperliquid/hyperliquid_tiny_live_m2_fill_window.py`
- `examples/hyperliquid/cross_exchange_t024_same_window_acceptance.py`
- `examples/hyperliquid/hyperliquid_tiny_live_sdk_readiness.py`
- Focused tests for the modules above
- `.workflow/tasks/0720T023.md`
- `.workflow/reports/0720T023-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

action：
- Parse the official `orderStatus` contract：
  - top-level `status=order`；
  - nested order envelope has one exact status enum；
  - exact oid/cloid identity must match the managed reference；
  - legacy direct-status test fixtures may remain supported only when structurally unambiguous。
- Keep `unknownOid`、malformed、mismatched、filled-without-raw-fill and query errors fail-closed。
- Do not stop cancel reconciliation merely because account-wide owned open orders are empty while a managed reference remains unresolved。
- Continue bounded direct oid/cloid queries inside the existing five-second terminal reconciliation budget。
- Add one bounded `historical_orders` fallback per unresolved reference after direct status remains unknown：
  - scan only exact oid/cloid matches；
  - reject zero、duplicate、conflicting or malformed matches；
  - accept cancel/reject only from an exact authoritative status row；
  - treat filled as unresolved until raw fill attribution proves quantity、price、fee and role。
- Persist the complete bounded query-attempt audit separately from one canonical terminal result per reference。
- Extend producer and acceptance independent reconstruction for the new canonical query method and exact identity contract。
- Require the official SDK readiness surface to expose `historical_orders` before a future live uses this fallback。
- Derive `validation_report.md` title from `expected_task_id` instead of hard-coding `0718T024`。
- Add positive/adversarial tests for official nested status、legacy direct status、bounded retry、historical exact match、duplicate/conflict/mismatch、persistent unknown and filled-without-raw-fill。

verify：
- Focused executor/manager/watcher/fill/acceptance/readiness tests pass。
- Full `python -m pytest -q -p no:cacheprovider examples/hyperliquid` passes。
- Modified modules/tests pass `py_compile`。
- Acceptance and SDK readiness CLI `--help` pass。
- Persistent `unknownOid` and account-wide absence cannot become terminal success。
- Official nested canceled/rejected statuses and exact historical rows are independently classifiable。
- Query attempt count and fallback call count stay within explicit bounds。
- T022 exact replay remains offline、byte-preserving and blocked for its missing authoritative buy terminal fact；decision remains `43/43`。
- T016 historical replay remains blocked and no historical evidence is synthesized。
- `validation_report.md` uses the externally supplied task id。
- `git diff --check` and implementation `git show --check` pass。
- No live/private/account/order/cancel/network/remote/service action occurs。

done：
- Future cancel-unknown lifecycle has a bounded official-schema terminal recovery path。
- Every accepted terminal result remains exact-reference authoritative and independently reconstructable。
- Full query audit and canonical terminal evidence are separate、bounded contracts。
- T022 historical evidence is not upgraded by inference。
- Independent QA accepts T023 before any private historical read or new bounded live。

stop conditions：
- `unknownOid`、generic cancel error or account-wide absence is treated as terminal success。
- Historical fallback accepts no-match、duplicate、conflicting、foreign or malformed rows。
- Filled status bypasses raw fill proof。
- Retry or historical calls are unbounded。
- Only the final query is preserved with no bounded-attempt audit。
- T022 or T016 historical evidence is rewritten or upgraded。
- Strategy、quote、signal、risk cap、activation or endpoint authorization changes。
- Any verification requires live/private/account/order/cancel/network/remote/service。

blockers：
- Independent QA failure keeps private historical recovery、new bounded live、Task 8 and adaptive/multi-level activation locked。

commit：
- 无

提交信息：
- 无
