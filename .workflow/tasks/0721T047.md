# 0721T047 BOUNDED-DYNAMIC-SPREAD-ACTIVATION

执行线程：
- 总控 auto-loop / 业务执行线程

任务ID：
- 0721T047

标题：
- Enable bounded dynamic spread as the only adaptive quote change

简短描述：
- 在 T046 已通过的 fixed-quote two-sided manager 基线上，增加独立的 bounded dynamic-spread activation path。
- 动态候选必须使用现有 event-time estimator，满足 hard bounds、rate limit、valid-fit 条件；不满足时回退 fixed base。fill feedback、inventory skew、multi-level 和其它报价变量继续关闭。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0721T046`（已通过）

规则更新提醒：
- 当前任务允许一次保守的 exact-envelope tiny-live；必须先完成 source、execution-safety、public/private evidence-integrity 和 same-window replay gate。
- live 是主要数据来源；shadow/replay 只用于防止明显错误、复现 live 决策和控制变量，不作为等待大量样本的前置条件。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `docs/superpowers/plans/2026-07-17-principal-alignment-p0-p3.md`
- `docs/qa-acceptance-report.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交。
- 回报必须带 `commit id` 和提交信息。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

source：
- Dispatch base：`90f7985`
- Live source must be the implementation commit from this task after independent QA-ready local verification.

files：
- Modify: `examples/hyperliquid/cross_exchange_shared_signal_kernel.py`
- Modify: `examples/hyperliquid/cross_exchange_online_estimators.py`
- Modify: `examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- Modify: `examples/hyperliquid/cross_exchange_live_remote_orchestrator.py`
- Modify: focused tests under `examples/hyperliquid/test_cross_exchange_*` and `test_hyperliquid_tiny_live_m2_event_driven_watcher.py`
- Workflow: task、business、QA、fixed QA、`task_plan.md`、`progress.md`、`findings.md`

action：
- Add a separate exact profile and explicit CLI activation flag for bounded dynamic spread.
- Pass the current estimator candidate into the manager quote builder.
- Select the dynamic half-spread only when the candidate is `pass`, bounded, finite and within the configured hard range; otherwise use fixed Task 7 base and record the fallback reason.
- Preserve post-only, inventory eligibility, position/loss/submission caps, two-sided manager lifecycle and kill-switch precedence.
- Record activation, candidate, authoritative half-spread, fixed baseline comparison and actual quote behavior in status and terminal manifests.
- Keep fill feedback, inventory skew, multi-level and all quote-size/edge changes disabled.
- Run one exact-envelope live window only after preflight and account/open-order/kill-switch gates pass.
- Run same-window acceptance/replay and independent checksum reconciliation; do not modify T044 immutable evidence.

verify：
- Focused unit tests for dynamic activation, fallback, bounds, rate limit, post-only and unchanged fixed path.
- Orchestrator parser/preflight/command tests for the new exact profile and activation flag.
- Acceptance tests prove dynamic profile expects activation while legacy fixed profile remains off.
- Full `examples/hyperliquid` regression, `py_compile`, and `git diff --check`.
- One live window: one window, <=1800s, 0.005 BTC/order, 0.01 BTC position, 1 USDC loss, 2 submissions, 2 manager side attempts, 3s hold, 10s wait, Alo, Binance public lead / Hyperliquid lag, no fill feedback/skew/levels.
- Same-window acceptance must fail closed on missing candidate provenance, invalid/fallback candidate claims, quote/config mismatch, lifecycle/evidence mismatch, nonzero final orders/position, checksum mismatch, or any unexpected endpoint boundary.

done：
- QA-ready implementation commit and business report.
- Live artifact root and acceptance output with exact source/runtime/evidence hashes.
- No adaptive component except dynamic spread is enabled; no stable PnL/fill-rate/viability/promotion claim.

blockers：
- Any failed execution-safety, evidence-integrity, same-window replay or final account-state gate stops the task without widening the envelope.

提交信息：
- `Dispatch T047 bounded dynamic spread activation`
