# 0718T020 P2-EXPOSURE-WEIGHTED-FILL-FEEDBACK-OBSERVE-ONLY

执行线程：
- 总控 auto-loop / 业务实现线程

任务ID：
- 0718T020

标题：
- P2 exposure-weighted fill feedback observe-only

简短描述：
- Implement Principal Alignment Task 9 observe-only lifecycle accounting for quote exposure and public/live fill evidence.
- Record bounded feedback offsets without changing Task 7 fixed quotes, Task 8 dynamic candidate activation, submission size, or order envelope.

状态：
- 执行中

执行顺序：
- 当前唯一任务

前置任务：
- 0718T019

规则更新提醒：
- T019 QA accepted event-time estimators and exact replay, but found zero live resting quote exposure.
- This task must preserve that evidence boundary. Rejected/never-resting attempts are not ordinary no-fill observations; short hold, run-end and forced cancel are censored.
- Dynamic spread activation, multi-level and promotion remain unauthorized.

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `docs/superpowers/plans/2026-07-17-principal-alignment-p0-p3.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- Modify: `examples/hyperliquid/cross_exchange_online_estimators.py`
- Modify: `examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- Create: `examples/hyperliquid/test_cross_exchange_fill_feedback.py`

action：
- 每个 quote lifecycle 记录 side、price、distance、level、resting start/end、exposure seconds、submitted/resting/rejected/canceled/expired/partial/full、filled/original quantity、cancel reason、market regime、inventory add/reduce side、fill identity 和 attempt key。
- rejected/never-resting 不计为普通 no-fill；short hold、run end、forced cancel、missing terminal public coverage 标记 censored。
- partial fill 按 filled/original quantity 保留 ratio；重复 lifecycle/fill event 按 identity/attempt key 幂等。
- 从 T018/T019 artifact contract 兼容 manager attempt timing、cancel ack、fill ledger、public resting interval capture；没有 lifecycle facts 时 fail closed，不补造 fill。
- 输出 pooled exposure-weighted arrival/fill statistics；样本不足时 feedback offset unavailable/neutral。
- 生成带 hysteresis、rate limit、min observations、anti-windup 和 version 的 bounded feedback offset candidate；该 candidate 只写 artifact/status，不改变 Task 7 fixed quotes 或 Task 8 dynamic candidate。
- restart 状态恢复只接受经过 checksum/schema 校验的 state；恢复失败回到 neutral。
- kill-switch、risk、toxicity 和 post-only guard 优先；不调用 order/cancel/private endpoint。

verify：
- lifecycle status transition、censoring、partial ratio、duplicate idempotency、attempt key/fill identity、exposure weighted aggregation、restart checksum、hysteresis/rate-limit/anti-windup tests。
- T018/T019 watcher, manager, estimator, fill/replay regression。
- public-only observe-only run writes fill-feedback artifacts with endpoint boundary false。
- deterministic replay reproduces lifecycle rows and feedback candidate.
- modified Python `py_compile`、CLI help、`git diff --check`。

done：
- Report lifecycle schema/semantics, censored evidence counts, feedback candidate boundary, replay determinism and explicit confirmation that actual quote/order behavior was unchanged.

blockers：
- 无

commit：
- 无

提交信息：
- 无
