```md
执行线程：
- 业务线程-research

任务ID：
- 0617T001

标题：
- awsserver1 cross-exchange checkout repair and python3 preflight dry-run

简短描述：
- 修复 `0616T007` 的环境阻塞：不改动远端 Binance maker `master` 路径，在 `awsserver1` 新建独立 `hftbacktest-cross-exchange` checkout 保存当前本地 `cross-exchange` 分支，并使用远端系统 `python3` 执行只读/无交易 preflight。该任务不读取凭证、不调用私有接口、不查询账号、不下单、不撤单、不启动 live bot。

状态：
- 待执行

执行顺序：
- 当前唯一任务

前置任务：
- `0616T007` QA 为 `阻塞`，阻塞原因已确认。

规则更新提醒：
- 请先阅读 `.workflow/workflow-kit/workflow-manual.md`、`.workflow/workflow-kit/task-dispatch-template.md`、`.workflow/workflow-kit/thread-report-template.md`、`.workflow/workflow-kit/qa-acceptance-template.md`、`task_plan.md`、`progress.md`、`findings.md`、`docs/hyperliquid_tiny_live_0616T006_T008_auto_loop.md` 后再执行本任务。
- 本任务只修复远端 checkout / Python preflight，不是 live execution。

按线程规则执行：
- `AGENTS.md`
- `docs/hyperliquid_tiny_live_0616T006_T008_auto_loop.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0617T001.md`
- `.workflow/reports/0617T001-business.md`
- `docs/hyperliquid_tiny_live_0616T006_T008_auto_loop.md`
- `local_live_analysis/hyperliquid_awsserver1_cross_exchange_python3_preflight_0617T001/**`
- `task_plan.md`
- `progress.md`
- `findings.md`

action：
- Preserve `/home/admin/hft_live/hftbacktest` as the remote Binance maker `master` route; do not modify or clean it.
- Create or refresh a separate remote checkout at `/home/admin/hftbacktest-cross-exchange` from the current local `cross-exchange` HEAD.
- Because the local branch is ahead of `origin/cross-exchange`, transfer the current local branch state by a task-scoped git bundle or equivalent deterministic artifact, not by forcing remote `master`.
- Verify the remote checkout branch is `cross-exchange`, the commit matches local HEAD, and the worktree is clean.
- Use remote system `python3` as the selected remote Python; record path, version, and whether required imports for this task are available.
- Create remote dry-run artifacts with no secrets and no private/account/order data.
- Pull artifacts back locally and validate them with `/home/liushuai/workspace/hftbacktest/.conda-envs/hft-py38/bin/python`.
- Preserve fail-closed behavior for host mismatch, branch mismatch, commit mismatch, dirty worktree, missing Python, missing dry-run marker, any private/order/account/live claim, or missing no-order boundary.

acceptance criteria：
- Remote `/home/admin/hft_live/hftbacktest` is not modified.
- Remote `/home/admin/hftbacktest-cross-exchange` exists and is on branch `cross-exchange`.
- Remote checkout commit matches the local `cross-exchange` HEAD used for transfer.
- Remote checkout worktree is clean.
- Remote selected Python is recorded as system `python3`; version and import checks are recorded.
- Artifact pullback and local validation pass.
- All artifacts preserve no-private/no-credential/no-account/no-order/no-cancel/no-live-bot boundary.
- Final recommendation must be exactly one of:
  - `hyperliquid_awsserver1_cross_exchange_python3_preflight_ready_for_qa`
  - `hyperliquid_awsserver1_cross_exchange_python3_preflight_needs_revision`
  - `hyperliquid_awsserver1_cross_exchange_python3_preflight_blocked`

verify：
- `ssh awsserver1 ...` host and checkout checks.
- Remote `python3 --version` and task-scoped import/help checks where available.
- Pullback checksum verification.
- Local JSON validation with `/home/liushuai/workspace/hftbacktest/.conda-envs/hft-py38/bin/python -m json.tool <artifact>.json`.
- Boundary text check for no live/order/private authorization.
- `git diff --check`

done：
- 回报远端新路径、分支、commit、worktree clean 状态、系统 `python3` 路径/版本、artifact pullback、本地验证结果。
- 明确说明没有改动 Binance maker `master` 路径，没有读取凭证、没有调用私有接口、没有账号查询、没有下单/撤单/改单、没有 live bot。
- 明确说明本任务是否解除 `0616T007` 的环境阻塞，以及下一步是否应该重新做 live-capable preflight dry-run QA。
```
