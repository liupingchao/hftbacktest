```md
执行线程：
- 业务线程-research

任务ID：
- 0601T006

标题：
- Binance-led Hyperliquid public multi-sample robustness validation

简短描述：
- 验证 `0601T005` 的 4 个 Binance lead allowlist features 是否跨多个 synchronized public samples 稳定，而不是只在 `0602T001` 单段样本上成立。本任务允许 public-only 同步采集 2-3 个新样本并复用已验收链路生成 multi-sample aggregate artifacts；不实现策略、不接 private/order、不做参数搜索、不 live/default-on/tiny-live、不 promotion。

状态：
- 待执行

执行顺序：
- 当前唯一任务

前置任务：
- `0601T005` QA 已通过。
- `0601T004` QA 已通过。
- `0601T003` QA 已通过。
- `0601T002` QA 已通过。
- `0602T001` QA 已通过，作为现有 synchronized public sample 参考样本。

规则更新提醒：
- 请先阅读规则文档后再执行本任务。
- 本任务是 public-only multi-sample robustness validation，不是 strategy implementation、private/order connector work、parameter search、live authorization 或 promotion。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `docs/thread-playbook.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交任务文件、业务报告、必要 runner/test、task-scoped artifacts 和 workflow tracking 更新。
- 回报必须带 `commit id` 和 `提交信息`。
- 不得触碰 private/order/strategy/live/parameter/default-on/tiny-live/promotion 边界。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0601T006.md`
- `.workflow/reports/0601T006-business.md`
- `examples/hyperliquid/binance_led_multi_sample_robustness.py`
- `examples/hyperliquid/test_binance_led_multi_sample_robustness.py`
- `local_live_analysis/binance_led_hyperliquid_multisample_robustness_0601T006/**`
- New synchronized public sample directories only if collected for this task:
  - `local_live_analysis/cross_exchange_public_sample_0601T006_*/**`
- 必要 workflow tracking 文件仅限本任务状态/结论更新。

read-only inputs：
- Existing accepted sample/reference artifacts:
  - `local_live_analysis/cross_exchange_public_sample_0602T001/**`
  - `local_live_analysis/cross_exchange_lead_lag_join_0601T002/**`
  - `local_live_analysis/cross_exchange_lead_lag_analysis_0601T003/**`
  - `local_live_analysis/binance_led_hyperliquid_data_contract_0601T004/**`
  - `local_live_analysis/binance_led_hyperliquid_pricing_signal_0601T005/**`
- Existing accepted public-only code paths may be reused:
  - `examples/hyperliquid/synchronized_public_collection.py`
  - `examples/hyperliquid/hyperliquid_public_sample.py`
  - `examples/hyperliquid/hyperliquid_raw_alignment.py`
  - `examples/binance_tick_mm/pipeline_live_raw.py`
  - `examples/binance_tick_mm/binance_top5_provenance.py`
  - `examples/hyperliquid/cross_exchange_lead_lag_join.py`
  - `examples/hyperliquid/cross_exchange_lead_lag_analysis.py`
  - `examples/hyperliquid/binance_led_pricing_signal_runner.py`
  - exact script names may be adjusted to the accepted repo names if they differ; report any mismatch explicitly.

sample plan：
- Collect 2-3 new synchronized public-only Binance lead / Hyperliquid lag BTC samples, prioritizing regime diversity:
  - active/high-vol window
  - quiet/low-vol window
  - normal liquidity window
- Preferred duration per sample: enough overlap for the existing join/analyzer/runner chain; target comparable to `0602T001` where practical.
- If a planned regime cannot be collected in the task window, report it as a blocker or caveat; do not substitute private/order or strategy-live data.

forbidden files / areas：
- Hyperliquid private connector, account/order endpoints, order lifecycle, submit/cancel/fill logic.
- Binance private/account/order endpoints, user streams, order lifecycle, submit/cancel/fill logic.
- Binance or Hyperliquid strategy implementation.
- Strategy live, production config/default changes, parameter search, default-on behavior, tiny-live, promotion.
- Standard npz schema, canonical audit schema, connector/core API changes unless a later task explicitly authorizes them.
- Any conclusion that promotes the pricing-signal line beyond read-only research evidence.

action：
- Reuse the accepted public-only synchronized collection chain to collect 2-3 new samples across distinct regimes when feasible.
- For each sample, reuse the existing accepted chain:
  - synchronized public collection
  - `0601T002` style local-observation-time as-of join
  - `0601T003` style lead-lag analysis
  - `0601T005` pricing-signal runner
- Build a task-scoped aggregate robustness runner that consumes the existing `0602T001` sample plus the newly accepted task samples.
- Evaluate whether each of the 4 `0601T004/0601T005` primary allowlist features is stable across samples:
  - `binance_top5_imbalance`
  - `binance_microprice_minus_mid_ticks`
  - `binance_mid_move_ticks_from_prev`
  - `binance_top5_bid_qty`
- Compare feature direction, effect size, support, missingness, effective future-age aliasing, label behavior, and venue-state conditioning across samples.
- Preserve the `0601T005` no-future-leakage policy:
  - decision-time inputs use only current joined rows.
  - future labels remain separated by nominal horizon.
  - all horizon outputs must include `effective_future_age_ms` or equivalent effective-age audit fields.
- Generate task-scoped aggregate outputs:
  - `multi_sample_manifest.json`
  - `sample_quality_matrix.csv`
  - `feature_horizon_stability_across_samples.csv`
  - `effective_horizon_aliasing_by_sample.csv`
  - `venue_state_conditioning_across_samples.csv`
  - `pricing_signal_robustness_recommendation.md`

acceptance criteria：
- At least the existing `0602T001` reference sample plus 2 new public synchronized samples are processed, or the business report explains why the task is blocked/caveated.
- Every included sample has explicit collection/join/analyzer/runner artifact mapping in `multi_sample_manifest.json`.
- Sample quality matrix reports overlap, row counts, missing/future joins, effective horizon aliasing, Hyperliquid cadence constraints, and regime classification.
- Stability artifact reports feature x horizon x label behavior across samples, including direction consistency and effect/support metrics.
- Effective horizon aliasing is reported by sample so nominal `100/250/500ms` style horizons are not misread as independent if they map to the same Hyperliquid future row.
- Venue-state conditioning aggregate reports whether effects are global or only present under specific Hyperliquid venue-state regimes.
- Recommendation is exactly one of:
  - `continue_read_only_runner_refinement`
  - `needs_more_public_samples`
  - `narrow_to_specific_venue_state_regime`
  - `reject_for_runner_design`
- Interpretation rules:
  - If all 4 allowlist features have direction/effect stability across multiple samples, recommend only continued read-only runner refinement.
  - If evidence is still single-sample-like or mixed but not negative, recommend `needs_more_public_samples` or `narrow_to_specific_venue_state_regime`.
  - If cross-sample instability is clear, recommend `reject_for_runner_design` and stop this Binance-led Hyperliquid pricing-signal line.
- Boundary flags explicitly prohibit private/order/live/strategy/parameter/default-on/tiny-live/promotion.

verify：
- Help / import checks for any new aggregate runner.
- Focused pytest for any new aggregate runner tests.
- Existing chain help/tests as needed for touched scripts only.
- Parse every generated JSON manifest with `python -m json.tool`.
- Required artifact existence checks for all six aggregate outputs.
- Reproduction command that writes the aggregate artifacts to a temporary output directory from the collected sample manifests.
- `git diff --check`

done：
- Business report must include collected sample ids, intended regimes, actual overlap windows, artifact paths, row counts, quality classifications, effective-age aliasing findings, cross-sample feature/horizon stability, venue-state conditioning result, final recommendation, verification commands, blockers/caveats if any, commit id, and commit message.
- Business report must explicitly state that the output is public-only read-only robustness evidence and does not authorize strategy implementation, private/order endpoints, live/default-on/tiny-live, parameter search, or promotion.
- Business report must end in `待验收` if local verification completes, or `阻塞` / `未通过` if criteria above cannot be met.
```
