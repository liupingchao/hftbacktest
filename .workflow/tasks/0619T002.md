```md
执行线程：
- 业务线程-research

任务ID：
- 0619T002

标题：
- M2 maker quote placement / size / time-of-day redesign contract

简短描述：
- 基于 `0618T011`、`0618T012` 和 `0619T001` 的已接受/已阻塞证据，重新设计下一步 M2 maker-only fill-acquisition 路线：从“generic flow-aware touch join”切换为“fresh-touch quote placement + dynamic size-by-throughput + session-gated time-of-day”。
- 本任务只做 design-only contract，不改代码、不下单、不刷新 remote checkout、不重跑 final gate；目标是把下一次实现任务的 policy shape 收紧成可直接落地的边界。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- `0618T011` QA 为 `已通过`。
- `0618T012` QA 为 `已通过`。
- `0619T001` QA 为 `阻塞`，且其阻塞原因是 `ledger_no_live_realized_pnl_proof`。

规则更新提醒：
- 请先阅读 `.workflow/workflow-kit/workflow-manual.md`、`.workflow/workflow-kit/task-dispatch-template.md`、`.workflow/workflow-kit/thread-report-template.md`、`.workflow/workflow-kit/qa-acceptance-template.md`、`task_plan.md`、`progress.md`、`findings.md` 后再执行本任务。

按线程规则执行：
- `AGENTS.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0619T002.md`
- `.workflow/reports/0619T002-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

read-only inputs：
- `.workflow/tasks/0618T011.md`
- `.workflow/tasks/0618T012.md`
- `.workflow/tasks/0619T001.md`
- `.workflow/reports/0618T011-qa.md`
- `.workflow/reports/0618T012-qa.md`
- `.workflow/reports/0619T001-qa.md`
- `local_live_analysis/hyperliquid_tiny_live_m2_no_fill_diagnosis_0618T011/**`
- `local_live_analysis/hyperliquid_tiny_live_m2_public_flow_diagnosis_0618T012/**`
- `local_live_analysis/hyperliquid_tiny_live_m2_flow_aware_retry_0619T001/**`
- `examples/hyperliquid/hyperliquid_tiny_live_m2_fill_window.py`
- `examples/hyperliquid/hyperliquid_tiny_live_m2_fill_loop.py`

hard boundary：
- Design only. No strategy code change, no live order, no remote checkout refresh, no final gate rerun, no credential read, no private/account/order endpoint call.
- No taker/crossing, no `Ioc`, no cap relaxation, no size increase, no duration increase, no promotion, no default-on, no stable PnL claim, and no M3 readiness claim.
- Do not pretend there is already enough evidence for a fixed time-of-day schedule. If the evidence is insufficient, the design must explicitly keep that decision gated.

design goal：
- Produce one focused and concrete next-policy contract for a later implementation task under the same or smaller caps.
- The design must answer:
  - how maker quote placement stops joining stale/crowded touch queues;
  - how order size becomes a function of recent visible flow rather than a fixed `0.00999 BTC`;
  - how side selection and time-of-day gating stop the loop from opening low-value attempts;
  - what later implementation and QA must log to prove the new route is actually different from `0619T001`.

required redesign conclusions：
- Explain why the current `0619T001` thresholds are still too loose for fill acquisition:
  - a sell candidate at about `120x` same-side top depth multiple still produced no fill after a full `15s` hold;
  - a buy candidate at about `199x` same-side top depth multiple lost touch and drifted adversely after about `1s`;
  - skipped attempts at about `330x-836x` confirm the loop is still seeing mostly crowded-touch states.
- Define the next quote-placement policy shape:
  - keep `quote_offset_ticks=0` when a quote is allowed;
  - do not use one-tick-back placement as the fill-acquisition repair;
  - require fresh-touch entry rather than generic top-of-book join.
- Define conservative candidate classes:
  - `quality_a`:
    - same-side top depth multiple `<=20x`
    - same-side top order count `<=6`
    - recent same-side strict-through support present
    - touch level age / queue-reset freshness required
    - max hold `<=3s`
  - `quality_b`:
    - same-side top depth multiple `>20x` and `<=100x`
    - same-side top order count `<=12`
    - recent same-side strict-through support present
    - enough recent touch-trade throughput to justify a small passive order
    - max hold `<=1s`
  - any state outside these bands must skip rather than submit.
- Define the next size policy:
  - hard cap `<=0.005 BTC`
  - dynamic size must be `min(bucket_cap, 0.25 * recent_same_side_at_or_through_trade_qty_btc_last_3s, 0.005 BTC)` floored to lot size
  - `quality_a` bucket cap `0.005 BTC`
  - `quality_b` bucket cap `0.002 BTC`
  - if computed size is too small or recent throughput is insufficient, skip instead of forcing a tiny order
- Define the next side policy:
  - default `buy_only`
  - sell is disabled unless both the current precheck and a later hour-by-side scorecard show sell materially better than buy
  - short single-window parity is not enough to re-enable sell after `0618T012` and `0619T001`
- Define the next time-of-day policy:
  - no fixed allowed UTC hour yet
  - next live attempt may execute only inside a current public precheck micro-window that passes the quote-placement and size gates
  - any fixed hour include/exclude decision requires a later cross-hour public scorecard task first
- Define the next loop shape:
  - at most `2` live submissions in a window
  - abort the window when no `quality_a` or `quality_b` candidate appears quickly
  - no more long six-attempt blind retry behavior under deteriorating crowd conditions

required later implementation contract：
- Name the later expected code touch points:
  - `examples/hyperliquid/hyperliquid_tiny_live_m2_fill_window.py`
  - `examples/hyperliquid/hyperliquid_tiny_live_m2_fill_loop.py`
  - focused tests under `examples/hyperliquid/test_hyperliquid_tiny_live_m2_fill_loop.py`
- Name the later required artifacts:
  - `touch_freshness_matrix.csv`
  - `dynamic_size_decision_matrix.csv`
  - `session_side_eligibility.csv`
  - `time_gate_decision_matrix.csv`
  - updated `quote_attempt_matrix.csv`
- Name the later fail-closed conditions:
  - no eligible micro-window
  - missing touch-freshness evidence
  - missing recent throughput evidence
  - sell not re-authorized by evidence
  - any gate/final-open-orders/T008 ledger failure

acceptance criteria：
- The report writes a concrete next-policy shape, not generic prose.
- The design clearly states what changes versus `0619T001`: tighter queue eligibility, shorter holds, smaller dynamic size, buy-default side discipline, and no fixed time-of-day claim yet.
- The design is specific enough that the next implementation task can be created without rediscovering policy bands or artifact requirements.
- The task remains design-only and does not change strategy behavior or live state.

verify：
- `git diff --check`

done：
- 回报 redesign 结论、引用的关键证据、下一步实现任务需要改的模块与 artifacts、验证命令、blockers、commit id、提交信息。
- 明确说明 M2 仍 blocked，且这个 redesign 本身不代表 live fill、PnL、或稳定性证明。
```
