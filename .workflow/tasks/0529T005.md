```md
执行线程：
- 业务线程-python

任务ID：
- 0529T005

标题：
- Stage 9L fill-quality rejection decomposition / bucket coarsening read-only analysis

简短描述：
- 在 `0529T002` Stage 9K 输出基础上，只读拆解 `reject_quality_negative` 与 `needs_more_clean_fills` bucket。判断哪些约束是 hard gate，哪些可降级为 warning / penalty；对 decision-visible trigger buckets 做合并/降维，检查是否出现可进入后续 design-only policy contract 的稳定正质量 bucket。本任务不改策略、不跑 live、不做参数搜索、不 default-on、不 promotion。

状态：
- 待验收

执行顺序：
- 当前唯一任务；`0529T002` QA 已通过后执行。

前置任务：
- `0529T001` QA 已通过。
- `0529T002` QA 已通过，最新 QA 结果见 `docs/qa-acceptance-report.md`。

规则更新提醒：
- 请先阅读规则文档后再执行本任务。
- 本任务是 read-only evidence refinement，不是策略实现、参数搜索、live、tiny-live、default-on 或 promotion 任务。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `docs/thread-playbook.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交 runner/analysis artifacts、tests、workflow/task/report 更新。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

Allowed files：
- `.workflow/tasks/0529T005.md`
- `.workflow/reports/0529T005-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `.workflow/dashboard.html` only if dashboard can be regenerated without mixing unrelated parallel worktree changes
- `.workflow/dispatch_suggestions.md` only if dashboard can be regenerated without mixing unrelated parallel worktree changes
- `examples/binance_tick_mm/fill_quality_rejection_decomposition.py`
- `examples/binance_tick_mm/test_fill_quality_rejection_decomposition.py`
- `local_live_analysis/stage9l_fill_quality_rejection_decomposition_0529T005/**`

Allowed read-only inputs：
- `.workflow/tasks/0529T002.md`
- `.workflow/reports/0529T002-business.md`
- `.workflow/reports/0529T002-qa.md` after QA exists
- `docs/qa-acceptance-report.md` after `0529T002` QA
- `docs/binance_tick_mm_next_policy_design.md`
- `local_live_analysis/stage9k_fill_quality_bucket_synthesis_0529T002/**`
- Existing current-format sample directories under `local_live_analysis/**`
- Existing Stage 5 / Stage 5C / Stage 6 / Stage 9 artifacts under `local_live_analysis/**`
- Existing Binance maker analysis source files under `examples/binance_tick_mm/**` for read-only reference.

Forbidden files / areas：
- Strategy behavior, quote placement implementation, fair/target formula, inventory logic, risk guards, live configs, production defaults.
- Any live/tiny-live run, remote deploy, parameter search, default-on behavior, guard relaxation, or promotion claim.
- Reopening compact audit work, replay lifecycle semantics, queue/touch repair, exact queue position, full L2 proof, connector/core API changes.
- Treating future fill, future markout, future spread capture, fill outcome, same-sample PnL feedback, exact queue position, or hidden queue assumptions as strategy trigger inputs.
- Treating caveated samples as strict-clean proof.

Problem statement：
- `0529T002` found `314` clean-only decision-visible trigger buckets:
  - `0` `ready_for_policy_design`
  - `68` `needs_more_clean_fills`
  - `246` `reject_quality_negative`
- Controller inspection of the Stage 9K aggregate CSV shows all `246` `reject_quality_negative` clean trigger buckets are non-`churn_normal`:
  - `recent_reject_or_throttle`: `134`
  - `fast_cancel_churn`: `73`
  - `reject_throttle_or_drop`: `26`
  - `cancel_readd_pressure`: `13`
- A rough controller sanity check that treats churn as warning rather than a hard gate still produced no ready candidate: about `309` buckets remained `needs_more_clean_fills`, `5` became markout rejects, and `0` became ready.
- Therefore the key question is not only whether `reject_throttle_churn_bucket` is too coarse. The task must determine whether decision-visible coarsening can create stable positive-quality buckets, or whether the current fill-quality direction should remain rejected / needs targeted clean-fill collection.
- Stage 9K aggregate CSVs are not sufficient by themselves for coarsening because they do not preserve sample identity inside merged buckets. Coarsened `sample_count` and `fill_sample_count` must be computed from sample-level observed submit rows.

Implementation scope：
1. Add a read-only CLI analysis runner:
   - `python examples/binance_tick_mm/fill_quality_rejection_decomposition.py --help`
   - default input: `local_live_analysis/stage9k_fill_quality_bucket_synthesis_0529T002/`
   - default output: `local_live_analysis/stage9l_fill_quality_rejection_decomposition_0529T005/`
2. Reconstruct Stage 9L analysis rows from existing read-only inputs:
   - read Stage 9K `run_manifest.json` for sample list, caveated sample ids, thresholds, and boundary metadata
   - reuse or import Stage 9K sample discovery / row synthesis helpers where practical, instead of duplicating incompatible bucket logic
   - recompute clean-only / accepted-set observed submit rows from Stage 5 labels, Stage 5 markouts, Stage 5C safety diagnostics, and live audit fields
   - preserve `sample_id` and caveated status so coarsened `sample_count` and `fill_sample_count` are correct
   - do not rely only on Stage 9K aggregate CSVs for any coarsened verdict
3. Decompose `reject_quality_negative` buckets into concrete components:
   - adverse 5s markout
   - weak / negative spread capture
   - high fill-after-cancel sensitivity
   - reject / throttle / drop
   - fast cancel churn
   - cancel/readd pressure
   - recent reject/throttle counters
   - stale / anchor / post-only safety context
4. Evaluate whether `reject_throttle_churn_bucket` is too coarse:
   - compare hard-gate behavior vs warning / penalty behavior
   - keep true reject/drop and post-only risk as hard-gate candidates by default
   - report whether recent reject/throttle counters, fast cancel churn, cancel/readd pressure, and min-move/churn diagnostics can be demoted to warning / penalty without creating poor-quality candidates
   - do not describe any demotion as production guard relaxation; this is an offline evidence sensitivity only
5. Coarsen only decision-visible trigger dimensions:
   - merge edge buckets where justified
   - merge latency/stale buckets only when market-view safety remains explicit
   - merge quote-distance buckets only within decision-visible quote-distance families
   - optionally collapse `reject_throttle_churn_bucket` into documented hard-risk / warning-risk / normal variants for sensitivity
   - keep true reject/drop and post-only risk visible as separate safety context unless the runner explicitly classifies them as hard gates
   - do not use `fill_after_cancel_bucket` or future outcome fields as trigger dimensions
6. Re-evaluate Shape A and Shape B candidate tables under documented coarsening variants.
7. Produce sample/regime gap recommendations if no candidate becomes ready:
   - if coarsened buckets are positive-quality but under-filled, recommend targeted clean-fill collection by decision-visible regime
   - if coarsened buckets remain quality-negative after warning/penalty sensitivity, recommend rejecting the current fill-quality direction
   - if evidence is mixed or unstable across samples, classify as `not_decisionable`

Required outputs：
- `run_manifest.json`
- `rejection_reason_decomposition.csv`
- `churn_gate_sensitivity.csv`
- `coarsened_trigger_bucket_metrics.csv`
- `coarsened_shape_candidates.csv`
- `sample_gap_by_regime.csv`
- `stage9l_recommendation.md`
- `.workflow/reports/0529T005-business.md`

Required runner design details：
- `run_manifest.json` must record the Stage 9K input directory, Stage 9K manifest path, sample ids, caveated sample ids, thresholds, coarsening variants, and read-only boundary flags.
- `rejection_reason_decomposition.csv` should include both bucket counts and row/fill mass by rejection component, not only a text reason list.
- `churn_gate_sensitivity.csv` should compare at least:
  - original Stage 9K hard-gate behavior
  - recent reject/throttle as warning
  - fast-cancel churn / cancel-readd pressure as warning
  - all non-true-reject churn diagnostics as warning while true reject/drop and post-only risk remain hard gates
- `coarsened_trigger_bucket_metrics.csv` must recompute sample/fill coverage from row-level observed submits, not by summing Stage 9K bucket `sample_count`.
- `coarsened_shape_candidates.csv` must state the coarsening variant and candidate shape for every candidate row; empty output is acceptable if no candidate qualifies.
- `sample_gap_by_regime.csv` must identify which decision-visible regimes need clean fills if the final classification is `needs_targeted_clean_fills`.

Required final classifications：
- `ready_for_policy_design_after_coarsening`
- `needs_targeted_clean_fills`
- `reject_current_fill_quality_direction`
- `not_decisionable`

Acceptance criteria：
- Runner is read-only and reproducible from CLI.
- Focused tests cover rejection decomposition, churn-gate sensitivity, and decision-visible coarsening.
- Focused tests verify that coarsened sample coverage is computed from row-level `sample_id`, not by summing pre-aggregated Stage 9K rows.
- The report states whether any Shape A / Shape B candidate becomes ready after coarsening.
- If no candidate becomes ready, the report identifies whether the next action is targeted clean-fill collection or rejection of the current fill-quality direction.
- If churn diagnostics are demoted from hard gate to warning in any sensitivity variant, the report must state whether that alone creates ready candidates; based on controller pre-check, do not assume it will.
- The report explicitly states no strategy behavior, live behavior, fill/cancel replay semantics, parameter search, default-on, guard relaxation, tiny-live, or promotion was changed.

verify：
- `python examples/binance_tick_mm/fill_quality_rejection_decomposition.py --help`
- `python -m pytest examples/binance_tick_mm/test_fill_quality_rejection_decomposition.py -q`
- Run the Stage 9L runner on `local_live_analysis/stage9k_fill_quality_bucket_synthesis_0529T002/`
- `git diff --check`
- `python3 .workflow/build_dashboard.py` only if unrelated parallel worktree changes are absent; otherwise report the isolation-preserving skip.

done：
- Business report ends in `待验收`.
- Report lists exact input artifacts, generated files, key verdict counts, coarsening variants, and next recommended task.
- Report states whether constraints can be relaxed safely or should remain hard gates.
- Report includes `commit id` and `提交信息`.
```
