# 0722T048 DYNAMIC-ACCEPTANCE-CONTRACT-REPAIR

执行线程：
- 总控 auto-loop / 业务执行线程

任务ID：
- 0722T048

标题：
- Repair dynamic-profile same-window acceptance contract

简短描述：
- 修复 T047 暴露的 offline same-window acceptance verifier 缺陷。
- 动态 profile 必须接受 `two-sided-dynamic-manager` 和显式
  `--enable-dynamic-spread`；legacy fixed profile 仍必须拒绝动态 flag。

状态：
- 执行中

执行顺序：
- 当前唯一任务

前置任务：
- 0721T047（live 已完成，same-window acceptance 因 verifier contract 未通过）

规则更新提醒：
- 本任务 offline-only；不得启动第二个 live window，不得访问 private/account/network/remote/service/order/cancel。
- T047 remote run root 保持 sealed；post-live account proof 作为 run 外附加证据保存。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `AGENTS.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交。
- 回报必须带 `commit id` 和提交信息。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `examples/hyperliquid/cross_exchange_t024_same_window_acceptance.py`
- `examples/hyperliquid/test_cross_exchange_t024_same_window_acceptance.py`
- workflow task/business/QA/fixed QA records

action：
- 让 canonical watcher grammar 按 expected dynamic activation 条件允许且只允许显式动态 flag。
- 让 exact envelope profile expectation 在 fixed/dynamic acceptance mode 间条件化。
- 保留 fixed profile 对动态 flag 的 fail-closed 行为。
- 在 T047 local sealed run 视图中重跑 checksum、estimator replay、fill-feedback replay 和 same-window acceptance。
- post-live account proof 不纳入已封存 terminal manifest，只作为 run 外独立终态证据。

verify：
- focused acceptance tests；
- `python -m pytest examples/hyperliquid/test_cross_exchange_t024_same_window_acceptance.py -q`；
- T047 same-window acceptance expected dynamic profile；
- fixed-profile regression；
- `py_compile` 和 `git diff --check`；
- 不执行 live/private/account/network/remote/service/order/cancel。

done：
- acceptance 对 T047 返回机制/证据完整性通过；
- dynamic activation provenance、candidate/fallback contract 和所有其它 adaptive flags 仍符合预期；
- 记录 T047 的 `110/110` checksum、estimator/fill-feedback snapshot match、独立 post-live account proof；
- 不宣称稳定 PnL、fill-rate、maker viability、promotion 或 multi-level unlock。

提交信息：
- `Repair dynamic-profile same-window acceptance contract`
