# 0722T055 P3-MULTI-LEVEL-MUTATION-AND-STATUS-IDENTITY-REPAIR

执行线程：
- 总控 auto-loop / 业务执行线程

任务ID：
- 0722T055

标题：
- Repair pre-submit mutation ordering and watcher status identity

简短描述：
- 修复 T054 独立 QA 的两个局部 P1：最终 per-submit halt gate 前不得
  分配 generation；所有 ladder-aware watcher status 必须由同一 ladder
  config 派生 config hash 和 multi-level snapshot。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- `0722T054`（未通过；implementation
  `4db5fb6f061106d13f5cab16269aa1bd58926507`，QA records `ae7b564`）

规则更新提醒：
- offline-only；禁止 live、private/account、真实 order/cancel、network、
  remote、service 和 cap expansion。
- 所有 endpoint tests 仅允许 fake client。
- Standing envelope 保持 `0.005 BTC/order`、`0.01 BTC position`、
  `1 USDC loss`、`2 submissions/window`、`1800s/window`。
- Multi-level live、经济性、promotion 和完整 restart readiness 均不得宣称。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交。
- 回报必须带 exact implementation `commit id` 和提交信息。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `examples/hyperliquid/hyperliquid_maker_order_manager.py`
- `examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- `examples/hyperliquid/test_hyperliquid_maker_order_manager.py`
- `examples/hyperliquid/test_hyperliquid_tiny_live_m2_event_driven_watcher.py`
- workflow task/business/QA/fixed QA records

action：
- `_submit()` 的 generation allocation/commit 必须发生在最终
  `before_submit` callback 成功之后。Callback failure 对 generation、
  order、submission 和 endpoint state 必须零 mutation。
- 为 watcher 提供由 exact `QuoteLadderConfigV1` 派生的统一
  multi-level status snapshot。
- 所有使用 ladder-aware `task7_config_hash` 的 startup、disconnect、
  candidate waiting 和 terminal status 必须携带 matching snapshot。
- 保持 normal cycle 已有 quote-result snapshot、default-off single-level、
  active/adaptive 互斥、standing cap 和 restart fail-closed 行为不变。

verify：
- Hostile second-halt：第二个 addition 被 callback 阻止后，第二个 logical
  key 不得存在于 `generation_by_key`，且 order calls/submissions 均保持 `1`。
- Hostile status identity：active `levels=2` config 的 early status 同时报告
  matching config hash、`requested_levels=2`、activation 和 prerequisite。
- 检查每个 ladder-aware `task7_config_hash` call site 都传入 matching
  snapshot。
- 运行 focused manager/ladder/event watcher、related watcher/shared/executor、
  full `python -m pytest examples/hyperliquid -q`、py_compile 和
  `git diff --check`。

done：
- 两个 T054 QA P1 有精确 hostile regression 并修复。
- Callback failure 保证 proposed addition 的所有 mutable state 不变。
- Watcher lifecycle evidence 的 config hash 与 multi-level snapshot 具有同源
  identity。
- 经独立 QA 接受后，Principal Task 10 可关闭为 code/action-path
  readiness；multi-level live 继续保持锁定。

blockers：
- 若修复需要扩大 submission cap、执行 live 或改变已接受的 T052 单层
  baseline，则本任务必须 fail-closed；不得自行授权。

提交信息：
- `Preserve multi-level halt and status identity`
