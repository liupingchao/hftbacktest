```md
执行线程：
- 业务线程-python

任务ID：
- 0529T001

标题：
- Fill-quality-first maker edge synthesis and next policy design

简短描述：
- 结合 `0526T004`、`0526T005`、`0528T001`、`0528T002` 以及 Stage 5/6/9 findings，重新整理 Binance maker 下一步策略研究方向：从“调规则参数 / inventory 直接驱动 quote placement”切换为“先识别正质量成交状态，再设计 quote policy”。本任务只做 planning/read-only synthesis，不实现策略、不跑 live、不做参数搜索。

状态：
- 已通过

执行顺序：
- 当前唯一任务；正式派发后执行。

前置任务：
- `0526T004` QA 已通过。
- `0526T005` QA 已通过。
- `0528T001` QA 已通过。
- `0528T002` QA 已通过。

规则更新提醒：
- 请先阅读规则文档后再执行本任务。
- 本任务是 planning/read-only synthesis，不是策略实现、参数搜索、live、tiny-live 或 promotion 任务。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `docs/thread-playbook.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 任务执行后提交 task/report/plan/progress/findings/dashboard 更新。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

Allowed files：
- `.workflow/tasks/0529T001.md`
- `.workflow/reports/0529T001-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `.workflow/dashboard.html`
- `.workflow/dispatch_suggestions.md`
- `docs/binance_tick_mm_next_policy_design.md`
- `local_live_analysis/stage9j_fill_quality_policy_synthesis_0529T001/**`，仅允许 read-only synthesis 输出。

Allowed read-only inputs：
- `.workflow/reports/0525T001-qa.md`
- `.workflow/reports/0526T004-qa.md`
- `.workflow/reports/0526T005-qa.md`
- `.workflow/reports/0526T006-qa.md`
- `.workflow/reports/0528T001-qa.md`
- `.workflow/reports/0528T002-qa.md`
- `findings.md`
- `task_plan.md`
- Existing Stage 5 / Stage 6 / Stage 9 artifacts under `local_live_analysis/**`
- Existing runner source files under `examples/binance_tick_mm/**` for read-only reference only.

Forbidden files / areas：
- Strategy behavior, quote placement implementation, fair/target formula, inventory logic, risk guards, live configs, production defaults.
- Any live/tiny-live run, remote deploy, parameter search, default-on behavior, guard relaxation, or promotion claim.
- Reopening `0528T002` compact audit work unless a concrete regression is found.
- Continuing to tune the rejected `0528T001` fixed inventory-aware quote placement skeleton as if it only needed parameters.
- Continuing the current `0526T004` min-move projected-suppression grid as the main route without a new design reason.
- Queue/touch residual repair, exact queue position, full L2 proof, connector/core API changes.

Problem statement：
- Recent evidence says the next blocker is not audit input scaling and not one more parameter sweep.
- `0528T002` fixed Stage 6 input bloat; it does not create strategy edge.
- `0528T001` rejected the fixed inventory-aware quote placement skeleton: request buckets increased fills but produced worse fill quality, with worse 5s markout and spread capture than no-change buckets.
- `0526T004` found no promising `min_move_quote_age_churn_guard` parameter sets.
- Therefore the next useful task is to synthesize evidence around fill quality first: identify states where fills are good enough, then design the next quote policy shape around those states.

Task goals：
1. Summarize recent accepted facts from `0526T004`, `0526T005`, `0526T006`, `0528T001`, and `0528T002`.
2. Clearly list rejected / deprioritized tracks:
   - fixed `inventory_aware_quote_placement_request` skeleton from `0528T001`
   - current `min_move_quote_age_churn_guard` projected-suppression grid from `0526T004`
   - immediate tiny-live / default-on / promotion
   - continued work on audit bloat after `0528T002`, unless regression appears
3. Define a fill-quality-first policy-design contract:
   - trigger base should come from positive fill-quality buckets, not raw fill count or inventory state alone
   - inventory should be a risk / sizing / side-pressure context, not the direct quote-placement trigger
   - quote distance should define participation frontier
   - stale / latency / post-only should remain safety filters
   - exact queue position and hidden queue assumptions must not be required
4. Identify at most `1-2` next candidate policy shapes for later implementation tasks.
   - For each policy shape, specify:
     - trigger conditions
     - inventory role
     - quote-distance role
     - safety filters
     - labels / metrics required for validation
     - why it is materially different from the rejected `0528T001` fixed skeleton
5. Define what evidence each candidate must show before strategy implementation:
   - clean-only fill mass
   - side-adjusted markout
   - spread capture
   - fill-after-cancel sensitivity
   - quote-distance / latency / stale strata stability
   - inventory recovery or inventory damage
   - no degradation in post-only / reject / throttle / churn safety
6. Recommend the immediate next task after this one:
   - either a read-only fill-quality bucket synthesis runner, or
   - a design-only next-policy contract, depending on what evidence already exists.

Expected outputs：
- A concise design / synthesis document at `docs/binance_tick_mm_next_policy_design.md`.
- Optional small CSV / JSON summaries under `local_live_analysis/stage9j_fill_quality_policy_synthesis_0529T001/` if the executor needs to aggregate existing artifacts.
- Do not add a new formal runner in this task. Lightweight aggregation from existing artifacts is allowed; if evidence is insufficient, output the later runner requirements instead.
- `.workflow/reports/0529T001-business.md` ending in `待验收`.
- Updated `task_plan.md`, `progress.md`, and `findings.md` with the accepted next-direction summary.

Acceptance criteria：
- The report explains why the next step should be fill-quality-first rather than parameter tuning or direct inventory placement.
- The report explicitly states what was rejected and why.
- The report proposes at most `1-2` concrete next policy shapes or says there is not enough evidence and specifies the missing read-only evidence.
- The report does not authorize implementation, live, parameter search, default-on, guard relaxation, tiny-live, or promotion.
- If optional aggregation is run, it must use existing local artifacts only and must be reproducible from command lines in the report.

verify：
- Required static / workflow checks:
  - `python3 .workflow/build_dashboard.py`
  - `git diff --check`
- If code or aggregation scripts are added, add focused tests or a reproducible command and report the result.
- No live command.
- No remote deploy.
- No parameter sweep.

done：
- Business report ends in `待验收`.
- Report includes the next-policy recommendation and rejected tracks.
- Report states exact files read and any artifacts generated.
- Report explicitly states no strategy behavior, live behavior, fill/cancel replay semantics, parameter search, default-on, guard relaxation, or promotion was changed.
```
