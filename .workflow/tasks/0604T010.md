```md
执行线程：
- 业务线程-research

任务ID：
- 0604T010

标题：
- Live safety same-count open-order drift case matrix and root-cause diagnosis

简短描述：
- 系统性检查 `evaluate_live_safety()` 是否漏检“REST 与本地 open order 数量相同但订单内容不同”的 open-order drift。目标是枚举该 bug 的所有主要 case，逐类定位、复现、判断是否被判成 `ok`，并找到共同根本原因。本任务只做诊断/复现/根因定位，不做修复、不改策略行为。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- 无

规则更新提醒：
- 请先阅读规则文档后再执行本任务。
- 本任务只做诊断/根因定位；不得修复代码、不得改 live safety 行为、不得启动 live、不得做默认配置变更。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 不需要

提交要求：
- 不需要提交
- 回报必须带 `commit：无` 和 `提交信息：无`

是否进行QA验收：
- 否

QA参与：
- 否

QA验收方式：
- 无

QA验收线程动作：
- 无

files：
- `.workflow/tasks/0604T010.md`
- `.workflow/reports/0604T010-business.md`
- `examples/binance_tick_mm/strategy_core.py`
- `examples/binance_tick_mm/live_tick_mm.py`
- `examples/binance_tick_mm/test_backtest_tick_mm.py`
- 必要时只读检查 `examples/binance_tick_mm/audit_schema.py`、`examples/binance_tick_mm/compare_audit.py`、相关 live safety / audit 文档。
- 可在报告中附只读复现矩阵和一次性 Python 片段输出；不得把复现脚本落盘为新代码文件。

forbidden：
- 不修复 `evaluate_live_safety()` 或任何 live safety 逻辑。
- 不新增/修改测试。
- 不修改策略、connector、audit schema、生产配置或默认参数。
- 不启动 live / tiny-live / default-on。
- 不做参数搜索、回放实验扩展、promotion 或任何行为变更。
- 不静默扩大到其他 live safety 风险项。

action：
- 只读检查当前 review 是否属实，并把检查范围扩展为完整 same-count drift case matrix：
  - `evaluate_live_safety()` 是否只在 `rest_open_order_count != local_open_order_count` 时进入 open-order mismatch 分支。
  - `open_order_diff` 非空但 count 相同的情况下是否仍可能返回 `ok`。
  - live 主循环中 `open_order_mismatch_count`、`fail_on_mismatch`、audit row 写入与该状态的关系。
  - 现有测试是否覆盖 count 相同但 side/price/qty 不同的 drift。
- 枚举并逐类复现该 bug 的主要 case，至少覆盖：
  - 单订单同 count、方向不同：local bid vs REST ask，或反向。
  - 单订单同 count、价格 tick 不同。
  - 单订单同 count、数量不同。
  - 单订单同 count、方向相同但价格和数量同时不同。
  - 双订单同 count、其中一张漂移：例如 bid 正常、ask 价格/数量/方向漂移。
  - 双订单同 count、两边都是 2 张但集合不同：local-only 与 rest-only 同时存在。
  - 本地和 REST 各 1 张但 client/order id 不同、quote key 不同，确认不是正常 random client id 映射场景。
  - `open_order_diff()` 归一化 key 为空/非空的边界：确认 quote key 相同且 id 不同时不应误报，quote key 不同时应能产生 diff。
  - 如代码支持状态字段进入 key 或诊断，检查 status / leaves / executed qty / cancel requested 等字段是否会影响 diff；如果不会，报告要明确这是当前 diff 表达能力边界，而不是本任务 silently 修复的范围。
- 对每个 case 输出复现矩阵，至少包含：
  - case 名称。
  - local open orders 摘要。
  - REST open orders 摘要。
  - local/rest count。
  - `open_order_diff` 是否非空。
  - `evaluate_live_safety()` 返回的 `safety_status` / `safety_detail`。
  - 是否触发 `open_order_mismatch_count` 累计。
  - 是否可能触发 `fail_on_mismatch` 停止路径。
  - 判定：漏检 / 非漏检 / 不适用。
- 定位根本原因：
  - 判定条件是否只使用数量差异作为 mismatch gate。
  - `open_order_diff` 是否只作为诊断字段保留而未参与 safety status 判定。
  - confirmation / grace 机制是否依赖 status 进入 mismatch/pending，导致同数量内容 drift 不累计确认。
  - `open_order_diff()` 的归一化 key 能识别哪些内容漂移，不能识别哪些内容漂移；把“diff 已能识别但 safety 未消费”和“diff 本身不能表达”的根因分开记录。
  - live safety 的 audit 写入是否会留下 diff 证据，但不改变风控决策；如会，说明这是“可观测但不阻断”的 silent failure。
- 输出诊断报告，说明：
  - review 结论是否成立。
  - 完整 case matrix 与每类复现结果。
  - 风险影响路径。
  - 共同根因和分支根因。
  - 建议的后续修复任务范围，但不得在本任务内实施修复。

acceptance criteria：
- 报告必须给出明确结论：review 属实 / 不属实 / 部分属实。
- 如属实，报告必须引用具体代码位置说明为什么同数量内容 drift 会绕过 safety status。
- 报告必须包含 same-count drift case matrix，且至少覆盖 action 中列出的 case；每个 case 都必须有复现输入、实际 status、是否漏检的判断。
- 报告必须区分两类根因：
  - `open_order_diff` 已经识别出 diff，但 `evaluate_live_safety()` 没有消费 diff。
  - `open_order_diff` 自身没有能力表达某类订单内容变化。
- 报告必须覆盖 live 主循环如何处理该状态，以及是否会触发 `open_order_mismatch_count` 和 `fail_on_mismatch`。
- 报告必须说明现有测试覆盖缺口。
- 报告必须给出后续修复任务建议边界，包括建议修复应覆盖的回归测试 case 列表，但不包含任何代码修改。

verify：
- 只读代码检查。
- 可运行一次性 Python 复现片段或现有 focused pytest；如运行，报告必须记录命令与结果。
- 复现片段应批量覆盖 case matrix，而不是只覆盖单个 bid/ask 例子。
- 不允许新增测试或修改代码。

done：
- 业务报告写入 `.workflow/reports/0604T010-business.md`。
- 回报必须说明结论、case matrix 覆盖情况、逐类复现结果、共同根本原因、分支根因、影响路径、测试缺口、建议后续修复范围。
- 状态应为 `待验收` 或 `阻塞`，但本任务当前不进入 QA。
- 本任务按用户要求已派发并执行；本轮不进入 QA。
```
