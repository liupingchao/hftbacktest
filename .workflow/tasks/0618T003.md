```md
执行线程：
- 业务线程-research

任务ID：
- 0618T003

标题：
- Hyperliquid token location scan in XEMM repo

简短描述：
- 在 `awsserver1` 的 `XEMM` repo 候选文件中定位 Hyperliquid 凭证 / token 所在文件，只记录文件路径、字段名和脱敏确认，不返回 token 本文。
- 候选文件限定为这四个路径：
  - `/home/admin/XEMM_rust/.env`
  - `/home/admin/XEMM_rust/config.json`
  - `/home/admin/XEMM_rust_latest/.env`
  - `/home/admin/XEMM_rust_latest/config.json`

状态：
- 待执行

执行顺序：
- 当前唯一任务

前置任务：
- 无

规则更新提醒：
- 请先阅读 `.workflow/workflow-kit/workflow-manual.md`、`.workflow/workflow-kit/task-dispatch-template.md`、`.workflow/workflow-kit/thread-report-template.md`、`.workflow/workflow-kit/qa-acceptance-template.md`、`task_plan.md`、`progress.md`、`findings.md` 后再执行本任务。

按线程规则执行：
- `AGENTS.md`

是否需要提交代码：
- 不需要

提交要求：
- `commit：无`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0618T003.md`
- `.workflow/reports/0618T003-business.md`
- `local_live_analysis/hyperliquid_token_location_scan_0618T003/**`
- `/home/admin/XEMM_rust/.env`
- `/home/admin/XEMM_rust/config.json`
- `/home/admin/XEMM_rust_latest/.env`
- `/home/admin/XEMM_rust_latest/config.json`

action：
- 只检查这四个候选文件，定位 Hyperliquid token / 凭证实际所在文件。
- 只记录文件路径、字段名、环境变量名或 JSON key 名，不记录 token 值本身。
- 如果多个候选文件命中，分别列出命中的路径和字段名；如果没有命中，明确说明未找到。
- 不修改候选文件内容，不把 token 写入 repo、日志、artifact 或聊天回报。

acceptance criteria：
- 四个候选文件都已检查。
- 明确给出实际包含 token / 凭证的文件路径，或明确报告未找到。
- 回报中没有任何 token 本文、私钥、本质凭证值或可直接复用的密文片段。
- 不新增任何仓库内凭证文件。
- 不调用任何与 live trading 无关的额外 Hyperliquid 私有接口。

forbidden：
- 不要返回 token 本文。
- 不要把 token 写入任务文件、报告、日志、artifact、shell 历史或提交内容。
- 不要修改这四个候选文件。
- 不要凭空推断文件位置，必须以实际扫描结果为准。

verify：
- 扫描这四个候选文件并记录命中位置。
- 用脱敏结果复核回报中没有 token 本文。
- `git diff --check`

done：
- 回报实际命中的文件路径、字段名 / key 名、脱敏确认和未命中说明。
- 明确说明没有返回 token 本文。
- 明确说明本任务是否修改了候选文件；预期应为没有。
```
