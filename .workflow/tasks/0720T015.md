# 0720T015 EXACT-ENVELOPE-DURATION-CONTRACT-ALIGNMENT

执行线程：
- 总控 auto-loop / 业务执行线程

任务ID：
- 0720T015

标题：
- Align exact-envelope duration authorization and acceptance to the standing 1800-second cap

简短描述：
- Remove the historical `900s` hard coupling from the exact two-sided live contract.
- Keep duration externally authorized and exact per task while permitting any positive bounded value up to the standing `1800s` maximum.

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- `0720T014`（已通过）

规则更新提醒：
- 本任务是 offline-only duration-contract repair。
- 不得调用 live、private、account、order、cancel、network、remote 或 service。
- 不得修改 signal freshness、edge threshold、quote formula、risk cap、activation、manager lifecycle 或 evidence identity 语义。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `.workflow/reports/0720T014-qa.md`
- `docs/superpowers/plans/2026-07-17-principal-alignment-p0-p3.md`

是否需要提交代码：
- 需要

提交要求：
- 先提交 implementation，再提交 business report。
- 回报必须带 implementation `commit id`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- Modify: `examples/hyperliquid/cross_exchange_live_remote_orchestrator.py`
- Modify: `examples/hyperliquid/test_cross_exchange_live_remote_orchestrator.py`
- Modify: `examples/hyperliquid/cross_exchange_t024_same_window_acceptance.py`
- Modify: `examples/hyperliquid/test_cross_exchange_t024_same_window_acceptance.py`
- Workflow: this task, business report, QA report, `docs/qa-acceptance-report.md`, `task_plan.md`, `progress.md`, `findings.md`

action：
- Replace the historical exact-envelope `900s` maximum with the standing authorization maximum `1800s`.
- Preserve exact task binding:
  - orchestrator accepts only one positive window no longer than `1800s`;
  - acceptance receives an external `expected_window_seconds`;
  - preflight and runtime watcher argv must both equal that external value;
  - artifact self-report cannot authorize a different duration.
- Default the acceptance expected duration to `900s` for deterministic historical replay compatibility.
- Add CLI `--expected-window-seconds` for new live tasks.
- Add focused regressions proving:
  - exact two-sided `1800s` preflight passes without starting watcher/private work;
  - duration above `1800s` fails before output or endpoint work;
  - a synchronized valid acceptance fixture passes with external expected `1800s`;
  - artifact/preflight/runtime `900s` fails when external expected is `1800s`;
  - T011 remains replayable with expected `900s`.

verify：
- Focused orchestrator and Task 12 acceptance tests pass.
- Full `python -m pytest -q examples/hyperliquid` passes.
- Modified modules/tests pass `py_compile`.
- Orchestrator and acceptance CLI `--help` pass.
- Exact local preflight for a future `1800s` two-sided-manager window passes and records no watcher/private/order/cancel activity.
- A `1800.001s` exact preflight fails closed before writing the requested preflight artifact.
- T011 byte-exact replay with explicit legacy bridge and `--expected-window-seconds 900` remains `validation_reasons=[]`, provenance `112/112`, lifecycle blocked.
- `git diff --check` passes.
- No live/private/account/order/cancel/network/remote/service action occurs.

done：
- The exact live gate and acceptance agree on the standing `1800s` maximum.
- Every task still supplies one exact external duration; artifacts cannot self-authorize it.
- Historical `900s` evidence remains deterministically replayable.
- Independent QA accepts T015 before the next live task is dispatched.

stop conditions：
- Fix requires changing strategy formula, signal freshness, edge threshold, risk cap, activation or producer quote behavior.
- Acceptance can pass a duration different from the external expected value.
- Orchestrator accepts a duration above `1800s`.
- T011 loses deterministic replay or becomes lifecycle accepted.
- Any verification requires live/private/account/order/cancel/network/remote/service.

blockers：
- Independent QA failure keeps the next live and multi-level locked.

commit：
- `35ceea2a5ca861a9329e384a2faeca11d9615cf7`

提交信息：
- `Align exact live duration contract`
