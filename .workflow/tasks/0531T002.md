```md
执行线程：
- 业务线程-python

任务ID：
- 0531T002

标题：
- Stage 9N clean-fill evidence viability refinement

简短描述：
- 基于 `0530T002` Stage 9M 结果，做一次只读证据精炼任务，判断是否继续围绕 top Stage 9L gap 拉长采集时间，还是停止该 clean-fill collection line 并转向更有价值的 bucket / regime。任务只读分析现有 artifacts，不采集新样本、不改策略、不做 policy design、不做参数搜索、不 default-on、不 tiny-live/promotion。

状态：
- 待执行

执行顺序：
- 当前唯一 Binance 后续任务草案；必须等 `0530T002` QA 正常验收通过后才能执行。它与 Hyperliquid `0531T001` 独立，但不得修改、依赖或混入 Hyperliquid artifacts。

前置任务：
- `0529T002` QA 已通过。
- `0529T005` QA 已通过。
- `0530T002` 业务执行完成但仍在 `待验收`；本任务必须等 `0530T002` QA 通过后执行。
- 直接事实源：
  - `.workflow/tasks/0530T002.md`
  - `.workflow/reports/0530T002-business.md`
  - `local_live_analysis/stage9m_targeted_clean_fill_0530T002/**`
  - `local_live_analysis/stage9k_fill_quality_bucket_synthesis_0529T002/**`
  - `local_live_analysis/stage9l_fill_quality_rejection_decomposition_0529T005/**`

规则更新提醒：
- 请先阅读规则文档后再执行本任务。
- 本任务是 read-only evidence refinement，不是新采集、策略实现、policy design、候选启用、guard 放宽、参数搜索、tiny-live、default-on 或 promotion 任务。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `docs/thread-playbook.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交任务文件、业务报告、必要 workflow 更新，以及本任务生成的 task-scoped read-only artifacts。
- 仅当现有 artifacts 无法用可靠、可复现方式完成分析时，才允许新增一个窄范围只读 runner 和 focused tests；不得改策略、runner 语义、采集逻辑或既有 Stage 9K/9L 输出语义。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

Allowed files：
- `.workflow/tasks/0531T002.md`
- `.workflow/reports/0531T002-business.md`
- `examples/binance_tick_mm/stage9n_clean_fill_refinement.py` only if a deterministic read-only runner is needed.
- `examples/binance_tick_mm/test_stage9n_clean_fill_refinement.py` only if a new runner is added.
- `local_live_analysis/stage9n_clean_fill_refinement_0531T002/**`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `.workflow/dashboard.html` and `.workflow/dispatch_suggestions.md` only if they can be regenerated without mixing unrelated task state into this task.

Allowed read-only inputs：
- `.workflow/tasks/0529T002.md`
- `.workflow/reports/0529T002-business.md`
- `.workflow/reports/0529T002-qa.md`
- `.workflow/tasks/0529T005.md`
- `.workflow/reports/0529T005-business.md`
- `.workflow/reports/0529T005-qa.md`
- `.workflow/tasks/0530T002.md`
- `.workflow/reports/0530T002-business.md`
- `.workflow/reports/0530T002-qa.md` once it exists and passes.
- `local_live_analysis/stage9k_fill_quality_bucket_synthesis_0529T002/**`
- `local_live_analysis/stage9l_fill_quality_rejection_decomposition_0529T005/**`
- `local_live_analysis/stage9m_targeted_clean_fill_0530T002/**`
- Existing current-format Binance Stage 5 / Stage 5C / Stage 6 summary artifacts only if needed to trace row/sample provenance.
- Existing Binance maker analysis scripts under `examples/binance_tick_mm/**` for read-only reference only.

Forbidden files / areas：
- New live collection, remote deploy, AWS mutation, tmux startup, raw-data collection, or pulling new exchange data.
- Strategy behavior, quote placement implementation, fair/target formula, inventory logic, risk guards, production defaults, live configs, or config defaults.
- Candidate enablement, guard relaxation, parameter search, policy implementation, default-on behavior, tiny-live, promotion claim, or production behavior change.
- Replay lifecycle semantic changes, queue/touch repair, exact queue position claims, hidden queue assumptions, connector/core API changes, standard npz schema changes, or canonical audit schema changes.
- Treating caveated samples as strict-clean proof.
- Treating future fill, future markout, future spread capture, same-sample PnL feedback, exact queue position, or hidden queue assumptions as live decision trigger inputs.
- Hyperliquid public/private work, including `0531T001`.

Problem statement：
- `0530T002` Stage 9M added aggregate Stage 9K clean-only fill mass (`994 -> 1102`) but the top Stage 9L gap moved only `34 -> 36` fills.
- The target gap remains:
  - `churn_warning_coarsened / large_skew_or_low_score / add_side / step_back_gt1 / edge_non_adverse / market_view_usable / post_only_clean / warning_churn_context`
  - after Stage 9M: `2901` rows, `36` fills, `8` samples, `7` fill samples, still `4` fills short of the Stage 9L minimum.
- The observed fill rate for that top gap is about `1.24%`, while broader `touch` regimes have materially higher fill rates.
- Before another collection task is created, the controller needs a read-only decision artifact answering whether more collection time is worth it, and if so whether the goal is only crossing the minimum threshold or supporting a later policy-design direction.

Execution scope：
1. Confirm `0530T002` QA has passed before execution. If QA has not passed, stop and report `阻塞`.
2. Reconstruct the Stage 9M before/after fill-flow using existing artifacts only:
   - where the `+108` aggregate Stage 9K fills went
   - which buckets gained rows/fills
   - which buckets remained under-filled
   - which buckets are quality-negative or churn/cancel-fill contaminated
3. Diagnose why the top gap filled slowly:
   - `step_back_gt1` vs `touch`
   - `add_side` vs `reduce_side`
   - `warning_churn_context` vs `churn_normal`
   - `post_only_clean` vs guarded fallback / clamped
   - market-view usable vs stale/high-latency buckets
4. Evaluate top-gap viability:
   - projected hours to cross the `40` fill minimum
   - projected hours to hit the interpretive `+20` top-gap target
   - whether quality metrics make additional collection useful or likely wasteful
   - fill-after-cancel / warning churn / markout / spread-capture caveats
5. Identify better follow-up regimes, if any, using only decision-visible bucket axes:
   - buckets with enough fills and acceptable quality
   - buckets close to threshold with better fill-rate / lower churn than the top gap
   - buckets that should be rejected despite higher fills
6. Produce a decision recommendation with one of these classifications:
   - `stop_top_gap_collection`
   - `short_collection_to_cross_minimum_only`
   - `pivot_to_alternative_read_only_regime_refinement`
   - `blocked_by_artifact_inconsistency`
7. If recommending more collection, specify a narrow duration range and exact purpose, for example `4h-6h only to cross minimum, not policy-ready`.
8. If recommending a pivot, specify the alternative regime-selection criteria, not a strategy implementation.

Required outputs：
- `.workflow/reports/0531T002-business.md`
- A task-scoped output directory `local_live_analysis/stage9n_clean_fill_refinement_0531T002/` containing:
  - `run_manifest.json`
  - `fill_flow_decomposition.csv`
  - `axis_fill_rate_summary.csv`
  - `target_gap_viability.csv`
  - `candidate_regime_triage.csv`
  - `collection_time_estimate.csv`
  - `stage9n_recommendation.md`

Acceptance criteria：
- The task reads only accepted local artifacts and does not collect new data.
- The task states whether the top Stage 9L gap is worth further collection.
- The task quantifies why `0530T002` added only `+2` fills to the top gap despite `+108` aggregate fills.
- The task estimates collection duration for both `36 -> 40` and `+20` top-gap targets.
- The task identifies whether alternative regimes look more promising, using only decision-visible bucket axes and observed-quality labels.
- The task clearly separates decision-visible axes from after-the-fact labels.
- The task does not implement or propose a live strategy. Any later strategy/policy design must be a separate design-only task after QA.
- The task does not modify strategy behavior, enable candidates, relax guards, run parameter search, default-on anything, start tiny-live, or claim promotion.
- The business report ends in `待验收` if executed successfully, or `阻塞` if `0530T002` QA is not yet passed.

verify：
- `python examples/binance_tick_mm/stage9n_clean_fill_refinement.py --help` if a runner is added.
- `python -m pytest examples/binance_tick_mm/test_stage9n_clean_fill_refinement.py -q` if a runner is added.
- Run the read-only analysis over `local_live_analysis/stage9m_targeted_clean_fill_0530T002/`.
- `python -m json.tool local_live_analysis/stage9n_clean_fill_refinement_0531T002/run_manifest.json`
- CSV artifact schema sanity for `fill_flow_decomposition.csv`, `axis_fill_rate_summary.csv`, `target_gap_viability.csv`, `candidate_regime_triage.csv`, and `collection_time_estimate.csv`.
- `git diff --check`
- No live command.
- No remote deploy.
- No collection command.
- No parameter-search command.

done：
- Business report lists files read, files changed, commands run, generated artifacts, final classification, and explicit forbidden-boundary confirmation.
- Report includes `commit id` and `提交信息`.
- Report explicitly states:
  - whether top-gap collection should stop, continue briefly, or pivot
  - why `0530T002` top-gap fills were low
  - whether any alternative regime deserves a later design-only contract
  - that no new collection, strategy implementation, candidate enablement, guard relaxation, parameter search, default-on, tiny-live, promotion, or Hyperliquid work was done
```
