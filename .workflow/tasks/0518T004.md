```md
执行线程：
- 业务线程-python

任务ID：
- 0518T004

标题：
- Step 5C narrow quote-anchor safety layer

简短描述：
- 基于 `0518T002` 设计合同和 `0518T003` read-only diagnostic 结果，实施一个很窄的 default-off / diagnostic-first quote-anchor safety layer。目标是防止未来启用 fast BBO/bookTicker hard anchor 时引入 crossed / post-only 风险；本任务不是 drift 修复、不是 quote-control 策略、不是 live promotion。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0518T002` QA 已通过
- `0518T003` QA 已通过

规则更新提醒：
- 本任务只允许实现窄的 quote-anchor safety layer。
- 不允许修复 audit_depth/bookTicker/top5 row-exact drift。
- 不允许把 top5 提升为 hard quote anchor。
- 不允许改 fair/reservation model、quote-control strategy、replay lifecycle、standard npz schema、live scripts 或 AWS/remote state。
- 不允许 default-on，不允许 live promotion。

按线程规则执行：
- .workflow/workflow-kit/thread-report-template.md
- docs/thread-playbook.md
- task_plan.md
- progress.md
- findings.md

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

Allowed files：
- `.workflow/tasks/0518T004.md`
- `.workflow/reports/0518T004-business.md`
- `examples/binance_tick_mm/quote_anchor_safety.py`
- `examples/binance_tick_mm/test_quote_anchor_safety.py`
- `examples/binance_tick_mm/quote_anchor_diagnostic.py`
- `examples/binance_tick_mm/test_quote_anchor_diagnostic.py`
- `examples/binance_tick_mm/strategy_core.py`
- `examples/binance_tick_mm/backtest_tick_mm.py`
- `examples/binance_tick_mm/live_tick_mm.py`
- `examples/binance_tick_mm/test_backtest_tick_mm.py`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `.workflow/dashboard.html`
- `.workflow/dispatch_suggestions.md`

Allowed generated outputs：
- `local_live_analysis/5-13-day-control-30min/stage5c_quote_anchor_safety_0518T004/**`

Allowed read-only inputs：
- `.workflow/reports/0518T002-business.md`
- `.workflow/reports/0518T002-qa.md`
- `.workflow/reports/0518T003-business.md`
- `.workflow/reports/0518T003-qa.md`
- `local_live_analysis/5-13-day-control-30min/stage5b_quote_anchor_diagnostic_0518T003/**`
- `local_live_analysis/5-13-day-control-30min/audit_live_5-13-day-control-30min.csv`
- `local_live_analysis/5-13-day-control-30min/t009_fixed_sidecar/**`

Forbidden files / areas：
- `examples/binance_tick_mm/deploy/**`
- live scripts, AWS/remote state, live collection, live config deployment
- fair/reservation formulas or alpha/pricing model changes
- generic quote placement redesign beyond the explicit safety clamp/re-check path
- replay fill model, replay lifecycle repair, or queue/touch repair
- core Rust, connector, py `event_dtype`, standard npz schema
- audit_depth/bookTicker/top5 row-exact drift repair
- top5 hard-anchor promotion
- default-on behavior or live promotion

Requires separate task：
- Any source-level market-view drift repair.
- Any top5 hard-anchor promotion.
- Any fair/reservation or pricing model implementation.
- Any quote-control strategy experiment.
- Any Step 9 replay experiment.
- Any live micro test, live collection, or promotion decision.

Fatal Failures：
- 默认开启 Step 5C 行为。
- 修改当前默认 quote placement / submit 行为。
- 把 top5 用作最终 hard post-only anchor。
- 把 audit_depth/bookTicker/top5 drift 当作本任务 repair 范围。
- 启动 live、改 live scripts、改远端、或做 promotion 判断。

action：
- 实现一个 default-off / diagnostic-first quote-anchor safety layer。
- 必须保持默认行为不变：配置未显式开启时，现有 target ticks、planned action、submit/cancel 行为不得改变。
- 实现或抽出可测试 helper，至少覆盖：
  - anchor arbitration：优先 fast BBO/bookTicker-equivalent；缺失、不新鲜或 invalid 时只允许 guarded depth fallback；top5 不作为 final hard anchor
  - side-conservative rounding：bid floor，ask ceil
  - anchor clamp：bid `<= anchor_best_bid_tick`，ask `>= anchor_best_ask_tick`
  - post-clamp re-check：再次检查 crossed / post-only risk
  - stale / missing / join-age guard：对 fresh add-side submit 或 re-add churn 做 suppress / diagnostic，不靠 GTX reject 当正常控制流
  - diagnostic counters：至少输出 suppressed / clamped / missing-anchor / stale-anchor / fallback-used / post-only-risk-after-recheck 这类计数
- 如果当前 live path 没有可用 fast BBO/bookTicker-equivalent 输入，不得发明 connector/core 变更；应保持 default-off，并在 diagnostic 中明确 source unavailable / guarded fallback 行为。
- 在 `5-13-day-control-30min` 上生成 Stage 5C diagnostic artifact，证明启用候选 safety layer 后没有新增 post-only/crossed risk；该 artifact 仍是离线诊断，不代表 live readiness。

verify：
- `python -m pytest examples/binance_tick_mm/test_quote_anchor_safety.py`
- `python -m pytest examples/binance_tick_mm/test_quote_anchor_diagnostic.py`
- `python -m pytest examples/binance_tick_mm/test_backtest_tick_mm.py`
- 如果新增 CLI 或扩展 T003 runner：运行相应 `--help`
- 在 `5-13-day-control-30min` 上生成 `stage5c_quote_anchor_safety_0518T004` 诊断产物
- `python3 .workflow/build_dashboard.py`
- `git diff --check`

done：
- 回报必须说明默认行为是否保持不变，以及用什么测试证明。
- 回报必须说明 anchor arbitration、rounding、clamp、re-check、guarded fallback、stale/join-age suppression 的实现位置。
- 回报必须说明 top5 仍然不是 final hard anchor。
- 回报必须说明没有修复 audit_depth/bookTicker/top5 row-exact drift。
- 回报必须说明没有改 fair/reservation、没有改 replay lifecycle、没有启动 live、没有 default-on。
- 回报必须给出 Stage 5C diagnostic 关键计数和产物路径。
- 回报必须写 `commit id` 和提交信息。
```
