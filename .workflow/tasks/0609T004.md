```md
执行线程：
- 业务线程-research

任务ID：
- 0609T004

标题：
- Basis-positive clean context read-only case-design contract

简短描述：
- 基于 `0609T003` 的 `candidate_for_read_only_case_design` 结果，设计一个只读 case-design 合同，把 `basis_positive_clean_context` 的 observation-layer 发现整理成可 QA 的字段、标签、验收门槛、拒绝条件和 execution evidence gap map。本任务只做 read-only design/contract，不实现 case-library、不生成 shadow decision、不输出任何可执行交易动作。

状态：
- 待验收

执行顺序：
- 当前下一任务；必须在 `0609T003` QA 通过后执行。

前置任务：
- `0609T002` QA 已通过，final recommendation 为 `tail_filter_hypothesis_validated_for_read_only_research`。
- `0609T003` 需 QA 已通过，final recommendation 必须为 `candidate_for_read_only_case_design`。
- `0609T003` 的 execution gap register 仍是本任务的执行层边界事实源。

规则更新提醒：
- 本任务只设计 read-only case-design contract，不允许新采集、不允许远端执行、不允许 private/account/order endpoint、不允许策略实现、不允许 case-library implementation、不允许 shadow decision、不允许 live/default-on/tiny-live、不允许 parameter search 或 promotion。
- 任何 case label / case field 都必须显式标记为 research/design artifact，不得变成 executable trigger、order side、quote price、size、leverage、stop/take-profit、deployment recommendation 或 live instruction。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `docs/thread-playbook.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 自测和本地验证通过后提交任务文件、业务报告、design docs/artifacts 和必要 workflow tracking 更新。
- 回报必须带 `commit id` 和 `提交信息`。
- 不得混入无关 dirty 文件。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0609T004.md`
- `.workflow/reports/0609T004-business.md`
- `docs/basis_positive_clean_context_case_design.md`
- Local task artifacts:
  - `local_live_analysis/basis_positive_clean_case_design_0609T004/**`
- 必要 workflow tracking 文件仅限本任务状态/结论更新。

read-only inputs：
- `local_live_analysis/basis_positive_filtered_context_viability_0609T003/**`
- `local_live_analysis/basis_positive_targeted_public_collection_0609T002/**`
- `local_live_analysis/canonical_basis_positive_wrong_way_decomposition_0609T002/**`
- `.workflow/reports/0609T003-business.md`
- `.workflow/reports/0609T003-qa.md` after QA exists.

action：
- Read T003 manifest, raw-vs-filtered summary, rejected-tail summary, sample/horizon/conditioning stability, research labels, next-step recommendation, and execution gap register.
- Produce a read-only case-design contract for `basis_positive_clean_context`.
- Define case fields and mark each field as one of:
  - `decision_time_visible_context`
  - `diagnostic_context`
  - `future_label_for_research_only`
  - `execution_gap_reference`
- Define read-only case labels, naming rules, allowed use, and forbidden use.
- Define acceptance gates for any later separately dispatched read-only case-library design discussion.
- Define reject conditions that would stop the case-library direction.
- Map execution-layer gaps to future evidence tasks without filling those gaps in this task.

required outputs：
- `docs/basis_positive_clean_context_case_design.md`
- `local_live_analysis/basis_positive_clean_case_design_0609T004/case_design_manifest.json`
- `local_live_analysis/basis_positive_clean_case_design_0609T004/case_field_contract.csv`
- `local_live_analysis/basis_positive_clean_case_design_0609T004/case_label_contract.csv`
- `local_live_analysis/basis_positive_clean_case_design_0609T004/case_acceptance_gate.md`
- `local_live_analysis/basis_positive_clean_case_design_0609T004/case_reject_conditions.csv`
- `local_live_analysis/basis_positive_clean_case_design_0609T004/execution_gap_to_future_evidence_map.md`
- `.workflow/reports/0609T004-business.md`

acceptance criteria：
- `0609T003` QA has passed before T004 execution starts.
- Must not generate row-level case-library entries, shadow decisions, trading instructions, order side, quote price, quote size, leverage, stop/take-profit, or executable triggers.
- Must explicitly preserve T003 boundary that current evidence is public observation-layer only and does not prove fill probability, queue/queue-ahead, post-only reject behavior, cancel-fill race, fees/rebates/spread capture, inventory lifecycle, or real order lifecycle.
- `case_field_contract.csv` must distinguish decision-time visible context from diagnostic context and future labels.
- Any future-label fields must be marked `future_label_for_research_only` and forbidden as inputs/triggers.
- `case_label_contract.csv` must mark every label as `read_only_design_label`.
- `case_acceptance_gate.md` must require, at minimum:
  - T003/T004 QA pass before later case-library design discussion.
  - clean context remains represented across at least `7` samples.
  - max sample row share remains below `0.40`.
  - clean p95 wrong-way loss improvement versus raw remains positive.
  - no sample/horizon/conditioning negative-mean reversal.
  - execution-layer gaps remain explicitly unproven unless separately dispatched and QA-accepted.
- `case_reject_conditions.csv` must include rejection of any proposal that attempts executable trigger, private/order endpoint, strategy implementation, shadow decision, live/default-on/tiny-live, parameter search, or promotion.
- Final recommendation must be exactly one of:
  - `case_design_contract_ready_for_qa`
  - `needs_more_design_detail`
  - `reject_case_design_contract`
- Any recommendation remains design/read-only only and does not authorize strategy/live/private/order behavior.

verify：
- Parse T003 manifest and T004 manifest with `python -m json.tool`.
- Parse T004 CSV artifacts.
- Check required markdown artifacts exist and contain boundary language.
- Boundary text check for no private/order/strategy/live/default-on/tiny-live/case-library implementation/shadow/promotion/executable authorization.
- `git diff --check`

forbidden：
- New public collection or remote execution.
- Private/account/order endpoint use on Binance or Hyperliquid.
- Order lifecycle, submit/cancel/fill logic, user stream, account state, positions, signing, nonce handling.
- Strategy implementation, production config/default changes, parameter search, default-on, tiny-live, promotion.
- Case-library implementation, row-level case entries, shadow decision generation, executable trading instruction, actual order side, quote price/size, leverage, stop rule, take-profit rule, deployment recommendation.
- Connector/core API changes, standard npz schema changes, canonical Binance maker audit schema changes.

done：
- Business report must include input artifact paths, design outputs, field taxonomy, label taxonomy, acceptance gates, reject conditions, execution gap map, final recommendation, verification commands, blockers/caveats if any, commit id, and commit message.
- Business report must explicitly state that this task is read-only design only and does not prove execution-layer maker viability.
- Business report must end in `待验收` if local validation completes, or `阻塞` / `未通过` if criteria cannot be met.
```
