# 任务派发

执行线程：
- 业务线程-controlled-evidence

任务ID：
- 0716T006

标题：
- T011-CONTROLLED-EVIDENCE-ACQUISITION-WITH-LIQUIDITY-ROLE-CONTRACT

简短描述：
- 基于 0716T005 QA-accepted preflight contract，执行下一轮 controlled evidence acquisition，用于证明 fill source / maker-taker role artifact path。
- 若需要 live execution，本任务必须先获得 exact UTC schedule、host/account scope、live envelope、size/submission caps、max loss 和 controller authorization；缺任一项必须阻塞，不得运行 live。

状态：
- 阻塞

执行顺序：
- 当前唯一任务

前置任务：
- `0716T005` QA 已通过。
- `0716T006` initial authorization gate QA was `阻塞` at commit `a5431d8`.
- Controller subsequently authorized a live rerun in conversation on 2026-07-16.
- Live rerun connectivity recovery completed on 2026-07-17; full artifact package was pulled back and validated.
- Current blocker is no longer connectivity. It is `no_fill_role_evidence_absent`: all three windows had zero fill rows and zero liquidity-role evidence rows.

live rerun authorization：
- host:
  - `awsserver1`
- remote repo:
  - `/home/admin/hftbacktest-cross-exchange`
- interpreter:
  - `/home/admin/.venvs/hyperliquid-sdk-0618T002/bin/python`
- credential/source boundary:
  - use existing awsserver1 env file `/home/admin/XEMM_rust_latest/.env`
  - do not print, copy, commit, or pull credential values
- source branch/commit:
  - `cross-exchange / a5431d8b24da7d77671148d316f789b0b25cf3f8`
- exact UTC schedule:
  - target start no earlier than `2026-07-16T07:20:58Z`
  - run three sequential windows; record actual start/end for each window
- venue/symbol:
  - Hyperliquid `BTC`
- live duration/window count:
  - `3` windows, each bounded by `1800s`
- post-only behavior:
  - post-only `Alo`
- max order size:
  - `0.005 BTC`
- max submissions:
  - `2` per window
- max position / inventory delta:
  - `0.01 BTC`
- max loss:
  - `1 USDC`
- explicit real-order authorization:
  - user authorized real order submit/cancel under this envelope only

规则更新提醒：
- 请先阅读规则文档后再执行本任务。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `docs/cross_exchange_first_three_execution_sequence.md`
- `local_live_analysis/cross_exchange_fill_source_liquidity_role_preflight_0716T005/future_controlled_evidence_task_template.md`

是否需要提交代码：
- 需要

提交要求：
- 若只产出 blocker/authorization gap report，也需要提交 report 和 tracking 更新。
- 若产出 controlled evidence artifacts，artifact、business report 和 tracking 更新必须提交。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0716T006.md`
- `.workflow/reports/0716T006-business.md`
- `local_live_analysis/cross_exchange_controlled_role_evidence_0716T006/`
- `task_plan.md`
- `progress.md`
- `findings.md`

required controller inputs before any live execution：
- exact UTC schedule
- host/account scope
- symbol and venue
- live duration/window count
- post-only behavior
- max order size
- max submissions
- max position or inventory delta
- max loss
- credential/source boundary
- source branch/commit
- explicit controller authorization for real orders

action：
- First validate whether this task has a complete authorized evidence source:
  - If live execution inputs are incomplete, do not run live; produce a blocker report and set status `阻塞`.
  - If the controller supplies a non-live artifact source, verify it can satisfy 0716T005 required artifacts before using it.
  - If a fully authorized live envelope is supplied, collect only the artifacts required by 0716T005 and preserve the approved envelope.
- Required artifacts for a successful evidence package:
  - `fill_liquidity_role_evidence.csv`
  - `user_fills_pullback_audit.json`
  - `live_fill_ledger.csv`
  - `order_intent_audit.csv`
  - `private_order_response_audit.json`
  - `resting_interval_lifecycle_matrix.csv`
  - `public_stream_coverage.csv`
  - `boundary_manifest.json`
- Evaluate route decision:
  - `route_to_public_shadow_with_role_source_evidence`
  - `route_to_source_path_repair`
  - `route_to_controlled_evidence_rerun`
  - `blocked_missing_live_authorization`
  - `blocked_boundary_violation`

verify：
- Parse all generated JSON/CSV artifacts.
- Assert every attributed fill has a stable `attempt_key`.
- Assert every attributed fill has `role_status` in `confirmed_maker`, `confirmed_taker`, `unknown_liquidity_role`.
- Assert any `unknown_liquidity_role` blocks fee/PnL calibration.
- Assert boundary manifest preserves the authorized envelope and records no scope expansion.
- `git diff --check`.

done：
- 回报 whether controlled evidence was collected or blocked.
- 回报 role/source-path evidence status, route decision, unsupported claims, verification commands, commit id。

latest recovery result：
- awsserver1 SSH recovered and SSM status is `connected`.
- no remaining `0716T006` / `hyperliquid_tiny_live` process was observed.
- read-only Hyperliquid `open_orders()` recovery proof returned `0`.
- full artifact root was pulled to `local_live_analysis/cross_exchange_controlled_role_evidence_0716T006_20260716T073133Z_full/`.
- recursive artifact files: `233`; JSON files parsed `101` with `0` errors; CSV files parsed `117` with `0` errors.
- all three windows show `real_order_endpoint_called=true`, `real_cancel_endpoint_called=true`, `shutdown_proof_status=pass`, and `final_open_orders_count=0`.
- all three windows show `fill_count=0`, `ledger_fill_rows=0`, and `fill_liquidity_role_evidence.csv` role rows `0`.
- final route: `route_to_controlled_evidence_rerun_or_explicit_downgrade_no_fill_role_evidence`.
