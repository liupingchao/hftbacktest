# 0718T021 P3-MULTI-LEVEL-PREREQUISITE-GATE

执行线程：
- 总控 auto-loop / 业务实现线程

任务ID：
- 0718T021

标题：
- P3 multi-level prerequisite gate and default-off ladder contract

简短描述：
- Implement only the fail-closed prerequisite gate and deterministic default-off ladder contract for Principal Alignment Task 10.
- Preserve the current single-level fixed quote/order path and do not activate levels.

状态：
- 待执行

执行顺序：
- 当前唯一任务

前置任务：
- 0718T020

规则更新提醒：
- T020 QA passed, but the public-only live evidence contains zero real single-level resting/fill lifecycle.
- Task 10's plan prerequisite is therefore not satisfied. This task may prove ladder generation, price/legalization, duplicate-price coalescing and exposure accounting in default-off mode, but may not activate multi-level.
- Dynamic spread, fill feedback activation, inventory skew changes, quote-distance changes, size/cap relaxation and promotion remain unauthorized.

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

files：
- Modify: `examples/hyperliquid/cross_exchange_shared_signal_kernel.py`
- Modify: `examples/hyperliquid/hyperliquid_maker_order_manager.py`
- Modify: `examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py` only if required for gate/status artifact
- Test: existing manager/kernel tests; create a focused ladder test only if no existing contract covers the new behavior

action：
- Define a typed default-off ladder configuration with level count, gap, size decay and activation/prerequisite state.
- Generate level 0 from the existing authoritative reservation and fixed half-spread path; deeper levels must use explicit configured gap and size decay.
- Apply the existing price legalization, tick/lot/min/max and duplicate-price coalescing/fail-closed rules.
- Validate aggregate working/inflight exposure and submission budget against the existing runtime envelope before any hypothetical ladder is emitted.
- Keep inventory-worsening side reducible or disabled and reduce-side behavior explicit, without changing current single-level live quotes.
- Persist an artifact/status gate stating `activation_enabled=false`, `single_level_lifecycle_prerequisite=false`, `actual_quote_behavior_changed=false`.

verify：
- level-0 parity with the fixed Task 7 quote path;
- configured gap/size decay and deterministic ordering;
- duplicate rounded prices coalesce or fail closed;
- invalid price/lot/minimum/maximum and aggregate exposure fail closed;
- prerequisite false blocks ladder activation while pure generation remains replayable;
- existing manager/kernel/watcher regressions, modified Python `py_compile`, CLI help and `git diff --check`;
- no private/order/cancel endpoint and no live run is required unless a public-only artifact boundary check is needed.

done：
- Do not claim Task 10 complete, multi-level live readiness, maker viability or promotion unless a later task first obtains an accepted single-level resting/fill lifecycle and QA explicitly accepts it.

blockers：
- The real single-level lifecycle prerequisite is currently unsatisfied by T020 evidence; this task must record that blocker and remain default-off.

commit：
- 无

提交信息：
- 无
