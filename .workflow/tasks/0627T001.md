```md
执行线程：
- 业务线程-research-aws

任务ID：
- 0627T001

标题：
- Hyperliquid fast l2Book synchronized sample rerun

简短描述：
- 修改 Hyperliquid public collector，使 `l2Book` 可显式使用 `fast=true` shallow/top5 public snapshot 订阅，并在 `awsserver1` 重新收集三段 Binance `BTCUSDT` lead + Hyperliquid `BTC` lag public-only 同步窗口。
- 本任务目标是替换 0625T002 中 nominal `1000ms` / effective `5000ms` 的样本缺陷，验证 fast HL book 是否能提供 near-target `1000ms` future labels。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- `0625T002` repair business result: `needs_more_public_samples` because `1000ms_near_target_label_coverage_insufficient`.
- Controller explicitly requested a new task to modify the HL interface and rerun the collection.

规则更新提醒：
- 请先阅读 `.workflow/workflow-kit/workflow-manual.md`、`.workflow/workflow-kit/task-dispatch-template.md`、`.workflow/workflow-kit/thread-report-template.md`、`.workflow/workflow-kit/qa-acceptance-template.md`、`task_plan.md`、`progress.md`、`findings.md`、`docs/cross_exchange_maker_mvp_plan.md` 后再执行本任务。

按线程规则执行：
- `AGENTS.md`
- `docs/thread-playbook.md`

是否需要提交代码：
- 需要

提交要求：
- 完成 HL fast public collector 支持、AWS 采集、本地处理、task-scoped manifests 和业务报告后提交。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0627T001.md`
- `.workflow/reports/0627T001-business.md`
- `examples/hyperliquid/hyperliquid_public_sample.py`
- `examples/hyperliquid/synchronized_public_collection.py`
- `examples/hyperliquid/cross_exchange_sample_expansion.py`
- focused tests for changed collectors/runners
- `local_live_analysis/cross_exchange_public_sample_xemm_0627_t001_hlfast_*/**`
- `local_live_analysis/cross_exchange_lead_lag_join_xemm_0627_t001_hlfast_*/**`
- `local_live_analysis/cross_exchange_lead_lag_analysis_xemm_0627_t001_hlfast_*/**`
- `local_live_analysis/binance_led_hyperliquid_pricing_signal_xemm_0627_t001_hlfast_*/**`
- `local_live_analysis/cross_exchange_mvp_hl_fast_sample_expansion_0627T001/**`
- `task_plan.md`
- `progress.md`
- `findings.md`

read-only inputs / reusable code：
- `0625T002` repaired package and evidence:
  - `local_live_analysis/cross_exchange_mvp_sample_expansion_0625T002/**`
- Existing collectors/runners:
  - `examples/hyperliquid/hyperliquid_public_sample.py`
  - `examples/hyperliquid/synchronized_public_collection.py`
  - `examples/hyperliquid/hyperliquid_raw_alignment.py`
  - `examples/binance_tick_mm/binance_top5_provenance.py`
  - `examples/hyperliquid/cross_exchange_lead_lag_join.py`
  - `examples/hyperliquid/cross_exchange_lead_lag_analysis.py`
  - `examples/hyperliquid/binance_led_pricing_signal_runner.py`
  - `examples/hyperliquid/cross_exchange_sample_expansion.py`

hard boundary：
- Public-only collection and offline local processing.
- `awsserver1` is raw public collection only. Alignment must not run on `awsserver1`; use macmini or amdserver for Binance/Hyperliquid alignment and downstream processing.
- No live orders, no credentials, no private/account/order/cancel endpoints, no user stream, no live client initialization for trading, no remote final gate, no T008 ledger claim.
- No watcher strategy change, signal threshold tuning, side-mapping freeze, edge-buffer reduction, quote-distance change, cap relaxation, taker/crossing, canary, M3, stable-PnL, default-on or promotion.
- Future Hyperliquid values are labels only and must never be decision inputs.
- Do not reinterpret 5s effective labels as 1s labels.
- Do not count a window as T003-ready unless near-target effective horizon coverage passes.

implementation contract：
- Add explicit `fast=true` support only for Hyperliquid `l2Book` subscriptions.
- Preserve default collector behavior unless `--l2book-fast` / `--hyperliquid-l2book-fast` is passed.
- Record subscription options in manifests.
- The accepted AWS collection commands for this task must use `--hyperliquid-l2book-fast --skip-alignment`.
- `--skip-alignment` must write `alignment_status=skipped`, `raw_collection_only=true`, and `alignment_execution_host=macmini_or_amdserver` in manifests.
- Task sample naming:
  - `xemm_0627_t001_hlfast_<utc-session-or-regime>_<a|b|c>`

sample contract：
- Collect exactly three new accepted candidate windows on `awsserver1`.
- Requested duration per window: `1800s`.
- Each accepted window must have synchronized overlap `>=1500s`.
- Window start times must be separated by at least `30` minutes.
- The accepted set must cover at least two observed public volatility/liquidity regime buckets.
- Hyperliquid `l2Book` count must demonstrate that fast mode materially improved cadence relative to T002 ordinary mode:
  - preferred minimum `>=1800` `l2Book` messages per 1800s window
  - if below this, report as `hl_fast_cadence_not_confirmed` and do not unlock T003.

near-target effective-horizon gate：
- Nominal `1000ms` label rows are valid for 1s signal acceptance only when:
  - `1000ms <= effective_future_age_ms <= 1250ms`
- To unlock T003, valid near-target `1000ms` signal contexts must satisfy:
  - at least `100` aggregate rows
  - at least `20` rows per accepted window
- If this gate fails, final recommendation must be `needs_more_public_samples` or `sample_collection_invalid`, and `t003_creation_unlocked=false`.

required output package：
- `sample_expansion_manifest.json`
- `sample_quality_matrix.csv`
- `field_coverage_matrix.csv`
- `regime_summary.csv`
- `effective_horizon_coverage.csv`
- `effective_horizon_validity_matrix.csv`
- `symmetric_edge_context_coverage.csv`
- `boundary_manifest.json`
- `recommendation.md`

final recommendation enum：
- `sample_contract_ready_for_signal_acceptance`
- `needs_more_public_samples`
- `sample_collection_invalid`

verify：
- Focused tests:
  - `python -m pytest examples/hyperliquid/test_hyperliquid_public_sample.py examples/hyperliquid/test_synchronized_public_collection.py examples/hyperliquid/test_cross_exchange_sample_expansion.py -q`
- Collector help:
  - `python examples/hyperliquid/hyperliquid_public_sample.py --help`
  - `python examples/hyperliquid/synchronized_public_collection.py collect --help`
- Parse every T001 JSON manifest with `python -m json.tool`.
- Verify every copied `raw.gz` against recorded SHA256.
- Verify AWS collection manifests show `alignment_status=skipped` / `raw_collection_only=true` before copyback alignment.
- Validate required CSV schemas, near-target horizon counts, row thresholds, non-empty files and boundary flags.
- Reproduce the task-scoped package into a temporary directory from accepted local sample paths.
- `git diff --check`

done：
- Report the exact HL subscription payload used for `l2Book`.
- Report all three sample ids, requested/actual duration, overlap, host/interpreter/commit, stream counts, reconnects, checksum results and artifact paths.
- Compare HL `l2Book` cadence against 0625T002 ordinary l2Book cadence.
- Report per-window alignment rows, join rows, primary/excluded rows, future/missing/stale join counts and source-age distributions.
- Report nominal/effective horizon coverage, near-target valid `1000ms` rows per window and aggregate, and final recommendation.
- Explicitly state whether T003 is unlocked.
- Explicitly state no signal/side contract was frozen, no live strategy behavior changed, no private/order endpoints were used, no orders were placed, and no canary/promotion was authorized.
```
