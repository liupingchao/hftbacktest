# 0722T056 CLEAN-CLONE-FIXTURE-PORTABILITY

执行线程：
- 总控 / 业务执行线程

任务ID：
- 0722T056

标题：
- Repair historical artifact path portability and clean-clone test behavior

简短描述：
- 修复 full Hyperliquid regression 中两个依赖本机历史 artifact 的失败。
- 历史集成事实不被伪造；缺失外部数据包时显式 skip，旧 repo-root
  绝对路径在对应 repo-relative artifact 实际存在时才允许安全重定位。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- 0722T055（已通过）

规则更新提醒：
- offline-only；不得执行 network、remote、private/account、order/cancel、
  live 或 service 操作。
- 不得生成伪造的 0602T001/0609T002 历史采集数据，也不得改变历史统计
  结论。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交。
- 回报必须带 exact implementation commit id 和提交信息。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `examples/hyperliquid/basis_positive_row_level_read_only_generator.py`
- `examples/hyperliquid/test_basis_positive_row_level_read_only_generator.py`
- `examples/hyperliquid/cross_exchange_lead_lag_join.py`
- `examples/hyperliquid/test_cross_exchange_lead_lag_join.py`
- `.workflow/tasks/0722T056.md`
- `.workflow/reports/0722T056-business.md`
- controller tracking files

action：
- 增加仅针对已知 repo-owned artifact roots 的安全路径重定位。
- 历史 artifact 完整时继续执行原有 exact-count integration assertions。
- 历史 artifact 未随 clone 提供时，测试必须以明确 reason skip，而不是
  FileNotFoundError。
- 增加 self-contained regression，证明旧 amdserver repo-root 可以重定位，
  且不允许把任意绝对路径映射进 repo。

verify：
- Focused 两个历史测试模块。
- Full `conda run -n hftbacktest python -m pytest examples/hyperliquid -q`。
- `python -m py_compile`。
- `git diff --check`。

done：
- Clean clone / 无历史数据包环境不再出现两个 collection failure。
- 有完整历史数据包的环境仍保留 exact historical integration checks。
- 不改变策略、alpha、quote、risk、order lifecycle 或 live 行为。

blockers：
- 无。

提交信息：
- `Make historical artifact tests portable`
