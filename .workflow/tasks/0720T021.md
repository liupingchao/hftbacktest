# 0720T021 POSITION-ROW-IDENTITY-NUMERIC-PARSER-REPAIR

执行线程：
- 总控 auto-loop / 业务执行线程

任务ID：
- 0720T021

标题：
- Harden position row identity and numeric evidence parsing

简短描述：
- Close the single T020 QA P1 in the shared `extract_position_szi()` parser。
- Reject missing/ambiguous coin identity and boolean quantities without changing valid empty、foreign-only or numeric BTC semantics。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- `0720T020`（未通过；status propagation accepted, parser strictness remains open）

规则更新提醒：
- 本任务 offline-only。
- No live/private/account/order/cancel/network/remote/service action is authorized。
- New bounded live、Task 8 and adaptive/multi-level activation remain locked。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `.workflow/reports/0720T020-qa.md`
- `docs/superpowers/plans/2026-07-17-principal-alignment-p0-p3.md`

是否需要提交代码：
- 是

提交要求：
- 实现和测试提交。
- 业务报告单独提交。
- 回报必须带 implementation commit id 和提交信息。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

source：
- Branch: `cross-exchange`
- Dispatch base: `e4c3675`

files：
- `examples/hyperliquid/hyperliquid_tiny_live_real_order_executor.py`
- `examples/hyperliquid/test_hyperliquid_tiny_live_real_order_executor.py`
- Focused manager/watcher tests
- `.workflow/tasks/0720T021.md`
- `.workflow/reports/0720T021-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

action：
- Require every non-empty position row to have one explicit canonical non-empty string coin identity。
- Reject boolean/non-string coin identity and conflicting direct/nested coin identity。
- Reject boolean `szi` before numeric conversion。
- Preserve valid `assetPositions=[]` verified zero。
- Preserve explicit well-formed foreign-only rows as valid non-BTC evidence。
- Preserve one finite numeric/string BTC position and duplicate BTC rejection。
- Add parser、manager and finalizer adversarial coverage for empty rows、empty nested rows、boolean coin/szi and identity conflicts。

verify：
- Focused executor/manager/watcher/fill/acceptance tests pass。
- Full `python -m pytest -q -p no:cacheprovider examples/hyperliquid` passes。
- Modified modules/tests pass `py_compile`。
- Acceptance CLI `--help` passes。
- Empty/missing/boolean/conflicting coin rows and boolean BTC `szi` fail closed。
- Valid empty、explicit foreign-only and one finite BTC row pass with correct position。
- T020 status propagation and T017-T019 accepted behavior remain green。
- Exact T016 replay remains byte-preserving, decision `43/43` pass and lifecycle blocked for historical attempt 2。
- `git diff --check` and implementation `git show --check` pass。
- No live/private/account/order/cancel/network/remote/service action occurs。

done：
- Malformed position rows cannot become verified zero/nonzero position evidence。
- Canonical row identity and numeric contracts are independently tested。
- Independent QA accepts T021 before any new bounded live task。

stop conditions：
- Missing coin row can still be skipped as foreign。
- Boolean coin or `szi` can be accepted。
- Direct/nested coin conflict can be normalized silently。
- Valid empty or explicit foreign-only semantics regress。
- T017-T020 accepted behavior regresses, historical T016 is upgraded, or strategy/risk/activation changes。
- Any verification requires live/private/account/order/cancel/network/remote/service。

blockers：
- Independent QA failure keeps new bounded live、Task 8 and adaptive/multi-level locked。

commit：
- implementation `fa1095af12415d46dda80894e78a5e7f266281b4`

提交信息：
- `Harden position row evidence parsing`
