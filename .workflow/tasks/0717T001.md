# 任务派发

执行线程：
- 业务线程-live-infra

任务ID：
- 0717T001

标题：
- LIVE-SSH-RESILIENCE-REMOTE-JOB-ORCHESTRATOR

简短描述：
- 修复 live test 采集链路对长公网 SSH 会话的依赖，新增 SSM-friendly remote live orchestrator。
- 本任务只做离线/代码级修复和测试，不运行 live、不提交真实订单、不改变策略参数或 live envelope。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0716T006` latest QA is `阻塞` because no fill role evidence was produced.
- `0716T006` also exposed repeated公网 SSH timeout / broken-pipe risk during live artifact collection.

规则更新提醒：
- 请先阅读规则文档后再执行本任务。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0717T001.md`
- `.workflow/reports/0717T001-business.md`
- `.workflow/reports/0717T001-qa.md`
- `docs/qa-acceptance-report.md`
- `examples/hyperliquid/`
- `task_plan.md`
- `progress.md`
- `findings.md`

will change：
- Add a remote live orchestration wrapper that can be launched through SSM RunCommand or SSH.
- Add a nonblocking live lock to prevent overlapping live jobs.
- Add `run_status.json`, `heartbeat.json`, per-window status files, `abort_manifest.json`, `run_complete.json`, and `remote_sha256_manifest.txt`.
- Ensure every completed/failed window attempts an independent read-only `open_orders()` proof by default.
- Add focused offline tests using fake watcher commands and skipped private proof mode.
- Document the new operating rule: future live tests should be SSM-first and detached; SSH/scp is only an acceleration path.

will not change：
- No live execution.
- No strategy parameter, quote policy, threshold, order size, max submission, max loss, or fill-seeking change.
- No T004 public shadow unlock.
- No fee/PnL calibration.
- No S3 artifact upload in this task.
- No permanent systemd service installation or AWS security group/VPC change.
- No credential value printing, copying, committing, or pulling.

action：
- Implement the remote orchestrator and tests.
- Update workflow status and QA acceptance docs.
- Keep the fix scoped to collection reliability and fail-closed evidence recovery.

verify：
- `python -m pytest examples/hyperliquid/test_cross_exchange_live_remote_orchestrator.py`
- `python examples/hyperliquid/cross_exchange_live_remote_orchestrator.py --help`
- `python -m py_compile examples/hyperliquid/cross_exchange_live_remote_orchestrator.py`
- `git diff --check`

done：
- 回报改了什么、哪些问题已覆盖、哪些问题仍然 deferred、验证命令和 commit id。
