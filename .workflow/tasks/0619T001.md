```md
执行线程：
- 业务线程-research

任务ID：
- 0619T001

标题：
- M2 maker-only flow-aware retry repair for quote aging and side selection

简短描述：
- 基于 `0618T012` public-flow 诊断结果，把 M2 fill-acquisition 从盲目 buy/sell 轮换修复为 flow-aware maker-only retry：重点处理 quote aging / fast drift、side selection 和 crowded-touch queue 过滤。
- 本任务仍保持 Hyperliquid `Alo` post-only、T008 ledger fail-closed、同等或更小 caps；不得切 taker/crossing，不得放宽风险上限。

状态：
- 阻塞

执行顺序：
- 当前唯一任务

前置任务：
- `0618T012` QA 为 `已通过`。
- `0618T008` QA 为 `已通过`，T008 ledger/reconciler 必须继续作为 M2 PnL 事实源。

规则更新提醒：
- 请先阅读 `.workflow/workflow-kit/workflow-manual.md`、`.workflow/workflow-kit/task-dispatch-template.md`、`.workflow/workflow-kit/thread-report-template.md`、`.workflow/workflow-kit/qa-acceptance-template.md`、`task_plan.md`、`progress.md`、`findings.md` 后再执行本任务。

按线程规则执行：
- `AGENTS.md`
- `docs/hyperliquid_tiny_live_real_order_executor.md`
- `docs/hyperliquid_tiny_live_final_go_no_go_gate.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0619T001.md`
- `.workflow/reports/0619T001-business.md`
- `.workflow/reports/0619T001-qa.md`
- `docs/qa-acceptance-report.md`
- `examples/hyperliquid/hyperliquid_tiny_live_m2_fill_window.py`
- `examples/hyperliquid/hyperliquid_tiny_live_m2_fill_loop.py`
- `examples/hyperliquid/hyperliquid_tiny_live_m2_public_flow_diagnosis.py`
- `examples/hyperliquid/hyperliquid_tiny_live_m2_pnl_ledger.py`
- `examples/hyperliquid/test_hyperliquid_tiny_live_m2_fill_loop.py`
- `examples/hyperliquid/test_hyperliquid_tiny_live_m2_public_flow_diagnosis.py`
- `examples/hyperliquid/test_hyperliquid_tiny_live_m2_pnl_ledger.py`
- `local_live_analysis/hyperliquid_tiny_live_m2_flow_aware_retry_0619T001/**`
- `findings.md`
- `progress.md`
- `task_plan.md`

approved live envelope：
- `host_machine=awsserver1`
- `remote_path=/home/admin/hftbacktest-cross-exchange`
- `credential_source=/home/admin/XEMM_rust_latest/.env`
- `symbol=BTC`
- `duration<=10 minutes` for the whole live window
- `requote_attempts<=6`
- `quote_hold_seconds<=45`, with the repaired default expected to be materially shorter than T010 unless the guard explains otherwise
- `max_order_size<=0.01 BTC`, and actual retry order size must be the same as or smaller than the prior `0.00999 BTC` M2 attempt size
- `max_order_notional<=700 USDC`
- `max_position<=0.04 BTC`
- `max_position_notional<=2800 USDC`
- `max_notional<=3000 USDC`
- `max_loss<=30 USDC`
- `maker_only/post_only=true`, Hyperliquid `Alo` only
- `real_orders_allowed=true` only after local self-tests pass, git-safe remote refresh succeeds, final gate returns go, and the flow-aware guards produce an allowed candidate
- `schedule_cancel_required=false`
- `tracked_cancel_required=true`
- `final_open_orders_empty_required=true`

hard boundary：
- This is not another blind same-caps retry. The retry must use `0618T012` conclusions to change quote selection behavior.
- No taker order, market order, crossing order, `Ioc`, cap relaxation, size increase, duration increase, multiple simultaneous open orders, or untracked order.
- No live order may be submitted if remote checkout is dirty, not on `cross-exchange`, cannot fast-forward, final gate blocks, flow guards reject all candidates, or cap validation fails.
- If public collection is unavailable or flow scoring is inconclusive, stop fail-closed before live order unless a clearly documented conservative fallback still satisfies all guards.
- Do not modify the Binance maker route `/home/admin/hft_live/hftbacktest`.
- Do not claim stable PnL, maker viability, M3 readiness, promotion, default-on, or scale-up from this task.

repair policy：
- Add a `flow_aware` side/quote policy that consumes fresh public L2/trades flow or an explicitly recorded recent public-flow sample.
- Side selection must score buy and sell separately using recent touch/strict-through flow, same-side top-depth / queue-depletion proxy, lost-touch / adverse BBO drift, and side-level sample quality.
- The policy must not blindly alternate sides. Sell should be disabled or made stricter unless fresh public-flow evidence supports it.
- Add quote-aging protection:
  - refresh BBO immediately before order submit;
  - use shorter hold / TTL than T010 by default;
  - cancel and skip/requote if the quote loses touch, BBO drifts adversely, spread/crossing guard fails, or market data is stale;
  - keep one tracked open order at a time.
- Add crowded-touch queue filtering:
  - skip candidates with same-side top depth / order-count or depletion proxy outside the conservative guard;
  - record every skip reason as an artifact row, not as silent behavior.
- Preserve T008 ledger fail-closed: live fills without fee/inventory/mark evidence remain incomplete evidence.

action：
- Extend the M2 fill window runner and loop orchestrator with flow-aware maker retry controls, including:
  - `--side-policy flow_aware`
  - fresh public-flow precheck or recorded recent public-flow input;
  - side score / quote guard artifacts;
  - quote aging / lost-touch / stale-data cancellation;
  - per-attempt pre/post BBO, L2/trades summary, side decision, skip reason, submit/cancel/fill lifecycle, and ledger linkage.
- Add focused tests for:
  - buy/sell side scoring and sell rejection when flow is materially worse;
  - lost-touch / adverse drift cancellation;
  - stale or missing public-flow fail-closed behavior;
  - no taker/crossing/cap relaxation paths;
  - one tracked open order at a time;
  - artifact schema / redaction boundaries.
- Run local focused verification.
- If local verification passes, sync/refresh `awsserver1:/home/admin/hftbacktest-cross-exchange` using the existing git-safe bundle + `merge --ff-only` pattern.
- Rerun the final go/no-go gate after remote refresh.
- Only if the final gate returns go and flow-aware guards allow a candidate, run one controlled live retry window under the approved envelope.
- Run `0618T008` ledger in `live_pulled_back` mode against the pulled-back artifacts.

acceptance criteria：
- Local implementation and tests prove the new retry is flow-aware, maker-only, cap-preserving, fail-closed, and artifact-complete.
- Any live attempt is post-only `Alo`, non-crossing, one tracked order at a time, and protected by tracked cancel plus final open-orders proof.
- If at least one maker fill is observed and T008 ledger reconciles fill, fee/rebate, inventory, and mark/PnL evidence, M2 real PnL accounting may be ready for QA judgment.
- If there is no fill, no safe candidate, incomplete ledger evidence, gate block, or remote/precheck block, the task must end as `阻塞` and M2 remains incomplete.
- No credential value, private key, raw signature, nonce, unredacted oid/cloid, or account address is written to repo artifacts or reports.

verify：
- `python -m pytest examples/hyperliquid/test_hyperliquid_tiny_live_m2_fill_loop.py examples/hyperliquid/test_hyperliquid_tiny_live_m2_public_flow_diagnosis.py examples/hyperliquid/test_hyperliquid_tiny_live_m2_pnl_ledger.py examples/hyperliquid/test_hyperliquid_tiny_live_real_order_executor.py -q`
- `python -m py_compile examples/hyperliquid/hyperliquid_tiny_live_m2_fill_loop.py examples/hyperliquid/hyperliquid_tiny_live_m2_fill_window.py examples/hyperliquid/hyperliquid_tiny_live_m2_public_flow_diagnosis.py examples/hyperliquid/hyperliquid_tiny_live_m2_pnl_ledger.py`
- `python examples/hyperliquid/hyperliquid_tiny_live_m2_fill_loop.py --help`
- `python examples/hyperliquid/hyperliquid_tiny_live_m2_fill_window.py --help`
- `python examples/hyperliquid/hyperliquid_tiny_live_m2_fill_loop.py --output-dir local_live_analysis/hyperliquid_tiny_live_m2_flow_aware_retry_0619T001 --windows 1 --wait-seconds 300 --quote-offset-ticks 0 --requote-attempts 6 --quote-hold-seconds 15 --side-policy flow_aware --max-order-size 0.00999`
- JSON validation for aggregate, side-score, attempt, remote-refresh, final-gate, and ledger manifests.
- Artifact non-empty check.
- Boundary/redaction scan.
- Independent final open-orders check on `awsserver1`.
- `git diff --check`

done：
- 回报 flow-aware side decision、quote-aging guard 行为、skip/submit/cancel/fill/open-orders/ledger 结果、artifact path、验证命令、blockers、commit id、提交信息。
- 明确说明 M2 是否仍 blocked；如果没有 T008 ledger 认可的真实 maker fill + fee/inventory/PnL 证据，必须保持 M2 blocked。
```
