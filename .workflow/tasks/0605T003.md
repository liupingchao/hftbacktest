```md
执行线程：
- 业务线程-python

任务ID：
- 0605T003

标题：
- Harden shutdown final proof with exchange reconciliation and proof-level reporting

简短描述：
- 修复 `0605T002` 诊断确认的 shutdown final proof 缺口：当前 `local_absent_from_local_orders` 只是 local-only proof，不等价于交易所 open orders 已清空。本任务只增强 shutdown final proof 语义、summary logging、safe exchange open-orders reconciliation 和 audit tail proof，不修改策略 quote/submit/cancel 行为，不改 binding/Rust/connector。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0604T016` 已修复 wait-result 分类与 `order_response_received` / `terminal_confirmed` 独立语义。
- `0605T001` 已修复 `PARTIALLY_FILLED` local active order 误判问题。
- `0605T002` 已通过 QA，确认当前 local absent proof 与缺少 exchange-side final proof 的风险属实。

规则更新提醒：
- 无

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `docs/thread-playbook.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交任务文件、业务报告、`live_tick_mm.py` 修复和 focused tests。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0605T003.md`
- `.workflow/reports/0605T003-business.md`
- `examples/binance_tick_mm/live_tick_mm.py`
- `examples/binance_tick_mm/test_backtest_tick_mm.py`
- 如 audit tail 需补最小字段，可改对应 audit/helper 文件，但不得扩大到 schema 大改。

forbidden：
- 不修改 py binding、Rust live bot/backtest、connector 核心实现、production config、normal trading loop cancel 语义、strategy quote/submit/cancel 行为。
- 不做参数搜索、回放实验扩展、promotion。
- 不启动 live / tiny-live / default-on。
- 不把本任务扩大成通用 live safety 重构。

review under test：
- 当前 shutdown helper 在 local snapshot 找不到 `order_id` 时，会给 `terminal_confirmed=True` / `final_order_status=local_absent_from_local_orders`。
- `0605T002` 已证明：这只能算 local-only proof，当前 helper 不做 exchange-side final proof。
- 当前 summary / audit tail 也没有表达 final proof level。

action：
- 增强 `ShutdownCancelResult`，增加独立 exchange-side final proof 维度，至少包括：
  - `exchange_reconciliation_checked`
  - `exchange_open_order_absent`
  - `exchange_confirmation_source`
  - `final_proof_level`
- 保持 `order_response_received` 和 `terminal_confirmed` 的独立语义，不得回退到仅凭 `wait_result == 3` 推导 terminal。
- 为 shutdown final proof 增加 safe exchange open-orders reconciliation：
  - 仅在已有安全调用路径存在时使用。
  - 必须定义检查失败、超时、权限缺失、无 client、symbol 不可用时的状态，不得伪造 exchange confirmed。
- 调整 local absent proof 语义，二选一但必须在报告中明确：
  - 方案 A：`local_absent_from_local_orders` 不再单独设置高等级 final confirmed。
  - 方案 B：保留 local terminal 判断，但必须通过 `final_proof_level=local_only` 与 exchange-reconciled proof 区分。
- 更新 shutdown summary logging，至少分开输出：
  - `wait_requests`
  - `order_responses`
  - `terminal_confirmed`
  - `unknown_or_timeout`
  - `exchange_reconciliation_checked`
  - `exchange_open_order_absent`
  - `final_proof_level` 的聚合计数或等价口径
  - `failed`
- 补最小 audit/log tail proof，使 shutdown 末尾至少能留下：
  - local proof
  - exchange reconciliation result
  - final proof level
- 新增 focused tests，至少覆盖：
  - `local_absent + exchange_absent`
  - `local_absent + exchange_still_open`
  - `exchange_check_failed`
  - `local_active_order`
  - `local_terminal + exchange_absent`
- 如当前代码已有 normal loop REST diagnostics helper，可复用其最小能力，但不得改变 normal loop 行为。

acceptance criteria：
- `local_absent` 与 `exchange_absent` 必须被独立表达，不能再混成一个 proof。
- exchange still-open 时，不得给出高等级 final proof。
- exchange reconciliation 失败时，状态必须明确，不能伪造 confirmed/absent。
- `wait_result == 3` 仍只能表达 order response，不得单独推导 terminal confirmed。
- summary / audit tail 必须能看出 final proof level。
- focused tests 必须覆盖 local absent / exchange absent、local absent / exchange still-open、exchange failure、local active、local terminal 组合场景。
- package-level `examples/binance_tick_mm/test_*.py` 应通过。
- 不得修改 binding/Rust/connector 核心实现、策略行为、live/default-on/tiny-live/promotion 边界。

verify：
- `/home/molly/anaconda3/envs/hftbacktest/bin/python -m pytest examples/binance_tick_mm/test_backtest_tick_mm.py -k "shutdown_cancel or terminal_confirmed or exchange_reconciliation or local_absent or open_orders or final_proof_level"`
- `/home/molly/anaconda3/envs/hftbacktest/bin/python -m pytest examples/binance_tick_mm/test_*.py`
- `git diff --check`

done：
- 业务报告写入 `.workflow/reports/0605T003-business.md`。
- 回报必须说明选用的 final proof 语义方案、exchange reconciliation 能力边界、summary/audit 变化、focused test 覆盖、验证结果、commit id 和 commit message。
- 报告必须明确本任务未修改策略行为、未改 binding/Rust/connector 核心、未启动 live、未做 promotion。
```
