```md
执行线程：
- 业务线程-python

任务ID：
- 0511T002

标题：
- adverse-selection timing guard default-off 实现与 Stage 6J replay

简短描述：
- 在 `0511T001` 设计规格和验收合同通过后，实现默认关闭的 adverse-selection timing guard，接入 live/backtest 共享策略路径，补充 audit 字段、unit tests 和 Stage 6J replay 候选。本任务只做本地测试和离线 replay，不启动 live，不连接交易所。

状态：
- 待执行

执行顺序：
- 0511T001 通过后执行

前置任务：
- 0511T001

规则更新提醒：
- 必须严格遵守 `0511T001` 输出的设计合同。
- 新规则必须 default-off，默认策略行为必须保持不变。
- live/backtest 必须复用同一个 `strategy_core.py` helper。
- 规则输入只能来自决策时可见状态；未来 markout 只能作为验收指标。
- 第一版只允许小 helper + 配置 + audit + tests + replay；不做 `SignalSnapshot` / `GuardState` / `DecisionContext` / `DecisionResult` 大重构。
- 第一版只允许 block add-side，reduce-side 必须继续允许。

按线程规则执行：
- .workflow/workflow-kit/thread-report-template.md
- docs/thread-playbook.md
- docs/5-8-future-plan.md
- docs/stage6i-cancel-requested-fill-risk-plan.md

是否需要提交代码：
- 需要

提交要求：
- 提交前必须包含测试和 replay 证据
- 回报必须写 `commit：<hash>` 或 `commit：无`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- examples/binance_tick_mm/strategy_core.py
- examples/binance_tick_mm/live_tick_mm.py
- examples/binance_tick_mm/backtest_tick_mm.py
- examples/binance_tick_mm/audit_schema.py
- examples/binance_tick_mm/stage6j_replay.py
- examples/binance_tick_mm/test_backtest_tick_mm.py
- examples/binance_tick_mm/test_stage6j_replay.py
- docs/5-8-future-plan.md
- findings.md
- progress.md

Fatal Failures：
- 默认配置改变现有策略行为。
- live/backtest 规则逻辑不一致。
- 使用未来 markout、future mid 或 audit overlay 作为优化决策输入。
- 候选改善来自简单减少交易，而不是目标 source-path 风险下降。
- 把 replay-only working/position overlay、未来 fill 后价格路径或 live 当时不可见诊断输出用于 live 决策。
- 未经 QA 和多样本 replay 证据，声称可以 live micro test 或默认开启规则。

MUST NOT：
- 不启动真实 live，不连接交易所，不改 AWS 状态。
- 不默认开启新规则。
- 不扩大为 maker 参数搜索或全面策略重构。
- 不使用 audit overlay 做优化 replay。
- 不做 `SignalSnapshot` / `GuardState` / `DecisionContext` / `DecisionResult` 重构。
- 不把 future mid、future markout、未来 fill 后价格路径、replay-only working/position overlay 或 live 不可见诊断输出作为策略输入。

允许输入：
- working orders。
- target bid/ask ticks。
- working order price tick。
- position。
- inflight exposure。
- cancel-requested side qty。
- last side cancel-fill timestamp。
- order age。
- feed/order latency signal。
- cancel latency bucket。
- short-horizon historical drift / rolling toxicity state。

禁止输入：
- future mid。
- future markout。
- 未来 fill 后价格路径。
- audit overlay state。
- replay-only working overlay。
- replay-only position overlay。
- live 当时不可见的诊断输出。

第一版触发条件：
- `target_deterioration`：buy side 当 `working_buy_tick - target_bid_tick >= N` 时，或 sell side 当 `target_ask_tick - working_sell_tick >= N` 时，启动 side-specific toxic cooldown。
- `cancel_requested_fill_timing`：某侧 cancel-requested fill 后，在 `50/100/200ms` 候选窗口内阻断同侧 add-side。
- `pending_cancel_toxic_window`：某侧已有 cancel-requested in-flight exposure 且 target deterioration 同时成立时，阻断同侧 add-side。

helper 合同：
- 在 `strategy_core.py` 新增类似 `adverse_timing_guard_side_blocks(...) -> GuardBlockResult` 的 shared helper。
- helper 至少返回 `buy_block`、`sell_block`、`buy_reason`、`sell_reason`。
- 如果不引入 dataclass，至少返回 `(buy_block, sell_block, buy_reason, sell_reason)`。
- live/backtest 必须调用同一个 helper，禁止复制两套规则逻辑。

配置项合同：
- `risk.adverse_timing_guard_enabled = false`
- `risk.adverse_timing_guard_target_deterioration_enabled = true`
- `risk.adverse_timing_guard_pending_cancel_enabled = true`
- `risk.adverse_timing_guard_post_cancel_fill_enabled = true`
- `risk.adverse_timing_guard_cooldown_ms = 100.0`
- `risk.adverse_timing_guard_min_target_move_ticks = 2`
- `risk.adverse_timing_guard_block_mode = "add_side_only"`

audit 字段合同：
- `adverse_timing_guard_buy_active`
- `adverse_timing_guard_sell_active`
- `adverse_timing_guard_buy_reason`
- `adverse_timing_guard_sell_reason`
- `adverse_timing_guard_buy_until_ts`
- `adverse_timing_guard_sell_until_ts`
- `adverse_timing_guard_target_move_ticks_buy`
- `adverse_timing_guard_target_move_ticks_sell`

action：
- 按 `0511T001` 设计合同在 `strategy_core.py` 实现 default-off adverse timing helper。
- 接入 live/backtest 同一路径，保持 add-side only / reduce-side allowed 语义。
- 增加配置项和 audit 字段，字段必须与本任务 audit 字段合同一致。
- 增加 unit tests：默认关闭行为不变、target deterioration 达阈值时对应 side block、cooldown 生效、cooldown 过期、add-side blocked、reduce-side allowed、pending cancel + target deterioration 能触发 block、helper 参数不得包含 future mid / future markout。
- 扩展 Stage 6J replay 候选，至少包含 `baseline_inflight_only`、`add_side_guard_only`、`adverse_timing_target_deterioration_50ms`、`adverse_timing_target_deterioration_100ms`、`adverse_timing_target_deterioration_200ms`、`add_side_guard_plus_adverse_timing_100ms`、`broad_add_side_cooldown_200ms_control`。
- 跑单样本和跨样本 replay，比较 PnL、max position、drop rate、churn、cancel-fill source-path。
- 更新 docs/finding，说明是否仍然 `diagnostic_only_no_promotion`。

verify：
- `python3 -m pytest examples/binance_tick_mm/test_backtest_tick_mm.py examples/binance_tick_mm/test_stage6j_replay.py`
- `python3 examples/binance_tick_mm/stage6j_replay.py --local-root local_live_analysis --run-id 5-10-day-control-1h-06 --out-dir /tmp/0511T002-stage6j-single`
- 跨样本 runner 或等价命令必须覆盖 `5-10-day-control-1h-06`、`5-9-small`、`5-9-noon`、`5-8-stage3-15m-livetest-v4`
- 验证 optimization replay overlays 为 `off/off/off`
- 验证 maker acceptance hard gates 没有回退

done：
- 回报必须说明实现是否严格遵守 `0511T001` 设计合同。
- 回报必须列出新增配置项、audit 字段、Stage 6J 候选。
- 回报必须给出测试和跨样本 replay 结果。
- 回报必须说明是否允许进入 live micro test。默认不允许，除非多样本证据充分且 QA 通过。
- 回报必须把 durable finding 写入 `findings.md`。
- 回报必须明确是否保持 default-off，默认行为是否不变。
- 回报必须明确是否未做大重构。
```
