```md
执行线程：
- 测试线程

任务ID：
- 0512T006

标题：
- T005 blocked-row attribution 分析计划

简短描述：
- 基于 `0512T005` 的核心现象，制定一个只读 attribution / experiment plan，用来解释为什么 pure toxic timing candidates 有 action-path coverage 但没有 Stage 6J replay risk improvement。本任务只做分析计划和实验计划，不实现脚本、不跑实验、不设计更严格候选规则；后续真正实现和实验另建 `0512T007`。

状态：
- 已通过

执行顺序：
- `0512T005` QA 后执行；如总控明确允许，也可在 `0512T005` 待验收期间作为计划任务先执行

前置任务：
- 0512T005

规则更新提醒：
- 本任务是计划任务，不改策略代码，不运行长 replay，不启动 live。
- 必须沿用 `0512T004` 三层证据边界：action-path coverage、replay-model regression、live-derived source-path proof。
- 目标是解释 T005 现象，不是提出或实现更严格候选规则。
- 第四点 “更严格候选规则实验” 暂时不做；必须等 T006 找到原因后由 `0512T007` 或后续任务处理。

按线程规则执行：
- .workflow/workflow-kit/thread-report-template.md
- docs/thread-playbook.md
- .workflow/reports/0512T005-business.md
- local_live_analysis/stage6j_cross_sample_0512T005/STAGE6J_B_REPLAY_SUMMARY.md

是否需要提交代码：
- 不需要

提交要求：
- 不需要提交
- 回报必须写 `commit：无`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- .workflow/tasks/0512T006.md
- .workflow/reports/0512T005-business.md
- local_live_analysis/stage6j_cross_sample_0512T005/stage6j_replay_summary.csv
- local_live_analysis/stage6j_cross_sample_0512T005/STAGE6J_B_REPLAY_SUMMARY.md
- local_live_analysis/stage6j_cross_sample_0512T005/stage6j_replay_decision.json
- findings.md
- progress.md
- task_plan.md

Fatal Failures：
- 修改策略代码、runner 代码或 replay 输出。
- 启动 live、连接交易所、修改 AWS 状态。
- 运行新的 Stage 6J replay 或长耗时实验。
- 在本任务内设计、实现或推荐更严格候选规则作为结论。
- 把 Stage 6J source-path 指标当成 live-derived source-path proof。
- 跳过 T005 的事实：pure toxic timing candidates 有 action-path coverage，但没有 replay risk improvement。

MUST NOT：
- 不改代码。
- 不运行 live。
- 不运行新的长 replay。
- 不新增候选规则。
- 不把 T006 写成实现任务。
- 不把 “下一步 T007 实验方向” 写成已验证结论。

背景事实：
- `0512T005` pure toxic timing candidates blocked add-side submit `594`，blocked reduce-side `0`。
- `0512T005` pure toxic timing candidates baseline action/planned diff `199`，blocked submit overlap `56`，submit removed `56`。
- `0512T005` pure toxic timing candidates没有改善 Stage 6J replay risk：baseline cancel-fill `4` / same-side worsening `3`，pure toxic timing cancel-fill `4` / same-side worsening `3`。
- `5-11-night-active` blocked `299`，但 baseline submit removal 为 `0`；`5-9-noon` 和 `5-9-small` 有 submit removal。
- 需要先解释这些现象，再决定 T007 是否值得实现 attribution runner 或新实验。

action：
- 读取 `0512T005` business report 和 Stage 6J summary，整理已知事实表。
- 制定 blocked rows 分层分析计划：
  - blocked rows 是否对应 baseline 实际 `submit_buy` / `submit_sell`。
  - blocked rows 是否只是 pre-submit eligibility 命中，但后续被 cap、soft limit、cancel-race guard、quote throttle、API guard、latency guard、two-phase replace 或其他路径吞掉。
  - 重点解释 `5-11-night-active` blocked `299` 但 submit removed `0`。
- 制定 actual submit-removal rows 分析计划：
  - 只看 `submit_removed_by_guard_count=56` 对应 rows。
  - 计划如何关联这些 rows 的后续 cancel-fill、same-side worsening、position worsening、risk source-path。
  - 判断这些 submit removals 是否命中真实 replay 风险源，还是只是命中了非风险 submit。
- 制定 reason attribution 分析计划：
  - 拆分 `pending_cancel`、`recent_cancel_request`、`recent_cancel_fill`、`target_move`、`latency` 等 blocked reason。
  - 分别统计 reason 对 blocked rows、baseline action diff、submit overlap、submit removed、risk-event overlap 的贡献。
  - 解释为什么 50/100/200ms 结果完全相同，判断 window 不是主变量还是被 pending/inflight/replay timing 离散化主导。
- 输出 `.workflow/reports/0512T006-business.md`，内容必须是计划，不是执行结果。
- 更新 `findings.md` 和 `progress.md`，记录 T006 只授权计划，不授权 T007 实验或 live。

verify：
- 人工检查 `.workflow/reports/0512T006-business.md` 是否包含三类计划：
  - blocked rows 分层计划。
  - actual submit-removal rows 分析计划。
  - reason attribution 分析计划。
- 人工检查报告是否明确不包含更严格候选规则设计。
- `python3 .workflow/build_dashboard.py`

done：
- 回报必须明确 T006 只做计划，没有实现、没有实验、没有 live。
- 回报必须明确 T006 只覆盖前三点分析，不做第四点更严格候选规则。
- 回报必须给出 T007 可执行输入清单：需要读取哪些 CSV、输出哪些表、哪些指标作为下一步决策依据。
- 回报必须明确 T007 仍应是只读 attribution / experiment implementation，除非总控另建规则设计任务。
- 回报必须明确 no live micro test。
```
