```md
执行线程：
- 业务线程-python

任务ID：
- 0528T002

标题：
- Compact replay lifecycle audit export for Stage 6 input

简短描述：
- 根据 `0527T001` 已通过的诊断结论，修复 `0526T008` 暴露的 replay audit repeated `cancel_ack` bloat：实现正式 compact replay lifecycle audit export，并在该 compact export 内按 Stage 6 语义对 terminal lifecycle rows 去重，使 Stage 6 默认消费 compact lifecycle artifact，避免 30min 样本生成 21GB full replay audit 才能校准。不得改策略行为、live 行为、fill/cancel replay 语义、参数、guard 或 promotion 状态。

状态：
- 待验收

执行顺序：
- 当前下一任务；执行前无需重新诊断 `0527T001` 已回答的问题。

前置任务：
- `0527T001` QA 已通过。
- `0526T008` QA 已通过。

规则更新提醒：
- 请先阅读规则文档后再执行本任务。
- 本任务是 replay audit 输出/Stage 6 输入规模修复，不是策略、live、parameter search、queue/touch repair 或 promotion 任务。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `docs/thread-playbook.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交代码、workflow/task/report/dashboard 更新。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

Allowed files：
- `.workflow/tasks/0528T002.md`
- `.workflow/reports/0528T002-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `.workflow/dashboard.html`
- `.workflow/dispatch_suggestions.md`
- `examples/binance_tick_mm/backtest_tick_mm.py`
- `examples/binance_tick_mm/test_backtest_tick_mm.py`
- `examples/binance_tick_mm/execution_outcome_calibration.py` only if needed to select the formal compact lifecycle artifact by contract
- `examples/binance_tick_mm/test_execution_outcome_calibration.py` only if `execution_outcome_calibration.py` is changed
- `examples/binance_tick_mm/align_live_run.py` only if needed to wire a new compact audit output path into audit replay post-processing
- `examples/binance_tick_mm/test_align_live_run.py` only if `align_live_run.py` is changed
- `local_live_analysis/stage6_audit_bloat_repair_0528T002/**`

Allowed read-only inputs：
- `.workflow/tasks/0527T001.md`
- `.workflow/reports/0527T001-business.md`
- `.workflow/reports/0527T001-qa.md`
- `.workflow/tasks/0526T008.md`
- `.workflow/reports/0526T008-business.md`
- `.workflow/reports/0526T008-qa.md`
- `local_live_analysis/5-26-active-minmove-control-30min-b/**`
- `local_live_analysis/archive/5-26-active-minmove-control-30min-b.tar.gz.sha256`

Forbidden files / areas：
- Strategy behavior, quote placement, risk, fair/target formula, inventory logic, guard semantics, production defaults, live configs.
- Core/connector APIs, standard npz schema, converter behavior.
- General replay lifecycle semantic repair, queue/touch fill repair, fill/cancel model changes.
- Any live/tiny-live run, parameter search, default-on behavior, guard relaxation, or promotion claim.
- Deleting or overwriting preserved `audit_bt_audit_replay.full.csv` or archived evidence.

Fatal Failures：
- Changing live/backtest strategy decisions, order placement, cancel/submit behavior, fill semantics, or risk controls.
- Treating compact lifecycle audit as evidence that live/replay fill/cancel semantics improved beyond the existing Stage 6 result.
- Removing full forensic audit evidence without explicit task authorization.
- Leaving Stage 6 dependent on a manually produced lifecycle-min workaround.
- Producing a full replay audit path that still writes tens of millions of duplicate `cancel_ack` rows by default for `0526T008`.
- Claiming readiness for live/default-on/tiny-live/promotion.

Input bug facts from `0527T001`：
- `0526T008` sample: `5-26-active-minmove-control-30min-b`
- full replay audit rows: `33,293,775`
- full replay audit `cancel_ack` rows: `32,893,719`
- unique cancel-ack `order_id`: `10,380`
- lifecycle-min canonical Stage 6 input rows: `401,642`
- lifecycle-min `cancel_ack` rows: `10,380`
- Diagnosis: repeated rows are primarily audit replay lifecycle export / order-state tracking repetition, not Stage 6 label-generation output.

Implementation requirements：
1. Add a formal compact replay lifecycle audit output as the chosen route.
2. Preserve all decision rows needed for action/reject/throttle/market-view alignment.
3. Preserve lifecycle facts needed by Stage 6:
   - order submit
   - order new/update if required by existing labels
   - cancel sent / cancel request
   - first cancel ack per order
   - fill / partial fill
   - expired / rejected
4. Inside the compact export, de-dup terminal lifecycle rows by Stage 6 semantics: keep the first meaningful terminal fact per `event_type + order_id`.
5. Keep full forensic audit optional or explicitly preserved; it must not be the default Stage 6 input.
6. Make Stage 6 consume the formal compact lifecycle artifact by contract, without requiring manual file renaming or manual lifecycle-min post-processing.
7. Add focused regression tests proving repeated terminal `cancel_ack` rows are compacted while distinct lifecycle facts are preserved.

Acceptance target on `0526T008`：
- Running the repaired audit replay / post-processing path should produce the compact lifecycle audit artifact automatically.
- Compact replay lifecycle audit should have `cancel_ack` count close to unique cancel-ack `order_id` count, not tens of millions.
- Stage 6 calibration on `5-26-active-minmove-control-30min-b` should run against the compact artifact without exit `137`.
- Existing maker acceptance hard gates should not regress:
  - action match
  - planned action match
  - reject reason match
  - throttle reason match
  - working lifecycle semantic/blocking gates
  - strict replay lag gate
- No strategy/live/default/promotion behavior changes.

Required output/report：
- `.workflow/reports/0528T002-business.md`
- If using local diagnostic output:
  - `local_live_analysis/stage6_audit_bloat_repair_0528T002/run_manifest.json`
  - optional small summaries proving compact counts and Stage 6 command result

verify：
- Focused tests for changed code, at minimum:
  - `python -m pytest examples/binance_tick_mm/test_backtest_tick_mm.py -q`
- If Stage 6 input selection changes:
  - `python -m pytest examples/binance_tick_mm/test_execution_outcome_calibration.py -q`
- Run the repaired path or a task-scoped lightweight reproduction against `5-26-active-minmove-control-30min-b` sufficient to prove compact output is generated automatically and Stage 6 can consume it.
- `python3 .workflow/build_dashboard.py`
- `git diff --check`

done：
- Business report ends in `待验收`.
- Report states exact code changes and the compact lifecycle artifact contract.
- Report includes before/after counts for `cancel_ack` or a bounded reproduction that demonstrates the fix.
- Report states whether `0526T008` Stage 6 now runs without manual lifecycle-min workaround.
- Report explicitly states no strategy behavior, live behavior, fill/cancel semantics, parameter search, default-on, guard relaxation, or promotion was changed.
```
