```md
执行线程：
- 测试线程-research

任务ID：
- 0604T002

标题：
- Event-driven horizon rerun and comparison for xemm quiet a/b/c

简短描述：
- 使用 `0604T001` 修改后的 event-driven Hyperliquid alignment / de-aliased horizon logic，对 `xemm_0603_quiet_a`、`xemm_0603_quiet_b`、`xemm_0603_quiet_c` 三个既有 public-only synchronized samples 做本地只读重跑与比较；不重新采集、不改代码、不接 private/order、不改策略、不做参数搜索、不 live/default-on/tiny-live、不 promotion。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0604T001` implementation commit `4dd05a9` / metadata commit `a55880e` 已完成，等待 QA。

规则更新提醒：
- 请先阅读规则文档后再执行本任务。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `docs/thread-playbook.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 不需要

提交要求：
- 不需要代码提交；如写 workflow report，可提交报告/任务文件或由总控后续统一收口。
- 不得触碰 private/order/strategy/live/parameter/default-on/tiny-live/promotion 边界。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0604T002.md`
- `.workflow/reports/0604T002-business.md`
- `local_live_analysis/event_horizon_comparison_0604T002/**`

read-only inputs：
- `local_live_analysis/cross_exchange_public_sample_xemm_0603_quiet_a/**`
- `local_live_analysis/cross_exchange_public_sample_xemm_0603_quiet_b/**`
- `local_live_analysis/cross_exchange_public_sample_xemm_0603_quiet_c/**`
- `local_live_analysis/binance_led_hyperliquid_data_contract_0601T004/**`

action：
- 对三个样本分别生成 task-scoped event-mode artifacts：
  - Hyperliquid event-mode alignment
  - cross-exchange join
  - lead-lag analysis
  - pricing-signal runner
- 比较三类指标：
  - 数据/对齐质量：overlap、decision rows、join coverage、primary rows、missing/future joins。
  - horizon alias / effective horizon：100/250/500 是否仍指向相同 future row、future-row delta 分布、effective age。
  - market regime / signal：Binance/Hyperliquid vol、trade counts、lead-lag verdict counts、pricing recommendation。

verify：
- `python examples/hyperliquid/hyperliquid_raw_alignment.py --decision-mode event ...` for each sample.
- `python examples/hyperliquid/cross_exchange_lead_lag_join.py ...` for each sample.
- `python examples/hyperliquid/cross_exchange_lead_lag_analysis.py ...` for each sample.
- `python examples/hyperliquid/binance_led_pricing_signal_runner.py ...` for each sample.
- Parse generated JSON manifests with `python -m json.tool`.
- Run a focused comparison script over generated CSV/JSON outputs.

done：
- Report sample ids, artifact paths, quality metrics, horizon alias findings, volatility/regime comparison, lead-lag verdict counts, pricing recommendations, blockers/caveats, and boundary statement.
- Business report should end in `待验收` if all three local reruns and comparison complete.
```
