# 任务派发

执行线程：
- 业务线程-python/cross-exchange-research

任务ID：
- 0730T014

标题：
- RESEARCH-DATASET-FAIL-CLOSED-BOUNDARY-REPAIR

简短描述：
- 修复 T013 QA 发现的三个 P2：clean replacement 保留、timeline
  index/manifest/CSV 边界闭环、raw identity/count/provenance 完整门禁。
- 重建现有本地 R0 产物并重新执行独立完整性复算。

状态：
- 已通过

执行顺序：
- 当前唯一研究实现任务

前置任务：
- `0730T013` QA `未通过`。

是否需要提交代码：
- 不需要

提交要求：
- 当前工作区包含未提交前置任务，不创建混合提交。
- 回报使用 `commit：无`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 重放 T013 counterexamples，独立复算重建后的真实产物。

files：
- `.workflow/tasks/0730T014.md`
- `.workflow/reports/0730T014-business.md`
- `examples/hyperliquid/cross_exchange_research_dataset.py`
- `examples/hyperliquid/test_cross_exchange_research_dataset.py`
- `docs/skhynix_cross_exchange_research_dataset.md`
- `local_live_analysis/skhynix_cross_exchange_research_0730T013/`
- `task_plan.md`
- `progress.md`
- `findings.md`

action：
- Build replacement in a temporary sibling while preserving the current
  output; publish with backup/rollback only after full success.
- Validate index campaign/profile/continuity and recompute segment gaps.
- Scan real timeline CSV for row count, first/last common timestamp and
  timestamp monotonicity.
- Validate timeline manifest source-raw SHA against current Binance, fast L2
  and standard L2 source files.
- Validate Binance symbol on depth/bookTicker/trade and exact snapshot/event
  counts.
- Validate fast/standard/auxiliary Hyperliquid coin/dex, exact channel set,
  raw row count and collection-manifest reconciliation.
- Add focused failure-injection tests for every QA counterexample.
- Rebuild the real eight-segment output and independently rehash/recount it.

hard boundary：
- Local existing data only。
- No network, AWS, SSH or new collection。
- No signal fitting, parameter search, order action or strategy process。
- R1 remains locked until T014 independent QA passes。

verify：
- Focused tests include old-output preservation, bad index campaign/time/
  continuity, bad Binance depth symbol, bad standard coin, extra channel,
  raw-row mismatch and timeline source-raw mismatch.
- Real rebuild plus `24/24` output, `56/56` source and timeline-boundary
  independent checks.
- `py_compile`, CLI `--help`, focused regression and `git diff --check`.

done：
- All three T013 P2 counterexamples fail closed。
- Failed clean rebuild preserves the prior accepted output。
- Successful rebuild atomically publishes the new output。
- T014 enters `待验收`; R1 remains locked pending QA。
