# 任务派发

执行线程：
- SKHYNIX Liquidity Break Onset A0 Contract 业务线程

任务ID：
- 0828T007

标题：
- 起草并冻结 LIQUIDITY_BREAK_ONSET_V1 A0 Causal-Anchor Contract

简短描述：
- 在 `OBI_REVERSAL_V1` A3 失败后，建立一个独立版本的 outcome-blind
  causal-anchor contract。把 alignment 从完成态确认移动到原始消息流中
  第一次可因果观测的多通道流动性破坏 onset。

状态：
- 待验收

执行顺序：
- 当前唯一正式任务

前置任务：
- 0828T006

规则更新提醒：
- 已阅读 AGENTS.md、workflow-kit manual、task/report/QA templates、
  Conditional-Risk Research Methodology Kernel v1、phase-alignment 结果和
  OBI reversal A0-A3 canonical plan/results。

按线程规则执行：
- AGENTS.md
- .workflow/workflow-kit/workflow-manual.md

是否需要提交代码：
- 需要

提交要求：
- contract 静态验证后使用 exact-path commit 冻结
- 回报必须带 commit id、文档 SHA256 和提交信息

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- docs/skhynix_binance_liquidity_break_onset_v1_a0_causal_anchor_contract_20260828.md
- .workflow/tasks/0828T007.md
- .workflow/reports/0828T007-business.md

action：
- 注册全新 hypothesis identifier `LIQUIDITY_BREAK_ONSET_V1`。
- 明确它不是 `OBI_REVERSAL_V1` 的 faster-alignment robustness rescue。
- 冻结原始消息 event-time ordering、snapshot/depth continuity 和 reset
  boundaries。
- 冻结三个可解释 pressure components：
  - vulnerable-side net depletion；
  - aggressive trade pressure；
  - whole-book flow pressure。
- 冻结 50ms primary rolling window、60s causal robust baseline、500ms
  guard、`z_star=3.0` 和 two-of-three coherence。
- 冻结 `start_anchor` 为首次满足 predicate 的当前 message，不允许
  backdate、future dwell 或 offline change point。
- 冻结 `end_anchor` 仅用于 active lock、duplicate suppression 和支持
  geometry，不作为 recovery outcome。
- 冻结 outcome-blind controls、downstream adverse/recovery target stub、
  H0/H1 information family、A0 gates 和 A1 timeliness stop gate。
- 明确 A0 zero-outcome access boundary、输出 schema、failure
  classifications 和 forbidden rescue operations。

verify：
- 对照现有 raw reconstruction、A0 contracts 和 methodology kernel。
- 检查所有常量、方向、窗口、边界、分类和后续 gate 在 outcome access
  前闭合。
- 检查 Markdown fence parity。
- `git diff --check`。
- 计算并记录 contract SHA256。
- exact-path Git commit，不纳入已有无关 staged/untracked files。

done：
- 回报 contract path、hypothesis/version boundary、start/end anchor
  semantics、zero-outcome boundary、A0 gates、A1 stop gate、SHA256、
  commit id 和 QA 边界。

明确禁止：
- 不实现或执行 A0 detector。
- 不读取未来价格或目标。
- 不拟合 H0/H1。
- 不修改 OBI_REVERSAL_V1 artifacts 或 classification。
- 不采集数据，不访问 private API，不下单。
- 不提交工作树中已有无关文件。
