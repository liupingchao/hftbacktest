# 任务派发

执行线程：
- 业务线程-python/public-collector

任务ID：
- 0729T006

标题：
- BINANCE-SAME-CONNECTION-SNAPSHOT-REFRESH-REPAIR

简短描述：
- 修复 `0729T005` live canary 暴露的 Binance bootstrap 问题。
- 当 REST snapshot 落后于第一条 buffered diff 时，保留当前 WebSocket
  和有界 buffer，重新获取 snapshot 并重新扫描同一 buffered 序列。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- `0729T005` QA `未通过`。

规则更新提醒：
- 请先阅读规则文档后再执行本任务。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 不需要

提交要求：
- 当前工作区已有未提交 workflow、hunting 与 collector 工作。
- 本任务不创建混合提交，回报使用 `commit：无`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0729T006.md`
- `.workflow/reports/0729T006-business.md`
- `.workflow/reports/0729T006-qa.md`
- `examples/hyperliquid/synchronized_public_collection.py`
- `examples/hyperliquid/test_synchronized_public_collection.py`
- `docs/qa-acceptance-report.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

action：
- 保持 WebSocket reader 和有界 buffer 存活。
- snapshot `lastUpdateId` 小于第一条可用 diff `U` 时，不关闭连接；
  在同一连接上重新获取 REST snapshot。
- 每次刷新后从完整 retained buffer 重新查找
  `U <= lastUpdateId <= u` bridge。
- snapshot refresh 次数必须有界，耗尽后 fail closed。
- manifest 记录 snapshot refresh 次数、请求总数、每次 snapshot ID、
  stale candidate 和最终 bridge 证据。
- 保持 reconnect、continuity、reader shutdown、public-only、Hyperliquid
  和策略行为不变。

hard boundary：
- Offline implementation and tests only.
- No AWS or exchange collection in T006。
- No private/account/order/cancel endpoint。
- No strategy, fill, PnL or promotion change。

verify：
- 新增首次 snapshot 过旧、第二次 snapshot 在同连接成功 bridge 的回归。
- 新增 snapshot refresh 耗尽 fail-closed 回归。
- 保持 missing bridge、continuity、overflow、reconnect 和 shutdown 回归。
- `python -m py_compile examples/hyperliquid/synchronized_public_collection.py`
- `python -m pytest examples/hyperliquid/test_synchronized_public_collection.py -q`
- `python -m pytest examples/hyperliquid/test_hyperliquid_public_sample.py -q`
- `git diff --check`。

done：
- Report exact same-connection refresh semantics、manifest fields、测试计数、
  blockers and QA readiness。
