```md
执行线程：
- 业务线程-docs

任务ID：
- 0519T002

标题：
- Step 6 closure decision and boundary update

简短描述：
- 基于 `0519T001` 的 final lifecycle calibration 结果，planning-only 收口 Step 6：判断 Step 6 是否关闭，是否允许进入 Step 7 / Step 8 / Step 9，并把 residual queue/touch 风险、样本需求和后续边界写清楚。

状态：
- 已通过

执行顺序：
- 依赖 `0519T001`，不可并行

前置任务：
- `0519T001` QA 已通过

规则更新提醒：
- 本任务是 planning-only closure decision，不允许改代码、不允许跑新实验、不允许启动 live。

按线程规则执行：
- .workflow/workflow-kit/thread-report-template.md
- docs/thread-playbook.md
- task_plan.md
- progress.md
- findings.md

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

Allowed files：
- `.workflow/tasks/0519T002.md`
- `.workflow/reports/0519T002-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `.workflow/dashboard.html`
- `.workflow/dispatch_suggestions.md`

Allowed read-only inputs：
- `.workflow/tasks/0519T001.md`
- `.workflow/reports/0519T001-business.md`
- `.workflow/reports/0519T001-qa.md`
- `local_live_analysis/5-13-day-control-30min/stage6_final_calibration_0519T001/**`
- prior Step 6 reports from `0514T006` through `0518T001`

Forbidden files / areas：
- Any strategy, replay, runner, live, connector, schema, or generated sample implementation file.
- Any live scripts, AWS/remote state, live collection, or promotion action.

Requires separate task：
- Any Step 7 inventory/execution implementation.
- Any Step 8 quote-update mechanics implementation.
- Any Step 9 replay experiment.
- Any new sample collection.
- Any residual queue/touch repair.

Fatal Failures：
- 修改代码或运行新的实验。
- 将 single-sample evidence 过度声明为 live readiness。
- 将 `4948` / queue-ahead proxy residual 从 design-only 擅自升级为 repair authorization。

action：
- 读取 `0519T001` QA 结果和产物。
- 更新 Step 6 closure decision：
  - Step 6 是否关闭
  - replay lifecycle 是否足够支持后续 default-off quote-adjustment replay experiment
  - `4948` / queue-ahead proxy residual 是否继续搁置
  - 是否需要更多 current-format samples，以及它是否阻塞 Step 7 / Step 8 / Step 9
  - Step 7 / Step 8 / Step 9 的前置边界和禁止事项
- 必须把结论写入 `task_plan.md`、`progress.md`、`findings.md`。

verify：
- 文档一致性检查：确认 `task_plan.md`、`progress.md`、`findings.md` 对 Step 6 closure 不冲突。
- `python3 .workflow/build_dashboard.py`
- `git diff --check`

done：
- 回报必须明确 Step 6 的最终状态。
- 回报必须明确下一步建议启动 Step 7、Step 8、Step 9 还是补样本。
- 回报必须明确 residual queue/touch issue 的处理边界。
- 回报必须写 `commit id` 和提交信息。
```
