```md
执行线程：
- 业务线程-live-awsserver1

任务ID：
- 0624T001

标题：
- M2 AWS public BBO evidence-chain diagnosis

简短描述：
- 基于 `0623T010` 的 public-only candidate funnel 结果，诊断 accepted fresh-touch / queue-reset 证据链为什么几乎全部落到 `synthetic_current_event_only`，并定位是 public BBO 数据连续性、事件排序、缓存使用、queue-reset 识别，还是策略证据条件本身导致。
- 本任务只优化诊断和证据可解释性，不放松 quote distance、cap、post-only、fresh-touch 证据要求、private/order 边界，也不授权真实 canary。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- `0623T010` business execution complete and reported as `待验收`.

规则更新提醒：
- 请先阅读 `.workflow/workflow-kit/workflow-manual.md`、`.workflow/workflow-kit/task-dispatch-template.md`、`.workflow/workflow-kit/thread-report-template.md`、`.workflow/workflow-kit/qa-acceptance-template.md`、`task_plan.md`、`progress.md`、`findings.md` 后再执行本任务。

按线程规则执行：
- `AGENTS.md`

是否需要提交代码：
- 需要

提交要求：
- 自测 / 远端 public-only 诊断证据生成后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

host / execution location：
- `awsserver1`
- Remote repo: `/home/admin/hftbacktest-cross-exchange`
- Preferred remote interpreter: `/home/admin/.venvs/hyperliquid-sdk-0618T002/bin/python`
- Local machine may create the task/report, perform git-safe bundle sync, pull back artifacts, validate outputs, and record results.

files：
- `.workflow/tasks/0624T001.md`
- `.workflow/reports/0624T001-business.md`
- `examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- `examples/hyperliquid/test_hyperliquid_tiny_live_m2_event_driven_watcher.py`
- `local_live_analysis/hyperliquid_tiny_live_m2_aws_bbo_evidence_chain_0624T001/**`
- `task_plan.md`
- `progress.md`
- `findings.md`

read-only inputs：
- `.workflow/tasks/0623T010.md`
- `.workflow/reports/0623T010-business.md`
- `local_live_analysis/hyperliquid_tiny_live_m2_aws_candidate_funnel_0623T010_20260623T064432Z/**`
- `.workflow/tasks/0623T009.md`
- `.workflow/reports/0623T009-business.md`
- `.workflow/reports/0623T009-qa.md`

hard boundary：
- No live orders, no credential reads, no private/account/order endpoints, no real order endpoint, no cancel endpoint, no live client initialization, no remote final gate rerun, no T008 live ledger claim, no quote-distance change, no one-tick-back, no inside-spread, no cap relaxation, no taker/crossing, no default-on behavior, no M3/stable PnL/promotion.
- Public live source use is allowed only for Hyperliquid public L2/trades and Binance public market data.
- Do not weaken accepted fresh-touch / queue-reset requirements. The task may add diagnostics, counters, row-level fields, or post-processing, but must not convert missing evidence into a pass.

action：
- Add or reuse task-scoped diagnostics that explain the `missing_touch_freshness_or_queue_reset_evidence` layer at row level.
- For rejected public candidates, capture:
  - candidate trigger source: trade, book update, synthetic fallback, or mixed
  - latest real BBO history count and time span available at decision time
  - last book update age and last trade age
  - touch stability duration, last touch change time, and same-touch history availability
  - top reset / visible queue depletion status from top qty and order-count changes
  - event ordering around the candidate: immediately preceding/following l2Book and trade timestamps
  - same-side strict-through and at-or-through qty/count within the configured window
  - dynamic-size throughput inputs and skip reason atoms
- Produce aggregate diagnostics for:
  - BBO history continuity and gaps
  - l2Book/trade density by minute
  - synthetic-current-event-only share by trigger source
  - queue-reset detector pass/block reasons
  - candidate examples for each dominant rejection family
- Run a public-only no-submit AWS diagnosis window or post-process the T010 artifacts if sufficient. Prefer a fresh AWS window if needed to collect missing row-level evidence.
- Decide whether the dominant blocker is data collection density, event ordering/cache visibility, queue-reset detector behavior, or genuinely rare fresh-touch/reset conditions in the sampled market.

verify：
- `python -m pytest examples/hyperliquid/test_hyperliquid_tiny_live_m2_event_driven_watcher.py -q`
- `python -m py_compile examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- `python examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py --help`
- Remote public-only no-submit diagnosis on `awsserver1` with `/home/admin/.venvs/hyperliquid-sdk-0618T002/bin/python`, if fresh data is needed
- Validate JSON/CSV outputs, non-empty required artifacts, and reason histograms
- `git diff --check`

done：
- 回报 public stream counts, candidate count, BBO continuity/gap summary, l2Book/trade density, `synthetic_current_event_only` share, queue-reset pass/block summary, event-ordering/cache findings, representative rejected-candidate examples, and whether the dominant blocker is data continuity, ordering/cache usage, detector logic, or real market rarity.
- 明确说明本任务是否发现需要修代码；如果需要，给出最小下一步修复建议。
- 明确说明本任务不授权真实下单、读凭据、private/account/order/cancel endpoint、remote final gate、T008 live ledger claim、quote-distance change、one-tick-back、inside-spread、cap relaxation、M3/stable PnL/default-on/promotion。
- 回报 artifact path, commit id, 提交信息。
```
