```md
执行线程：
- 业务线程-live-submit

任务ID：
- 0706T002

标题：
- 0625T008 Edge-Qualified Tiny-Live Calibration

简短描述：
- 根据总控最新授权“授权 first live-submit calibration / 授权开始”，创建并执行第一轮 `0625T008` live-submit calibration。
- 本任务只允许一笔最小 live-submit calibration：Hyperliquid `BTC`，post-only `Alo`，far-from-mid canary order，随后 tracked cancel 和 final open-orders proof。若任何门失败，必须 fail-closed。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- `0706T001 / 0625T008-PREFLIGHT` QA 已通过。
- 用户/总控已在当前会话明确授权：`授权 first live-submit calibration` / `授权开始`。

规则更新提醒：
- 请先阅读 `.workflow/workflow-kit/workflow-manual.md`、`.workflow/workflow-kit/task-dispatch-template.md`、`.workflow/workflow-kit/thread-report-template.md`、`.workflow/workflow-kit/qa-acceptance-template.md`、`task_plan.md`、`progress.md`、`findings.md` 后再执行本任务。

按线程规则执行：
- `AGENTS.md`
- `docs/thread-playbook.md`
- `docs/cross_exchange_mvp_auto_loop_plan.md`

是否需要提交代码：
- 不需要

提交要求：
- 不需要提交
- 回报必须带 `commit：无` 和 `提交信息：无`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0706T002.md`
- `.workflow/reports/0706T002-business.md`
- `.workflow/reports/0706T002-qa.md`
- `local_live_analysis/cross_exchange_mvp_t008_live_submit_calibration_0706T002/**`
- `docs/qa-acceptance-report.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

authorized live envelope：
- host: `awsserver1`
- remote repo: `/home/admin/hftbacktest-cross-exchange`
- remote python: `/home/admin/.venvs/hyperliquid-sdk-0618T002/bin/python`
- env file path: `/home/admin/XEMM_rust_latest/.env`
- symbol: `BTC`
- order type: limit
- time in force: `Alo`
- max order count: `1`
- max order size: `0.01 BTC`
- max order notional: `700 USDC`
- max position delta: `0.01 BTC`
- max loss: `30 USDC`
- canary price policy: far-from-mid post-only buy, `--canary-price-offset-bps 200`, with executor cap `MAX_CANARY_LIMIT_PX=69900`
- tracked cancel: required
- final open orders proof: required

hard boundary：
- Do not run a continuous bot.
- Do not place more than one order.
- Do not relax post-only `Alo`.
- Do not use taker/crossing/inside-spread/one-tick-back.
- Do not allow pre-existing open orders unless separately authorized.
- Do not disclose or persist credential/secret values, private keys, raw signatures, or nonces.
- Do not promote, deploy, set default-on behavior, or claim final MVP pass.
- If preflight, order, cancel, pullback, redaction, or final open-orders proof fails, stop fail-closed.

action：
- Record current local/remote state and credential file path existence without printing secret values.
- Execute the existing QA-accepted Hyperliquid real-order canary path on `awsserver1`.
- Pull back redacted artifacts locally.
- Validate manifest, endpoint flags, order/cancel/final-open-orders evidence, and redaction boundary.
- Write business report and send to QA.

verify：
- Remote checkout clean and on `cross-exchange`.
- Hyperliquid SDK importable on remote Python.
- `executor_manifest.json` parses.
- `real_order_endpoint_called=true`, `real_cancel_endpoint_called=true`, `shutdown_proof_status=pass`, and final open orders empty for the tracked canary.
- Redaction scan finds no raw credential/private key/signature values.
- `git diff --check`.

done：
- Report whether order submitted, whether it rested/rejected, whether cancel succeeded, final open-orders status, artifact path, and final recommendation.
- Explicitly state that no further live-submit, canary expansion, default-on, deployment, promotion, or final MVP pass is authorized.

commit：
- 无

提交信息：
- 无
```
