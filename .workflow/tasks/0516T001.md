```md
执行线程：
- 业务线程-python

任务ID：
- 0516T001

标题：
- Queue/priority evidence diagnosis for residual case 4948

简短描述：
- 基于 `0515T006` 已通过的单 case diagnosis，继续只读研究 `28940|sell` / `4948` 的 queue / priority / exposure evidence，目标是把 `queue_exposure_proxy_bias_possible` 进一步拆成更可操作的原因，或明确为什么现有证据仍不足以支持 repair。

状态：
- 待验收

执行顺序：
- 当前唯一后续任务

前置任务：
- `0515T006` QA 已通过

规则更新提醒：
- 本任务仍是 single-case read-only diagnosis，不是 repair。
- 只允许分析 `4948`，不得扩展到 generalized queue / touch repair。
- 不允许修改 replay fill model、queue logic、strategy logic。

按线程规则执行：
- .workflow/workflow-kit/thread-report-template.md
- docs/thread-playbook.md
- task_plan.md
- progress.md
- findings.md

是否需要提交代码：
- 需要

提交要求：
- 自测/检查通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

Allowed files：
- `.workflow/tasks/0516T001.md`
- `.workflow/reports/0516T001-business.md`
- `examples/binance_tick_mm/replay_lifecycle_mismatch_diagnosis.py`
- `examples/binance_tick_mm/test_replay_lifecycle_mismatch_diagnosis.py`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `.workflow/dashboard.html`
- `.workflow/dispatch_suggestions.md`
- `docs/qa-acceptance-report.md`

Allowed generated outputs：
- `local_live_analysis/5-13-day-control-30min/stage6h_queue_priority_evidence_0516T001/**`

Allowed read-only inputs：
- `.workflow/reports/0515T006-business.md`
- `.workflow/reports/0515T005-business.md`
- `local_live_analysis/5-13-day-control-30min/stage6g_single_residual_fill_diagnosis_0515T006/**`
- `local_live_analysis/5-13-day-control-30min/stage6f_cancel_race_residual_repair_0515T005/**`
- `local_live_analysis/5-13-day-control-30min/audit_live_5-13-day-control-30min.csv`
- `local_live_analysis/5-13-day-control-30min/out/backtest_audit_replay/audit_bt_audit_replay.csv`
- `local_live_analysis/5-13-day-control-30min/raw_market_data/**`
- `local_live_analysis/5-13-day-control-30min/t009_fixed_sidecar/**`

Forbidden files / areas：
- `examples/binance_tick_mm/backtest_tick_mm.py`
- `examples/binance_tick_mm/strategy_core.py`
- replay engine behavior / backtest fill model implementation
- strategy behavior, fair/target formulas, configs, risk guards, quote placement behavior
- live scripts, AWS/remote state, live collection
- sample expansion tasks
- core Rust, connector, py `event_dtype`, standard npz schema

Requires separate task：
- 任何 replay repair implementation。
- generalized touch fill assumption change。
- generalized queue exposure / priority model change。
- 新采集 current-format 样本。

Fatal Failures：
- 在 diagnosis 任务里直接改 replay repair 代码。
- 将 `4948` 的单 case diagnosis 扩展成 broad queue/touch model change。
- 在缺乏额外证据时把 `queue_exposure_proxy_bias_possible` 直接写成已证实 root cause。

action：
- 只分析 `4948` 的 queue / priority / exposure 证据。
- 进一步回答：
  - submit 后到 replay fill 前，top1/top5 size 的变化能否说明 live 更可能因为 queue priority 没吃到这笔 fill
  - 同价 supportive trade 的 cumulative quantity 与 top1 ask qty / visible size 对比如何
  - replay fill 前后的 depth evolution 是否支持“触价但排不到”而不是“根本不该 fill”
  - 当前 evidence 是否已经足够支持一个专门的 queue-proxy repair design task
- 输出必须明确：
  - `4948` 更像哪一种 queue/priority failure mode
  - 当前仍缺什么证据
  - 是否应该继续停在 diagnosis，而不是开 repair

verify：
- `python -m pytest examples/binance_tick_mm/test_replay_lifecycle_mismatch_diagnosis.py`
- `python examples/binance_tick_mm/replay_lifecycle_mismatch_diagnosis.py --help`
- `python3 .workflow/build_dashboard.py`
- `git diff --check`

done：
- 回报必须明确 `4948` 的 queue/priority evidence diagnosis 结论。
- 回报必须明确是否足以进入新的 repair design / implementation task。
- 回报必须明确本任务未修改 replay、未改策略、未补样本、未启动 live。
- 回报必须写 `commit id` 和 `提交信息`。
```
