# 0721T042 PERSISTED-HISTORY-IDENTITY-REDACTION-REPAIR

执行线程：
- 总控 auto-loop / 业务执行线程

任务ID：
- 0721T042

标题：
- Enforce exact redaction for persisted nested history identity

简短描述：
- 修复 T041 QA 发现的 persisted representation fail-open。
- 每个已持久化 nested oid/cloid alias 必须严格为 `<redacted>`；raw identity即使与token完全匹配也fail closed。

状态：
- 执行中

执行顺序：
- 当前唯一任务

前置任务：
- `0721T041`（未通过）

规则更新提醒：
- 本任务offline-only；不得执行live、private、account、order、cancel、network、remote或service操作。
- Immutable T040 evidence不得修改，不得启动第二个T040 window。
- Dynamic spread、fill feedback、inventory skew、multi-level继续锁定。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `.workflow/reports/0721T041-qa.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交。
- 回报必须带implementation commit和提交信息。
- 业务报告、QA报告和fixed QA文档按workflow分别提交。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- Modify: `examples/hyperliquid/cross_exchange_delayed_history_probe_acceptance.py`
- Modify/Test: `examples/hyperliquid/test_cross_exchange_delayed_history_probe_acceptance.py`
- Workflow: task、business、QA、fixed QA、`task_plan.md`、`progress.md`、`findings.md`

source：
- Branch: `cross-exchange`
- Dispatch base: `866a59d`
- Immutable live artifact: `local_live_analysis/principal_alignment_delayed_history_probe_0721T040/`

action：
- 对每个persisted nested order的`oid/orderId/order_id/cloid/clientOrderId/client_order_id`，只要字段存在，其值必须exact为`<redacted>`。
- Raw numeric/string identity、partial redaction marker、null或empty alias均fail closed。
- Token、alias map、marker presence和outer identity的T041合同保持严格。
- 新增raw OID、raw cloid、raw dual-identity hostile fixtures；即使token/alias map同步匹配也必须blocked。
- OID-only、cloid-only和dual identity producer-written redacted round-trip保持pass。
- Immutable T040 artifact与六个原QA hostile fixtures重新验收。

verify：
- Focused acceptance tests通过。
- QA `raw_nested_identity`反例blocked且无异常。
- Immutable T040 artifact `14/14 pass`。
- 六个原QA hostile artifacts全部blocked。
- Full Hyperliquid regression通过。
- T037/T031/T026/T022/T016 exact historical boundaries保持；T026使用`1800s`。
- 全部验证offline-only。

done：
- Persisted nested identity同时满足semantic token binding和exact redaction representation contract。
- T041 QA唯一P1及其P2测试缺口关闭。
- 独立QA通过前不得启动新的fixed-quote live rerun。

stop conditions：
- 修复需要改写immutable evidence。
- 原始T040 artifact被错误拒绝。
- 任一raw identity hostile仍pass。
- 需要任何live/private/network/remote/service操作。

blockers：
- 无。

提交信息：
- `Enforce persisted history identity redaction`
