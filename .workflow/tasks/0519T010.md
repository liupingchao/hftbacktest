```md
执行线程：
- 业务线程-python

任务ID：
- 0519T010

标题：
- Step 9C multi-sample quote-adjustment validation plan

简短描述：
- 基于 `0519T008` runner 已通过和 `0519T009` current-format T006 样本已通过，制定 Step 9 后续 planning-only 合同。目标是定义多样本场景覆盖、复跑方法、稳定性判定、候选接受/拒绝标准，以及 promotion/live 前置边界。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0519T008` QA 已通过
- `0519T009` QA 已通过

规则更新提醒：
- 本任务是 planning-only。不得改策略代码、不得改 runner、不得跑 replay sweep、不得采集 live、不得 default-on、不得 promotion。

按线程规则执行：
- `.workflow/workflow-kit/thread-report-template.md`
- `docs/thread-playbook.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 只提交 workflow/plan/findings/report 文档。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

Allowed files：
- `.workflow/tasks/0519T010.md`
- `.workflow/reports/0519T010-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `.workflow/dashboard.html`
- `.workflow/dispatch_suggestions.md`

Allowed read-only inputs：
- `.workflow/reports/0519T008-qa.md`
- `.workflow/reports/0519T009-qa.md`
- `.workflow/reports/0519T008-business.md`
- `.workflow/reports/0519T009-business.md`
- `examples/binance_tick_mm/quote_adjustment_replay.py`
- `examples/binance_tick_mm/test_quote_adjustment_replay.py`
- `local_live_analysis/5-13-day-control-30min/stage9b_quote_adjustment_replay_0519T008/**`
- `local_live_analysis/5-19-day-control-30min/stage9b_quote_adjustment_replay_0519T009/**`
- `local_live_analysis/5-19-day-control-30min/maker_acceptance.json`
- `local_live_analysis/5-19-day-control-30min/alignment_report_audit_replay.json`

Forbidden files / areas：
- Strategy code, runner code, tests, live scripts, AWS/remote state, production configs, connector/core, converter/schema, local large data artifacts.
- Any replay sweep, live collection, default-on behavior, live micro test, production behavior change, Step 5C promotion, queue/touch repair, inventory-control implementation, or promotion claim.

Requires separate task：
- Implementing any runner change.
- Collecting additional current-format samples.
- Running multi-sample replay validation.
- Promoting or live-testing any candidate.
- Changing quote behavior, inventory behavior, post-only anchor behavior, or queue/touch repair logic.

Fatal Failures：
- 修改代码或运行实验而非制定计划。
- 把 `0519T009` 的 `promising_but_single_sample` 解释为 promotion、live readiness 或 generalized profitability proof。
- 用单样本 PnL 作为候选接受标准。
- 忽略 `0519T008` 的旧样本 T006 字段缺失边界。

action：
- 制定 Step 9C planning-only 合同，至少回答：
  1. Step 9 后续到底要解决什么问题。
  2. 数据场景覆盖率应如何定义和分桶。
  3. 需要哪些 current-format 样本，样本数量和最低事件量门槛是什么。
  4. 如何在多样本上复跑 `quote_adjustment_replay.py`。
  5. 每个 candidate family 的跨场景稳定性如何判定。
  6. 哪些 metrics 是 hard gate，哪些是 diagnostic/proxy。
  7. 如何处理 single-sample `promising_but_single_sample`。
  8. 什么条件下 candidate 应被 reject / keep-for-research / ready-for-tiny-live-design。
  9. Step 10 live validation 前置条件是什么。
- 明确 Step 9 的主线：
  - 首先解决 current-format 数据场景覆盖率和复跑合同。
  - 然后寻找能在多个场景下稳定改善 maker execution quality 的 default-off candidate。
  - 稳定性优先于单样本 PnL。
- 场景覆盖至少包括：
  - volatility / markout regime
  - spread width / tick distance regime
  - trade intensity / fill opportunity regime
  - stale / latency / join-age / anchor-age regime
  - API / token / throttle / churn regime
  - inventory state / recovery regime
  - post-only / bad-price / clamp / suppress regime
  - cancel-fill / fill-after-cancel risk regime
  - top5 / market-view quality regime
- 产出写入 `task_plan.md`、`progress.md`、`findings.md`，并生成 `.workflow/reports/0519T010-business.md`。

verify：
- 文档中必须明确：
  - no code change
  - no replay sweep
  - no live
  - no default-on
  - no promotion
  - no single-sample PnL acceptance
- `python3 .workflow/build_dashboard.py`
- `git diff --check`

done：
- 回报必须给出 Step 9C 计划摘要。
- 回报必须列出下一步应该是采集任务、read-only multi-sample runner task，还是先修改 runner 的条件。
- 回报必须说明 `0519T008` 和 `0519T009` 在计划里的角色：
  - T008 证明 runner / artifact mechanics。
  - T009 证明 current-format T006 sample 可用，但仍是 single-sample diagnostic。
```
