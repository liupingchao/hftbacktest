# 0719T005 STRICT-CANCEL-SUCCESS-BOUNDED-ATTEMPT-REPAIR

执行线程：
- 总控 auto-loop / 业务实现线程

任务ID：
- 0719T005

标题：
- Strict authoritative cancel-success semantics and bounded attempt parsing

简短描述：
- Repair the T004 QA fail-open where malformed `success` values are accepted as authoritative cancel proof, and make oversized canonical attempt strings fail closed rather than raise.
- This task is offline-only and does not authorize live/private/account/order/cancel/network/remote/service actions.

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- `0719T004`（未通过；redaction-safe token and ordinary strict-attempt repair passed, cancel success-value semantics and oversized attempt handling failed）

规则更新提醒：
- No new live task may be created or started until this task is independently QA accepted.
- Multi-level, dynamic spread, fill feedback, inventory skew and actual quote behavior changes remain off.

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`

是否需要提交代码：
- 需要

提交要求：
- 离线 focused/full regression 通过后提交。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- Modify: `examples/hyperliquid/hyperliquid_tiny_live_real_order_executor.py`
- Modify: `examples/hyperliquid/hyperliquid_tiny_live_m2_fill_window.py`
- Modify: `examples/hyperliquid/cross_exchange_t024_same_window_acceptance.py`
- Modify/add focused tests under `examples/hyperliquid/`
- Workflow: this task, business report, QA report, `docs/qa-acceptance-report.md`, `task_plan.md`, `progress.md`, `findings.md`

action：
- Define an explicit single-cancel authoritative-success contract:
  - outer response status is exactly `ok`;
  - `response.data.statuses` is a list of exactly one row;
  - accepted row is exactly string `success`, or an object containing only `success`;
  - object success value is a nonempty canonical string or a positive non-bool integer reference;
  - false, null, zero, empty/whitespace string, float, object, list, extra keys, error keys and multiple statuses fail closed.
- Apply this contract in the executor producer path.
- Independently implement the same semantics in Task 12 acceptance without importing/calling the executor/producer helper.
- Keep generic redundant `already canceled, or filled` detection non-authoritative.
- Bound cancel-reference attempt identity to `1..2147483647`:
  - reject digit strings longer than the maximum canonical width before integer conversion;
  - catch conversion errors;
  - reject values above the maximum;
  - preserve the T004 ordinary malformed-attempt contract.
- Add producer, independent rebuild and complete acceptance regressions for malformed success values and oversized attempts.
- Preserve the T004 persisted token contract and all earlier adversarial regressions.
- Keep quote, strategy, risk, envelope and activation behavior unchanged.

verify：
- T004 QA `[{"success": false}]` complete acceptance reproduction fails closed.
- `success=null/0/""/" "/0.0/{}/[]` all fail producer, independent rebuild and complete acceptance.
- Extra-key and multi-status payloads fail closed.
- Exact string `success`, nonempty reference string and positive integer reference continue to pass.
- 5000-digit attempt returns structured fail-closed in producer and acceptance without exception.
- `2147483647` passes; `2147483648` and longer digit strings fail closed.
- T004 persisted standalone/manager exact-equality integrations continue to pass.
- Prior partial/conflicting target, forged summary, ambiguous raw response, missing raw lists, cross-attempt and redundant generic regressions remain fail closed/pass as specified.
- Modified Python `py_compile`, CLI `--help`, `git diff --check`.
- Focused executor/fill/acceptance/standalone/manager regressions.
- Full `python -m pytest examples/hyperliquid -q`.

stop conditions：
- Any falsey/container/multi-status success payload can become authoritative.
- Any malformed/oversized attempt raises instead of returning fail-closed.
- Acceptance imports/calls producer cancel-success or attempt helpers.
- Any strategy behavior, live envelope or activation change.

done：
- Producer and acceptance independently enforce one explicit cancel-success protocol.
- Oversized attempts are bounded and fail closed.
- T004 token/redaction repair remains intact.
- Independent QA accepts the offline repair.

blockers：
- No new live task may start before this task is QA accepted.

commit：
- `19e4b4a7e740a01763fcaf67df28ef3283abbabe`

提交信息：
- `Harden authoritative cancel proof semantics`
