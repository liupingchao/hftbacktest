```md
执行线程：
- 业务线程-python

任务ID：
- 0514T005

标题：
- Maker execution outcome label runner implementation

简短描述：
- 基于 `0514T004` 已通过的 requirements-only 合同，实现只读 maker execution outcome label runner，并在 `5-13-day-control-30min` 数据集上生成 label 测试/覆盖产物。任务只实现 label 构造、统计摘要和测试，不改策略、不启动 live、不做 queue/fill calibration、不声称 counterfactual 结论。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- 0514T004 QA 已通过

规则更新提醒：
- 请先阅读规则文档后再执行本任务。T004 已通过，但它只授权本任务作为单独实现任务；不得把实现回写到 T004。

按线程规则执行：
- .workflow/workflow-kit/thread-report-template.md
- docs/thread-playbook.md
- task_plan.md
- progress.md
- findings.md

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

Allowed files：
- `.workflow/tasks/0514T005.md`
- `.workflow/reports/0514T005-business.md`
- `examples/binance_tick_mm/execution_outcome_labels.py`
- `examples/binance_tick_mm/test_execution_outcome_labels.py`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `.workflow/dashboard.html`
- `.workflow/dispatch_suggestions.md`
- `local_live_analysis/5-13-day-control-30min/stage5_execution_outcome_labels_0514T005/**`

Allowed read-only inputs：
- `.workflow/tasks/0514T004.md`
- `.workflow/reports/0514T004-qa.md`
- `local_live_analysis/5-13-day-control-30min/audit_live_5-13-day-control-30min.csv`
- `local_live_analysis/5-13-day-control-30min/out/backtest_audit_replay/audit_bt_audit_replay.csv`
- `local_live_analysis/5-13-day-control-30min/t009_fixed_sidecar/joined_decisions.csv`
- `local_live_analysis/5-13-day-control-30min/t009_fixed_sidecar/metrics.json`
- `local_live_analysis/5-13-day-control-30min/t009_fixed_sidecar/joined_decisions.metrics.json`
- `local_live_analysis/5-13-day-control-30min/maker_acceptance_stage3.json`
- `local_live_analysis/5-13-day-control-30min/stage4_pricing_research_0514T003/candidate_signal_metrics.csv`
- `local_live_analysis/5-13-day-control-30min/stage4_pricing_research_0514T003/markout_by_horizon.csv`

Forbidden files / areas：
- strategy behavior, fair/target formulas, configs, risk guards, quote placement behavior
- live scripts, AWS/remote state, live collection
- core Rust, connector, py `event_dtype`, standard npz schema
- canonical `audit_schema.py` changes
- modifying existing local_live_analysis artifacts outside the new T005 output directory

Requires separate task：
- 改策略或接入 T003/T005 信号。
- 做 queue/fill model calibration。
- 做 exact queue-position proof。
- 做 counterfactual quote placement simulation。
- 启动 live 或 live micro test。
- 修改 audit schema、connector/core、standard npz schema。

Fatal Failures：
- 把 observed labels 解释为“如果换价/不撤/改信号就一定成交或盈利”。
- 把本任务的 label runner 结果解释为策略 PnL 证明、live readiness、production promotion 或 exact queue proof。
- 修改策略行为、默认参数、live 配置、connector/core 或 canonical schema。
- 写入旧的 Stage 4/T009/T007 产物目录，覆盖历史研究产物。

action：
- 实现 `examples/binance_tick_mm/execution_outcome_labels.py` 只读 runner。
- Runner 必须从 existing local artifacts 构造 order/action/lifecycle observed labels，输出到：
  - `local_live_analysis/5-13-day-control-30min/stage5_execution_outcome_labels_0514T005/`
- 必须覆盖 T004 原始 outcome 主线：
  - fill probability：submit 后 `100ms / 500ms / 1s / 5s` 是否 fill。
  - time-to-fill：submit 到 first fill 的 elapsed ms。
  - adverse selection after fill：fill 后 `100ms / 500ms / 1s / 5s` side-adjusted markout。
  - spread capture：fill price vs future mid / side-adjusted captured ticks。
  - queue / priority proxy：quote placement、top1/top5 size、book age、join age、latency 相关 proxy labels。
  - cancel-to-fill race：fill-after-cancel-request、cancel request to fill delay、对应 adverse markout。
  - post-only / reject / throttle / churn：reject/throttle/drop/churn buckets、quote update/cancel-readd count/rate。
  - inventory impact：position before/after、inventory_score bucket、inventory-reducing vs inventory-increasing fill。
- 必须覆盖 T004 新增 7 类 label：
  - quote placement / distance：touch/inside/step-back、distance-to-BBO ticks、crossed/post-only-risk、rounding bucket。
  - missed-fill / opportunity cost：unfilled-then-favorable-mid-move、missed spread capture、conservative-quote bucket。
  - realized PnL decomposition：realized spread、effective spread、fee-adjusted captured ticks、inventory mark-to-market component、per-fill net EV proxy。
  - tail risk：p1/p5/worst side-adjusted markout、tail adverse bucket、tail cancel-fill bucket、tail inventory exposure。
  - partial-fill / lifecycle：full/partial/no fill、fill ratio、remaining qty、multi-fill count、final order state、cancel ack before/after fill。
  - inventory cycle：time-to-flat、max inventory excursion、inventory holding time、cycle reset after crossing zero。
  - sample validity / censoring：horizon-observable flag、right-censored flag、tail-truncated flag、missing lifecycle flag。
- 必须按 T004 的统计方法要求输出 label test summaries：
  - continuous labels：Spearman/Pearson、bucket mean/median、top-bottom spread、monotonicity、time split stability。
  - binary labels：event rate、lift vs baseline、odds ratio。
  - time-to-event / censored labels：censoring counts、discrete hazard bucket / Kaplan-Meier-style survival table；若只实现 discrete hazard，必须说明 Cox-style 仍是后续建模候选。
  - count/rate labels：count rate、exposure-normalized rate、rate ratio。
  - multiclass/lifecycle labels：contingency table、conditional probability / one-vs-rest lift。
  - tail labels：tail quantile、tail mean、worst bucket concentration、exceedance rate。
  - placement/opportunity-cost labels：placement bucket x signal bucket tradeoff table。
- 必须输出至少这些产物：
  - `execution_outcome_label_summary.md`
  - `execution_outcome_labels.csv`
  - `label_coverage.csv`
  - `label_statistics.csv`
  - `fill_horizon_labels.csv`
  - `fill_markout_labels.csv`
  - `placement_opportunity_tradeoff.csv`
  - `lifecycle_label_summary.csv`
  - `censoring_summary.csv`
  - `run_manifest.json`
- 如果某类 label 在当前数据集缺少可观测字段或样本过少，runner 不得静默跳过；必须在 `label_coverage.csv` 和 summary 中标记 `unavailable` / `low_sample` / `observed_only_proxy` 及原因。

verify：
- `python -m pytest examples/binance_tick_mm/test_execution_outcome_labels.py`
- `python examples/binance_tick_mm/execution_outcome_labels.py --help`
- `python examples/binance_tick_mm/execution_outcome_labels.py --run-dir local_live_analysis/5-13-day-control-30min --output-dir local_live_analysis/5-13-day-control-30min/stage5_execution_outcome_labels_0514T005`
- 人工检查输出目录包含所有 required artifacts。
- 人工检查 `label_coverage.csv` 覆盖 T004 原始 outcome 主线和新增 7 类 label，且每类 label 有 `available`、`unavailable`、`low_sample` 或 `observed_only_proxy` 状态。
- `python3 .workflow/build_dashboard.py`
- `git diff --check`

done：
- 回报必须说明每个 T004 label 类是否已在 `5-13-day-control-30min` 上实现并测试。
- 回报必须说明哪些 label 是 fully observed，哪些是 observed-only proxy，哪些 unavailable/low_sample 及原因。
- 回报必须说明使用了哪些统计方法，并明确没有把 observed 相关性解释为 counterfactual queue/fill proof。
- 回报必须列出输出目录和关键文件。
- 回报必须明确未改策略、未启动 live、未改 core/connector/schema、未覆盖历史产物。
- 回报必须写 `commit id` 和提交信息。
```
