```md
执行线程：
- 业务线程-python

任务ID：
- 0513T002

标题：
- MarketView provenance / top5 audit transparency 最小实现

简短描述：
- 基于 `0513T001` 已通过的规划合同，在 Binance maker 策略层实现最小 `MarketView / BookViewSnapshot` 透明化和 top5 audit provenance。只做透明化、audit 字段、focused tests 和最小验证；不改交易策略行为、不改 `hbt.depth(0)` core API、不运行 live、不运行新的 replay/sweep。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- 0513T001

规则更新提醒：
- 严格执行 `0513T001` 文件边界。
- 只允许修改 `strategy_core.py`、`live_tick_mm.py`、`backtest_tick_mm.py`、`test_backtest_tick_mm.py` 以及 workflow/report 文档。
- 不允许修改 core API、converter/npz schema、connector book management、configs、stage6j candidate/ranking、live scripts、AWS 状态。
- 不允许改变交易语义、fair/target 公式、risk guard、submit/cancel/reduce-side 规则、默认参数。

按线程规则执行：
- .workflow/workflow-kit/thread-report-template.md
- docs/thread-playbook.md
- .workflow/reports/0513T001-business.md
- .workflow/reports/0513T001-qa.md

是否需要提交代码：
- 不需要

提交要求：
- 不需要提交
- 回报必须写 `commit：无`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

Allowed files：
- `examples/binance_tick_mm/strategy_core.py`
- `examples/binance_tick_mm/live_tick_mm.py`
- `examples/binance_tick_mm/backtest_tick_mm.py`
- `examples/binance_tick_mm/test_backtest_tick_mm.py`

Allowed generated / workflow files：
- `.workflow/tasks/0513T002.md`
- `.workflow/reports/0513T002-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `.workflow/dashboard.html`
- `.workflow/dispatch_suggestions.md`

Forbidden files / areas：
- `hftbacktest/`
- `py-hftbacktest/`
- Rust crates / workspace core files
- connector / live deployment scripts
- `examples/binance_tick_mm/config*.toml`
- `examples/binance_tick_mm/stage6j_replay.py`
- live run scripts、AWS/remote deployment state、archive tarballs

Requires separate task：
- 修改 `hbt.depth(0)` 或 core depth API。
- 修改 Binance raw converter / npz schema。
- 修改 connector 本地 order book 管理。
- 增加 `U/u/pu`、`lastUpdateId`、bookTicker provenance，如果当前 Python 策略层拿不到这些字段。
- 实现 microprice / OFI / queue feature study。
- 修改交易规则、fair price 公式、quote placement、risk guard 或 live 参数。

Fatal Failures：
- 改变 `decide_actions()` 交易语义、submit/cancel/reduce-side 规则、guard 判定结果、order id 生成规则或默认参数。
- 修改 fair/target 公式、throttle、latency guard、inventory guard、position limit、risk 参数默认值。
- 修改 `hbt.depth(0)` core API、converter/npz schema、connector book management。
- 运行 live 或新的 Stage 6J replay/sweep。
- 把 top5 overlay/provenance 写成 full L2 / replay book reconstruction proof。

action：
- 在 `strategy_core.py` 新增轻量 `MarketView` / `BookViewSnapshot` 结构和 `build_market_view_from_depth(...)` helper。
- live/backtest 外层改为通过 helper 构造 decision market view。
- 增加 audit provenance 字段：`market_view_source`、`top5_source`、`market_overlay_source`、`top5_overlay_source`、`book_view_ts_local`、`book_view_ts_exch`、`book_view_feed_latency_ns`、`book_view_stale_ms`、`top5_depth_best_bid_tick`、`top5_depth_best_ask_tick`。
- overlay 语义优先采用 T001 推荐 B：不 overlay top5，只显式标源。即 `market_view_source=audit_overlay` 时，`top5_source` 仍可为 `replay_depth`。
- 如果 update id / bookTicker provenance 当前层拿不到，不实现伪字段；在报告中标为 later task。
- 增加 focused tests 覆盖 helper、audit schema、overlay provenance。

verify：
- `python -m pytest examples/binance_tick_mm/test_backtest_tick_mm.py -k "top5 or market_state_overlay or build_audit_row or market_view"`
- `python examples/binance_tick_mm/maker_acceptance.py --help`
- `python examples/binance_tick_mm/align_live_run.py --help`
- `python3 .workflow/build_dashboard.py`
- `git diff --name-only` 检查实现文件边界。

done：
- 回报必须说明实现内容、测试结果和未实现的 later-task provenance 字段。
- 回报必须确认未改交易语义、未改 core API、未改 configs、未运行 live、未运行新的 replay/sweep。
- 回报必须写 `commit：无`。
```
