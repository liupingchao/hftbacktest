# 任务派发

执行线程：
- 业务线程-python/public-collector

任务ID：
- 0729T004

标题：
- BINANCE-SNAPSHOT-BUFFER-BRIDGE-REPLAY-REPAIR

简短描述：
- 修复 `0729T003` 暴露的 Binance REST snapshot 与 WebSocket diff-depth
  启动桥接缺口。
- 在 snapshot 获取期间并发读取和缓存 WebSocket 消息，只有满足 bridge
  与连续性门禁才允许产出 replay-ready Binance L2 数据。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- `0729T003` QA `未通过`。

规则更新提醒：
- 请先阅读规则文档后再执行本任务。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 不需要

提交要求：
- 当前工作区已有未提交工作流与 hunting 证据，本任务不创建混合提交。
- 回报使用 `commit：无`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0729T004.md`
- `.workflow/reports/0729T004-business.md`
- `.workflow/reports/0729T004-qa.md`
- `examples/hyperliquid/synchronized_public_collection.py`
- `examples/hyperliquid/test_synchronized_public_collection.py`
- `docs/qa-acceptance-report.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

action：
- Binance WebSocket subscribe 后立即启动持续 reader。
- REST snapshot 获取期间把收到的 public messages 缓存在有界队列中。
- 只接受第一条满足 `U <= lastUpdateId <= u` 的 depth event。
- 丢弃 snapshot 之前的过期 depth events，从 bridge event 开始写入。
- 每条后续 depth event 强制 `pu == previous u`。
- snapshot 无法 bridge、buffer overflow、depth shape/continuity 断裂时
  fail closed，并按既有 reconnect 上限重新 bootstrap。
- 每次 reconnect 必须重新获取 snapshot 并重新 bridge。
- manifest 必须记录 bridge、buffer、continuity、bootstrap failure 和
  replay-ready 状态。
- 保持 public-only、symbol profile、Hyperliquid、alignment 和策略行为
  不变。

hard boundary：
- Offline implementation and tests only.
- No AWS/remote execution.
- No public/private network collection.
- No exchange credential, account, order or cancel endpoint.
- No new 2H/8H run in this task.
- No strategy, fill, PnL or promotion claim.

verify：
- `python -m py_compile examples/hyperliquid/synchronized_public_collection.py`
- `python examples/hyperliquid/synchronized_public_collection.py collect-binance-public --help`
- `python -m pytest examples/hyperliquid/test_synchronized_public_collection.py -q`
- `python -m pytest examples/hyperliquid -q`
- `git diff --check -- examples/hyperliquid/synchronized_public_collection.py examples/hyperliquid/test_synchronized_public_collection.py .workflow/tasks/0729T004.md .workflow/reports/0729T004-business.md`

done：
- Report implementation boundary, bridge/reconnect/continuity semantics,
  manifest fields, focused/full test counts, blockers and QA readiness.
