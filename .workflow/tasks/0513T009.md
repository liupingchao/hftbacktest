```md
执行线程：
- 业务线程-python

任务ID：
- 0513T009

标题：
- 修复 T007 Binance snapshot bootstrap / buffered depth replay

简短描述：
- 基于 `0513T007` QA 未通过结论，修复 T007 standalone sidecar 的 Binance local book bootstrap 逻辑：snapshot 到达后必须使用 snapshot 前已缓冲 depthUpdate 中满足 `U <= lastUpdateId + 1 <= u` 的第一条 update，并从该 update 开始按 `pu == previous_u` 连续应用。验收固定使用 `5-13-day-control-30min`，不重新 live 采集。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- 0513T007 QA 未通过
- 0513T008 已采集 `5-13-day-control-30min`

规则更新提醒：
- 本任务只修复 T007 sidecar local-book bootstrap / sync-state 逻辑。
- 不允许启动 live，不允许重新采集，不允许改策略行为，不允许修改标准 hftbacktest npz 主事件 schema。
- 验收只使用既有本地样本 `local_live_analysis/5-13-day-control-30min/`。

按线程规则执行：
- .workflow/workflow-kit/thread-report-template.md
- docs/thread-playbook.md
- .workflow/reports/0513T007-qa.md
- .workflow/reports/0513T008-business.md
- docs/binance_top5_provenance.md
- findings.md
- task_plan.md

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

Allowed files：
- `.workflow/tasks/0513T009.md`
- `.workflow/reports/0513T009-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `.workflow/dashboard.html`
- `.workflow/dispatch_suggestions.md`
- `docs/qa-acceptance-report.md`
- `docs/binance_top5_provenance.md`
- `examples/binance_tick_mm/binance_top5_provenance.py`
- `examples/binance_tick_mm/test_binance_top5_provenance.py`
- `local_live_analysis/5-13-day-control-30min/t009_fixed_sidecar/**`
- `local_live_analysis/5-13-day-control-30min/t009_sample_classification.md`

Allowed read-only inputs：
- `local_live_analysis/5-13-day-control-30min/**`
- `.workflow/reports/0513T007-qa.md`
- `.workflow/reports/0513T008-business.md`

Forbidden files / areas：
- hftbacktest core Rust `Event` / data reader / `hbt.depth(0)` API
- py-hftbacktest global `event_dtype`
- standard converted npz `data` schema or existing event semantics
- connector local book management
- `examples/binance_tick_mm/align_live_run.py`
- `examples/binance_tick_mm/maker_acceptance.py`
- strategy rule logic, fair/target formula, risk guards, quote placement
- `examples/binance_tick_mm/deploy/*.sh`
- AWS/remote deployment state
- live run directories or raw input artifacts outside T009 output path
- full L2 persistence beyond top5 sidecar

Requires separate task：
- 将 sidecar/join 集成进 `align_live_run.py`。
- 修改 formal market-view acceptance gate。
- 做 top5 microprice / OFI / queue proxy feature research。
- 改 connector/core API 让策略实时读取 provenance/top5 字段。
- 启动新 live 采集或 live micro test。

Fatal Failures：
- 重新采集 live 或修改远端 AWS 状态。
- 修改标准 npz 主事件 schema 或 core/event dtype。
- 只隐藏 `sync_gap` 字段而没有按 Binance local book 规则正确 bootstrap。
- 使用未来 top5 row join decision row。
- 输出仍然 `first_valid_update_aligned=false` 或 full-run `gap_crossed_join_count` 大面积非零，却把样本升级为 top5 microprice / OFI / queue proxy 可用。

Bug evidence：
- `5-13-day-control-30min` full-run 中，snapshot `raw_seq=6`，`lastUpdateId=10537138804218`。
- Binance first valid update 条件要求覆盖 `lastUpdateId + 1 = 10537138804219`。
- snapshot 前 buffered depth `raw_seq=5`：`U=10537138802913`，`u=10537138805036`，`pu=10537138802699`，覆盖 `10537138804219`。
- snapshot 后 depth `raw_seq=7`：`U=10537138805242`，`u=10537138807174`，`pu=10537138805036`，正好接上 `raw_seq=5` 的 `u`。
- 当前 T007 没有在 snapshot 后回放/使用 buffered `raw_seq=5`，而是把 `raw_seq=7` 当作 first valid update，导致 `first_valid_update_aligned=false`、`sync_gap=true`、`gap_crossed_join_count=47499/47499`。

action：
- 修改 `examples/binance_tick_mm/binance_top5_provenance.py` 的 reconstructed top5 book bootstrap：
  - 在 snapshot 前 buffer depthUpdate metadata 和 top-level level deltas。
  - snapshot 到达后，从 buffered + future updates 中找到第一条满足 `U <= lastUpdateId + 1 <= u` 的 update。
  - 对第一条 valid update，先应用 snapshot，再从该 update 开始应用 level deltas。
  - 后续每条 depthUpdate 必须检查 `pu == previous_u`；连续时保持 `sync_aligned=true` / `sync_gap=false`，断链时显式 `sync_gap=true`。
  - top5 sidecar 必须保留 raw_seq、U/u/pu、snapshot_lastUpdateId、sync flags、startup_excluded、first_valid_update_aligned、bookTicker/depth BBO match/age。
  - join 逻辑保持 as-of only，不使用未来 top5 row。
- 增加 focused tests 覆盖：
  - snapshot 前 buffered update 覆盖 `lastUpdateId+1`，修复后 `first_valid_update_aligned=true`。
  - first valid update 可以来自 snapshot 前 buffer，且 snapshot 后下一条 `pu` 可接上 buffered update 的 `u`。
  - 如果 buffer 中没有覆盖 update，仍应显式 `sync_gap=true`。
  - decision join 不使用未来值，`future_join_count=0`。
- 用 `5-13-day-control-30min` 重新生成 T009 fixed sidecar：
  - 输出目录：`local_live_analysis/5-13-day-control-30min/t009_fixed_sidecar/`
  - 使用完整 raw gzip，不使用 max-messages smoke。
  - 重新运行 decision join。
- 写入 `local_live_analysis/5-13-day-control-30min/t009_sample_classification.md`。

verify：
- `python -m pytest examples/binance_tick_mm/test_binance_top5_provenance.py`
- `python examples/binance_tick_mm/binance_top5_provenance.py build-sidecars --input-gz local_live_analysis/5-13-day-control-30min/raw_market_data/btcusdt_20260513.gz --out-dir local_live_analysis/5-13-day-control-30min/t009_fixed_sidecar --sample-id 5-13-day-control-30min-t009 --symbol BTCUSDT --tick-size 0.1 --buffer-size 8000000`
- `python examples/binance_tick_mm/binance_top5_provenance.py join-decisions --audit-csv local_live_analysis/5-13-day-control-30min/audit_live_5-13-day-control-30min.csv --top5-csv local_live_analysis/5-13-day-control-30min/t009_fixed_sidecar/top5_sidecar.csv --out-csv local_live_analysis/5-13-day-control-30min/t009_fixed_sidecar/joined_decisions.csv --max-age-ms 250`
- `python3 .workflow/build_dashboard.py`

Acceptance metrics：
- `first_valid_update_aligned=true`
- full-run `depth_pu_mismatch_count=0`
- `future_join_count=0`
- `decision_join_coverage=1.0`
- `gap_crossed_join_count=0` or explicitly explained if any isolated gaps remain
- final `data` row mapping coverage remains `1.0`
- standard npz dtype unchanged
- bookTicker/depth BBO match/mismatch reported
- sample classification updated using fixed sidecar

done：
- 回报必须说明 bug 如何修复、哪些 tests 覆盖、T009 fixed sidecar metrics、join metrics、样本分类是否升级。
- 回报必须明确没有 live、没有策略变更、没有 core/npz schema 变更。
- 回报必须写 `commit id` 和提交信息。
```
