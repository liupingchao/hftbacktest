```md
执行线程：
- 业务线程-python

任务ID：
- 0604T011

标题：
- Fix live safety same-count open-order drift detection

简短描述：
- 修复 `evaluate_live_safety()` 漏检 REST 与本地 open order 数量相同但订单内容不同的 drift。必须复用 `0604T010` 的 case matrix 做回归验证，证明同数量 side/price/qty/order-set drift 不再返回 `ok`，同时保持 quote key 相同但 id 不同的正常场景不误报。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- `0604T010` business diagnosis 已完成，报告路径：`.workflow/reports/0604T010-business.md`。

规则更新提醒：
- 请先阅读规则文档后再执行本任务。
- 本任务只修复 live safety open-order drift 判定和 focused tests；不得扩大到策略 quote/order 行为、connector、生产配置、live 启动、参数搜索或 promotion。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交任务文件、业务报告和本任务代码/测试改动。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0604T011.md`
- `.workflow/reports/0604T011-business.md`
- `examples/binance_tick_mm/strategy_core.py`
- `examples/binance_tick_mm/test_backtest_tick_mm.py`
- 必要 workflow tracking 文件仅限本任务状态/结论更新。

forbidden：
- 不修改策略 quote placement / submit / cancel / fill 行为。
- 不修改 connector、生产配置、默认 live 参数、audit schema 或 compare 语义。
- 不启动 live / tiny-live / default-on。
- 不做参数搜索、回放实验扩展、promotion。
- 不在本任务内扩展 `open_order_diff()` 的 key 语义到 status / executedQty / tif / cancel-request 等元数据字段；这些字段保持 `0604T010` 记录的表达能力边界。
- 不修复与 same-count open-order drift 无关的 live safety 风险项。

action：
- 修复 `evaluate_live_safety()` 的 open-order mismatch gate，使其在以下任一条件成立时进入现有 open-order mismatch 流程：
  - `rest_open_order_count != local_open_order_count`
  - `open_order_diff` 非空
- 复用现有 open-order safety 状态机：
  - `open_order_grace`
  - `open_order_mismatch_pending`
  - `open_order_mismatch`
  - `open_order_mismatch_confirmations`
  - `safety_detail = open_order_diff`
- 保持现有数量不一致行为不回退。
- 基于 `0604T010` 的 case matrix 补 focused regression tests，至少覆盖：
  - `single_side_drift_bid_vs_ask`
  - `single_price_tick_drift`
  - `single_qty_drift`
  - `single_price_and_qty_drift`
  - `two_orders_one_leg_price_drift`
  - `two_orders_same_count_disjoint_set`
  - `single_random_client_quote_key_different`
  - `boundary_quote_key_same_id_different_expected_no_diff`
  - `boundary_status_exec_tif_different_same_quote_key`
- 对前 7 个 `open_order_diff` 非空的 same-count drift case，验证：
  - 第一次确认不足时返回 `open_order_mismatch_pending`。
  - 达到 `open_order_mismatch_confirmations` 后返回 `open_order_mismatch`。
  - `safety_detail` 保留对应 `open_order_diff`。
- 对后 2 个边界 case，验证：
  - quote key 相同但 id/client id 不同仍返回 `ok`。
  - status/exec/tif/cancel-request 元数据差异在当前 diff key 语义下仍不产生 `open_order_diff`，不作为本任务 hard mismatch。

acceptance criteria：
- `0604T010` 中 7 个 `open_order_diff` 非空的 same-count drift case 不再返回 `ok`。
- confirmation 逻辑有效：pending 与 confirmed mismatch 都有测试覆盖。
- `safety_detail` 在 same-count drift mismatch 中包含非空 `open_order_diff`。
- `open_order_mismatch_count` 的 live 主循环触发条件无需改动即可消费新的 `open_order_mismatch_pending/open_order_mismatch` status；如需要说明，报告必须解释。
- 正常 random client/order id 场景不误报。
- 不修改 `open_order_diff()` 元数据语义；报告必须说明 status/exec/tif 等字段仍是后续单独任务边界。
- 不改 audit schema、生产配置、策略 quote/order 行为。

verify：
- `python -m pytest examples/binance_tick_mm/test_backtest_tick_mm.py -k "evaluate_live_safety or open_order_diff"`
- `python -m pytest examples/binance_tick_mm`
- 可选：一次性 Python 片段复跑 `0604T010` case matrix，报告中记录前 7 个 case 已从 `ok` 变为 pending/mismatch。
- `git diff --check`

done：
- 业务报告写入 `.workflow/reports/0604T011-business.md`。
- 回报必须说明改动点、`0604T010` case matrix 的实际验证结果、focused pytest 结果、是否有未覆盖边界、commit id 和 commit message。
- 业务报告应以 `待验收` 收口并进入 QA。
```
