```md
执行线程：
- 业务线程-python

任务ID：
- 0513T001

标题：
- MarketView provenance / top5 audit transparency 规划合同

简短描述：
- 基于 `0512T008` 结论，把原 T009 收敛为新的 `0513T001` planning-only 任务：制定 `MarketView / BookViewSnapshot` 策略层透明化、top5 audit provenance、overlay 语义和验收方案。本任务只做实现计划和验收方案，不做代码实现、不修改 `hbt.depth(0)` 底层 API、不运行 replay/live。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- 0512T008

规则更新提醒：
- 本任务使用 plan model / 规划模式。
- 只写实现计划、边界和验收方案。
- 不做代码实现，不改策略逻辑，不改 `hbt.depth(0)` 底层 API，不运行 replay/sweep，不启动 live。
- `0513T001` 是原 T009 的日期更新版本；后续真正实现必须另建任务。

按线程规则执行：
- .workflow/workflow-kit/thread-report-template.md
- docs/thread-playbook.md
- task_plan.md
- progress.md
- findings.md
- .workflow/reports/0512T008-business.md

是否需要提交代码：
- 不需要

提交要求：
- 不需要提交
- 回报必须写 `commit：无`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- .workflow/tasks/0513T001.md
- .workflow/reports/0513T001-business.md
- task_plan.md
- progress.md
- findings.md

Fatal Failures：
- 修改 `examples/binance_tick_mm/` 策略逻辑或配置默认值。
- 修改 hftbacktest core / `hbt.depth(0)` 底层 API。
- 启动 live、连接交易所、修改 AWS 状态。
- 运行新的 Stage 6J replay、backtest sweep 或候选规则实验。
- 把 top5 overlay 对齐写成 replay book reconstruction 已修复或 full L2 已证明。
- 直接进入 microprice / OFI / queue 信号研究或策略实现。

MUST NOT：
- 不改代码实现。
- 不运行 live。
- 不运行新的 replay 或 sweep。
- 不设计交易规则或策略收益候选。
- 不声明 live promotion。

目标：
- 明确是否需要修改 `hbt.depth(0)`：本任务结论应倾向先不改底层 API，而是在 Binance maker 策略层新增显式 `MarketView / BookViewSnapshot` 包装层。
- 设计一个最小透明化实现方案，让 live/backtest 都通过统一 helper 构造 decision market view。
- 设计 top5 对齐和 provenance 方案：区分 `live_depth`、`replay_depth`、`audit_overlay` 来源，避免 overlay 后制造虚假对齐感。
- 设计 audit 字段扩展方案，覆盖 market view source、top5 source、overlay source、decision timestamp、feed latency、book staleness，以及可得的 depth/bookTicker provenance。
- 设计验收方案，确保后续实现只提升透明度和可诊断性，不改变交易行为。

后续 `0513T002` 文件边界：
- Allowed files：
  - `examples/binance_tick_mm/strategy_core.py`
  - `examples/binance_tick_mm/live_tick_mm.py`
  - `examples/binance_tick_mm/backtest_tick_mm.py`
  - `examples/binance_tick_mm/test_backtest_tick_mm.py`
- Allowed generated / report files：
  - `.workflow/tasks/0513T002.md`
  - `.workflow/reports/0513T002-business.md`
  - `task_plan.md`
  - `progress.md`
  - `findings.md`
  - `.workflow/dashboard.html`
  - `.workflow/dispatch_suggestions.md`
- Constraints inside allowed files：
  - `strategy_core.py` 只能新增 `MarketView` / `BookViewSnapshot`、helper、audit 字段和纯函数测试支撑；不能改变 `decide_actions()` 的交易语义、submit/cancel/reduce-side 规则、guard 判定结果或默认参数。
  - `live_tick_mm.py` 和 `backtest_tick_mm.py` 只能接入 market-view helper、provenance 字段和 overlay source 标记；不能改 fair/target 公式、throttle、latency guard、inventory guard、submit/cancel 规则、position limit、risk 参数默认值。
  - `test_backtest_tick_mm.py` 只能增加/调整 focused tests 来覆盖 helper、audit schema、overlay provenance；不能通过放宽断言掩盖行为变化。
- Forbidden files / areas：
  - `hftbacktest/`
  - `py-hftbacktest/`
  - Rust crates / workspace core files
  - connector / live deployment scripts
  - `examples/binance_tick_mm/config*.toml` 默认策略配置
  - `examples/binance_tick_mm/stage6j_replay.py` candidate/replay ranking 逻辑
  - live run scripts、AWS/remote deployment state、archive tarballs
- Requires separate task：
  - 修改 `hbt.depth(0)` 或 core depth API。
  - 修改 Binance raw converter / npz schema。
  - 修改 connector 本地 order book 管理。
  - 增加 `U/u/pu`、`lastUpdateId`、bookTicker provenance，如果当前 Python 策略层拿不到这些字段。
  - 实现 microprice / OFI / queue feature study。
  - 修改交易规则、fair price 公式、quote placement、risk guard 或 live 参数。

计划范围：
- 代码结构计划：
  - 在策略层新增 `MarketView` 或 `BookViewSnapshot` 数据结构。
  - 新增 `build_market_view_from_depth(...)` helper，封装从 `hbt.depth(0)` 读取 best bid/ask、mid、spread、top5 ticks/qtys、top5 sizes 的逻辑。
  - live/backtest 外层都使用该 helper，`strategy_core.decide_actions()` 仍只接收 target/action state，不直接接收 full depth。
  - 不修改 hftbacktest core depth API；如后续发现底层缺字段，再另建 core task。
- Overlay 计划：
  - 明确 `market_state_overlay=audit` 当前只 overlay compressed market/fair/target 字段。
  - 后续实现可新增 top5 overlay 选项或统一 overlay record，但必须写 provenance 字段。
  - overlay 后的 top5 对齐只能表示 audit replay decision view 被强制对齐，不能表示 replay reconstructed depth 已对齐。
- Audit schema 计划：
  - 增加 `market_view_source`、`top5_source`、`market_overlay_source`、`top5_overlay_source`。
  - 增加 `book_view_stale_ms`、`book_view_feed_latency_ns`、`book_view_ts_local`、`book_view_ts_exch`。
  - 增加 top5 provenance 字段，例如 `top5_depth_best_bid_tick`、`top5_depth_best_ask_tick`。
  - 如果当前层可取到，增加 `depth_update_u`、`depth_update_pu`、`last_update_id`、`bookticker_bid`、`bookticker_ask`；若不可取，计划中必须标为 blocker / later task。
- 验收计划：
  - 对 live/backtest helper 增加单元测试，验证 top5 序列、size aggregation、invalid book handling。
  - 对 audit row 增加 schema 测试，验证 provenance 字段存在且默认值稳定。
  - 对 audit replay overlay 增加 focused test，验证 compressed market 与 top5 overlay 来源字段不会混淆。
  - 对既有样本跑最小 audit/acceptance smoke，确认 action/planned/reject/throttle 不因透明化改动而变化。

验收方案：
- 必须输出 `.workflow/reports/0513T001-business.md`，状态为 `待验收`。
- 必须明确 implementation task 的 scope、non-scope、file ownership、audit fields、tests、acceptance gates。
- 必须明确“不改 `hbt.depth(0)` 底层 API”的理由，以及何时才需要另建 core API task。
- 必须明确 top5 对齐是必要但不充分：可作为当前策略 decision-view 透明度门槛，但不能替代 full L2 / queue / OFI proof。
- 必须明确后续真正实现需要新任务，不由 `0513T001` 授权直接实现或 live。
- 必须刷新 workflow dashboard。

action：
- 写出 `0513T001` planning-only business report。
- 更新 `task_plan.md`、`progress.md`、`findings.md`，把原 T009 调整为 `0513T001`。

verify：
- 检查 `.workflow/tasks/0513T001.md` 和 `.workflow/reports/0513T001-business.md` 存在。
- `python3 .workflow/build_dashboard.py`
- `git diff -- examples/binance_tick_mm` 确认没有代码实现改动。

done：
- 回报必须给出 0513T001 的实现计划和验收方案摘要。
- 回报必须说明本任务只规划，不实现。
- 回报必须说明下一步需要 QA 0513T001；若通过，另建 0513T002 才能实现。
- 回报必须写 `commit：无`。
```
