# 0719T006 EXACT-TWO-SIDED-MANAGER-EVIDENCE-CONTRACT

执行线程：
- 总控 auto-loop / 业务实现线程

任务ID：
- 0719T006

标题：
- Exact Binance-edge two-sided manager orchestration and evidence contract

简短描述：
- Make the exact remote orchestrator, Task 7 exchange-reconciled manager artifacts and Task 12 acceptance agree on one single-level two-sided Binance-lead / Hyperliquid-lag lifecycle contract.
- This task is offline-only. It prepares but does not execute the next bounded live window.

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0719T005`（已通过）

规则更新提醒：
- T005 cleared the execution-safety/evidence-integrity repair gate.
- A new live task still may not start until this exact two-sided contract is independently QA accepted.
- Multi-level, dynamic spread, fill feedback, inventory skew and actual quote behavior changes remain off.

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `docs/superpowers/plans/2026-07-17-principal-alignment-p0-p3.md`

是否需要提交代码：
- 需要

提交要求：
- 离线 focused/full regression 通过后提交。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- Modify: `examples/hyperliquid/cross_exchange_live_remote_orchestrator.py`
- Modify: `examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- Modify: `examples/hyperliquid/cross_exchange_t024_same_window_acceptance.py`
- Modify: focused tests for orchestrator/watcher/acceptance
- Workflow: this task, business report, QA report, `docs/qa-acceptance-report.md`, `task_plan.md`, `progress.md`, `findings.md`

action：
- Add an explicit exact-envelope profile for the next two-sided manager window:
  - one window, `<=900s`;
  - `0.005 BTC` per order, `0.01 BTC` max position delta, `1 USDC` max loss, exactly two submissions;
  - mode `event-driven-edge-gate-live`;
  - Binance public lead source active;
  - `--exchange-reconciled-manager`;
  - `--requote-attempts 2`;
  - Hyperliquid fast L2 enabled;
  - private proof mode `live_open_orders`;
  - all activation flags false.
- Preserve the legacy exact profile only when explicitly selected; the new live task must select the two-sided profile.
- Ensure orchestrator preflight and runner command persist the exact profile, manager flag and both budgets.
- Fix watcher CLI propagation so inline mode passes `max_real_order_submissions` from its dedicated argument.
- Ensure task/window identity is present in approved config, watcher, edge/anti-drift and copied fill artifacts.
- Emit manager evidence per side:
  - exactly one buy intent and one sell intent;
  - two distinct canonical attempt IDs/keys;
  - side/price/size/status rows for both order results;
  - two tracked references and per-reference terminal cancel reconciliation;
  - no aggregate `side=buy+sell` row as the primary lifecycle proof.
- Upgrade Task 12 acceptance to require the explicit two-sided profile:
  - command/profile/manager/edge/Binance-source contract;
  - exactly two intents and exactly two submitted attempt rows;
  - side set `{buy,sell}`;
  - distinct attempt IDs/keys and intent/attempt field equality by side;
  - two submitted/resting lifecycles and two target-bound terminal references;
  - exact task/window/config identity and activation-off state;
  - producer raw-proof reconstruction and all existing safety gates.
- Add actual producer integration:
  - generate two-sided manager artifacts through the real writer;
  - place them into a complete same-window acceptance fixture;
  - require nominal mechanism pass;
  - prove one-sided, duplicate-side, aggregate-row, stale identity, missing manager flag, wrong mode/budget and forged lifecycle variants fail closed.
- Keep quote formulas, thresholds, edge policy, risk caps and manager behavior unchanged.

verify：
- Exact two-sided preflight renders edge-gate mode, manager flag, requote `2`, submissions `2` and all task-scoped caps.
- Exact profile rejects missing manager, wrong mode, wrong budgets, missing fast L2/private proof and activation change.
- Manager writer produces two per-side intents/attempts/statuses and exact tokenized cancel proof.
- Complete acceptance passes an actual producer-written no-fill two-sided manager lifecycle.
- One-sided or aggregate `buy+sell` fixtures fail.
- Intent/attempt side, price, size, attempt-key or identity mismatch fails.
- Existing orchestrator timeout/provenance/checksum, watcher manager, T005 cancel proof and acceptance adversarial regressions pass.
- Modified Python `py_compile`, CLI `--help`, `git diff --check`.
- Focused orchestrator/watcher/manager/acceptance regressions.
- Full `python -m pytest examples/hyperliquid -q`.

stop conditions：
- Exact profile can launch a non-manager or non-Binance-edge path.
- Acceptance can pass one-sided or aggregate manager evidence.
- Two intents share one attempt identity or one cancel proof.
- Any strategy threshold, quote formula, live cap or activation behavior changes.
- Any live/private/account/order/cancel/network/remote/service action occurs.

done：
- The next formal live task has one exact, test-proven two-sided manager command and evidence contract.
- Actual producer-written manager artifacts pass independent same-window acceptance.
- Independent QA accepts this offline contract.

blockers：
- No live task may start before this task is QA accepted.

commit：
- 待填写

提交信息：
- 待填写
