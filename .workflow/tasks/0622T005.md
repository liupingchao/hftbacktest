```md
执行线程：
- 业务线程-live

任务ID：
- 0622T005

标题：
- M2 event-driven inline reprice / post-only reject repair

简短描述：
- 修复 `0622T004` 暴露的 guard-pass 后 exchange-side BBO fast-drift/post-only reject 问题：不要再从 watcher 调用完整 `fill_window.run_window` 提交路径，而是在 watcher 同一远端进程内用同一份最新 in-memory L2/BBO current candidate 完成 private open-orders 防重、即时重定价、严格 immediate guard 和 post-only `Alo` submit。
- 本任务目标是缩短 `guard -> reprice -> submit` 路径并在 post-only reject 后只用最新 public event/current BBO 重新评估；不是放宽风险边界或用 taker/crossing 强制成交。

状态：
- 执行中

执行顺序：
- 当前唯一任务

前置任务：
- `0622T004` business execution 为 `待验收`。它已经修复 controller pullback、separate live-window、batch candidate、慢 private/account preflight 等主要延迟，但正式 rerun 的 post-only `Alo` buy 在 exchange 侧因 BBO 从 `64107/64108` 快速漂移到 `64090@64091` 被拒绝，没有 resting/fill。
- `0618T008` QA 为 `已通过`，T008 ledger/reconciler 必须继续作为 M2 PnL 事实源。
- `0619T002` QA 为 `已通过`，fresh-touch / dynamic-size / session-gate 策略合同仍然有效。

规则更新提醒：
- 请先阅读 `.workflow/workflow-kit/workflow-manual.md`、`.workflow/workflow-kit/task-dispatch-template.md`、`.workflow/workflow-kit/thread-report-template.md`、`.workflow/workflow-kit/qa-acceptance-template.md`、`task_plan.md`、`progress.md`、`findings.md` 后再执行本任务。

按线程规则执行：
- `AGENTS.md`
- `docs/hyperliquid_tiny_live_real_order_executor.md`
- `docs/hyperliquid_tiny_live_final_go_no_go_gate.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- `examples/hyperliquid/hyperliquid_tiny_live_m2_fill_window.py`
- `examples/hyperliquid/hyperliquid_tiny_live_m2_fill_loop.py`
- `examples/hyperliquid/test_hyperliquid_tiny_live_m2_public_watcher.py`
- `examples/hyperliquid/test_hyperliquid_tiny_live_m2_event_driven_watcher.py`
- `examples/hyperliquid/test_hyperliquid_tiny_live_m2_fill_loop.py`
- `local_live_analysis/hyperliquid_tiny_live_m2_inline_reprice_0622T005/**`
- `.workflow/reports/0622T005-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

read-only inputs：
- `.workflow/tasks/0622T004.md`
- `.workflow/reports/0622T004-business.md`
- `local_live_analysis/hyperliquid_tiny_live_m2_event_driven_current_candidate_0622T004_rerun_fast_submit/**`
- `local_live_analysis/hyperliquid_tiny_live_m2_pnl_ledger_0618T008/**`

hard boundary：
- Keep Hyperliquid `Alo` post-only. No taker/crossing, no `Ioc`, no one-tick-back workaround, no cap relaxation, no default-on behavior, no promotion, no stable PnL claim, and no M3 readiness claim.
- Dynamic order size hard cap remains `<=0.005 BTC`; smaller size is allowed and preferred when current rolling throughput is low.
- Live execution is limited to one event-driven triggered micro-window and at most `2` real order endpoint submissions total.
- Waiting phase remains public-only until a current event-driven candidate triggers. Before trigger it must not read credentials, call private/account/order endpoints, submit orders, cancel orders, or start a continuous live bot.
- After trigger, the only required pre-submit private call is `open_orders` 防重 / shutdown safety. Do not reintroduce slow pre-submit `all_mids` / `meta` / `user_state` / `user_fees` calls.
- Reprice must happen after `open_orders` using the latest in-memory BBO/current L2 snapshot, not the older trigger quote.
- If BBO changes, re-evaluate from the latest in-memory snapshot or fail closed. Do not reuse an old quote.
- On Hyperliquid post-only reject / would-immediately-match, do not switch to taker, crossing, one-tick-back, larger size, or looser queue bands. Wait for the next public L2/trade event or latest in-memory BBO update, re-evaluate fresh-touch / dynamic-size / session-gate, and submit at most one retry while staying inside the same total `<=2` order endpoint calls.
- Selected quote must be current touch, post-only/non-crossing, size must still be `<=0.005 BTC`, queue-depth/order-count must still satisfy accepted `quality_a` / `quality_b` bands, and `Alo` must be the only TIF.
- Preserve tracked cancel, final open-orders proof, independent remote open-orders check, redaction, and T008 ledger fail-closed.
- Do not print or persist credential values, private keys, raw signatures, nonces, wallet secrets, or unredacted account identifiers.

implementation action：
- Add an inline live-submit path to the event-driven watcher, for example `--event-driven-inline-reprice-live`, reusing the existing public watcher state and current candidate logic.
- Do not call `fill_window.run_window` for the triggered submit path. Submit inside the watcher process using the same current L2/BBO and rolling trade-flow state.
- Flow after trigger:
  1. current candidate from in-memory L2/BBO + rolling trades
  2. private `open_orders` 防重 / safety check
  3. immediately reprice from latest in-memory BBO/current L2 snapshot
  4. strict immediate guard on current-touch, age, non-crossing, size, queue band
  5. submit post-only `Alo`
  6. if resting, quote-aging cancel with faster check cadence or public-event-driven cancel
  7. if post-only reject, re-evaluate latest public state and allow at most one maker-only retry
  8. tracked cancel and final open-orders proof
- Add latency artifacts that split `trigger_to_open_orders_start`, `open_orders_elapsed`, `open_orders_end_to_reprice`, `reprice_to_order_submit`, and exchange response time.
- Record guard BBO, submit-intent BBO, exchange reject reason/BBO text when present, and retry decision matrix.
- Keep T008 ledger as the final realized PnL fact source. If no maker fill or incomplete fee/inventory/realized PnL evidence appears, M2 remains blocked.

required artifacts：
- `inline_reprice_manifest.json`
- `inline_reprice_latency_matrix.csv`
- `inline_reprice_attempt_matrix.csv`
- `inline_reprice_guard_matrix.csv`
- `inline_reprice_post_only_reject_matrix.csv`
- `event_driven_watcher_manifest.json`
- `event_driven_latency_matrix.csv`
- `event_driven_trigger_decision_matrix.csv`
- `current_candidate_audit.csv`
- `rolling_flow_state.csv`
- `order_intent_audit.csv`
- `quote_attempt_matrix.csv`
- `window_result_matrix.csv`
- `public_stream_summary.json`
- final gate manifest and remote facts
- tracked cancel / shutdown proof
- final remote open-orders proof
- independent remote open-orders check
- aggregate live fill ledger
- T008 ledger manifest/output
- `inline_reprice_no_submit_report.md` when trigger occurs but inline guard blocks submission
- `event_driven_no_current_candidate_report.md` when no trigger occurs

live execution rules：
- Run focused local tests and CLI checks before any remote/live-capable step.
- Refresh `/home/admin/hftbacktest-cross-exchange` by git-safe incremental bundle / `merge --ff-only`; fail if branch is not `cross-exchange` or dirty count is nonzero.
- Rerun final gate; stop before watcher/live if `allow_create_0617T008 != true`.
- Run the inline reprice watcher/live controller on `awsserver1` using the refreshed checkout and configured Python path.
- During watcher waiting, only public L2/trades collection is allowed.
- After trigger, submit at most `2` post-only `Alo` orders and only when the inline immediate guard passes.
- Always tracked-cancel live orders and prove final open orders are empty.
- Always run T008 ledger after pullback; no fill or incomplete economics/inventory evidence must return fail-closed.

expected outcomes：
- `已通过` is possible only if implementation and QA criteria pass and either:
  - T008 proves live maker fill + fee/inventory/mark/realized PnL evidence; or
  - QA accepts that inline reprice/post-only-reject repair produced complete no-current-candidate / guard-fail / no-fill fail-closed evidence under the unchanged risk contract.
- M2 completion is possible only if T008 proves live maker fill plus complete fee/inventory/realized PnL evidence.
- If no current candidate appears, if inline guard blocks, if post-only rejects without resting, if no order fills, if final gate fails, if open-orders proof fails, or if T008 cannot prove realized live PnL, report M2 as blocked and do not enter M3.

verify：
- `python -m pytest examples/hyperliquid/test_hyperliquid_tiny_live_m2_event_driven_watcher.py -q`
- `python -m pytest examples/hyperliquid/test_hyperliquid_tiny_live_m2_public_watcher.py -q`
- `python -m pytest examples/hyperliquid/test_hyperliquid_tiny_live_m2_fill_loop.py examples/hyperliquid/test_hyperliquid_tiny_live_m2_pnl_ledger.py -q`
- `python -m py_compile examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py examples/hyperliquid/hyperliquid_tiny_live_m2_fill_window.py examples/hyperliquid/hyperliquid_tiny_live_m2_fill_loop.py`
- `python examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py --help`
- `python examples/hyperliquid/hyperliquid_tiny_live_m2_fill_window.py --help`
- `python examples/hyperliquid/hyperliquid_tiny_live_m2_fill_loop.py --help`
- `git diff --check`
- If watcher/live reaches remote execution: remote refresh evidence, final gate go/no-go, inline watcher/live artifacts, pulled-back artifacts, independent remote open-orders check, and T008 ledger output.

done：
- 回报 event runtime、public message counts、event-driven evaluation count、current candidate count、trigger count、trigger_to_open_orders_start、open_orders_elapsed、open_orders_end_to_reprice、reprice_to_order_submit、guard BBO、submit-intent BBO、exchange reject reason、post-only retry matrix、live submissions 数、fill_count / maker_fill_count、T008 ledger 结论、artifact 路径、blockers、commit id、提交信息。
- 明确说明 M2 是否仍 blocked；除非 T008 ledger 证明 live fill / fee / inventory / realized PnL，否则不得声明 M2 完成、稳定 PnL、或 M3 ready。
```
