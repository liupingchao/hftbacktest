# 任务派发

执行线程：
- 业务线程-python/public-data-infra + cross-exchange-research

任务ID：
- 0803T001

标题：
- FAST-L2-AGE-MASK-REPAIR-AND-R1-REBUILD

简短描述：
- 修复 T001 因单个短暂 Hyperliquid fast-L2 source-age 尾部而拒绝整个
  campaign 的 fail-closed 语义。
- 仅在显式启用、短暂、已恢复、总时长受限时，以 exact mask 保留 segment；
  成功后对同一批 raw 完成 pullback、R0 和 R1 rebuild。

状态：
- 待验收

执行顺序：
- 当前唯一任务。

前置任务：
- `0802T001` raw collection `10/10` completed but strict campaign failed.
- `0730T011` auxiliary degraded-interval contract accepted.
- `0730T016` R1 alignment audit gate accepted.
- 用户明确授权本次 repair、分段可用性检查和同批数据 R1 processing。

规则更新提醒：
- 无。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 不需要

提交要求：
- 当前工作区已有未提交前置工作，不创建混合提交。
- 回报使用 `commit：无`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 独立检查 fast-L2 stale interval contract、remote postprocess evidence、
  local pullback integrity、R0/R1 provenance/masks/labels。

files：
- `examples/hyperliquid/cross_exchange_l2_timeline.py`
- `examples/hyperliquid/cross_exchange_collection_supervisor.py`
- `examples/hyperliquid/cross_exchange_research_dataset.py`
- `examples/hyperliquid/cross_exchange_alignment_acceptance.py`
- focused tests for the above modules
- remote campaign:
  `/home/admin/0802T001_skhynix_5h_10x30m`
- local campaign/output:
  `local_live_analysis/cross_exchange_collection_campaign_0802T001_skhynix_5h_10x30m/`
  `local_live_analysis/skhynix_cross_exchange_research_0803T001/`

action：
- Preserve default source-age hard-failure behavior.
- Add explicit opt-in fast-L2 stale-interval recovery only when every interval
  is recovered, each interval and aggregate duration satisfy frozen bounds,
  and exact masks are written through R0/R1.
- Keep Binance and Hyperliquid standard-L2 age/reconnect gates hard fail.
- Deploy only the repair source to a new remote source root.
- Run remote postprocess-only against the existing raw campaign with the
  explicit fast-L2 bounded-interval policy.
- If remote campaign passes, pull it locally, verify file/hash integrity,
  build a new R0 event store and new R1 alignment package.
- If the repair cannot pass, report per-segment availability without pulling
  or treating any segment as R1-ready.

hard boundary：
- Do not recollect or mutate remote raw gzip bytes.
- Do not loosen the default fast-L2 gate globally.
- Do not weaken Binance or standard-L2 source-age/reconnect gates.
- No private/account/order endpoint and no strategy execution.
- Do not overwrite T011/T013/T016 historical artifacts.
