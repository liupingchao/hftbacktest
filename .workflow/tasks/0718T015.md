# 0718T015 PRICE-TAXONOMY-QUOTE-ELIGIBILITY

执行线程：
- 总控 auto-loop / 业务实现线程

任务ID：
- 0718T015

标题：
- 价格分类合同与 quote eligibility 解耦

简短描述：
- Implement Principal Alignment Task 4 (C1/C2): replace the untyped signal/fair/quote path with a validated `PricingConfigV1`, preserve accepted normalization stats, and separate alpha/confidence classification from quote eligibility.
- The shared kernel must produce both sides of a forecast-derived quote for valid market/risk state; signal threshold may classify confidence but must not select the only quote direction.

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- 0718T014

规则更新提醒：
- 无

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `docs/superpowers/plans/2026-07-17-principal-alignment-p0-p3.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `examples/hyperliquid/cross_exchange_shared_signal_kernel.py`
- `examples/hyperliquid/test_cross_exchange_shared_signal_kernel.py`
- `examples/hyperliquid/cross_exchange_production_shadow.py`
- `examples/hyperliquid/cross_exchange_public_replay_alignment.py`
- `examples/hyperliquid/test_cross_exchange_production_shadow.py`
- `examples/hyperliquid/test_cross_exchange_public_replay_alignment.py`
- `docs/cross_exchange_price_taxonomy_contract.md`
- Local repository mapping: the plan's `test_cross_exchange_pricing_stack.py` does not exist; the existing shared-kernel test file is the focused equivalent.

action：
- Add immutable, validated, serializable `PricingConfigV1` with schema version, normalization stats hash, forecast/half-spread/inventory parameters, feature switches, and levels.
- Make the kernel accept the typed pricing config and emit the config payload/hash in every decision and generated fixture artifact.
- Preserve the accepted feature schema, side mapping, horizon, and supplied normalization stats; reject missing/invalid production stats and do not use zero/one defaults in the production decision path.
- Emit `hl_mid_px`, guarded `hl_micro_px`, `signal_score`, `forecast_mid_px`, `reservation_px`, and two-sided `quote_bid_px`/`quote_ask_px` for valid market state.
- Separate `alpha_adjustment`/confidence bucket from `quote_eligibility`; below-threshold signal must not create `action=block` solely because it is below threshold.
- Keep fail-closed behavior for missing/stale/incoherent market data, invalid BBO/tick/normalization, and post-only crossing.
- Keep this task offline-only: no live client initialization, private endpoint, order/cancel, network, remote, strategy promotion, or tiny-live.

verify：
- Focused shared-kernel tests for typed config validation/serialization/hash stability.
- Below-threshold signal still yields two-sided quote output with an explicit confidence bucket.
- Positive/negative alpha moves the quote center without selecting a single side; zero alpha centers both quotes locally.
- Missing/stale signal and incoherent microprice snapshot fail closed; valid same-snapshot BBO quantities produce microprice.
- Replay/live fixture decision fields and config hash are identical for the same input.
- `python -m pytest examples/hyperliquid/test_cross_exchange_shared_signal_kernel.py examples/hyperliquid/test_cross_exchange_public_replay_alignment.py examples/hyperliquid/test_cross_exchange_production_shadow.py -q`
- `python -m py_compile examples/hyperliquid/cross_exchange_shared_signal_kernel.py examples/hyperliquid/test_cross_exchange_shared_signal_kernel.py`
- `git diff --check`
- Explicitly confirm no live/private/order/cancel/network/remote/service action.

done：
- Report changed files, taxonomy contract, typed-config and hash semantics, quote-eligibility behavior, focused/regression counts, commit id, and explicit endpoint-action confirmation.
