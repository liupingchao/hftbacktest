# 0719T001 T024-RUNTIME-PROVENANCE-FILL-RECONCILIATION-REPAIR-RERUN

执行线程：
- 总控 auto-loop / 业务实现线程

任务ID：
- 0719T001

标题：
- T024 runtime source provenance and no-fill reconciliation repair/re-run

简短描述：
- Repair the two independent-QA blockers from T024: seal runtime source bytes before watcher startup, and make no-fill cancel/fill reconciliation structured and fail-closed.
- Execute one new clean replacement window for Principal Alignment Task 12 after offline verification. T024 remains diagnostic and is not reclassified.

状态：
- 执行中

执行顺序：
- 当前唯一任务

前置任务：
- 0718T024（未通过；运行时源码证据和无成交对账不足）

规则更新提醒：
- Standing authorization permits uninterrupted task-scoped live work within BTC, Binance lead / Hyperliquid maker, `0.005 BTC` per order, `0.01 BTC` aggregate position delta, `1 USDC` max loss, `2` submissions and `900s` for this bounded window.
- Live is the main data source. Shadow/replay prevents obvious errors, reproduces live decisions and controls variables; it is not a long pre-live research stage.
- Execution-safety and evidence-integrity gates remain mandatory before any real order.
- Single-level fixed-spread post-only behavior remains authoritative. Dynamic spread, fill feedback, inventory skew, multi-level activation and actual quote behavior changes remain off.
- Any source, blocker classification, envelope, identity, reconciliation, termination or checksum mismatch is a stop condition; no second live window is allowed inside this task.

是否需要提交代码：
- 需要

提交要求：
- 离线修复、自测和 preflight 通过后提交，再从该 exact commit 创建 clean remote source archive。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

files：
- Modify: `examples/hyperliquid/cross_exchange_live_remote_orchestrator.py`
- Modify: `examples/hyperliquid/hyperliquid_tiny_live_m2_fill_window.py`
- Modify: `examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- Modify: `examples/hyperliquid/cross_exchange_t024_same_window_acceptance.py`
- Add/modify focused tests under `examples/hyperliquid/`
- Evidence: `local_live_analysis/principal_alignment_task12_repair_0719T001/`
- Workflow: this task, business report, QA report, `docs/qa-acceptance-report.md`, `task_plan.md`, `progress.md`, `findings.md`

action：
- Before any watcher child starts, write `runtime_source_provenance.json` into the run root with exact task identity, source commit, remote source root, watcher command inputs and SHA-256 for every critical runtime source file.
- Require every critical source file and source marker to exist; fail before watcher/private/order work on any missing file, invalid commit marker or digest failure.
- Make the runtime provenance artifact part of the terminal checksum manifest.
- Extend offline acceptance to compute expected critical-file hashes independently from the expected local Git commit and require exact equality with the sealed runtime artifact; remove the remote-path/preflight fallback.
- Replace cancel-message string inference with structured no-fill reconciliation:
  - successful authoritative tracked cancel plus empty final owned orders, empty fill pullbacks, zero unattributed fills and reconciled account state may be classified `no_fill_reconciled`;
  - missing successful cancel, conflicting fill/account evidence or unknown terminal state remains fail-closed.
- Record the structured reconciliation status in producer artifacts.
- Require acceptance to inspect producer `blocking_reasons`; permit only explicitly classified economics-only no-fill reasons and fail on every unclassified producer blocker.
- Run focused regressions, full Hyperliquid tests, exact-envelope preflight and account/service preflight before live.
- Commit the repair and deploy an isolated archive from that exact commit.
- Run exactly one bounded event-driven single-level fixed-spread post-only Hyperliquid BTC live window, at most `900s`, `0.005 BTC` per order and `2` submissions.
- Pull artifacts locally, verify terminal and remote/local SHA-256, then run same-window acceptance and independent QA.

verify：
- Runtime provenance exists and is complete before the fake/real watcher observes startup.
- Runtime provenance source commit and each critical-file digest equal independently computed expected Git blob contents.
- Missing marker, missing critical file and digest mismatch fail before watcher/private/order work.
- Successful tracked cancel followed by redundant generic cancel responses with zero fills reconciles as `no_fill_reconciled`.
- Generic ambiguous cancel without prior authoritative cancellation remains fail-closed.
- Acceptance fails for missing/mismatched runtime provenance and every unclassified producer blocker.
- Acceptance permits only the structured economics-only no-fill boundary while retaining mechanism/evidence checks.
- Existing orchestrator, event-driven watcher, executor, integrated acceptance and fill attribution regressions.
- Modified Python `py_compile`, relevant CLI `--help`, `git diff --check` and full `python -m pytest examples/hyperliquid -q`.
- Preflight exact-match assertions for task/window identity, source, envelope and activation-off state.
- Endpoint call counts, order lifecycle, fill identity/role/fee evidence if present, final open orders, account position and max-loss reconciliation.
- Terminal artifact checksum and remote/local SHA-256 equality.

stop conditions：
- runtime source marker or critical-file digest mismatch;
- watcher starts before runtime provenance is durably written;
- unclassified producer blocker or ambiguous no-fill reconciliation;
- exact task/window identity mismatch;
- runtime max loss/position/order-size/submission/duration envelope mismatch or breach;
- duplicate/ambiguous fill or fill attribution conflict;
- unknown order state;
- owned open orders cannot be proven empty;
- account position cannot be reconciled;
- price precision/post-only invariant failure;
- process termination/reap failure;
- status writer failure;
- artifact checksum failure;
- concurrent account activity contamination.

done：
- Runtime source bytes used for live are sealed before watcher startup and independently reproducible from the expected commit.
- Producer and acceptance agree on structured no-fill reconciliation and fail closed on unknown blockers.
- One clean bounded replacement window is collected and accepted for lifecycle/evidence integrity, or this task records the first formal stop condition without relaxing any gate.
- QA may close Principal Alignment Task 12 on mechanism/evidence integrity with zero or single-digit fills, but must keep unsupported economics explicit.
- Multi-level remains locked; Task 10 still requires a separately accepted single-level two-sided manager lifecycle.

blockers：
- Any stop condition ends this task's live execution and prevents a second window or strategy-variable expansion.

commit：
- 待填写

提交信息：
- 待填写
