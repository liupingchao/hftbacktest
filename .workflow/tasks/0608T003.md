```md
执行线程：
- 业务线程-research

任务ID：
- 0608T003

标题：
- Regime 011 read-only directional momentum viability assessment

简短描述：
- 基于 `0608T002` 对 `regime_011_1000_spread_10_20_ticks` 的 maker executability rejection，进一步只读评估该 regime 是否更适合作为 directional momentum / 单边持仓研究对象。任务只判断方向性可行性、费用/延迟/滑点 proxy 后的净边际、样本稳定性和后续 evidence gap，不实现策略、不输出交易指令、不启动 live。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0604T003` QA 已通过，canonical event-mode artifacts 是正式证据源。
- `0604T004` QA 已通过，canonical event-mode evidence loader / validator foundation 可复用。
- `0604T005` QA 已通过，canonical source-lock guard 可复用。
- `0604T009` QA 已通过，唯一 read-only Milestone 3 regime 输入为 `regime_011_1000_spread_10_20_ticks`。
- `0608T002` QA 已通过，maker executability final recommendation 为 `reject_not_maker_executable`；该结果只支持后续 read-only directional momentum viability assessment，不支持 maker case-library / live / promotion。

规则更新提醒：
- 请先阅读规则文档后再执行本任务。
- 本任务只做 read-only directional momentum viability assessment；不得实现策略、不得输出交易动作/下单方向/quote/order 行为、不得接 private/order endpoint、不得 live/default-on/tiny-live、不得 promotion。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `docs/thread-playbook.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交任务文件、业务报告、read-only runner/tests、task-scoped artifacts 和必要 workflow tracking 更新。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0608T003.md`
- `.workflow/reports/0608T003-business.md`
- `examples/hyperliquid/canonical_directional_momentum_viability.py`
- `examples/hyperliquid/test_canonical_directional_momentum_viability.py`
- `local_live_analysis/canonical_directional_momentum_viability_0608T003/**`
- 必要 workflow tracking 文件仅限本任务状态/结论更新。

read-only inputs：
- Formal canonical source:
  - `local_live_analysis/event_mode_canonical_pricing_signal_0604T003/**`
  - Row-level canonical sample files must be resolved from `local_live_analysis/event_mode_canonical_pricing_signal_0604T003/multi_sample_manifest.json` via `samples[].pricing_signal_rows`; do not assume a root-level `pricing_signal_rows.csv` exists under the canonical aggregate directory.
- Candidate / regime context:
  - `local_live_analysis/canonical_regime_synthesis_0604T009/**`
- T002 maker executability artifacts:
  - `local_live_analysis/canonical_maker_executability_0608T002/**`
- Loader / source-lock foundation:
  - `examples/hyperliquid/canonical_event_mode_evidence.py`
  - `local_live_analysis/canonical_event_mode_evidence_0604T004/**`
  - `local_live_analysis/canonical_evidence_source_lock_0604T005/**`
- Signal and horizon context:
  - `local_live_analysis/canonical_signal_quality_ranking_0604T006/**`
  - `local_live_analysis/canonical_horizon_regime_diagnostics_0604T007/**`
- Decision-time-visible input contract:
  - `local_live_analysis/binance_led_hyperliquid_data_contract_0601T004/**`

candidate scope：
- Regime id:
  - `regime_011_1000_spread_10_20_ticks`
- Base context:
  - `horizon_ms=1000`
  - `hyperliquid_context_quality=primary_usable`
  - `hyperliquid_join_age_bucket=fresh_0_50ms`
  - `hyperliquid_spread_bucket=spread_10_20_ticks`
- Existing T002 fact:
  - Maker executability final recommendation is `reject_not_maker_executable`.
  - Public proxy rows `242`, sample count `3`.
  - Maker rejection came from spread/adverse/post-only/latency proxy risk, not from missing trigger mass.
  - QA accepted T002 as read-only public-data proxy evidence only; it does not authorize maker case-library / strategy implementation / live / promotion.

forbidden：
- New data collection.
- Strategy implementation, directional strategy implementation, order behavior implementation, case-library implementation, shadow decision generation, live/default-on/tiny-live, promotion.
- Hyperliquid or Binance private/account/order endpoints.
- Order lifecycle, submit/cancel/fill logic, production config/default changes, parameter search or optimization sweep.
- Connector/core API changes, standard npz schema changes, canonical Binance maker audit schema changes.
- Treating ordinary synthetic fixed-grid artifacts as formal evidence.
- Treating `100/250ms` evidence as canonical independent support.
- Outputting executable trading instructions, actual order side, quote price, size, leverage, stop rule, take-profit rule, or deployment recommendation.

action：
- Implement a read-only directional momentum viability runner that consumes canonical event-mode artifacts through the accepted loader/source-lock path.
- Restrict analysis to `regime_011_1000_spread_10_20_ticks` and decision-time-visible features only.
- Reconstruct row-level candidate observations by reading `multi_sample_manifest.json`, validating each sample has `decision_mode=event` and `canonical_status=canonical_event_mode`, then loading each sample's `samples[].pricing_signal_rows` file and applying the regime context.
- Evaluate whether the fast-move behavior that rejected maker execution can plausibly become directional momentum edge after conservative public-data proxy costs.
- Separate:
  - base-regime directionality
  - feature-conditioned directionality
  - fee/slippage/latency adjusted directional edge proxy
  - sample stability
  - tail risk
  - evidence gaps
- Keep all claims public-data / read-only / proxy-scoped. Do not claim executable strategy PnL, real order fill, private execution feasibility, or live readiness.

candidate directional features：
- Evaluate only decision-time-visible fields, at minimum:
  - `input_binance_top5_imbalance`
  - `input_binance_microprice_minus_mid_ticks`
  - `input_binance_mid_move_ticks_from_prev`
  - `input_binance_top5_bid_qty`
  - `context_hyperliquid_top5_imbalance`
  - `context_hyperliquid_microprice_minus_mid_ticks`
  - `context_basis_mid_ticks`
- If fields are unavailable or contract-caveated, report that explicitly instead of inventing a value.

horizons：
- Primary horizon:
  - `1000ms`
- Diagnostic horizons:
  - `5000ms`
  - `10000ms`
- `100/250ms` may be parsed only as diagnostic/watch context and must not be used as independent canonical support.

directionality metrics：
- Suggested metrics:
  - `row_count`
  - `sample_count`
  - `positive_future_count`
  - `negative_future_count`
  - `direction_hit_rate`
  - `mean_signed_future_move_ticks`
  - `median_signed_future_move_ticks`
  - `mean_abs_future_move_ticks`
  - `p05_signed_future_move_ticks`
  - `p95_signed_future_move_ticks`
  - `per_sample_signed_edge_ticks`
  - `sample_direction_consistency`
  - `effect_concentration_ratio`
- Evaluate both sign-following variants and threshold variants:
  - sign only: feature `> 0` / `< 0`
  - absolute-strength buckets: top quartile or equivalent deterministic threshold
  - optional combined filters only if deterministic and limited, not a broad parameter sweep.

cost / latency proxy：
- Use conservative public-data proxy assumptions only; do not claim actual exchange execution.
- Suggested outputs:
  - `gross_directional_edge_ticks`
  - `fee_proxy_ticks`
  - `slippage_proxy_ticks`
  - `latency_decay_proxy_ticks`
  - `net_directional_edge_proxy_ticks`
  - `net_edge_positive_rate`
  - `latency_sensitive_reversal_rate`
- Cost assumptions must be manifest fields and easy to audit.
- Do not optimize cost assumptions to make a result pass.

tail risk：
- Suggested metrics:
  - `wrong_way_rate`
  - `mean_wrong_way_loss_ticks`
  - `p95_wrong_way_loss_ticks`
  - `max_wrong_way_loss_ticks`
  - `tail_loss_classification`

output classification values：
- Directionality:
  - `directional_signal_supported`
  - `directional_signal_watch`
  - `directional_signal_reject`
- Cost-adjusted viability:
  - `net_edge_plausible_proxy`
  - `net_edge_marginal_proxy`
  - `net_edge_negative_proxy`
- Stability:
  - `multi_sample_stable`
  - `sample_concentrated_watch`
  - `sample_unstable_reject`
- Tail risk:
  - `tail_risk_acceptable_proxy`
  - `tail_risk_watch`
  - `tail_risk_reject`

final recommendation taxonomy：
- `candidate_for_directional_case_library`
- `watch_needs_more_public_samples`
- `watch_needs_execution_cost_evidence`
- `reject_directional_edge_unstable`
- `reject_net_edge_negative`
- `reject_public_data_insufficient`

strict acceptance line for `candidate_for_directional_case_library`：
- Directional edge is present after deterministic feature conditioning.
- Edge is multi-sample and not single-sample dominated.
- Conservative fee/slippage/latency proxy does not turn net edge negative.
- Tail risk is not clearly unacceptable.
- Conclusion does not rely on `100/250ms` aliased evidence.
- All inputs are decision-time-visible.
- Report remains read-only and does not output executable trading instructions or live/promotion claims.

outputs：
- `directional_momentum_manifest.json`
- `base_regime_directionality.csv`
- `feature_directionality_summary.csv`
- `cost_latency_adjusted_edge.csv`
- `tail_risk_summary.csv`
- `directional_candidate_watch_reject.csv`
- `directional_momentum_report.md`

acceptance criteria：
- Runner refuses non-canonical or diagnostic-only synthetic inputs through the accepted loader/source-lock path.
- Runner validates that T002 input recommendation is `reject_not_maker_executable` before reframing the regime as directional research.
- Runner resolves row-level `pricing_signal_rows.csv` inputs from `multi_sample_manifest.json` `samples[].pricing_signal_rows` and does not require a root-level canonical aggregate `pricing_signal_rows.csv`.
- Assessment is limited to `regime_011_1000_spread_10_20_ticks`.
- Outputs include all required artifact files and final recommendation taxonomy.
- Report explicitly states conclusions are read-only public-data proxies, not executable strategy PnL or private execution proof.
- Report does not output actual order side, quote price, size, leverage, stop rule, take-profit rule, strategy action, private/order endpoint, live/default-on/tiny-live, or promotion claim.
- Tests cover canonical input acceptance, diagnostic input refusal, T002 maker-rejection prerequisite validation, feature-conditioned directionality, cost-adjusted taxonomy, and at least one reject/watch path.

verify：
- `python examples/hyperliquid/canonical_directional_momentum_viability.py --help`
- `python -m py_compile examples/hyperliquid/canonical_directional_momentum_viability.py examples/hyperliquid/test_canonical_directional_momentum_viability.py`
- `python -m pytest examples/hyperliquid/test_canonical_directional_momentum_viability.py`
- `python -m json.tool local_live_analysis/event_mode_canonical_pricing_signal_0604T003/multi_sample_manifest.json`
- `python examples/hyperliquid/canonical_directional_momentum_viability.py --input-dir local_live_analysis/event_mode_canonical_pricing_signal_0604T003 --candidate-dir local_live_analysis/canonical_regime_synthesis_0604T009 --maker-executability-dir local_live_analysis/canonical_maker_executability_0608T002 --signal-ranking-dir local_live_analysis/canonical_signal_quality_ranking_0604T006 --horizon-regime-dir local_live_analysis/canonical_horizon_regime_diagnostics_0604T007 --output-dir local_live_analysis/canonical_directional_momentum_viability_0608T003`
- `python -m json.tool local_live_analysis/canonical_directional_momentum_viability_0608T003/directional_momentum_manifest.json`
- Scripted or manual check that the report contains no executable trading instruction, order side, quote price, size, private/order endpoint, live/default-on/tiny-live, or promotion claim.
- `git diff --check`

done：
- Business report must include method, input source-lock, T002 prerequisite check, directionality results, cost/latency proxy assumptions, tail-risk results, final recommendation, artifact paths, verification commands, blockers/caveats if any, commit id, and commit message.
- Business report must explicitly state this is only read-only directional momentum viability assessment and does not authorize strategy implementation, private/order endpoints, order lifecycle, live/default-on/tiny-live, parameter search, case-library implementation, shadow decisions, or promotion.
- Business report should end in `待验收` if verification passes.
```
