```md
执行线程：
- 业务线程-live

任务ID：
- 0623T007

标题：
- M2 live public-source fair-mid shadow test

简短描述：
- 在 `0623T006` 已实现 / 接受 decision-time public fair-mid provider contract 后，执行下一步 live public shadow：接真实 Hyperliquid / Binance public feed，运行 watcher-local fair-mid source 和 edge gate，但强制 no-submit。
- 本任务只验证 live public source path 是否新鲜、可解释、fail-closed；不读凭据、不调用 private/account/order endpoint、不下单、不 remote refresh、不跑 final gate。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0623T006` business execution complete and reported with implementation commit `a6ee959`.

规则更新提醒：
- 请先阅读 `.workflow/workflow-kit/workflow-manual.md`、`.workflow/workflow-kit/task-dispatch-template.md`、`.workflow/workflow-kit/thread-report-template.md`、`.workflow/workflow-kit/qa-acceptance-template.md`、`task_plan.md`、`progress.md`、`findings.md` 后再执行本任务。

按线程规则执行：
- `AGENTS.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- `examples/hyperliquid/test_hyperliquid_tiny_live_m2_event_driven_watcher.py`
- `local_live_analysis/hyperliquid_tiny_live_m2_public_shadow_source_0623T007/**`
- `.workflow/reports/0623T007-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

read-only inputs：
- `.workflow/tasks/0623T006.md`
- `.workflow/reports/0623T006-business.md`
- `local_live_analysis/hyperliquid_tiny_live_m2_fair_mid_source_0623T006/**`
- `docs/qa-acceptance-report.md`

hard boundary：
- No live orders, no credential reads, no private/account/order endpoints, no real order endpoint, no cancel endpoint, no remote checkout refresh, no final gate rerun, no T008 live ledger claim, no quote-distance change, no one-tick-back, no inside-spread, no cap relaxation, no taker/crossing, no default-on behavior, no M3/stable PnL/promotion.
- Public live source use is allowed only for Hyperliquid public L2/trades and Binance public market data.
- If live public source cannot be collected from this environment, record the blocker and still provide local mock/shadow path verification; do not fabricate live pass evidence.

action：
- Add a `0623T007` public shadow path that connects / can connect Hyperliquid public events and Binance public market data into the `0623T006` fair-mid provider.
- The path must run watcher-local candidate evaluation, fair-mid source build, and fair-value edge gate, but must force no-submit before any private/account/order endpoint or live client initialization.
- Produce shadow artifacts under `local_live_analysis/hyperliquid_tiny_live_m2_public_shadow_source_0623T007/`:
  - live/mock public source manifest
  - fair-mid source matrix
  - edge gate matrix
  - public source freshness matrix
  - candidate audit
  - no-submit proof
  - boundary manifest proving no credentials/private/order endpoints
- Add focused tests for:
  - shadow path can pass source/edge without order calls
  - missing / stale Binance source blocks
  - no live client / no private/order endpoint is initialized
  - artifact generation is non-empty and records no-submit boundary
- Try a short public-only live shadow run if local network permits; if blocked by network / environment, report exact blocker and keep task status `待验收` only with mock/public-source-compatible artifacts.

verify：
- `python -m pytest examples/hyperliquid/test_hyperliquid_tiny_live_m2_event_driven_watcher.py -q`
- `python -m py_compile examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- `python examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py --help`
- Generate T007 local public-shadow artifacts and validate JSON/CSV outputs.
- If attempted live public shadow cannot run due environment/network, capture that blocker in report and artifacts.
- `git diff --check`

done：
- 回报 T007 public shadow source contract、live/mock collection result、pass/block matrix、artifact path、remaining blockers、whether real live public source was observed、whether any private/order endpoint was touched、whether next tiny real maker canary is authorized、commit id、提交信息。
- 明确说明本任务不授权真实下单、读凭据、private/account/order endpoint、remote refresh、final gate、quote-distance change、one-tick-back、inside-spread、cap relaxation、M3/stable PnL/default-on/promotion。
```
