# 任务派发

执行线程：
- 业务线程-terminal-seal

任务ID：
- 0717T010

标题：
- TERMINAL-ARTIFACT-SEAL-REPAIR

简短描述：
- 实现 `0717T006` Phase 4，修复成功和失败路径的终态 artifact ordering、相对路径 sha256 manifest、立即校验和 verification summary。
- 保持 watcher 策略逻辑、live 参数、订单边界、私有证明调用方式和 Phase 1-3 行为不变。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- `0717T009 / WATCHER-TERMINATION-TIMEOUT-REPAIR` QA 为 `已通过`。

规则更新提醒：
- 无

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- focused tests 通过后提交。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `examples/hyperliquid/cross_exchange_live_remote_orchestrator.py`
- `examples/hyperliquid/test_cross_exchange_live_remote_orchestrator.py`
- `docs/cross_exchange_live_collection_resilience.md`
- `.workflow/tasks/0717T010.md`
- `.workflow/reports/0717T010-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

action：
- 让 manifest 使用 run-root-relative paths，并排除 manifest 与 verification summary。
- 统一成功/失败终态顺序：child reap、window/root evidence final、final status/event row、heartbeat stop/join、manifest 单次生成、立即逐项校验。
- 写出 `remote_sha256_verification.json`，记录 entry、verified、missing、mismatch 和 status。
- 确保 manifest seal 后没有 included artifact writer 继续修改文件。
- 添加成功、失败、heartbeat/status/event ordering、相对路径和 post-seal mutation detection 测试。

verify：
- `python -m pytest examples/hyperliquid/test_cross_exchange_live_remote_orchestrator.py`
- `python -m py_compile examples/hyperliquid/cross_exchange_live_remote_orchestrator.py examples/hyperliquid/test_cross_exchange_live_remote_orchestrator.py`
- `git diff --check`
- 离线 fake-watcher fixture；不执行 live/private/order/cancel/network/remote/service 动作。

done：
- 成功和失败 fixture 的 `remote_sha256_manifest.txt` 均使用相对路径并立即验证通过。
- `run_complete.json` 或 `abort_manifest.json`、最终 `run_status.json`、event log 和 heartbeat 的 seal ordering 有回归证据。
- 修改 included artifact 后，verification 能报告 mismatch，而不是静默通过。
- 不改变策略、quote policy、订单边界或 live 访问。

not authorized：
- 不执行 live 或 remote。
- 不修改 strategy、quote-policy、order-size、max-submission、max-loss、threshold。
- 不进入 Phase 5 integrated offline acceptance。
