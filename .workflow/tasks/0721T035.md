# 0721T035 RAW-HISTORICAL-SEMANTICS-TRIGGER-REPAIR

执行线程：
- 总控 auto-loop / 业务执行线程

任务ID：
- 0721T035

标题：
- Trigger delayed validation from raw historical semantics and reject method/result mismatch

简短描述：
- 修复 T034 QA 的两个 P1：delayed protocol requirement 必须在 raw attempt/result 结构验证之前识别 historical 语义。
- Direct method 携带 nested historical result、historical method 携带 non-historical result，以及 malformed historical attempt 都必须显式 fail closed。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0721T034`（未通过）

规则更新提醒：
- 本任务 offline-only。
- T031 live artifacts immutable。
- T033/T034 已接受的 exact timing/caps、manager contract、quarantine、Task 12 rollout、legacy direct-only compatibility 和历史边界不得回退。
- 独立 QA 通过前不得进行 private read、新 live 或任何 adaptive/multi-level activation。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `.workflow/reports/0721T034-qa.md`

是否需要提交代码：
- 需要

提交要求：
- 业务实现和业务报告分开提交。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

files：
- Modify: `examples/hyperliquid/hyperliquid_tiny_live_m2_fill_window.py`
- Modify: `examples/hyperliquid/cross_exchange_t024_same_window_acceptance.py`
- Test: `examples/hyperliquid/test_hyperliquid_tiny_live_m2_fill_attribution.py`
- Test: `examples/hyperliquid/test_cross_exchange_t024_same_window_acceptance.py`
- Workflow: task、business、QA、fixed QA、`task_plan.md`、`progress.md`、`findings.md`

action：
- Derive delayed-history-required from every raw dict attempt/result before attempt id、method or result schema validation。
- Treat top-level `method=historical_orders` and nested `result.status=historical_orders` as independent historical-semantics triggers。
- Add an explicit method/result mismatch reason when direct method carries historical result or historical method carries non-historical result。
- Ensure malformed historical attempts still require exact marker/timing contract in addition to their existing malformed/count failures。
- Keep non-dict rows fail closed without inventing historical semantics that are not observable。
- Preserve legacy direct-only pass only when method/result semantics are consistently direct and no delayed fields exist。

verify：
- Direct method plus nested historical result fails producer and independent audit identically with protocol/mismatch reasons。
- Historical method plus direct/non-historical nested result fails identically。
- Historical method with missing/bool/zero/container attempt id triggers protocol validation and existing malformed/count failure。
- Canonical result row carrying nested historical status triggers even when attempt list is malformed or absent。
- Exact production historical contract remains pass。
- Marker-only/delayed-field-only malformed contracts remain fail closed。
- Non-dict rows remain fail closed and cannot create a legacy pass through count normalization。
- Legacy consistent direct-only v4 remains pass。
- Task 12 T035+ hostile cases fail end to end。
- Focused/full Hyperliquid regression passes。
- T031 blocked、T026 pass、T016/T022 blocked。
- No live/private/network/remote/service operation occurs。

stop conditions：
- Any weakening of T033 exact timing、manager or quarantine contracts。
- Any weakening of T034 normal marker trigger or Task 12 rollout。
- Any live executor timing、endpoint or strategy behavior change。
- Any historical evidence modification。
- Any live/private/network/remote/service action。

done：
- Raw historical semantics cannot be normalized away before protocol validation。
- Producer、independent helper and Task 12 agree on method/result mismatch and malformed historical evidence。
- Source is ready for independent QA and only then a fixed-quote observe-only live task。

blockers：
- Independent QA acceptance。

commit：
- implementation `180d24bd520e68487cdf843385b3940f426b0dc8`

提交信息：
- implementation `Repair raw historical semantics trigger`
