# 0722T060 UNCONDITIONAL-LEGACY-SOURCE-CONTAINMENT-REPAIR

执行线程：
- 总控 / 业务执行线程

任务ID：
- 0722T060

标题：
- Make known-legacy source containment unconditional

简短描述：
- 修复 T059 QA 的 P1：legacy parent symlink 加 missing final child 不能绕过
  source-side containment。
- 保持 destination containment、missing-only skip 和 test-only scheduler
  tolerance 不变。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- `0722T059`（未通过）

规则更新提醒：
- offline-only。
- 不修改 production strategy、timestamp、deadline、quote、risk 或
  lifecycle 行为。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交并进入独立 QA。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `examples/hyperliquid/basis_positive_row_level_read_only_generator.py`
- `examples/hyperliquid/test_basis_positive_row_level_read_only_generator.py`
- workflow/report/tracking files

action：
- Known legacy path 无条件 `resolve(strict=False)`，并验证 resolved source
  仍属于 resolved legacy root。
- 新增 existing parent symlink + missing final child + safe current
  same-relative target hostile regression。
- 保持所有既有正向、traversal、final symlink、relative/current-root 和
  missing-vs-malformed contracts。

verify：
- Focused resolver/availability tests。
- Deadline hostile repeated。
- Full Hyperliquid 连续两次。
- py_compile / diff checks。

done：
- 所有 known legacy source 路径不再依赖 final child existence 才执行
  source containment。
- 经独立 QA 接受后进入 basis regression alpha。

blockers：
- 无。

QA验收：
- 已通过。
- QA report：`.workflow/reports/0722T060-qa.md`。
- 上轮 parent-symlink + missing-final-child P1 已关闭。
- Focused `21 passed, 2 skipped`；deadline hostile `20/20`。
- Full Hyperliquid 连续两轮均为 `1260 passed, 2 skipped`。

提交信息：
- `Make legacy source containment unconditional`
