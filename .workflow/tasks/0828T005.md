# 任务派发

执行线程：
- SKHYNIX OBI Reversal Track A1/A2 业务线程

任务ID：
- 0828T005

标题：
- 执行 OBI_REVERSAL_V1 A1 State Ledger And A2 First-Passage Targets

简短描述：
- 使用 0828T004 已冻结并通过 QA 的 OBI、reversal、control、barrier、
  censoring 和 `tau_max` contracts，物化 causal state ledger、primary
  common risk set 和 symmetric one-tick midpoint first-passage competing
  risks。本任务不拟合 H0/H1，不根据 target 修改 A0 参数。

状态：
- 待验收

执行顺序：
- 当前唯一正式任务

前置任务：
- 0828T004

规则更新提醒：
- 已阅读 AGENTS.md、workflow-kit manual、task/report/QA templates、
  Conditional-Risk Research Methodology Kernel v1、canonical OBI reversal
  Track A 方案、0828T004 contracts/artifacts 和 0827T004 feature schema。

按线程规则执行：
- AGENTS.md
- .workflow/workflow-kit/workflow-manual.md

是否需要提交代码：
- 需要

提交要求：
- 聚焦测试和 artifact closure 后提交
- 回报必须带 `commit id` 和提交信息

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- examples/hyperliquid/skhynix_obi_reversal_track_a1_a2.py
- examples/hyperliquid/test_skhynix_obi_reversal_track_a1_a2.py
- docs/skhynix_binance_continuous_background_interpretable_m_state_competing_risk_track_a_plan_20260828.md
- local_live_analysis/skhynix_obi_reversal_track_a1_a2_0828T005/
- .workflow/tasks/0828T005.md
- .workflow/reports/0828T005-business.md

action：
- 验证 0828T004 A0 run-manifest、classification、contracts 和 state CSV
  size/SHA closure。
- 验证 0827T004 feature caches 与 frozen source identities。
- A1 物化 reversal/control directional entries 和 exact decision-time H0
  context，不读取 decision 后价格。
- 仅保留同 research date、side、absolute-OBI bin 具有双边支持的 primary
  common risk set；unique no-reuse pairs 仅保留为诊断。
- A2 从 decision 后的 `midpoint_delta_ticks` 累加得到 midpoint
  displacement，物化首次 `+1/-1 tick` follow/fail cause、event time 和
  censoring。
- 将 reversal side 作为新 OBI 方向；oriented displacement 为
  `side * future_midpoint_displacement_ticks`。
- 单独物化 cross-to-detection pre-detection transition，禁止计入
  decision-time 后可捕捉结果。
- 冻结 A2 target variation gates：
  - 每个 cause 至少 200 events；
  - 每个 cause 至少覆盖 8 个 research dates；
  - 每个 cause 最大单日占比不超过 0.35；
  - primary risk-set censoring fraction 不超过 0.50；
  - interval ambiguity fraction 不超过 0.01；
  - reversal 和 control 两类各自都必须同时观察到 follow/fail。
- 生成 access ledger、target diagnostics、classification 和 manifest。

verify：
- focused pytest。
- synthetic midpoint-path fixtures 覆盖 follow、fail、tau/capture/quality
  censoring 和 pre-detection transition。
- A1 decision-time feature mutation boundary 测试。
- A0 frozen-contract hash closure。
- full historical materialization 与 artifact size/SHA closure。
- deterministic build 两次 hash 一致。
- `git diff --check`。

done：
- 回报 primary entry counts、follow/fail/censoring、event-time support、
  date concentration、pre-detection transition、A1/A2 gates、
  classification、artifact 路径、测试和 A3 边界。

明确禁止：
- 不读取 post-first-passage return、markout、fill、PnL 或 maker
  economics。
- 不拟合 H0/H1，不查看模型增量，不选择 favorable horizon/barrier。
- 不修改 A0 冻结的 OBI formula、threshold、dwell、alignment、control
  key、barrier、censoring 或 `tau_max`。
- 不访问 private API，不下单，不采集新数据。
- 不把历史 materialization 宣称为 prospective。
