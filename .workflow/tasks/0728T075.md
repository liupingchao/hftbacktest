# 0728T075 EC2-HUNT-INTERRUPT-TASK-IDENTITY-REPAIR

执行线程：
- 总控 / 业务执行线程

任务ID：
- 0728T075

标题：
- 修复直接 KeyboardInterrupt 审计和 task identity 漂移

简短描述：
- 保留 T074 已通过的实际 Canary、S3/setup control path 和零遗留。
- 纯离线关闭 T074 QA 的两个 P2 和一个 P3。

状态：
- 未通过

执行顺序：
- 当前唯一正式任务

前置任务：
- `0728T074`（未通过；AWS/data/control path 已通过）

是否需要提交代码：
- 需要实现并加入 Git 跟踪；本任务不要求创建 commit。

是否进行QA验收：
- 是

files：
- `latency-probe/ec2_hunt_orchestrator.py`
- `latency-probe/test_ec2_hunt.py`
- `docs/evidence/ec2_latency_hunt_20260728/README.md`
- `docs/evidence/ec2_latency_hunt_20260728/execution_manifest.json`
- `.workflow/tasks/0728T075.md`
- `.workflow/reports/0728T075-business.md`
- `.workflow/reports/0728T075-qa.md`
- `docs/qa-acceptance-report.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

action：
- 任意 KeyboardInterrupt 都写 interrupted=true，再进入 finally cleanup。
- CLI `--task` 改为 required，禁止默认 task identity。
- 增加直接 KeyboardInterrupt 和缺少 `--task` 的 regressions。
- 修正 README final Canary task ID。

verify：
- Python/Rust/fmt/clippy/diff check 全通过。
- 直接 KeyboardInterrupt journal 为 interrupted=true 且 cleanup 一次。
- parse_args 缺少 `--task` 时退出。
- 不执行 AWS mutation，不重复已通过的 T074 Canary。

done：
- T074 QA 的剩余离线缺陷关闭，Goal 2 可最终 QA。

blockers：
- 无。
