```md
执行线程：
- 业务线程-python

任务ID：
- 0605T001

标题：
- Fix shutdown terminal proof for partially filled local orders

简短描述：
- 修复 `0604T016` QA 指出的 terminal proof 漏洞：shutdown final local-order proof 不得复用只收集 `NEW` 订单的 `collect_working_orders(...)`，必须直接遍历本地 `hbt.orders(asset_no)` snapshot，正确区分 `NEW` / `PARTIALLY_FILLED` active 状态与 terminal 状态，并补 focused regression。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- `0604T016` QA 未通过，缺陷为 active `PARTIALLY_FILLED` 本地订单可能被误判为 terminal confirmed。

规则更新提醒：
- 无

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `docs/thread-playbook.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交任务文件、业务报告、`live_tick_mm.py` 修复和 focused tests。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0605T001.md`
- `.workflow/reports/0605T001-business.md`
- `examples/binance_tick_mm/live_tick_mm.py`
- `examples/binance_tick_mm/test_backtest_tick_mm.py`

forbidden：
- 不修改 py binding、Rust live bot/backtest、connector、production config、normal trading loop cancel 语义、audit schema、strategy quote/submit/cancel 行为。
- 不启动 live / tiny-live / default-on。
- 不做 REST private/order endpoint 集成。
- 不做参数搜索、回放实验扩展、promotion。

action：
- 修改 `_confirm_shutdown_terminal_state_from_local_orders(...)`，直接遍历 `hbt.orders(asset_no)` 的原始本地 snapshot。
- 对同一 `order_id`：
  - `NEW` / `PARTIALLY_FILLED`：视为 active，`terminal_confirmed=False`。
  - `EXPIRED` / `FILLED` / `CANCELED` / `REJECTED`：视为 terminal，`terminal_confirmed=True`。
  - unknown status：不得确认 terminal，`terminal_confirmed=False`。
- 找不到该 `order_id` 时，可保留 local absent proof，`terminal_confirmed=True`。
- 读取本地订单失败时，`terminal_confirmed=False`。
- 保持 `order_response_received` 与 `terminal_confirmed` 独立；不得改变 T016 已通过的 wait-result 分类和 summary 口径。
- 补 focused regression：`wait_result=3 + local PARTIALLY_FILLED same order_id` 必须 `order_response_received=True` 且 `terminal_confirmed=False`。

verify：
- `/home/molly/anaconda3/envs/hftbacktest/bin/python -m pytest examples/binance_tick_mm/test_backtest_tick_mm.py -k "live_shutdown or shutdown_cancel or wait_order_response or terminal_confirmed or order_response_received or partially_filled"`
- `/home/molly/anaconda3/envs/hftbacktest/bin/python -m pytest examples/binance_tick_mm/test_*.py`
- `git diff --check`

done：
- 业务报告写入 `.workflow/reports/0605T001-business.md`。
- 回报必须说明 `PARTIALLY_FILLED` regression、状态分类、验证命令、commit id 和 commit message。
- 业务报告应以 `待验收` 收口。
```
