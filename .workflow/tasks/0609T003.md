```md
执行线程：
- 业务线程-research

任务ID：
- 0609T003

标题：
- Basis-positive filtered context read-only viability assessment

简短描述：
- 基于 `0609T001` 的 basis-positive wrong-way decomposition 和 `0609T002` 的 targeted public collection 结果，只在观测层 public/canonical artifacts 上评估 filtered basis-positive context 是否仍有稳定 edge、tail-risk 是否可见可分、以及是否值得进入后续 read-only case-design 讨论。本任务不补执行层证据；执行层内容只作为 gap/boundary register。

状态：
- 阻塞

执行顺序：
- 当前下一任务；必须在 `0609T002` QA 通过后执行。

前置任务：
- `0609T001` QA 已通过，final recommendation 为 `targeted_collection_ready`。
- `0609T002` 需 QA 已通过，final recommendation 必须为 `tail_filter_hypothesis_validated_for_read_only_research`。
- `0604T003` canonical event-mode artifacts / `0604T004` loader / `0604T005` source-lock guard 仍是 formal evidence foundation。

规则更新提醒：
- 本任务只处理 local read-only public artifacts，不允许新采集，不允许远端执行，不允许 private/order endpoint，不允许策略、case-library、shadow decision、live/default-on/tiny-live、parameter search 或 promotion。
- 当前数据是观测层 public market-data evidence，不是执行层 evidence；执行层只能登记缺口，不能在本任务中声称已证明。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `docs/thread-playbook.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 自测和本地验证通过后提交任务文件、业务报告、task-scoped artifacts、必要 runner/test 变更（如有）和必要 workflow tracking 更新。
- 回报必须带 `commit id` 和 `提交信息`。
- 如果执行中未改代码，只提交任务报告、artifacts 和 tracking；不得混入无关 dirty 文件。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0609T003.md`
- `.workflow/reports/0609T003-business.md`
- New read-only runner/test only if needed, preferably under:
  - `examples/hyperliquid/`
- Local task artifacts:
  - `local_live_analysis/basis_positive_filtered_context_viability_0609T003/**`
- 必要 workflow tracking 文件仅限本任务状态/结论更新。

read-only inputs：
- `local_live_analysis/basis_positive_targeted_public_collection_0609T002/**`
- `local_live_analysis/canonical_basis_positive_wrong_way_decomposition_0609T002/**`
- `local_live_analysis/canonical_basis_positive_wrong_way_decomposition_0609T001/**`
- `local_live_analysis/canonical_basis_positive_robustness_0608T006/**`
- `local_live_analysis/event_mode_canonical_pricing_signal_0604T003/**`

primary research questions：
- Filtered edge:
  - Compare raw `basis > 0`, clean filtered basis-positive subset, and rejected tail-risk subset.
  - Candidate reject/tail conditions must start from T002 validated read-only hypotheses:
    - `basis_magnitude_bucket=basis_positive_small`
    - `hl_top5_imbalance_bucket=hl_top5_imbalance_negative_small`
    - `hl_microprice_minus_mid_bucket=hl_microprice_minus_mid_negative_small`
  - Report hit rate, mean/median future move, wrong-way rate, p95/max wrong-way loss, sample count, max sample row share, and per-sample stability.
- Observation-layer viability:
  - Assess horizon stability, spread bucket, join-age bucket, volatility/market-activity proxy if available, Binance momentum control, and Hyperliquid book-state conditioning.
  - This is not maker execution proof; it is only a public-market-data proxy for whether the filtered context remains worth later research.
- Research labels:
  - Produce read-only context labels such as `basis_positive_clean_context`, `basis_positive_tail_risk_context`, or more specific labels if mechanically supported by data.
  - Labels must not include order side, quote price, size, leverage, stop/take-profit, executable instruction, case-library trigger, or shadow decision.
- Execution-layer gap register:
  - Explicitly list what remains unproven because current evidence is observation-only public data: fill probability, queue/queue-ahead, post-only reject behavior, cancel-fill race, fees/rebates/spread capture, inventory lifecycle, and real order lifecycle.
  - Do not attempt to fill these gaps in this task.

required outputs：
- `local_live_analysis/basis_positive_filtered_context_viability_0609T003/filtered_context_viability_manifest.json`
- `local_live_analysis/basis_positive_filtered_context_viability_0609T003/raw_vs_filtered_basis_positive_summary.csv`
- `local_live_analysis/basis_positive_filtered_context_viability_0609T003/tail_risk_reject_subset_summary.csv`
- `local_live_analysis/basis_positive_filtered_context_viability_0609T003/per_sample_filtered_context_stability.csv`
- `local_live_analysis/basis_positive_filtered_context_viability_0609T003/horizon_filtered_context_stability.csv`
- `local_live_analysis/basis_positive_filtered_context_viability_0609T003/conditioning_filtered_context_summary.csv`
- `local_live_analysis/basis_positive_filtered_context_viability_0609T003/research_context_labels.csv`
- `local_live_analysis/basis_positive_filtered_context_viability_0609T003/execution_evidence_gap_register.md`
- `local_live_analysis/basis_positive_filtered_context_viability_0609T003/filtered_context_next_step_recommendation.md`
- `.workflow/reports/0609T003-business.md`

acceptance criteria：
- No new collection, no remote run, and no private/order/strategy/live process is used.
- Formal evidence must use T002 local event-mode/canonical artifacts; diagnostic-only synthetic artifacts may be mentioned only as diagnostics and must not drive the final recommendation.
- `basis > 0` vs filtered/rejected comparisons must exclude missing/non-numeric basis rows.
- Binance momentum control must use `input_binance_mid_move_ticks_from_prev`; if unavailable, emit an unavailable bucket and do not substitute a future label.
- T002 validated tail hypotheses must be evaluated both individually and as a combined reject/tail-risk mask.
- If a clean filtered subset is recommended, it must show:
  - at least `5` canonical samples represented,
  - max sample row share below `0.40`, or a caveated non-promotion recommendation,
  - wrong-way p95 loss improvement versus raw `basis > 0`, and
  - no obvious reversal across sample/horizon conditioning.
- Research labels must be explicitly marked read-only context labels and must not be executable case-library/shadow-decision artifacts.
- Execution-layer gap register must clearly state that current public observation data does not prove maker fill quality, order lifecycle, post-only behavior, queue position, fee/rebate capture, or inventory execution viability.
- Final recommendation must be exactly one of:
  - `candidate_for_read_only_case_design`
  - `keep_as_context_only_signal`
  - `needs_more_filtered_public_samples`
  - `reject_filtered_context_viability`
- Any recommendation remains read-only research only and does not authorize strategy/live/private/order behavior.

verify：
- Parse all input manifests used by the task with `python -m json.tool` or equivalent.
- Parse all output JSON/CSV artifacts.
- If a runner is added or changed:
  - `python examples/hyperliquid/<runner>.py --help`
  - `python -m py_compile examples/hyperliquid/<runner>.py`
  - focused pytest for any new/changed tests
- Boundary text check for no private/order/strategy/live/default-on/tiny-live/case-library/shadow/promotion authorization.
- `git diff --check`

forbidden：
- New public collection or remote collection.
- Local workstation collection counted as accepted sample.
- Private/account/order endpoint use on Binance or Hyperliquid.
- Order lifecycle, submit/cancel/fill logic, user stream, account state, positions, signing, nonce handling.
- Strategy implementation, strategy live, production config/default changes, parameter search, default-on, tiny-live, promotion.
- Case-library implementation, shadow decision generation, executable trading instruction, actual order side, quote price/size, leverage, stop rule, take-profit rule, deployment recommendation.
- Connector/core API changes, standard npz schema changes, canonical Binance maker audit schema changes unless a separate task explicitly authorizes them.

done：
- Business report must include input artifact paths, filtering definitions, raw-vs-filtered metrics, rejected-tail metrics, sample/horizon/conditioning stability, research labels, execution evidence gap register, final recommendation, verification commands, blockers/caveats if any, commit id, and commit message.
- Business report must explicitly state that this task is observation-layer read-only research only and does not prove execution-layer maker viability.
- Business report must end in `待验收` if local validation completes, or `阻塞` / `未通过` if criteria cannot be met.
```
