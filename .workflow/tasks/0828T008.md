# 任务派发

执行线程：
- SKHYNIX Liquidity Break Onset A0 Execution 业务线程

任务ID：
- 0828T008

标题：
- 实现并执行 LIQUIDITY_BREAK_ONSET_V1 A0 Causal Detector

简短描述：
- 按 0828T007 已验收 Revision 2 contract，在现有 29 个 Binance
  SKHYNIX captures 上实现 raw-message event-driven liquidity-break onset
  detector，生成 anchors、active intervals、outcome-blind controls 和
  support classification，严格禁止未来 target access。

状态：
- 待验收

执行顺序：
- 当前唯一正式任务

前置任务：
- 0828T007

规则更新提醒：
- 已阅读 AGENTS.md、workflow-kit manual、Conditional-Risk Research
  Methodology Kernel v1、0828T007 final contract/QA 和 predecessor
  A0-A3 results。

按线程规则执行：
- AGENTS.md
- .workflow/workflow-kit/workflow-manual.md

是否需要提交代码：
- 需要

提交要求：
- focused tests、full historical execution、deterministic rebuild 和
  artifact closure 后提交
- 回报必须带 commit id 和提交信息

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- examples/hyperliquid/skhynix_liquidity_break_onset_a0.py
- examples/hyperliquid/test_skhynix_liquidity_break_onset_a0.py
- docs/skhynix_binance_liquidity_break_onset_v1_a0_causal_anchor_contract_20260828.md
- local_live_analysis/skhynix_liquidity_break_onset_a0_0828T008/
- .workflow/tasks/0828T008.md
- .workflow/reports/0828T008-business.md

action：
- 验证 0828T007 final contract SHA 和 source inventory closure。
- 从 calibration captures 机械估计 frozen denominator floors。
- 对每个 capture 重建 causal top-five book 和 20ms component
  checkpoints。
- Checkpoint 在同 timestamp message 之前完成；不得读取未来或同 timestamp
  尚未处理的 message。
- 使用 `shift=26 checkpoints` 和 2,975-row history 实现
  `[c-60s,c-500ms)` rolling median/IQR。
- Global IQR scale floors 只由 2026-07-29 calibration role 拟合。
- 重新回放 raw stream，在每个 eligible depth/trade message 后计算 50ms
  components，并用最近完成 checkpoint normalizer 检测 onset。
- 实现 two-of-three coherence、direction ambiguity、precursor、
  active-lock release 和 opposite-onset switch。
- 生成 250ms outcome-blind controls；不得根据 future anchor 或 target
  排除候选。
- 按 frozen matching order 建立 no-reuse common-support pairs。
- 只使用 capture boundary/quality geometry 选择 `tau_max`。
- 计算 A0-0 至 A0-6 gates 和 canonical classification。
- 若 A0 未通过，停止当前版本，不打开 A1 outcome。

verify：
- Unit tests覆盖 event ordering、mirror direction、rolling windows、
  median/IQR、guard、start/end anchors、ambiguity、reset、controls 和
  zero-outcome ledger。
- 小型 synthetic positive/negative fixtures。
- 29 captures full historical execution。
- 两次从独立输出目录构建，compact artifact manifests 一致。
- Python compile、Markdown fence parity 和 `git diff --check`。
- Formal output size/SHA closure。

done：
- 回报 denominator/scale floors、anchor count/rate/date support、
  precursor delay、inter-anchor geometry、active occupancy、component pair
  diversity、control overlap、selected `tau_max`、A0 gates/classification
  和是否授权 A1。

明确禁止：
- 不读取 future midpoint/best-price transition。
- 不物化 adverse/recovery labels。
- 不拟合 H0/H1。
- 不修改 50ms window、threshold、coherence、release 或 support gates。
- 不用 20ms/100ms diagnostics rescue primary。
- 不访问 private API，不下单，不采集数据。
- 不提交已有无关 staged/untracked files。
