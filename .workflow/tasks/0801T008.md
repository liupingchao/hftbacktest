# 任务派发

执行线程：
- 业务线程-python/cross-exchange-research

任务ID：
- 0801T008

标题：
- HYPERLIQUID-RESIDUAL-MOTIF-PROTOTYPE-V2-REPAIR

简短描述：
- 基于已验收 T007 R2 conditional baseline 构建独立 `motif_v2/`，
  修复 discovery-only residual motif、prototype、surrogate 和
  post-selection 诊断合同。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- `0801T007` QA `已通过`。

规则更新提醒：
- `segment_0004-0008` 已被历史任务和 T007 消费，只能标记
  `post_selection`，不得恢复正式 `held_out`。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `docs/hyperliquid_liquidity_response_case_hierarchy_plan.md`

是否需要提交代码：
- 不需要

提交要求：
- 不需要提交
- 回报写 `commit：无`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `examples/hyperliquid/cross_exchange_liquidity_response_motif_v2.py`
- `examples/hyperliquid/test_cross_exchange_liquidity_response_motif_v2.py`
- `local_live_analysis/skhynix_liquidity_response_case_hierarchy/motif_v2/`
- `.workflow/tasks/0801T008.md`
- `.workflow/reports/0801T008-business.md`
- `.workflow/reports/0801T008-qa.md`
- `docs/qa-acceptance-report.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

action：
- 严格验证 T007 R2 discovery/post-selection manifests、contract、outputs
  row-count/SHA 和 post-selection consumption binding。
- 只从 discovery `segment_0001-0003` 拟合 robust transforms、observation
  masks、PCA、mutual `k=10` graph、Louvain communities 和 medoid
  prototypes。
- Motif 输入覆盖 episode shock shape、非 adverse 的 standardized response
  residual、latency/phase structure 和 observation masks。
- Motif feature allowlist 禁止 segment ID、absolute timestamp、
  adverse/PnL/profit/fee/fill/markout 字段，命中数必须为 `0`。
- Discovery community 需满足 size `>=100`、覆盖三个 discovery segments、
  单段占比 `<=50%`，每个 episode graph degree `<=10`。
- 冻结真实 medoid、discovery member distance p95、response quantiles、
  nearest support/counterexamples 和 full-pipeline surrogate screening。
- `0004-0008` 仅做 `post_selection` prototype-distance assignment 和
  诊断；不得输出正式 held-out walk-forward 支持。
- 无 fresh segment/date 时，classification 上限为
  `response_structure_only` / `needs_fresh_holdout` / `not_supported`。
- 输出 exact schema/path/row-count/SHA、输入 provenance、deterministic
  rebuild 和 fail-closed drift gates。

verify：
- focused tests 覆盖 feature leakage、discovery-only fit、graph degree、
  medoid identity、p95 assignment、surrogate determinism、contract/input
  drift、post-selection 标签和禁止 supported claim。
- `ruff`、`py_compile`、CLI help。
- 真实 T007 R2 -> motif v2 build 与重复构建。
- 独立复核 PCA/graph/community/prototype、输出 SHA、零 adverse/PnL
  features 和零 formal held-out label。
- `git diff --check`。

done：
- `motif_v2/` 独立发布，旧 `motif/` 保持不变。
- 不声明正式 held-out motif support、tradable signal、maker identity、
  exact fill 或 maker PnL。
- 业务报告进入 `待验收`，独立 QA 通过后才解锁 T009 regime repair。
