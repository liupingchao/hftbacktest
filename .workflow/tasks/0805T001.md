# 任务派发

执行线程：
- 业务线程-python/public-data-infra + cross-exchange-replay

任务ID：
- 0805T001

标题：
- CORE-L2-RECOVERED-RECONNECT-EPOCH-REPLAY-AND-EXACT-HORIZON-MASKS

简短描述：
- 将满足严格恢复证明的 Hyperliquid fast/standard L2 重连从整段永久拒绝，
  改为显式 opt-in 的 epoch-aware 分段回放。
- 对 0804T009 4H SKHYNIX 原始数据在 amdserver 执行只读 postprocess、
  R0、R1 重建，并形成可审计的 exact reconnect/horizon masks。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- `0804T001` 已通过，提供 continuous collection、R0/R1 v3 和 runtime
  provenance 合同。
- `0803T001` 已提供 bounded fast-L2 stale interval exact-mask 合同。
- `0804T009` 已完成 4H 原始采集并拉回 amdserver；原始 campaign 因
  Hyperliquid 各轨道 transport reconnect 被严格门禁拒绝。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 不需要

提交要求：
- 当前工作区包含已验收但未提交的前置工作，不创建混合提交。
- 回报使用 `commit：无`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 独立检查 legacy transport-marker reinterpretation、core-L2 recovery
  snapshot/ACK closure、epoch isolation、R0 masks、R1 horizon exclusion、
  raw immutability、remote runtime provenance 和重复构建确定性。

files：
- `examples/hyperliquid/hyperliquid_public_sample.py`
- `examples/hyperliquid/cross_exchange_collection_supervisor.py`
- `examples/hyperliquid/cross_exchange_l2_timeline.py`
- `examples/hyperliquid/cross_exchange_research_dataset.py`
- `examples/hyperliquid/cross_exchange_alignment_acceptance.py`
- 上述模块的 focused tests
- amdserver source campaign:
  `/home/molly/project/hftbacktest/local_live_analysis/0804T009_skhynix_4h_continuous`
- amdserver rebuilt campaign/R0/R1 under new `0805T001` output paths
- `.workflow/reports/0805T001-business.md`
- `.workflow/reports/0805T001-qa.md`
- `docs/qa-acceptance-report.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

action：
- 保留默认 core-L2 reconnect hard failure。
- 区分 malformed JSON 与 legacy empty-frame transport-close marker；只允许
  与 manifest disconnect 一一对应的空 marker 被重新解释。
- 对 fast/standard L2 验证 reconnect count、connection attempts、全部
  subscription ACK、必要 channel 恢复、recovery snapshot、raw row/SHA、
  freshness 和 bounded interval。
- 为每条恢复后的 L2 状态建立新的 `connection_epoch_id`；禁止旧状态跨
  reconnect forward-fill。
- 将 core reconnect、辅助轨道 reconnect 和 fast-L2 stale interval
  分别写入 exact masks。
- R1 decision lookback 和所有 outcome horizon 只要与相关 reconnect mask
  相交即不可用，不允许跨 epoch label、future join 或跨 mask PnL。
- 在 amdserver 对 4H raw 执行只读 rebuild；不修改原始 gzip、SHA、
  collection manifest、abort manifest 或 supervisor events。

verify：
- focused pytest 覆盖默认拒绝、opt-in 通过及 malformed/ACK/snapshot/
  recovery/interval/epoch/horizon hostile cases。
- `py_compile`、`git diff --check`。
- 4H source raw 构建前后逐文件 SHA 不变。
- rebuilt campaign/R0/R1 的 manifest、gzip、row count、mask count、
  source hash、runtime source 和重复构建 SHA 对账。
- R1 必须报告 exact masks、cross-epoch label count、mask-intersection
  exclusion count 和 accepted horizons；不得通过放宽 reconciliation
  或 coverage 门禁宣告合格。

done：
- 发布可回测、研究和审计的 segmented replay artifact。
- 明确区分 `raw_integrity_pass`、`continuous_exact_replay=false`、
  `segmented_replay_eligible`、`research_eligible_with_masks` 和最终
  R1/formal eligibility。

hard boundary：
- 不重新采集，不修改历史 raw bytes。
- 不允许 Binance reconnect 恢复。
- 不允许 malformed/non-empty JSON parse error 被降级接受。
- 不允许旧 L2 状态、订单、feature window 或 label 跨 reconnect epoch。
- 不形成 exact fill、可执行套利、maker PnL 或 live promotion 结论。
