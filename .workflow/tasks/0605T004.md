```md
执行线程：
- 业务线程-python

任务ID：
- 0605T004

标题：
- Shutdown final proof no-order dry-run validation

简短描述：
- 基于 `0605T003` 已验收的 shutdown final proof 分层语义，做一个本地只读/不下单 dry-run 验证任务。任务只使用 fake HBT / fake REST 路径生成 summary 与 audit tail proof artifacts，不连接交易所、不启动 live、不调用真实 cancel/order/openOrders。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- `0605T003` 已通过 QA，shutdown final proof 已支持 local proof 与 exchange open-orders reconciliation 分层表达。

规则更新提醒：
- 无

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `docs/thread-playbook.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交任务文件、业务报告、dry-run runner、focused tests 和本地 dry-run artifacts。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0605T004.md`
- `.workflow/reports/0605T004-business.md`
- `examples/binance_tick_mm/shutdown_proof_dry_run.py`
- `examples/binance_tick_mm/test_shutdown_proof_dry_run.py`
- `local_live_analysis/shutdown_proof_dry_run_0605T004/`

forbidden：
- 不连接 Binance / Hyperliquid / 任何交易所网络端点。
- 不启动 `live_tick_mm.py`、不启动 live / tiny-live / default-on。
- 不调用真实 `hbt.cancel()`、真实 `wait_order_response()`、真实 REST private/order endpoint。
- 不修改 py binding、Rust live bot/backtest、connector 核心实现、production config、normal trading loop cancel 语义、strategy quote/submit/cancel 行为。
- 不做参数搜索、回放实验扩展、promotion。

action：
- 新增一个本地 dry-run runner，只用 fake HBT / fake REST 调用 `cancel_working_orders_for_shutdown()` 和 `_append_shutdown_final_proof_audit_tail()`。
- dry-run 至少生成这些场景：
  - local absent + exchange absent -> `exchange_reconciled`
  - local absent + exchange still open -> `exchange_still_open`
  - exchange check failed -> 明确 failure status，不伪造 exchange proof
  - local active + exchange absent -> `exchange_absent_only`
  - local terminal + exchange absent -> `exchange_reconciled`
  - no REST client -> `local_only`
- 输出 artifacts 至少包括：
  - `run_manifest.json`
  - `shutdown_proof_summary.csv`
  - `shutdown_final_proof_audit_tail.csv`
  - `shutdown_proof_dry_run_report.md`
- 报告必须明确这是 local fake dry-run，只验证 proof/log/audit 可观测性，不代表真实交易所运行证明。

acceptance criteria：
- runner 不需要网络和真实 live/binding/connector。
- artifacts 能明确看到每个场景的 local proof、exchange reconciliation result 和 final proof level。
- `exchange_still_open`、`exchange_check_failed` 不得被误标为 `exchange_reconciled`。
- `wait_result == 3` 仍不得单独推导 terminal confirmed。
- focused tests 覆盖 runner scenario 输出、artifact schema、无网络/无真实 client 边界。
- package-level `examples/binance_tick_mm/test_*.py` 应通过。

verify：
- `/home/molly/anaconda3/envs/hftbacktest/bin/python examples/binance_tick_mm/shutdown_proof_dry_run.py --output-dir local_live_analysis/shutdown_proof_dry_run_0605T004`
- `/home/molly/anaconda3/envs/hftbacktest/bin/python -m pytest examples/binance_tick_mm/test_shutdown_proof_dry_run.py`
- `/home/molly/anaconda3/envs/hftbacktest/bin/python -m pytest examples/binance_tick_mm/test_*.py`
- `git diff --check`

done：
- 业务报告写入 `.workflow/reports/0605T004-business.md`。
- 回报必须说明 dry-run 场景、artifact 路径、验证结果、commit id 和 commit message。
- 报告必须明确本任务未连接交易所、未下单、未启动 live、未修改策略行为、未做 promotion。
```
