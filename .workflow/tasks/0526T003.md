```md
执行线程：
- 业务线程-python

任务ID：
- 0526T003

标题：
- Step 9F narrow min_move_quote_age_churn_guard parameter-sweep design

简短描述：
- 基于 `0525T001` / `0526T001` / `0526T002` 的 Step 9D aggregate 结果，制定一个窄范围的 `min_move_quote_age_churn_guard` 参数搜索设计合同。seed 只围绕 `inventory_state=large_skew_or_low_score` 和 `latency_stale_age=stale_latency_medium`，目标是定义后续 read-only sweep implementation 的输入、参数网格、评价指标、verdict 和边界，不执行参数搜索、不启动 live、不做 promotion。

状态：
- 已通过

执行顺序：
- 当前唯一任务；已派发并完成 business-thread design execution

前置任务：
- `0526T002` QA 已通过
- `0525T001` QA 已通过
- `0519T006` QA 已通过，T006 quote-update audit fields 已落地

规则更新提醒：
- 请先阅读规则文档后再执行本任务。
- 本任务是 planning/design-only，不是 runner implementation、parameter sweep execution、live test、tiny-live design、default-on 或 promotion 任务。
- 不允许扩大 seed scope，不允许把 caveated samples 作为 strict-clean promotion 证据，不允许放宽 guard 来制造 fills。

按线程规则执行：
- `.workflow/workflow-kit/thread-report-template.md`
- `docs/thread-playbook.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交 workflow/task/report/doc updates。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

Allowed files：
- `.workflow/tasks/0526T003.md`
- `.workflow/reports/0526T003-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `.workflow/dashboard.html`
- `.workflow/dispatch_suggestions.md`

Allowed read-only inputs：
- `.workflow/reports/0525T001-business.md`
- `.workflow/reports/0525T001-qa.md`
- `.workflow/reports/0526T001-business.md`
- `.workflow/reports/0526T001-qa.md`
- `.workflow/reports/0526T002-business.md`
- `.workflow/reports/0526T002-qa.md`
- `local_live_analysis/stage9d_candidate_bucket_refinement_0525T001/**`
- `local_live_analysis/stage9d_candidate_bucket_refinement_0526T001_aggregate/**`
- `local_live_analysis/stage9d_candidate_bucket_refinement_0526T002_aggregate/**`
- `local_live_analysis/stage9c_multi_sample_validation_0521T002/**`
- `local_live_analysis/5-19-day-control-30min/**`
- `local_live_analysis/5-19-night-active-30min-a/**`
- `local_live_analysis/5-19-night-active-30min-b/**`
- `local_live_analysis/5-19-night-active-30min-c/**`
- `local_live_analysis/5-21-day-control-60min/**`
- `local_live_analysis/5-26-active-minmove-control-30min-a/**`
- `local_live_analysis/5-26-active-minmove-control-60min-a/**`
- `examples/binance_tick_mm/quote_adjustment_replay.py`
- `examples/binance_tick_mm/candidate_bucket_refinement.py`
- `examples/binance_tick_mm/strategy_core.py`
- `examples/binance_tick_mm/quote_anchor_safety.py`
- `examples/binance_tick_mm/execution_outcome_labels.py`
- `examples/binance_tick_mm/execution_outcome_calibration.py`

Forbidden files / areas：
- Strategy behavior, production defaults, live configs, fair/target formula, risk/position logic, quote placement logic.
- Core/connector APIs, standard npz schema, converter behavior, replay lifecycle repair, queue/touch repair.
- Any runner implementation, parameter sweep execution, sample collection, live/tiny-live run, default-on behavior, promotion claim, or `ready_for_tiny_live_design` classification.

Requires separate task：
- Implementing the parameter-sweep runner.
- Running the parameter sweep over samples.
- Collecting more strict-clean active samples.
- Promoting any candidate into tiny-live design or production.
- Changing any quote-update or risk-control behavior.

Fatal Failures：
- Treating this task as implementation rather than design.
- Expanding scope beyond `min_move_quote_age_churn_guard`.
- Adding seed buckets beyond `inventory_state=large_skew_or_low_score` and `latency_stale_age=stale_latency_medium` without creating a separate task.
- Using future fill, future markout, same-sample PnL feedback, or fill-after-cancel outcome as decision inputs.
- Treating caveated samples as strict-clean proof.
- Emitting `ready_for_tiny_live_design`, default-on, or live-promotion recommendations.

design goal：
- Produce a concrete implementation contract for a later read-only sweep task that answers:
  - Whether `min_move_quote_age_churn_guard` has a stable local parameter region inside the two accepted seed regimes.
  - Whether the useful effect is mainly churn reduction, toxic-fill reduction, or simply fewer fills.
  - Whether the best next step is implementation of a read-only sweep runner, more strict-clean sample collection, or rejection / deprioritization.

sample policy：
- Use the seven current-format samples as the design input universe:
  - `5-19-day-control-30min`
  - `5-19-night-active-30min-a`
  - `5-19-night-active-30min-b`
  - `5-19-night-active-30min-c`
  - `5-21-day-control-60min`
  - `5-26-active-minmove-control-30min-a`
  - `5-26-active-minmove-control-60min-a`
- Mark these two as caveated:
  - `5-19-night-active-30min-a`
  - `5-26-active-minmove-control-60min-a`
- Treat strict-clean samples as the primary decision set.
- Use caveated samples only for sensitivity / broader evidence.
- Report both clean-only and all-sample expected evaluation paths.

seed scope：
- Candidate family:
  - `min_move_quote_age_churn_guard`
- Seed bucket A:
  - `inventory_state=large_skew_or_low_score`
- Seed bucket B:
  - `latency_stale_age=stale_latency_medium`
- Required design slices:
  - A only
  - B only
  - A and B intersection
  - A or B union, only as an aggregate context view

required parameter grid design：
- Define a small first-pass grid and justify why it is narrow enough to avoid blind sweeping.
- Include at minimum:
  - `min_move_ticks`: suggested values `1`, `2`, `3`
  - `min_quote_age_ms`: suggested values `250`, `500`, `1000`, `2000`
  - `churn_window_ms`: suggested values `1000`, `3000`, `5000`
  - `max_readd_count_in_window`: suggested values `1`, `2`, `3`
  - `stale_latency_guard_ms`: suggested values `50`, `100`, `200`, only within the `latency_stale_age=stale_latency_medium` seed slice
- Define which parameters are global across both seeds and which are seed-local.
- Define a smaller fallback grid if fill mass inside the strict-clean seed slices is too low.
- Define the expected parameter-set scale:
  - maximum `432` unique parameter sets under the full grid
  - maximum `756` seed-slice evaluations when evaluating A only, B only, and A/B intersection
  - `A or B` union is an aggregate context view and should not create a separate sweep pass

required compute / parallelization design：
- Treat the current local machine (`amdserver`) as the target execution server for the later sweep implementation.
- The later implementation should explicitly use the available `32` CPU cores and about `60G` memory for parallel acceleration.
- Define a parallelization plan over independent units:
  - `sample_id`
  - seed slice: A only, B only, A/B intersection
  - parameter chunks
- Define a reasonable default worker / chunk policy for the future runner, for example:
  - default workers bounded by CPU cores and memory pressure
  - parameter chunks of roughly `25` to `50` parameter sets per worker shard
  - local smoke mode with a tiny grid before full 7-sample execution
- Require shard-level outputs for the later implementation:
  - `sample_id`
  - `seed_slice`
  - `param_hash`
  - `parameter_values`
  - `worker_id`
  - shard start/end time
  - row counts and error status
- Require deterministic merge semantics:
  - each shard writes independent CSV/JSON artifacts
  - final reducer combines shards into stable sorted summary outputs
  - reruns with the same inputs and parameters should produce identical aggregate rows
- Require resource safety:
  - avoid loading all full-sample artifacts into every worker if a streaming or per-sample preload plan is feasible
  - cap workers if memory pressure would exceed the `60G` local budget
  - fail a shard explicitly rather than silently dropping parameter sets
- This compute plan is design-only in T003; no actual parallel runner or sweep execution is authorized by this task.

required decision-input policy：
- The later sweep may use only decision-time-visible inputs, such as:
  - quote age
  - join age
  - anchor age
  - stale / latency bucket
  - min-move distance
  - recent cancel / re-add churn state
  - inventory score / skew state
  - spread and post-only safety state
- The later sweep must not use:
  - future fill
  - future markout
  - same-sample PnL feedback
  - fill-after-cancel outcome
  - audit replay overlay fields that would not exist at live decision time

required strategy-shape design：
- Define the intended rule shape in pseudocode.
- Required default shape:
  - If one of the two seed regimes is matched, suppress or delay a quote update when price move is below `min_move_ticks`, quote age is below `min_quote_age_ms`, churn pressure is active, and replacing the order does not materially improve post-only safety or inventory risk.
  - Outside the seed regimes, behavior remains unchanged.
- Define any allowed bypass cases, for example a replacement that materially improves post-only safety or reduces inventory risk.
- Require bypass cases to be counted separately in the later implementation.

required evaluation metrics：
- For each sample, seed slice, and parameter combination, define outputs for:
  - decision rows
  - submit orders
  - submit reduction
  - cancel / re-add reduction
  - fast-cancel churn delta
  - filled orders
  - fill rate and delta versus bucket baseline
  - 5s side-adjusted markout and delta versus bucket baseline
  - spread capture delta
  - fill-after-cancel delta
  - inventory-increasing fill rate delta
  - inventory-reducing fill rate delta
  - post-only risk after recheck
  - missing / stale anchor exposure
  - reject / throttle / drop exposure
  - net PnL proxy delta, diagnostic only

required stability summary：
- Define cross-sample summaries:
  - clean-only median delta
  - clean-only worst-sample delta
  - clean-only win rate
  - all-sample sensitivity
  - caveated-sample influence
  - fill sufficiency by seed slice
  - coverage sufficiency by seed slice
- Define minimum evidence rules for distinguishing:
  - true churn / toxic-fill improvement
  - simple fill suppression
  - caveated-only effect
  - insufficient-fill uncertainty

required verdict taxonomy：
- Each parameter combination must have one of:
  - `sweep_seed_promising`
  - `stable_but_low_fill`
  - `churn_only_no_execution_benefit`
  - `too_conservative_fill_loss`
  - `caveated_only`
  - `reject`
  - `not_decisionable`
- The task must define the exact conditions for each verdict.

required outputs：
- `.workflow/reports/0526T003-business.md`
- A design section in `findings.md` or `task_plan.md` summarizing the accepted sweep contract.
- The report must include:
  - final seed scope
  - sample policy
  - parameter grid
  - evaluation metrics
  - verdict rules
  - implementation boundary for the future runner task
  - explicit non-goals
  - whether the design supports creating a later read-only sweep implementation task

verify：
- `python3 .workflow/build_dashboard.py`
- `git diff --check`
- Manual consistency check that:
  - task status remains `待执行` until formally派发
  - no code / runner / live / sample collection changes were made
  - scope is only `min_move_quote_age_churn_guard`
  - seed scope is only `inventory_state=large_skew_or_low_score` and `latency_stale_age=stale_latency_medium`

done：
- State whether the design supports a later read-only parameter-sweep implementation task.
- State that this task does not authorize tiny-live, default-on, live promotion, guard relaxation, or `ready_for_tiny_live_design`.
- State whether more strict-clean sample collection remains an alternative path if the controller requires the `500` strict-clean fill gate before any sweep implementation.
```
