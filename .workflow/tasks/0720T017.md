# 0720T017 CANCEL-TERMINAL-STATUS-EVIDENCE-REPAIR

执行线程：
- 总控 auto-loop / 业务执行线程

任务ID：
- 0720T017

标题：
- Repair reference-bound cancel terminal evidence, final operator status and submitted-attempt acceptance

简短描述：
- Close the three offline defects identified by T016 QA without changing strategy behavior or inventing historical terminal evidence.
- Preserve cancel failure responses, add independently reconstructable oid/cloid-bound terminal query evidence, refresh terminal `live_status`, and make exact-two lifecycle checks operate on submitted rows.

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0720T016`（未通过；账户终态安全，但 lifecycle 未接受）

规则更新提醒：
- 本任务 offline-only。
- T016 historical attempt 2 terminal evidence cannot be synthesized or upgraded。
- No live/private/account/order/cancel/network/remote/service action is authorized inside this task。
- Task 8 and every adaptive/multi-level live action remain locked。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `.workflow/reports/0720T016-qa.md`
- `docs/superpowers/plans/2026-07-17-principal-alignment-p0-p3.md`

是否需要提交代码：
- 是

提交要求：
- 实现和测试提交。
- 业务报告单独提交。
- 回报必须带 implementation commit id 和提交信息。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

source：
- Branch: `cross-exchange`
- Dispatch base: `b0052265170250686e7a63cf23b742715811eaaf`

files：
- `examples/hyperliquid/hyperliquid_maker_order_manager.py`
- `examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- `examples/hyperliquid/hyperliquid_tiny_live_m2_fill_window.py`
- `examples/hyperliquid/cross_exchange_t024_same_window_acceptance.py`
- Focused tests for the modules above
- `.workflow/tasks/0720T017.md`
- `.workflow/reports/0720T017-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

action：
- Preserve the redacted raw exchange response when cancel validation fails；do not reduce it to an exception string。
- Persist canonical per-attempt terminal query evidence:
  - exact attempt、oid/cloid token and query method；
  - redacted raw query response or redacted query error；
  - independently classifiable status；
  - no credential、account address、raw signature or unredacted reference。
- Treat terminal cancellation as proven only when one of these exact facts exists:
  - target-bound authoritative cancel success；or
  - target-bound exchange query proves `cancel_confirmed` and the same owned reference is absent from final open orders。
- Do not accept generic “already canceled or filled”、account-wide empty orders、unknown query or mismatched oid/cloid as reference-bound terminal proof。
- A query classified `filled` cannot replace complete raw fill attribution/quantity/role proof。
- Update manager reconciliation so a target-bound `cancel_confirmed` query becomes terminal；unknown remains fail-closed。
- Force the final `live_status.json` write after terminal manager/account reconciliation:
  - zero owned open orders must produce zero working exposure；
  - unresolved reference remains visible as unknown and a blocker；
  - writer health remains independent from semantic terminal status。
- Extend producer cancel/fill evidence and acceptance independent reconstruction for the new terminal-query contract。
- Change exact-two lifecycle cardinality to use only submitted rows (`order_endpoint_called=true`)；continue to validate all candidate/no-submit rows and their causal joins separately。
- Add positive and adversarial tests for:
  - cancel validation failure preserves redacted response；
  - exact oid/cloid-bound canceled query proves terminal；
  - generic、unknown、mismatched、duplicate or malformed query evidence fails closed；
  - query `filled` without complete raw fill proof fails closed；
  - final status is zero after terminal reconciliation and remains unknown when unresolved；
  - four candidate-attempt rows with exactly two submitted rows pass the exact-two decision check；
  - one or three submitted rows remain blocked。
- Replay exact T016 locally:
  - remove only the decision cardinality false-negative；
  - preserve lifecycle blocker for historical attempt 2；
  - preserve provenance/config/checksum and unsupported claims。

verify：
- Focused manager/watcher/fill/acceptance tests pass。
- Full `python -m pytest -q examples/hyperliquid` passes。
- Modified modules/tests pass `py_compile`。
- Acceptance CLI `--help` passes。
- Exact T016 replay remains offline、byte-preserving and blocked for missing attempt-2 terminal proof；decision replay no longer fails solely because candidate rows are `4`。
- Synchronized forged terminal-query evidence remains blocked after fixture reseal。
- `git diff --check` and implementation `git show --check` pass。
- No live/private/account/order/cancel/network/remote/service action occurs。

done：
- Future cancel-unknown paths preserve enough redacted raw evidence for independent classification。
- Reference-bound terminal query evidence can prove a canceled order without weakening fill or ownership semantics。
- Terminal operator status agrees with final manager/account reconciliation。
- Candidate evidence and submitted lifecycle cardinality are separate contracts。
- T016 remains historically blocked where evidence is genuinely absent。
- Independent QA accepts T017 before any new bounded live task。

stop conditions：
- Repair treats account-wide empty open orders as sufficient reference terminal proof。
- Generic cancel error is accepted without exact target-bound terminal query evidence。
- Filled query bypasses raw fill attribution/quantity/role proof。
- Final status hides an unresolved owned reference。
- T016 historical attempt 2 becomes terminal through synthetic or inferred evidence。
- Strategy formula、signal freshness、edge threshold、risk cap、activation or quote behavior changes。
- Any verification requires live/private/account/order/cancel/network/remote/service。

blockers：
- Independent QA failure keeps the new bounded live、Task 8 and multi-level locked。

commit：
- implementation `b35ea1e665595d9c3ae54b069cb239be44480256`

提交信息：
- `Repair terminal cancel evidence reconciliation`
