# 0720T032 LEADING-CENSOR-DELAYED-HISTORY-PROTOCOL-REPAIR

执行线程：
- 总控 auto-loop / 业务执行线程

任务ID：
- 0720T032

标题：
- Add explicit leading left-censor evidence and delay the single terminal-history fallback

简短描述：
- 修复 T031 QA 的两个 blocker：
  - producer/replay/Task 12 对 first usable interval-local book 之前的 leading event-time prefix 使用同一显式 left-censor contract；
  - generic cancel unknown 的唯一 historical fallback 等待 bounded propagation delay 后再调用。
- 本任务只修改 evidence/recovery protocol，不修改 quote、order、risk、submission envelope 或任何 adaptive activation。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0720T031`（未通过）

规则更新提醒：
- 本任务 offline-only。
- T031 live artifacts immutable，不得修改、重写或离线升级为通过。
- Account-wide empty open orders 仍不能替代 per-reference authoritative terminal proof。
- 独立 QA 通过前不得进行 private read、新 live 或 dynamic/fill/inventory/multi-level activation。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `.workflow/reports/0720T031-qa.md`
- `docs/superpowers/plans/2026-07-17-principal-alignment-p0-p3.md`

是否需要提交代码：
- 需要

提交要求：
- 业务实现和业务报告分开提交。
- 回报必须包含 implementation commit、验证命令和结果。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- Modify: `examples/hyperliquid/cross_exchange_online_estimators.py`
- Modify: `examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- Modify: `examples/hyperliquid/cross_exchange_t024_same_window_acceptance.py`
- Modify: `examples/hyperliquid/hyperliquid_maker_order_manager.py` only if needed for bounded history protocol/config evidence
- Test: `examples/hyperliquid/test_cross_exchange_online_estimators.py`
- Test: `examples/hyperliquid/test_hyperliquid_tiny_live_m2_event_driven_watcher.py`
- Test: `examples/hyperliquid/test_cross_exchange_t024_same_window_acceptance.py`
- Test: `examples/hyperliquid/test_hyperliquid_maker_order_manager.py` only if manager config/query evidence changes
- Workflow: this task, business report, QA report, fixed QA document, `task_plan.md`, `progress.md`, `findings.md`

action：
- Define a versioned confirmed-resting censor row schema with attempt、side、event-time bounds、reason and inference scope。
- Compute one leading left-censor interval from effective coverage start to the first usable interval-local book when that book starts later。
- Persist censor rows separately from quarantine；no leading prefix may be silently skipped。
- Keep malformed interval、no usable book、interior missing reference、future、out-of-order and continuity failures in quarantine。
- Make watcher snapshot include censor row count and keep activation false。
- Make estimator replay rebuild and exact-compare exposure、censor and quarantine rows；snapshot success requires all three contracts to match and quarantine to be empty。
- Make Task 12 independently rebuild and exact-compare the same censor contract；it must reject missing、duplicate、overlapping、non-leading or malformed censor rows。
- Preserve legacy contract-absent replay behavior for T026/T016/T022。
- Add a bounded terminal-history propagation delay for unresolved generic cancel references：
  - five direct rounds maximum；
  - five seconds total budget maximum；
  - one historical call maximum per reference；
  - history call not before the configured propagation delay；
  - enough remaining deadline must exist for the bounded call and final open-orders snapshot。
- Persist planned/actual history wait、not-before point、call count and post-history snapshot completion evidence。
- Keep generic cancel unknown fail closed when delayed history is absent、foreign、conflicting、malformed、nonterminal or filled without raw fill proof。

verify：
- A T031-shaped local-receive/exchange-time straddle produces the same six exposure rows plus explicit leading censor rows and zero quarantine。
- Producer、replay and Task 12 rebuild censor rows exactly and deterministically。
- A trade-only interval with no usable book remains quarantine，not a clean censor-only result。
- Interior reference gaps and invalid/future/out-of-order evidence remain quarantine。
- Missing or forged censor artifacts fail replay/acceptance closed。
- Delayed history is not called before its not-before time。
- Direct round count never exceeds `5`，history call count never exceeds `1`，total recovery never exceeds `5s` plus existing scheduling tolerance。
- Exact delayed canceled/rejected history may close a reference；filled still requires raw fill proof。
- Generic error plus empty account without target-bound history remains unknown。
- T031 immutable replay remains blocked；no exact bridge upgrades its missing terminal proof or persisted old quarantine。
- T026 exact acceptance remains pass；T016/T022 remain blocked at their historical terminal boundary。
- Dynamic spread、fill feedback、inventory skew、multi-level and actual quote behavior activation remain false。
- Focused and full Hyperliquid regression pass。
- No live、private/account、order、cancel、network、remote or service operation occurs。

stop conditions：
- Any T031 raw artifact modification。
- Any account-wide inference used as per-reference terminal proof。
- Any second history call or history call outside the total deadline。
- Any silent leading-prefix skip without persisted censor evidence。
- Any weakening of interior/invalid/future/out-of-order quarantine。
- Any T026/T016/T022 historical boundary regression。
- Any quote、risk、size、submission、side-set or activation behavior change。
- Any live/private/network/remote/service action。

done：
- Shared leading left-censor and delayed one-call history protocols are implemented with hostile tests and deterministic evidence。
- T031 remains immutable blocked evidence。
- The repaired source is ready for independent QA and, only after acceptance, a separately dispatched fixed-quote observe-only single window。

blockers：
- Independent QA acceptance is required before any new private read/live or Task 8 activation。

commit：
- implementation `a01593b545fa5cb50c69460e1e96805bf46d6a78`

提交信息：
- implementation `Repair censor and delayed history protocols`
