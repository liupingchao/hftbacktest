```md
执行线程：
- 业务线程-live

任务ID：
- 0623T001

标题：
- M2 public state freshness / post-open_orders BBO resync repair

简短描述：
- 修复 `0622T005` / `0622T006` 暴露的核心硬伤：`open_orders()` 同步阻塞期间 public stream 可能已经产生新 BBO，但 inline reprice 仍可能使用旧的 in-memory snapshot。
- 本任务只修状态新鲜度和 post-`open_orders` reprice 事实源，不扩大真实提交次数、不改变 quote distance、不接入新 alpha，也不放宽 maker-only 风险边界。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- `0622T006` 业务执行 / QA 结果应先由总控确认；若 `0622T006` 已产生相关代码，业务线程必须在其基础上最小化修复，不得回滚无关改动。
- `0622T005` QA 为 `已通过`，其 artifacts 证明 post-`open_orders` reprice-to-submit 本地路径很快，但 exchange-side BBO drift 仍导致 post-only reject。
- `0618T008` QA 为 `已通过`，T008 ledger/reconciler 继续作为 M2 PnL 事实源。

规则更新提醒：
- 请先阅读 `.workflow/workflow-kit/workflow-manual.md`、`.workflow/workflow-kit/task-dispatch-template.md`、`.workflow/workflow-kit/thread-report-template.md`、`.workflow/workflow-kit/qa-acceptance-template.md`、`task_plan.md`、`progress.md`、`findings.md` 后再执行本任务。

按线程规则执行：
- `AGENTS.md`
- `docs/hyperliquid_tiny_live_real_order_executor.md`
- `docs/hyperliquid_tiny_live_final_go_no_go_gate.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- `examples/hyperliquid/test_hyperliquid_tiny_live_m2_event_driven_watcher.py`
- `examples/hyperliquid/test_hyperliquid_tiny_live_m2_public_watcher.py`
- `local_live_analysis/hyperliquid_tiny_live_m2_state_freshness_0623T001/**`
- `.workflow/reports/0623T001-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

read-only inputs：
- `.workflow/tasks/0622T005.md`
- `.workflow/reports/0622T005-business.md`
- `.workflow/reports/0622T005-qa.md`
- `.workflow/tasks/0622T006.md`
- `local_live_analysis/hyperliquid_tiny_live_m2_inline_reprice_0622T005/**`

hard boundary：
- Keep Hyperliquid `Alo` post-only. No taker/crossing, no `Ioc`, no one-tick-back workaround, no cap relaxation, no default-on behavior, no promotion, no stable PnL claim, and no M3 readiness claim.
- Dynamic order size hard cap remains `<=0.005 BTC`.
- This task must not increase real submission caps or run a live order window unless total control separately dispatches a live-calibration follow-up.
- Public waiting remains public-only. Private/order endpoints may be represented only by local mocks/tests unless a separately authorized live task is created.
- Do not print or persist credential values, private keys, raw signatures, nonces, wallet secrets, or unredacted account identifiers.

action：
- Refactor event-driven inline submit so public L2/trades consumption can continue updating an authoritative public state while private `open_orders()` is in progress, or otherwise require a new public event after `open_orders()` before submit.
- Add per-snapshot freshness metadata such as `public_state_seq`, `exchange_time_ms`, `local_receive_ts_ns`, and `state_observed_after_open_orders_end`.
- After `open_orders()` returns, reprice only from a public snapshot observed after `open_orders_end`. If no new public state arrives inside a short bounded wait, fail closed with `post_open_orders_public_state_stale`.
- Add artifacts / matrix columns proving which BBO state was used before `open_orders`, after `open_orders`, at guard, and at submit intent.
- Add focused tests simulating BBO movement during `open_orders()` and verify the order endpoint is not called with the stale pre-`open_orders` BBO.

verify：
- `python -m pytest examples/hyperliquid/test_hyperliquid_tiny_live_m2_event_driven_watcher.py -q`
- `python -m pytest examples/hyperliquid/test_hyperliquid_tiny_live_m2_public_watcher.py -q`
- `python -m py_compile examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- `python examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py --help`
- `git diff --check`

done：
- 回报 stale-BBO 修复方式、post-`open_orders` public-state freshness 规则、超时 fail-closed 规则、mock 覆盖的 BBO drift 场景、order endpoint 是否被正确阻断、artifact 路径、blockers、commit id、提交信息。
- 明确说明本任务是否只完成状态新鲜度修复；除非另有正式 live 任务和 T008 证明，不得声明 M2 完成、稳定 PnL、或 M3 ready。
```
