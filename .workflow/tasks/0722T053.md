# 0722T053 P3-MULTI-LEVEL-ACTIVATION-READINESS

执行线程：
- 总控 auto-loop / 业务执行线程

任务ID：
- 0722T053

标题：
- Complete Principal Task 10 executable ladder and multi-order manager integration

简短描述：
- 在 T052 已接受的单层双边 lifecycle 前置条件上，完成 Task 10
  `levels>1` 的 executable quote ladder、price-keyed multi-order reconcile
  和 aggregate exposure/submission fail-closed；保持默认关闭且本任务
  offline-only。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0718T021`（已通过；仅 default-off ladder contract/gate）
- `0722T052`（已通过；单层双边 lifecycle mechanism/evidence baseline）

规则更新提醒：
- offline-only；禁止 live、private/account、order/cancel、network、remote、
  service 和 existing sealed evidence 修改。
- T052 满足 single-level lifecycle prerequisite，但不自动授权 multi-level
  live；standing live envelope 的 `2 submissions` 上限保持。
- 本任务只完成 code/action-path/replay readiness，不宣称 live multi-level、
  fill-rate、maker viability、promotion 或稳定经济性。

是否需要提交代码：
- 需要

是否进行QA验收：
- 是

files：
- `examples/hyperliquid/cross_exchange_shared_signal_kernel.py`
- `examples/hyperliquid/hyperliquid_maker_order_manager.py`
- `examples/hyperliquid/test_cross_exchange_quote_ladder.py`
- `examples/hyperliquid/test_hyperliquid_maker_order_manager.py`
- workflow task/business/QA/fixed QA records

action：
- 当 `levels>1`、activation enabled 且 single-level prerequisite true 时，
  ladder builder 输出 executable `quote_intents`，并标记 actual quote
  behavior changed；default-off 和 prerequisite false 继续返回空 intents。
- manager 支持配置化有限 `max_levels_per_side`，以
  `(symbol, side, canonical_price_key)` 管理同侧多个 owned orders。
- reconcile desired 按 exact price key hold/cancel/submit，不允许同侧重复
  desired price；撤销不再需要的旧 level 后才补新 level。
- 在任何新 submit 前原子校验 proposed ladder 的 side cardinality、
  submission budget 和 aggregate projected exposure；预算不足不得形成
  partial ladder submit。
- 保持 unknown/cancel-pending exposure、post-only、price normalization、
  ownership cloid generation、anti-churn 和 kill-switch/risk 优先级。

verify：
- active ladder success：至少 2 levels/side，executable intents 数量和
  price/size ordering 确定。
- default-off/prerequisite false 不产生 executable intents。
- manager 多层首次 submit、相同 ladder hold、移除 level cancel、价格变化
  cancel-before-readd、restart/open-order reconcile。
- duplicate desired price、per-side level cap、aggregate exposure 和
  submission budget 均在任何 submit 前 fail-closed。
- focused ladder/manager tests、相关 watcher/kernel/executor regression、
  full `python -m pytest examples/hyperliquid -q`、py_compile、
  `git diff --check`。

done：
- Task 10 code-level multi-level activation contract 和 manager action path
  通过独立 QA。
- Multi-level live 仍需后续独立 exact-envelope task；当前 `2 submissions`
  cap 不被扩大。

blockers：
- 若真实 multi-level live 需要超过 standing `2 submissions` 上限，必须
  另行获得风险 envelope 决策；本任务不得自行扩张。

提交信息：
- `Enable guarded multi-level ladder execution`
