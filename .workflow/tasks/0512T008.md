```md
执行线程：
- 测试线程

任务ID：
- 0512T008

标题：
- hbt.depth live/replay view 确认与数据层质量门禁

简短描述：
- 只读确认当前 Binance maker MM 策略在 live 与 backtest/replay 中通过 `hbt.depth(0)` 实际看到的 market view，以及现有 audit/raw 数据是否足够支持后续 microprice / OFI / queue 研究。本任务制定计划和验收方案，并执行现有样本上的数据层诊断；不改策略逻辑、不运行新的 replay、不启动 live。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- 0512T007

规则更新提醒：
- 本任务是只读 market-data / strategy-view 诊断。
- 不修改策略代码、不改变配置默认值、不运行新的 Stage 6J replay、不启动 live。
- 本任务只能决定数据层是否足够支撑后续研究，不能声明任何策略收益改善或 live promotion。

按线程规则执行：
- .workflow/workflow-kit/thread-report-template.md
- docs/thread-playbook.md
- task_plan.md
- progress.md
- findings.md

是否需要提交代码：
- 不需要

提交要求：
- 不需要提交
- 回报必须写 `commit：无`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- .workflow/tasks/0512T008.md
- .workflow/reports/0512T008-business.md
- .workflow/runners/run_0512T008.py
- local_live_analysis/t008_market_data_view_quality/
- task_plan.md
- progress.md
- findings.md

Fatal Failures：
- 修改 `examples/binance_tick_mm/` 策略逻辑或配置默认值。
- 启动 live、连接交易所、修改 AWS 状态。
- 运行新的 Stage 6J replay、backtest sweep 或候选规则实验。
- 把当前任务的 view/data-quality 结论写成策略风险改善、PnL 改善或 live source-path proof。
- 在现有 audit 字段不足时继续直接推进 microprice / OFI / queue 策略实现。

MUST NOT：
- 不改交易策略代码。
- 不运行 live。
- 不运行新的 replay 或 sweep。
- 不设计或实现新交易规则。
- 不声明 live promotion。

计划：
- 代码路径确认：梳理 live/backtest 中 `hbt.depth(0)` 的读取位置、压缩字段、fair/target 计算位置，以及 `strategy_core.decide_actions()` 是否接收 full depth。
- Audit schema coverage：检查 live audit、audit replay、Stage 6J baseline audit 是否包含 best bid/ask、mid、top5、target ticks、timestamp、latency、update id、bookTicker、full depth/top-N 等字段。
- Overlay 语义确认：比较 live audit 与 `market_state_overlay=audit` 的 audit replay，确认 compressed market state 是否由 live audit 强制喂回 replay。
- Replay reconstructed depth 对比：比较 live audit 与 Stage 6J baseline no-overlay audit 在 matched decision timestamp 上的 best bid/ask、mid、top5、bid/ask size、target ticks 差异。
- Raw Binance depth stream 检查：只读解析本地 raw gzip，检查 depthUpdate / snapshot / bookTicker 事件存在性、`U/u/pu` 连续性、local/exchange timestamp latency 分布，以及当前 converted npz 是否保留 update ids。
- 结论分层：区分“当前简单策略 audit 对齐是否够用”和“后续 microprice / OFI / queue 研究是否需要先改数据层/audit schema”。

验收方案：
- 必须输出一份业务报告 `.workflow/reports/0512T008-business.md`，状态为 `待验收`。
- 必须输出 `local_live_analysis/t008_market_data_view_quality/T008_MARKET_DATA_VIEW_SUMMARY.md`。
- 必须输出至少以下机器可读结果：sample summary、audit schema coverage、overlay comparison、Stage 6J no-overlay view comparison、raw depth sequence quality、summary metrics。
- 必须覆盖主样本 `5-11-night-active`，并尽量覆盖 sanity 样本 `5-10-day-control-1h-06`、`5-9-noon`、`5-9-small`；缺失样本或字段必须说明。
- 必须明确回答：
  - `strategy_core` 是否直接看到 full depth。
  - live/backtest 是否都调用 `hbt.depth(0)`，以及为什么 shared API 不等于 shared view。
  - audit replay overlay 是否会掩盖 replay depth reconstruction 与 live book 的差异。
  - 现有 audit/raw 是否足够支持 microprice / OFI / queue。
  - 下一步是否应进入 T009 数据层/audit schema 修改任务。
- 必须验证没有改策略代码、没有新 replay、没有 live。
- 必须刷新 workflow dashboard。

action：
- 新建并运行只读 T008 runner。
- 写入 T008 数据质量 summary、CSV/JSON 输出和业务报告。
- 更新 `task_plan.md`、`progress.md`、`findings.md`，记录 T008 结论。

verify：
- `python3 .workflow/runners/run_0512T008.py`
- 检查 T008 输出目录和 required files 存在。
- `python3 .workflow/build_dashboard.py`
- `git status --short` 确认策略代码未被修改。

done：
- 回报必须给出 T008 核心结论和下一步建议。
- 回报必须说明 T008 不授权策略实现、new replay、default enable 或 live micro test。
- 回报必须明确是否建议进入 T009 数据层/audit schema 修改。
- 回报必须写 `commit：无`。
```
