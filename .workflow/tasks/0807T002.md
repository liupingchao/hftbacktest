# 任务派发

执行线程：
- 业务线程-python/research-postprocess

任务ID：
- 0807T002

标题：
- SKHYNIX-0807-BASIS-RESEARCH-POSTPROCESS

简短描述：
- 使用 `0807T001` 的 inventory-verified raw campaign，在 amdserver 的
  独立 working copy 上完成 common L2、R0、R1 和 point-in-time
  basis/dislocation 后处理。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- `0807T001` 已完成采集和三端 raw inventory 对账，等待 QA。
- `0805T003` 已通过，提供可执行 `basis-research` pipeline 和 Skill。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `AGENTS.md`
- `.agents/skills/cross-exchange-postprocess/SKILL.md`

是否需要提交代码：
- 不需要

提交要求：
- 当前工作区包含已验收但未提交的前置工作，不创建混合提交。
- 回报使用 `commit：无`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 检查 raw archive immutability、supervisor postprocess admission、
  source inventory lock、stage/runtime/artifact SHA、R0/R1 exact masks、
  reconnect epoch reset 和 basis strict-as-of 语义。

files：
- immutable local raw：
  `/Users/liu/Documents/hftbacktest/local_live_analysis/0807T001_skhynix_4h_continuous`
- immutable AMD raw：
  `/home/molly/project/hftbacktest/local_live_analysis/0807T001_skhynix_4h_continuous`
- AMD working campaign：
  `/home/molly/project/hftbacktest/local_live_analysis/0807T002_skhynix_4h_postprocessed_campaign`
- AMD pipeline output：
  `/home/molly/project/hftbacktest/local_live_analysis/0807T002_skhynix_basis_postprocess`

action：
- 从 AMD raw archive 创建独立 working copy。
- 第一次 supervisor `--postprocess-only` 按默认合同拒绝两次 Binance
  reconnect；保留该失败 working copy 和日志。
- 新增显式 `--allow-recovered-binance-reconnects` 门禁：必须闭合
  connection/subscription/embedded snapshot bridge、depth/bookTicker/trade
  恢复和时长上限证据。
- 第一轮独立 QA 发现 opt-in 下缺失或伪造为 0 的 Binance
  `reconnect_count` 可能绕过恢复证明。修复后强制 reconnect、attempt、
  subscription response、snapshot bridge、disconnect event 和 bootstrap
  result 数量闭合，并加入 hostile 回归。
- R0/R1 统一发布 Binance connection epoch/degraded sidecar；R1 label
  schema 升级为 `cross_exchange_frozen_decision_labels_v4`，对 source
  sidecar、exact reconnect masks 和 timeline epoch 做一致性验收。
- 在新 working copy 上运行 supervisor `--postprocess-only`，同时启用
  Binance 与 Hyperliquid bounded recovered core-L2 reconnect，构建
  connection-epoch-aware common L2 timeline。
- 使用 Skill 的 `inspect` admission 后运行 `basis-research` profile。
- 运行 `validate`、`report` 和一次 `resume`，确认有效 stage 全复用。
- 完整 2.5GB pipeline 产物保留在 amdserver；将 manifests、reports、
  quality CSV、runtime source 和 provenance compact evidence 同步回本机。

verify：
- 本机与 AMD 相关测试均为 `108 passed`。
- raw archive 运行前后保持 `39` 文件且逐项 inventory 不变；before/after
  inventory SHA256 均为
  `a30654f32cea375c49e4b26e39ed3c0de436e6d1f08de959601db749f262a003`。
- pipeline `status=complete`、`passes=true`、`source_immutable=true`。
- timeline 为 `501,220` 行；Binance 为 `3` epochs / `2` boundaries，
  Hyperliquid fast/standard 均为 `2` epochs / `1` boundary。
- R0/R1 和 basis stage 全部通过；future join、cross-epoch label、
  timestamp regression、venue epoch regression 和 old-state leak 均为 0。
- R1 v4 生成 `714,063` 个 decision labels，exact masks 与 exact horizon
  masks 均通过。
- pipeline validator `passes=true`、resume 复用全部 required stages；
  resume 后最终 manifest SHA256 为
  `6d5e7ec31a461569dec9392945263a4e7e1a4221d3a5f8ef1206691ebcffc4d0`。

done：
- 0807 数据形成可审计的 R0/R1 与 point-in-time basis/dislocation 数据集。
- basis 输出 `2,751,082` state rows，其中 `2,745,399` book eligible、
  `1,976,897` feature eligible。
- 输出不包含 lead-lag、maker diagnostics、exact fill、PnL 或 live
  promotion 结论。

hard boundary：
- 不修改 immutable raw archive。
- 不启动新采集，不访问 private/account/order/cancel endpoint。
- 不执行未实现的 `signal-research` 或 `full-research` profile。
