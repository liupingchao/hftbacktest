# 任务派发

执行线程：
- 业务线程-python/cross-exchange-research

任务ID：
- 0730T017

标题：
- HYPERLIQUID-LIQUIDITY-RESPONSE-EPISODE-BUILDER

简短描述：
- 在已通过 QA 的 R0/R1 数据边界上，构建 Binance 主动成交冲击触发的
  Hyperliquid liquidity-response motif family episode 数据集。
- 本轮只冻结事件、响应和结果特征，不做聚类、信号拟合或 maker 身份归因。

状态：
- 未通过

执行顺序：
- 当前唯一任务

前置任务：
- `0730T016` QA `已通过`。

规则更新提醒：
- 无

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `docs/motif-maker-pattern.md`
- `docs/skhynix_liquidity_response_motif.md`

是否需要提交代码：
- 不需要

提交要求：
- 不需要提交
- 回报写 `commit：无`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `examples/hyperliquid/cross_exchange_liquidity_response_episodes.py`
- `examples/hyperliquid/test_cross_exchange_liquidity_response_episodes.py`
- `docs/skhynix_liquidity_response_motif.md`
- `local_live_analysis/skhynix_liquidity_response_0730T017/`
- `.workflow/tasks/0730T017.md`
- `.workflow/reports/0730T017-business.md`
- `.workflow/reports/0730T017-qa.md`
- `docs/qa-acceptance-report.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

action：
- Fail closed 校验 R0 manifest、R1 alignment manifest、八段 identity、
  source/output SHA 和已接受 horizon。
- 以同主机 local receipt time 构造同方向、固定起点 `10ms` Binance
  aggressive-trade burst。
- 使用 burst 前严格 as-of 的同侧 Binance best queue 为分母；累计穿过该
  best price 的成交量达到 `30%` 时定义 `shock_ts`。
- 要求 `shock_ts` 后 `100ms` 内同侧 best queue 至少下降 `30%` 或原价档
  耗尽，并将该确认时刻定义为 `decision_ts`。
- 每个 `(side, decision_ts, pre-best price)` 只允许发布一个 primary
  episode；重复使用同一深度确认的候选保留在 audit。
- 冻结 trade-driven / mixed / cancel-driven attribution；只有
  `trade_driven` 且全部资格门禁通过的事件进入 primary episode。
- 保存 Hyperliquid BBO/fast-L2 的撤退、补充、跟随特征，以及
  `100/250/500ms` diagnostic 和 `1000/2000ms` primary response /
  adverse-markout 字段。
- 输出逐候选 trigger audit、逐 segment episode、segment summary 和
  manifest；所有输出使用临时目录验证后原子发布。

verify：
- `python -m pytest examples/hyperliquid/test_cross_exchange_liquidity_response_episodes.py`
- `python -m pytest examples/hyperliquid/test_cross_exchange_alignment_acceptance.py examples/hyperliquid/test_cross_exchange_liquidity_response_episodes.py`
- 在现有八段本地 SKHYNIX 数据上运行 builder。
- 独立复核输入 SHA、输出 SHA/行数、触发门槛、无 future join、
  无跨 segment response、primary horizon 限定和拒绝原因 reconciliation。

done：
- 交付可复现的 M1 episode 数据集及 manifest。
- 明确候选数、primary episode 数、方向/segment/attribution 分布和各
  horizon 覆盖。
- 结论只能命名为 `Hyperliquid liquidity-response motif family`；
  不声称识别具体 maker，不声称 exact fill、maker PnL 或生产可用信号。

hard boundary：
- 仅使用现有本地数据。
- 不访问 AWS、SSH 或网络。
- 不新增采集。
- 不做聚类、PCA、参数搜索、basis/lead-lag 拟合或策略回测。
- 不将 `100-500ms` diagnostic 字段用于主要信号结论。
