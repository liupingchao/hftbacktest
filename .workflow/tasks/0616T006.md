```md
执行线程：
- 业务线程-research

任务ID：
- 0616T006

标题：
- Hyperliquid tiny-live live-capable preflight / operator packet

简短描述：
- 基于 `0616T005` 已通过的 tiny-live protocol design，创建 Hyperliquid tiny-live 的 live-capable preflight / operator packet，使后续任务可以在明确审批后于 `awsserver1` 执行并把数据拉回本地验证。本任务只进入 live-capable 准备态，不真实下单、不撤单、不启动 live bot、不声明 PnL 或 maker viability。

状态：
- 待执行

执行顺序：
- 当前唯一任务

前置任务：
- `0616T005` QA 必须为 `已通过`。

规则更新提醒：
- 请先阅读 `.workflow/workflow-kit/workflow-manual.md`、`.workflow/workflow-kit/task-dispatch-template.md`、`.workflow/workflow-kit/thread-report-template.md`、`.workflow/workflow-kit/qa-acceptance-template.md`、`task_plan.md`、`progress.md`、`findings.md` 后再执行本任务。
- 本任务是 live-capable preflight / operator packet，不是 live execution。
- 真实订单任务必须由总控另行批准 symbol、max notional、max order size、max position、max loss、duration、host/machine、account scope 和 real orders allowed。

按线程规则执行：
- `AGENTS.md`
- `docs/cross_exchange_auto_loop_protocol.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0616T006.md`
- `.workflow/reports/0616T006-business.md`
- `docs/hyperliquid_tiny_live_live_capable_preflight_operator_packet.md`
- `examples/hyperliquid/**`
- `local_live_analysis/hyperliquid_tiny_live_live_capable_preflight_operator_packet_0616T006/**`
- `task_plan.md`
- `progress.md`
- `findings.md`

action：
- Consume `0616T002-T005` as prior accepted facts, especially the `0616T005` human approval gate.
- Define an operator packet for running a future Hyperliquid tiny-live window on `awsserver1` and pulling artifacts back to the local machine for validation.
- Define host preflight requirements for `awsserver1`: repository path, branch, Python/conda environment, clock/timezone, disk space, log path, network reachability, process ownership, and artifact archive/checksum policy.
- Define Binance lead-side public/read-only input requirements and Hyperliquid lag/maker venue readiness requirements.
- Define a live-capable configuration schema that keeps all approval fields explicit: symbol, max notional, max order size, max position, max loss, duration, host/machine, account scope, and real orders allowed.
- Keep unapproved fields as `pending_controller_approval`; do not invent caps or account values.
- Define local validation for pulled-back artifacts, including manifest, public market data, preflight result, run-intent marker, shutdown/cancel-all evidence placeholder, checksums, and boundary validation.
- If code is added, implement only local/offline validators, schema checks, command generators, or dry-run artifact builders. Generated commands must be inert by default and require explicit operator approval before any real private/order action.
- Preserve fail-closed behavior for missing approval fields, missing shutdown evidence, missing checksums, stale host metadata, or any artifact that claims real order placement without explicit approval.

acceptance criteria：
- The operator packet explicitly references `0616T002-T005` and treats `0616T005` as the current live approval gate.
- The packet names `awsserver1` as the intended host for future execution.
- The packet defines how artifacts are pulled back and validated locally.
- All required live approval fields remain explicit; unapproved fields remain `pending_controller_approval`.
- The task does not authorize or perform real order placement, cancellation, amendment, live bot startup, account query, credential disclosure, signing/nonce implementation, deployment, promotion, PnL proof, or maker viability proof.
- Final recommendation must be exactly one of:
  - `hyperliquid_tiny_live_live_capable_preflight_operator_packet_ready_for_qa`
  - `hyperliquid_tiny_live_live_capable_preflight_operator_packet_needs_revision`
  - `hyperliquid_tiny_live_live_capable_preflight_operator_packet_blocked`

verify：
- Use `/home/liushuai/workspace/hftbacktest/.conda-envs/hft-py38/bin/python`, not the default `python`.
- Run any focused tests added for the local validator / command generator.
- Run any new CLI with `--help`.
- Validate generated JSON artifacts with `/home/liushuai/workspace/hftbacktest/.conda-envs/hft-py38/bin/python -m json.tool <artifact>.json`.
- Check required CSV/markdown artifacts are non-empty.
- Boundary text check for no live/order authorization.
- `git diff --check`

done：
- 回报本任务是否已产生 live-capable operator packet。
- 回报 `awsserver1` 执行边界、数据拉回本地验证边界、审批字段状态、验证命令、生成 artifact 路径。
- 明确说明是否仍停在 `pending_controller_approval`，以及下一步是否需要总控批准真实 tiny-live 参数。
```
