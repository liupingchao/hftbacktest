# 0728T072 EC2-LATENCY-HUNT-CLOCK-CLEANUP-REPAIR

执行线程：
- 总控 / 业务执行线程

任务ID：
- 0728T072

标题：
- 修复时钟可信度和清理编排并重跑 EC2 latency hunt

简短描述：
- 修复 T071 QA 指出的完整 clock error bound、采集前后实时 Chrony
  证据、失败留痕和自动清理缺口。
- 使用同一候选矩阵重新执行 10 台 x86_64 `xlarge`、双 venue、每台
  900 秒采集；若候选不能满足冻结时钟门禁，显式记录并使用预定备用
  instance family 补足，不降低门限。
- Hyperliquid 使用策略 live 路径一致的 `l2Book fast=true`，不再使用
  稀疏 `bbo` channel。

状态：
- 未通过

执行顺序：
- 当前唯一正式任务

前置任务：
- `0728T071`（未通过）

是否需要提交代码：
- 需要实现并加入 Git 跟踪；本任务不要求创建 commit。

是否进行QA验收：
- 是

files：
- `latency-probe/ec2_hunt_remote.sh`
- `latency-probe/ec2_hunt.py`
- `latency-probe/ec2_hunt_orchestrator.py`
- `latency-probe/ec2_hunt_build.py`
- `latency-probe/ec2_hunt_user_data.sh`
- `latency-probe/test_ec2_hunt.py`
- `docs/ec2_latency_hunt_protocol.md`
- `docs/evidence/ec2_latency_hunt_20260728/`
- `.workflow/tasks/0728T072.md`
- `.workflow/reports/0728T072-business.md`
- `.workflow/reports/0728T072-qa.md`
- `docs/qa-acceptance-report.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

action：
- 将完整 clock error bound 定义为
  `abs(system_time) + 0.5 * root_delay + root_dispersion`，门限
  `750us`。该阈值在第三次尝试启动前冻结；前两次 `500us` 失败记录保留。
- 每个 accepted window 在采集前和采集后独立实时运行
  `chronyc tracking`，保存 UTC 采样时间、原始输出和解析值。
- analyzer 同时校验 before/after evidence、阈值和不同采样时间；失败
  host 不得进入 Binance feed ranking。
- 新增受测的 Git 跟踪编排器；所有实例、placement group、bucket 和失败
  事件在创建后立即原子写入 state file。
- cleanup 同时使用 state file 与 task/run tags 发现资源；中断或失败后
  可独立重入。
- 重新执行 2 台 canary 和总计 10 台 900 秒正式采集。

verify：
- Python 单测覆盖 clock bound、前后证据和 cleanup plan 故障路径。
- `bash -n latency-probe/ec2_hunt_remote.sh`。
- Rust fmt/test/clippy。
- canary `2/2` 时钟前后门禁、双 venue 和重建通过。
- full run `10/10`，每台 before/after error bound 均不高于 `750us`。
- raw-to-summary 远端和本机独立重建均为 `20/20`。
- AWS task/run tag 和 state file 均证明零遗留资源。
- `git diff --check`。

done：
- 可信的 Binance feed 和两家 local pipeline 排名可复核。
- 推荐实例不依赖未通过时钟界的样本。
- launch、failure journal 和 cleanup 可由仓库代码复现。

blockers：
- Hyperliquid `feed_network` 继续降级，不进入实例推荐。
- 本任务仍不证明订单 ACK/fill/PnL。
