```md
执行线程：
- 业务线程-research

任务ID：
- 0604T009

标题：
- Canonical signal + horizon/regime synthesis for candidate regime definitions

简短描述：
- Milestone 2 read-only synthesis：综合 `0604T006/T008` canonical signal quality ranking 与 `0604T007` canonical horizon / regime diagnostics，使用 `0604T003` canonical event-mode evidence 定义 decision-time-visible candidate regimes，或明确当前证据只足够保留 watch/reject list。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- `0604T005` QA 已通过。
- `0604T007` QA 已通过。
- `0604T008` QA 已通过，已闭环 `0604T006` report bucket consistency 缺陷。

规则更新提醒：
- 请先阅读规则文档后再执行本任务。
- 本任务只做 read-only regime synthesis / candidate definition；不做 maker action、不做 case-library、不做 shadow decisions、不实现 strategy、不接 private/order、不做参数搜索、不 live/default-on/tiny-live、不 promotion。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `docs/thread-playbook.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交任务文件、业务报告、read-only runner/test 修改、task-scoped artifacts 和必要 workflow tracking 更新。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0604T009.md`
- `.workflow/reports/0604T009-business.md`
- `examples/hyperliquid/canonical_regime_synthesis.py`
- `examples/hyperliquid/test_canonical_regime_synthesis.py`
- `local_live_analysis/canonical_regime_synthesis_0604T009/**`
- 必要 workflow tracking 文件仅限本任务状态/结论更新。

read-only inputs：
- Formal canonical source:
  - `local_live_analysis/event_mode_canonical_pricing_signal_0604T003/**`
- Source-lock / loader foundation:
  - `examples/hyperliquid/canonical_event_mode_evidence.py`
  - `local_live_analysis/canonical_event_mode_evidence_0604T004/**`
  - `local_live_analysis/canonical_evidence_source_lock_0604T005/**`
- Signal ranking:
  - `local_live_analysis/canonical_signal_quality_ranking_0604T006/**`
- Horizon / regime diagnostics:
  - `local_live_analysis/canonical_horizon_regime_diagnostics_0604T007/**`
- T008 QA / refreshed T006 report consistency evidence:
  - `.workflow/reports/0604T008-qa.md`
- Explicit decision-time-visible feature scope / allowlist contract:
  - `local_live_analysis/binance_led_hyperliquid_data_contract_0601T004/feature_decision_table.csv`

forbidden：
- New data collection.
- Maker side selection, quote behavior, order behavior, case-library construction, or shadow decision generation.
- Hyperliquid or Binance private/account/order endpoints.
- Order lifecycle, submit/cancel/fill logic.
- Strategy implementation, production config/default changes, parameter search, default-on, tiny-live, promotion.
- Connector/core API changes, standard npz schema changes, canonical Binance maker audit schema changes.
- Treating `100/250ms` evidence as canonical independent support.
- Using non-decision-time-visible fields as regime conditions.
- Using fields outside the explicit decision-time-visible allowlist / existing canonical context fields selected in this task.
- Using any `watch_regime_dependent` feature as the sole or primary anchor of a promoted candidate regime.

action：
- Implement a read-only synthesis runner that consumes only canonical event-mode evidence through the accepted loader/guard path.
- Combine T006/T008 signal ranking with T007 horizon/regime diagnostics to form decision-time-visible candidate regime definitions.
- Lock the synthesis to a strict explicit allowlist:
  - Primary anchor feature eligible for candidate regime definitions:
    - `binance_mid_move_ticks_from_prev`
  - Secondary filter / context-only features:
    - `binance_top5_imbalance`
    - `binance_top5_bid_qty`
    - `binance_microprice_minus_mid_ticks`
  - Allowed Hyperliquid / venue-state context must be decision-time-visible fields already present in canonical artifacts, limited to explicit context fields such as join-age bucket, spread bucket, and context quality buckets used by accepted canonical diagnostics.
- `watch_regime_dependent` features may appear only as secondary filters or descriptive context layered on top of an allowed primary anchor; they must not by themselves define a promoted candidate regime.
- For every candidate bucket, compute:
  - row count
  - sample count
  - per-sample future-move direction
  - direction consistency ratio
  - mean future move ticks
  - effect / mean absolute correlation context
  - sample concentration ratio
  - effective future-row-delta support
  - horizon support class, prioritizing `1000ms+`, allowing `500ms+` diagnostic support, and treating `100/250ms` as watch-only.
- Generate task-scoped outputs:
  - `candidate_regime_definitions.csv`
  - `candidate_regime_evidence_summary.csv`
  - `candidate_regime_watch_reject_list.csv`
  - `canonical_regime_synthesis_manifest.json`
  - `canonical_regime_synthesis_report.md`
- Use explicit output classifications:
  - `candidate_for_milestone3_executability`
  - `watch_needs_more_samples`
  - `reject_insufficient_support`
  - `reject_unstable_direction`
  - `reject_concentrated_or_aliased`
  - `reject_not_decision_time_visible`

acceptance criteria：
- Runner refuses non-canonical or diagnostic-only synthetic inputs through the accepted loader/guard path.
- Candidate regime definitions use only decision-time-visible fields within the explicit allowlist / accepted canonical context scope.
- Any row classified `candidate_for_milestone3_executability` must be anchored by `binance_mid_move_ticks_from_prev`; `watch_regime_dependent` features may only appear as secondary filters/context.
- No candidate may rely primarily on `100/250ms` evidence.
- Every candidate or watch/reject row includes row count, sample count, direction consistency, sample concentration, horizon support, and reason.
- If no regime meets `candidate_for_milestone3_executability`, the report must clearly state that result and keep outputs as watch/reject only.
- Output must not specify maker side, quote placement, order behavior, strategy action, default-on, tiny-live, or promotion.
- Tests cover decision-time-visible field filtering, short-horizon rejection/watch behavior, sample concentration rejection, minimum support classification, and non-canonical input refusal.

verify：
- `python examples/hyperliquid/canonical_regime_synthesis.py --help`
- `python -m py_compile examples/hyperliquid/canonical_regime_synthesis.py examples/hyperliquid/test_canonical_regime_synthesis.py`
- `python -m pytest examples/hyperliquid/test_canonical_regime_synthesis.py`
- `python examples/hyperliquid/canonical_regime_synthesis.py --input-dir local_live_analysis/event_mode_canonical_pricing_signal_0604T003 --signal-ranking-dir local_live_analysis/canonical_signal_quality_ranking_0604T006 --horizon-regime-dir local_live_analysis/canonical_horizon_regime_diagnostics_0604T007 --output-dir local_live_analysis/canonical_regime_synthesis_0604T009`
- `python -m json.tool local_live_analysis/canonical_regime_synthesis_0604T009/canonical_regime_synthesis_manifest.json`
- Scripted or manual check that candidate definitions use only allowed decision-time-visible fields, that only `binance_mid_move_ticks_from_prev` can anchor a promoted candidate, and that no `100/250ms`-only candidate is promoted.
- `git diff --check`

done：
- Business report must include synthesis method, candidate/watch/reject counts, top candidate or watch rows with reasons, whether any regime is eligible only for Milestone 3 executability assessment, artifact paths, verification commands, blockers/caveats if any, commit id, and commit message.
- Business report must explicitly state this is only read-only regime synthesis and does not authorize maker action, case-library construction, strategy implementation, private/order endpoints, live/default-on/tiny-live, parameter search, or promotion.
- Business report should end in `待验收` if verification passes.
```
