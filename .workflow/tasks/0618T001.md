```md
执行线程：
- 业务线程-research

任务ID：
- 0618T001

标题：
- Hyperliquid tiny-live minimal real-order executor and final gate repair

简短描述：
- 基于 `0528T003` / `0529T003` / `0530T001` 记录的 Hyperliquid 官方资料和 `0617T007` no-go blockers，实现一个最小 live-capable Hyperliquid tiny-live real-order executor，并重新运行 final go/no-go gate。
- 本任务是执行器与 gate 修复任务，不是 `0617T008` live execution；除非后续单独创建并 QA 通过 `0617T008`，本任务不得实际下单。

状态：
- 执行中

执行顺序：
- 当前唯一任务

前置任务：
- `0617T007` QA 为 `已通过`，但 gate decision 为 `allow_create_0617T008=false`。
- `awsserver1:/home/admin/hftbacktest-cross-exchange` 已同步到当前 `cross-exchange` 分支进度。

规则更新提醒：
- 请先阅读 `.workflow/workflow-kit/workflow-manual.md`、`.workflow/workflow-kit/task-dispatch-template.md`、`.workflow/workflow-kit/thread-report-template.md`、`.workflow/workflow-kit/qa-acceptance-template.md`、`task_plan.md`、`progress.md`、`findings.md` 后再执行本任务。
- 请先回收 `0528T003`、`0529T003`、`0529T004`、`0530T001`、`0531T001`、`0617T003`、`0617T004`、`0617T005`、`0617T006`、`0617T007` 的已验收材料。
- 本任务允许实现 live-capable order code path，但不得在本任务内执行真实 live order placement；真实 10 分钟 tiny-live 只能在后续单独 `0617T008` 或总控新命名 live task 中执行。

按线程规则执行：
- `AGENTS.md`
- `docs/hyperliquid_live_replay_alignment_design.md`
- `docs/hyperliquid_public_market_data_research_consumer_design.md`
- `docs/hyperliquid_tiny_live_0616T006_T008_auto_loop.md`
- `docs/hyperliquid_tiny_live_final_go_no_go_gate.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0618T001.md`
- `.workflow/reports/0618T001-business.md`
- `docs/hyperliquid_tiny_live_real_order_executor.md`
- `examples/hyperliquid/hyperliquid_tiny_live_real_order_executor.py`
- `examples/hyperliquid/test_hyperliquid_tiny_live_real_order_executor.py`
- `examples/hyperliquid/hyperliquid_tiny_live_final_go_no_go_gate.py`
- `examples/hyperliquid/test_hyperliquid_tiny_live_final_go_no_go_gate.py`
- `docs/hyperliquid_tiny_live_final_go_no_go_gate.md`
- `local_live_analysis/hyperliquid_tiny_live_real_order_executor_0618T001/**`
- `local_live_analysis/hyperliquid_tiny_live_final_go_no_go_gate_0618T001/**`
- `task_plan.md`
- `progress.md`
- `findings.md`

official references to recheck：
- Hyperliquid API overview: `https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api`
- Exchange endpoint: `https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/exchange-endpoint`
- Info endpoint: `https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint`
- WebSocket endpoint: `https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket`
- WebSocket subscriptions: `https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket/subscriptions`
- Tick and lot size: `https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/tick-and-lot-size`
- Signing: `https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/signing`
- Nonces and API wallets: `https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/nonces-and-api-wallets`
- Rate limits and user limits: `https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/rate-limits-and-user-limits`
- Official Python SDK: `https://github.com/hyperliquid-dex/hyperliquid-python-sdk`

action：
- Recheck the current official Hyperliquid docs and official Python SDK references before relying on order, cancel, `Alo` post-only, open orders, order status, user fills, signing, nonce, tick/lot, and rate-limit behavior.
- Implement a minimal Hyperliquid tiny-live real-order executor with a default safe mode and an explicit live mode gate.
- Use the official `hyperliquid-python-sdk` when available for signing/order/cancel interactions; if the SDK or required dependency is unavailable on local or `awsserver1`, fail closed and record the blocker instead of hand-rolling an unverified signing path.
- Implement strict immutable caps:
  - `symbol=BTC`
  - `duration<=10 minutes`
  - `max_order_size=0.01 BTC`
  - `max_order_notional=700 USDC`
  - `max_position=0.04 BTC`
  - `max_position_notional=2800 USDC`
  - `max_notional=3000 USDC`
  - `max_loss=30 USDC`
  - maker-only / post-only only via Hyperliquid `Alo` limit orders
- Implement hard fail-closed config validation:
  - reject any non-`BTC` symbol
  - reject any order size over `0.01 BTC`
  - reject any order or position notional over caps
  - reject any duration over `600` seconds
  - reject any non-`Alo` time-in-force
  - reject market orders, IOC, GTC, reduceOnly behavior not explicitly justified, or any fallback to taker behavior
  - reject missing tick/lot precision facts
  - reject missing max-loss evaluator
  - reject missing artifact directory
- Implement max-loss cap logic:
  - before order placement, require a mark/mid source and a deterministic loss estimate policy
  - during live mode, stop placing new orders and enter shutdown if estimated loss reaches or exceeds `30 USDC`
  - if loss cannot be computed, fail closed into shutdown
- Implement cancel-all shutdown:
  - cancel tracked open orders by oid/cloid
  - query open orders after cancel where authorized
  - optionally use Hyperliquid scheduled cancel / dead-man switch only if official docs and SDK behavior are rechecked and artifacts record the configured time
  - produce shutdown evidence that distinguishes submitted, resting, canceled, rejected, filled, unknown, and final-open states
  - fail closed if final open-order proof is missing or ambiguous
- Implement artifacts:
  - run intent marker
  - approved config snapshot
  - environment/dependency snapshot
  - official doc recheck summary
  - precision/tick/lot snapshot
  - preflight validation summary
  - order-intent audit
  - private order response audit with redacted account/order identifiers
  - cancel/shutdown proof
  - max-loss monitor summary
  - final safety summary
  - sha256 manifest
- Implement `awsserver1` operator path:
  - remote path `/home/admin/hftbacktest-cross-exchange`
  - Python `/usr/bin/python3` unless a better recorded interpreter is selected
  - remote artifact directory under `/home/admin/hftbacktest_live_artifacts/0618T001_real_order_executor/`
  - pullback to `local_live_analysis/hyperliquid_tiny_live_real_order_executor_0618T001/` using `scp` or another explicitly recorded method
- Update `hyperliquid_tiny_live_final_go_no_go_gate.py` so the final gate can evaluate the `0618T001` executor evidence rather than hard-coded placeholder failures.
- Rerun final go/no-go gate after the executor implementation and remote/pullback evidence are produced, writing `0618T001`-scoped gate artifacts under `local_live_analysis/hyperliquid_tiny_live_final_go_no_go_gate_0618T001/`.
- Do not create `.workflow/tasks/0617T008.md` in this task.
- Do not run a 10-minute live test in this task.

acceptance criteria：
- The executor has focused tests proving cap validation, post-only `Alo` enforcement, max-loss fail-closed behavior, cancel-all shutdown flow, artifact writing, and no accidental live mode.
- The executor exposes a safe/dry-run or mock-client path for tests and QA that does not call Hyperliquid private/order endpoints.
- Any live-capable mode requires explicit flags and refuses to run without all caps, artifact path, official-doc/SDK compatibility, and operator acknowledgement.
- No credential values are printed, written to artifacts, committed, or exposed in logs.
- Artifact redaction is tested.
- Remote `awsserver1` preflight/pullback evidence is produced without placing orders.
- The updated final gate records current local and remote commit, remote clean status, executor readiness evidence, approved caps, boundary facts, and whether `0617T008` may be created.
- If any required real-order precondition remains missing, final gate must return `allow_create_0617T008=false` with concrete blockers.
- If final gate returns `allow_create_0617T008=true`, that only authorizes total control to create a later separate live task; it does not execute the live window inside `0618T001`.

forbidden：
- Do not place real orders in `0618T001`.
- Do not cancel real existing orders except inside an explicitly approved shutdown-only safety command that total control authorizes separately.
- Do not query account/private endpoints outside the executor's explicitly gated preflight/live-capable path.
- Do not disclose or commit credentials, private keys, API wallet secrets, account addresses if they are sensitive, raw signatures, or nonces tied to secrets.
- Do not implement strategy default-on behavior.
- Do not relax `10min / 0.01 BTC / post-only / max loss cap`.
- Do not promote, deploy, scale, or claim maker viability/PnL.

verify：
- `python -m pytest examples/hyperliquid/test_hyperliquid_tiny_live_real_order_executor.py -q`
- `python -m pytest examples/hyperliquid/test_hyperliquid_tiny_live_final_go_no_go_gate.py -q`
- `python examples/hyperliquid/hyperliquid_tiny_live_real_order_executor.py --help`
- `python examples/hyperliquid/hyperliquid_tiny_live_real_order_executor.py --self-test --output-dir local_live_analysis/hyperliquid_tiny_live_real_order_executor_0618T001`
- `python examples/hyperliquid/hyperliquid_tiny_live_final_go_no_go_gate.py --output-dir local_live_analysis/hyperliquid_tiny_live_final_go_no_go_gate_0618T001 --remote-facts local_live_analysis/hyperliquid_tiny_live_final_go_no_go_gate_0618T001/remote_state_input.json`
- `python -m json.tool local_live_analysis/hyperliquid_tiny_live_real_order_executor_0618T001/executor_manifest.json`
- `python -m json.tool local_live_analysis/hyperliquid_tiny_live_final_go_no_go_gate_0618T001/final_go_no_go_manifest.json`
- Required CSV / markdown artifacts are non-empty.
- Boundary text check for no real order placement in this task.
- Credential redaction check.
- `git diff --check`

done：
- 回报 executor readiness recommendation、final gate recommendation、`allow_create_0617T008`、remaining blockers if any、remote state、cap matrix、shutdown evidence path、artifact pullback path、验证命令、commit id、提交信息。
- 明确说明本任务是否实际调用任何 private/order endpoint；默认应为没有。
- 明确说明本任务没有创建或执行 `0617T008`。
```
