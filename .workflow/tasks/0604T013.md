```md
执行线程：
- 业务线程-python

任务ID：
- 0604T013

标题：
- Fix live shutdown cancel acknowledgement wait

简短描述：
- 修复 `live_tick_mm.py` shutdown cancel 不等待 acknowledgement 的问题。必须复用 `0604T012` 的 shutdown cancel case matrix 做 focused regression tests，证明 buy/sell/extra 正常 case 会 bounded wait cancel ack，不可撤订单不误发 cancel，cancel 异常不阻止剩余订单继续尝试，且 `close()` 不再被当作唯一 ack drain 机制。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0604T012` QA 已通过，诊断报告路径：`.workflow/reports/0604T012-business.md`。

规则更新提醒：
- 请先阅读规则文档后再执行本任务。
- 本任务只修复 `examples/binance_tick_mm/live_tick_mm.py` shutdown cancel/close 语义和 focused tests；不得扩大到 connector、Rust live bot `close()`、生产配置、live 启动、参数搜索或 promotion。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交任务文件、业务报告和本任务代码/测试改动。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0604T013.md`
- `.workflow/reports/0604T013-business.md`
- `examples/binance_tick_mm/live_tick_mm.py`
- `examples/binance_tick_mm/test_backtest_tick_mm.py` 或更合适的 focused live shutdown test 文件。
- 必要 workflow tracking 文件仅限本任务状态/结论更新。

forbidden：
- 不修改 connector、py binding、Rust live bot、Rust `close()`、生产配置或默认 live 参数。
- 不启动 live / tiny-live / default-on。
- 不做参数搜索、回放实验扩展、promotion。
- 不修改策略 quote placement / submit / normal-loop cancel 语义，除非为测试抽取纯 shutdown helper 必需。
- 不把 REST final open-orders check 做成 live 行为，除非任务执行中明确证明现有接口已可安全本地测试；默认只做 bounded cancel ack wait 和 focused tests。
- 不静默扩大到其他 shutdown 风险项。

action：
- 在 `live_tick_mm.py` 中把 shutdown cancel 逻辑抽取为可测试 helper，或用同等方式实现 focused testable shutdown cancel flow。
- 修复 shutdown cancel：
  - buy/sell primary orders：仅当 `cancellable=true` 时尝试 cancel。
  - extra orders：遍历完整 `ExtraOrder` 对象，必须检查 `extra.cancellable` 且 `extra.req != "cancel"` 后才尝试 cancel。
  - 对每个需要 cancel 的 order 使用 bounded ack wait：
    - 可用 `hbt.cancel(0, order_id, True)`；或
    - `hbt.cancel(0, order_id, False)` 后显式 `hbt.wait_order_response(0, order_id, timeout_ns)`。
  - timeout 必须有明确常量/参数，不允许无限等待。
  - 单个 order cancel 或 wait 失败/异常，不得阻止后续订单继续尝试 shutdown cancel。
  - shutdown 应记录每个 order 的 cancel/wait 结果，至少用于 log 和 tests。
  - `hbt.close()` 仍可调用，但不得作为唯一 cancel ack drain 机制。
- 保持 normal trading loop 的 cancel path 不变，除非后续单独任务要求。
- 基于 `0604T012` case matrix 补 focused regression tests，至少覆盖：
  - `buy_only_cancellable`
  - `sell_only_cancellable`
  - `buy_sell_cancellable`
  - `extra_buy_exists`
  - `mixed_buy_sell_extras`
  - `primary_not_cancellable`
  - `extra_not_cancellable_still_cancelled` 的修复后行为：不可撤 extra 不应被 cancel。
  - `cancel_false_success_no_wait` 的修复后行为：应等待 ack。
  - `cancel_false_exception_still_position_close` 的修复后行为：某单异常后仍继续尝试剩余订单。
  - `close_after_pending_cancel_no_wait_api` 的修复后行为：close 前已经对 pending cancel 做 bounded wait。

acceptance criteria：
- `0604T012` 中所有正常已发 cancel 的 shutdown case 都不再是 fire-and-forget；测试必须证明 `wait=True` 或显式 `wait_order_response(...)` 被调用。
- 不可撤 primary/extra orders 不误发 cancel。
- 已 cancel-pending extra（`req == "cancel"`）不重复发 cancel。
- 单个 cancel/wait 异常不会阻止剩余 orders 继续尝试 shutdown cancel。
- `close()` 仍被调用，但测试必须证明 ack wait 发生在 close 之前。
- 不修改 connector、Rust live bot、生产配置、audit schema、normal loop cancel 语义。
- 业务报告必须明确说明是否未做 REST final open-orders check；如未做，必须把它保留为后续可选任务，而非本任务缺陷。

verify：
- `python -m pytest examples/binance_tick_mm/test_backtest_tick_mm.py -k "shutdown or cancel"`
- `python -m pytest examples/binance_tick_mm/test_backtest_tick_mm.py -k "live_shutdown or shutdown_cancel"`
- `python -m pytest examples/binance_tick_mm/test_*.py`
- `git diff --check`
- 如 `python -m pytest examples/binance_tick_mm` 因已知 `run_env_test.py` / numba cache collection 问题失败，业务报告必须记录具体错误，并以 `test_*.py` 回归作为本任务可执行 package-level verification。

done：
- 业务报告写入 `.workflow/reports/0604T013-business.md`。
- 回报必须说明改动点、`0604T012` case matrix 的实际修复后验证结果、focused pytest/package pytest 结果、是否有未覆盖边界、commit id 和 commit message。
- 业务报告应以 `待验收` 收口并进入 QA。
```
