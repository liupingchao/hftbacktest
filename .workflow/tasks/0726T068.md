# 0726T068 SEEDED-DYNAMIC-LIVE-FILL-EVIDENCE

执行线程：
- 总控 / 业务执行线程

任务ID：
- 0726T068

标题：
- Collect bounded live fill evidence with the exact seeded dynamic profile

简短描述：
- 在 `0722T067` 已验收的 exact seed wiring 和 strict submit gate 上，执行三次
  相互独立的 1800 秒 Hyperliquid BTC maker-only live window。
- 本任务先完成 no-order preflight；真实下单阶段必须取得新的 exact live
  authorization，旧的 2026-07-16 授权不得复用。

状态：
- 执行中

执行顺序：
- 当前唯一任务

前置任务：
- `0722T067`（已通过）

规则更新提醒：
- No-order preflight 可执行；任何 private/account/order/cancel/live action
  必须等待 fresh exact authorization。
- 每个正式窗口必须使用独立的 exact-envelope orchestrator invocation：
  `windows=1`，不得用一个三窗口 invocation 绕过 single-window gate。
- `xemm.service` 必须保持 inactive；不得并发运行其他 live strategy。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`

是否需要提交代码：
- 视 preflight/运行发现决定；正式 evidence、业务报告和 QA 必须提交。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

proposed live envelope：
- Host:
  `awsserver1 / i-02c64c088f311cbc1 / ap-northeast-1`.
- Source:
  exact `cross-exchange/a0bc92898ecea43cbdc4219efc1acbcd69580969`.
- Venue/symbol:
  Hyperliquid `BTC` only.
- Profile:
  `two-sided-seeded-dynamic-manager`.
- Seed contract:
  `local_live_analysis/public_multi_distance_dynamic_seed_0722T066/dynamic_spread_seed_contract.json`.
- Seed exposures:
  `local_live_analysis/public_multi_distance_dynamic_seed_0722T066/quote_exposure_intervals.csv`.
- Expected seed SHA-256:
  `e35c7fd8f3ec8268e5d50c7963889b73409470c9f01f92ca3a0d598a96562be9`.
- Execution:
  post-only `Alo`, real submit/cancel allowed only after authorization.
- Windows:
  three sequential independent windows, each `1800s`.
- Artifact identity:
  three separate `windows=1` invocations with `window_id_offset=0/1/2`,
  producing global artifact window IDs and run IDs `01/02/03`.
- Per-window caps:
  max order size `0.005 BTC`, max submissions `2`, max position
  `0.01 BTC`, max loss `1 USDC`, quote hold `3s`, private wait `10s`.
- Control plane:
  SSM-first detached remote orchestrator; SSH/SCP are optional recovery paths.

action：
- Phase 0, no-order:
  validate exact local preflight command, source/seed availability, instance/
  SSM health, disk/clock/service/process state and no concurrent live job.
- After fresh authorization:
  materialize the exact source on awsserver1, run an independent no-order
  preflight for each window, then execute the three windows sequentially.
- Before every submit require:
  exact seed loaded, current candidate `pass`, bounded, no fallback, final
  quote behavior changed, post-only invariant, clear kill switch and exact
  account evidence chain.
- Pull artifacts back with SSM-first transport and verify SHA-256.
- Derive role-known fills, resting lifecycles, dynamic quote behavior,
  fees/rebates and markout/PnL from source artifacts.

verify：
- Preflight command renders all exact seed and strict flags and passes without
  reading credentials or starting watcher.
- Instance `running`, EC2 checks pass, SSM connected, clock/disk healthy,
  `xemm.service=inactive`, no collector/watcher/orchestrator process and no
  live lock holder before authorization.
- Every window has exact source/config/account identity, heartbeat/status,
  terminal open-orders proof and checksum verification.
- Any submitted quote has strict seeded gate `pass` and
  `actual_quote_behavior_changed=true`.
- Any fill has source/oid/cloid/account linkage and role classification
  `confirmed_maker` or `confirmed_taker`; unknown role cannot support economics.
- No-fill windows remain valid operational evidence but do not pass fill or
  economics acceptance.

done：
- T068 may pass only with independently accepted live seeded-dynamic mechanism
  evidence and role-known fill evidence.
- Fill-feedback activation remains a later formal task after T068 lifecycle
  eligibility is derived.

blockers：
- Fresh exact live authorization is mandatory before private/account/order/
  cancel/live work.
- Wrong source/hash/account, candidate fallback, no final quote change,
  pre-existing orders, active strategy/service, failed SSM/infra gate or
  incomplete terminal proof blocks a window.

Phase 0 执行结果：
- Initial exact source:
  `8736efb36a0866f2c62c51e4afe4fb590c9ac72b`; superseded before live because
  three single-window invocations reused artifact/run identity `window_01`.
- Identity repair source:
  `a0bc92898ecea43cbdc4219efc1acbcd69580969`.
- Repair verification:
  focused `42 passed`; full Hyperliquid `1309 passed, 2 skipped`.
- EC2/SSM:
  `running / system ok / instance ok / Online`.
- Remote:
  Debian 13, NTP synchronized, `xemm.service=inactive`, no live process and no
  lock holder.
- Resource snapshot:
  root disk `75%`, `/tmp` `68%`, available memory about `1.8 GiB`, no swap.
- Isolated source:
  `/home/admin/hftbacktest-cross-exchange-0726T068R1`, owner `admin:admin`.
- Linux admin preflight:
  three exact seeded profile invocations `pass`, no credential/private/
  account/order/cancel/watcher action.
- Artifact identities:
  `0726T068:window_01`, `0726T068:window_02`,
  `0726T068:window_03`.
- Preflight SHA-256:
  `f43e1170de717a21c8fc68ae2309c55b92639626023de2b2516982f943b8bd96`,
  `a85672dd7ea3ad4f21c3117da2f0356af22edbece2d80363663bb129f2a9d9c1`,
  `36b678480f3f7edf01d5cdc7ccafcfa7526b03cbd465c1bae9e194be458d0b89`.
- Final no-order assertion:
  `2026-07-26T07:56:15Z`, `xemm.service=inactive`, no live Python process,
  no lock holder and zero credential/private/order/cancel action.
- Current blocker:
  cleared by fresh exact authorization for an earliest start of
  `2026-07-26T09:00:00Z`.

Fresh live authorization：
- Exact source:
  `a0bc92898ecea43cbdc4219efc1acbcd69580969`.
- Allows:
  reading `/home/admin/XEMM_rust_latest/.env`, private account/open-orders
  queries and real post-only `Alo` submit/cancel.
- Requires:
  order-submit account equals fill-pullback account equals post-state account;
  raw credentials must not be pulled locally.
