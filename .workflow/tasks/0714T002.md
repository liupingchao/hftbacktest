# 任务派发

执行线程：
- 业务线程-python/offline-repair

任务ID：
- 0714T002

标题：
- T011-RESTING-INTERVAL-CAPTURE-CONTRACT-REPAIR

简短描述：
- 基于 `0714T001` QA 已通过的 `route_to_resting_interval_capture_contract_repair`，修复 controlled watcher 的 resting-interval capture artifact contract。
- 本任务目标是让未来 controlled live evidence 能区分 complete interval zero rows 与 missing capture；不是 live retry、不是提高成交率、不是 quote policy design。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- 0714T001 QA 已通过

规则更新提醒：
- 请先阅读规则文档后再执行本任务。

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `.workflow/workflow-kit/task-dispatch-template.md`
- `.workflow/workflow-kit/thread-report-template.md`
- `.workflow/workflow-kit/qa-acceptance-template.md`
- `docs/cross_exchange_resting_interval_public_flow_auto_loop_plan.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交。
- 回报必须带 `commit id` 和 `提交信息`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `.workflow/tasks/0714T002.md`
- `.workflow/reports/0714T002-business.md`
- `.workflow/reports/0714T002-qa.md`
- `docs/qa-acceptance-report.md`
- `docs/cross_exchange_resting_interval_public_flow_auto_loop_plan.md`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- `examples/hyperliquid/test_hyperliquid_tiny_live_m2_event_driven_watcher.py`
- `local_live_analysis/cross_exchange_resting_interval_capture_contract_repair_0714T002/`

accepted input：
- `0714T001` QA:
  - `.workflow/reports/0714T001-qa.md`
  - `docs/qa-acceptance-report.md`
- Repair/design artifact:
  - `local_live_analysis/cross_exchange_public_flow_interval_artifact_repair_design_0714T001/`

action：
- Offline/mock implementation only.
- Extend the existing resting-interval capture artifact writer so future controlled live artifacts include the v2 contract fields:
  - stable `attempt_key`
  - lifecycle interval source/status fields
  - attempt-keyed public trades with quote relation flags
  - resting-start L2/depth source/status fields
  - interval public stream coverage artifact
  - explicit zero-row interpretation that distinguishes complete-coverage zero trades from missing capture
- Preserve existing artifact filenames and backward-compatible fields where practical.
- Generate a local mock validation package under:
  - `local_live_analysis/cross_exchange_resting_interval_capture_contract_repair_0714T002/`

hard boundary：
- No live-submit.
- No live retry.
- No remote/AWS execution.
- No credential reads.
- No private/account/order/cancel endpoint calls.
- No new market-data collection.
- No threshold change.
- No quote-envelope change.
- No order-size or max-submission change.
- No strategy behavior change.
- No quote policy design.
- No fill-probability, queue-priority, fee/rebate, realized-PnL, maker-viability, T012, promotion, or final MVP claim.

verify：
- Focused pytest for resting-interval capture writer.
- `python -m py_compile` for changed runner/test.
- Runner CLI help.
- Generate the mock repair package.
- JSON/CSV parse and schema checks for generated artifacts.
- Check that `complete_interval_coverage_zero_trades` and `artifact_gap_not_no_exchange_trades` are represented separately.
- Boundary manifest confirms offline-only and no endpoint/live/parameter changes.
- `git diff --check`.

done：
- Report changed schema fields and generated artifact paths.
- Report verification results.
- Report final route / next recommendation.
- Confirm no threshold/quote-envelope/live retry behavior occurred.
