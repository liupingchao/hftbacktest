```md
执行线程：
- 业务线程-python

任务ID：
- 0515T003

标题：
- Replay lifecycle repair implementation

简短描述：
- 基于 `0515T002` 已通过的 repair design contract，实施 narrow replay lifecycle repair，优先收窄 cancel-requested fill eligibility、cancel-ack / canceled terminal-state semantics，以及 step-back placement 的 long-horizon persistence bias。任务只改 replay-side lifecycle / fill behavior，不改策略，不补样本，不启动 live。

状态：
- 待执行

执行顺序：
- 当前唯一任务

前置任务：
- `0515T002` QA 已通过

规则更新提醒：
- 本任务允许改 replay-side lifecycle / fill implementation，但不允许改策略行为、fair/reservation、quote placement、live scripts、AWS 状态或 sample collection。
- 本任务必须保持 scope 收窄在 replay lifecycle repair，不得把它扩展成 broader strategy redesign。

按线程规则执行：
- .workflow/workflow-kit/thread-report-template.md
- docs/thread-playbook.md
- task_plan.md
- progress.md
- findings.md

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

Allowed files：
- `.workflow/tasks/0515T003.md`
- `.workflow/reports/0515T003-business.md`
- `examples/binance_tick_mm/backtest_tick_mm.py`
- `examples/binance_tick_mm/strategy_core.py`
- `examples/binance_tick_mm/test_backtest_tick_mm.py`
- `examples/binance_tick_mm/test_replay_lifecycle_mismatch_diagnosis.py`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `.workflow/dashboard.html`
- `.workflow/dispatch_suggestions.md`
- `docs/qa-acceptance-report.md`

Allowed generated outputs：
- `local_live_analysis/5-13-day-control-30min/stage6d_replay_lifecycle_repair_0515T003/**`

Allowed read-only inputs：
- `.workflow/reports/0515T002-business.md`
- `.workflow/reports/0515T002-qa.md`
- `.workflow/reports/0515T001-business.md`
- `.workflow/reports/0515T001-qa.md`
- `local_live_analysis/5-13-day-control-30min/stage6c_replay_lifecycle_mismatch_0515T001/**`
- `local_live_analysis/5-13-day-control-30min/stage6_execution_calibration_0514T007/**`
- `local_live_analysis/5-13-day-control-30min/audit_live_5-13-day-control-30min.csv`
- `local_live_analysis/5-13-day-control-30min/out/backtest_audit_replay/audit_bt_audit_replay.csv`
- `local_live_analysis/5-13-day-control-30min/t009_fixed_sidecar/**`

Forbidden files / areas：
- strategy behavior, fair/target formulas, configs, risk guards, quote placement behavior
- live scripts, AWS/remote state, live collection
- new replay generation / Stage 6J sweep / long experiment matrix beyond the required local regression rerun
- core Rust, connector, py `event_dtype`, standard npz schema
- sample-expansion tasks

Requires separate task：
- 新采集 current-format 样本。
- quote-adjustment promotion / strategy redesign。
- exact queue / MBO modeling。
- multi-sample expansion after repair。

Fatal Failures：
- 通过改策略逻辑而不是 replay lifecycle 逻辑来“修复” mismatch。
- repair 后不做 same-sample regression validation 就宣称问题解决。
- 扩展为 sample collection、quote redesign、live readiness 判断。

action：
- 实施 narrow replay lifecycle repair，优先：
  1. cancel-requested fill eligibility cutoff
  2. cancel-ack / canceled terminal-state semantics
  3. step-back placement 的 long-horizon persistence bias
- 在相同样本 `5-13-day-control-30min` 上做回归验证，至少重新生成：
  - Stage 6B calibration artifacts
  - Stage 6C mismatch diagnosis artifacts
  - a new repair regression summary under `stage6d_replay_lifecycle_repair_0515T003/`
- 回报必须对比修复前后：
  - `replay_only_fill_rows`
  - `live_cancel_replay_fill_rows`
  - `canceled -> filled` transition
  - fill-after-cancel gap
  - time-to-fill gaps
  - step-back / slow-latency hot spots

verify：
- `python -m pytest examples/binance_tick_mm/test_backtest_tick_mm.py`
- `python -m pytest examples/binance_tick_mm/test_replay_lifecycle_mismatch_diagnosis.py`
- 需要的 focused commands for local regression run
- `python3 .workflow/build_dashboard.py`
- `git diff --check`

done：
- 回报必须明确 repair 是否实质缩小了 core lifecycle mismatches。
- 回报必须明确是否还有残余 hot spots。
- 回报必须明确本任务未改策略、未补样本、未启动 live。
- 回报必须写 `commit id` 和 `提交信息`。
```
