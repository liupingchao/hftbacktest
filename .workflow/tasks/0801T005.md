# 任务派发

执行线程：
- 业务线程-python/cross-exchange-research

任务ID：
- 0801T005

标题：
- HYPERLIQUID-TEMPORARY-REGIME

简短描述：
- 按 `docs/hyperliquid_liquidity_response_case_hierarchy_plan.md` 执行
  Goal 4，基于 context windows 构建 TemporaryRegime，并在边界冻结后链接
  episodes/motifs。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- `0801T004` QA `已通过`。

规则更新提醒：
- 无

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `docs/hyperliquid_liquidity_response_case_hierarchy_plan.md`

是否需要提交代码：
- 不需要

提交要求：
- 不需要提交
- 回报写 `commit：无`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `examples/hyperliquid/cross_exchange_liquidity_response_case_hierarchy.py`
- `examples/hyperliquid/test_cross_exchange_liquidity_response_case_hierarchy.py`
- `local_live_analysis/skhynix_liquidity_response_case_hierarchy/`
- `.workflow/tasks/0801T005.md`
- `.workflow/reports/0801T005-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

action：
- 构建每段独立的 non-overlapping `1min` context windows。
- 用 discovery median/IQR 形成 context change score，候选边界必须经过
  surrogate max-score calibration。
- 只发布 segment mechanical boundaries 和 surrogate-significant data-driven
  boundaries；不显著候选留在 audit。
- 在 regime boundaries 冻结后，再链接 episode 和 motif membership。
- 输出 regime manifest、context windows、boundary audit、boundaries、
  surrogate summary、intervals、episode-regime membership、motif-by-regime 和
  transition summary。

verify：
- `python -m pytest examples/hyperliquid/test_cross_exchange_liquidity_response_case_hierarchy.py -q`
- `python examples/hyperliquid/cross_exchange_liquidity_response_case_hierarchy.py --help`
- 真实 Goal 4 builder。
- 独立脚本复核 boundaries、interval coverage、episode membership、motif
  linkage after freeze 和 output SHA。

done：
- TemporaryRegime artifacts published under `regime/`。
- 未通过 surrogate calibration 的 internal candidate 不进入
  `regime_intervals.csv`。
- 本轮不声称 permanent market ontology、trading signal、maker identity、exact
  fill 或 maker PnL。
