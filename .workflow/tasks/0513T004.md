```md
执行线程：
- 业务线程-python

任务ID：
- 0513T004

标题：
- Deployment reproducibility / startup compatibility gate

简短描述：
- 基于十步计划 Step 1，实现 Binance maker live 启动前的部署可复现与 schema/strategy 兼容性门禁。只做 preflight、manifest、run_live 启动前检查和 focused tests；不启动 live、不改策略行为、不改 AWS 状态。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- 0513T003

规则更新提醒：
- T003 已暴露远端 `audit_schema.py` 滞后会让 live 在 CSV 写入阶段失败。本任务必须把这类问题前移到启动前失败。
- 不允许将本任务解释为 live promotion。

按线程规则执行：
- .workflow/workflow-kit/thread-report-template.md
- docs/thread-playbook.md
- task_plan.md

是否需要提交代码：
- 不需要

提交要求：
- 不需要提交
- 回报必须写 `commit：无`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

Allowed files：
- `examples/binance_tick_mm/deploy/run_live.sh`
- `examples/binance_tick_mm/deploy/preflight_live_run.py`
- `examples/binance_tick_mm/test_deploy_preflight.py`

Allowed workflow / docs files：
- `.workflow/tasks/0513T004.md`
- `.workflow/reports/0513T004-business.md`
- `.workflow/reports/0513T004-qa.md`
- `docs/qa-acceptance-report.md`
- `task_plan.md`
- `progress.md`
- `findings.md`
- `.workflow/dashboard.html`
- `.workflow/dispatch_suggestions.md`

Forbidden files / areas：
- strategy rule logic, fair/target formula, risk guard behavior, quote placement behavior
- core `hbt.depth(0)` API, converter/npz schema, connector local book management
- AWS/remote deployment state
- live samples, archive tarballs, raw market-data artifacts

Fatal Failures：
- 启动 live 或采集新样本。
- 改变交易语义或默认策略参数。
- preflight 只记录信息但不能在 schema/strategy mismatch 时非零退出。
- manifest 缺少 deployed commit、config hash、audit schema hash 或启动兼容性检查结果。

action：
- 新增 live preflight 脚本，记录 git commit/branch/dirty status、config/connector/configured code file sha256、audit schema field count、schema/strategy 兼容性检查结果、symbol/data-dir/manifest path。
- preflight 必须用当前 `strategy_core.build_audit_row(...)` 生成代表性 decision row，并验证 row keys 与最终 `AUDIT_FIELDS` 完全一致，字段重复、缺失、额外字段均应失败。
- `run_live.sh` 在启动 tmux/collector/connector/live bot 之前执行 preflight；preflight 失败时直接退出，不启动 live。
- manifest 默认写到 run-local path，可用 `PREFLIGHT_MANIFEST` 覆盖；只要不启动 live，本地验证可写临时目录。
- 增加 focused tests 覆盖当前 schema 通过、缺字段失败、重复字段失败、manifest 输出。

verify：
- `python -m pytest examples/binance_tick_mm/test_deploy_preflight.py`
- `python examples/binance_tick_mm/deploy/preflight_live_run.py --help`
- `bash -n examples/binance_tick_mm/deploy/run_live.sh`
- `python -m py_compile examples/binance_tick_mm/deploy/preflight_live_run.py examples/binance_tick_mm/test_deploy_preflight.py`
- `python3 .workflow/build_dashboard.py`

done：
- 回报必须说明 preflight 写入哪些部署证据、如何阻止 stale schema、验证命令结果、未启动 live、未改策略语义。
- 回报必须明确 T004 只完成 Step 1 的本地启动门禁，不证明策略收益，不证明 market-view/full L2 对齐。
- 回报必须写 `commit：无`。
```
