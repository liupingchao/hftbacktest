```md
执行线程：
- 业务线程-live

任务ID：
- 0622T004

标题：
- M2 event-driven current-candidate maker submit repair

简短描述：
- 修复 `0622T003` 暴露的 batch-candidate half-life 问题：把 public L2/trades watcher 从固定窗口批处理候选改成 event-driven rolling current-candidate 评估，并在同一个远端进程里用同一份当前 L2/BBO/rolling flow state 完成 immediate guard 和 post-only `Alo` submit/cancel。
- 本任务目标不是放宽 guard，而是缩短 candidate event -> guard -> submit 路径；如果当前快照不再满足 queue/quote/age 条件，必须继续 fail-closed。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0622T003` business execution 为 `待验收`，其 artifacts 证明 controller pullback / separate live-window latency 已不是主要瓶颈，当前瓶颈是 public collection / candidate selection / immediate guard 路径内的 opportunity half-life。
- `0618T008` QA 为 `已通过`，T008 ledger/reconciler 必须继续作为 M2 PnL 事实源。
- `0619T002` QA 为 `已通过`，fresh-touch / dynamic-size / session-gate 策略合同仍然有效。

规则更新提醒：
- 请先阅读 `.workflow/workflow-kit/workflow-manual.md`、`.workflow/workflow-kit/task-dispatch-template.md`、`.workflow/workflow-kit/thread-report-template.md`、`.workflow/workflow-kit/qa-acceptance-template.md`、`task_plan.md`、`progress.md`、`findings.md` 后再执行本任务。

按线程规则执行：
- `AGENTS.md`
- `docs/hyperliquid_tiny_live_real_order_executor.md`
- `docs/hyperliquid_tiny_live_final_go_no_go_gate.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- `examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- `examples/hyperliquid/hyperliquid_tiny_live_m2_fill_window.py`
- `examples/hyperliquid/hyperliquid_tiny_live_m2_fill_loop.py`
- `examples/hyperliquid/test_hyperliquid_tiny_live_m2_public_watcher.py`
- `examples/hyperliquid/test_hyperliquid_tiny_live_m2_fill_loop.py`
- 可新增窄范围 focused tests，例如 `examples/hyperliquid/test_hyperliquid_tiny_live_m2_event_driven_watcher.py`
- `local_live_analysis/hyperliquid_tiny_live_m2_event_driven_current_candidate_0622T004/**`
- `.workflow/reports/0622T004-business.md`
- `task_plan.md`
- `progress.md`
- `findings.md`

read-only inputs：
- `.workflow/tasks/0622T003.md`
- `.workflow/reports/0622T003-business.md`
- `local_live_analysis/hyperliquid_tiny_live_m2_same_process_watcher_0622T003/**`
- `local_live_analysis/hyperliquid_tiny_live_m2_same_process_watcher_0622T003_rerun_short_iter/**`
- `local_live_analysis/hyperliquid_tiny_live_m2_pnl_ledger_0618T008/**`

hard boundary：
- Keep Hyperliquid `Alo` post-only. No taker/crossing, no `Ioc`, no one-tick-back workaround, no cap relaxation, no default-on behavior, no promotion, no stable PnL claim, and no M3 readiness claim.
- Dynamic order size hard cap remains `<=0.005 BTC`; smaller size is allowed and preferred when current rolling throughput is low.
- Live execution is limited to one event-driven triggered micro-window and at most `2` real order submissions.
- Waiting phase remains public-only until a current event-driven candidate triggers. Before trigger it must not read credentials, call private/account/order endpoints, submit orders, cancel orders, or start a continuous live bot.
- The candidate used for order intent must be generated from the current in-memory L2/BBO snapshot and current rolling trade-flow state. Do not submit using an older batch candidate, a pulled-back artifact candidate, or a candidate whose source snapshot is no longer the current touch.
- Immediate guard maximum age must be stricter than `0622T003`; target `candidate_event_to_guard_start <= 500ms` and hard fail-closed when selected current candidate age is `>1.0s`.
- If BBO changes between candidate evaluation and guard, re-evaluate from the new current snapshot or fail closed. Do not reuse the prior quote.
- Selected quote must still be current touch, post-only/non-crossing, size must still be `<=0.005 BTC`, queue-depth/order-count must still satisfy accepted `quality_a` / `quality_b` bands, and `Alo` must be the only TIF.
- Artifact writing before submit must not introduce blocking latency. Buffer pre-submit audit rows in memory and flush after no-submit, submit response, cancel/shutdown, or fail-closed boundary.
- Preserve tracked cancel, final open-orders proof, independent remote open-orders check, redaction, and T008 ledger fail-closed.
- Do not print or persist credential values, private keys, raw signatures, nonces, wallet secrets, or unredacted account identifiers.

implementation action：
- Add an event-driven mode to the `0622T003` same-process watcher path, for example `--event-driven-live`.
- Maintain an in-memory current L2/BBO state and rolling recent trade-flow state directly from Hyperliquid public WebSocket events.
- Re-evaluate fresh-touch / dynamic-size / session-gate eligibility immediately after relevant `l2Book` or `trades` events instead of waiting for a fixed `iteration_seconds` batch to finish.
- Generate exactly one current candidate context from the same current snapshot that immediate guard will verify. Include side, quote, size, quality bucket, same-side top depth/order count, rolling strict-through / at-or-through quantities, source event timestamp, local receive timestamp, and candidate age.
- Convert the immediate guard to consume the current event-driven candidate context and current in-memory L2/BBO state. It should either pass within the strict age/current-touch/queue limits or write a precise fail-closed reason.
- On guard pass only, perform private preflight and at most `2` post-only `Alo` submissions, then tracked cancel and final open-orders proof.
- If no current event-driven candidate appears during the timebox, emit a no-trigger report. If a candidate appears but guard blocks, emit a no-submit report with latency and current book facts.
- After pullback, always run T008 ledger. If no fill or incomplete economics/inventory evidence appears, T008 must fail closed.

required artifacts：
- `event_driven_watcher_manifest.json`
- `event_driven_latency_matrix.csv`
- `event_driven_trigger_decision_matrix.csv`
- `current_candidate_audit.csv`
- `rolling_flow_state.csv`
- `immediate_pre_submit_guard_matrix.csv`
- `order_intent_audit.csv`
- `quote_attempt_matrix.csv`
- `window_result_matrix.csv`
- `public_stream_summary.json`
- final gate manifest and remote facts
- tracked cancel / shutdown proof
- final remote open-orders proof
- independent remote open-orders check
- aggregate live fill ledger
- T008 ledger manifest/output
- `event_driven_no_submit_report.md` when trigger occurs but immediate guard blocks submission
- `event_driven_no_current_candidate_report.md` when no trigger occurs

live execution rules：
- Run focused local tests and CLI checks before any remote/live-capable step.
- Refresh `/home/admin/hftbacktest-cross-exchange` by git-safe incremental bundle / `merge --ff-only`; fail if branch is not `cross-exchange` or dirty count is nonzero.
- Rerun final gate; stop before watcher/live if `allow_create_0617T008 != true`.
- Run the event-driven watcher/live controller on `awsserver1` using the refreshed checkout and configured Python path.
- During watcher waiting, only public L2/trades collection is allowed.
- After trigger, submit at most `2` post-only `Alo` orders and only when the event-driven immediate guard passes.
- Always tracked-cancel live orders and prove final open orders are empty.
- Always run T008 ledger after pullback; no fill or incomplete economics/inventory evidence must return fail-closed.

expected outcomes：
- `已通过` is possible only if task implementation and QA criteria pass and either:
  - T008 proves live maker fill + fee/inventory/mark/realized PnL evidence; or
  - QA accepts that the event-driven path correctly produced complete no-current-candidate / stale-before-submit / guard-fail fail-closed evidence under the unchanged risk contract.
- M2 completion is possible only if T008 proves live maker fill plus complete fee/inventory/realized PnL evidence.
- If no current candidate appears, if immediate guard blocks, if no order fills, if final gate fails, if open-orders proof fails, or if T008 cannot prove realized live PnL, report `阻塞` for M2 and do not enter M3.

verify：
- `python -m pytest examples/hyperliquid/test_hyperliquid_tiny_live_m2_public_watcher.py -q`
- If an event-driven watcher test file is added: `python -m pytest examples/hyperliquid/test_hyperliquid_tiny_live_m2_event_driven_watcher.py -q`
- `python -m pytest examples/hyperliquid/test_hyperliquid_tiny_live_m2_fill_loop.py examples/hyperliquid/test_hyperliquid_tiny_live_m2_pnl_ledger.py -q`
- `python -m py_compile examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py examples/hyperliquid/hyperliquid_tiny_live_m2_fill_window.py examples/hyperliquid/hyperliquid_tiny_live_m2_fill_loop.py`
- `python examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py --help`
- `python examples/hyperliquid/hyperliquid_tiny_live_m2_fill_window.py --help`
- `python examples/hyperliquid/hyperliquid_tiny_live_m2_fill_loop.py --help`
- `git diff --check`
- If watcher/live reaches remote execution: remote refresh evidence, final gate go/no-go, event-driven watcher/live artifacts, pulled-back artifacts, independent remote open-orders check, and T008 ledger output.

done：
- 回报 event runtime、public message counts、event-driven evaluation count、current candidate count、trigger count、candidate_event_to_guard_start latency、guard-to-private-preflight latency、private-preflight-to-submit latency、immediate guard status/reason、live submissions 数、fill_count / maker_fill_count、T008 ledger 结论、artifact 路径、blockers、commit id、提交信息。
- 明确说明 M2 是否仍 blocked；除非 T008 ledger 证明 live fill / fee / inventory / realized PnL，否则不得声明 M2 完成、稳定 PnL、或 M3 ready。
```
