# 任务派发

执行线程：
- 业务线程-python/public-data-infra + cross-exchange-research

任务ID：
- 0804T001

标题：
- CONTINUOUS-COLLECTION-WARMUP-ASOF-ALIGNMENT-REPAIR

简短描述：
- 修复 segment 启动阶段 Binance bookTicker 早于可重建 timeline 时被错误
  计入 reconciliation failure 的问题，并修复 future label 依赖目标时刻后
  必须出现新 BBO 的 coverage 缺口。
- 完成后立即在 `c6in-winner` 进行一次用户已授权的 2H SKHYNIX public
  collection；云端只采集，拉回本地后完成全部 postprocess/R0/R1 验证。

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- `0803T001` 提供失败样本与根因证据。
- `0803T002` QA 已通过并收口，不与本任务并行写入。

规则更新提醒：
- 无

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `AGENTS.md`

是否需要提交代码：
- 不需要

提交要求：
- 当前工作区存在多轮未提交前置工作，不创建混合提交。
- 回报写 `commit：无`。

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 独立复核 warmup、as-of label、continuous collection、source SHA、
  local-only postprocess 和新 2H 数据的 R1 结果。

files：
- `examples/hyperliquid/cross_exchange_collection_supervisor.py`
- `examples/hyperliquid/cross_exchange_alignment_acceptance.py`
- 对应 focused tests。
- 新 2H remote raw campaign、local pullback、R0/R1 evidence。

action：
- 增加 continuous collection 模式；本次 2H 以单一 `7200s` segment
  采集，避免每 30 分钟重新建立 snapshot/socket。
- segment ready 之前的 bookTicker decision 仍保留在 label audit 中，但
  明确标记 warmup，不计入 reconciliation missing。
- primary outcome 改为 target-time strict historical as-of BBO state；
  source age 保留为诊断字段，first-after-target state 也仅作为诊断。
- 目标时刻状态的有效性由单段采集连续性、reconnect/degraded interval、
  source provenance 和 segment boundary 门禁保证，不把“期间未变价”
  错误解释为数据缺失。
- 不允许 future join、无限 forward-fill、跨 segment 标签或放宽 stale
  门禁。
- 云端不运行 timeline/R0/R1。完成采集后拉回本地，使用本机 conda
  环境完成 postprocess、R0 和 R1。

verify：
- focused pytest、py_compile、CLI help、`git diff --check`。
- 旧 5H 数据本地重放应消除 startup missing-asof 假失败，并报告新的
  as-of coverage。
- 新 2H raw source SHA/gzip/row reconciliation。
- 新 2H 本地 campaign postprocess、R0、R1；重点检查 reconciliation、
  accepted primary horizon、future join、source age 和 exact masks。

done：
- 修复代码和测试完成。
- 2H raw collection 完成并拉回本地。
- 本地验证给出修复是否有效的明确结论。

hard boundary：
- 仅 public market data；不访问 credential/private/account/order/cancel
  endpoint，不下单。
- 用户已在 2026-08-04 明确授权立即开始本次 2H 采集。
- 云端只采集，不执行 timeline/R0/R1 或研究统计计算。
- 不删除、覆盖或重写 8 月 3 日历史 campaign/R0/R1。
