# 0718T019 P2-EVENT-TIME-ESTIMATORS-DYNAMIC-SPREAD-OBSERVE-ONLY

执行线程：
- 总控 auto-loop / 业务实现线程

任务ID：
- 0718T019

标题：
- P2 event-time buckets、online estimators 与 dynamic-spread observe-only

简短描述：
- Implement Principal Alignment Task 8 observe-only phase: deterministic 1s event-time market-state buckets, side-aware volatility/liquidity/toxicity/intensity evidence, and a bounded dynamic half-spread candidate.
- Record estimator and candidate outputs in public/live artifacts without changing Task 7 fixed-spread quotes or placing orders because of the candidate.

状态：
- 已通过

执行顺序：
- 当前唯一任务

前置任务：
- 0718T018

规则更新提醒：
- T018 QA accepted the watcher/manager/status/live gate implementation but observed zero real submissions.
- Task 8 observe-only may proceed; dynamic-spread quote activation, fill feedback, multi-level and promotion remain unauthorized in this task.

按线程规则执行：
- `.workflow/workflow-kit/workflow-manual.md`
- `docs/superpowers/plans/2026-07-17-principal-alignment-p0-p3.md`

是否需要提交代码：
- 需要

提交要求：
- 自测通过后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

files：
- Create: `examples/hyperliquid/cross_exchange_online_estimators.py`
- Modify: `examples/hyperliquid/cross_exchange_shared_signal_kernel.py`
- Modify: `examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- Test: `examples/hyperliquid/test_cross_exchange_online_estimators.py`
- Related watcher/kernel tests only as required

action：
- 建立固定 1s event-time buckets；同一状态在同一 bucket 内不按 websocket 消息数重复加权。
- 拒绝或隔离 out-of-order/future events，保留明确 audit reason。
- 输出 realized volatility、spread/depth/liquidity、adverse-flow/toxicity、trade sweep/depth penetration 和 quote exposure interval evidence。
- intensity 使用成交前 L2、trade direction、sweep penetration、side quote depth/resting exposure；不得用 `abs(trade_px-mid)` 直接替代 arrival intensity。
- 拟合 side-specific `A/k`，记录 observation count、有效 bucket、fit error/confidence；insufficient/invalid 时 dynamic output unavailable。
- 新增 bounded/rate-limited `compute_dynamic_half_spread()` candidate，使用 volatility、A、k、risk aversion、inventory ratio、liquidity/toxicity；invalid/cold-start 回退 fixed base。
- 在 watcher/public live 中 observe-only 写入 estimator/candidate artifacts 和 status fields；actual desired/final quotes 继续使用 Task 7 fixed half-spread。
- focused replay 对同一 event rows 复现 bucket/estimator/candidate；不要求 queue-fill prediction。
- 不启用 dynamic spread，不调用 order/cancel，不改变 size/submission/position envelope。

verify：
- deterministic bucket dedupe、out-of-order/future quarantine、cold-start fallback、volatility/liquidity/toxicity、side-specific A/k、bounds/rate-limit tests。
- shared-kernel and watcher focused regression。
- public-only short observe-only run records estimator/candidate with credential/private/order/cancel false。
- replay produces identical bucket/candidate output for the recorded deterministic input.
- modified Python `py_compile`、CLI help、`git diff --check`。

done：
- 新增固定 1s event-time bucket estimator、state dedupe、out-of-order/future quarantine、realized volatility、spread/depth/liquidity、directional toxicity、sweep/depth penetration、quote-intent exposure interval 与 side-specific A/k fit。
- A/k 输出 observation count、effective bucket count、fit RMSE、confidence 与近似 95% confidence bounds；A、k、volatility、liquidity、toxicity、inventory 均进入 bounded dynamic-spread candidate。
- cold/invalid estimator 固定回退 `0.5 tick`；candidate 使用 hard min/max 和最大变化速率，始终 `observe_only`。
- watcher/status/live artifact 写入 estimator event rows、bucket matrix、quarantine、quote exposure、intensity fit、core snapshot 和 fixed-quote pricing overlay；actual quote path 未读取 candidate。
- 远端 public-only window：`60.058079s`、`146` evaluations、`113` L2 events、`161` trades、无 reconnect/disconnect、quarantine `0`；无 credential/private/account/order/cancel endpoint。
- live artifact terminal checksum `22/22` remote/local pass；public shadow `0` would-submit，A/k 因无 quote-exposure lifecycle 保持 unavailable，dynamic candidate 按设计回退固定 `0.5 tick`。
- recorded `274` event rows 的离线 replay snapshot hash 与原始 core snapshot 完全一致；replay manifest `snapshot_match=true`。
- 不改变 Task 7 fixed spread、size、submission、position envelope；不启用 dynamic spread、fill feedback、multi-level 或 promotion。

blockers：
- 没有真实 resting quote exposure，因此当前 A/k 不能形成 live calibration；dynamic-spread activation 仍需单独 formal task 和新的 lifecycle evidence gate。

commit：
- `cd513f6`

提交信息：
- `Add observe-only event-time estimators`
