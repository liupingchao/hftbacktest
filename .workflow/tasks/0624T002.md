```md
执行线程：
- 业务线程-live-awsserver1

任务ID：
- 0624T002

标题：
- M2 public BBO evidence-chain repair + public-only validation

简短描述：
- 基于 `0624T001` 的诊断结论，修复 public-shadow 路径的 BBO history/cache 可见性、fresh-touch block reason、queue-reset same-touch 证据、event ordering / local visibility 诊断，并用 T010 replay 与 AWS public-only no-submit rerun 验证。
- 本任务只修证据链和可解释性，不放松 quote distance、cap、post-only、fresh-touch / queue-reset 放行条件，也不授权真实 canary。

状态：
- 待验收

执行顺序：
- 当前唯一任务

前置任务：
- `0624T001` business execution complete; execute only after `0624T001` QA passes or total control explicitly confirms this prepared task may proceed.

规则更新提醒：
- 请先阅读 `.workflow/workflow-kit/workflow-manual.md`、`.workflow/workflow-kit/task-dispatch-template.md`、`.workflow/workflow-kit/thread-report-template.md`、`.workflow/workflow-kit/qa-acceptance-template.md`、`task_plan.md`、`progress.md`、`findings.md` 后再执行本任务。

按线程规则执行：
- `AGENTS.md`

是否需要提交代码：
- 需要

提交要求：
- 自测 / 远端 public-only no-submit 验证证据生成后提交
- 回报必须带 `commit id` 和 `提交信息`

是否进行QA验收：
- 是

QA参与：
- 是

QA验收方式：
- 正常验收

QA验收线程动作：
- 正常验收

host / execution location：
- Local implementation and T010 replay validation are allowed.
- Fresh public validation, if needed, must run on `awsserver1`.
- Remote repo: `/home/admin/hftbacktest-cross-exchange`
- Preferred remote interpreter: `/home/admin/.venvs/hyperliquid-sdk-0618T002/bin/python`

files：
- `.workflow/tasks/0624T002.md`
- `.workflow/reports/0624T002-business.md`
- `examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- `examples/hyperliquid/test_hyperliquid_tiny_live_m2_event_driven_watcher.py`
- `local_live_analysis/hyperliquid_tiny_live_m2_bbo_evidence_chain_repair_0624T002/**`
- `task_plan.md`
- `progress.md`
- `findings.md`

read-only inputs：
- `.workflow/tasks/0624T001.md`
- `.workflow/reports/0624T001-business.md`
- `local_live_analysis/hyperliquid_tiny_live_m2_aws_bbo_evidence_chain_0624T001/**`
- `.workflow/tasks/0623T010.md`
- `.workflow/reports/0623T010-business.md`
- `local_live_analysis/hyperliquid_tiny_live_m2_aws_candidate_funnel_0623T010_20260623T064432Z/**`

hard boundary：
- No live orders, no credential reads, no private/account/order endpoints, no real order endpoint, no cancel endpoint, no live client initialization, no remote final gate rerun, no T008 live ledger claim, no quote-distance change, no one-tick-back, no inside-spread, no cap relaxation, no taker/crossing, no default-on behavior, no M3/stable PnL/promotion.
- Public live source use is allowed only for Hyperliquid public L2/trades and Binance public market data.
- Do not weaken accepted fresh-touch / queue-reset requirements. `synthetic_current_event_only` must remain fail-closed and must not be converted into a pass.
- The task may improve BBO history retention, decision-time visibility accounting, reason taxonomy, and diagnostics; it must not fabricate BBO history or queue reset evidence.

action：
- Repair BBO history/cache evidence:
  - Maintain decision-time-visible BBO history with enough metadata to explain each candidate.
  - Record `bbo_history_count`, `bbo_history_span_ms`, `last_l2_age_ms`, `same_touch_bbo_count`, and whether the visible BBO history is sufficient, sparse, stale, or missing.
  - Separate local receive ordering from exchange timestamp ordering.
- Repair fresh-touch block reason taxonomy:
  - Split `synthetic_current_event_only` / `no_bbo_history` / `bbo_history_too_sparse` / `last_l2_too_old` / `same_touch_seen_but_not_stable` / `same_touch_stable_enough`.
  - Preserve fail-closed behavior unless accepted real BBO-history touch stability or accepted same-touch queue reset is proven.
- Repair queue-reset detector evidence:
  - For same-touch history, record previous/current top qty and previous/current order count.
  - Emit `previous_top_qty`, `current_top_qty`, `reset_qty_delta`, `previous_order_count`, `current_order_count`, and `reset_order_count_delta`.
  - Separate `no_prior_same_touch_bbo`, `history_present_no_reset`, and `reset_supported`.
- Repair event ordering / local visibility diagnostics:
  - For each candidate, record previous/next l2Book and trade event sequence/time context.
  - Emit `local_receive_ordering_status` and `exchange_time_ordering_status`.
  - Treat local receive order as the live decision-time visibility boundary; exchange timestamp conflicts must be diagnosed, not used to retroactively pass candidates.
- Validate with:
  - T010 replay artifacts.
  - A fresh AWS public-only no-submit rerun if needed to prove the repaired fields are populated in live public mode.

acceptance criteria：
- Rejected candidates have explicit evidence-chain fields:
  - `bbo_history_count`
  - `bbo_history_span_ms`
  - `last_l2_age_ms`
  - `same_touch_bbo_count`
  - `touch_stability_ms`
  - `previous_top_qty`
  - `current_top_qty`
  - `reset_qty_delta`
  - `previous_order_count`
  - `current_order_count`
  - `reset_order_count_delta`
  - `local_receive_ordering_status`
  - `exchange_time_ordering_status`
  - explicit fresh-touch / queue-reset block reason
- `synthetic_current_event_only` remains fail-closed.
- The output distinguishes:
  - missing BBO history
  - sparse BBO history
  - stale latest l2Book
  - same-touch history present but not stable
  - same-touch history present but no reset
  - accepted same-touch stability
  - accepted same-touch reset
- Public-only validation produces a manifest explaining whether the remaining dominant blocker is feed density, cache visibility, event ordering, detector logic, or truly rare market conditions.

verify：
- `python -m pytest examples/hyperliquid/test_hyperliquid_tiny_live_m2_event_driven_watcher.py -q`
- `python -m py_compile examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py`
- `python examples/hyperliquid/hyperliquid_tiny_live_m2_public_watcher.py --help`
- T010 replay diagnosis / validation using the repaired evidence fields
- If fresh data is needed: AWS public-only no-submit validation on `awsserver1` with `/home/admin/.venvs/hyperliquid-sdk-0618T002/bin/python`
- Validate JSON/CSV outputs, required fields, non-empty artifacts, and reason histograms
- `git diff --check`

done：
- 回报是否改代码、artifact path、T010 replay结果、AWS public-only rerun结果或未 rerun 的理由、BBO history/cache 字段覆盖率、fresh-touch reason taxonomy 分布、queue-reset detector 分布、event ordering/local visibility 分布、dominant blocker after repair、是否仍有 `synthetic_current_event_only` 高占比及原因。
- 明确说明本任务不授权真实下单、读凭据、private/account/order/cancel endpoint、remote final gate、T008 live ledger claim、quote-distance change、one-tick-back、inside-spread、cap relaxation、M3/stable PnL/default-on/promotion。
- 回报 commit id 和提交信息。
```
